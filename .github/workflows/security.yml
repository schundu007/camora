name: Security Scan

on:
  schedule:
    - cron: '0 2 * * 1'   # Every Monday 02:00 UTC
  push:
    branches: [main]
    paths:
      - '**/package.json'
      - '**/requirements.txt'
      - '**/Dockerfile'
      - 'pnpm-lock.yaml'
  workflow_dispatch:

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── Node.js audit ─────────────────────────────────────────────────────────
  audit-node:
    name: Node.js dependency audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 9.15.0

      - uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: pnpm audit (high + critical)
        run: pnpm audit --audit-level=high

      - name: Audit lumora-backend
        working-directory: apps/lumora-backend
        run: |
          npm audit --audit-level=high --json > /tmp/audit-lumora.json
          python3 -c "import json; d=json.load(open('/tmp/audit-lumora.json')); v=d.get('metadata',{}).get('vulnerabilities',{}); print('lumora-backend:',v.get('high',0)+v.get('critical',0),'high/critical')"

      - name: Audit ascend-backend
        working-directory: apps/ascend-backend
        run: |
          npm audit --audit-level=high --json > /tmp/audit-ascend.json
          python3 -c "import json; d=json.load(open('/tmp/audit-ascend.json')); v=d.get('metadata',{}).get('vulnerabilities',{}); print('ascend-backend:',v.get('high',0)+v.get('critical',0),'high/critical')"

  # ── Python audit ──────────────────────────────────────────────────────────
  audit-python:
    name: Python dependency audit (ai-services)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - name: Install pip-audit
        run: pip install pip-audit

      - name: Audit ai-services
        working-directory: apps/ai-services
        run: pip-audit -r requirements.txt --format markdown

  # ── Secret detection ──────────────────────────────────────────────────────
  scan-secrets:
    name: Secret detection (Gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  # ── CodeQL static analysis ────────────────────────────────────────────────
  codeql:
    name: CodeQL (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read
    strategy:
      fail-fast: false
      matrix:
        language: [javascript-typescript, python]
    steps:
      - uses: actions/checkout@v7

      - uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          queries: security-and-quality

      - uses: github/codeql-action/autobuild@v4

      - uses: github/codeql-action/analyze@v4
        with:
          category: codeql-${{ matrix.language }}
