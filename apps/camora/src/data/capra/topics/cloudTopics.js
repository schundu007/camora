// Cloud / AWS category — interview prep for cloud engineers.
// Content sourced from AWS documentation, AceCloudInterviews.com,
// AWS Well-Architected Framework, and AWS whitepapers.
// Diagrams generated at /diagrams/cloud/*.png

export const cloudCategories = [
  { id: 'compute',     name: 'Compute',                    icon: 'cpu',        color: '#f97316' },
  { id: 'storage',     name: 'Storage',                    icon: 'database',   color: '#3b82f6' },
  { id: 'networking',  name: 'Networking & CDN',           icon: 'globe',      color: '#06b6d4' },
  { id: 'databases',   name: 'Databases',                  icon: 'server',     color: '#8b5cf6' },
  { id: 'security',    name: 'Security & Identity',        icon: 'shield',     color: '#ef4444' },
  { id: 'serverless',  name: 'Serverless & Messaging',     icon: 'zap',        color: '#f59e0b' },
  { id: 'containers',  name: 'Containers & Orchestration', icon: 'package',    color: '#ec4899' },
  { id: 'monitoring',  name: 'Monitoring & Observability', icon: 'activity',   color: '#22c55e' },
  { id: 'aiml',        name: 'AI / ML Services',           icon: 'cpu',        color: '#a855f7' },
  { id: 'devtools',    name: 'Developer Tools & IaC',      icon: 'tool',       color: '#14b8a6' },
  { id: 'migration',   name: 'Migration & Transfer',       icon: 'send',       color: '#6366f1' },
];

export const cloudTopicCategoryMap = {
  // Compute
  'aws-ec2':                  'compute',
  'aws-ec2-advanced':         'compute',
  'aws-lambda':               'compute',
  'aws-lambda-advanced':      'compute',
  'aws-ecs':                  'compute',
  'aws-eks':                  'compute',
  'aws-fargate':              'compute',
  'aws-batch':                'compute',
  'aws-auto-scaling':         'compute',
  'aws-elastic-beanstalk':    'compute',
  'aws-lightsail':            'compute',
  // Storage
  'aws-s3':                   'storage',
  'aws-s3-advanced':          'storage',
  'aws-ebs':                  'storage',
  'aws-efs':                  'storage',
  'aws-fsx':                  'storage',
  'aws-s3-glacier':           'storage',
  'aws-storage-gateway':      'storage',
  'aws-backup':               'storage',
  // Networking
  'aws-vpc':                  'networking',
  'aws-vpc-advanced':         'networking',
  'aws-cloudfront':           'networking',
  'aws-route53':              'networking',
  'aws-elb':                  'networking',
  'aws-api-gateway':          'networking',
  'aws-direct-connect':       'networking',
  'aws-transit-gateway':      'networking',
  'aws-privatelink':          'networking',
  'aws-global-accelerator':   'networking',
  // Databases
  'aws-rds':                  'databases',
  'aws-aurora':               'databases',
  'aws-dynamodb':             'databases',
  'aws-dynamodb-advanced':    'databases',
  'aws-elasticache':          'databases',
  'aws-redshift':             'databases',
  'aws-documentdb':           'databases',
  'aws-neptune':              'databases',
  'aws-keyspaces':            'databases',
  'aws-timestream':           'databases',
  // Security
  'aws-iam':                  'security',
  'aws-iam-advanced':         'security',
  'aws-cognito':              'security',
  'aws-kms':                  'security',
  'aws-secrets-manager':      'security',
  'aws-waf':                  'security',
  'aws-shield':               'security',
  'aws-guardduty':            'security',
  'aws-security-hub':         'security',
  'aws-inspector':            'security',
  'aws-macie':                'security',
  'aws-certificate-manager':  'security',
  // Serverless & Messaging
  'aws-lambda-patterns':      'serverless',
  'aws-step-functions':       'serverless',
  'aws-eventbridge':          'serverless',
  'aws-sqs':                  'serverless',
  'aws-sns':                  'serverless',
  'aws-kinesis':              'serverless',
  'aws-sam':                  'serverless',
  'aws-app-runner':           'serverless',
  // Containers
  'aws-ecs-deep-dive':        'containers',
  'aws-eks-deep-dive':        'containers',
  'aws-ecr':                  'containers',
  'aws-fargate-patterns':     'containers',
  'aws-copilot':              'containers',
  // Monitoring
  'aws-cloudwatch':           'monitoring',
  'aws-cloudtrail':           'monitoring',
  'aws-xray':                 'monitoring',
  'aws-config':               'monitoring',
  'aws-trusted-advisor':      'monitoring',
  'aws-health':               'monitoring',
  // AI/ML
  'aws-sagemaker':            'aiml',
  'aws-bedrock':              'aiml',
  'aws-rekognition':          'aiml',
  'aws-comprehend':           'aiml',
  'aws-textract':             'aiml',
  'aws-transcribe':           'aiml',
  'aws-polly':                'aiml',
  'aws-lex':                  'aiml',
  'aws-translate':            'aiml',
  // Developer Tools & IaC
  'aws-codepipeline':         'devtools',
  'aws-codebuild':            'devtools',
  'aws-codedeploy':           'devtools',
  'aws-codecommit':           'devtools',
  'aws-cloudformation':       'devtools',
  'aws-cdk':                  'devtools',
  'aws-cloud9':               'devtools',
  // Migration
  'aws-migration-hub':        'migration',
  'aws-dms':                  'migration',
  'aws-snow-family':          'migration',
  'aws-transfer-family':      'migration',
  'aws-datasync':             'migration',
  'aws-application-migration':'migration',
  // Compute (new)
  'iaas-paas-saas':            'compute',
  'aws-architecture-3tier':    'compute',
  // Containers (new)
  'aws-ecs-vs-eks':            'containers',
};

export const cloudTopics = [
{
  id: 'aws-ec2',
  title: 'EC2 — Elastic Compute Cloud',
  icon: 'server',
  color: '#f97316',
  description: 'Virtual servers in the cloud — the foundation of AWS compute.',
  introduction: '**Amazon EC2** provides resizable virtual machines running on AWS infrastructure. You choose the CPU, memory, storage, and networking capacity you need, launch instances in minutes, and pay only for what you use. **EC2** is the workhorse behind most cloud architectures, giving teams full OS-level control over their environment.',
  whenToUse: 'Use EC2 when you need full OS-level control, custom runtime environments, persistent long-running processes, or workloads that do not fit a managed service. It is the right choice for lift-and-shift migrations, stateful applications, and workloads requiring specific kernel versions or hardware features like GPU or local NVMe storage.',
  keyConcepts: [
    { title: 'Instance Families', description: '**General purpose** (`t3`, `m6i`) balance compute/memory/network for web apps and small databases. **Compute-optimized** (`c6i`, `c7g`) target CPU-intensive batch and HPC. **Memory-optimized** (`r6i`, `x2idn`) serve in-memory databases and real-time analytics. **Storage-optimized** (`i3`, `i4i`) provide high IOPS local NVMe for NoSQL and data warehousing.' },
    { title: 'Amazon Machine Image (AMI)', description: 'An **AMI** is a snapshot of an OS configuration, software, and data volumes used to launch instances. **AWS** provides public AMIs for Amazon Linux, Ubuntu, and Windows. Custom AMIs baked with your application reduce boot time, enforce security baselines, and enable immutable infrastructure patterns.' },
    { title: 'Purchasing Options', description: '**On-Demand** charges per second with no commitment. **Reserved Instances** offer 30-60% savings for 1-3 year commitments. **Spot Instances** bid on spare capacity at up to 90% discount but can be interrupted with 2-minute notice. **Savings Plans** provide flexible discounts tied to compute spend rather than specific instance types.' },
    { title: 'Networking and Security Groups', description: 'Instances live in a **VPC** subnet (public or private). **Security groups** act as stateful virtual firewalls controlling inbound and outbound traffic at the instance level. **Elastic IPs** provide static public IPv4 addresses. Enhanced networking (`ENA`) delivers up to 100 Gbps bandwidth on supported instance types.' },
  ],
  approach: [
    { step: 'Choose instance family and size', details: 'Profile the workload CPU, memory, and I/O requirements. Start with a general-purpose m6i for unknown workloads, then monitor CloudWatch metrics for CPU and memory utilization. Right-size after 2 weeks of production data before committing to Reserved Instances.' },
    { step: 'Design for high availability', details: 'Always spread instances across at least two Availability Zones. Use an Application Load Balancer to distribute traffic. Configure Auto Scaling Groups so the fleet self-heals on instance failure. Avoid single-instance architectures for any customer-facing workload.' },
    { step: 'Build a patched, minimal AMI', details: 'Create a hardened base AMI with the OS patched, agent software (SSM, CloudWatch) installed, and unnecessary services disabled. Use EC2 Image Builder to automate AMI pipelines. Tag AMIs with pipeline version and expiry date. Never deploy from a stale AMI that predates the last critical patch.' },
    { step: 'Implement least-privilege IAM roles', details: 'Attach an IAM instance profile instead of embedding credentials. Grant only the specific S3 buckets, Secrets Manager paths, and service APIs the application needs. Rotate instance profiles via rolling replacement rather than in-place credential injection.' },
  ],
  pitfalls: [
    { title: 'Over-provisioning instance size', description: 'Teams frequently pick too large an instance based on peak estimates. Use CloudWatch CPU and memory metrics alongside Compute Optimizer recommendations to right-size. A t3.medium often handles traffic that teams initially provision on an m5.xlarge.' },
    { title: 'Treating instances as pets', description: 'Manually SSH-patching or configuring running instances creates configuration drift and makes scaling unreliable. Use AMI-based immutable deployments, terminate old instances, and manage configuration through user data scripts or Systems Manager rather than interactive shell access.' },
    { title: 'Public subnet placement for backend services', description: 'Backend databases and application servers placed in public subnets expose unnecessary attack surface even with restrictive security groups. Always place non-internet-facing tiers in private subnets and route outbound traffic through NAT Gateways.' },
  ],
  keyQuestions: [
    { q: 'How do you choose between On-Demand, Reserved, and Spot instances?', a: '- **On-Demand** suits unpredictable traffic patterns and dev/test environments where flexibility outweighs cost.\n- **Reserved Instances** (Standard or Convertible) suit steady-state production workloads running over 60% of the month, saving 30-60% over 1-3 year terms.\n- **Spot Instances** suit fault-tolerant, stateless batch processing, CI runners, and ML training jobs where a 2-minute interruption is acceptable — at up to 90% discount.\n\nMost production architectures mix all three: Reserved for the baseline, On-Demand for moderate spikes, and Spot for burst capacity.' },
    { q: 'How do you achieve zero-downtime EC2 deployments?', a: 'Use an **Auto Scaling Group** behind an **Application Load Balancer** with a rolling update or blue/green deployment strategy.\n\n- **Rolling update**: ASG replaces a configurable percentage of instances at a time, waiting for new instances to pass ELB health checks before terminating old ones.\n- **Blue/green**: Creates a second ASG with the new version, shifts traffic via weighted target group rules, and tears down the old fleet after validation.\n\n**CodeDeploy** integrates directly with both patterns and supports automatic rollback on alarm threshold breach.' },
    { q: 'What is the difference between stopping and terminating an EC2 instance?', a: '- **Stopping** an EBS-backed instance preserves the root volume and its data; the instance retains its private IP, instance ID, and attached volumes, and billing pauses for compute (storage continues).\n- **Terminating** permanently deletes the instance and, by default, the root EBS volume (controlled by the `DeleteOnTermination` flag).\n\nInstance store-backed instances cannot be stopped — only terminated or rebooted, making them unsuitable for stateful workloads.' },
    { q: 'How do you debug a high-CPU spike on a production EC2 fleet?', a: '1. Check the **CloudWatch** `CPUUtilization` metric across the ASG to confirm it is fleet-wide versus isolated.\n2. Use **EC2 Systems Manager Session Manager** to connect to a hot instance without opening SSH ports and run `top` or `perf` to identify the offending process.\n3. If the spike correlates with a recent deployment, trigger a rollback via **CodeDeploy**.\n4. Set a **CloudWatch alarm** on `CPUUtilization` above 80% for 5 minutes to page the on-call engineer automatically.\n5. Configure **Compute Optimizer** to surface right-sizing recommendations once normal patterns resume.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/',
    'https://aws.amazon.com/ec2/instance-types/',
    'https://docs.aws.amazon.com/autoscaling/ec2/userguide/',
  ],
  visualizations: [
    { title: 'EC2 Instance Lifecycle', caption: 'All EC2 instance states from pending through running, stopped, and terminated, with valid transitions and billing implications at each state.', image: '/diagrams/cloud/aws-ec2-lifecycle.png' },
    { title: 'EC2 Purchasing Options', caption: 'Decision map for On-Demand, Reserved Instances, Savings Plans, Spot, and Dedicated Hosts — which workload profile fits each pricing model.', image: '/diagrams/cloud/aws-ec2-purchasing.png' },
  ],
},

{
  id: 'aws-ec2-advanced',
  title: 'EC2 Advanced — Placement, Networking, and Spot',
  icon: 'cpu',
  color: '#f97316',
  description: 'Deep-dive into placement groups, Spot fleets, enhanced networking, and Nitro.',
  introduction: 'Once you master basic EC2 operations, advanced topics become critical for high-performance computing, cost optimization, and building resilient fleets. This covers **placement groups** for latency-sensitive workloads, **Spot Fleet** and **EC2 Fleet** strategies, the **Nitro** hypervisor architecture, **Enhanced Networking**, and advanced **Auto Scaling lifecycle hooks**.',
  whenToUse: 'Apply advanced EC2 patterns when running HPC or ML training clusters needing low-latency instance-to-instance networking, running large Spot fleets where interruption handling and diversification matter, or when cost/performance tuning requires understanding of the Nitro System and ENA networking capabilities.',
  keyConcepts: [
    { title: 'Placement Groups', description: '**Cluster** placement groups pack instances onto low-latency 10 Gbps or 100 Gbps bisectional bandwidth hardware in a single AZ — ideal for MPI workloads and ML training. **Spread** placement groups distribute instances across distinct underlying hardware racks to minimize correlated failure risk for critical VMs. **Partition** placement groups divide instances into logical partitions sharing no hardware, useful for Hadoop and Cassandra clusters where rack-awareness matters.' },
    { title: 'Nitro System', description: 'The **AWS Nitro System** offloads VPC networking, EBS I/O, and security functions to dedicated hardware and software, allowing nearly all host resources to be allocated to the guest VM. Nitro-based instances (`C5`, `M5`, `R5` and newer) achieve near-bare-metal performance. **Nitro Enclaves** provide isolated compute environments for cryptographic key processing, running as stripped-down VMs with no persistent storage, no interactive access, and no network access beyond a local `vsock`.' },
    { title: 'Spot Fleets and EC2 Fleet', description: '**Spot Fleet** maintains a target capacity by automatically diversifying across multiple instance types and AZs. **EC2 Fleet** extends this to mix On-Demand and Spot in configurable ratios. The allocation strategy options are `lowest-price` (maximize savings), `diversified` (minimize interruption), and `capacity-optimized` (pick the pool with most available capacity — recommended for critical Spot workloads). Spot Interruption Notices arrive via instance metadata at `/latest/meta-data/spot/termination-time` and through **EventBridge** 2 minutes before reclamation.' },
    { title: 'Enhanced Networking and ENA', description: '**Elastic Network Adapter** (`ENA`) provides up to 100 Gbps bandwidth, lower CPU overhead via SR-IOV, and consistent single-digit microsecond latency between instances. **Elastic Fabric Adapter** (`EFA`) adds HPC-grade RDMA-like OS-bypass networking on top of ENA, bypassing the OS kernel network stack for MPI collective operations.' },
  ],
  approach: [
    { step: 'Select placement strategy for the workload', details: 'For tightly-coupled distributed training or HPC simulations, use a Cluster placement group — all nodes land on the same high-bandwidth switch. For stateful services where a host failure should not take down more than one instance, use Spread. For Cassandra or HDFS where rack-awareness is encoded in the application topology, use Partition and map each partition to an application rack label.' },
    { step: 'Build a Spot-resilient architecture', details: 'Always use at least 3 instance families in the Spot Fleet to reduce correlated interruption. Set allocation strategy to capacity-optimized. Implement a SIGTERM handler in the application to checkpoint state on interruption. Store intermediate results in S3 or EFS so a replacement instance can resume rather than restart. Use Spot Hibernation (supported on certain instance types) for stateful workloads.' },
    { step: 'Tune ENA for network-intensive workloads', details: 'Verify ENA module version with modinfo ena and update via the latest Amazon Linux AMI or the ENA Linux driver. Enable jumbo frames (MTU 9001) within the VPC to increase throughput for large data transfers. For HPC, switch to EFA-capable instances (c5n, p4d) and compile MPI with the EFA provider.' },
    { step: 'Use Lifecycle Hooks for graceful operations', details: 'ASG Lifecycle Hooks pause instance launch or termination in a Wait state for up to 2 hours. On scale-out, use a launch hook to register the instance with a service mesh or warm the local cache before the instance receives traffic. On scale-in, use a termination hook to drain connections, flush buffers to S3, and deregister from service discovery before the instance is terminated.' },
  ],
  pitfalls: [
    { title: 'Insufficient Spot diversification', description: 'Relying on a single instance type for a large Spot fleet creates a correlated interruption risk when that capacity pool is reclaimed. Always specify at least 4-6 instance types of similar vCPU/memory ratios and span multiple AZs. The capacity-optimized strategy outperforms lowest-price for sustained workloads by selecting pools least likely to be interrupted.' },
    { title: 'Cluster placement group in multiple AZs', description: 'A Cluster placement group is scoped to a single AZ. Attempting to launch instances into it across AZs silently places them outside the group, losing the low-latency guarantee. Always verify placement group membership via the describe-instances response and plan for the capacity constraint risk when launching large clusters.' },
    { title: 'Ignoring Spot interruption notices', description: 'Applications that do not subscribe to the instance metadata endpoint or EventBridge spot-interruption events have only a 2-minute window before the instance is terminated without checkpointing. Implement a polling loop or EventBridge rule to trigger graceful shutdown logic as soon as the termination-time field is populated.' },
  ],
  keyQuestions: [
    { q: 'When would you use a Cluster placement group versus spreading instances across AZs?', a: 'A **Cluster placement group** is appropriate when the workload is tightly coupled with frequent inter-node communication — for example, MPI-based HPC simulations, distributed deep learning training with NCCL collective operations, or low-latency trading systems where microseconds of network jitter matter. The tradeoff is that all instances share a single fault domain, so a rack failure takes down the entire cluster.\n\nSpreading across AZs is the right default for web tiers and stateless services where availability and fault isolation outweigh raw network throughput between nodes.' },
    { q: 'How does the Nitro hypervisor differ from Xen, and why does it matter?', a: 'Traditional **Xen** hypervisors ran a privileged Domain 0 VM that consumed host resources for I/O processing. The **Nitro System** offloads networking, EBS I/O, and security monitoring to dedicated Nitro cards, freeing virtually all host CPU and memory for the tenant VM — achieving near-bare-metal performance.\n\nFor I/O-intensive databases and high-throughput applications, this results in measurably lower latency and higher IOPS compared to older Xen-based instance types. Nitro also provides a hardware root of trust for the **Nitro Security Chip**, which cryptographically verifies the firmware and software stack at boot.' },
    { q: 'How do you handle Spot Instance interruptions in a data processing pipeline?', a: 'Implement a two-minute interruption handler by polling the instance metadata service endpoint for `spot/termination-time` or subscribing to the **EC2 Spot Instance Interruption** EventBridge event. When triggered:\n\n- Stop accepting new work units\n- Checkpoint in-progress tasks to **S3** or **DynamoDB**\n- Deregister from the work queue\n\nUse a task queue with visibility timeouts (**SQS**) so interrupted tasks automatically become visible again for another instance to pick up. For long-running ML training, use PyTorch or TensorFlow checkpoint-to-S3 hooks triggered on `SIGTERM`.' },
    { q: 'What is EFA and when should you use it over standard ENA?', a: '**Elastic Fabric Adapter** (`EFA`) adds an OS-bypass capability on top of `ENA`, allowing MPI libraries to communicate directly with the network hardware without going through the Linux kernel network stack. This removes kernel overhead for collective operations like `AllReduce`, which is a bottleneck in large-scale distributed training and tightly-coupled HPC simulations.\n\n**EFA** is supported on `p4d`, `p3dn`, and `c5n` instance families. For most application workloads — web servers, databases, microservices — standard **ENA** with up to 100 Gbps is sufficient and EFA adds no benefit since they do not use MPI primitives.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/placement-groups.html',
    'https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-spot-instances.html',
    'https://aws.amazon.com/ec2/nitro/',
    'https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/efa.html',
  ],
},

{
  id: 'aws-lambda',
  title: 'Lambda — Serverless Functions',
  icon: 'zap',
  color: '#f97316',
  description: 'Event-driven, auto-scaling compute without managing servers.',
  introduction: '**AWS Lambda** runs code in response to events without provisioning or managing servers. You upload a function, configure a trigger, and **Lambda** handles execution, scaling, fault tolerance, and billing — charging only for the compute time consumed. It is the cornerstone of serverless architectures and integrates natively with over 200 AWS services.',
  whenToUse: 'Use Lambda for event-driven workloads, API backends via API Gateway or Function URLs, data transformation pipelines, scheduled jobs via EventBridge, and glue logic between AWS services. It excels when invocation patterns are bursty or unpredictable, when you want zero idle cost, or when the execution duration is under 15 minutes.',
  keyConcepts: [
    { title: 'Execution Environment', description: 'Each Lambda invocation runs in an isolated microVM (**Firecracker**) with a dedicated runtime process. Execution environments are created on first invocation (**cold start**) and reused for subsequent invocations (**warm start**). Code outside the handler function runs once during initialization and is reused across warm invocations, making it the right place to initialize database connections, SDK clients, and configuration.' },
    { title: 'Concurrency Model', description: '**Lambda** scales by creating new execution environments in parallel, up to the account concurrency limit (default `1000` per region). **Reserved Concurrency** caps a function at a defined maximum and guarantees that capacity is never consumed by other functions. **Provisioned Concurrency** pre-initializes a set number of environments, eliminating cold starts for latency-sensitive APIs at the cost of ongoing charges.' },
    { title: 'Triggers and Event Sources', description: '**Synchronous invocations** (`API Gateway`, `ALB`, `Lambda URL`) return a response directly to the caller. **Asynchronous invocations** (`S3`, `SNS`, `EventBridge`) place events in an internal queue; Lambda retries on failure with configurable retry counts and a dead-letter queue. **Event source mappings** (`SQS`, `Kinesis`, `DynamoDB Streams`, `Kafka`) poll the source and batch records into the function.' },
    { title: 'Cold Start Anatomy', description: 'A cold start comprises environment creation (50-200ms for **Firecracker**), runtime initialization (loading the language runtime), and init code execution (your initialization block). For Java, the JVM startup dominates. For Node.js and Python, cold starts are typically under 100ms on `arm64` Graviton. **SnapStart** (Java only) snapshots the initialized state and restores it on invocation, eliminating JVM cold starts.' },
  ],
  approach: [
    { step: 'Structure code to maximize warm reuse', details: 'Move all initialization outside the handler — SDK client construction, database connection pool creation, environment variable parsing, and ML model loading. These run once per execution environment and are reused across thousands of invocations. Treat the global scope as a cache, but never assume it persists across cold starts.' },
    { step: 'Choose the right memory and architecture', details: 'Lambda CPU allocation scales linearly with memory. Profile with AWS Lambda Power Tuning to find the optimal memory/duration combination that minimizes cost. Arm64 (Graviton2) delivers 20% better price-performance for most Node.js, Python, and Go workloads — switch by changing the architecture property in the function configuration.' },
    { step: 'Design error handling per invocation type', details: 'For synchronous invocations, return appropriate HTTP status codes and error objects. For asynchronous invocations, configure a Dead Letter Queue (SQS or SNS) and set maximum retry attempts (0-2). For SQS event source mappings, use batch item failure reporting to partially succeed a batch and return failed message IDs so only those messages are retried.' },
    { step: 'Manage concurrency and downstream protection', details: 'Set Reserved Concurrency on functions that talk to RDS or ElastiCache to prevent connection exhaustion. Use SQS as a buffer between high-throughput event sources and functions that call rate-limited downstream APIs. Use Provisioned Concurrency only for user-facing APIs where p99 latency matters and cold starts exceed your SLA.' },
  ],
  pitfalls: [
    { title: 'Opening database connections inside the handler', description: 'Creating a new RDS or ElastiCache connection on every invocation exhausts the database connection limit quickly at scale. Always initialize connection pools in the global scope outside the handler. Use RDS Proxy to multiplex thousands of Lambda connection requests onto a small pool of long-lived database connections.' },
    { title: 'Ignoring cold start impact on p99 latency', description: 'Median latency looks good in dashboards, but cold starts can add 500ms to 3 seconds at p99 for JVM runtimes. Profile with load tests that force cold starts by sending requests after idle periods. Add Provisioned Concurrency selectively for the most latency-sensitive endpoints rather than globally.' },
    { title: 'Exceeding the 15-minute timeout for long jobs', description: 'Lambda is not designed for jobs running longer than 15 minutes. For data processing pipelines, use Step Functions to chain Lambda invocations, or move the work to Fargate or Batch. Trying to work around the timeout with recursive self-invocation creates complex state management and failure handling problems.' },
  ],
  keyQuestions: [
    { q: 'How do you reduce Lambda cold start latency for a Java API?', a: '- **Lambda SnapStart** (Java 11 and 21 on Corretto) snapshots the initialized Firecracker microVM and restores it instead of booting, reducing cold starts from several seconds to under 100ms.\n- Use **GraalVM native image** compilation to minimize classes loaded during initialization.\n- Reduce the deployment package size to decrease class-loading time.\n- Use **Provisioned Concurrency** for the specific API endpoints with the tightest SLAs.\n- `arm64` Graviton reduces cold starts by 20% on average.' },
    { q: 'How does Lambda handle failures for SQS-triggered functions?', a: 'When a Lambda function throws an error while processing an **SQS** batch, the entire batch becomes visible again in the queue after the visibility timeout expires and all messages are retried.\n\nTo avoid re-processing successful messages in a batch, use the `reportBatchItemFailures` feature: return a response object with `batchItemFailures` listing only the message IDs that failed. **Lambda** will delete successful messages and return only failed ones to the queue.\n\nConfigure a **Dead Letter Queue** on the SQS source queue so messages that exhaust their `maxReceiveCount` are captured for inspection rather than dropped.' },
    { q: 'What is the difference between Reserved Concurrency and Provisioned Concurrency?', a: '- **Reserved Concurrency**: an upper bound that caps a function at N simultaneous executions, preventing it from consuming the account-wide pool. It does **not** eliminate cold starts.\n- **Provisioned Concurrency**: pre-warms N execution environments so they are ready to respond immediately, eliminating cold starts entirely for that capacity — but you pay for those environments even when idle.\n\nUse **Reserved Concurrency** to protect downstream resources from Lambda fan-out, and **Provisioned Concurrency** only for latency-sensitive user-facing functions where the cold start duration exceeds your p99 target.' },
    { q: 'How do you implement a fan-out pattern with Lambda and SNS?', a: 'Publish a single message to an **SNS** topic and subscribe multiple **Lambda** functions to it. **SNS** delivers the message to each subscriber concurrently, triggering parallel execution.\n\nEach subscriber Lambda processes its own concern:\n- One sends a confirmation email\n- One updates a **DynamoDB** record\n- One triggers a downstream pipeline\n\nUse **SNS message filtering policies** to route messages to specific Lambda subscribers based on message attributes, avoiding unnecessary invocations. This decouples producers from consumers and allows each processing path to scale independently.' },
  ],
  references: [
    'https://docs.aws.amazon.com/lambda/latest/dg/',
    'https://aws.amazon.com/blogs/compute/operating-lambda-performance-optimization-part-1/',
    'https://docs.aws.amazon.com/lambda/latest/dg/provisioned-concurrency.html',
  ],
  visualizations: [
    { title: 'Lambda Invocation Models', caption: 'Synchronous, asynchronous, and stream-based invocation paths showing how callers, event queues, DLQs, and polling consumers connect to Lambda.', image: '/diagrams/cloud/aws-lambda-invocation.png' },
    { title: 'Lambda Cold Start vs Warm', caption: 'Step-by-step breakdown of the cold-start penalty (environment init → package download → runtime init → handler) compared to a warm container reuse, with mitigation strategies.', image: '/diagrams/cloud/aws-lambda-coldstart.png' },
  ],
},

{
  id: 'aws-lambda-advanced',
  title: 'Lambda Advanced — Extensions, Layers, and SnapStart',
  icon: 'zap',
  color: '#f97316',
  description: 'Lambda Extensions, Layers, SnapStart, custom runtimes, and event-driven patterns.',
  introduction: 'Advanced **Lambda** topics go beyond basic function authoring into the ecosystem that enables production-grade serverless systems. **Lambda Layers** promote code reuse across functions. **Lambda Extensions** provide a sidecar model for telemetry, secrets refresh, and security agents. **SnapStart** eliminates JVM cold starts. Custom runtimes let you run any language. **Power Tuning** and **function URLs** round out the toolkit for serious serverless engineers.',
  whenToUse: 'Apply advanced Lambda patterns when building a multi-function serverless application that needs shared libraries (Layers), when integrating third-party APM agents without modifying function code (Extensions), when Java cold starts are causing SLA violations (SnapStart), or when designing complex event-driven workflows that span multiple services.',
  keyConcepts: [
    { title: 'Lambda Layers', description: 'A **Layer** is a ZIP archive added to the function execution environment at `/opt`, available to the runtime at function start. Layers separate shared libraries, custom runtimes, and configuration from function code. A function can reference up to five Layers. Layers are **versioned and immutable** — updating a Layer creates a new version; existing functions continue using the pinned version until explicitly updated.' },
    { title: 'Lambda Extensions', description: 'Extensions run as companion processes alongside the function runtime. Internal extensions run in the same process (via wrapper scripts). External extensions run as separate processes and receive lifecycle events (Init, Invoke, Shutdown) from the Lambda Runtime API. Extensions are the sanctioned way to integrate APM agents (Datadog, Dynatrace), secrets management sidecars, and log shipping without modifying function code. They add latency to the init phase, so keep them lightweight.' },
    { title: 'SnapStart', description: 'SnapStart for Java (Corretto 11, 21) takes a snapshot of the initialized execution environment — after the runtime and your init code have run — and caches it encrypted in S3. On invocation, Lambda restores from the snapshot instead of booting, cutting cold starts from seconds to under 300ms. Functions must implement the CRaC (Coordinated Restore at Checkpoint) interfaces to handle resource reconnection (sockets, file handles) properly after restoration.' },
    { title: 'Custom Runtimes and provided.al2', description: 'The provided.al2 runtime allows you to bring any language by implementing the Lambda Runtime API — a simple HTTP polling loop that retrieves events from /runtime/invocation/next, processes them, and posts results to /runtime/invocation/:requestId/response. Go, Rust, and C++ Lambda functions use this approach. AWS provides bootstrap binary conventions; deploy the compiled binary as the handler.' },
  ],
  approach: [
    { step: 'Structure shared code as versioned Layers', details: 'Package shared utility libraries, the AWS SDK (for runtimes that do not bundle it), and large ML model binaries as Layers. Version Layers semantically and pin function configurations to specific Layer versions in infrastructure-as-code. Keep Layers under 50MB uncompressed to avoid cold start penalty from extraction time.' },
    { step: 'Integrate observability via Extensions', details: 'Deploy your APM agent as an external Lambda Extension distributed as a Layer. The extension registers for Invoke events, collects telemetry during the function invocation, and flushes metrics after the function returns its response. This model means telemetry flushing runs on Lambda time rather than blocking the response to the caller. Configure the extension via environment variables injected by your deployment pipeline.' },
    { step: 'Adopt SnapStart with CRaC lifecycle hooks', details: 'Enable SnapStart on the function configuration. Implement BeforeCheckpoint to close sockets, flush caches, and release file handles before the snapshot is taken. Implement AfterRestore to re-open connections, refresh ephemeral credentials, and reseed random number generators that must not be replayed from snapshot state. Test restoration with the Lambda SnapStart developer guide test harness.' },
    { step: 'Use Lambda Function URLs for direct HTTPS access', details: 'Function URLs provide a dedicated HTTPS endpoint for a Lambda function without API Gateway. Supports CORS configuration and IAM or no-auth modes. Use for simple webhooks, internal tool backends, and Stripe webhook receivers where API Gateway features (throttling, request validation, caching) are not needed. They cost nothing beyond the Lambda invocation itself.' },
  ],
  pitfalls: [
    { title: 'Layer size creep degrading cold starts', description: 'Stacking multiple large Layers — especially Python dependency bundles or Node.js node_modules — adds extraction time to every cold start. Audit Layer sizes regularly. For Python, use Lambda-specific optimized packages (manylinux wheels) and remove test data from dependency bundles. Consider compiling dependencies natively in an Amazon Linux 2 Docker container to avoid compatibility shims.' },
    { title: 'SnapStart without CRaC hooks breaking production', description: 'Enabling SnapStart without implementing AfterRestore causes subtle bugs: database connections restored from snapshot point to stale TCP state, TLS sessions are invalid, and UUID generators seeded from snapshot entropy produce collisions. Always audit every resource initialized in the init phase and add CRaC hooks to reconnect or re-seed them after restoration.' },
    { title: 'Extensions adding unacceptable init latency', description: 'External extensions must complete their own initialization before Lambda reports the environment as ready. A poorly optimized extension that takes 800ms to initialize adds that overhead to every cold start. Measure extension init time separately using the Init Duration in CloudWatch Logs and hold extension authors to a strict initialization latency budget.' },
  ],
  keyQuestions: [
    { q: 'How do Lambda Layers differ from building dependencies into the deployment package?', a: 'Bundling dependencies into the deployment package is simple but creates redundancy when many functions share the same libraries, slows down deployments as the package grows, and complicates security patching since every function must be redeployed when a shared library has a CVE. Layers centralize shared code, allow independent versioning, and can be patched and rolled out to all consuming functions by bumping the Layer version in a single infrastructure change. The tradeoff is that Layer management adds operational overhead and Layers add to total unzipped package size limits.' },
    { q: 'What are the Lambda concurrency scaling limits and how do you handle thundering-herd scenarios?', a: 'Lambda scales at 1000 concurrent executions per region by default, with burst scaling of 500-3000 new instances per minute depending on region. A thundering herd — where a sudden traffic spike exceeds the burst limit — results in 429 TooManyRequestsException errors for synchronous callers. Mitigate with SQS buffering between the event source and Lambda so requests queue rather than fail. For API-driven workloads, add a WAF rate limit at API Gateway. Request a concurrency limit increase proactively for known traffic events.' },
    { q: 'How would you implement a distributed saga pattern using Lambda and Step Functions?', a: 'Define each step of the business transaction as a Lambda function and model the saga as a Step Functions Express or Standard Workflow. Each step function invokes the forward action and registers a compensating transaction. On failure at any step, the orchestrator walks back the completed steps by invoking the compensating Lambda functions in reverse order. Step Functions handles state persistence, retry logic with exponential backoff, and timeout enforcement without you managing a state database. This is the recommended pattern for distributed transactions across microservices where two-phase commit is impractical.' },
    { q: 'How do you securely pass secrets to Lambda functions without hardcoding them?', a: 'Store secrets in AWS Secrets Manager or SSM Parameter Store. At runtime, fetch them in the initialization block (outside the handler) and cache them in module-level variables for reuse across warm invocations. For automatic rotation, use the Secrets Manager Extension (AWS-Parameters-and-Secrets-Lambda-Extension), which maintains a local cache refreshed on TTL expiry and serves secrets over a localhost HTTP endpoint. This avoids hardcoding, avoids environment variable plaintext exposure, and reduces Secrets Manager API calls through caching.' },
  ],
  references: [
    'https://docs.aws.amazon.com/lambda/latest/dg/configuration-layers.html',
    'https://docs.aws.amazon.com/lambda/latest/dg/lambda-extensions.html',
    'https://docs.aws.amazon.com/lambda/latest/dg/snapstart.html',
  ],
},

{
  id: 'aws-ecs',
  title: 'ECS — Elastic Container Service',
  icon: 'box',
  color: '#f97316',
  description: 'Fully managed container orchestration on AWS without Kubernetes complexity.',
  introduction: '**Amazon ECS** is a native AWS container orchestration service that runs Docker containers on clusters of EC2 instances or serverless **Fargate** capacity. **ECS** integrates tightly with **IAM**, **VPC**, **ALB**, **CloudWatch**, and **CodeDeploy**, making it the lowest-friction path for teams migrating containerized workloads to AWS without adopting Kubernetes operational overhead.',
  whenToUse: 'Use ECS when your team is deploying containerized services on AWS and wants deep AWS-native integration without the learning curve of Kubernetes. It is ideal for microservices that need IAM task roles, ALB path-based routing, blue/green deployments via CodeDeploy, and tight CloudWatch integration — all without managing a Kubernetes control plane.',
  keyConcepts: [
    { title: 'Task Definitions', description: 'A **Task Definition** is a JSON blueprint describing one or more containers: Docker image, CPU/memory allocation, port mappings, environment variables, secrets (from **Secrets Manager** or **SSM**), IAM task role, logging configuration, and health check command. Task Definitions are versioned; deploying a new image version creates a new Task Definition revision.' },
    { title: 'Services and Tasks', description: 'An **ECS Service** maintains a desired count of running tasks, integrates with an ALB target group, handles task replacement on failure, and supports deployment strategies (rolling update, blue/green). A standalone **Task** is a one-shot container execution — suitable for batch jobs, migrations, and cron-triggered work via **EventBridge**.' },
    { title: 'Launch Types', description: '**EC2 launch type** runs containers on EC2 instances you manage in the cluster — gives control over instance type, storage, and placement. **Fargate launch type** is serverless: AWS provisions the underlying compute per task based on CPU/memory request, eliminating cluster node management. **External launch type** extends ECS to on-premises servers via **ECS Anywhere**.' },
    { title: 'IAM Task Roles', description: '**Task Roles** attach IAM policies to individual containers, providing fine-grained AWS API access without embedding credentials. The ECS agent injects temporary credentials via the task metadata endpoint. Each microservice should have its own **Task Role** with only the permissions it needs — following least privilege at the container level.' },
  ],
  approach: [
    { step: 'Design the task definition for production', details: 'Set CPU and memory hard limits based on profiled peak usage. Declare secrets via the secrets block referencing Secrets Manager ARNs — never pass credentials as plaintext environment variables. Configure the awslogs log driver to ship container logs to CloudWatch Logs with a log group per service and a 30-day retention policy. Add a HEALTHCHECK instruction in the Dockerfile and mirror it in the task definition health check.' },
    { step: 'Configure service auto-scaling', details: 'Attach Application Auto Scaling to the ECS service using ECSServiceAverageCPUUtilization or custom CloudWatch metrics as scaling targets. Set scale-out at 60% CPU and scale-in at 30% with a 5-minute cooldown. For traffic-driven scaling, use ALBRequestCountPerTarget as the metric. Always test scaling behavior under load before production launch.' },
    { step: 'Implement blue/green deployments via CodeDeploy', details: 'Create an ECS CodeDeploy deployment group pointing to two ALB target groups (blue and green). On deployment, CodeDeploy registers new tasks in the green target group, runs validation tests, shifts traffic with a configurable percentage, and terminates the blue tasks after a bake period. Configure automatic rollback on CloudWatch alarm breach to revert traffic to blue if error rates spike.' },
    { step: 'Enforce least-privilege networking', details: 'Run tasks in private subnets. Attach a task security group that allows inbound only from the ALB security group on the container port and allows outbound only to required downstream services and AWS service VPC endpoints. Do not use broad 0.0.0.0/0 rules. Use VPC Endpoints for ECR, S3, CloudWatch, and Secrets Manager to keep traffic off the public internet.' },
  ],
  pitfalls: [
    { title: 'Sharing IAM roles across task definitions', description: 'Giving all services a single shared task role violates least privilege and means a compromised container has lateral movement access to all resources any service uses. Create a dedicated task role per service with only the specific S3 prefixes, DynamoDB tables, and Secrets Manager paths that service requires.' },
    { title: 'Not setting CPU and memory hard limits', description: 'Without hard limits, a runaway container on an EC2 launch type cluster can starve co-located containers of resources. Always set both CPU reservation and memory hard limit. For Fargate, CPU and memory are the billing units so undersizing causes OOM kills and oversizing wastes money — profile carefully and add 20% headroom above peak.' },
    { title: 'Stale container image tags in task definitions', description: 'Using the latest tag in a task definition means a service rolling update always pulls whatever latest resolves to at deployment time, making deployments non-deterministic and rollbacks impossible. Always tag images with the Git commit SHA or build number and reference that exact tag in the task definition revision.' },
  ],
  keyQuestions: [
    { q: 'How does ECS service discovery work and when would you use it over an ALB?', a: '**ECS Service Discovery** integrates with **AWS Cloud Map** to register tasks as DNS A or SRV records automatically as they start and stop. Client services resolve the service name via DNS and connect directly to task IPs, making it suitable for internal service-to-service communication with low-overhead protocols like gRPC.\n\nAn **ALB** is better for HTTP/HTTPS workloads that need path-based routing, HTTPS termination, WAF integration, and health-check-based traffic distribution.\n\n- Use **Service Discovery** for internal microservice meshes\n- Use **ALB** for public or consumer-facing services' },
    { q: 'How do you perform a zero-downtime deployment on ECS?', a: 'Configure the service deployment with `minimumHealthyPercent: 100` and `maximumPercent: 200`. **ECS** will launch new tasks before draining old ones, ensuring capacity never drops below 100%.\n\nPair this with an **ALB** target group deregistration delay (default 300 seconds — reduce to 30 seconds for fast deployments) so old tasks finish in-flight requests before termination.\n\nFor more controlled cutover, use **CodeDeploy** blue/green which allows traffic shifting by percentage with an automated bake period before old task termination.' },
    { q: 'What is the difference between ECS task CPU/memory reservation and hard limits?', a: '- **CPU reservation** is a soft guarantee — the container gets at least that many CPU units when the host is contended, but can burst above it when capacity is available.\n- **Memory hard limit** causes the container to be OOM-killed if it exceeds that value.\n- `memoryReservation` (soft limit) influences scheduling placement on EC2 clusters without causing a kill.\n\nFor **Fargate**, the task-level CPU and memory values are both hard limits and billing dimensions. Always set memory hard limits conservatively above your observed peak to prevent spurious OOM kills under load spikes.' },
    { q: 'How do you handle secret rotation for ECS tasks using Secrets Manager?', a: 'Reference secrets in the task definition using the `secrets` block with the **Secrets Manager** ARN. On task launch, ECS injects the current secret value as an environment variable. However, environment variables are **fixed at task start** — they do not refresh when **Secrets Manager** rotates the secret.\n\nFor truly dynamic rotation:\n- Have the application fetch secrets from **Secrets Manager** at runtime using the SDK with a TTL-based in-memory cache\n- Alternatively, trigger a service rolling update (force new deployment) via **EventBridge** when rotation completes, replacing all running tasks with new ones that inject the new credentials' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/',
    'https://docs.aws.amazon.com/AmazonECS/latest/bestpracticesguide/',
    'https://aws.amazon.com/blogs/containers/deploy-amazon-ecs-applications-using-blue-green-with-aws-codedeploy/',
  ],
  visualizations: [
    { title: 'ECS Architecture', caption: 'How Clusters, Services, Task Definitions, and running Tasks relate, and where EC2 launch type, Fargate, ALB, and ECR each fit in the topology.', image: '/diagrams/cloud/aws-ecs-architecture.png' },
    { title: 'ECS on EC2 vs Fargate', caption: 'Shared-responsibility comparison showing which layers you manage yourself on EC2 launch type versus what Fargate abstracts away entirely.', image: '/diagrams/cloud/aws-ecs-vs-fargate.png' },
  ],
},

{
  id: 'aws-eks',
  title: 'EKS — Elastic Kubernetes Service',
  icon: 'layers',
  color: '#f97316',
  description: 'Managed Kubernetes control plane on AWS with deep cloud-native integration.',
  introduction: '**Amazon EKS** runs a managed Kubernetes control plane — `etcd`, `kube-apiserver`, and `kube-controller-manager` — across multiple AZs with automatic patching and version upgrades. Worker nodes run on EC2 managed node groups, self-managed groups, or **Fargate** profiles. **EKS** integrates with IAM via **IRSA**, with load balancing via the **AWS Load Balancer Controller**, and with storage via the **EBS** and **EFS** CSI drivers.',
  whenToUse: 'Use EKS when your team already operates Kubernetes workloads, needs ecosystem portability across clouds, or requires advanced Kubernetes features like custom controllers, operators, admission webhooks, and fine-grained pod-level scheduling. Also choose EKS when adopting a service mesh (Istio, Linkerd, AWS App Mesh) or when running GPU-accelerated ML training with complex pod affinity rules.',
  keyConcepts: [
    { title: 'EKS Control Plane', description: 'AWS manages the Kubernetes control plane components across three AZs. The API server endpoint can be public, private, or both. The control plane is patched and upgraded by AWS on a rolling basis. You are responsible for upgrading worker nodes separately — control plane and data plane upgrades are decoupled. EKS Kubernetes versions are supported for approximately 14 months after release.' },
    { title: 'IRSA — IAM Roles for Service Accounts', description: 'IRSA replaces node-level IAM roles with pod-level identity. Each Kubernetes service account is annotated with an IAM role ARN. The EKS OIDC provider issues JWT tokens that the AWS SDK exchanges for temporary credentials via STS AssumeRoleWithWebIdentity. This gives each pod its own least-privilege IAM role without sharing the node instance profile across all workloads on the node.' },
    { title: 'Managed Node Groups', description: 'Managed Node Groups provision EC2 instances using a Launch Template, register them with the cluster, and handle rolling updates that drain pods before terminating nodes. They integrate with EC2 Auto Scaling Groups, support Spot and On-Demand mixed capacity, and apply AWS security patches automatically. Karpenter is the modern alternative — a just-in-time node autoprovisioner that selects optimal instance types per pending pod requirements.' },
    { title: 'AWS Load Balancer Controller', description: 'The AWS Load Balancer Controller (formerly ALB Ingress Controller) provisions ALBs for Kubernetes Ingress resources and NLBs for LoadBalancer-type Services. It supports target type ip for direct pod routing (bypassing kube-proxy iptables), which reduces hops and improves throughput. Annotations on Ingress and Service resources control SSL termination, WAF attachment, and target group configuration.' },
  ],
  approach: [
    { step: 'Bootstrap the cluster with eksctl or Terraform', details: 'Use eksctl create cluster with a ClusterConfig YAML or Terraform aws_eks_cluster for repeatable, version-controlled cluster creation. Define node groups with instance diversification across multiple instance families and AZs. Enable OIDC provider at creation time to unlock IRSA. Store kubeconfig in a secrets manager, never in CI environment variables in plaintext.' },
    { step: 'Implement IRSA for every workload', details: 'Create a unique IAM role per Kubernetes service account. Annotate the service account with eks.amazonaws.com/role-arn. Grant the IAM role only the specific AWS API calls the workload needs. Delete the node instance profile broad permissions once all workloads are migrated to IRSA. Audit with IAM Access Analyzer to detect overly permissive roles.' },
    { step: 'Configure Karpenter for node autoscaling', details: 'Install Karpenter via Helm and create NodePool and EC2NodeClass resources. Karpenter watches for unschedulable pods and provisions the smallest-cost instance type that satisfies pod resource requests, node affinity, and taints. It consolidates underutilized nodes by migrating pods and terminating the emptied instance. Karpenter replaces Cluster Autoscaler for new EKS deployments and reduces mean time to scale from minutes to under 60 seconds.' },
    { step: 'Control pod networking and security', details: 'Use Amazon VPC CNI so pods receive VPC IPs and security groups can be applied at the pod level (via Security Groups for Pods). Deploy Kubernetes Network Policies using a CNI plugin that enforces them (Calico, Cilium, or VPC CNI native policy support). Require all containers to run as non-root and apply Pod Security Standards at namespace level via admission control.' },
  ],
  pitfalls: [
    { title: 'Skipping version upgrades until forced', description: 'EKS removes support for Kubernetes versions 14 months after release. Teams that skip minor versions accumulate technical debt and face forced upgrades that skip multiple versions, requiring add-on compatibility testing across a large gap. Adopt a policy of upgrading one minor version per quarter to stay current and make each upgrade incremental and low-risk.' },
    { title: 'Using node instance profiles instead of IRSA', description: 'A broad EC2 instance profile applied to all worker nodes grants every pod on those nodes the same AWS permissions, violating least privilege. A compromised container can exfiltrate credentials for every AWS service any workload on that node needs. Migrate all workloads to IRSA and revoke the broad node instance profile permissions.' },
    { title: 'Insufficient resource requests causing noisy neighbor problems', description: 'Pods without CPU and memory requests are scheduled as if they need zero resources, leading to CPU throttling and OOM kills when nodes become contended. Always set resource requests based on profiled steady-state usage and set limits at peak-plus-headroom. Use Vertical Pod Autoscaler in recommendation mode to surface right-sizing suggestions for running workloads.' },
  ],
  keyQuestions: [
    { q: 'How does IRSA work under the hood and why is it better than instance profiles?', a: 'IRSA uses an EKS-hosted OIDC provider that issues JWT tokens scoped to a specific Kubernetes service account. The AWS SDK inside the pod calls AssumeRoleWithWebIdentity with the JWT, and STS returns short-lived credentials scoped to the annotated IAM role. Because the token includes the service account name and namespace, the IAM role trust policy can restrict assumption to a specific pod identity. Instance profiles grant the same permissions to every process on every node, so a compromised container can enumerate and access all AWS resources any other workload needs. IRSA makes blast radius pod-scoped rather than node-scoped.' },
    { q: 'How would you upgrade an EKS cluster with zero downtime?', a: 'First upgrade the control plane by updating the cluster version in eksctl or Terraform — this is in-place and takes 10-20 minutes with no data-plane disruption. Then update add-ons (VPC CNI, CoreDNS, kube-proxy) to versions compatible with the new Kubernetes version. Finally, perform a rolling node group upgrade: eksctl nodegroup upgrade or Karpenter drift detection will cordon old nodes, drain pods (respecting PodDisruptionBudgets), terminate old nodes, and bring up new nodes on the new AMI. Always set PodDisruptionBudgets with minAvailable for production workloads so the drain operation respects availability guarantees.' },
    { q: 'How do you implement multi-tenancy on a shared EKS cluster?', a: 'Use Kubernetes namespaces as the primary isolation boundary and assign each tenant a dedicated namespace. Apply ResourceQuotas to limit CPU, memory, and persistent volume claims per namespace. Apply Network Policies to prevent inter-namespace pod communication. Use IRSA with namespace-scoped trust policies to ensure tenant workloads can only assume their own IAM role. For stronger isolation, use separate node groups with node selectors and taints to ensure tenant A pods never co-reside with tenant B pods on the same node.' },
    { q: 'What is the role of the Karpenter node consolidation feature and how does it work?', a: 'Karpenter consolidation continuously evaluates whether running nodes can be bin-packed more efficiently. When it detects that a set of pods could fit onto fewer nodes, it cordons a candidate node, migrates pods via eviction (respecting PodDisruptionBudgets and terminationGracePeriod), and terminates the now-empty node. This reduces cluster cost during off-peak hours without manual intervention. Consolidation respects do-not-disrupt annotations on pods for workloads that must not be evicted mid-operation, such as long-running batch jobs or ML training runs.' },
  ],
  references: [
    'https://docs.aws.amazon.com/eks/latest/userguide/',
    'https://aws.github.io/aws-eks-best-practices/',
    'https://karpenter.sh/docs/',
  ],
  visualizations: [
    { title: 'EKS Architecture', caption: 'AWS-managed control plane components (apiserver, etcd, scheduler) and the customer-managed data plane options (managed node groups and Fargate profiles) with key add-ons.', image: '/diagrams/cloud/aws-eks-architecture.png' },
    { title: 'EKS Networking', caption: 'VPC CNI pod-IP assignment, ClusterIP service routing, ALB Ingress Controller integration, and per-pod Security Groups for private RDS access.', image: '/diagrams/cloud/aws-eks-networking.png' },
  ],
},

{
  id: 'aws-fargate',
  title: 'Fargate — Serverless Containers',
  icon: 'box',
  color: '#f97316',
  description: 'Run containers without managing EC2 nodes — pay per vCPU and memory second.',
  introduction: 'AWS Fargate is a serverless compute engine for containers that works with both ECS and EKS. With Fargate you define the CPU and memory your container needs, and AWS provisions the underlying infrastructure invisibly. There are no nodes to patch, no AMIs to manage, and no cluster bin-packing to optimize. Billing is per vCPU-second and GB-second of memory consumed.',
  whenToUse: 'Use Fargate for workloads where you want to eliminate node management overhead, for short-lived batch and scheduled tasks where idle EC2 nodes would waste money, for development and staging environments that need rapid spin-up, and for workloads with unpredictable resource needs where precise right-sizing of EC2 nodes is impractical.',
  keyConcepts: [
    { title: 'Fargate Task Isolation', description: 'Each Fargate task runs in its own dedicated kernel using Firecracker microVMs — containers from different customers never share a host kernel or hardware, providing stronger isolation than traditional container-on-shared-EC2 models. This makes Fargate suitable for regulated workloads that require hypervisor-level tenant isolation.' },
    { title: 'CPU and Memory Sizing', description: 'Fargate supports discrete CPU/memory combinations: 0.25 vCPU / 0.5-2 GB, 0.5 vCPU / 1-4 GB, up to 16 vCPU / 120 GB. You must choose a valid combination. Unlike EC2, there is no burst credit mechanism — the task always runs at the allocated CPU. Overallocating wastes money since billing starts when the task starts, including pull and init time.' },
    { title: 'Fargate Spot', description: 'Fargate Spot runs tasks on spare AWS capacity at up to 70% discount. Tasks receive a 2-minute interruption notice before termination. Use it for fault-tolerant batch workloads, CI runners, and stateless data processing. Combine regular Fargate (base capacity) with Fargate Spot (burst) in an ECS Capacity Provider Strategy to optimize cost automatically.' },
    { title: 'EKS Fargate Profiles', description: 'An EKS Fargate Profile defines which pods (by namespace and label selectors) should run on Fargate. Matching pods are scheduled onto Fargate nodes transparently — the pod specification does not change. CoreDNS and critical system pods must be excluded from Fargate profiles and run on managed node groups, as they require daemonset scheduling which Fargate does not support.' },
  ],
  approach: [
    { step: 'Right-size tasks using CloudWatch Container Insights', details: 'Enable Container Insights on the ECS cluster. After 1-2 weeks in production, examine the p95 CPU and memory utilization metrics per task. Size the task allocation at p95 usage plus a 30% headroom buffer. For EKS Fargate, Vertical Pod Autoscaler recommendation mode provides similar sizing guidance.' },
    { step: 'Use Capacity Provider Strategy for cost/availability tradeoffs', details: 'Create an ECS Capacity Provider for Fargate (base) and Fargate Spot (burst). Set the strategy weight so the base capacity uses regular Fargate for your minimum viable task count and Fargate Spot absorbs scale-out. This minimizes interruption risk for the core fleet while leveraging Spot discounts for the elastic portion.' },
    { step: 'Configure VPC networking for Fargate tasks', details: 'Fargate tasks in ECS run in awsvpc networking mode, giving each task a dedicated ENI and a VPC IP address. Place tasks in private subnets with NAT Gateway for outbound internet access. Add VPC Endpoints for ECR (ecr.api, ecr.dkr), S3 (for ECR layer pulls), CloudWatch Logs, and Secrets Manager to eliminate NAT costs for AWS API traffic.' },
    { step: 'Optimize image pull time', details: 'Fargate pulls the container image on every cold task start — large images (over 500MB) significantly extend cold start time. Use ECR and enable ECR image pull caching. Keep production images minimal using multi-stage Docker builds. Store large model files in S3 or EFS and mount them at runtime rather than baking them into the image.' },
  ],
  pitfalls: [
    { title: 'Ignoring image pull latency on cold starts', description: 'A 1GB container image pulled from Docker Hub over the public internet can take 30-60 seconds on a Fargate cold start, making auto-scaling appear sluggish. Always use ECR in the same region, enable ECR pull-through cache, and reduce image size with multi-stage builds to keep cold start under 10 seconds.' },
    { title: 'Missing VPC endpoints causing NAT Gateway charges', description: 'Fargate tasks pulling images from ECR and sending logs to CloudWatch via NAT Gateway generate substantial data-transfer costs at scale. Each ECR pull traverses NAT unless VPC Endpoints are configured. Deploy com.amazonaws.region.ecr.dkr, ecr.api, s3, and logs VPC Endpoints to eliminate this cost.' },
    { title: 'Using Fargate for CPU-bound long-running workloads', description: 'Fargate charges by the second from task start to stop. A CPU-intensive 8-hour batch job running on Fargate at 4 vCPU costs significantly more than running the equivalent workload on a Spot EC2 instance. Evaluate Fargate Spot for batch, and compare total cost against EC2 Spot plus node management overhead before committing.' },
  ],
  keyQuestions: [
    { q: 'How does Fargate billing work and how do you optimize for cost?', a: 'Fargate bills per vCPU-second and per GB-second of memory from task start (first pull begins) to task stop, rounded up to the nearest second. Optimization strategies include right-sizing via Container Insights metrics, using Fargate Spot for fault-tolerant workloads (70% discount), minimizing image size to reduce pull time billed, and using Compute Savings Plans which apply to Fargate spend. For predictable steady-state workloads, compare Fargate costs against Reserved EC2 instances — EC2 Reserved often wins for 24/7 workloads above a certain scale.' },
    { q: 'What are the limitations of Fargate compared to EC2 launch type?', a: 'Fargate does not support daemonsets (so you cannot run node-level agents like Datadog node agent), does not allow mounting the Docker socket, does not support privileged containers, has no GPU support (as of 2024), and limits ephemeral storage to a configurable maximum of 200GB. You cannot use custom AMIs, access instance metadata for node-level information, or use placement groups. For workloads needing any of these capabilities, the EC2 launch type is required.' },
    { q: 'How do you share storage between containers in a Fargate task?', a: 'Containers within the same Fargate task definition can share ephemeral storage by declaring a named volume in the task definition and mounting it at different paths in each container. This is equivalent to a Docker named volume shared between containers on the same host. For persistent storage that survives task termination, mount an EFS file system via an EFS volume declaration in the task definition — EFS access points support POSIX UID/GID enforcement for container-level access control. EBS attachment to individual Fargate tasks is not natively supported.' },
    { q: 'How do you implement blue/green deployments for an ECS Fargate service?', a: 'Use AWS CodeDeploy with the ECS deployment type. CodeDeploy creates a new task set (green) registered with a second ALB target group, runs optional test traffic validation against the green target group, then shifts production traffic with a configurable linear or canary strategy (e.g., 10% every 5 minutes). After the bake period, the old task set (blue) is terminated. Automatic rollback triggers if a CloudWatch alarm on error rate or latency breaches a threshold during the traffic shift. This entire flow is codified in a CodeDeploy AppSpec file checked into the repository.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonECS/latest/userguide/what-is-fargate.html',
    'https://aws.amazon.com/fargate/pricing/',
    'https://docs.aws.amazon.com/eks/latest/userguide/fargate.html',
  ],
},

{
  id: 'aws-batch',
  title: 'Batch — Managed Batch Computing',
  icon: 'layers',
  color: '#f97316',
  description: 'Run hundreds of thousands of batch jobs on dynamically provisioned compute.',
  introduction: 'AWS Batch dynamically provisions EC2 or Fargate compute resources scaled to the volume and resource requirements of submitted jobs. You define job queues, compute environments, and job definitions, then submit jobs — Batch handles scheduling, scaling, retries, and dependency management. It eliminates the need to build and maintain custom batch scheduling infrastructure.',
  whenToUse: 'Use AWS Batch for large-scale data processing, genomic analysis, financial risk modeling, video transcoding, machine learning preprocessing, and any workload consisting of discrete computational jobs that can run in parallel. It is the right choice when jobs have diverse CPU/memory requirements, when job volume is too high for Lambda, and when workloads run too long for Lambda\'s 15-minute limit.',
  keyConcepts: [
    { title: 'Job Queues and Priority', description: 'Jobs are submitted to named queues with a priority integer. Higher-priority queues receive compute capacity first when multiple queues share compute environments. A queue can be associated with multiple compute environments ordered by priority, allowing failover from preferred On-Demand capacity to Spot.' },
    { title: 'Compute Environments', description: 'Managed compute environments let AWS provision and scale EC2 or Fargate instances automatically based on job demand. Specify minimum and maximum vCPUs, allowed instance types (or optimal for auto-selection), and launch templates. Unmanaged environments let you run your own ECS cluster as the backing compute, useful for custom AMI requirements.' },
    { title: 'Job Definitions', description: 'A Job Definition is a template specifying the container image, vCPU and memory requirements, environment variables, IAM job role, mount points, retry strategy, and timeout. Jobs override specific definition properties at submission time. Multi-node parallel jobs run multiple coordinated containers across instances, suitable for tightly-coupled MPI workloads.' },
    { title: 'Array Jobs and Dependencies', description: 'Array jobs create N identical child jobs (up to 10,000) that differ only by the AWS_BATCH_JOB_ARRAY_INDEX environment variable — ideal for parameter sweep experiments. Job dependencies using dependsOn with N_TO_N or SEQUENTIAL strategies allow DAG-style pipelines where downstream jobs wait for specific upstream jobs to succeed.' },
  ],
  approach: [
    { step: 'Design compute environment with Spot diversification', details: 'Configure managed Spot compute environments with multiple instance families (m5, m6i, c5, c6i) and sizes. Batch will choose the most available capacity pool, reducing Spot interruption frequency. Set a Spot price percentage at 60% of On-Demand or use SPOT_CAPACITY_OPTIMIZED allocation strategy. Create a paired On-Demand compute environment as a fallback queue association for urgent jobs.' },
    { step: 'Use container image caching strategies', details: 'Large container images pulled on every job startup dominate cold start time for short jobs. Use custom AMIs baked with the image pre-pulled via EC2 Image Builder to reduce pull time to near-zero. For Fargate Batch, keep images under 200MB and store large data dependencies in S3, mounting them via the job\'s init phase rather than the image layer.' },
    { step: 'Instrument jobs for retry safety', details: 'Design every job to be idempotent — running it twice produces the same output as running it once. Use job attempt numbers (AWS_BATCH_JOB_ATTEMPT) to namespace outputs per attempt. Configure retry strategies with up to 10 attempts and set exit code conditions (ON_EXIT_CODE, ON_REASON) to retry on infrastructure failures but not on application errors.' },
    { step: 'Orchestrate complex pipelines with Step Functions', details: 'Use Step Functions with the AWS Batch optimized integration to submit jobs, wait for completion, and branch on job status. This eliminates custom polling code. Model preprocessing, training, evaluation, and publishing as Batch job states connected by Step Functions transitions. Set Step Functions timeouts and heartbeat checks to handle stuck jobs.' },
  ],
  pitfalls: [
    { title: 'Underspecifying instance types in compute environments', description: 'Setting optimal instance type selection in a large compute environment can result in Batch choosing memory-optimized instances for CPU-bound jobs, wasting memory allocation and increasing cost. Specify the exact instance families that match your job resource ratio to control instance selection.' },
    { title: 'Not handling Spot interruptions in job code', description: 'A Spot interruption terminates the EC2 node running your job containers. Without checkpointing, the job restarts from scratch on a new node. For jobs longer than 30 minutes, implement periodic checkpoint writes to S3 and detect the AWS_BATCH_JOB_ATTEMPT variable on startup to resume from the latest checkpoint rather than restarting from the beginning.' },
    { title: 'Submitting many jobs with no queue priority strategy', description: 'Treating all jobs with equal priority means urgent production jobs wait behind batch preprocessing jobs submitted earlier. Design a multi-queue architecture: a high-priority queue for production-critical jobs on On-Demand compute, a standard queue for scheduled workloads on Spot, and a low-priority queue for speculative or experimental jobs.' },
  ],
  keyQuestions: [
    { q: 'How does AWS Batch differ from Lambda for large-scale data processing?', a: 'Lambda is limited to 15 minutes of execution, 10 GB of memory, and 512 MB to 10 GB of ephemeral storage, making it unsuitable for long-running or memory-intensive jobs. AWS Batch runs jobs in containers with no execution time limit, up to 96 vCPU and 624 GB of memory per job, and full EBS or EFS storage attachment. Batch also supports multi-node parallel jobs for tightly coupled computation that Lambda cannot model. Lambda is better for sub-second event-driven reactions; Batch is designed for jobs measured in minutes to hours.' },
    { q: 'How do you implement fan-out parallelism with Batch array jobs?', a: 'Submit an array job with arrayProperties.size set to N, where N equals the number of parallel work items. Each child job receives AWS_BATCH_JOB_ARRAY_INDEX (0 to N-1) as an environment variable and uses it to select its data partition — for example, reading lines array_index*chunk_size through (array_index+1)*chunk_size-1 from S3. A downstream aggregation job uses dependsOn with the parent array job ID and type N_TO_N, ensuring the aggregator runs only after all N child jobs complete successfully.' },
    { q: 'How do you monitor and alert on stuck Batch jobs?', a: 'Use EventBridge rules on Batch job state changes to detect transitions to FAILED, SUCCEEDED, or RUNNABLE (which indicates queued jobs waiting for capacity). Set a CloudWatch alarm on the time-in-queue metric or use a Lambda function triggered by EventBridge to page on-call if a RUNNING job exceeds its expected duration. Configure job-level timeout in the job definition as a safety net — jobs exceeding the timeout transition to FAILED and trigger retry logic or alerting.' },
    { q: 'How do you reduce job startup latency for short Batch jobs?', a: 'Short Batch jobs dominated by container image pull time and EC2 instance warmup lose most of their value to overhead. Use warm EC2 pools by setting minvCPUs above zero in the compute environment to keep instances running between job waves. Pre-pull container images by baking them into a custom AMI via EC2 Image Builder. Use Fargate for jobs under 10 minutes to eliminate EC2 provisioning time entirely, trading the AMI trick for a smaller image. Structure the job to read input parameters from SSM Parameter Store or S3 at runtime rather than rebuilding images per job variant.' },
  ],
  references: [
    'https://docs.aws.amazon.com/batch/latest/userguide/',
    'https://aws.amazon.com/blogs/compute/orchestrating-an-application-process-with-aws-batch-using-aws-step-functions/',
  ],
},

{
  id: 'aws-auto-scaling',
  title: 'Auto Scaling — Dynamic Capacity Management',
  icon: 'layers',
  color: '#f97316',
  description: 'Automatically adjust EC2, ECS, DynamoDB, and other resource capacity to match demand.',
  introduction: 'AWS Auto Scaling monitors applications and automatically adjusts capacity to maintain steady, predictable performance at the lowest possible cost. It spans EC2 Auto Scaling Groups, Application Auto Scaling (ECS services, DynamoDB, Aurora, Lambda), and AWS Auto Scaling plans that coordinate scaling across multiple resource types together.',
  whenToUse: 'Use Auto Scaling for any production workload with variable traffic patterns. It is essential for cost optimization (scale in during off-hours), resilience (replace failed instances automatically), and handling traffic spikes without manual intervention. Combine with load balancers for stateless tiers and tune scaling policies to match your traffic pattern shape.',
  keyConcepts: [
    { title: 'Scaling Policy Types', description: 'Target Tracking Scaling maintains a metric (e.g., 60% CPU utilization) automatically, adding and removing capacity to keep the metric near the target — simplest to configure and recommended for most use cases. Step Scaling triggers predefined capacity adjustments at metric threshold steps. Scheduled Scaling changes capacity at predictable times (pre-scale for known traffic events). Predictive Scaling (EC2 only) uses ML to forecast traffic and pre-provisions capacity.' },
    { title: 'Warm-Up and Cooldown', description: 'Scale-out cooldown prevents launching new instances while prior launches are still initializing. Instance Warm-Up tells Auto Scaling not to count newly launched instances toward the Auto Scaling group metrics until the warm-up period expires, preventing premature scale-in of recently launched instances. Setting these correctly prevents scale-out thrashing.' },
    { title: 'Health Checks', description: 'EC2 health checks verify the instance is running and reachable. ELB health checks verify the application passes the load balancer health check. Auto Scaling replaces instances failing either check. Always use ELB health checks in production — an EC2 instance can pass the EC2 health check while its application is returning 500 errors.' },
    { title: 'Launch Templates', description: 'Launch Templates define the instance configuration for new EC2 instances: AMI, instance type, key pair, security groups, IAM instance profile, user data, and storage. They support versioning and mixed instance policies, allowing a single ASG to run multiple instance types and purchase options (On-Demand + Spot) simultaneously.' },
  ],
  approach: [
    { step: 'Set target tracking as the primary scaling policy', details: 'For CPU-driven scaling, set target tracking at 60% CPUUtilization — this leaves headroom for traffic spikes before new instances must register. For ALB-driven scaling, use ALBRequestCountPerTarget at a value benchmarked from load tests to represent the request rate per instance at acceptable latency. Avoid step scaling policies for new systems — they require manual tuning of steps and cloudwatch alarm thresholds.' },
    { step: 'Configure scale-in protection for critical operations', details: 'Use Instance Scale-In Protection on instances running long-running transactions (database migrations, batch processing jobs) to prevent Auto Scaling from terminating them mid-operation. Use ASG Lifecycle Hooks to drain connections gracefully: hook into the EC2_INSTANCE_TERMINATING state, drain, then signal complete to allow termination to proceed.' },
    { step: 'Implement Predictive Scaling for recurring patterns', details: 'Enable Predictive Scaling on ASGs with recurring daily or weekly traffic patterns (office hours, lunchtime spikes). Predictive Scaling analyzes 14 days of CloudWatch history to generate a forecast and pre-scales capacity 5 minutes before predicted demand. Run it in forecast-only mode for 2 weeks before enabling automatic scaling to validate forecast accuracy against actual traffic.' },
    { step: 'Test scaling behavior under load', details: 'Run synthetic load tests that ramp from 10% to 200% of peak traffic over 10 minutes. Observe scale-out trigger time, time from trigger to healthy new instance in the load balancer, and error rate during scale-out. Tune warmup period, health check grace period, and cooldown intervals based on test results. Verify that scale-in does not prematurely terminate instances during gradual traffic ramp-down.' },
  ],
  pitfalls: [
    { title: 'Aggressive cooldown causing scale-out thrashing', description: 'Short cooldown periods (under 60 seconds) combined with a rapidly fluctuating metric cause Auto Scaling to launch and terminate instances in rapid succession, wasting money and increasing risk of availability disruptions during instance churn. Set cooldowns to at least 2x your application startup time and prefer target tracking over step scaling to benefit from built-in dampening.' },
    { title: 'Not using ELB health checks in the ASG', description: 'An ASG configured with only EC2 health checks will not replace instances whose application is crashing but whose EC2 status checks pass. Always enable ELB health checks on the ASG and set a health check grace period equal to your application startup time to prevent premature instance replacement during initialization.' },
    { title: 'Fixed instance types in a Spot ASG', description: 'An ASG using a single Spot instance type can be interrupted entirely when that capacity pool is reclaimed. Always configure mixed instance policies with at least 4-6 instance types of similar CPU/memory profiles. The capacity-optimized Spot allocation strategy selects the pool least likely to be interrupted rather than simply the cheapest at the moment.' },
  ],
  keyQuestions: [
    { q: 'How does Target Tracking differ from Step Scaling, and when would you choose each?', a: 'Target Tracking Scaling continuously adjusts capacity to maintain a metric at a target value — it works like a thermostat and self-tunes the scale-in and scale-out speeds automatically. It is simpler to configure and handles gradual traffic changes smoothly. Step Scaling requires you to define specific CloudWatch alarm thresholds and the exact capacity change to apply at each threshold — it gives more control but requires more tuning. Choose Target Tracking for most web and API workloads. Use Step Scaling when you need precise control over how much capacity to add at specific load thresholds, such as adding a fixed number of instances for every N jobs in a queue.' },
    { q: 'How do you prevent Auto Scaling from terminating an instance running a critical job?', a: 'Apply EC2 Instance Scale-In Protection to the instance via the ASG API or from within the instance itself via the instance metadata service. The ASG will skip that instance during scale-in events. Additionally, use an ASG Lifecycle Hook on the EC2_INSTANCE_TERMINATING transition — the hook pauses termination, sends a message to SQS or SNS, and your application code can complete the current job before signaling the hook to allow termination. Set the heartbeat timeout to allow sufficient time for the job to complete (up to 2 hours). Remember to remove scale-in protection programmatically once the job completes to avoid accumulating protected instances.' },
    { q: 'How does Predictive Scaling work and what are its limitations?', a: 'Predictive Scaling uses Amazon Forecast under the hood, analyzing up to 14 days of CloudWatch metric history to identify recurring patterns (daily, weekly) and generate a capacity forecast. It pre-provisions instances 5 minutes before forecast demand, avoiding the reaction lag of reactive policies. Limitations: it requires at least 24 hours of data to generate forecasts and works best with regular, predictable traffic patterns. It does not handle sudden unpredictable spikes (news events, marketing campaigns) — pair it with reactive target tracking as a safety net. It is also EC2 ASG-only and not available for ECS or other Application Auto Scaling targets.' },
    { q: 'How do you right-size the minimum and maximum capacity for an Auto Scaling Group?', a: 'Set minimum capacity to the number of instances needed to handle baseline traffic without any scale-out, which is typically the off-peak floor from historical CloudWatch metrics. This ensures availability even if Auto Scaling fails to scale out in time. Set maximum capacity at 2-3x the peak observed traffic to allow for emergency headroom, but also set a hard ceiling to cap runaway scaling costs from a traffic spike caused by a DDoS or misconfigured load test. Review and adjust both values after every seasonal peak using the 13-week rolling max as a data point.' },
  ],
  references: [
    'https://docs.aws.amazon.com/autoscaling/ec2/userguide/',
    'https://docs.aws.amazon.com/autoscaling/application/userguide/',
    'https://docs.aws.amazon.com/autoscaling/ec2/userguide/ec2-auto-scaling-predictive-scaling.html',
  ],
},

{
  id: 'aws-elastic-beanstalk',
  title: 'Elastic Beanstalk — PaaS Application Deployment',
  icon: 'server',
  color: '#f97316',
  description: 'Deploy and manage web applications without configuring the underlying infrastructure.',
  introduction: '**AWS Elastic Beanstalk** is a platform-as-a-service that handles provisioning, load balancing, auto scaling, monitoring, and deployment for web applications and services. You upload your code, choose a platform (Node.js, Python, Java, .NET, PHP, Ruby, Go, Docker), and **Beanstalk** manages the rest. You retain full control of the underlying AWS resources and can access and configure them at any time.',
  whenToUse: 'Use Elastic Beanstalk for teams that want fast deployment without infrastructure expertise, for migrating traditional web apps to AWS with minimal rework, for proof-of-concept and small production workloads where managed complexity outweighs customization flexibility. When you need full infrastructure control or complex multi-service architectures, ECS or EKS are more appropriate.',
  keyConcepts: [
    { title: 'Environments and Tiers', description: 'A **Beanstalk Application** contains multiple Environments. **Web server environments** run HTTP/HTTPS workloads behind an ALB with Auto Scaling. **Worker environments** process tasks from an **SQS** queue — Beanstalk injects a local SQS daemon that polls the queue and POSTs messages to a local HTTP endpoint in your application. Environments map to specific platform versions and can be updated independently.' },
    { title: '.ebextensions Configuration', description: '**`.ebextensions`** are YAML/JSON configuration files placed in a `.ebextensions` directory in your application bundle. They customize environment resources: install system packages, create IAM instance profiles, configure **CloudWatch** alarms, set environment properties, and modify the ALB configuration. They run during each deployment before the application starts.' },
    { title: 'Deployment Policies', description: '- **All-at-once**: replaces all instances simultaneously — fastest but causes downtime.\n- **Rolling**: replaces instances in batches — reduced capacity during deployment.\n- **Rolling with additional batch**: launches extra instances first — maintains full capacity.\n- **Immutable**: launches a new full set of instances and swaps them atomically — safest, no reduced capacity.\n- **Blue/Green**: uses URL swap to cut over between environments with instant rollback capability.' },
    { title: 'Platform Versions and Managed Updates', description: 'Each platform (e.g., Node.js 20 running on Amazon Linux 2023) is versioned. **Managed Platform Updates** automatically apply minor version patches and OS security fixes during a configurable maintenance window. You control when major version upgrades happen. Platforms are eventually end-of-life, requiring migration to newer platform versions periodically.' },
  ],
  approach: [
    { step: 'Use Immutable deployments for production', details: 'Configure the deployment policy as Immutable. Beanstalk creates a new set of instances in a temporary Auto Scaling Group, deploys the new version, verifies health checks pass, then migrates the new instances to the production ASG and terminates the old ones. This guarantees no capacity reduction during deployment and provides instant rollback by swapping back.' },
    { step: 'Manage configuration as code with .ebextensions', details: 'Place all environment customizations in .ebextensions files committed to version control alongside application code. Use commands for one-time setup (package installation), container_commands for operations that run after the app source is extracted (database migrations), and option_settings for Beanstalk configuration properties. Never manually configure Beanstalk environments through the console — this causes configuration drift.' },
    { step: 'Separate environments per stage with environment variables', details: 'Create separate Beanstalk environments for dev, staging, and production pointing to the same application. Inject environment-specific configuration (DATABASE_URL, API keys) via Beanstalk Environment Properties referencing Secrets Manager rather than hardcoding in the application bundle. Use .env.production equivalents managed through Beanstalk rather than checked into the repository.' },
    { step: 'Monitor with enhanced health and alarms', details: 'Enable Enhanced Health Monitoring for request-based metrics: HTTP 4xx/5xx rates, latency percentiles, and cause codes. Configure CloudWatch Alarms on UnhealthyHostCount and ApplicationRequests5xx to alert before customer impact. Use the Beanstalk health dashboard to correlate deployment events with health changes and quickly attribute degradation to a specific deployment.' },
  ],
  pitfalls: [
    { title: 'Using All-at-once deployments in production', description: 'All-at-once deployments briefly take down all instances simultaneously, causing 10-30 seconds of downtime even for small applications. This is acceptable in development but catastrophic in production. Always use Rolling with Additional Batch or Immutable deployment policies for any customer-facing environment.' },
    { title: 'SSH-ing into instances for configuration changes', description: 'Manual changes made via SSH are overwritten on the next deployment and are not reproducible. All server configuration must live in .ebextensions files or be applied through Beanstalk configuration options. Treat Beanstalk instances as immutable and resist the temptation to quick-fix production issues via direct SSH access.' },
    { title: 'Neglecting platform version updates', description: 'Beanstalk platform versions fall out of support, leaving the underlying OS and runtime unpatched. Enable Managed Platform Updates to apply minor and patch-level updates automatically during a low-traffic maintenance window. Plan major version migrations (e.g., Amazon Linux 1 to 2023) well in advance of end-of-support dates.' },
  ],
  keyQuestions: [
    { q: 'How does the Elastic Beanstalk worker tier integrate with SQS?', a: 'The **Worker Tier** runs a local **SQS** daemon on each instance that polls a configurable SQS queue. When a message arrives, the daemon POSTs the message body to `http://localhost/` (or a configurable path) and waits for an HTTP `2xx` response.\n\n- On success, the daemon deletes the message from the queue.\n- On failure or timeout, the message becomes visible again for retry.\n\nThe application simply exposes an HTTP endpoint and processes messages synchronously — no SQS SDK integration is needed in the application code.' },
    { q: 'How do you perform a blue/green deployment with Elastic Beanstalk?', a: 'Create a new Beanstalk environment (green) cloned from the production environment (blue) with the new application version deployed. After validating via smoke tests or a canary user group, use the **Swap Environment URLs** action to atomically exchange the CNAMEs of the two environments — production traffic instantly routes to green.\n\nThe original **blue** environment remains intact with the old version for instant rollback by swapping URLs back. This is safer than immutable deployments for major version changes that require extended validation periods.' },
    { q: 'How do you run database migrations safely during a Beanstalk deployment?', a: 'Use `container_commands` in `.ebextensions` with `leader_only: true` to ensure the migration runs on exactly one instance during a rolling deployment. Container commands run after application source is extracted but before the application starts, in alphabetical order by key name.\n\nFor safer migrations, run the migration as a one-time Task in a dedicated migration environment before deploying the application code, ensuring **backward-compatible schema changes** (additive only) that work with both the old and new application versions simultaneously.' },
    { q: 'What is the difference between Elastic Beanstalk configuration files and environment properties?', a: '- **Environment Properties**: simple key-value pairs injected as environment variables into the running application process — right for credentials, API URLs, and feature flags that vary by environment.\n- **`.ebextensions` files**: YAML documents executed by Beanstalk\'s configuration management during deployment to modify AWS resources (create **CloudFormation** resources, install packages, configure the nginx reverse proxy, set ALB rules).\n\nUse **Environment Properties** for runtime application configuration and **`.ebextensions`** for infrastructure-level customizations.' },
  ],
  references: [
    'https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/',
    'https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/using-features.deploy-existing-version.html',
  ],
},

{
  id: 'aws-lightsail',
  title: 'Lightsail — Simplified Cloud for Small Workloads',
  icon: 'server',
  color: '#f97316',
  description: 'Predictable monthly pricing for VPS, databases, containers, and static sites.',
  introduction: '**Amazon Lightsail** provides virtual private servers, managed databases, load balancers, CDN distributions, container services, and static site hosting with a simplified management console and predictable flat monthly pricing. It is designed for developers, small businesses, and students who need straightforward cloud resources without the complexity of the full AWS console.',
  whenToUse: 'Use Lightsail for simple web applications, WordPress sites, development and test environments, personal projects, and small business applications with predictable low-to-medium traffic. When workloads grow beyond Lightsail capacity limits or require advanced AWS integrations (VPC peering, IAM fine-grained policies, complex networking), migrate to full EC2, RDS, and related services.',
  keyConcepts: [
    { title: 'Instances and Bundles', description: '**Lightsail** instances are priced as fixed **bundles**: a combination of CPU, memory, SSD storage, and data transfer allowance at a flat monthly rate starting at $3.50/month. Each bundle maps to an underlying EC2 instance type. You can choose pre-configured **application blueprints** (WordPress, LAMP, Node.js, Django) that bootstrap the instance with the application stack pre-installed.' },
    { title: 'Managed Databases', description: '**Lightsail** managed databases run **MySQL** and **PostgreSQL** with automated backups, patching, and **high-availability (HA) plans** that add a standby replica for automatic failover. HA databases automatically failover in under 60 seconds. You connect using standard database clients and connection strings — no need to configure RDS subnet groups or parameter groups.' },
    { title: 'Static Sites and CDN', description: '**Lightsail** static site hosting deploys HTML/CSS/JS sites from GitHub repositories with automatic builds via **Lightsail distribution** (CDN). Distributions can front any origin (Lightsail instance, **S3**, or external URL) and are powered by **CloudFront** under the hood, supporting custom SSL certificates from **ACM** and geographic restrictions.' },
    { title: 'Container Services', description: '**Lightsail** container services run Docker containers without managing nodes. You push an image to the Lightsail container registry and deploy it as a service with a defined **scale** (number of nodes) and **power** (CPU/memory per node). Container services include a public HTTPS endpoint automatically. They are simpler than **ECS** but have fewer configuration options and no IAM task roles.' },
  ],
  approach: [
    { step: 'Start with the right blueprint to reduce setup time', details: 'Choose the application blueprint (WordPress, LAMP, Nginx, Node.js) that most closely matches your stack. Lightsail pre-installs and configures the software, sets up the web server, and provides a one-click SSL certificate wizard via Let\'s Encrypt. This eliminates 1-2 hours of manual server configuration.' },
    { step: 'Enable automated snapshots for backup', details: 'Enable automatic daily snapshots on all Lightsail instances and databases. Snapshots are stored at the regional level and can be used to restore a new instance or create a manual snapshot before a risky change. Set snapshot retention to 7 days for development instances and 30 days for production. Note that snapshot storage is charged separately from the instance bundle.' },
    { step: 'Use peering with VPC for AWS service access', details: 'If the Lightsail application needs to connect to resources in a VPC (RDS, ElastiCache, internal microservices), enable VPC Peering in the Lightsail console to peer the Lightsail network with the default VPC in the region. This gives Lightsail instances private IP connectivity to VPC resources without traversing the public internet.' },
    { step: 'Plan the EC2 migration path upfront', details: 'Lightsail instances can be exported to EC2 as Amazon Machine Images. If your workload is likely to grow beyond Lightsail\'s maximum bundle (32 vCPU, 64 GB), architect the application to be stateless from day one, store state in the managed database (not on the instance), and use S3 for file storage so the migration to EC2 requires only compute replacement.' },
  ],
  pitfalls: [
    { title: 'Treating Lightsail as a substitute for production AWS architectures', description: 'Lightsail lacks IAM instance roles, VPC subnet isolation, security groups with the full rule set, and deep integrations with services like SQS, ElastiCache, and ECS. Applications that start on Lightsail and grow into multi-tier architectures quickly hit these limits. Design a clear migration threshold and trigger the move to EC2/ECS before traffic makes the migration risky.' },
    { title: 'Exceeding the data transfer allowance', description: 'Each Lightsail bundle includes a monthly data transfer allowance. Outbound data beyond the allowance is charged per GB at standard rates, which can exceed the flat instance price for high-traffic sites. Use Lightsail distributions (CDN) to offload static asset delivery from the origin instance and cache at edge locations, dramatically reducing outbound transfer from the instance.' },
    { title: 'Single-instance production setup with no high availability', description: 'A single Lightsail instance with no load balancer or database HA plan is a single point of failure. For business-critical workloads, add a Lightsail Load Balancer with multiple instances and use the HA managed database plan. This adds cost but eliminates the 30-60 minute downtime of restoring from snapshot when a single instance fails.' },
  ],
  keyQuestions: [
    { q: 'When would you choose Lightsail over EC2, and what are the migration triggers?', a: '**Lightsail** is appropriate when simplicity and cost predictability outweigh flexibility: personal projects, small business websites, development sandboxes, and WordPress blogs with under 100K monthly visitors.\n\nMigration triggers to **EC2**:\n- Needing VPC placement with fine-grained security groups\n- Requiring IAM instance roles for AWS service access\n- Exceeding Lightsail bundle limits (160 GB SSD, 8 vCPU, 32 GB RAM max)\n- Needing **Auto Scaling Groups** for variable traffic\n- Building microservice architectures requiring **ALB** path-based routing and service discovery' },
    { q: 'How does Lightsail database HA work and how does failover occur?', a: '**Lightsail HA databases** provision a primary instance and a standby replica in different Availability Zones using the same underlying **RDS Multi-AZ** technology. Data is synchronously replicated to the standby.\n\nOn primary failure, the managed service promotes the standby to primary and updates the connection string DNS endpoint (CNAME) to point to the new primary. Failover completes in under 60 seconds. Your application must handle a brief connection drop; **reconnect logic with exponential backoff** is essential.' },
    { q: 'How do you serve a Lightsail WordPress site to global users with low latency?', a: '1. Attach a **Lightsail Distribution** (powered by **CloudFront**) to the Lightsail instance origin.\n2. Configure cache behaviors to cache static assets (images, CSS, JS) at edge locations for 7 days and pass-through dynamic WordPress requests uncached.\n3. Install a WordPress caching plugin (**W3 Total Cache** or **WP Super Cache**) on the origin to generate static HTML for anonymous users — these pages are then cached by the CDN.\n4. Attach a custom domain with an **ACM** SSL certificate via the distribution configuration.\n\nThis combination reduces origin load by 80-90% and serves global traffic from the nearest edge location.' },
    { q: 'What is the data transfer model in Lightsail and how do you control costs?', a: 'Each **Lightsail** bundle includes a fixed monthly **outbound data transfer allowance** (from 1 TB for the $3.50 plan to 7 TB for the $160 plan). Inbound transfer is always free. Transfer between Lightsail resources in the same region and AZ is also free. Outbound transfer beyond the allowance is charged at `$0.09/GB`.\n\nCost controls:\n- Attach a **Lightsail Distribution** to offload static asset delivery to CDN edge locations\n- Enable gzip compression on the web server to reduce payload sizes\n- Monitor the monthly transfer metric in the Lightsail console to detect anomalous traffic early' },
  ],
  references: [
    'https://docs.aws.amazon.com/lightsail/latest/userguide/',
    'https://aws.amazon.com/lightsail/pricing/',
  ],
},

{
  id: 'aws-s3',
  title: 'S3 — Simple Storage Service',
  icon: 'database',
  color: '#3b82f6',
  description: 'Infinitely scalable object storage for any type of data.',
  introduction: '**Amazon S3** stores objects — files of any type, up to 5 TB each — in named buckets. It provides 99.999999999% (11 nines) durability by replicating data across multiple AZs in a region. **S3** serves as the default data lake, static website host, deployment artifact store, backup target, and content delivery origin for the majority of AWS architectures.',
  whenToUse: 'Use S3 to store any data that does not require a file system or database: images, videos, documents, backups, log archives, ML training datasets, website assets, deployment artifacts, and data lake raw zones. S3 is the right choice when you need massive scalability, pay-per-use storage costs, built-in versioning and lifecycle management, and fine-grained access control.',
  keyConcepts: [
    { title: 'Object Model and Keys', description: '**S3** stores objects identified by a bucket name and key. Keys are flat strings — S3 has no real directory hierarchy, but the slash convention in keys allows the console and SDKs to present a folder-like view. Object size ranges from 0 bytes to 5 TB; objects over 100 MB should use **multipart upload** for reliability and performance.' },
    { title: 'Storage Classes', description: '- **S3 Standard** (99.99% availability, all AZs) for frequently accessed data.\n- **S3 Standard-IA** (infrequent access, lower storage cost, retrieval fee) for data accessed monthly.\n- **S3 One Zone-IA** for reproducible data tolerating single-AZ loss.\n- **S3 Glacier Instant Retrieval** for archives accessed quarterly.\n- **S3 Glacier Flexible Retrieval** for archives with 1-12 hour retrieval.\n- **S3 Glacier Deep Archive** for 7-10 year compliance archives at `$0.00099/GB-month`.' },
    { title: 'Bucket Policies and ACLs', description: '**Bucket policies** are JSON IAM-style policies attached directly to the bucket — the recommended way to control access. **ACLs** are legacy per-object access controls; AWS recommends disabling ACLs and using bucket policies exclusively. **Block Public Access** is a guardrail that overrides all policies to prevent public exposure, enabled by default on new accounts.' },
    { title: 'Versioning and Lifecycle', description: '**Versioning** preserves all versions of every object, enabling recovery from accidental deletion or overwrite. **Lifecycle rules** automatically transition objects between storage classes or expire (delete) objects after a defined number of days, based on object age, prefix, or tags. Combine versioning with lifecycle rules to retain current versions in **S3 Standard** and move old versions to **Glacier** after 90 days.' },
  ],
  approach: [
    { step: 'Design the bucket structure and naming convention', details: 'Create separate buckets per environment (dev, staging, prod) and per concern (raw-data, processed-data, artifacts, backups) rather than using prefix namespacing within a single bucket. This simplifies IAM policies, enables per-bucket versioning and lifecycle settings, and isolates production data from accidental writes by dev workloads.' },
    { step: 'Enable encryption, versioning, and logging from the start', details: 'Enable SSE-S3 or SSE-KMS encryption on every bucket at creation — it is free with SSE-S3 and adds milliseconds of latency overhead. Enable versioning on any bucket storing critical data. Enable S3 server access logging to an audit bucket for compliance and security investigation. Enable CloudTrail S3 data event logging for high-value buckets to record every object-level API call.' },
    { step: 'Implement lifecycle policies for cost control', details: 'Add lifecycle rules immediately for every bucket containing time-series or log data. A standard policy: transition to Standard-IA after 30 days, Glacier Instant Retrieval after 90 days, Glacier Deep Archive after 365 days, and expire after 2557 days (7 years). For versioned buckets, add rules to expire non-current versions after 90 days and abort incomplete multipart uploads after 7 days.' },
    { step: 'Optimize for high-throughput uploads and downloads', details: 'Use the AWS SDK multipart upload API for objects over 100 MB — it splits the object into parts uploaded in parallel, improving throughput and allowing resumption on failure. For downloads at scale, prefix the object keys with a random hash to distribute reads across S3 partitions. Use S3 Transfer Acceleration for uploads from geographically distant clients by routing through CloudFront edge locations.' },
  ],
  pitfalls: [
    { title: 'Accidentally making buckets or objects public', description: 'A single misconfigured bucket policy or disabled Block Public Access setting has caused numerous large data breaches. Always enable Block Public Access at the account level via S3 account settings. Use AWS Config rule s3-bucket-public-read-prohibited to alert on any bucket that becomes public. Audit bucket policies quarterly using IAM Access Analyzer for S3.' },
    { title: 'Not using multipart upload for large objects', description: 'Uploading a 5 GB file as a single PUT request fails if the connection drops partway through, requiring a full restart. Single-part uploads over 100 MB also saturate a single TCP connection. Always use multipart upload (AWS SDK handles this automatically via TransferManager) for objects above 100 MB to enable parallel chunked upload and resumable uploads.' },
    { title: 'Ignoring S3 storage costs by leaving all data in Standard', description: 'S3 Standard at $0.023/GB-month accumulates significant cost for cold data in logs, backups, and archives. A 100 TB log archive in Standard costs $2,300/month; in Glacier Deep Archive it costs $99/month. Always attach lifecycle rules at bucket creation, before data accumulates, rather than retroactively when the S3 bill arrives.' },
  ],
  keyQuestions: [
    { q: 'How does S3 achieve 11 nines of durability?', a: '**S3 Standard** stores data by automatically replicating each object across a minimum of three Availability Zones within a region. When you write an object, **S3** stores it on multiple devices across multiple AZs before returning success to the client.\n\nAnnual object durability of `99.999999999%` means losing one object per 100 billion objects stored per year. This is **not backup** — it is redundancy against hardware failure. For protection against accidental deletion or data corruption, use **versioning** and **Cross-Region Replication**.' },
    { q: 'How do you secure an S3 bucket used as a static website origin for CloudFront?', a: '1. Create a **CloudFront Origin Access Control** (OAC, the successor to OAI) and attach it to the distribution origin configuration.\n2. Set the bucket policy to allow `s3:GetObject` only from the CloudFront service principal with the OAC condition key `aws:SourceArn` pointing to the specific distribution ARN.\n3. Enable **Block Public Access** on the bucket so objects are never directly accessible via the S3 website endpoint.\n\nThis ensures all traffic flows through **CloudFront** — enforcing WAF rules, HTTPS, and signed URL/cookie access controls.' },
    { q: 'What are the consistency guarantees in S3?', a: 'Since December 2020, **S3** provides **strong read-after-write consistency** for all object operations in all regions: after a successful `PUT`, any subsequent `GET` or `LIST` returns the new version immediately.\n\nThis applies to both new objects and overwrites, and to `LIST` operations — a `LIST` after a `PUT` will include the new key. There is no longer a need for workarounds like **DynamoDB** conditional writes or random suffixes to enforce consistency.' },
    { q: 'How do you implement fine-grained access control for a multi-tenant S3 data lake?', a: 'Organize the bucket with a prefix structure per tenant: `tenants/{tenant_id}/raw/` and `tenants/{tenant_id}/processed/`. Create an IAM policy per tenant with `s3:GetObject` and `s3:PutObject` scoped to their prefix using the `aws:PrincipalTag` or explicit prefix condition.\n\nUse **S3 Access Points** — one per tenant — each with its own access policy scoping access to the tenant prefix. Access Points expose a unique hostname, allowing the application to pass the tenant-specific endpoint rather than the bucket URL, making access control enforcement transparent to the application.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonS3/latest/userguide/',
    'https://aws.amazon.com/s3/storage-classes/',
    'https://docs.aws.amazon.com/AmazonS3/latest/userguide/access-control-best-practices.html',
  ],
  visualizations: [
    { title: 'S3 Storage Classes', caption: 'Lifecycle progression from Standard through Standard-IA, Glacier Instant, Glacier Flexible, and Deep Archive, with cost/availability tradeoffs at each tier.', image: '/diagrams/cloud/aws-s3-storage-classes.png' },
    { title: 'S3 Event-Driven Architecture', caption: 'How an S3 PutObject triggers fan-out to SNS, SQS, Lambda, and EventBridge for real-time processing, alerting, and batch ETL pipelines.', image: '/diagrams/cloud/aws-s3-event-flow.png' },
  ],
},

{
  id: 'aws-s3-advanced',
  title: 'S3 Advanced — Event Notifications, Replication, and Analytics',
  icon: 'database',
  color: '#3b82f6',
  description: 'S3 event-driven pipelines, Cross-Region Replication, Intelligent-Tiering, and S3 Select.',
  introduction: 'Beyond basic storage, **S3** offers powerful capabilities for building event-driven data pipelines, geo-redundant replication, intelligent cost optimization, and serverless SQL queries against stored data. These advanced features enable **S3** to serve as the foundation for data lakes, disaster recovery architectures, and real-time processing pipelines without additional infrastructure.',
  whenToUse: 'Apply advanced S3 patterns when building event-driven ETL pipelines triggered on object upload, replicating data across regions for disaster recovery or data sovereignty compliance, optimizing storage costs across unpredictable access patterns with Intelligent-Tiering, or querying subsets of large CSV and JSON files without downloading them entirely.',
  keyConcepts: [
    { title: 'S3 Event Notifications', description: '**S3** can publish events (`ObjectCreated`, `ObjectRemoved`, `ObjectRestore`, `Replication`) to **SNS**, **SQS**, **Lambda**, or **EventBridge**. Lambda trigger integration invokes a function synchronously on every matching object operation. **EventBridge** integration sends all events to the default event bus, enabling complex routing to multiple targets and content-based filtering without code. Use **EventBridge** as the preferred destination for new architectures.' },
    { title: 'Cross-Region Replication (CRR) and Same-Region Replication (SRR)', description: 'Replication rules copy objects from a source bucket to a destination bucket in the same or different region, optionally filtering by prefix or tag. **CRR** addresses disaster recovery, data sovereignty, and latency reduction for geographically distributed consumers. **SRR** addresses compliance (maintaining a second copy in the same region), log aggregation across accounts, and test/production data synchronization. **Replication does not copy existing objects** — only objects written after enabling replication are replicated.' },
    { title: 'S3 Intelligent-Tiering', description: '**S3 Intelligent-Tiering** monitors object access patterns and automatically moves objects between **Frequent Access** and **Infrequent Access** tiers at no retrieval fee. Optional deep archive tiers (`Archive Access`, `Deep Archive Access`) activate after 90 and 180 days of no access with a 12-hour retrieval time. It adds a small per-object monitoring fee (`$0.0025/1000 objects/month`) but eliminates the need to manually tune lifecycle policies for unpredictable access patterns.' },
    { title: 'S3 Select and Glacier Select', description: '**S3 Select** allows SQL-like queries (`SELECT`, `WHERE`, `LIMIT`) against individual CSV, JSON, or Parquet objects stored in S3, returning only the matching rows rather than downloading the entire object. This can reduce data transfer and processing time by 400% for large files. It is not a query engine — **Athena** handles cross-file SQL — but it is powerful for filtering a specific S3 object before processing in **Lambda** or **EC2**.' },
  ],
  approach: [
    { step: 'Build event-driven pipelines with S3 and EventBridge', details: 'Enable EventBridge notifications on the source bucket. Create an EventBridge rule matching s3:ObjectCreated:Put events filtered by prefix and suffix (e.g., prefix: uploads/, suffix: .csv). Route matching events to a Step Functions state machine that validates, transforms, and loads the data. This pattern replaces polling-based batch pipelines with sub-second triggered processing and provides full observability through EventBridge event history.' },
    { step: 'Configure CRR for multi-region disaster recovery', details: 'Enable versioning on both source and destination buckets (required for replication). Create a replication rule with a destination bucket in a second region, an IAM role with s3:ReplicateObject permissions, and optionally enable Replication Time Control (RTC) for a 15-minute SLA on replication lag with metrics. Test failover by pointing application configuration to the replica bucket URL and verifying read operations succeed before a real disaster.' },
    { step: 'Apply S3 Inventory for large-bucket management', details: 'Enable S3 Inventory to generate daily or weekly flat-file reports (CSV or Parquet) listing all objects with metadata: key, size, last modified, storage class, replication status, and encryption. Use Athena to query Inventory files to find objects not yet transitioned to Glacier, identify unreplicated objects, or audit encryption compliance across hundreds of millions of objects without SDK list operations.' },
    { step: 'Use S3 Object Lambda for on-the-fly transformation', details: 'S3 Object Lambda intercepts GetObject requests and invokes a Lambda function to transform the object before returning it to the caller. Use cases: redacting PII from CSV files before serving to analytics users, resizing images on demand, converting XML to JSON, or adding row-level security filters. The caller sees a standard S3 endpoint; the transformation is transparent and centrally managed.' },
  ],
  pitfalls: [
    { title: 'Assuming replication is synchronous and immediate', description: 'Standard S3 replication is asynchronous and best-effort — replication lag ranges from seconds to hours depending on object size and regional replication traffic. Never assume the replica is current for transactional use cases. For applications where near-synchronous replication is required, enable Replication Time Control (RTC) which provides a 15-minute SLA and publishes replication lag metrics to CloudWatch.' },
    { title: 'Missing the existing-objects limitation on replication', description: 'When you enable a replication rule, it applies only to objects written after the rule is created. Existing objects are not retroactively replicated. Use S3 Batch Replication to trigger replication of existing objects explicitly, specifying the bucket, prefix, and destination in a Batch Operations job. This is a common gotcha when teams enable CRR and assume the replica bucket contains a full copy of the source.' },
    { title: 'Excessive small-file S3 event triggers causing Lambda throttling', description: 'A pipeline that writes millions of small files per hour to S3 triggers millions of Lambda invocations simultaneously, hitting the account concurrency limit and causing invocation throttling. Buffer small events by routing S3 event notifications to SQS with a 5-minute batch window before triggering Lambda, dramatically reducing invocation frequency and enabling batch processing of multiple files per invocation.' },
  ],
  keyQuestions: [
    { q: 'How does S3 Replication Time Control work and when do you need it?', a: '**S3 Replication Time Control (RTC)** is an add-on for **CRR** and **SRR** that provides a contractual 99.99% SLA for replicating 99.99% of objects within 15 minutes. Without RTC, replication completes within seconds for most objects but has no SLA for delays caused by service disruptions.\n\nRTC publishes `ReplicationLatency` and `BytesPendingReplication` metrics to **CloudWatch**, enabling alarms on replication lag. Use RTC for compliance-driven DR requirements that specify a maximum recovery point objective under 15 minutes, and for regulated industries (financial, healthcare) where replication SLA must be demonstrable.' },
    { q: 'How would you design an S3-based data lake with fine-grained access control?', a: 'Structure the lake in zones: `raw/`, `cleaned/`, `curated/`, and `sandbox/` prefixes in one or more S3 buckets. Use **S3 Access Points** to create zone-specific endpoints with policies restricting which IAM roles can read and write each zone.\n\nRegister the bucket with **AWS Lake Formation** to apply column-level and row-level access controls on top of S3, enforced when **Athena**, **Redshift Spectrum**, or **Glue** read the data. Store metadata in the **Glue Data Catalog**. Grant permissions through **Lake Formation** rather than raw IAM policies — this provides a centralized, database-style access control model over flat S3 objects.' },
    { q: 'How do you optimize S3 for high-request-rate workloads?', a: '**S3** automatically partitions the request load across its infrastructure based on the key prefix. S3 can handle 3,500 `PUT`/`POST`/`DELETE` and 5,500 `GET` requests per second per prefix with no prefetching.\n\nFor workloads exceeding these limits:\n- Distribute objects across multiple prefixes by adding a random hash or date component at the beginning of the key name\n- Avoid sequential numeric prefixes which cause hot partitions\n- For read-heavy workloads, add a **CloudFront** distribution in front of S3 to cache frequently accessed objects at edge locations' },
    { q: 'What is S3 Object Lambda and how does it differ from Lambda@Edge?', a: '**S3 Object Lambda** intercepts S3 `GetObject` API calls and routes them through a Lambda function before returning data to the client — transforming stored data on-read without storing multiple copies. **Lambda@Edge** intercepts CloudFront HTTP requests at edge locations, enabling request/response transformation for cached web content.\n\n- Use **S3 Object Lambda** to serve different views of the same stored data to different consumers (redacted for analytics, full for admin) without ETL duplication.\n- Use **Lambda@Edge** for URL rewriting, authentication at the CDN edge, and A/B testing on web traffic.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonS3/latest/userguide/replication.html',
    'https://docs.aws.amazon.com/AmazonS3/latest/userguide/EventNotifications.html',
    'https://docs.aws.amazon.com/AmazonS3/latest/userguide/object-lambda.html',
  ],
},

{
  id: 'aws-ebs',
  title: 'EBS — Elastic Block Store',
  icon: 'database',
  color: '#3b82f6',
  description: 'High-performance block storage volumes for EC2 instances.',
  introduction: 'Amazon EBS provides persistent block storage volumes that attach to EC2 instances like virtual hard drives. EBS volumes persist independently from the instance lifecycle, support snapshots to S3 for backup and AMI creation, can be resized online, and offer multiple volume types optimized for different performance and cost tradeoffs. They are the primary storage layer for databases, boot volumes, and applications requiring low-latency block I/O.',
  whenToUse: 'Use EBS for EC2 instance boot volumes, databases running on EC2 (MySQL, PostgreSQL, Cassandra, MongoDB), applications requiring low-latency block storage, and any workload that needs POSIX file system semantics on a persistent single-instance volume. For shared access across multiple instances, use EFS instead.',
  keyConcepts: [
    { title: 'Volume Types', description: 'gp3 (General Purpose SSD) is the default: 3,000 IOPS and 125 MB/s baseline, independently configurable up to 16,000 IOPS and 1,000 MB/s, at $0.08/GB-month. io2 Block Express (Provisioned IOPS SSD) delivers up to 256,000 IOPS and 4,000 MB/s for I/O-intensive databases — priced at $0.125/GB plus $0.065/provisioned IOPS. st1 (Throughput Optimized HDD) suits sequential workloads (data warehouses, Kafka) at $0.045/GB. sc1 (Cold HDD) is cheapest ($0.015/GB) for archives read infrequently.' },
    { title: 'Multi-Attach', description: 'io1 and io2 volumes support Multi-Attach, allowing attachment to up to 16 Nitro-based EC2 instances in the same AZ simultaneously. All attached instances can read and write concurrently. The application or cluster software (shared file system, database cluster) must manage concurrent write coordination — EBS provides no built-in write ordering or locking above the block level.' },
    { title: 'Snapshots', description: 'EBS Snapshots are incremental backups stored in S3 (regionally managed, not in your buckets). The first snapshot copies all data; subsequent snapshots copy only changed blocks. Snapshots can be copied to other regions, shared with other accounts, and used to create new volumes or AMIs. Snapshot creation is non-disruptive — the volume remains available during the snapshot.' },
    { title: 'Performance Characteristics', description: 'EBS volume performance is constrained by both the volume type limits and the EC2 instance EBS bandwidth limits. An instance with 2.5 Gbps EBS bandwidth cannot saturate a gp3 volume configured for 1,000 MB/s. Always check the instance\'s maximum EBS bandwidth in the EC2 documentation when designing for high-throughput workloads. EBS-optimized instances use dedicated bandwidth for EBS traffic, separate from network bandwidth.' },
  ],
  approach: [
    { step: 'Choose the right volume type and size for each workload', details: 'Use gp3 for all general-purpose workloads: boot volumes, application servers, and databases under 16,000 IOPS. Configure IOPS and throughput independently to match the workload profile — start with CloudWatch VolumeReadOps, VolumeWriteOps, and VolumeThroughputBytes metrics from a prior run. Use io2 Block Express only for tier-1 relational databases (Oracle, SQL Server) that require sub-millisecond consistent latency and over 64,000 IOPS.' },
    { step: 'Implement automated snapshot management', details: 'Use Amazon Data Lifecycle Manager (DLM) to create snapshot policies: daily snapshots at 2 AM, retain the last 7 daily snapshots, cross-copy to a DR region weekly. Tag volumes with environment and application labels and use DLM tag-based policies to apply different retention rules per tier. Test restore procedures quarterly by restoring a snapshot to a new volume and verifying data integrity.' },
    { step: 'Monitor for volume performance limits', details: 'Create CloudWatch alarms on VolumeQueueLength (queue depth) exceeding 1 per 1,000 IOPS provisioned, on BurstBalance below 20% for gp2 volumes (indicator of IOPS exhaustion), and on VolumeThroughputPercentage and VolumeConsumedReadWriteOps at 80% of provisioned limits. High queue depth is the primary symptom of I/O saturation causing application latency spikes.' },
    { step: 'Resize volumes online without downtime', details: 'EBS volumes can be resized, volume type changed, and IOPS adjusted on a live attached volume without detaching or stopping the instance. After modifying the volume in the console or CLI, wait for the modification to complete (ModificationState = completed), then extend the file system: growpart and resize2fs for ext4, or xfs_growfs for XFS. No downtime is required. Note that volume modifications can only be made once every 6 hours.' },
  ],
  pitfalls: [
    { title: 'Leaving gp2 volumes instead of migrating to gp3', description: 'gp2 volumes burst IOPS from a credit bucket tied to volume size (3 IOPS/GB, max 16,000). Large volumes burn through burst credits and throttle to baseline. gp3 always provides 3,000 IOPS regardless of size, with additional IOPS configurable independently — and costs 20% less per GB than gp2. Migrate all gp2 volumes to gp3 using the online volume modification API at no downtime cost.' },
    { title: 'Provisioning IOPS without checking instance EBS bandwidth limits', description: 'A 64,000-IOPS io2 volume attached to an m5.large (2.88 Gbps EBS bandwidth, ~18,000 IOPS max) cannot deliver its provisioned IOPS because the instance is the bottleneck. Always verify the instance\'s maximum EBS bandwidth and baseline/burst IOPS in the EC2 documentation before provisioning a high-IOPS volume, and right-size the instance to match the volume performance target.' },
    { title: 'Not encrypting EBS volumes containing sensitive data', description: 'Unencrypted EBS snapshots shared accidentally or through a configuration error expose data in plaintext. Enable EBS encryption by default at the account and region level in the EC2 console to automatically encrypt all new volumes and snapshots. Use a customer-managed KMS key to maintain control over key rotation and access auditing. Encrypting an existing unencrypted volume requires creating an encrypted snapshot copy and creating a new volume from it.' },
  ],
  keyQuestions: [
    { q: 'How do you choose between gp3 and io2 for a PostgreSQL database?', a: 'For most PostgreSQL deployments, gp3 is the correct choice: it provides 3,000 to 16,000 configurable IOPS at $0.08/GB plus $0.005 per provisioned IOPS above 3,000, covering the vast majority of production database workloads. io2 Block Express is appropriate only when you need consistent sub-millisecond latency at high queue depths (throughput above 1,000 MB/s or IOPS above 64,000) — typical for tier-1 OLTP databases with hundreds of concurrent transactions per second. Use CloudWatch VolumeQueueLength and read/write latency metrics to identify whether a gp3 volume is being saturated before committing to io2 pricing.' },
    { q: 'How do EBS snapshots work and how do you restore from one?', a: 'EBS snapshots are incremental: the first snapshot copies all allocated blocks to S3; each subsequent snapshot copies only the blocks changed since the previous snapshot. Despite incremental storage, each snapshot is self-consistent and can independently restore a full volume — AWS assembles the full volume from the snapshot chain transparently. To restore, create a new EBS volume from the snapshot in the target AZ, attach it to an instance, and mount the file system. The restore is immediate — the volume is accessible before all data is copied, with background lazy loading of blocks from S3 as they are accessed (EBS Fast Snapshot Restore pre-warms the volume for immediate full performance).' },
    { q: 'What is the EBS Multi-Attach feature and what are its constraints?', a: 'Multi-Attach allows a single io1 or io2 volume to be attached to up to 16 EC2 Nitro instances simultaneously in the same AZ. All instances have read/write access to the volume at the block level. The critical constraint is that EBS does not enforce any write ordering, locking, or consistency above the raw block layer — the application must manage concurrent access. This makes Multi-Attach suitable only for cluster-aware applications like databases that implement their own distributed locking (Oracle RAC, HANA) or shared file systems (GFS2, OCFS2). Using Multi-Attach with a standard file system like ext4 or XFS will cause data corruption.' },
    { q: 'How do you migrate a production EC2 instance to a new EBS volume type without downtime?', a: 'Use the EBS Modify Volume API (or console) to change the volume type from gp2 to gp3 on the live attached volume. The modification runs in the background without detaching the volume or stopping the instance. Monitor the ModificationState field — it transitions from modifying to optimizing to completed over 15-30 minutes for most volumes. No file system changes are required for a type-only change. If also increasing size, after the modification completes, run lsblk to confirm the OS sees the new size, use growpart to expand the partition, and resize2fs or xfs_growfs to expand the file system. The entire process requires zero downtime.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/AmazonEBS.html',
    'https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebs-volume-types.html',
    'https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebs-modifying-snapshot-permissions.html',
  ],
},

{
  id: 'aws-efs',
  title: 'EFS — Elastic File System',
  icon: 'database',
  color: '#3b82f6',
  description: 'Serverless, elastic NFS file system shared across multiple EC2 instances and containers.',
  introduction: 'Amazon EFS provides a scalable, fully managed NFS file system that multiple EC2 instances, ECS tasks, Lambda functions, and EKS pods can mount simultaneously. Capacity grows and shrinks automatically as files are added and removed. EFS eliminates the need to provision storage upfront and removes the single-attachment constraint of EBS, making it ideal for shared workloads.',
  whenToUse: 'Use EFS when multiple compute resources need concurrent access to the same file system: web tier servers sharing uploaded content, container tasks sharing config files, ML training jobs reading a shared dataset, home directories for multi-user systems, and CMS or WordPress installations sharing media. For single-instance block storage, EBS is more cost-effective and delivers lower latency.',
  keyConcepts: [
    { title: 'Performance Modes', description: 'General Purpose mode (default) optimizes for latency-sensitive workloads — first I/O is under 1ms. Max I/O mode scales to higher aggregate throughput and IOPS at the cost of slightly higher per-operation latency — suitable for highly parallelized big data workloads with hundreds of concurrent clients. Performance mode is set at creation and cannot be changed.' },
    { title: 'Throughput Modes', description: 'Bursting Throughput (default) provides throughput that scales with file system size using a credit bucket model. Elastic Throughput (recommended) automatically scales throughput from 100 MB/s to 10 GB/s based on workload with no provisioning — billed per GB transferred. Provisioned Throughput fixes a throughput value independent of storage size, useful for small file systems with high throughput requirements.' },
    { title: 'Storage Classes and Lifecycle', description: 'EFS Standard stores data across multiple AZs for 99.99% availability. EFS Standard-IA (Infrequent Access) costs 91% less per GB-month but charges a retrieval fee per GB read. EFS Intelligent-Tiering automatically moves files not accessed for 30 days to IA and returns them to Standard on first access. For One Zone storage classes, data is stored in a single AZ at lower cost, suitable for dev/test.' },
    { title: 'Access Points', description: 'EFS Access Points are application-specific entry points into an EFS file system with a specific root directory path, enforced POSIX user identity (UID/GID), and permissions. Containerized applications use Access Points to restrict each service to its own directory within a shared file system, preventing cross-service data access without managing separate file systems.' },
  ],
  approach: [
    { step: 'Deploy mount targets in every AZ', details: 'Create an EFS mount target in each Availability Zone of the VPC where clients will run. Each mount target has a private IP and a security group that allows NFS (port 2049) from client security groups. EFS clients automatically connect to the mount target in their AZ, keeping traffic within the AZ for latency and avoiding inter-AZ data transfer costs.' },
    { step: 'Use Amazon EFS mount helper and TLS', details: 'Install the amazon-efs-utils package and mount with efs mount type instead of nfs4: mount -t efs -o tls fs-xxxxxx:/ /mnt/efs. The mount helper handles NFS mount options, automatic TLS encryption in transit, and stunnel configuration. Add _netdev to fstab options to ensure the mount completes after network initialization on reboot.' },
    { step: 'Enable Intelligent-Tiering for cost optimization', details: 'Enable EFS Lifecycle Management with a 30-day transition policy to automatically move cold files to EFS IA, reducing storage costs by 85-91% for infrequently accessed data. Intelligent-Tiering adds transparency by also auto-transitioning files back to Standard on access. Monitor EFS DataReadIOBytes and DataWriteIOBytes metrics split by storage class to verify tiering is working as expected.' },
    { step: 'Set up Access Points for container workloads', details: 'Create one Access Point per service or team, each rooted at a dedicated directory within the file system (e.g., /app1, /app2). Set the posixUser UID/GID and rootDirectory creationInfo to automatically create the directory with correct ownership. Mount the Access Point in ECS task definitions or Kubernetes PersistentVolumes using the EFS CSI driver, ensuring each container can only access its designated path.' },
  ],
  pitfalls: [
    { title: 'Mounting EFS from a different AZ and incurring latency and transfer charges', description: 'EFS clients connecting to a mount target in a different AZ experience 1-3ms additional latency compared to same-AZ mounts and incur EC2 inter-AZ data transfer charges ($0.01/GB in each direction). Always create mount targets in every AZ you operate in and verify that clients are connecting to their local AZ mount target using the regional DNS name which automatically resolves to the closest mount target.' },
    { title: 'Using Bursting Throughput mode for CI/CD or ML workloads', description: 'Bursting Throughput builds credits at 50 KB/s per GB of storage size. A 10 GB file system builds only 500 KB/s of credit — far too little for a CI/CD system running build jobs. When the credit bucket drains, throughput drops to the 100 KB/s minimum baseline, causing builds to stall. Switch to Elastic Throughput which delivers burst throughput on demand without requiring a large file system size to unlock it.' },
    { title: 'Storing millions of small files causing metadata IOPS exhaustion', description: 'EFS IOPS in General Purpose mode is limited to 35,000 for metadata operations (open, close, getattr, readdir). A workload creating millions of small files rapidly saturates this limit, causing operations to slow dramatically. Batch small files into archives, use Max I/O performance mode for highly concurrent metadata access, or redesign to store small objects in S3 with metadata in DynamoDB instead of a POSIX file system.' },
  ],
  keyQuestions: [
    { q: 'How does EFS differ from EBS and S3, and when do you choose each?', a: 'EBS is a block device attached to a single EC2 instance (or up to 16 with Multi-Attach on io2), offering the lowest latency and highest IOPS for single-instance workloads like databases. EFS is a shared POSIX-compliant NFS file system concurrently accessible from hundreds of instances with automatic scaling — the right choice when multiple compute resources need to read and write the same files. S3 is object storage accessed via HTTP APIs with no file system semantics, unlimited scale, and the lowest cost per GB — the right choice for data that does not require POSIX semantics, like backups, data lakes, and static assets. Never use EFS as a database storage layer; use EBS for databases.' },
    { q: 'How do you secure an EFS file system shared between multiple teams?', a: 'Create separate EFS Access Points for each team, each with a dedicated root directory path and a POSIX user configuration that enforces a team-specific UID/GID regardless of what the client application requests. Attach an EFS resource policy that allows mount only from specific IAM roles or VPC security groups, blocking access from unauthorized EC2 instances. Enable EFS encryption at rest using KMS and enforce TLS in transit using the efs mount helper tls option. Use CloudTrail EFS data access logging to audit which IAM principals are accessing which paths.' },
    { q: 'How would you troubleshoot high EFS latency for an application workload?', a: 'First check the EFS ClientConnections, TotalIOBytes, PercentIOLimit, and BurstCreditBalance metrics in CloudWatch. A PercentIOLimit above 80% or a BurstCreditBalance trending toward zero indicates the file system is approaching IOPS limits. If using General Purpose mode near the limit, switch to Max I/O mode (requires a new file system and data migration). Verify clients are mounting to the same-AZ mount target — cross-AZ mounts add 1-3ms per operation which accumulates for metadata-intensive workloads. Profile the application with strace or inotifywait to determine if it is generating excessive metadata operations (open, stat, readdir) that should be cached application-side.' },
    { q: 'How do you mount EFS in an EKS pod using the EFS CSI driver?', a: 'Install the AWS EFS CSI Driver as an EKS add-on or via Helm. Create an IAM role with IRSA for the driver service account, granting elasticfilesystem:DescribeFileSystems and elasticfilesystem:DescribeMountTargets permissions. Create a StorageClass referencing the EFS file system ID and the provisioner efs.csi.aws.com. Create a PersistentVolumeClaim using the StorageClass — the CSI driver automatically creates an EFS Access Point and returns a PersistentVolume. Mount the PVC in the pod spec. Pods across all nodes in any AZ mount the same EFS file system through AZ-local mount targets via the CSI driver.' },
  ],
  references: [
    'https://docs.aws.amazon.com/efs/latest/ug/',
    'https://docs.aws.amazon.com/efs/latest/ug/performance.html',
    'https://github.com/kubernetes-sigs/aws-efs-csi-driver',
  ],
},

{
  id: 'aws-fsx',
  title: 'FSx — High-Performance Managed File Systems',
  icon: 'database',
  color: '#3b82f6',
  description: 'Fully managed Windows, Lustre, NetApp ONTAP, and OpenZFS file systems.',
  introduction: 'Amazon FSx provides fully managed third-party file systems optimized for specific workloads: FSx for Windows File Server for SMB-based Windows applications, FSx for Lustre for high-performance computing and ML workloads, FSx for NetApp ONTAP for enterprise NAS migration, and FSx for OpenZFS for Linux file system workloads. FSx handles hardware provisioning, patching, and backups.',
  whenToUse: 'Use FSx for Windows File Server when migrating Windows applications requiring SMB shares, Active Directory integration, or Windows-native file system features. Use FSx for Lustre for HPC, ML training on large datasets, and genomics workloads requiring hundreds of GB/s throughput and millions of IOPS. Use FSx for ONTAP when migrating NetApp NAS workloads or needing multi-protocol access (NFS, SMB, iSCSI).',
  keyConcepts: [
    { title: 'FSx for Lustre', description: 'Lustre is an open-source parallel distributed file system designed for HPC. FSx for Lustre delivers hundreds of GB/s of throughput and millions of IOPS with sub-millisecond latency. It integrates natively with S3 — a file system can be linked to an S3 bucket as a data repository, transparently presenting S3 objects as files and lazily loading them on first access. Export jobs write modified files back to S3. Ideal for ML training on datasets too large for EBS.' },
    { title: 'FSx for Windows File Server', description: 'Built on Windows Server 2019, this file system supports the full SMB protocol suite, Windows ACLs, Distributed File System (DFS) namespaces, shadow copies (VSS snapshots), and Active Directory integration for user authentication. Deploy in Single-AZ (lower cost) or Multi-AZ configurations (automatic failover in under 30 seconds). Accessible from Windows EC2 instances, FSx File Gateway on-premises, and via SMB from Linux clients.' },
    { title: 'FSx for NetApp ONTAP', description: 'ONTAP file systems support NFS, SMB, and iSCSI protocols simultaneously. They support ONTAP features: snapshots, cloning, compression, deduplication, tiering to S3, and SnapMirror replication. Multi-protocol access enables the same data to be accessed by Windows and Linux clients concurrently. This makes FSx ONTAP the choice for enterprise organizations lifting-and-shifting NetApp NAS infrastructure.' },
    { title: 'Storage and Throughput Capacity', description: 'FSx for Lustre offers Scratch (temporary, no replication, cheaper) and Persistent file systems (replicated within AZ, for long-running workloads). Throughput scales from 50 MB/s to 2 GB/s per TiB of storage. For Windows File Server, throughput capacity is configured independently from storage, from 8 MB/s to 2,048 MB/s. ONTAP SVM volumes can be tiered: hot data on SSD, cold data automatically tiered to S3.' },
  ],
  approach: [
    { step: 'Link FSx for Lustre to S3 for ML data lakes', details: 'Create an FSx for Lustre Persistent file system linked to the S3 bucket containing your training dataset. Set the import path and export path. On first access, FSx lazily loads the file from S3 to the Lustre SSD — the application sees standard POSIX file access. After training, use the ExportPath to write model checkpoints back to S3. This pattern eliminates pre-copy time for large datasets (hundreds of GB) versus loading from S3 into local EBS.' },
    { step: 'Deploy FSx for Windows in Multi-AZ for production', details: 'Create a Multi-AZ Windows File Server deployment with a preferred and standby file server in separate AZs. AWS manages failover automatically in under 30 seconds on preferred file server failure. Integrate with AWS Managed Microsoft AD for user authentication using domain join during file system creation. Use DFS namespaces to abstract file share paths from the underlying file server hostname, enabling transparent failover for mapped drives.' },
    { step: 'Right-size throughput capacity for Windows File Server', details: 'Throughput capacity for FSx Windows is configured in MB/s and determines the file system IOPS. Start by profiling the current on-premises NAS using Windows Performance Monitor to capture peak read/write MB/s and IOPS. Set FSx throughput to 20% above the observed peak. Use CloudWatch FSx metrics (DataReadBytes, DataWriteBytes) after migration to validate the selection and adjust if needed.' },
    { step: 'Use FSx ONTAP clones for dev/test environments', details: 'FSx for ONTAP supports volume clones via FlexClone — instant, space-efficient copies of a production volume. Provision production data once and create thin-provisioned clones for each developer or test pipeline. Clones share unchanged blocks with the source (space-efficient) and diverge as writes occur. This enables developers to work against realistic production-scale datasets without duplicating storage costs.' },
  ],
  pitfalls: [
    { title: 'Using FSx Lustre Scratch for long-running training jobs', description: 'FSx Lustre Scratch file systems are not replicated within the AZ and are designed for temporary processing. A hardware failure can lose all data on a Scratch file system. For ML training runs lasting more than a few hours, use a Persistent file system (replicated SSD storage with 99.9% availability SLA) or checkpoint model state to S3 frequently to minimize the cost of a restart.' },
    { title: 'Forgetting to export modified files back to S3 from Lustre', description: 'Files created or modified on an FSx Lustre file system linked to S3 are not automatically written back to the S3 data repository. Explicitly run an ExportPath data repository task (or the Lustre hsm_archive command) to flush modified data back to S3 before terminating the file system. Without this step, all work written during a training job is lost when the file system is deleted.' },
    { title: 'Undersizing Windows File Server throughput for SMB metadata operations', description: 'SMB file share workloads generate heavy metadata traffic (file open, close, directory list, rename) in addition to read/write I/O. Throughput capacity controls both data I/O and metadata IOPS on FSx for Windows. A file system configured for data I/O requirements alone may throttle on metadata-heavy office productivity workloads. Include metadata operation rates in the sizing analysis and choose a throughput tier that covers both data and metadata traffic.' },
  ],
  keyQuestions: [
    { q: 'How does FSx for Lustre integrate with S3 and why does it matter for ML workloads?', a: 'An FSx for Lustre file system linked to an S3 bucket transparently presents S3 objects as files in a POSIX namespace. When a training job opens a file, Lustre checks if the file is cached on SSD; on a cache miss, it lazily loads the data from S3 in the background while returning partial data to the requesting process. This eliminates the need to pre-copy training datasets from S3 to EBS before a job starts — a process that can take hours for multi-TB datasets. The result is that training jobs start immediately and read data at Lustre speeds (up to 2 GB/s per TiB) as the working set warms. After training, modified files (model checkpoints, output data) are exported back to S3 via a data repository task.' },
    { q: 'How does FSx for Windows compare to building a Windows File Server on EC2?', a: 'A self-managed Windows File Server on EC2 requires provisioning EC2 instances, configuring Windows Storage Spaces or DFS Replication for HA, managing OS patching, handling SMB server configuration, setting up VSS for backups, and operating Active Directory integration — all manual operational work. FSx for Windows automates all of this: automatic patching, Multi-AZ failover in under 30 seconds, native AD integration, VSS shadow copies with one-click restore, and AWS Backup integration. The tradeoff is less configuration flexibility and higher cost per GB compared to DIY, but the operational overhead savings are significant for teams without dedicated Windows storage expertise.' },
    { q: 'When would you choose FSx for ONTAP over FSx for Windows File Server?', a: 'Choose FSx for ONTAP when the workload requires multi-protocol access — Linux clients using NFS and Windows clients using SMB accessing the same data simultaneously — which ONTAP natively supports and Windows File Server does not. ONTAP is also preferred for organizations migrating from on-premises NetApp NAS who want to preserve ONTAP tooling, SnapMirror replication policies, and data management workflows without rewriting procedures. Additionally, if the workload requires iSCSI block storage alongside file access from the same managed service, ONTAP is the only FSx variant that supports it.' },
    { q: 'How do you design an HPC cluster on AWS using FSx for Lustre?', a: 'Deploy FSx for Lustre (Persistent HDD or SSD tier) linked to an S3 data repository containing input datasets. Use AWS ParallelCluster or EKS to provision a head node and compute nodes on EC2 Spot Instances (for cost) or p4d instances (for GPU-accelerated jobs). Mount the FSx file system on all compute nodes at launch via the FSx Lustre client in the AMI. The parallel file system delivers consistent high throughput to all compute nodes simultaneously — Lustre was designed for this concurrent-reader model. Store checkpoints and final results to the linked S3 bucket via data repository tasks. Terminate the compute nodes and file system after the job completes; the data persists in S3.' },
  ],
  references: [
    'https://docs.aws.amazon.com/fsx/latest/LustreGuide/',
    'https://docs.aws.amazon.com/fsx/latest/WindowsGuide/',
    'https://docs.aws.amazon.com/fsx/latest/ONTAPGuide/',
  ],
},

{
  id: 'aws-s3-glacier',
  title: 'S3 Glacier — Archive Storage',
  icon: 'archive',
  color: '#3b82f6',
  description: 'Ultra-low-cost cold storage for long-term data archiving and compliance retention.',
  introduction: 'Amazon S3 Glacier storage classes provide durable, secure, low-cost object storage designed for data archiving and long-term backup. Glacier offers three storage options with different retrieval latencies: S3 Glacier Instant Retrieval (milliseconds), S3 Glacier Flexible Retrieval (minutes to hours), and S3 Glacier Deep Archive (hours). They share S3\'s 11-nines durability model at a fraction of S3 Standard storage cost.',
  whenToUse: 'Use S3 Glacier for data that must be retained but is rarely or never accessed: compliance archives, regulatory records, database backups older than 90 days, media masters, scientific data, and disaster recovery copies. The storage class choice depends on how quickly you might need the data: instant for occasional quarterly queries, flexible for planned retrieval within 12 hours, deep archive for 7-10 year WORM retention with no expected retrieval.',
  keyConcepts: [
    { title: 'Storage Class Comparison', description: 'Glacier Instant Retrieval: $0.004/GB-month, millisecond access, $0.03/GB retrieval, suitable for medical images and news archives accessed quarterly. Glacier Flexible Retrieval: $0.0036/GB-month, Expedited (1-5 min), Standard (3-5 hr), or Bulk (5-12 hr) retrieval tiers with corresponding fees. Glacier Deep Archive: $0.00099/GB-month, Standard 12 hr retrieval, Bulk 48 hr retrieval — cheapest storage class in AWS.' },
    { title: 'Retrieval Options and Provisioned Capacity', description: 'Flexible Retrieval Expedited retrievals are charged $0.03/GB and typically complete in 1-5 minutes, but capacity is not guaranteed during unusually high demand. Purchase Provisioned Retrieval Capacity units ($100/month each) to guarantee Expedited retrieval capacity at any time — relevant for DR scenarios requiring SLA-bound data access.' },
    { title: 'Vault Lock and WORM Compliance', description: 'S3 Glacier Vaults (the legacy Glacier API) support Vault Lock policies with a WORM (Write Once Read Many) compliance mode using time-based retention rules that cannot be overridden by any user, including root. For S3-integrated Glacier classes, use S3 Object Lock in Compliance mode with a retention period to achieve equivalent WORM protection at the S3 API layer.' },
    { title: 'Lifecycle Transitions from S3', description: 'S3 Lifecycle rules transition objects from Standard or Standard-IA to Glacier storage classes based on age. Transitions have minimum storage duration requirements: Glacier Instant Retrieval and Flexible Retrieval require a minimum of 90 days (deleted before 90 days incurs a prorated charge). Glacier Deep Archive requires a 180-day minimum storage period. Objects smaller than 128 KB are not cost-effective to transition — Glacier per-object overhead charges exceed the storage savings.' },
  ],
  approach: [
    { step: 'Design a tiered lifecycle policy for long-term data', details: 'Define a lifecycle rule that transitions S3 Standard objects to Standard-IA at 30 days, to Glacier Instant Retrieval at 90 days, to Glacier Flexible Retrieval at 180 days, and to Glacier Deep Archive at 365 days. Expire objects at the legal retention limit (often 7 years for financial records). Apply the rule to specific bucket prefixes that contain archivable data, leaving frequently accessed prefixes on Standard.' },
    { step: 'Test retrieval procedures before a real DR event', details: 'Quarterly, submit a Glacier retrieval request for a representative sample of archived objects and verify the restored data integrity against checksums. For Flexible Retrieval, time the Expedited versus Standard retrieval to validate it meets your RTO. Document the retrieval process as a runbook. Discovering a retrieval issue during a real disaster is catastrophically worse than discovering it in a test.' },
    { step: 'Use S3 Object Lock for compliance WORM archives', details: 'Enable S3 Object Lock on the bucket in Compliance mode before writing any objects. Set a default retention period matching the regulatory requirement (e.g., 7 years for SEC 17a-4 financial records). Objects written to the bucket receive a locked retention date and cannot be deleted or overridden by any user until the retention period expires, even by the AWS account root. Compliance mode is irrevocable for the retention period — choose carefully versus Governance mode (which allows override by root).' },
    { step: 'Monitor retrieval costs with Cost Explorer tags', details: 'Tag all retrieval jobs with a cost allocation tag (department, project, retrieval_reason). Use AWS Cost Explorer to monitor Glacier restore charges, which can be significant if automated processes trigger retrieval of large archived datasets unexpectedly. Set a budget alert on the Glacier retrieval line item to detect runaway restore loops from misconfigured automation.' },
  ],
  pitfalls: [
    { title: 'Transitioning small objects to Glacier and incurring per-object charges', description: 'Glacier charges a per-object overhead for metadata storage equivalent to 8KB (Standard-IA and Glacier) or 32KB (Deep Archive) per object. For objects smaller than these thresholds, the overhead cost exceeds the storage savings from tiering. Combine small files into archives before lifecycle transition using S3 Batch Operations or Lambda to ZIP files before placing them in Glacier-destined prefixes.' },
    { title: 'Assuming Glacier Flexible Retrieval is instantaneous', description: 'Standard retrievals take 3-5 hours and Expedited retrievals take 1-5 minutes, but Expedited is not guaranteed during high demand without Provisioned Capacity. Teams that build DR plans assuming Glacier data is immediately available will fail their RTO during an actual disaster. Document retrieval times in the RTO and provision Provisioned Retrieval Capacity for any archive that must be recovered within 15 minutes.' },
    { title: 'Deleting objects before the minimum storage duration', description: 'Glacier Instant Retrieval and Flexible Retrieval have a 90-day minimum storage period. Deleting an object after only 30 days still incurs 60 days of Glacier storage charges. Deep Archive has a 180-day minimum. Before applying aggressive lifecycle rules that transition and immediately expire data, calculate whether the total cost including early deletion penalties exceeds leaving the data in Standard-IA.' },
  ],
  keyQuestions: [
    { q: 'How do you choose between Glacier Instant, Flexible, and Deep Archive?', a: 'Glacier Instant Retrieval is appropriate when the data is archived but occasionally accessed — medical imaging for follow-up consultations, news archives for editorial research, or data science raw materials accessed for model retraining once per quarter. The cost is 10x less than Standard with millisecond access latency. Glacier Flexible Retrieval suits data that might be needed but only with advance planning (1-12 hours notice) — weekly backup restores, annual audit data pulls. Glacier Deep Archive at $0.00099/GB is appropriate only for data almost certainly never retrieved — legal holds, end-of-life system archives, cold datasets archived for 10 years. The retrieval cost for Deep Archive ($0.02/GB Bulk) makes it uneconomical if retrieval happens more than annually.' },
    { q: 'How does S3 Object Lock in Compliance mode work for SEC 17a-4 compliance?', a: 'SEC Rule 17a-4 requires broker-dealer records to be preserved in a WORM format for 3-6 years with no modification or deletion permitted. S3 Object Lock Compliance mode implements this: once set, the retention date cannot be shortened by any user or process, including the AWS root account. A compliant architecture writes records to an S3 bucket with Object Lock enabled, sets a retention period matching the regulatory requirement, and uses a bucket policy that denies s3:DeleteObject and s3:PutObjectRetention to all principals. AWS provides an SEC 17a-4 attestation letter confirming S3 with Object Lock meets the regulation\'s electronic storage requirements.' },
    { q: 'How do you restore a large volume of Glacier objects efficiently?', a: 'For bulk restoration (data lake recovery, migration), use S3 Batch Operations with an S3 Initiate Restore operation. Create an S3 Inventory report to list all Glacier objects and their restore status, feed the inventory into a Batch Operations job, and initiate Bulk retrieval (cheapest, 5-12 hours) for all objects. Batch Operations handles thousands of restore requests concurrently. Monitor job progress in the Batch Operations console and set an S3 event notification to trigger a Lambda function when each restore completes to process or transfer the object before the 1-30 day restoration window expires.' },
    { q: 'What happens to Glacier objects during an S3 bucket deletion?', a: 'You cannot delete an S3 bucket that contains objects, including Glacier-archived objects. You must first empty the bucket. For Glacier Flexible Retrieval and Deep Archive objects, deleting an archived object before its minimum storage duration triggers an early deletion fee charged at the Glacier storage rate for the remaining days in the minimum period. Objects in Glacier Instant Retrieval have a 90-day minimum. If Object Lock is enabled on any object in Compliance mode, the object cannot be deleted until the retention period expires — the bucket cannot be emptied and therefore cannot be deleted until all retention periods have passed.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonS3/latest/userguide/glacier-storage-classes.html',
    'https://docs.aws.amazon.com/AmazonS3/latest/userguide/object-lock.html',
    'https://aws.amazon.com/s3/storage-classes/glacier/',
  ],
},

{
  id: 'aws-storage-gateway',
  title: 'Storage Gateway — Hybrid Cloud Storage',
  icon: 'shield',
  color: '#3b82f6',
  description: 'Bridge on-premises applications to AWS cloud storage with low-latency local caching.',
  introduction: 'AWS Storage Gateway is a hybrid cloud storage service that connects on-premises environments to AWS storage. It runs as a virtual machine appliance (or hardware appliance) at your data center, presenting local NFS, SMB, or iSCSI interfaces to existing applications while seamlessly tiering data to S3, Glacier, or EBS snapshots. Gateway provides the cloud economics of S3 with on-premises latency for active data.',
  whenToUse: 'Use Storage Gateway when on-premises applications need to access cloud storage using familiar protocols without rewriting application code, for extending on-premises NAS capacity to S3 without migration downtime, for disaster recovery by replicating on-premises volumes to AWS, for backup and archive workloads that need to land data in S3 or Glacier, and for phased cloud migration strategies.',
  keyConcepts: [
    { title: 'S3 File Gateway', description: 'S3 File Gateway presents NFS (v3/v4.1) and SMB interfaces backed by S3. Each mounted NFS share maps to an S3 bucket or prefix. Files written to the share are transparently uploaded to S3 as native S3 objects with metadata preserved. A configurable local cache stores recently accessed data for low-latency reads. Use cases: shared file storage for on-premises Linux servers, backup targets, and graduated migration of NAS data to S3.' },
    { title: 'Volume Gateway', description: 'Volume Gateway presents iSCSI block volumes to on-premises servers. Stored Volumes mode keeps primary data on-premises with asynchronous backup to S3 as EBS snapshots. Cached Volumes mode stores primary data in S3 with a local cache for frequently accessed data — reducing local storage requirements while maintaining low-latency reads for hot data. Both modes enable creating EBS volumes in AWS from snapshots for disaster recovery.' },
    { title: 'Tape Gateway', description: 'Tape Gateway emulates a virtual tape library (VTL) with an iSCSI interface, replacing physical tape infrastructure. Backup applications (Veeam, Veritas, Commvault) write to virtual tape cartridges that are stored in S3. Virtual tapes can be archived to S3 Glacier or Glacier Deep Archive. This enables existing tape-based backup workflows to migrate to cloud storage without replacing backup software or procedures.' },
    { title: 'Local Cache and Bandwidth Management', description: 'All gateway types maintain a local cache on the appliance disk for recently accessed or written data. Cache size determines the working set that can be served at on-premises latency versus waiting for S3 retrieval. Gateway supports bandwidth throttling schedules to limit upload bandwidth during business hours and maximize throughput during off-peak maintenance windows, preventing backup jobs from saturating WAN links.' },
  ],
  approach: [
    { step: 'Size the local cache for the active working set', details: 'Profile the on-premises NAS to determine the hot data set that is accessed within a rolling 7-day window using file system access time metadata. Size the gateway cache to hold at least the hot data set plus 20% headroom. Monitor the CacheHitPercent CloudWatch metric — below 90% indicates the cache is too small and application reads are frequently fetching from S3, adding latency.' },
    { step: 'Configure bandwidth scheduling for backup windows', details: 'Set a gateway bandwidth throttle schedule that limits upload to 10 Mbps during business hours (6 AM - 8 PM) and opens to 500 Mbps during the maintenance window (8 PM - 6 AM). This prevents backup traffic from competing with business-critical WAN traffic. Monitor UploadBufferUsed and UploadBufferFreeSpace metrics to verify the upload queue drains within the maintenance window.' },
    { step: 'Use S3 File Gateway for NAS migration', details: 'Mount the gateway NFS share on source Linux servers alongside the existing NAS mount. Run rsync from the existing NAS to the gateway share to replicate data to S3 incrementally. After initial sync, switch the application mount point from NAS to gateway during a maintenance window. The final cutover takes seconds since data is already in S3. Monitor S3 replication metrics during the transition and verify file integrity via checksums.' },
    { step: 'Enable CloudWatch monitoring and alerting', details: 'Create CloudWatch alarms on CacheHitPercent below 80% (cache undersized), IoWaitPercent above 50% (gateway VM resource constraint), and UploadBufferUsed above 80% (upload queue backing up). Set up a Storage Gateway maintenance notification via EventBridge for gateway software updates. Enable gateway audit logs to CloudWatch Logs for security investigation and compliance.' },
  ],
  pitfalls: [
    { title: 'Under-provisioning gateway VM resources', description: 'Storage Gateway VM resource requirements are minimum specifications, not recommendations. A gateway VM running with the minimum 4 vCPU and 16 GB RAM will exhibit I/O latency spikes under sustained backup loads. Right-size the gateway VM to match peak write throughput requirements. For high-throughput backup workloads (hundreds of GB/hour), allocate 8+ vCPU and 32 GB RAM and use dedicated SSD storage for the cache disk.' },
    { title: 'Sharing gateway cache disk with OS volume', description: 'The gateway cache and upload buffer disks must be separate raw disks — not partitions of the OS disk. A gateway configured to use OS disk partitions exhibits high I/O contention between gateway cache operations and OS functions. Always allocate dedicated raw EBS volumes (or physical disks for hardware appliances) to the cache and upload buffer roles during gateway activation.' },
    { title: 'Not testing DR recovery from Volume Gateway snapshots', description: 'Volume Gateway Stored Volumes back up to S3 as EBS snapshots, but recovery requires creating an EBS volume from the snapshot and attaching it to an EC2 instance in AWS. Teams that never test this process discover procedural and permission gaps during an actual disaster. Run quarterly DR drills: restore the latest snapshot to a test EC2 instance, verify data integrity, and document any IAM or VPC configuration issues found.' },
  ],
  keyQuestions: [
    { q: 'How does S3 File Gateway differ from simply mounting S3 with s3fs?', a: 'S3 File Gateway provides a production-grade NFS/SMB interface with a managed local SSD cache, bandwidth throttling, CloudWatch monitoring, AWS-native security (IAM, VPC, TLS), and a managed update lifecycle — it is an AWS service with an SLA. The s3fs FUSE mount is a community utility that translates POSIX file operations to S3 API calls without local caching, with limited POSIX semantics support, no AWS support, and frequent performance problems under concurrent writes due to the object storage impedance mismatch. For production on-premises NAS replacement, always use Storage Gateway over s3fs.' },
    { q: 'What is the difference between Stored and Cached Volume Gateway modes?', a: 'Stored Volumes keeps the complete data set on-premises storage with asynchronous EBS snapshot backups to S3. This provides the lowest read latency for all data (local disk) but requires sufficient on-premises storage capacity for the entire dataset. Cached Volumes stores primary data in S3 and caches only frequently accessed data locally, reducing on-premises storage requirements dramatically while accepting slightly higher latency for cache-miss reads. Use Stored Volumes when on-premises latency must be consistently sub-millisecond for all data and storage capacity is not constrained. Use Cached Volumes when migrating large datasets to the cloud while maintaining acceptable performance for the active working set.' },
    { q: 'How would you use Tape Gateway to modernize a legacy tape backup infrastructure?', a: 'Install the Tape Gateway appliance VM on-premises and activate it against your AWS account. Configure your existing backup software (Veeam, Veritas NetBackup) to recognize the gateway as a virtual tape library via the iSCSI VTL interface — no backup software license changes required. The backup software writes to virtual tapes exactly as it would to physical tapes. Active virtual tapes reside in S3; when the backup software ejects a tape to the vault, the gateway archives it to Glacier automatically. Tape retrieval for restore involves retrieving from Glacier to the virtual tape library. This eliminates tape rotation logistics, offsite vaulting costs, and tape media replacement while preserving existing backup workflows.' },
    { q: 'How do you size the Storage Gateway local cache for a file gateway workload?', a: 'Analyze the on-premises file system access patterns using audit logs or a file activity monitoring tool to determine the working set size — the files accessed at least once in the past 30 days. This is the minimum cache size. Add 20% headroom for cache management overhead. Also consider write buffer sizing: the upload buffer should hold at least 2 hours of peak write throughput in case of WAN interruption. Monitor CacheHitPercent in CloudWatch after deployment — if it drops below 90% under normal load, expand the cache disk (add a new raw disk allocation, Storage Gateway supports adding disks without downtime). A cache larger than the working set provides diminishing returns.' },
  ],
  references: [
    'https://docs.aws.amazon.com/storagegateway/latest/userguide/',
    'https://aws.amazon.com/storagegateway/file/',
    'https://aws.amazon.com/storagegateway/vtl/',
  ],
},

{
  id: 'aws-backup',
  title: 'AWS Backup — Centralized Backup Management',
  icon: 'shield',
  color: '#3b82f6',
  description: 'Policy-driven, centralized backup across EC2, RDS, DynamoDB, EFS, and 15+ AWS services.',
  introduction: 'AWS Backup provides a centralized, policy-driven service to automate backups across AWS compute, storage, and database services from a single console. It replaces service-specific backup configurations with a unified backup plan model, cross-region and cross-account copy capabilities, compliance audit frameworks, and a vault-based storage system with encryption and access control.',
  whenToUse: 'Use AWS Backup when managing backups across multiple AWS services and accounts at scale, when compliance regulations require demonstrable backup policies and audit trails, when you need cross-region DR copies of database and file system backups, and when you want to eliminate custom backup Lambda functions and CloudWatch Events wiring in favor of a managed, auditable backup service.',
  keyConcepts: [
    { title: 'Backup Plans and Rules', description: 'A Backup Plan defines when backups run, how long they are retained, and where they are copied. Each plan contains one or more rules specifying: schedule (cron expression), backup window, retention period (days to months), and optional copy actions to send copies to a Vault in another region or account. Plans are assigned resources via tag-based selection or explicit resource IDs.' },
    { title: 'Backup Vaults', description: 'A Backup Vault is a logical container for recovery points. Vaults have KMS encryption keys and resource-based access policies controlling who can restore, delete, or copy from them. The Default vault uses the AWS-managed key. Create dedicated vaults per team or compliance domain with customer-managed KMS keys. Vault Lock enforces retention policies in WORM mode — recovery points cannot be deleted before the retention period expires, even by root.' },
    { title: 'Supported Services', description: 'AWS Backup supports EC2 (AMI-based backups), EBS (snapshots), RDS (automated snapshots), Aurora (cluster snapshots), DynamoDB (point-in-time export to S3), EFS (file system backups), FSx (backups), S3 (object-level point-in-time restore), DocumentDB, Neptune, Timestream, and Storage Gateway volumes. Support continues to expand, making AWS Backup the consolidation point for multi-service backup governance.' },
    { title: 'Audit Manager and Compliance', description: 'AWS Backup Audit Manager assesses backup activity against compliance controls: backup frequency, minimum retention period, cross-region copy compliance, and encryption requirements. Controls generate automated reports and CloudWatch metrics. Integrate with AWS Config rules to flag resources missing backup plan assignments. Generate compliance reports (CSV, JSON) for auditors demonstrating backup coverage and recovery point age.' },
  ],
  approach: [
    { step: 'Tag resources for backup policy assignment', details: 'Establish a tagging convention: Backup:Plan=daily-30-day, Backup:Plan=weekly-1-year, Backup:Plan=none. Assign backup plans to resources using tag-based conditions. Enforce tagging via AWS Config rule required-tags with an SNS alert for non-compliant resources. This approach automatically includes new resources as they are created without requiring manual updates to backup plan resource lists.' },
    { step: 'Configure cross-region copy for disaster recovery', details: 'Add a CopyAction to each backup plan rule specifying the destination Vault in the DR region and a retention period. For RDS and Aurora, cross-region copies are independent snapshots that can be used to launch new database instances in the DR region. Test the restore procedure quarterly by launching a test RDS instance from the cross-region copy and validating data integrity.' },
    { step: 'Enable Vault Lock for compliance archives', details: 'Create a dedicated Compliance Vault with Vault Lock in Compliance mode, specifying minimum and maximum retention periods (e.g., min 2555 days / 7 years for financial data). Assign regulatory-sensitive resources to a backup plan that copies to this vault. Vault Lock in Compliance mode cannot be removed after a 72-hour cooling period, so test the configuration in a non-production account before enabling it on production data.' },
    { step: 'Monitor backup job status with CloudWatch and SNS', details: 'Create a CloudWatch Event Rule matching AWS Backup job state changes for FAILED status and route to an SNS topic that pages the on-call engineer. Create an EventBridge rule for ABORTED jobs (which can indicate resource policy or permission issues). Use AWS Backup Audit Manager compliance reports to identify resources that have not had a successful backup in the past N days, feeding into a weekly compliance review.' },
  ],
  pitfalls: [
    { title: 'Confusing AWS Backup with service-native backup features', description: 'Some AWS Backup recovery points (EFS, FSx) are different from the service-native backup format. EFS backups via AWS Backup are incremental file-system-level backups, not EFS-native recovery points. Restoring an EFS AWS Backup recovery point to a new file system takes longer than restoring a native EFS backup for small workloads. Understand the underlying format differences before designing RTO-sensitive recovery procedures.' },
    { title: 'Not testing restore procedures', description: 'A backup that has never been tested is not a backup — it is a hope. Schedule quarterly restore drills for each service type: restore an RDS instance from a cross-region copy, restore an EFS backup to a new file system, launch an EC2 instance from an AWS Backup AMI recovery point. Document restore times and compare against RTO requirements. Identify failures before a real incident.' },
    { title: 'Relying on default vault encryption for sensitive data', description: 'The default AWS Backup vault uses an AWS-managed KMS key. For data subject to strict compliance requirements (HIPAA, PCI), use a customer-managed KMS key so you can audit key usage via CloudTrail, enforce key policies, and control key rotation. Store the KMS key in the same account as the vault but deny access to the key from non-backup service principals to prevent unauthorized decryption of backup data.' },
  ],
  keyQuestions: [
    { q: 'How does AWS Backup differ from RDS automated backups or EBS snapshots?', a: 'RDS automated backups and EBS snapshots are service-specific, managed independently in each service\'s console with separate retention configurations and no centralized visibility. AWS Backup centralizes all backup policies, schedules, and monitoring in one service with a unified console and API. It adds cross-account and cross-region copy orchestration, compliance reporting via Audit Manager, Vault Lock for WORM compliance, and tag-based automatic resource assignment. For organizations managing dozens of services across multiple accounts, AWS Backup\'s governance and auditability capabilities justify the consolidation even if the underlying backup mechanism (RDS snapshots, EBS snapshots) is the same.' },
    { q: 'How do you implement a 3-2-1 backup strategy using AWS Backup?', a: 'The 3-2-1 rule requires 3 copies of data, on 2 different media types, with 1 copy offsite. In AWS Backup: the primary copy is the AWS Backup recovery point in the primary-region vault (copy 1, on S3-backed managed storage). Configure a cross-region CopyAction to send a copy to a vault in a second AWS region (copy 2, offsite). For the third copy on a different medium, use AWS Backup to copy DynamoDB tables to S3 via export, or enable RDS automated backups (native snapshots) as a parallel backup mechanism alongside AWS Backup managed snapshots. This achieves 3 copies across 2 services and 2 geographic regions.' },
    { q: 'How would you demonstrate backup compliance to an auditor using AWS Backup?', a: 'Configure AWS Backup Audit Manager with relevant compliance frameworks: backup resource protection (all resources have backup plans), backup plan minimum retention frequency, cross-region backup copy enabled, encryption in transit and at rest, and recovery point retention policy checks. Audit Manager generates daily compliance reports in CSV or JSON format exportable to S3. Configure AWS Config rules backup-plan-min-frequency-and-min-retention-check and backup-recovery-point-minimum-retention-check for real-time resource compliance assessment. Share the Audit Manager compliance reports directly with auditors as evidence of continuous backup compliance rather than producing manual backup logs.' },
    { q: 'How do you handle backup of a multi-account AWS organization with AWS Backup?', a: 'Enable AWS Backup in all member accounts and designate a dedicated central backup account as the cross-account copy destination. Use AWS Organizations integration to create Backup Policies at the Organization or OU level — these are JSON documents that deploy backup plans to all member accounts automatically, ensuring consistent backup frequency and retention without manual configuration per account. Cross-account recovery points are copied to the central backup account vault using CopyActions in the backup plan rule. From the central account, AWS Backup provides a consolidated view of all backup jobs and compliance status across the organization, simplifying multi-account backup governance.' },
  ],
  references: [
    'https://docs.aws.amazon.com/aws-backup/latest/devguide/',
    'https://docs.aws.amazon.com/aws-backup/latest/devguide/create-backup-plan.html',
    'https://docs.aws.amazon.com/aws-backup/latest/devguide/aws-backup-audit-manager.html',
  ],
},
{
    id: 'aws-vpc',
    title: 'VPC — Virtual Private Cloud',
    icon: 'globe',
    color: '#06b6d4',
    description: 'Isolated virtual network in AWS — the foundation of every cloud architecture.',
    introduction: 'Amazon VPC lets you provision a logically isolated section of the AWS cloud where you launch resources in a virtual network you define. You have complete control over your networking environment including IP address ranges, subnets, route tables, and network gateways. VPC is the foundational networking primitive on AWS — every EC2, RDS, and Lambda-in-VPC resource lives inside one.',
    whenToUse: 'Every AWS workload lives in a VPC. Design VPCs around workload isolation boundaries — separate VPCs for production, staging, and development. Use multiple availability zones for high availability. Consider VPC peering or AWS Transit Gateway when services across VPCs need to communicate.',
    keyConcepts: [
      { title: 'Subnets', description: 'Public subnets have a route to an internet gateway and are used for load balancers and bastion hosts. Private subnets route outbound traffic through a NAT gateway. Isolated subnets have no internet route and are reserved for databases.' },
      { title: 'Security Groups', description: 'Stateful virtual firewall operating at the ENI level. Rules are evaluated as an aggregate — all allow rules are checked, there are no explicit deny rules. Return traffic is automatically allowed.' },
      { title: 'Network ACLs', description: 'Stateless firewall at the subnet boundary. Rules are evaluated in order by rule number. Both inbound and outbound rules must explicitly allow traffic, including ephemeral port ranges for responses.' },
      { title: 'Route Tables', description: 'Each subnet is associated with a route table. The most specific route wins. Local routes cover VPC CIDR and cannot be deleted. Internet gateway, NAT gateway, and VPC peering connections are added as targets.' },
      { title: 'NAT Gateway', description: 'Managed NAT service allowing private subnet resources to reach the internet for outbound traffic while remaining unreachable from inbound. Deployed per AZ for high availability. Costs both hourly and per GB processed.' },
    ],
    approach: [
      { step: 'Plan CIDR ranges', details: 'Use /16 for the VPC to allow up to 65,536 addresses. Allocate /24 subnets per tier per AZ. Leave room for VPC peering — overlapping CIDR ranges prevent peering. Allocate non-overlapping ranges across all accounts upfront using an IP address management plan.' },
      { step: 'Design subnet tiers', details: 'Three-tier layout: public for internet-facing load balancers and bastions, private for application servers, isolated for databases. Each tier spans all AZs. Never place databases in public subnets regardless of security group rules.' },
      { step: 'Configure security groups and NACLs', details: 'Security groups are the primary control — use them for service-to-service rules. NACLs add a coarse subnet-level layer, useful for blocking known bad CIDR ranges. Allow ephemeral ports 1024-65535 on NACL inbound for responses from internet.' },
      { step: 'Enable VPC Flow Logs', details: 'Send to CloudWatch Logs or S3. Capture ACCEPT and REJECT traffic. Enables security forensics, traffic analysis, and debugging connectivity issues. Choose the right aggregation interval — 1 minute captures more data but costs more.' },
    ],
    pitfalls: [
      { title: 'Overlapping CIDRs', description: 'Cannot peer VPCs or connect to on-premises with overlapping CIDR ranges. Plan IP space upfront across all accounts and regions before deploying anything. RFC 1918 space runs out faster than expected in multi-account organizations.' },
      { title: 'Single NAT Gateway', description: 'A single NAT gateway creates an AZ dependency. If that AZ fails, all private subnet resources lose internet access. Deploy one NAT gateway per AZ and route each private subnet to the NAT in the same AZ.' },
      { title: 'Security group sprawl', description: 'Unreferenced security groups accumulate and make auditing difficult. Attach groups to resources intentionally, document their purpose, and use AWS Config rules to detect unused security groups.' },
    ],
    keyQuestions: [
      { q: 'How do you design a multi-tier VPC architecture?', a: 'Three tiers span all availability zones: public subnets hold internet-facing ALBs and NAT gateways, private subnets hold application servers and ECS tasks, isolated subnets hold RDS and ElastiCache. Security groups enforce tier-to-tier rules — the ALB security group allows 443 from 0.0.0.0/0, the app security group allows traffic only from the ALB security group, and the database security group allows only from the app security group. This creates a defense-in-depth model with no lateral movement path.' },
      { q: 'What is the difference between security groups and NACLs?', a: 'Security groups are stateful and operate at the ENI level — return traffic is automatically permitted. NACLs are stateless and operate at the subnet level — both inbound and outbound rules must explicitly allow traffic including ephemeral response ports. Security groups aggregate all rules with no ordering; NACLs evaluate rules by number with explicit denies possible. In practice, security groups are the primary access control mechanism and NACLs provide a secondary subnet-boundary layer.' },
      { q: 'How would you connect two VPCs so services can communicate privately?', a: 'VPC peering creates a direct network route between two VPCs using private IPs. It is non-transitive — if A peers with B and B peers with C, A cannot reach C through B. For hub-and-spoke or transitive routing, use AWS Transit Gateway, which acts as a managed regional router. For consuming a specific service from another VPC without full network access, AWS PrivateLink creates an endpoint that exposes only a single service, reducing blast radius compared to full peering.' },
      { q: 'How do you troubleshoot a connectivity issue between two resources in different subnets?', a: 'Work through the checklist in order: verify the route table has a path to the destination, verify the security group on the destination allows inbound from the source security group or CIDR, verify the source security group allows outbound, check NACL inbound and outbound rules on both subnets including ephemeral ports, then check VPC Flow Logs to confirm whether packets are being accepted or rejected at each hop. The flow logs REJECT record pinpoints exactly which layer is blocking traffic.' },
    ],
    references: [
      'https://docs.aws.amazon.com/vpc/latest/userguide/',
      'https://docs.aws.amazon.com/vpc/latest/userguide/vpc-security-best-practices.html',
    ],
  },
  {
    id: 'aws-vpc-advanced',
    title: 'VPC Advanced — Transit Gateway, Peering, and Egress',
    icon: 'layers',
    color: '#06b6d4',
    description: 'Multi-VPC connectivity patterns, shared services, and enterprise egress design.',
    introduction: 'At scale, single-VPC design gives way to multi-account, multi-VPC architectures that demand structured connectivity. AWS provides several building blocks: VPC Peering for point-to-point links, Transit Gateway for hub-and-spoke routing, PrivateLink for service exposure, and centralized egress patterns that reduce NAT gateway costs and improve observability.',
    whenToUse: 'Use advanced VPC patterns when you operate more than two VPCs, need transitive routing, want to centralize inspection or egress, or are onboarding to AWS Organizations with a landing zone. These patterns become essential once a workload grows beyond a single-account architecture.',
    keyConcepts: [
      { title: 'VPC Peering', description: 'Direct private IP routing between two VPCs in any region. Non-transitive — no routing through an intermediate VPC. Best for a small number of point-to-point connections. Requires non-overlapping CIDRs and route table updates on both sides.' },
      { title: 'Transit Gateway', description: 'Regional managed router that interconnects VPCs and on-premises networks in a hub-and-spoke model. Supports transitive routing via route tables and route table associations. Enables centralized inspection by sending all traffic through a shared security VPC.' },
      { title: 'Centralized Egress', description: 'A shared egress VPC contains NAT gateways for all spoke VPCs. Spokes route 0.0.0.0/0 to the Transit Gateway, which forwards to the egress VPC. Reduces NAT gateway sprawl and centralizes egress IP addresses for firewall allowlisting.' },
      { title: 'Centralized Inspection', description: 'A security VPC runs network firewall or third-party appliances. All inter-VPC and egress traffic routes through it via Transit Gateway appliance mode. Enables uniform policy enforcement without per-VPC firewall deployments.' },
      { title: 'Gateway Load Balancer', description: 'Distributes traffic to a fleet of virtual appliances transparently using GENEVE encapsulation. Used with Transit Gateway for inline inspection. Appliances see original source and destination IPs.' },
    ],
    approach: [
      { step: 'Define VPC topology', details: 'Decide between flat (full mesh peering), hub-and-spoke (Transit Gateway), or hierarchical (segmented TGW route tables). Hub-and-spoke scales better but adds a single point of routing. Full mesh is impractical beyond five VPCs due to peering connection limits and route table complexity.' },
      { step: 'Design Transit Gateway route tables', details: 'Segment route tables by environment: production, non-production, shared services. Associate each VPC attachment with one route table and propagate routes selectively. This enforces isolation — dev VPCs cannot reach prod even if both connect to the same TGW.' },
      { step: 'Implement centralized egress', details: 'Create an egress VPC with NAT gateways in each AZ. Add a default route in the TGW route table pointing to the egress VPC attachment. Remove NAT gateways from spoke VPCs. Monitor NAT gateway data processing charges — this pattern can significantly reduce costs at scale.' },
      { step: 'Enable flow logs and monitoring', details: 'Enable TGW flow logs in addition to VPC flow logs for cross-VPC visibility. Use Traffic Mirroring for deep packet inspection without inline appliances. Set CloudWatch alarms on TGW bytes per second and packet drops.' },
    ],
    pitfalls: [
      { title: 'Asymmetric routing with stateful firewalls', description: 'In active-active appliance setups, request and response packets can hit different appliances, breaking stateful inspection. Use Transit Gateway appliance mode to pin flows to a single AZ, ensuring both directions traverse the same appliance.' },
      { title: 'TGW route table misconfiguration', description: 'A missing propagation or wrong association means traffic is silently dropped at the TGW. Always test connectivity between every spoke pair after changes. Use AWS Network Manager Reachability Analyzer to programmatically verify paths.' },
      { title: 'Bandwidth and latency on TGW', description: 'Transit Gateway adds a routing hop with measurable latency. For ultra-low-latency paths between two specific VPCs, direct VPC peering is faster. TGW also has a maximum bandwidth per attachment — plan for multiple attachments if a VPC exceeds the limit.' },
    ],
    keyQuestions: [
      { q: 'When would you choose VPC Peering over Transit Gateway?', a: 'VPC Peering is preferable for a small, stable number of point-to-point connections where transitive routing is not needed. It has lower cost, lower latency, and simpler configuration. Transit Gateway is the right choice when you have many VPCs, need transitive routing, want centralized inspection or egress, or need to connect to multiple VPNs or Direct Connect attachments from a single point. The operational overhead of managing a Transit Gateway is justified once you have more than four or five VPCs.' },
      { q: 'How do you prevent prod and dev VPCs from communicating through a shared Transit Gateway?', a: 'Create separate TGW route tables for production and non-production environments. Associate production VPC attachments with the production route table and non-production attachments with the non-production route table. Propagate routes only within the same environment route table. A shared services VPC can be propagated into both tables to allow common dependencies like DNS or monitoring without opening direct prod-to-dev paths.' },
      { q: 'How does centralized egress reduce cost compared to per-VPC NAT gateways?', a: 'Each VPC with outbound internet access typically deploys NAT gateways per AZ, creating N VPCs times three AZs NAT gateway hourly charges. Centralized egress consolidates all egress through a single shared egress VPC with three NAT gateways. The savings on hourly charges are significant for organizations with many VPCs. Data processing charges remain similar, but the reduction in NAT gateway instances often yields 60-80% savings on egress infrastructure costs.' },
      { q: 'What is appliance mode in Transit Gateway and when must you use it?', a: 'Appliance mode ensures that both directions of a flow — request and response — traverse the same TGW attachment in the same AZ. This is required when stateful appliances like firewalls or IDS/IPS are deployed in a centralized inspection VPC. Without appliance mode, asymmetric routing can send the return packet through a different AZ attachment, which reaches a different appliance instance that has no state for the flow, causing it to drop the packet or pass it without inspection.' },
    ],
    references: [
      'https://docs.aws.amazon.com/vpc/latest/tgw/what-is-transit-gateway.html',
      'https://docs.aws.amazon.com/whitepapers/latest/building-scalable-secure-multi-vpc-network-infrastructure/welcome.html',
    ],
    visualizations: [
      {
        title: 'Multi-VPC Connectivity Architecture',
        description: `AWS VPC Advanced covers the set of patterns engineers reach for once a single VPC is no longer sufficient. The two primary building blocks are VPC Peering and Transit Gateway, and choosing between them is the first architectural decision.

VPC Peering creates a direct, non-transitive routing path between exactly two VPCs. Both VPCs must have non-overlapping CIDRs, and route tables on both sides must be updated manually. Because peering is non-transitive, a spoke A cannot reach spoke B through a shared hub -- each pair needs its own peering connection. This works well up to about five VPCs but produces N*(N-1)/2 connections at full mesh, which becomes unmanageable quickly.

Transit Gateway replaces the mesh with a hub-and-spoke model. Every VPC attaches to the TGW with one or more ENIs per AZ. The TGW maintains its own route tables, and each attachment associates with one route table and optionally propagates its CIDR into others. This means you can enforce isolation between environments (production, staging, dev) by placing their attachments in separate route tables with no cross-propagation -- dev traffic is dropped at the TGW router without needing security group rules between every pair.

Two advanced patterns build on TGW. Centralized egress places NAT gateways in a dedicated egress VPC. All spoke VPC route tables point their 0.0.0.0/0 default route to the TGW, which forwards to the egress VPC attachment, then out through NAT. This eliminates one NAT gateway per AZ per spoke VPC -- for a 20-VPC organization the savings can exceed 80% of NAT gateway hourly charges. Centralized inspection adds a security VPC with a Gateway Load Balancer fleet. TGW route tables send all inter-VPC and egress traffic through the inspection VPC before delivery. Appliance mode must be enabled on the inspection VPC attachment so stateful firewalls see both directions of each flow on the same appliance instance.

Key tradeoffs: TGW adds a routing hop (~1ms) and has an attachment bandwidth ceiling. VPC peering has lower latency and no bandwidth limit but does not scale operationally. TGW inter-region peering uses static routes only -- no dynamic propagation across regions. The default TGW configuration puts all attachments in one route table with full transitive routing -- disable this default and build explicit segmentation from day one.`,
      },
    ],
    quickFire: [
      { q: 'Is VPC Peering transitive?', a: 'No. VPC A peered with VPC B and VPC B peered with VPC C does not allow A to reach C. Each pair needs its own peering connection.' },
      { q: 'What is the maximum number of VPC attachments per Transit Gateway?', a: '5,000 VPC attachments per TGW.' },
      { q: 'What does appliance mode do on a TGW attachment?', a: 'It pins both directions of a flow to the same AZ attachment, ensuring stateful inspection appliances see both request and response packets.' },
      { q: 'What problem does centralized egress solve?', a: 'It eliminates NAT gateways in every spoke VPC, concentrating egress through one shared NAT VPC and reducing hourly NAT gateway charges significantly.' },
      { q: 'When must you use TGW appliance mode?', a: 'When routing traffic through stateful firewalls or IDS/IPS in a centralized inspection VPC -- without it, asymmetric routing breaks stateful inspection.' },
      { q: 'Does TGW inter-region peering support dynamic route propagation?', a: 'No. Routes across TGW peering connections must be added as static routes on both ends.' },
      { q: 'What tool validates network paths between VPCs?', a: 'AWS Network Manager Reachability Analyzer -- it programmatically checks whether a packet can travel from a source to a destination through the network topology.' },
      { q: 'What is the default TGW routing behavior you should disable?', a: 'By default all attachments associate with and propagate to a single route table, giving all VPCs full transitive access. Disable default association and propagation and build explicit segmented route tables from the start.' },
      { q: 'How does a transit VIF differ from a private VIF on Direct Connect?', a: 'A private VIF connects to a single VPC Virtual Private Gateway. A transit VIF connects to a Direct Connect Gateway linked to a TGW, enabling access to all TGW-attached VPCs across regions through one connection.' },
    ],
  },
  {
    id: 'aws-cloudfront',
    title: 'CloudFront — CDN and Edge Delivery',
    icon: 'zap',
    color: '#06b6d4',
    description: 'Global CDN that caches, accelerates, and secures content at 450+ edge locations.',
    introduction: 'Amazon CloudFront is a globally distributed content delivery network that serves content from edge locations closest to end users, dramatically reducing latency. It integrates tightly with S3, ALB, API Gateway, and custom origins. Beyond caching, CloudFront handles TLS termination, origin request signing, Lambda@Edge and CloudFront Functions for edge compute, and WAF integration for DDoS protection.',
    whenToUse: 'Use CloudFront for any globally distributed application, static asset delivery from S3, API acceleration, and as a mandatory layer in front of WAF. It is also the standard pattern for serving single-page applications where the S3 bucket is kept private.',
    keyConcepts: [
      { title: 'Distributions and Origins', description: 'A distribution is a CloudFront configuration that maps one or more origins (S3, ALB, API Gateway, custom HTTP) to cache behaviors. Each behavior defines cache policies, origin request policies, and viewer protocol policies for a path pattern.' },
      { title: 'Cache Behaviors', description: 'Ordered list of path-pattern rules that determine how requests are processed. Default behavior matches everything not caught by a more specific pattern. Each behavior independently controls caching TTL, compression, allowed HTTP methods, and edge function associations.' },
      { title: 'Origin Request and Cache Policies', description: 'Cache policy controls which headers, cookies, and query strings are part of the cache key. Origin request policy controls which values are forwarded to the origin. Separating these allows caching based on a subset of what is sent upstream.' },
      { title: 'Lambda@Edge and CloudFront Functions', description: 'Lambda@Edge runs Node.js or Python at regional edge locations for heavy logic like auth, URL rewrites, and A/B testing. CloudFront Functions run JavaScript at every edge PoP for lightweight, sub-millisecond transformations. Functions are significantly cheaper and faster but have strict memory and time limits.' },
      { title: 'Origin Access Control', description: 'Replaces the older OAI pattern. Grants CloudFront a signed identity to access a private S3 bucket. The bucket policy allows only requests signed by the specific CloudFront distribution, preventing direct S3 access.' },
    ],
    approach: [
      { step: 'Configure cache policies', details: 'Start with AWS managed cache policies and customize only when needed. Set minimum TTL to 0 to allow origins to control caching via Cache-Control headers. Use cache-busting file names (content hashing) for versioned assets to allow very long TTLs.' },
      { step: 'Separate behaviors by content type', details: 'Static assets like JavaScript, CSS, and images get aggressive caching (365 days with immutable). API paths under /api get a no-cache behavior with all headers forwarded. HTML files may get short TTLs to allow quick rollbacks.' },
      { step: 'Configure OAC for S3 origins', details: 'Create an Origin Access Control in the CloudFront console, associate it with the S3 origin, and update the S3 bucket policy to allow s3:GetObject only from the CloudFront service principal with a condition on the distribution ARN. Remove all public access settings from the bucket.' },
      { step: 'Associate WAF and enable logging', details: 'Attach an AWS WAF web ACL at the CloudFront level for global protection. Enable CloudFront access logs to S3 or real-time logs to Kinesis. Real-time logs have sub-second latency and are essential for live traffic analysis and incident response.' },
    ],
    pitfalls: [
      { title: 'Cache key pollution', description: 'Including unnecessary headers or query strings in the cache key fragments the cache and reduces hit ratios. Audit cache policies and remove anything that varies per user but does not actually affect the response content.' },
      { title: 'Forgetting to invalidate on deploy', description: 'After deploying new static assets to S3, cached copies at edge locations may serve stale content. Use content-hashed filenames to avoid invalidations entirely for versioned assets. For HTML and other non-versioned files, issue targeted invalidations for changed paths.' },
      { title: 'Lambda@Edge cold starts at global scale', description: 'Lambda@Edge functions replicate to all edge locations and can have cold starts in rarely-used regions. Use CloudFront Functions for simple transformations where cold start latency is unacceptable. Keep Lambda@Edge functions minimal and avoid heavy initialization code.' },
    ],
    keyQuestions: [
      { q: 'How do you serve a React SPA from S3 via CloudFront with proper routing?', a: 'Upload the build output to a private S3 bucket. Configure CloudFront with OAC to access the bucket. Set the default root object to index.html. Create a custom error response that redirects 403 and 404 errors to index.html with a 200 status code — this handles client-side routing where deep links would otherwise 404 at the S3 level. Hash all JS and CSS filenames and set their cache TTL to one year; set index.html TTL to zero or a short value.' },
      { q: 'What is the difference between Lambda@Edge and CloudFront Functions?', a: 'CloudFront Functions run at all 450-plus edge PoPs, execute in under one millisecond, cost 20 times less than Lambda@Edge, and are limited to viewer request and viewer response events with 2MB memory and no network access. Lambda@Edge runs at regional edge locations (around 13), supports all four event types including origin request and origin response, can make network calls and use larger memory and execution time, and is appropriate for auth flows, dynamic rewrites, and anything requiring external calls.' },
      { q: 'How do you implement authentication at the edge with CloudFront?', a: 'For JWT validation, use a CloudFront Function to inspect the Authorization header or a cookie, validate the JWT signature using a public key embedded in the function, and return a 401 response for invalid tokens before the request reaches the origin. For OAuth flows requiring network calls to an IdP, Lambda@Edge is required. Cognito integration can be handled via a Lambda@Edge function that validates the Cognito JWT and optionally rewrites headers for the origin.' },
      { q: 'How does CloudFront handle origin failover?', a: 'Configure an origin group with a primary and secondary origin and define the HTTP status codes that trigger failover (typically 500, 502, 503, 504). When the primary returns one of those codes, CloudFront immediately retries the full request against the secondary origin. This is suitable for static content failover from a primary region S3 bucket to a secondary region replica. For dynamic origins, note that the failover retry means the request is processed twice — ensure idempotency.' },
    ],
    references: [
      'https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/Introduction.html',
      'https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/edge-functions.html',
    ],
    visualizations: [
      {
        title: 'CloudFront Edge Delivery Architecture',
        description: `Amazon CloudFront sits in front of every origin -- S3, ALB, API Gateway, or any HTTP server -- and serves requests from the nearest of its 450-plus edge Points of Presence. The core data flow is: user DNS resolves to the nearest edge PoP via anycast, the edge checks its cache using the configured cache key, a hit returns immediately, a miss fetches from origin and caches the response per the TTL rules.

A distribution maps one or more origins to cache behaviors, where each behavior is a path-pattern rule evaluated in priority order. The default behavior (/) catches everything not matched by a specific pattern. Static assets like JS and CSS typically get a long TTL (1 year) combined with content-hashed filenames for instant cache busting. HTML files get a short TTL or Cache-Control: no-cache so that new deployments are reflected quickly. API paths use a pass-through behavior with no caching and forward all headers.

Origin Access Control (OAC) is the modern pattern for private S3 buckets. OAC gives CloudFront a signed identity; the S3 bucket policy allows only requests signed by that specific distribution ARN. This keeps S3 completely private while allowing CloudFront to serve its contents globally.

Edge compute comes in two flavors. CloudFront Functions run JavaScript at every edge PoP with sub-millisecond execution and 2MB memory. They only fire on viewer request and viewer response events. Lambda@Edge runs Node.js or Python at ~13 regional edge locations with up to 10 seconds execution time and network access -- required for auth flows, OAuth callbacks, and any logic that calls external services.

WAF attaches at the CloudFront layer for global DDoS protection and rate limiting before traffic ever reaches the origin. Combined with Shield Advanced, this provides the standard defense posture for public-facing applications. CloudFront access logs go to S3 for batch analysis or to Kinesis Data Streams for real-time dashboards with sub-second latency.`,
      },
    ],
    quickFire: [
      { q: 'How many edge locations does CloudFront have in 2026?', a: 'Over 450 Points of Presence across 90+ cities in 47 countries.' },
      { q: 'What is Origin Access Control and why use it?', a: 'OAC gives CloudFront a signed identity to access a private S3 bucket. The bucket stays fully private; only requests signed by the specific distribution ARN are allowed -- prevents direct S3 URL access.' },
      { q: 'What is the difference between Lambda@Edge and CloudFront Functions?', a: 'CloudFront Functions run at every PoP, cost 20x less, execute in under 1ms but have no network access. Lambda@Edge runs at ~13 regional locations, supports all 4 event types, can make network calls, and is needed for auth flows.' },
      { q: 'How do you prevent stale HTML after a deployment?', a: 'Set HTML TTL to 0 or use Cache-Control: no-cache. For JS/CSS, use content-hashed filenames and set TTL to 1 year -- the hash change acts as a cache buster without needing manual invalidations.' },
      { q: 'What HTTP status codes should trigger CloudFront origin failover?', a: '500, 502, 503, and 504 -- server-side errors indicating the primary origin is unavailable. Client errors like 404 should not trigger failover.' },
      { q: 'What controls the CloudFront cache key?', a: 'The cache policy -- it defines which headers, cookies, and query strings are part of the key. Including unnecessary values fragments the cache and reduces hit ratio.' },
      { q: 'How do you handle SPA deep links on CloudFront + S3?', a: 'Configure a custom error response that maps 403 and 404 status codes to /index.html with a 200 response -- the React router handles the path client-side.' },
      { q: 'What event types can Lambda@Edge intercept?', a: 'Four events: viewer request, viewer response, origin request, and origin response. CloudFront Functions only support viewer request and viewer response.' },
      { q: 'How do you attach WAF to CloudFront?', a: 'Create a WAF Web ACL in us-east-1 (CloudFront requires us-east-1 for WAF) and associate it with the CloudFront distribution. WAF rules apply globally at every edge PoP.' },
    ],
  },
  {
    id: 'aws-route53',
    title: 'Route 53 — DNS and Traffic Management',
    icon: 'globe',
    color: '#06b6d4',
    description: 'Authoritative DNS with health-checked routing policies for global traffic distribution.',
    introduction: 'Amazon Route 53 is a highly available, scalable DNS service that translates domain names into IP addresses and routes traffic based on configurable policies. Beyond basic DNS, it provides health checking, failover, weighted, latency-based, geolocation, and geoproximity routing. It also functions as a domain registrar and supports private hosted zones for internal DNS within VPCs.',
    whenToUse: 'Use Route 53 for all DNS needs on AWS — public domains, internal service discovery via private hosted zones, multi-region active-active or active-passive routing, and health-checked failover. Integrate with ALB, CloudFront, and API Gateway via alias records to avoid extra DNS hops.',
    keyConcepts: [
      { title: 'Hosted Zones', description: 'A public hosted zone contains DNS records for a domain accessible from the internet. A private hosted zone is associated with one or more VPCs and resolves names only within those VPCs. Both types support all standard DNS record types.' },
      { title: 'Alias Records', description: 'AWS-specific extension to DNS that maps a domain to an AWS resource like an ALB, CloudFront distribution, or S3 website endpoint. Unlike CNAME records, alias records work at the zone apex (naked domain) and do not incur extra DNS query charges.' },
      { title: 'Health Checks', description: 'Route 53 health checkers from multiple AWS regions probe an endpoint via HTTP, HTTPS, or TCP at configurable intervals. Records with associated failing health checks are removed from DNS responses. Calculated health checks aggregate multiple child checks.' },
      { title: 'Routing Policies', description: 'Simple routes to a single resource. Weighted distributes traffic by percentage. Latency routes to the region with lowest latency to the user. Failover designates primary and secondary records. Geolocation and geoproximity route based on user location. Multivalue returns multiple healthy records.' },
      { title: 'Resolver', description: 'Route 53 Resolver enables DNS resolution between VPCs and on-premises networks. Inbound endpoints accept DNS queries from on-premises. Outbound endpoints forward queries for specific domains to on-premises DNS servers via forwarding rules.' },
    ],
    approach: [
      { step: 'Use alias records for AWS resources', details: 'Always use alias records to point domains to ALBs, CloudFront distributions, and API Gateways. Alias records update automatically when the underlying resource IP changes, have no additional query charges, and work at the zone apex unlike CNAME records.' },
      { step: 'Configure health checks before routing policies', details: 'Health checks must exist before adding failover or multivalue routing records. Test health check thresholds carefully — too sensitive causes unnecessary failovers during brief blips, too loose delays detection of real outages. Three consecutive failures before marking unhealthy is a common baseline.' },
      { step: 'Design active-active multi-region routing', details: 'Use latency routing with health checks to route users to the lowest-latency healthy region. Each region gets a latency record pointing to its ALB with an associated health check. Failed regions are automatically removed from responses. Combine with weighted routing to do canary deployments across regions.' },
      { step: 'Set up private hosted zones for internal DNS', details: 'Associate private hosted zones with all VPCs that need to resolve internal service names. Use a consistent naming convention like service.internal. Enable DNS hostnames and DNS resolution on each VPC. Use Resolver forwarding rules to extend internal DNS to on-premises environments.' },
    ],
    pitfalls: [
      { title: 'TTL and failover speed', description: 'High TTL values mean DNS changes propagate slowly. For failover records, set TTL to 60 seconds so clients pick up the change quickly when a region fails. Caching resolvers may ignore TTL, so plan for some traffic to continue hitting a failed endpoint for the duration of the TTL.' },
      { title: 'Health check endpoint selection', description: 'Health checking the load balancer frontend is not the same as checking application health. Configure health checks against an application-level endpoint that exercises the critical path, not just a TCP connection check. Otherwise DNS will route traffic to a region where the LB is up but the application is broken.' },
      { title: 'Private hosted zone VPC association', description: 'A private hosted zone must be explicitly associated with each VPC that needs to use it. Forgetting to associate a new VPC is a common cause of DNS resolution failures in new environments. Use the associate-vpc-with-hosted-zone command for VPCs in other accounts.' },
    ],
    keyQuestions: [
      { q: 'How do you implement DNS-based failover between two AWS regions?', a: 'Create a primary failover record pointing to the primary region ALB alias with an associated health check. Create a secondary failover record pointing to the secondary region ALB alias. Route 53 serves the primary record when the health check passes and automatically switches to the secondary when the primary fails. Set TTL to 60 seconds to minimize propagation delay. The health check should monitor an application-level endpoint, not just the LB frontend.' },
      { q: 'What is the difference between latency routing and geolocation routing?', a: 'Latency routing routes users to the AWS region that provides the lowest measured network latency for their location, based on continuous latency measurements Route 53 maintains. Geolocation routing routes users based on their geographic location — continent, country, or US state — regardless of actual network latency. Use latency routing for performance optimization and geolocation routing for compliance requirements like serving different content by country or directing EU users to EU-hosted infrastructure.' },
      { q: 'How does Route 53 Resolver work for hybrid DNS?', a: 'In a hybrid environment, on-premises systems need to resolve AWS private hosted zone names and AWS resources need to resolve on-premises hostnames. Resolver inbound endpoints are ENIs in your VPC that accept DNS queries from on-premises servers forwarded over Direct Connect or VPN. Resolver outbound endpoints forward queries for specified domains to on-premises DNS servers using forwarding rules. This bidirectional setup enables seamless DNS resolution across the hybrid boundary without requiring a custom DNS infrastructure in AWS.' },
      { q: 'How would you migrate DNS from another provider to Route 53 with zero downtime?', a: 'Import all existing records into Route 53 before making any changes. Lower the TTL of all existing records at the current provider to 60 seconds and wait for the old TTL to expire so resolvers flush their caches. Create the hosted zone in Route 53 and populate all records. Verify resolution works by testing against Route 53 name servers directly. Update the domain registrar to use Route 53 name servers. Monitor both old and new authoritative servers for incoming queries and wait until traffic has completely shifted before raising TTLs.' },
    ],
    references: [
      'https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/Welcome.html',
      'https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/routing-policy.html',
    ],
    visualizations: [
      {
        title: 'Route 53 Traffic Flow and Routing Policies',
        description: `Amazon Route 53 is AWS's authoritative DNS service but its real power lies in the routing policy layer that sits above basic name resolution.

The fundamental concept is a hosted zone -- a container for DNS records for a domain. Public hosted zones serve the internet. Private hosted zones associate with one or more VPCs and resolve names only within those VPCs, enabling internal service discovery without a separate DNS infrastructure.

Alias records are the AWS-specific extension that separates Route 53 from standard DNS providers. Unlike CNAME records, alias records work at the zone apex (example.com, not sub.example.com), incur no additional DNS query charges, and automatically track the IP address changes of the underlying AWS resource (ALB, CloudFront, API Gateway, S3 website). Always use alias records for AWS resources.

The six routing policies serve distinct use cases. Simple routes to a single resource with no logic. Weighted splits traffic by percentage -- useful for canary deployments and gradual region migrations. Latency routes to the region with the lowest measured round-trip latency for the requesting resolver -- the standard policy for multi-region active-active. Failover designates primary and secondary records; Route 53 serves the secondary only when the primary health check fails. Geolocation routes based on the geographic origin of the DNS query -- used for compliance (EU users must hit EU infrastructure), content localization, and restricted distribution. Multivalue returns up to eight healthy records, providing basic client-side load balancing.

Health checks are the engine behind failover and latency routing. Route 53 health checkers from six to eight AWS regions probe the endpoint on the configured protocol and port. Three consecutive failures (configurable) mark the record unhealthy and remove it from DNS responses. The TTL on the record determines how quickly client resolvers pick up the change -- set it to 60 seconds for fast failover. Health check endpoints should test application-level health, not just TCP connectivity.

Route 53 Resolver enables hybrid DNS. Inbound endpoints accept queries from on-premises DNS servers forwarded over Direct Connect or VPN. Outbound endpoints forward queries for specified on-premises domains to on-premises DNS servers via resolver rules.`,
      },
    ],
    quickFire: [
      { q: 'What is the difference between an alias record and a CNAME?', a: 'Alias records work at the zone apex, track AWS resource IPs automatically, and have no extra query charge. CNAMEs cannot be used at the zone apex and always incur a DNS lookup cost.' },
      { q: 'What routing policy do you use for multi-region active-active?', a: 'Latency-based routing with health checks -- routes users to the lowest-latency region and automatically removes failing regions from DNS responses.' },
      { q: 'What is the TTL you should set for failover records?', a: '60 seconds -- balances fast failover propagation against resolver cache pressure. Lower TTLs increase DNS query volume.' },
      { q: 'What is the difference between latency routing and geolocation routing?', a: 'Latency routing picks the region with the lowest measured network latency. Geolocation routing picks based on where the user is located geographically, regardless of actual latency -- used for compliance.' },
      { q: 'How many Route 53 health check regions probe an endpoint?', a: 'Between 6 and 8 AWS regions by default, configurable. The endpoint is marked unhealthy only when a threshold percentage of checkers agree it has failed.' },
      { q: 'What are Route 53 Resolver inbound and outbound endpoints for?', a: 'Inbound endpoints allow on-premises DNS servers to resolve AWS private hosted zone names. Outbound endpoints allow VPC resources to resolve on-premises hostnames by forwarding queries to on-premises DNS servers.' },
      { q: 'How do you migrate a domain to Route 53 with zero downtime?', a: 'Reduce TTL at the current registrar to 60s, wait for old TTL to expire, populate all records in Route 53, test against Route 53 nameservers directly, then update the registrar nameservers to Route 53.' },
      { q: 'What is a calculated health check?', a: 'A health check that aggregates multiple child health checks using AND/OR/NOT logic -- used to mark a region healthy only when all critical components (app, database) are healthy.' },
      { q: 'Can Route 53 health checks monitor CloudWatch alarms?', a: 'Yes -- Route 53 supports CloudWatch metric health checks that mark a record unhealthy when a CloudWatch alarm enters ALARM state, enabling application-level health signals to drive DNS failover.' },
    ],
  },
  {
    id: 'aws-elb',
    title: 'ELB — Elastic Load Balancing',
    icon: 'activity',
    color: '#06b6d4',
    description: 'Managed load balancers for distributing traffic across compute targets at any scale.',
    introduction: 'AWS Elastic Load Balancing automatically distributes incoming application traffic across multiple targets — EC2 instances, containers, Lambda functions, or IP addresses — in one or more availability zones. Three load balancer types serve different use cases: Application Load Balancer for HTTP/HTTPS with advanced routing, Network Load Balancer for TCP/UDP at extreme performance, and Gateway Load Balancer for inline virtual appliances.',
    whenToUse: 'Use ALB as the default for web applications and microservices. Use NLB when you need static IPs, extreme throughput, ultra-low latency, or TLS pass-through. Use GLB for deploying third-party firewalls and IDS appliances. All load balancers are managed, multi-AZ, and integrate with Auto Scaling, WAF, ACM, and Route 53.',
    keyConcepts: [
      { title: 'Application Load Balancer', description: 'Layer 7 load balancer that routes based on HTTP host headers, paths, query strings, and source IPs. Supports WebSockets, HTTP/2, and gRPC. Native integration with ECS, EKS, Lambda, and Cognito. Listener rules with priority determine how requests are routed to target groups.' },
      { title: 'Network Load Balancer', description: 'Layer 4 load balancer optimized for TCP, UDP, and TLS traffic. Capable of handling millions of requests per second with single-digit millisecond latency. Supports static Elastic IP addresses per AZ, making it suitable for IP allowlisting. Preserves source IP address by default.' },
      { title: 'Target Groups', description: 'Logical grouping of targets — instances, IPs, Lambda functions, or ALBs. Health checks are configured at the target group level. A single target can be registered in multiple groups. ALB can route to different target groups based on listener rules.' },
      { title: 'Listener Rules', description: 'ALB listeners evaluate rules in priority order to determine routing. Conditions include host header, path pattern, HTTP method, query string, source IP, and HTTP headers. Actions include forward, redirect, fixed response, authenticate-cognito, and authenticate-oidc.' },
      { title: 'Connection Draining (Deregistration Delay)', description: 'When a target is deregistered, the LB waits for in-flight requests to complete before removing it. Default is 300 seconds. Set lower for short-lived requests, higher for long-running ones. During Blue-Green deployments this determines the switchover speed.' },
    ],
    approach: [
      { step: 'Choose the right load balancer type', details: 'ALB for HTTP/HTTPS microservices, gRPC, and WebSocket. NLB for high-throughput TCP, static IPs, and PrivateLink. GLB for transparent inspection appliances. Do not use the Classic Load Balancer for any new workload — it is the previous generation and lacks modern features.' },
      { step: 'Configure health checks correctly', details: 'Health check path should exercise the application, not a static file. Set healthy threshold to 2 and unhealthy threshold to 3. Interval of 15-30 seconds balances detection speed against unnecessary churn. For gRPC services, use the gRPC protocol health check type.' },
      { step: 'Enable access logs', details: 'ALB access logs capture every request with latency, request processing time, and backend processing time. Send to S3 and use Athena for analysis. These logs are essential for debugging latency spikes and identifying slow backend targets. NLB flow logs capture connection-level information.' },
      { step: 'Configure sticky sessions carefully', details: 'Duration-based or application-based stickiness pins users to a specific target. This helps stateful applications but defeats the purpose of horizontal scaling and complicates deployments. Prefer stateless applications with external session stores over sticky sessions.' },
    ],
    pitfalls: [
      { title: 'ALB IP address changes', description: 'ALB DNS names resolve to a changing set of IP addresses managed by AWS. Never hardcode ALB IP addresses in firewall rules or application configuration. Use the DNS name or alias records only. If you need static IPs, use NLB.' },
      { title: 'Slow health check recovery', description: 'After a deployment or scaling event, targets must pass health checks before receiving traffic. If the health check interval is 30 seconds and the healthy threshold is 3, new targets wait up to 90 seconds before receiving traffic. Tune these values to match your application startup time.' },
      { title: 'Cross-zone load balancing costs', description: 'ALB enables cross-zone load balancing by default at no charge. NLB charges for cross-zone data transfer when enabled. Understand your traffic pattern before enabling cross-zone on NLB — if targets are unevenly distributed across AZs, the data transfer cost may be significant.' },
    ],
    keyQuestions: [
      { q: 'How does ALB listener rule routing work and what are the priority rules?', a: 'ALB evaluates listener rules in ascending priority order — lower numbers are evaluated first. Each rule has one or more conditions and an action. The first rule whose all conditions match is applied. The default rule with no conditions always matches and is evaluated last. Conditions can combine host header, path pattern, HTTP method, query strings, headers, and source IP using AND logic within a rule. Multiple path patterns in a single condition use OR logic.' },
      { q: 'When would you choose NLB over ALB?', a: 'Choose NLB when you need static Elastic IP addresses for IP allowlisting by external partners, when you need TLS pass-through to preserve end-to-end encryption to the backend, when your workload requires handling millions of requests per second with sub-millisecond latency, or when you need to expose a service via AWS PrivateLink. ALB cannot provide static IPs and has higher per-request processing overhead due to Layer 7 inspection.' },
      { q: 'How do you implement blue-green deployments with ALB?', a: 'Create two target groups: blue pointing to the current version and green pointing to the new version. Initially, the listener rule routes 100% of traffic to blue. After deploying the new version to the green target group and verifying health, update the listener rule to route to green. For gradual rollout, use weighted target groups to shift traffic incrementally — start at 5% green, then 20%, then 50%, then 100%. Set connection draining timeout to match the maximum request duration to avoid cutting off in-flight requests.' },
      { q: 'How does the ALB integrate with ECS and how are tasks registered as targets?', a: 'ECS integrates with ALB via dynamic port mapping. Each ECS task container is assigned a random host port on the EC2 instance. The ECS service automatically registers task ENIs or instance-port combinations as targets in the target group when tasks start and deregisters them when tasks stop. For Fargate, tasks register their private IP directly. The health check on the target group monitors each task individually, so unhealthy tasks are removed from rotation without affecting healthy ones.' },
    ],
    references: [
      'https://docs.aws.amazon.com/elasticloadbalancing/latest/application/introduction.html',
      'https://docs.aws.amazon.com/elasticloadbalancing/latest/network/introduction.html',
    ],
    visualizations: [
      {
        title: 'ELB Types and Request Routing Architecture',
        description: `AWS Elastic Load Balancing offers three distinct load balancer types, each optimized for a different layer of the network stack.

Application Load Balancer operates at Layer 7 (HTTP/HTTPS). It inspects the full HTTP request and routes based on host headers, path patterns, query strings, HTTP methods, and source IPs. Listener rules are evaluated in priority order -- the first matching rule wins. Target groups hold the actual compute targets (EC2 instances, ECS tasks, Lambda functions, or IP addresses), and each rule action can forward, redirect, or return a fixed response. ALB natively supports WebSocket, HTTP/2, and gRPC. It integrates with Cognito and OIDC for built-in authentication. ALB IP addresses change dynamically -- never hardcode them.

Network Load Balancer operates at Layer 4 (TCP/UDP/TLS). It is designed for extreme throughput -- millions of requests per second with single-digit millisecond latency. NLB provides static Elastic IP addresses per AZ, making it the only choice when partners need to allowlist IP addresses. NLB preserves the client source IP natively. It is the required frontend for AWS PrivateLink endpoint services.

Gateway Load Balancer enables transparent inline inspection. Using GENEVE encapsulation on UDP port 6081, it distributes packets to a fleet of virtual appliances (firewalls, IDS/IPS) without changing source or destination IPs. The original packet is preserved end-to-end through the appliance. GWLB is deployed in a security VPC and integrated with Transit Gateway for centralized inspection across all VPCs.

Target group health checks are the mechanism that removes unhealthy targets from rotation. The health check path should test application-layer health -- not a static file. The healthy/unhealthy thresholds and interval determine how quickly targets are removed and reinstated. Connection draining (deregistration delay) lets in-flight requests complete before a target is removed -- set it to match your maximum request duration.

For deployments, ALB weighted target groups enable gradual traffic shifting between blue and green target groups without DNS changes, giving precise control over rollout speed and instant rollback capability.`,
      },
    ],
    quickFire: [
      { q: 'What layer does ALB operate at vs NLB?', a: 'ALB operates at Layer 7 (HTTP/HTTPS application layer). NLB operates at Layer 4 (TCP/UDP transport layer).' },
      { q: 'Which load balancer provides static Elastic IP addresses?', a: 'NLB -- one Elastic IP per AZ. ALB IPs are dynamic and change over time. Use NLB when partners need to allowlist specific IPs.' },
      { q: 'Does NLB preserve the client source IP?', a: 'Yes -- NLB preserves the original client source IP by default. ALB replaces it with the ALB IP (use X-Forwarded-For header to access the original).' },
      { q: 'What is connection draining (deregistration delay)?', a: 'The period ELB waits for in-flight requests to complete before removing a deregistering target. Default is 300s -- tune lower for fast-completing requests to speed up deployments.' },
      { q: 'Which load balancer is required for AWS PrivateLink endpoint services?', a: 'NLB -- endpoint services must be backed by a Network Load Balancer.' },
      { q: 'How does ALB handle WebSocket connections?', a: 'ALB natively upgrades HTTP connections to WebSocket via the standard HTTP Upgrade mechanism -- no special configuration needed beyond allowing the WebSocket port in the security group.' },
      { q: 'What is the Classic Load Balancer status in 2026?', a: 'Legacy -- AWS deprecated it. All new workloads should use ALB, NLB, or GWLB. Never create a Classic Load Balancer for new architecture.' },
      { q: 'How do you implement blue-green with ALB?', a: 'Use weighted target group rules -- create blue and green target groups, start at 100/0, shift incrementally (95/5, 80/20, 50/50, 0/100) and roll back instantly by adjusting weights.' },
      { q: 'What is slow start mode on ALB target groups?', a: 'Gradually ramps up traffic to new targets over a configurable period (30-900s) instead of sending full traffic immediately -- prevents overwhelming new instances during warmup.' },
    ],
  },
  {
    id: 'aws-api-gateway',
    title: 'API Gateway — Managed API Layer',
    icon: 'server',
    color: '#06b6d4',
    description: 'Fully managed service for creating, publishing, and securing REST, HTTP, and WebSocket APIs.',
    introduction: 'Amazon API Gateway handles all tasks involved in accepting and processing API calls, including traffic management, authorization, throttling, monitoring, and API version management. It supports three API types: REST APIs with full features, HTTP APIs for lower-cost high-performance proxying, and WebSocket APIs for bidirectional communication. It integrates natively with Lambda, ALB, and any HTTP backend.',
    whenToUse: 'Use API Gateway as the front door for serverless architectures, microservice APIs, and WebSocket backends. HTTP APIs are the default choice for Lambda proxies — they cost 70% less and have lower latency than REST APIs. Use REST APIs when you need request/response transformation, request validation, usage plans, or resource policies.',
    keyConcepts: [
      { title: 'REST vs HTTP APIs', description: 'REST APIs offer the full feature set: request/response mapping templates, usage plans with API keys, resource policies, caching, and all authorizer types. HTTP APIs are simpler, cheaper, and faster with automatic JWT authorization, Lambda proxy integration, and CORS. New projects should default to HTTP APIs unless a REST-specific feature is required.' },
      { title: 'Integrations', description: 'Lambda proxy integration passes the full request context as a structured event. HTTP integration proxies to any HTTP backend. AWS service integration calls AWS APIs directly — useful for putting messages on SQS without Lambda. Mock integration returns a configured response without calling a backend.' },
      { title: 'Authorizers', description: 'Lambda authorizers run custom authorization logic and return an IAM policy. JWT authorizers (HTTP APIs) validate JWTs against a JWKS endpoint without Lambda overhead. IAM authorization requires Signature Version 4 signing for machine-to-machine APIs. Cognito user pool authorizers validate Cognito tokens natively.' },
      { title: 'Throttling and Usage Plans', description: 'Default account limits are 10,000 requests per second. Stage-level throttling sets limits per API. Usage plans combine rate (requests per second) and quota (requests per day/week/month) with API keys for per-client throttling and monetization.' },
      { title: 'Stages and Deployments', description: 'A deployment is a snapshot of the API configuration. Stages are named references to deployments (dev, staging, prod). Stage variables act like environment variables and can parameterize integration URIs, Lambda ARNs, and other configuration. Canary releases allow gradually shifting a percentage of traffic to a new deployment.' },
    ],
    approach: [
      { step: 'Choose HTTP API over REST API by default', details: 'HTTP APIs cost 71% less per million requests and have lower latency. They support Lambda proxy, HTTP proxy, and JWT authorization which covers most use cases. Switch to REST API only when you need request validation, mapping templates, resource policies, VPC links to NLB, or usage plans with API keys.' },
      { step: 'Configure throttling at multiple levels', details: 'Set account-level burst and rate limits as a ceiling. Add stage-level throttling for each environment. Add method-level throttling for sensitive endpoints like authentication. For public APIs with API keys, use usage plans to enforce per-client quotas and prevent one client from starving others.' },
      { step: 'Enable CloudWatch logging and X-Ray', details: 'Enable execution logging at INFO level during development and ERROR level in production to reduce cost. Access logs capture per-request data in a structured format. Enable X-Ray tracing to get end-to-end traces from API Gateway through Lambda and downstream services.' },
      { step: 'Handle CORS correctly', details: 'For HTTP APIs, enable CORS in the API configuration — it automatically handles OPTIONS preflight requests. For REST APIs, enable CORS on each resource and ensure the Lambda function or integration returns the appropriate Access-Control-Allow-Origin header. Misconfigured CORS is a common source of frontend integration failures.' },
    ],
    pitfalls: [
      { title: '29-second timeout limit', description: 'API Gateway has a hard maximum integration timeout of 29 seconds for all API types. Long-running Lambda functions are incompatible with synchronous API Gateway invocations. Use asynchronous patterns — have the API start an async job and return a 202 with a job ID, then poll a status endpoint.' },
      { title: 'Cold starts amplified by API Gateway', description: 'Lambda cold starts add latency visible to the API client. Combined with API Gateway processing time, a cold start can add 500ms to several seconds to response time. Mitigate with Lambda provisioned concurrency for latency-sensitive endpoints and HTTP APIs which have lower overhead than REST APIs.' },
      { title: 'Stage URL structure in client code', description: 'REST API URLs include the stage name in the path: /prod/resource. If clients hardcode the stage name, changing stages requires client changes. Use a custom domain with base path mappings to expose a clean URL without the stage name in the path.' },
    ],
    keyQuestions: [
      { q: 'What is the difference between a Lambda proxy integration and a non-proxy integration?', a: 'In Lambda proxy integration, API Gateway passes the entire HTTP request — headers, path parameters, query strings, body, and context — as a structured JSON event. The Lambda function is responsible for returning a properly structured response object with statusCode, headers, and body. In non-proxy integration, API Gateway uses Velocity Template Language mapping templates to transform the request into a custom format before invoking Lambda and transforms the response back. Proxy integration is simpler and is the right default; non-proxy integration is used when the Lambda function cannot be modified or when complex transformation is required at the gateway layer.' },
      { q: 'How do you secure an API Gateway endpoint?', a: 'For public user-facing APIs, use a JWT authorizer against a Cognito user pool or Auth0 JWKS endpoint — the authorizer validates the token signature and claims without a Lambda invocation. For machine-to-machine APIs, use IAM authorization with Signature Version 4 signing and restrict callers using IAM policies. For sensitive internal APIs, add a resource policy that restricts access to specific VPCs using aws:sourceVpc conditions. Always enable AWS WAF for public APIs to protect against common web attacks and rate limit abusive clients.' },
      { q: 'How do you handle versioning in API Gateway?', a: 'Use URL path versioning with stages or custom domains with base path mappings. Deploy breaking changes as a new stage or a new API with a v2 base path. Use stage variables to point different stages at different Lambda function aliases (production stage points to the prod alias, staging stage points to the staging alias). For gradual rollout of a new version, use API Gateway canary releases to send a percentage of traffic to the new deployment while keeping rollback capability.' },
      { q: 'How does API Gateway handle burst traffic?', a: 'API Gateway uses a token bucket algorithm. The burst limit (default 5,000 requests) is the maximum number of concurrent requests the account can handle. The rate limit is the steady-state maximum requests per second. When traffic exceeds the burst limit, API Gateway returns 429 Too Many Requests. Design clients to implement exponential backoff with jitter when they receive 429 responses. For predictable traffic spikes, request a burst limit increase through AWS support in advance.' },
    ],
    references: [
      'https://docs.aws.amazon.com/apigateway/latest/developerguide/welcome.html',
      'https://docs.aws.amazon.com/apigateway/latest/developerguide/http-api.html',
    ],
    visualizations: [
      {
        title: 'API Gateway Request Lifecycle and Integration Types',
        description: `API Gateway sits between clients and backend compute, handling traffic management, authorization, throttling, and protocol translation. Understanding the request lifecycle is key to diagnosing latency, errors, and billing.

A request enters through a listener stage (e.g., /prod). The stage routes to a method resource (/users GET). API Gateway first evaluates the authorizer -- Lambda authorizer, JWT authorizer (HTTP APIs only), IAM Signature V4, or Cognito user pool. A failed authorization returns 401 or 403 immediately without touching the backend.

For REST APIs, a request mapping template (Velocity Template Language) can transform the incoming request before forwarding to the integration. For Lambda proxy integration no transformation occurs -- the full HTTP context is passed as a structured JSON event. The Lambda function must return a valid API Gateway response object with statusCode, headers, and body. For HTTP proxy integration, API Gateway forwards the request verbatim to the backend URL and returns the response verbatim.

The 29-second integration timeout is a hard limit across all API Gateway types -- it cannot be raised. This is the primary architectural constraint that forces long-running workloads into async patterns: the API accepts the request, starts an async job (Lambda invoke async, SQS, Step Functions), returns 202 Accepted with a job ID, and the client polls a status endpoint.

HTTP APIs are the default choice for new Lambda-backed APIs in 2026. They cost 71% less per million requests than REST APIs, have lower latency, and support JWT authorization natively without a Lambda authorizer. REST APIs are required only for: request validation, VTL mapping templates, resource policies, usage plans with API keys, VPC links to NLBs, and AWS service integrations without Lambda.

Throttling uses a token bucket: the burst limit (default 5,000) is the maximum concurrent requests; the rate limit is steady-state requests per second. Exceeding either returns 429. Stage-level and method-level throttling override account defaults for specific APIs.`,
      },
    ],
    quickFire: [
      { q: 'What is the maximum integration timeout for API Gateway?', a: '29 seconds -- hard limit, cannot be increased. Long-running operations must use async patterns (return 202, poll status).' },
      { q: 'When should you use HTTP API vs REST API?', a: 'HTTP API by default -- 71% cheaper, lower latency, native JWT auth. Use REST API only when you need mapping templates, request validation, usage plans, or resource policies.' },
      { q: 'What HTTP status code does API Gateway return when throttled?', a: '429 Too Many Requests. Clients should implement exponential backoff with jitter when receiving 429 responses.' },
      { q: 'What is Lambda proxy integration?', a: 'API Gateway passes the full HTTP request as a structured JSON event to Lambda. Lambda returns a response object with statusCode, headers, and body -- API Gateway does no request/response transformation.' },
      { q: 'How do you remove the stage name from API Gateway URLs?', a: 'Use a custom domain name with a base path mapping. Map the custom domain (api.example.com) to the API and stage, presenting a clean URL without /prod or /v1 in the path.' },
      { q: 'What is the difference between a Lambda authorizer and a JWT authorizer?', a: 'A Lambda authorizer runs custom code and returns an IAM policy -- flexible but adds ~100ms latency per request. A JWT authorizer (HTTP API) validates JWTs directly without invoking Lambda -- faster and cheaper.' },
      { q: 'How does API Gateway burst limit work?', a: 'Token bucket algorithm -- burst limit (default 5,000) is the bucket size for handling traffic spikes. Rate limit is the steady-state refill rate. Requests exceeding the burst get 429 until the bucket refills.' },
      { q: 'How do you enable CORS on an HTTP API?', a: 'Configure CORS in the API Gateway HTTP API settings -- it automatically generates correct preflight responses. For REST APIs you must enable CORS per resource and ensure your Lambda returns Access-Control-Allow-Origin headers.' },
      { q: 'What is a mock integration in API Gateway?', a: 'A mock integration returns a configured response without calling any backend -- useful for returning static responses, building API contracts before backend exists, or handling OPTIONS preflight without Lambda.' },
    ],
  },
  {
    id: 'aws-direct-connect',
    title: 'Direct Connect — Dedicated Network Connectivity',
    icon: 'server',
    color: '#06b6d4',
    description: 'Dedicated private network connection between on-premises data centers and AWS.',
    introduction: 'AWS Direct Connect establishes a dedicated network connection between your premises and AWS bypassing the public internet. It provides consistent network performance, reduced bandwidth costs for high-volume data transfer, and lower latency compared to internet-based VPN. Available in 1 Gbps, 10 Gbps, and 100 Gbps port speeds from Direct Connect locations worldwide.',
    whenToUse: 'Use Direct Connect for workloads requiring consistent sub-10ms latency to AWS, high-volume data transfer where bandwidth costs are significant, compliance requirements mandating private connectivity, and hybrid architectures where on-premises and cloud systems must communicate continuously at high throughput.',
    keyConcepts: [
      { title: 'Dedicated vs Hosted Connections', description: 'Dedicated connections are physical ports provisioned directly to your account in 1G, 10G, or 100G speeds. Hosted connections are sub-ports provisioned through an AWS Direct Connect partner, available in sizes from 50 Mbps to 10 Gbps. Hosted connections are faster to provision and allow smaller increments.' },
      { title: 'Virtual Interfaces', description: 'Private VIF connects to a VPC via a Virtual Private Gateway or Direct Connect Gateway. Public VIF connects to AWS public endpoints like S3 and DynamoDB. Transit VIF connects to a Direct Connect Gateway associated with a Transit Gateway for multi-VPC access. Each VIF uses BGP for route exchange.' },
      { title: 'Direct Connect Gateway', description: 'Enables a single Direct Connect connection to access VPCs across multiple AWS regions and accounts. Associate up to three Transit Gateways per DXGW for multi-VPC access. Eliminates the need for separate connections or VIFs per region.' },
      { title: 'Resilience and Redundancy', description: 'A single Direct Connect connection has no SLA. For high availability, provision connections at two separate Direct Connect locations. For maximum resilience, combine two Direct Connect connections from different locations with a VPN backup over the internet. AWS Resiliency Toolkit provides a checklist for each tier.' },
      { title: 'MACsec', description: 'IEEE 802.1AE encryption standard that provides point-to-point encryption on the Direct Connect link. Available on dedicated 10G and 100G connections. Provides wire-speed encryption without the overhead of IPsec VPN, meeting compliance requirements for encryption in transit on private links.' },
    ],
    approach: [
      { step: 'Plan for redundancy from day one', details: 'AWS offers four resilience tiers. Development uses a single connection. High availability uses two connections at one location. Resilient uses two connections at two locations. Maximum resilience uses two connections at two locations plus VPN backup. Start with high availability minimum for production workloads.' },
      { step: 'Choose VIF type based on required resources', details: 'Private VIF for VPC resources accessed by private IP. Transit VIF for multi-VPC or multi-region access through Transit Gateway. Public VIF for S3, DynamoDB, SNS, SQS accessed over public IPs without traversing the internet. A connection can have multiple VIFs of different types.' },
      { step: 'Configure BGP with prefix filtering', details: 'On the private VIF, AWS advertises the VPC CIDR ranges. On-premises router advertises its subnets. Always use BGP prefix lists to filter what routes are accepted and advertised — accepting all routes from AWS could introduce unexpected routing changes. Use AS-path prepending to prefer one connection over another for traffic engineering.' },
      { step: 'Measure and establish baseline performance', details: 'After provisioning, run iperf3 tests to validate actual throughput. Measure round-trip latency and establish a baseline. Set CloudWatch alarms on connection state and BPS metrics. Direct Connect connection state changes trigger SNS notifications for rapid response to link failures.' },
    ],
    pitfalls: [
      { title: 'Single connection has no SLA', description: 'AWS does not provide an SLA for a single Direct Connect connection. Physical fiber cuts or facility maintenance can cause outages. Treat a single connection as a development or testing setup only. Production workloads require at minimum two connections at separate Direct Connect locations.' },
      { title: 'Route propagation asymmetry', description: 'BGP path selection determines which connection traffic uses. If not configured intentionally, inbound and outbound traffic may take different connections, causing asymmetric routing that complicates troubleshooting. Use consistent BGP attributes on both connections to control path preference.' },
      { title: 'Insufficient bandwidth planning', description: 'Direct Connect bandwidth cannot be quickly increased — dedicated connections require provisioning lead times of weeks. Over-provision capacity initially or use hosted connections from a partner that can be resized more quickly. Monitor BPS utilization and initiate upgrade requests when consistently above 70%.' },
    ],
    keyQuestions: [
      { q: 'How do you design a highly available Direct Connect architecture?', a: 'Deploy two dedicated connections from different Direct Connect locations to the same Direct Connect Gateway. Connect the DXGW to a Transit Gateway for multi-VPC access. Configure BGP with AS-path prepending to prefer one connection as primary and use the other as secondary. Add a Site-to-Site VPN backup over the internet as a third failover layer for maximum resilience. Test failover by administratively bringing down each link and verifying traffic switches within the expected BGP convergence time.' },
      { q: 'What is the difference between a private VIF and a transit VIF?', a: 'A private VIF connects to a single VPC through its Virtual Private Gateway, providing access only to resources in that specific VPC. A transit VIF connects to a Direct Connect Gateway that is associated with one or more Transit Gateways, enabling access to all VPCs attached to those Transit Gateways across multiple regions and accounts through a single connection and VIF. Transit VIFs require a separate Direct Connect Gateway and Transit Gateway but dramatically reduce the number of VIFs and connections needed in multi-VPC environments.' },
      { q: 'How does Direct Connect pricing compare to VPN and what drives the decision?', a: 'Direct Connect charges a port hour fee plus a per-GB data transfer rate out of AWS. VPN charges only per connection hour plus standard internet data transfer rates. For large volumes of data transfer (typically over 1TB per month), Direct Connect data transfer rates are lower than internet rates, making it cheaper. VPN is simpler and faster to set up with no hardware procurement, making it suitable for smaller workloads. The decision is driven by throughput requirements, latency sensitivity, compliance requirements for private connectivity, and the volume of data transfer.' },
      { q: 'What is MACsec and when would you enable it on Direct Connect?', a: 'MACsec provides point-to-point Layer 2 encryption on the Direct Connect physical link between your router and the AWS Direct Connect device. It encrypts traffic at wire speed with minimal latency overhead. Enable it when compliance frameworks like PCI-DSS or HIPAA require encryption of data in transit on private links, or when your security policy mandates encryption even on private network segments. MACsec requires hardware support on your router and is only available on dedicated 10G and 100G connections, not hosted connections.' },
    ],
    references: [
      'https://docs.aws.amazon.com/directconnect/latest/UserGuide/Welcome.html',
      'https://docs.aws.amazon.com/directconnect/latest/UserGuide/resiliency_toolkit.html',
    ],
    visualizations: [
      {
        title: 'Direct Connect Architecture and Redundancy Tiers',
        description: `AWS Direct Connect replaces or supplements internet-based VPN by providing a dedicated physical network connection between your premises and an AWS Direct Connect location. Traffic never traverses the public internet, providing consistent latency, predictable throughput, and lower data transfer costs at high volumes.

The physical connection (1G, 10G, or 100G dedicated port, or 50Mbps-10G hosted connection through a partner) terminates at a Direct Connect location -- co-location facilities AWS operates in partnership with network providers worldwide. From there, traffic flows over the AWS global backbone to your target VPC or public AWS endpoints.

Virtual Interfaces (VIFs) are logical sub-connections over the physical port. A private VIF uses BGP to exchange routes with a single VPC's Virtual Private Gateway. A transit VIF connects to a Direct Connect Gateway (DXGW), which can bridge to Transit Gateways across multiple regions -- one physical connection, one DXGW, access to hundreds of VPCs across regions. A public VIF provides access to all AWS public IP space (S3, DynamoDB, API endpoints) without going over the internet.

AWS defines four resilience tiers. Development: single connection, no SLA. High availability: two connections at one Direct Connect location. Resilient: two connections at two locations. Maximum resilience: two connections at two locations plus Site-to-Site VPN backup over the internet. Production workloads should start at high availability minimum.

BGP configuration is where most operational complexity lives. Each VIF runs a BGP session. AWS advertises VPC CIDRs to your router; you advertise your on-premises subnets. Always use BGP prefix filters to control exactly which routes are accepted and advertised -- accepting all routes blindly can import unexpected paths. Use AS-path prepending or BGP local preference to establish primary and backup paths across two connections.

MACsec (IEEE 802.1AE) provides wire-speed Layer 2 encryption on dedicated 10G and 100G connections for compliance requirements mandating in-transit encryption even on private links.`,
      },
    ],
    quickFire: [
      { q: 'What speeds are available for dedicated Direct Connect connections?', a: '1 Gbps, 10 Gbps, and 100 Gbps. Hosted connections through partners offer 50 Mbps to 10 Gbps.' },
      { q: 'Does a single Direct Connect connection have an SLA?', a: 'No -- AWS provides no SLA for a single connection. Minimum for production is two connections at separate Direct Connect locations (Resilient tier).' },
      { q: 'What is the difference between a private VIF and a transit VIF?', a: 'Private VIF connects to one VPC via its Virtual Private Gateway. Transit VIF connects to a Direct Connect Gateway linked to a Transit Gateway, enabling access to many VPCs across regions.' },
      { q: 'What is a Direct Connect Gateway?', a: 'A global resource that bridges a Direct Connect connection to Transit Gateways (via transit VIF) or VGWs (via private VIF) across multiple regions and accounts -- one connection reaches VPCs worldwide.' },
      { q: 'What does MACsec provide on Direct Connect?', a: 'IEEE 802.1AE Layer 2 wire-speed encryption on dedicated 10G/100G connections. Satisfies compliance requirements for encryption in transit on private links without IPsec overhead.' },
      { q: 'How does Direct Connect compare to VPN on data transfer cost?', a: 'Direct Connect data-out rates (typically $0.02/GB) are lower than internet rates for high volumes. The crossover where Direct Connect is cheaper is usually around 1TB/month of data transfer out of AWS.' },
      { q: 'What causes asymmetric routing on Direct Connect and why does it matter?', a: 'If BGP path selection is not configured symmetrically, inbound and outbound traffic may take different connections. This complicates troubleshooting and can break stateful inspection. Use consistent BGP attributes on both connections.' },
      { q: 'How do you monitor Direct Connect connection health?', a: 'CloudWatch metrics: ConnectionState (0=down, 1=up), ConnectionBpsIngress, ConnectionBpsEgress. Set alarms on ConnectionState to page on-call immediately when a link drops.' },
      { q: 'What is a hosted Direct Connect connection?', a: 'A sub-port provisioned by an AWS Direct Connect Partner, available in smaller increments (50Mbps-10Gbps). Faster to provision than dedicated ports. One hosted connection = one VIF maximum.' },
    ],
  },
  {
    id: 'aws-transit-gateway',
    title: 'Transit Gateway — Cloud Router',
    icon: 'layers',
    color: '#06b6d4',
    description: 'Regional managed router connecting VPCs, VPNs, and Direct Connect at scale.',
    introduction: 'AWS Transit Gateway is a network transit hub that interconnects VPCs and on-premises networks through a central managed router. It replaces complex VPC peering meshes with a hub-and-spoke model, supports transitive routing, enables centralized inspection and egress, and integrates with Direct Connect for hybrid connectivity. Each Transit Gateway is regional and supports up to 5,000 VPC attachments.',
    whenToUse: 'Use Transit Gateway when you need transitive routing between more than two VPCs, when you need to share a single Direct Connect or VPN connection across multiple VPCs, when you want centralized egress through a shared NAT gateway VPC, or when you are building a multi-account AWS Organizations environment with a network hub account.',
    keyConcepts: [
      { title: 'Attachments', description: 'VPC attachments connect a VPC to the TGW using one ENI per AZ. VPN attachments create Site-to-Site VPN tunnels. Direct Connect Gateway attachments connect via a transit VIF. Peering attachments connect Transit Gateways in different regions. Each attachment can be associated with one TGW route table.' },
      { title: 'Route Tables', description: 'TGW route tables define how packets are forwarded between attachments. Each attachment has an association (which route table it reads from) and propagations (which route tables it advertises its routes to). Multiple route tables enable segmentation — prod and dev VPCs can use different tables with no cross-routes.' },
      { title: 'Appliance Mode', description: 'When enabled on a VPC attachment, Transit Gateway uses a consistent hash to route both directions of a flow through the same AZ attachment. Required for stateful inspection appliances — without it, asymmetric routing sends request and response through different AZ instances of the appliance.' },
      { title: 'TGW Peering', description: 'Connects Transit Gateways in different AWS regions. Traffic flows over the AWS global backbone. Static routes must be manually added — dynamic route propagation does not work across peering connections. Enables global hub-and-spoke architectures.' },
      { title: 'Network Manager', description: 'Global view of your network topology including TGWs, VPN, and Direct Connect across regions and accounts. Integrates with SD-WAN partners for unified on-premises and cloud visibility. Provides Reachability Analyzer for automated path analysis and troubleshooting.' },
    ],
    approach: [
      { step: 'Design route table segmentation', details: 'Create one TGW route table per security domain: production, non-production, shared services, inspection. Associate each VPC attachment with its domain route table. Propagate routes selectively — shared services propagates to all domains, prod does not propagate to non-prod. This architecture eliminates the need for security group rules to enforce environment isolation.' },
      { step: 'Implement centralized egress pattern', details: 'Create a dedicated egress VPC with NAT gateways per AZ. Add a default route in all spoke VPC TGW route tables pointing to the egress VPC attachment. The egress VPC route table routes 0.0.0.0/0 to the internet gateway. Spoke VPC route tables do not have an internet gateway. All outbound internet traffic flows through the single centralized NAT.' },
      { step: 'Implement centralized inspection pattern', details: 'Deploy a security VPC with Gateway Load Balancer and inspection appliances. Insert the security VPC between all inter-VPC and egress flows using TGW route tables. Spoke VPCs send all traffic to TGW, TGW routes to inspection VPC, GWLB distributes to appliances, appliances return to GWLB, GWLB sends back to TGW for delivery.' },
      { step: 'Monitor TGW metrics and flow logs', details: 'Enable TGW flow logs to capture five-tuple flows across all attachments. Key CloudWatch metrics include BytesIn, BytesOut, PacketsIn, PacketsOut, and PacketDropCountBlackhole. Alert on BlackholeDropCount which indicates a route is missing for traffic that matches the TGW routing table.' },
    ],
    pitfalls: [
      { title: 'Default route table gotcha', description: 'By default, all TGW attachments associate with and propagate to a single default route table, creating full transitive connectivity. In a multi-environment setup this is a security risk — prod, dev, and staging can reach each other. Always disable association propagation defaults and explicitly manage route tables from the start.' },
      { title: 'Inter-region peering is static routes only', description: 'Unlike VPC attachments which support dynamic BGP propagation, Transit Gateway peering connections require static routes. Both TGWs need manual route table entries for the remote CIDRs. Changes to remote VPC CIDRs require manual route table updates on both ends.' },
      { title: 'Bandwidth limits per attachment', description: 'Each TGW attachment has a maximum bandwidth limit. A VPC with very high-throughput requirements may need multiple attachments. Monitor BytesIn and BytesOut per attachment and compare to the documented limits before hitting them in production.' },
    ],
    keyQuestions: [
      { q: 'How do you use Transit Gateway route tables to enforce network segmentation?', a: 'Create separate TGW route tables for each trust boundary: production, non-production, and shared services. Associate each VPC attachment with exactly one route table. Use propagation controls so each VPC advertises its routes only to the route tables it should be reachable from. Production VPCs propagate to the production table. The shared services VPC propagates to all tables. Non-production VPCs propagate only to the non-production table. This means a production VPC looking up a route to a dev VPC finds no route and traffic is dropped at the TGW without any security group changes needed.' },
      { q: 'Walk me through the traffic flow in a centralized inspection architecture.', a: 'A request from VPC-A to VPC-B first exits VPC-A to the TGW. The TGW route table for VPC-A has a route for VPC-B CIDR pointing to the inspection VPC attachment rather than directly to VPC-B. The TGW forwards the packet to the inspection VPC. Gateway Load Balancer distributes it to a firewall appliance. The appliance inspects and allows the packet. The packet returns to the GWLB endpoint, which sends it back to the TGW. The TGW now forwards to VPC-B using the normal route. Appliance mode must be enabled on the inspection VPC attachment to ensure the return packet takes the same appliance.' },
      { q: 'How does Transit Gateway connect to Direct Connect?', a: 'Create a transit VIF on the Direct Connect connection, then create a Direct Connect Gateway and associate it with the Transit Gateway. The DXGW serves as the bridge between the physical Direct Connect connection and the TGW. On-premises BGP peering is with the DXGW, which distributes routes to and from the TGW. One DXGW can connect to TGWs in multiple regions, enabling a single Direct Connect connection to reach VPCs across the global AWS network.' },
      { q: 'What are the TGW limits you need to be aware of for large deployments?', a: 'Key limits include 5,000 VPC attachments per TGW, 50 peering connections per TGW, 20 TGW route tables per TGW, and the maximum bandwidth per VPC attachment. The 5,000 VPC limit is sufficient for most organizations but large enterprises approaching it should consider multiple TGWs with peering connections. Route table counts limit segmentation granularity. Attachment bandwidth limits constrain high-throughput applications and may require multiple attachments or moving the workload to a dedicated VPC with its own TGW.' },
    ],
    references: [
      'https://docs.aws.amazon.com/vpc/latest/tgw/what-is-transit-gateway.html',
      'https://docs.aws.amazon.com/vpc/latest/tgw/tgw-route-tables.html',
    ],
    visualizations: [
      {
        title: 'Transit Gateway Hub-and-Spoke and Route Table Segmentation',
        description: `AWS Transit Gateway is a regional managed router that replaces VPC peering meshes. The key architectural concept is the relationship between attachments, route tables, and route propagation.

Every attachment to the TGW (VPC, VPN, Direct Connect Gateway, or peering connection to another TGW) has two route-table relationships: an association (which route table the attachment reads from to forward packets) and propagations (which route tables the attachment advertises its CIDR into). This two-sided model enables fine-grained segmentation.

A standard three-domain segmentation pattern works as follows. Create three TGW route tables: Production, NonProduction, and SharedServices. Associate production VPC attachments with the Production route table. Associate dev/staging attachments with the NonProduction route table. Associate shared services (DNS, monitoring, artifact registries) with the SharedServices route table. SharedServices propagates its CIDRs into both Production and NonProduction so all environments can reach shared services. Production and NonProduction propagate only into their own route tables -- there are no cross-routes, so a dev workload has no routable path to production even though both attach to the same TGW.

Centralized egress adds a dedicated Egress VPC with NAT gateways. All spoke VPC route tables get a default route (0.0.0.0/0) pointing to the TGW. The TGW routes default traffic to the Egress VPC attachment. The Egress VPC routes it to its Internet Gateway. This pattern eliminates NAT gateways in every spoke VPC -- a 20-VPC environment saves 20 * 3 AZs * NAT hourly rate per hour.

Centralized inspection routes all inter-VPC and egress traffic through a security VPC running GWLB and firewall appliances. Appliance mode on the security VPC attachment is mandatory to ensure the firewall instance sees both directions of each flow.

TGW Network Manager provides a global topology view and Reachability Analyzer for automated path verification -- essential for validating segmentation after every route table change.`,
      },
    ],
    quickFire: [
      { q: 'What is the maximum number of VPC attachments per Transit Gateway?', a: '5,000 VPC attachments per TGW.' },
      { q: 'What are the two route-table relationships every TGW attachment has?', a: 'Association (which route table it reads/forwards from) and propagation (which route tables it advertises its CIDR into). Both are independently configurable.' },
      { q: 'What is the default TGW behavior you should always disable for production?', a: 'Default route table association and propagation -- it puts all attachments in one table with full transitive connectivity. Disable it and build explicit segmented route tables.' },
      { q: 'What does TGW appliance mode do?', a: 'Forces both directions of a TCP flow through the same AZ attachment, ensuring stateful firewall appliances see both request and response on the same instance.' },
      { q: 'Does TGW peering support dynamic route propagation?', a: 'No -- TGW-to-TGW peering requires static routes on both sides. Dynamic BGP propagation only works for VPC attachments within a single TGW.' },
      { q: 'What CloudWatch metric indicates a missing route in TGW?', a: 'PacketDropCountBlackhole -- packets that matched a TGW route table but had no attachment to forward to. Alarm on this metric.' },
      { q: 'How does centralized egress save money?', a: 'Consolidates all NAT gateways from N spoke VPCs into one shared Egress VPC. Reduces NAT gateway hourly charges by N*3 AZs - 3 gateways.' },
      { q: 'Can one Direct Connect connection reach multiple regions through TGW?', a: 'Yes -- one Direct Connect connection creates a transit VIF to a Direct Connect Gateway, which can associate with TGWs in multiple AWS regions.' },
      { q: 'How many TGW route tables can one TGW have?', a: '20 TGW route tables per Transit Gateway.' },
    ],
  },
  {
    id: 'aws-privatelink',
    title: 'PrivateLink — Private Service Endpoints',
    icon: 'shield',
    color: '#06b6d4',
    description: 'Private connectivity to AWS services and your own services without internet exposure.',
    introduction: 'AWS PrivateLink provides private connectivity between VPCs, AWS services, and on-premises networks without exposing traffic to the public internet. It uses Interface VPC Endpoints powered by ENIs in your subnets as the entry point for traffic destined to services. PrivateLink eliminates the need for internet gateways, NAT devices, VPC peering, or VPN connections to access AWS services or share your own services with other customers.',
    whenToUse: 'Use PrivateLink to access AWS managed services like S3, DynamoDB, SQS, and Secrets Manager from within a VPC without internet routing. Use it to expose your own services to other VPCs or AWS accounts without giving them network access to your VPC. It is the recommended pattern for SaaS providers and multi-account architectures where service-level isolation is required.',
    keyConcepts: [
      { title: 'Interface Endpoints', description: 'ENIs placed in your VPC subnets with private IP addresses. DNS for the service resolves to these private IPs within the VPC. Traffic stays on the AWS network. Support security groups for access control. Available for most AWS services and marketplace products.' },
      { title: 'Gateway Endpoints', description: 'Route table entries (not ENIs) that direct traffic for S3 and DynamoDB to the AWS service without traversing the internet or NAT. Free of charge. Target added to route tables for subnets that need access. Limited to S3 and DynamoDB.' },
      { title: 'Endpoint Services', description: 'Your own NLB-backed service exposed to other VPCs through PrivateLink. The service owner creates an endpoint service associated with an NLB. Consumers create an interface endpoint pointing to the service. The service owner approves connection requests and controls access via allowlists.' },
      { title: 'DNS Resolution', description: 'When an interface endpoint is created, AWS generates private DNS names. With private DNS enabled, the standard public service DNS name resolves to the endpoint ENI IP within the VPC, transparently redirecting traffic without application changes. Requires both enableDnsHostnames and enableDnsSupport on the VPC.' },
      { title: 'Endpoint Policies', description: 'Resource-based JSON policies attached to an endpoint that restrict which principals can perform which actions on which resources through the endpoint. More restrictive than IAM policies — both must allow access. Useful for preventing data exfiltration through endpoints.' },
    ],
    approach: [
      { step: 'Replace NAT gateway access to AWS services with endpoints', details: 'Identify AWS API calls from private subnets: Secrets Manager, SSM Parameter Store, ECR, CloudWatch Logs, SQS, SNS, and S3. Create interface endpoints for each service in each VPC that uses them. This eliminates NAT gateway charges for that traffic, improves security by keeping traffic off the internet, and often reduces latency.' },
      { step: 'Use Gateway Endpoints for S3 and DynamoDB', details: 'Gateway endpoints are free and have no ENI overhead. Add them to route tables for all private and isolated subnets. Combine with endpoint policies to restrict which S3 buckets are accessible through the endpoint, preventing EC2 instances from accessing unauthorized buckets.' },
      { step: 'Expose internal services via endpoint services', details: 'Front the service with an NLB. Create an endpoint service, whitelist consumer accounts, and share the service name. Consumers create interface endpoints in their VPCs pointing to the service name. Traffic flows from consumer ENI through the AWS fabric to the NLB without any peering or shared network access.' },
      { step: 'Configure endpoint policies for data exfiltration prevention', details: 'On S3 gateway endpoints, add a policy that restricts access to org-owned buckets using the aws:ResourceOrgID condition. This prevents malicious code running on EC2 from exfiltrating data to attacker-controlled S3 buckets through the same endpoint path that legitimate code uses.' },
    ],
    pitfalls: [
      { title: 'Endpoint per AZ cost', description: 'Interface endpoints are charged per endpoint per AZ per hour plus per GB processed. Creating endpoints for many services across many AZs and VPCs can be expensive. Centralize endpoints in a shared services VPC and route other VPCs through it via Transit Gateway if endpoint costs are a concern.' },
      { title: 'Private DNS requires VPC DNS settings', description: 'Private DNS for interface endpoints requires enableDnsHostnames and enableDnsSupport to both be true on the VPC. If either is disabled, the standard DNS name will not resolve to the private endpoint IP and traffic will either fail or route through the internet.' },
      { title: 'Endpoint service approval process', description: 'By default, endpoint service connections require manual approval from the service owner. This creates operational overhead in automated environments. Configure automatic acceptance for trusted consumer account IDs to streamline cross-account connectivity without manual intervention per connection request.' },
    ],
    keyQuestions: [
      { q: 'What is the difference between Gateway Endpoints and Interface Endpoints?', a: 'Gateway endpoints work only for S3 and DynamoDB. They add an entry to a VPC route table and are free. Interface endpoints work for most AWS services and third-party services. They create ENIs in your subnets with private IP addresses, support security groups and endpoint policies, and incur hourly and per-GB charges. Gateway endpoints are the right choice for S3 and DynamoDB due to cost. Interface endpoints are required for all other services including Secrets Manager, SSM, ECR, and CloudWatch Logs.' },
      { q: 'How do you expose a service to external customers using PrivateLink without giving them network access to your VPC?', a: 'Create an endpoint service backed by a Network Load Balancer that fronts your service. Share the endpoint service name with customers. The customer creates an interface endpoint in their VPC pointing to your service name. A connection request is generated that you approve manually or automatically. Once approved, the customer ENI receives a private IP in their subnet that connects through the AWS PrivateLink fabric to your NLB. The customer has no network route into your VPC — they can only reach the specific service exposed through the NLB.' },
      { q: 'How do you use PrivateLink to prevent data exfiltration from EC2 instances?', a: 'Attach an endpoint policy to the S3 gateway endpoint that includes a condition restricting access to S3 buckets owned by your AWS Organization using the aws:ResourceOrgID condition key. This means EC2 instances in the VPC can only access S3 buckets within your organization through that endpoint. An attacker who gains code execution cannot use the same endpoint to exfiltrate data to an external S3 bucket. Combine with SCP policies that deny s3:PutObject to buckets outside the organization for defense in depth.' },
      { q: 'How does DNS resolution work for interface endpoints and what breaks if it is misconfigured?', a: 'When you enable private DNS on an interface endpoint, AWS creates a Route 53 private hosted zone that overrides the public DNS name of the service within the VPC. Queries for the service DNS name (e.g., secretsmanager.us-east-1.amazonaws.com) resolve to the private IPs of the endpoint ENIs. If private DNS is enabled but the VPC does not have enableDnsHostnames and enableDnsSupport both set to true, DNS resolution fails and connections to the service break. If private DNS is disabled, you must update application configuration to use the regional endpoint-specific DNS name instead of the standard service name.' },
    ],
    references: [
      'https://docs.aws.amazon.com/vpc/latest/privatelink/what-is-privatelink.html',
      'https://docs.aws.amazon.com/vpc/latest/privatelink/vpc-endpoints.html',
    ],
    visualizations: [
      {
        title: 'PrivateLink Endpoint Types and Traffic Isolation Model',
        description: `AWS PrivateLink solves two distinct problems: accessing AWS-managed services without internet routing, and exposing your own services to other VPCs without giving them network access to your infrastructure.

Gateway Endpoints handle S3 and DynamoDB exclusively. They are route table entries -- not ENIs -- that redirect traffic destined for S3 or DynamoDB prefixes into the AWS service fabric without going through an internet gateway or NAT. Gateway endpoints are free and have no bandwidth limitation. You simply add the endpoint target to route tables for subnets that need private S3/DynamoDB access. Every new VPC should have gateway endpoints for S3 and DynamoDB added immediately.

Interface Endpoints create one or more ENIs in your VPC subnets with private IP addresses from your CIDR range. These ENIs become the private entry point for the target service. Traffic flows from your EC2 or Lambda over the ENI through the AWS PrivateLink fabric to the service -- never touching the internet. Interface endpoints cost approximately $0.01/hour per AZ plus $0.01/GB processed. For a VPC with five services in three AZs, that is 15 hourly charges. Centralize interface endpoints in a shared services VPC and route other VPCs through Transit Gateway to reduce this cost.

The endpoint service pattern enables your own services to be privately consumed by other VPCs or accounts. Front your service with an NLB. Create an endpoint service linked to that NLB. Consumers create interface endpoints pointing to your service name. Their connection request goes to you for approval (manual or automatic). Once approved, a private IP in the consumer's VPC connects through the PrivateLink fabric directly to your NLB -- the consumer has no network route into your VPC whatsoever.

Endpoint policies are JSON resource policies attached to an endpoint. On S3 gateway endpoints, an endpoint policy restricting access to buckets in your AWS Organization (aws:ResourceOrgID condition) is a critical data exfiltration control -- compromised compute cannot upload data to attacker-controlled S3 buckets through the same endpoint.

Private DNS requires both enableDnsHostnames and enableDnsSupport on the VPC. When enabled, the standard service DNS name (e.g., secretsmanager.us-east-1.amazonaws.com) resolves to the endpoint ENI IPs inside the VPC with zero application changes.`,
      },
    ],
    quickFire: [
      { q: 'What is the difference between a Gateway Endpoint and an Interface Endpoint?', a: 'Gateway Endpoints are free route table entries for S3 and DynamoDB only. Interface Endpoints create ENIs in your subnets for most AWS services -- they cost hourly per AZ plus per GB processed.' },
      { q: 'Which AWS services support Gateway Endpoints?', a: 'Only S3 and DynamoDB. All other AWS services require Interface Endpoints.' },
      { q: 'What VPC settings are required for Interface Endpoint private DNS?', a: 'Both enableDnsHostnames and enableDnsSupport must be true on the VPC. Without both, private DNS resolution fails.' },
      { q: 'How does an endpoint service expose your service without VPC peering?', a: 'You front your service with an NLB, create an endpoint service, and consumers create interface endpoints pointing to it. Traffic flows through PrivateLink -- the consumer gets a private IP in their VPC but has no network route into yours.' },
      { q: 'How do you prevent data exfiltration through an S3 gateway endpoint?', a: 'Add an endpoint policy with aws:ResourceOrgID condition restricting access to S3 buckets within your AWS Organization -- prevents uploads to attacker-controlled external buckets.' },
      { q: 'What is the NLB requirement for an endpoint service?', a: 'Endpoint services must be backed by a Network Load Balancer -- not an ALB or GWLB. The NLB is the gateway that PrivateLink routes consumer traffic to.' },
      { q: 'How do you reduce Interface Endpoint costs across many VPCs?', a: 'Create endpoints once in a shared services VPC and route all other VPCs through Transit Gateway to reach them. Pay for one set of endpoints instead of N VPCs * M services * 3 AZs.' },
      { q: 'What happens to endpoint service connections by default -- auto-accept or manual?', a: 'Manual approval by default. The service owner receives a connection request and must approve it. Configure automatic acceptance for trusted consumer account IDs to eliminate operational toil.' },
      { q: 'Can Interface Endpoints be accessed from on-premises via Direct Connect?', a: 'Yes -- if the Interface Endpoint is in a VPC reachable via Direct Connect or VPN, and the endpoint has private DNS enabled or you use the endpoint-specific DNS name, on-premises systems can access AWS services privately.' },
    ],
  },
  {
    id: 'aws-global-accelerator',
    title: 'Global Accelerator — Anycast Network Acceleration',
    icon: 'zap',
    color: '#06b6d4',
    description: 'Anycast routing across the AWS global backbone for consistent low-latency global traffic.',
    introduction: 'AWS Global Accelerator improves availability and performance of applications by routing traffic through the AWS global network instead of the public internet. It provides two static global anycast IP addresses that serve as a fixed entry point to your application endpoints across regions. Users connect to the nearest edge location, and traffic traverses the AWS backbone to the application endpoint, avoiding internet congestion and routing variability.',
    whenToUse: 'Use Global Accelerator for applications requiring consistent global performance, applications with users distributed across multiple continents, non-HTTP protocols that cannot use CloudFront, gaming backends requiring stable IPs and low latency, and disaster recovery architectures requiring instant traffic failover without DNS propagation delays.',
    keyConcepts: [
      { title: 'Anycast IP Addresses', description: 'Two static global IP addresses are assigned to each accelerator. These IPs are advertised from all AWS edge locations simultaneously. Users connect to the nearest edge location automatically based on BGP anycast routing, reducing first-hop latency significantly compared to resolving a regional DNS name.' },
      { title: 'Listeners and Endpoint Groups', description: 'Listeners define the protocol and port ranges that Global Accelerator accepts. Each listener has endpoint groups — one per AWS region. Endpoint groups contain the actual endpoints: ALBs, NLBs, EC2 instances, or Elastic IPs with traffic weights and health check configurations.' },
      { title: 'Traffic Dials', description: 'Each endpoint group has a traffic dial from 0 to 100 percent. Lowering the dial for a region reduces traffic sent there, enabling gradual regional migrations and canary deployments. Setting a dial to zero removes a region from rotation without removing the endpoint group configuration.' },
      { title: 'Health Checks', description: 'Global Accelerator performs continuous health checks on each endpoint. Unhealthy endpoints are removed from routing within 30 seconds. Traffic automatically shifts to healthy endpoints in other regions. This enables near-instant failover without any DNS propagation delay.' },
      { title: 'Client Affinity', description: 'Session affinity based on source IP address pins a client to a specific endpoint for the duration of the configured affinity period. Useful for stateful protocols where session state is stored locally at the endpoint rather than in a shared store.' },
    ],
    approach: [
      { step: 'Determine if Global Accelerator or CloudFront is appropriate', details: 'CloudFront is better for HTTP/HTTPS content with caching benefits. Global Accelerator is better for non-HTTP protocols like TCP and UDP, applications requiring static IPs, gaming or VoIP requiring consistent low latency, and cases where content caching is not applicable. Both can be used together with CloudFront as the origin behind Global Accelerator.' },
      { step: 'Configure endpoint groups per region', details: 'Add an endpoint group for each active region. Set traffic dial to 100 for all primary regions. Place secondary regions at 0 initially for disaster recovery. Associate ALBs or NLBs as endpoints within each group. Configure endpoint weights within a group to distribute traffic unevenly for canary testing.' },
      { step: 'Set up health checks to match application health', details: 'Configure health check port and protocol to match the application. Set the threshold to match your tolerance for false positives. With a 30-second interval, Global Accelerator detects failures and shifts traffic within approximately 60 seconds. Ensure the health check endpoint reflects real application health, not just TCP connectivity.' },
      { step: 'Use traffic dials for controlled deployments', details: 'When deploying to a new region, start with the traffic dial at 0. Deploy and validate the new region. Gradually increase the dial: 5%, 20%, 50%, 100%. Simultaneously reduce the dial on the old region if migrating. This provides a controlled migration with immediate rollback capability.' },
    ],
    pitfalls: [
      { title: 'Global Accelerator vs CloudFront confusion', description: 'Global Accelerator does not cache content. It simply routes TCP/UDP connections more efficiently over the AWS backbone. Using it for static content delivery instead of CloudFront adds unnecessary cost without the caching benefit. Evaluate the use case carefully and use the right tool.' },
      { title: 'Cost for long-running connections', description: 'Global Accelerator charges per accelerator per hour plus per GB of data transferred. For applications with high sustained throughput, data transfer costs can be significant. Evaluate whether the latency improvement justifies the cost increase over a simple Route 53 latency routing setup.' },
      { title: 'Static IP limitations', description: 'Global Accelerator provides only two static IPs per accelerator regardless of how many regions the application spans. If IP allowlisting at the destination is needed, these two IPs cover all global traffic, which may be a limitation in environments requiring per-region IP allowlisting.' },
    ],
    keyQuestions: [
      { q: 'How does Global Accelerator differ from Route 53 latency routing?', a: 'Route 53 latency routing resolves DNS to the region with the lowest measured latency, but traffic still traverses the public internet from the users location to the regional endpoint. Global Accelerator routes traffic to the nearest AWS edge location immediately, then sends it over the AWS global backbone to the endpoint region. The public internet path is only the last mile from the user to the edge PoP, which is typically a few milliseconds. Global Accelerator also provides static IPs and near-instant failover without DNS propagation delay, compared to Route 53 failover which is still subject to DNS TTL.' },
      { q: 'When would you use Global Accelerator for a gaming backend?', a: 'Gaming backends require low, consistent latency for real-time gameplay and often use UDP or custom TCP protocols not supported by CloudFront. Global Accelerator supports TCP and UDP, provides static IPs for matchmaking and client configuration, and uses anycast to minimize first-hop latency. Players from different countries all connect to their nearest edge PoP, which then routes over the low-latency AWS backbone to the game server region. Client affinity can pin a player to the same game server for the duration of a match.' },
      { q: 'How do you implement instant failover between two regions using Global Accelerator?', a: 'Configure an endpoint group for each region with 100% traffic dial on the primary and 0% on the secondary. Enable health checks on both endpoint groups. When the primary region health check fails, Global Accelerator automatically routes all traffic to the secondary region within approximately 60 seconds — no DNS change required. Because Global Accelerator uses anycast IPs that do not change, clients do not need to re-resolve DNS or update configuration. This contrasts with DNS-based failover where clients with cached DNS entries may continue hitting the failed region for the TTL duration.' },
      { q: 'What are the endpoint types supported by Global Accelerator?', a: 'Global Accelerator supports Application Load Balancers, Network Load Balancers, EC2 instances, and Elastic IP addresses as endpoints. ALBs are the most common for HTTP workloads. NLBs are used for TCP/UDP workloads requiring static IPs at the load balancer level. EC2 instance endpoints are used when an NLB is not needed. Elastic IPs allow routing to specific EC2 instances. Multiple endpoint types can coexist in the same endpoint group, enabling hybrid architectures where some traffic goes to an ALB fleet and some to individual instances.' },
    ],
    references: [
      'https://docs.aws.amazon.com/global-accelerator/latest/dg/what-is-global-accelerator.html',
      'https://docs.aws.amazon.com/global-accelerator/latest/dg/introduction-how-it-works.html',
    ],
    visualizations: [
      {
        title: 'Global Accelerator Anycast Routing vs Public Internet',
        description: `AWS Global Accelerator improves application availability and performance by routing user traffic through the AWS global backbone instead of the unpredictable public internet.

The core mechanism is anycast. Two static global IP addresses are assigned to every accelerator and advertised simultaneously from all AWS edge locations worldwide. When a user connects to one of these IPs, BGP routing on the internet directs their packets to the geographically nearest AWS edge PoP -- typically 1-10ms away. From that edge PoP, traffic travels over the AWS private global network to the application endpoint in the target region. The public internet exposure is only the last mile from the user to the nearest edge -- everything else uses AWS's low-latency, high-reliability backbone.

Contrast this with standard DNS-based routing (Route 53 latency routing): the DNS lookup returns the regional endpoint IP, but the user's traffic then traverses the full public internet path to reach that region. Internet routing can be suboptimal, subject to congestion, and variable in latency. Global Accelerator eliminates this variability.

Listeners define which ports and protocols the accelerator accepts (TCP, UDP). Endpoint groups hold the regional endpoints (ALBs, NLBs, EC2 instances, or Elastic IPs) with an associated traffic dial (0-100%) and health checks. Traffic dials enable gradual regional migrations: set new region to 5%, validate, ramp to 100%, zero out old region -- instant rollback by adjusting the dial, no DNS change needed.

Health checks run continuously. When an endpoint fails, Global Accelerator removes it from routing within approximately 60 seconds -- without any DNS propagation delay, because the anycast IPs never change. This makes failover faster and more deterministic than DNS-based failover.

Global Accelerator vs CloudFront: CloudFront caches content at edge locations. Global Accelerator does not cache -- it is purely a routing optimization for TCP/UDP. Use CloudFront for HTTP/HTTPS with caching. Use Global Accelerator for non-HTTP protocols, static IP requirements, gaming/VoIP backends, and applications where connection stability matters more than caching.`,
      },
    ],
    quickFire: [
      { q: 'How many static IP addresses does Global Accelerator provide?', a: 'Two static global anycast IP addresses per accelerator, regardless of how many regions the application spans.' },
      { q: 'How does Global Accelerator differ from Route 53 latency routing?', a: 'Route 53 still sends traffic over the public internet after DNS resolution. Global Accelerator routes traffic over the AWS backbone after the first edge PoP hop -- significantly reducing internet variability and latency.' },
      { q: 'What is a traffic dial?', a: 'A 0-100% setting on each endpoint group that controls what percentage of traffic that group receives. Used for gradual regional migrations and instant rollback without DNS changes.' },
      { q: 'Does Global Accelerator cache content like CloudFront?', a: 'No -- Global Accelerator is purely a routing optimization with no caching. It accelerates TCP/UDP connections by keeping traffic on the AWS backbone. Use CloudFront for content caching.' },
      { q: 'How long does failover take with Global Accelerator?', a: 'Approximately 60 seconds from health check failure detection to traffic shifting. No DNS propagation delay -- the anycast IPs never change.' },
      { q: 'What protocols does Global Accelerator support?', a: 'TCP and UDP -- unlike CloudFront which only supports HTTP/HTTPS. This makes it suitable for gaming, VoIP, and custom TCP protocols.' },
      { q: 'When would you use Global Accelerator for a gaming backend?', a: 'Players need low, consistent latency for real-time gameplay over UDP or custom TCP. Global Accelerator provides stable anycast IPs, minimizes first-hop latency, and routes over the AWS backbone to the game server region.' },
      { q: 'What is client affinity in Global Accelerator?', a: 'Session affinity that pins a client (by source IP) to the same endpoint for the configured period. Useful for stateful protocols where session state lives at the endpoint.' },
      { q: 'What endpoint types does Global Accelerator support?', a: 'ALB, NLB, EC2 instances, and Elastic IP addresses. Multiple types can coexist in the same endpoint group.' },
    ],
  },
  {
    id: 'aws-rds',
    title: 'RDS — Relational Database Service',
    icon: 'database',
    color: '#8b5cf6',
    description: 'Managed relational databases with automated backups, patching, and Multi-AZ failover.',
    introduction: 'Amazon RDS is a managed relational database service supporting MySQL, PostgreSQL, MariaDB, Oracle, and SQL Server engines. It handles time-consuming administration tasks including provisioning, patching, backups, recovery, failure detection, and repair. RDS provides Multi-AZ deployments for high availability with synchronous standby replication and automated failover, and Read Replicas for horizontal read scaling.',
    whenToUse: 'Use RDS for OLTP workloads requiring a traditional relational database with ACID compliance, when you have existing SQL-based applications to migrate, when your team needs standard SQL interfaces, and when you want managed operations without running database infrastructure. Consider Aurora when you need better performance, scaling, or multi-region capabilities.',
    keyConcepts: [
      { title: 'Multi-AZ Deployment', description: 'Maintains a synchronous standby replica in a different AZ. Primary commits a transaction only after the standby acknowledges the write. Automatic failover occurs within 1-2 minutes if the primary fails. The standby is not readable — it exists only for HA. The endpoint DNS name automatically updates to the new primary after failover.' },
      { title: 'Read Replicas', description: 'Asynchronous replicas for read-heavy workloads. Up to 15 read replicas per primary. Can be in different AZs or regions. Application must use the read replica endpoint explicitly. Cross-region read replicas support disaster recovery and serve local read traffic close to global users.' },
      { title: 'Automated Backups', description: 'Daily automated snapshots plus transaction logs enabling point-in-time recovery to any second within the retention period (1-35 days). Manual snapshots persist until deleted. Cross-region snapshot copy enables DR. Restore always creates a new DB instance — you cannot restore over an existing one.' },
      { title: 'Parameter and Option Groups', description: 'Parameter groups are configuration files for the DB engine (max_connections, innodb_buffer_pool_size). Option groups add optional engine features like Oracle TDE or SQL Server SSAS. Changes to dynamic parameters apply immediately; static parameters require a reboot.' },
      { title: 'Storage Types and Autoscaling', description: 'gp3 is the default general-purpose SSD offering consistent baseline IOPS and the ability to provision additional IOPS independently. io1 and io2 are provisioned IOPS storage for I/O-intensive workloads. Storage auto-scaling increases storage capacity automatically when free space falls below a threshold, with zero downtime.' },
    ],
    approach: [
      { step: 'Choose the right instance class', details: 'Memory-optimized (r6g, r7g) instances are best for large working sets that should fit in the buffer pool. General-purpose (m6g) for mixed workloads. Burstable (t3, t4g) for development and low-traffic production. Graviton instances offer the best price-performance for most workloads. Size the instance so the working set fits in the buffer pool to minimize disk I/O.' },
      { step: 'Enable Multi-AZ for production', details: 'Multi-AZ adds a standby replica in a different AZ at approximately double the cost. For production, this is non-negotiable — hardware failures, OS patching, and AZ issues trigger automatic failover. Test failover before relying on it by using the reboot with failover option in the console.' },
      { step: 'Configure parameter groups for the workload', details: 'For PostgreSQL, tune shared_buffers (25% of RAM), work_mem, max_connections, and enable pg_stat_statements for query performance monitoring. For MySQL, tune innodb_buffer_pool_size (70% of RAM), innodb_log_file_size, and query_cache_size. Apply parameter group changes during a maintenance window for static parameters.' },
      { step: 'Implement connection pooling', details: 'RDS instances have a finite max_connections limit based on instance size. Applications with many threads or Lambda functions should use RDS Proxy to pool connections and reduce connection overhead. RDS Proxy also handles failover transparently, pinning connections to the new primary without requiring application-level retry logic.' },
    ],
    pitfalls: [
      { title: 'Read replica replication lag', description: 'Read replicas use asynchronous replication. Writes not yet replicated are invisible on replicas, leading to read-after-write consistency issues. Applications reading from replicas must tolerate eventual consistency. Monitor ReplicaLag CloudWatch metric and route reads to the primary for session-consistent reads.' },
      { title: 'Forgetting RDS Proxy for Lambda', description: 'Lambda functions open a new database connection on every invocation cold start. At scale, hundreds of concurrent Lambda invocations can exhaust max_connections, causing connection refused errors. RDS Proxy pools connections between Lambda and RDS, multiplexing thousands of Lambda connections into a small connection pool.' },
      { title: 'Storage scaling is one-directional', description: 'RDS storage can only be increased, never decreased. Enabling storage autoscaling with a high maximum can lock you into paying for storage you allocated temporarily during a spike. Monitor storage consumption carefully and set the autoscaling maximum conservatively.' },
    ],
    keyQuestions: [
      { q: 'How does RDS Multi-AZ failover work and how long does it take?', a: 'Multi-AZ maintains a synchronous standby in a different AZ. Every committed write is synchronously replicated to the standby before the primary acknowledges it. When a failover is triggered — by primary instance failure, AZ failure, OS patching, or manual failover request — RDS updates the DNS CNAME of the endpoint to point to the standby, which is promoted to primary. This typically completes in 60 to 120 seconds. Applications using the endpoint DNS name automatically reconnect to the new primary after the DNS TTL expires. Applications must implement retry logic to handle the brief connection interruption during failover.' },
      { q: 'What is RDS Proxy and when should you use it?', a: 'RDS Proxy is a fully managed database proxy that sits between your application and RDS. It pools and multiplexes database connections, reducing the number of open connections to RDS. Use it for Lambda functions that create many short-lived connections, microservices architectures with many application instances, and any application where connection count approaching max_connections is a concern. It also improves failover resiliency by pinning applications to the new primary after a failover with faster recovery than waiting for DNS propagation.' },
      { q: 'How do you approach database sizing and what metrics indicate a need to scale?', a: 'Start with a memory-optimized instance sized so the working set fits in the buffer pool, monitored via the BufferCacheHitRatio CloudWatch metric. A ratio below 99% indicates the working set exceeds RAM and you need a larger instance. Monitor CPUUtilization, ReadIOPS, and WriteIOPS. If IOPS consistently hit the provisioned limit, upgrade to io2 storage or increase provisioned IOPS. Monitor FreeStorageSpace and enable autoscaling with a maximum that gives headroom. DatabaseConnections near max_connections indicates a need for RDS Proxy.' },
      { q: 'How do you perform a major version upgrade on RDS with minimal downtime?', a: 'Create a read replica of the current version and upgrade the replica to the new major version in a separate upgrade step. Validate application compatibility against the replica. When ready to cut over, initiate a planned maintenance window, stop writes to the primary, wait for replication lag to reach zero, promote the replica to a standalone primary, update the application connection string to the new instance endpoint, and re-enable writes. For lower-downtime migrations, use AWS Database Migration Service to continuously replicate changes and perform a final cutover sync with only seconds of write pause.' },
    ],
    references: [
      'https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Welcome.html',
      'https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/rds-proxy.html',
    ],
    visualizations: [
      {
        title: 'RDS Multi-AZ, Read Replicas, and RDS Proxy Architecture',
        description: `Amazon RDS provides three distinct availability and scaling mechanisms that are frequently confused: Multi-AZ standby, Read Replicas, and RDS Proxy.

Multi-AZ is a high availability feature, not a scaling feature. RDS maintains a synchronous standby instance in a different Availability Zone. Every committed write is synchronously replicated to the standby before the primary acknowledges it to the application -- zero data loss on failover. The standby cannot serve reads; it exists solely for HA. When a failure occurs (hardware, OS, AZ), RDS updates the DNS CNAME of the endpoint to point to the standby, promoting it to primary. This typically takes 60-120 seconds. Applications using the endpoint DNS name (never hardcode IPs) automatically reconnect. Applications must implement connection retry logic for the brief interruption window.

Read Replicas use asynchronous replication and do serve read traffic. They have their own endpoint URLs. Applications must explicitly direct read queries to replica endpoints -- RDS does not automatically route reads. Replication lag (ReplicaLag CloudWatch metric) means replicas may be milliseconds to seconds behind the primary, so read-after-write consistency cannot be guaranteed on replicas. Cross-region read replicas serve local reads near global users and provide a DR target that can be promoted to a standalone primary.

RDS Proxy sits between the application and RDS to solve the connection multiplexing problem. RDS instances have a hard max_connections limit based on instance RAM. Lambda functions, ECS tasks, and microservices can generate thousands of concurrent short-lived connections that exhaust this limit. RDS Proxy maintains a pool of long-lived connections to RDS and multiplexes thousands of application connections through them. It also handles Multi-AZ failover more gracefully -- instead of applications waiting for DNS TTL to expire, they reconnect to Proxy and Proxy handles the backend switchover, reducing application-visible failover time from 30-120 seconds to typically under 30 seconds.

Storage types: gp3 is the default and allows independently provisioned IOPS separate from storage size. io2 Block Express provides up to 256,000 IOPS for the most demanding OLTP workloads. Storage autoscaling only grows -- never shrinks.`,
      },
    ],
    quickFire: [
      { q: 'Is RDS Multi-AZ a read scaling feature?', a: 'No -- the standby is not readable. Multi-AZ is purely for high availability. Use Read Replicas for read scaling.' },
      { q: 'How long does RDS Multi-AZ failover take?', a: '60-120 seconds typically. RDS updates the endpoint DNS CNAME to the promoted standby. Applications need connection retry logic for this window.' },
      { q: 'What is the replication type used by Multi-AZ vs Read Replicas?', a: 'Multi-AZ uses synchronous replication (zero data loss). Read Replicas use asynchronous replication (eventual consistency, some lag).' },
      { q: 'When do you need RDS Proxy?', a: 'When Lambda functions or many microservices create thousands of short-lived connections. RDS Proxy pools connections and prevents max_connections exhaustion.' },
      { q: 'Can RDS storage be decreased after autoscaling?', a: 'No -- RDS storage is one-directional. Once allocated (manually or via autoscaling), it can only increase. Set autoscaling maximums conservatively.' },
      { q: 'What is BufferCacheHitRatio and what does a low value indicate?', a: 'The percentage of reads served from the buffer pool in memory. Below 99% means the working set exceeds RAM -- the instance needs more memory (larger instance class).' },
      { q: 'How do you perform a major RDS version upgrade with minimal downtime?', a: 'Create a read replica, upgrade the replica to the new major version, validate application compatibility, stop writes to primary, wait for zero lag, promote replica, update connection string.' },
      { q: 'What does RDS Proxy improve about Multi-AZ failover?', a: 'Applications reconnect to the same Proxy endpoint -- Proxy handles the backend failover transparently, reducing application-visible downtime to under 30 seconds vs waiting for DNS TTL expiry.' },
      { q: 'What storage type should you use for high-IOPS OLTP on RDS in 2026?', a: 'io2 Block Express -- provides up to 256,000 IOPS with 99.999% durability. gp3 is the default for general workloads up to 16,000 IOPS.' },
    ],
  },
  {
    id: 'aws-aurora',
    title: 'Aurora — Cloud-Native Relational Database',
    icon: 'database',
    color: '#8b5cf6',
    description: 'High-performance cloud-native MySQL/PostgreSQL with 6-way replication and auto-scaling.',
    introduction: 'Amazon Aurora is a cloud-native relational database engine that combines the speed and reliability of high-end commercial databases with the simplicity and cost-effectiveness of open-source databases. It is MySQL and PostgreSQL compatible and delivers up to five times the throughput of standard MySQL and three times the throughput of standard PostgreSQL. Aurora separates compute from storage, storing data across six copies in three AZs with automatic repair.',
    whenToUse: 'Use Aurora instead of RDS MySQL or RDS PostgreSQL for production workloads where performance, availability, and scalability matter. Aurora Global Database is the choice for multi-region active-active or active-passive architectures. Aurora Serverless v2 is ideal for variable workloads that need to scale from near-zero to thousands of connections.',
    keyConcepts: [
      { title: 'Storage Architecture', description: 'Aurora decouples compute from storage. The storage layer automatically replicates data across six copies in three AZs. Writes only need four of six copies to acknowledge for durability. Reads only need three of six. Individual disk failures are repaired automatically without any performance impact. Storage grows automatically in 10GB increments up to 128TB.' },
      { title: 'Aurora Replicas', description: 'Up to 15 Aurora replicas share the same underlying storage volume, eliminating replication lag. Reads on replicas always reflect writes committed to the storage layer within milliseconds. The primary and all replicas use the same shared storage cluster endpoint, with a separate reader endpoint for load balancing across replicas.' },
      { title: 'Aurora Serverless v2', description: 'Automatically scales compute capacity in fine-grained increments (0.5 ACU steps) based on application demand. Scales to zero during periods of no activity with a brief cold start. Capacity measured in Aurora Capacity Units — each ACU is approximately 2GB RAM. Writers and readers can independently autoscale.' },
      { title: 'Aurora Global Database', description: 'A single Aurora database spans multiple AWS regions with a single primary region and up to five secondary regions. Replication uses the Aurora storage layer, achieving typical lag under one second. Secondary regions serve reads locally. Managed planned and unmanaged failover promotes a secondary to primary in under a minute.' },
      { title: 'Backtrack', description: 'MySQL-compatible Aurora only. Rewinds the database to any point within the backtrack window (up to 72 hours) without restoring from a snapshot. The operation completes in seconds to minutes rather than the hours required for a traditional restore. Useful for recovering from accidental data changes.' },
    ],
    approach: [
      { step: 'Use cluster endpoints consistently', details: 'Always connect writes to the cluster endpoint, which routes to the primary. Connect reads to the reader endpoint, which load-balances across all healthy replicas. Application connection pooling should maintain separate pools for the writer and reader endpoints. Never hardcode instance endpoints as they change during maintenance and failover.' },
      { step: 'Configure Auto Scaling for read replicas', details: 'Define an Auto Scaling policy based on Aurora replica average CPU utilization or connections. Set minimum and maximum replica counts. Aurora adds or removes replicas based on load. Combined with the reader endpoint, the application transparently scales read capacity without any configuration changes.' },
      { step: 'Enable Performance Insights', details: 'Performance Insights visualizes database load broken down by wait events, SQL statements, users, and hosts. Use it to identify the top SQL consuming resources, bottlenecks (CPU-bound, I/O-bound, lock-bound), and the specific queries to optimize. Free tier includes seven days of retention.' },
      { step: 'Plan for Aurora Serverless scaling latency', details: 'Serverless v2 scales smoothly in 0.5 ACU increments with no connection drops. Scaling up is nearly instantaneous (seconds). Cold start from zero capacity takes a few seconds for the first connection. Use a minimum capacity above zero for latency-sensitive applications. Monitor ServerlessDatabaseCapacity and scale based on measured patterns.' },
    ],
    pitfalls: [
      { title: 'Writer endpoint is single-AZ until failover', description: 'The primary instance processes all writes. If the primary AZ has elevated latency, all writes are affected. Aurora failover to a replica takes approximately 30 seconds. For workloads requiring near-zero write downtime, Aurora Global Database with a local primary per region eliminates this dependency.' },
      { title: 'Cost of retained storage', description: 'Aurora charges for storage based on the peak allocation, not current usage. Deleted data does not reduce storage cost until a snapshot restore or clone is used. Be aware that bulk deletes do not immediately reclaim storage billing. Use Aurora clones for testing to avoid doubling storage costs.' },
      { title: 'Serverless cold starts at zero capacity', description: 'When Aurora Serverless scales to zero, the first connection after a period of inactivity triggers a cold start that can take 5-30 seconds. For web applications with unpredictable traffic, set minimum capacity to at least 0.5 ACU to keep the instance warm and avoid user-visible cold start latency.' },
    ],
    keyQuestions: [
      { q: 'How does Aurora storage differ from RDS storage and why does it matter?', a: 'RDS stores data on EBS volumes attached to the primary instance. Multi-AZ uses synchronous EBS replication to a standby. Aurora uses a distributed storage system that automatically replicates six copies across three AZs, managed by the Aurora storage layer rather than the compute instance. This means Aurora replicas have zero replication lag for reads because they access the same storage — they do not stream binlogs. Failover is faster because the replica already has all data. Storage repairs itself automatically. The compute instance does significantly less I/O amplification work compared to RDS.' },
      { q: 'Explain Aurora Global Database and how failover works.', a: 'Aurora Global Database replicates the primary region storage to secondary regions using a dedicated replication infrastructure built into the Aurora storage layer. Typical replication lag is under one second. Secondary regions have reader instances that serve local reads. For planned failover, Aurora detects the primary region is degraded and promotes a secondary within approximately 60 seconds of the detection. The old primary is converted to a secondary when it recovers. Applications in the new primary region now accept writes. Global Database eliminates the need for application-level multi-region data synchronization.' },
      { q: 'What is Aurora Serverless v2 and how does scaling work?', a: 'Aurora Serverless v2 automatically adjusts database capacity in Aurora Capacity Unit increments of 0.5, from a configured minimum (as low as 0.5 ACU) up to a configured maximum. The scaling is continuous and fine-grained, happening in sub-second intervals without dropping connections. When load increases, Aurora allocates more CPU and memory to the instance seamlessly. Both writer and reader instances can independently autoscale. This is ideal for applications with variable or unpredictable traffic patterns where provisioning peak capacity would waste money during low-traffic periods.' },
      { q: 'How would you migrate from RDS MySQL to Aurora MySQL with minimal downtime?', a: 'Create an Aurora MySQL cluster and set up RDS MySQL as the external source for a binary log replication channel. Let Aurora catch up to the RDS instance until replication lag is near zero. Put the application in read-only mode or stop writes briefly. Wait for lag to reach zero. Update the application connection string to point to the Aurora cluster endpoint. Re-enable writes. Total write downtime is typically under a minute. Alternatively, create an Aurora read replica from the existing RDS instance using the native migration path, wait for lag to zero, then promote it — this avoids configuring external replication.' },
    ],
    references: [
      'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/CHAP_AuroraOverview.html',
      'https://docs.aws.amazon.com/AmazonRDS/latest/AuroraUserGuide/aurora-global-database.html',
    ],
    visualizations: [
      {
        title: 'Aurora Storage Architecture and Global Database',
        description: `Aurora's most important architectural distinction from standard RDS is how it handles storage. RDS attaches EBS volumes to a compute instance and replicates at the block level to a standby. Aurora separates compute from storage entirely.

The Aurora storage layer automatically maintains six copies of all data across three Availability Zones -- two copies per AZ. Writes acknowledge after four of six copies confirm (quorum write). Reads require only three of six copies. Individual disk failures are detected and repaired automatically using the remaining copies without any compute involvement or performance impact. Storage grows in 10GB increments up to 128TB with no provisioning required.

Because all read replicas share the same distributed storage volume, there is no replication lag in the traditional sense. A write committed to the storage layer is immediately visible to all replicas reading from that same storage. This means Aurora replicas have near-zero read lag -- a fundamental difference from RDS read replicas which stream binary logs asynchronously and can fall seconds or minutes behind.

Aurora Global Database extends this architecture across regions. A dedicated high-throughput replication channel built into the storage layer propagates changes from the primary region to up to five secondary regions with typical lag under one second. Secondary regions have their own reader instances serving local reads at primary-region write latency. For planned failover, Aurora promotes a secondary to primary in under 60 seconds. For unplanned failover, the managed failover process detects the primary region degradation and promotes a secondary -- applications in the new primary region accept writes.

Aurora Serverless v2 attaches to the same shared storage cluster but scales compute capacity in 0.5 ACU increments continuously based on load -- scaling up in seconds, scaling down gradually. Writers and readers scale independently. Setting minimum capacity above zero avoids cold starts; the tradeoff is minimum cost even at zero traffic.

Backtrack (MySQL-compatible clusters only) rewinds the entire cluster to any point within the last 72 hours in minutes, without restoring from a snapshot -- invaluable for recovering from accidental bulk deletes.`,
      },
    ],
    quickFire: [
      { q: 'How many storage copies does Aurora maintain and across how many AZs?', a: 'Six copies across three AZs (two per AZ). Writes need 4/6 quorum; reads need 3/6.' },
      { q: 'Why do Aurora replicas have near-zero replication lag?', a: 'All replicas share the same distributed storage volume. They read directly from storage rather than streaming binary logs -- a write visible in storage is immediately visible to all replicas.' },
      { q: 'What is Aurora Global Database replication lag?', a: 'Typically under 1 second -- replication uses a dedicated storage-layer channel, not binary log streaming.' },
      { q: 'How long does Aurora Global Database failover take?', a: 'Managed planned failover: under 60 seconds. Unplanned failover after primary region detection: typically under 60 seconds with automatic promotion.' },
      { q: 'What is the minimum ACU increment for Aurora Serverless v2 scaling?', a: '0.5 ACU -- scaling is continuous in 0.5 ACU steps, with writers and readers scaling independently.' },
      { q: 'What is Aurora Backtrack and what is the maximum window?', a: 'Backtrack rewinds a MySQL-compatible Aurora cluster to any point in time within the configured window (up to 72 hours) in minutes -- no snapshot restore needed.' },
      { q: 'What is the difference between the cluster endpoint and the reader endpoint?', a: 'Cluster endpoint always routes to the primary (writer). Reader endpoint load-balances across all healthy replicas. Always use separate connection pools for each.' },
      { q: 'Does Aurora storage autoscale?', a: 'Yes -- Aurora storage grows automatically in 10GB increments up to 128TB. You never provision storage size upfront.' },
      { q: 'What Aurora clone feature saves cost for testing?', a: 'Aurora Fast Clone creates a copy-on-write clone of the cluster instantly. The clone shares storage with the original until pages are modified -- no full data duplication cost.' },
    ],
  },
  {
    id: 'aws-dynamodb',
    title: 'DynamoDB — Managed NoSQL at Any Scale',
    icon: 'database',
    color: '#8b5cf6',
    description: 'Fully managed serverless key-value and document database with single-digit millisecond performance.',
    introduction: 'Amazon DynamoDB is a fully managed NoSQL database service that provides fast and predictable performance with seamless scalability. It delivers single-digit millisecond response times at any scale, from small applications to the largest e-commerce platforms processing millions of requests per second. DynamoDB is serverless — there are no instances to manage, and it automatically scales throughput and storage.',
    whenToUse: 'Use DynamoDB for applications requiring single-digit millisecond latency at scale, session state storage, shopping carts, user profiles, gaming leaderboards, IoT data ingestion, and any workload where access patterns are known upfront and can be modeled around key-value or document access. Avoid DynamoDB for complex ad-hoc queries, joins, or OLAP workloads.',
    keyConcepts: [
      { title: 'Primary Keys', description: 'Every item must have a primary key. Partition key (simple primary key) distributes items across partitions by hash. Composite primary key (partition key + sort key) enables range queries and one-to-many relationships within a partition. Choose a high-cardinality partition key to avoid hot partitions.' },
      { title: 'Capacity Modes', description: 'On-demand mode scales instantly to any traffic level and charges per request. Provisioned mode charges for configured RCU and WCU regardless of actual usage but costs less for predictable traffic. Auto Scaling adjusts provisioned capacity based on target utilization. On-demand is best for unpredictable traffic; provisioned with auto scaling for stable workloads.' },
      { title: 'Global Secondary Indexes', description: 'GSIs create an alternate key structure that enables queries on non-primary key attributes. A GSI has its own partition key and optional sort key. It is updated asynchronously when the base table changes. GSIs have their own provisioned capacity. Up to 20 GSIs per table.' },
      { title: 'DynamoDB Streams', description: 'Captures a time-ordered sequence of item-level modifications in a DynamoDB table. Each stream record contains the old and new item images. Stream records are available for 24 hours. Lambda functions can process stream records for event-driven architectures, replication, and change data capture.' },
      { title: 'Transactions', description: 'TransactWriteItems and TransactGetItems provide ACID transactions across multiple items and tables within a single account and region. Transactions consume 2x the normal read or write capacity. Use for operations that must succeed or fail atomically, such as order placement that deducts inventory and creates an order record.' },
    ],
    approach: [
      { step: 'Design the access patterns first', details: 'DynamoDB requires knowing access patterns before designing the schema. List every query the application makes. Design the primary key and GSIs to serve all patterns without table scans. Use composite sort keys with structured prefixes (USER#123, ORDER#2024-01) to enable range queries within a partition.' },
      { step: 'Use single-table design', details: 'Store multiple entity types in one table using a generic PK and SK with entity type prefixes. Use GSIs with sparse projections for secondary access patterns. This eliminates joins and enables fetching related items in a single query using the Query operation with a partition key and sort key prefix.' },
      { step: 'Avoid hot partitions', details: 'DynamoDB spreads data across partitions by hashing the partition key. High write volume to the same partition key (like a date field) creates a hot partition that hits per-partition throughput limits. Add a random suffix or shard the partition key (user_id % 10 prefix) for high-write keys.' },
      { step: 'Use TTL for data expiration', details: 'Set a TTL attribute on items with an epoch timestamp. DynamoDB automatically deletes expired items within 48 hours. No capacity is consumed for TTL deletions. Essential for session tokens, temporary locks, and time-bounded data. TTL deletes appear in DynamoDB Streams for downstream cleanup.' },
    ],
    pitfalls: [
      { title: 'Scan operations', description: 'Scan reads every item in the table and consumes capacity proportional to the table size. Never use Scan for application queries — it defeats the purpose of DynamoDB and becomes more expensive and slower as the table grows. Every query must use Query or GetItem with a known key. Add a GSI if the access pattern is not covered by the primary key.' },
      { title: 'Item size limit', description: 'Individual items cannot exceed 400KB. Large items are an anti-pattern in DynamoDB. Store large binary data in S3 and keep only a reference in DynamoDB. Split large composite items into multiple items if attributes can be retrieved independently.' },
      { title: 'GSI eventual consistency on writes', description: 'GSI updates are asynchronous. A query on a GSI immediately after a write may return stale data. Design applications to tolerate GSI eventual consistency. For read-after-write consistency requirements on non-primary key attributes, consider reading from the base table by primary key after write confirmation.' },
    ],
    keyQuestions: [
      { q: 'How do you model a one-to-many relationship in DynamoDB?', a: 'Use a composite primary key where the partition key is the parent entity ID (customer_id) and the sort key indicates the relationship type and child entity ID (ORDER#2024-01-15#order-id-123). All orders for a customer are co-located in the same partition and can be retrieved with a single Query operation on the partition key. Use sort key prefixes to differentiate entity types in single-table design. A Query with KeyConditionExpression pk = customer_id AND begins_with(sk, ORDER#) retrieves all orders efficiently.' },
      { q: 'What is the difference between on-demand and provisioned capacity and how do you choose?', a: 'On-demand mode charges per request and scales instantly to any traffic level. It is simpler and ideal for unpredictable traffic, development environments, or applications with large traffic spikes. Provisioned mode charges for configured Read Capacity Units and Write Capacity Units and is approximately 60% cheaper than on-demand for predictable workloads at scale. Enable auto scaling on provisioned tables to handle load increases automatically. Start with on-demand, analyze actual consumption patterns over 30 days, and switch to provisioned with auto scaling if the traffic pattern is predictable enough to justify the optimization.' },
      { q: 'How do DynamoDB Streams enable event-driven architectures?', a: 'DynamoDB Streams record every create, update, and delete with both the before and after item images. A Lambda function connected as a stream processor receives batches of stream records and can trigger downstream workflows. Common patterns include replicating changes to a search index in OpenSearch, invalidating application caches on data changes, sending notifications via SNS, maintaining aggregate counts in a separate item, and auditing data changes. The 24-hour retention window provides resilience for stream processors that fall behind temporarily.' },
      { q: 'How would you implement optimistic locking in DynamoDB?', a: 'Add a version number attribute to each item initialized to zero. When updating an item, include a ConditionExpression that checks version = current_version. If the condition fails because another process incremented the version between your read and write, DynamoDB returns a ConditionalCheckFailedException. The application retries the read-modify-write cycle with the updated item. This prevents lost updates in concurrent scenarios without pessimistic locking and without consuming extra capacity beyond the normal update cost.' },
    ],
    references: [
      'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Introduction.html',
      'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/best-practices.html',
    ],
    visualizations: [
      {
        title: 'DynamoDB Data Model, Partitions, and Index Strategies',
        description: `DynamoDB's performance model is determined almost entirely by how you structure primary keys, secondary indexes, and access patterns before writing a single byte of data.

Every item requires a primary key. A simple primary key is just the partition key -- DynamoDB hashes it to determine which physical storage partition holds the item. A composite primary key adds a sort key, co-locating all items with the same partition key in sorted order. The Query operation retrieves all items in a partition or a range of sort keys in a single efficient call. The GetItem operation retrieves a single item by full primary key.

Physical partitions have throughput limits: 3,000 RCU and 1,000 WCU per partition per second, plus a 10GB storage limit. DynamoDB splits partitions automatically when these limits are approached. A hot partition -- one receiving a disproportionate share of traffic because many requests share the same partition key -- hits its per-partition limit even when the table has unused capacity elsewhere. High-cardinality partition keys (user ID, order ID, UUID) distribute load evenly. Low-cardinality keys (status, date, boolean) concentrate traffic.

Local Secondary Indexes share the table's partition key but use a different sort key. They can only be created at table creation time and provide strongly consistent reads. They consume the same partition's capacity. Global Secondary Indexes use a completely different partition key and sort key -- they are essentially an asynchronous projection of the table with their own partitions, capacity, and eventual consistency. Up to 20 GSIs per table. GSI updates lag slightly behind the base table; applications must tolerate this for GSI reads.

DynamoDB Streams capture every item-level change as an ordered sequence of records (INSERT, MODIFY, REMOVE) with old and new images. Records are available for 24 hours. Lambda event source mappings process stream records in batches for CDC pipelines, search index updates, cache invalidation, and cross-region replication triggers.

Transactions (TransactWriteItems, TransactGetItems) provide ACID semantics across up to 100 items in up to 100 tables within one account and region, at 2x capacity cost.`,
      },
    ],
    quickFire: [
      { q: 'What is a hot partition and how do you fix it?', a: 'A hot partition receives disproportionate traffic, hitting the 3,000 RCU / 1,000 WCU per-partition limit. Fix by choosing a high-cardinality partition key or adding a random suffix to distribute writes across N logical shards.' },
      { q: 'When should you use a GSI vs a LSI?', a: 'GSI when you need a completely different partition key (global query). LSI when you only need a different sort key within the same partition. LSI must be created at table creation; GSI can be added later.' },
      { q: 'What is the item size limit in DynamoDB?', a: '400KB per item. Store large blobs in S3 and keep only a reference in DynamoDB.' },
      { q: 'What does on-demand capacity mode offer vs provisioned?', a: 'On-demand scales instantly to any traffic level, charges per request, and requires no capacity planning. Provisioned with auto scaling is approximately 60% cheaper for predictable workloads.' },
      { q: 'How long are DynamoDB Stream records retained?', a: '24 hours. Consumers that fall behind for longer than 24 hours will miss records.' },
      { q: 'What operation should you never use for application queries in DynamoDB?', a: 'Scan -- it reads every item in the table, consuming capacity proportional to table size. Always use Query (with partition key) or GetItem (with full primary key).' },
      { q: 'What consistency modes does DynamoDB support for reads?', a: 'Eventually consistent reads (default, cheaper, may return slightly stale data) and strongly consistent reads (always return latest committed data, consume 2x RCU).' },
      { q: 'How do DynamoDB transactions differ from conditional writes?', a: 'Transactions coordinate up to 100 items atomically across tables (all succeed or all fail) at 2x cost. Conditional writes are single-item operations with a server-side condition expression -- cheaper for single-item atomic operations.' },
      { q: 'What is TTL in DynamoDB and does it consume capacity?', a: 'TTL automatically deletes items with an epoch timestamp attribute past the expiry. Deletion is eventually consistent (within 48 hours) and consumes no read or write capacity.' },
    ],
  },
  {
    id: 'aws-dynamodb-advanced',
    title: 'DynamoDB Advanced — Global Tables, DAX, and Design Patterns',
    icon: 'database',
    color: '#8b5cf6',
    description: 'Global multi-region replication, in-memory DAX caching, and advanced modeling patterns.',
    introduction: 'Advanced DynamoDB capabilities include Global Tables for multi-region active-active replication, DAX for in-memory microsecond caching, PartiQL for SQL-compatible queries, export to S3 for analytics, and advanced single-table design patterns. Understanding these features separates engineers who use DynamoDB from those who architect with it at scale.',
    whenToUse: 'Use Global Tables when your application requires multi-region writes with sub-second global replication. Use DAX when read latency must drop below DynamoDB single-digit milliseconds for hot data. Use PartiQL and S3 export for analytics use cases that complement the operational DynamoDB table.',
    keyConcepts: [
      { title: 'Global Tables', description: 'Active-active multi-region replication where writes are accepted in any region and replicated to all other regions within seconds. Last-writer-wins conflict resolution using timestamps. Application routing to nearest region reduces read and write latency globally. Requires on-demand capacity mode or auto scaling enabled on provisioned capacity.' },
      { title: 'DAX — DynamoDB Accelerator', description: 'In-memory cache cluster purpose-built for DynamoDB. Reduces read latency from single-digit milliseconds to microseconds for cached items. Write-through cache — writes go to both DAX and DynamoDB. Item cache for GetItem and BatchGetItem. Query/Scan cache for Query and Scan. Drop-in compatible with DynamoDB SDK via the DAX client.' },
      { title: 'PartiQL', description: 'SQL-compatible query language for DynamoDB. Supports SELECT, INSERT, UPDATE, and DELETE syntax. Does not change the underlying key-based access model — PartiQL still requires primary key predicates to avoid full table scans. Useful for developers familiar with SQL and for batch operations.' },
      { title: 'Conditional Expressions', description: 'Guard writes with conditions evaluated server-side. attribute_exists, attribute_not_exists, begins_with, contains, between, and comparison operators. Consumed capacity is charged even when the condition fails. Essential for optimistic locking, preventing duplicate inserts, and atomic counters.' },
      { title: 'DynamoDB Export to S3', description: 'Export a full table snapshot or incremental changes to S3 in DynamoDB JSON or Amazon Ion format. Export does not consume table throughput. Use Athena or EMR to analyze the exported data without impacting production DynamoDB performance. Incremental export captures changes since the last export using DynamoDB Streams.' },
    ],
    approach: [
      { step: 'Design for Global Tables conflict resolution', details: 'Global Tables use last-writer-wins based on an internal timestamp. To avoid conflicting writes from two regions updating the same item simultaneously, route writes for a given user or entity to a home region. Use the user ID to determine the home region. Secondary regions serve reads and accept writes for resiliency during primary region failures.' },
      { step: 'Layer DAX for read-heavy hot data', details: 'Identify the items with the highest read throughput — leaderboards, product catalog, session state. Add the DAX client library and point the endpoint at the DAX cluster. The SDK call pattern is identical. Configure item cache TTL based on how stale reads are acceptable. DAX significantly reduces consumed RCUs and DynamoDB throttling for hot-key workloads.' },
      { step: 'Implement write sharding for hot partitions', details: 'For high-volume writes to a small set of partition keys (game scores, counter updates), append a random suffix (key_1 through key_N) to distribute writes across N logical partitions. To read the aggregate value, use a parallel BatchGetItem across all N shards and sum on the application side. This trades read complexity for write scalability.' },
      { step: 'Use sparse GSIs to avoid index overhead', details: 'A GSI only includes items that have the GSI partition key attribute defined. Omitting the attribute from items that should not appear in the index keeps the GSI small and cheap. This pattern is useful for indexing a subset of items — only active orders, only admin users — without creating separate tables.' },
    ],
    pitfalls: [
      { title: 'DAX write-through does not protect reads during network partition', description: 'DAX is not a write-behind cache — it passes writes to DynamoDB synchronously. However, if the DAX cluster is unavailable, the application cannot fall back to DynamoDB automatically without SDK changes. Deploy DAX in Multi-AZ and implement a circuit breaker that falls back to the DynamoDB client if DAX returns errors.' },
      { title: 'Global Tables conflict resolution is silent', description: 'Last-writer-wins with timestamp conflict resolution means conflicting writes do not raise errors. One write silently wins and the other is discarded. Applications must design around this by either routing writes to a home region or using conditional expressions that will fail on conflict and expose the issue to the application layer.' },
      { title: 'PartiQL scans without key predicates', description: 'A PartiQL SELECT without a WHERE clause on the primary key performs a full table scan. The SQL-like syntax can mislead developers into writing queries that work on relational databases but are catastrophically expensive on DynamoDB. Always verify PartiQL queries use primary key or GSI key predicates and review the explain plan.' },
    ],
    keyQuestions: [
      { q: 'How does DynamoDB Global Tables handle write conflicts?', a: 'Global Tables uses a last-writer-wins conflict resolution strategy based on an internal timestamp assigned to each write. If two writes to the same item occur simultaneously in different regions, the write with the later timestamp wins and is replicated to all regions while the earlier write is discarded. This happens silently without any error to the application. To design around this, partition write responsibility by region using the item key — for example, hash user_id modulo the number of regions and route each user consistently to one home region for writes. During a regional failure, temporarily accept writes in any region knowing conflicts may occur, then resolve post-recovery.' },
      { q: 'When should you use DAX versus ElastiCache with DynamoDB?', a: 'DAX is a purpose-built DynamoDB cache that is API-compatible — you replace the DynamoDB client with the DAX client and it transparently caches GetItem, BatchGetItem, Query, and Scan responses. It handles cache invalidation automatically on writes because it is write-through. ElastiCache requires explicit cache management logic in the application: read from cache, on miss read from DynamoDB and populate cache, on write update cache. Use DAX when you want transparent caching with minimal application code changes. Use ElastiCache when you need fine-grained control over caching logic, multi-source caching (not just DynamoDB), or Redis-specific data structures like sorted sets for leaderboards.' },
      { q: 'Explain the single-table design pattern and its tradeoffs.', a: 'Single-table design stores multiple entity types in one DynamoDB table using generic attribute names (pk, sk) with entity-type prefixes. Customer records have pk=CUSTOMER#id, sk=PROFILE. Their orders have pk=CUSTOMER#id, sk=ORDER#date#order-id. This colocation enables fetching a customer and all their orders in a single Query. GSIs with sparse projections enable alternate access patterns. The tradeoffs are: the schema is harder to understand for developers unfamiliar with the pattern, queries spanning entity types require multiple operations, and migrating the schema is complex. Single-table design excels at DynamoDB performance optimization but adds developer cognitive overhead.' },
      { q: 'How do you handle a hot partition key problem in production?', a: 'First confirm the diagnosis using CloudWatch contributor insights to identify the specific partition keys consuming the most throughput. If the hot key is write-heavy, implement write sharding: append a random suffix 1 through N to create N logical partitions, write to a random shard, and read all N shards in parallel using BatchGetItem then aggregate. If the hot key is read-heavy, add DAX caching to absorb repeated reads without hitting DynamoDB. For time-series data with time-based hot keys like today date, add a random suffix and use parallel reads, or partition data by smaller time windows.' },
    ],
    references: [
      'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/GlobalTables.html',
      'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DAX.html',
    ],
    visualizations: [
      {
        title: 'DynamoDB Global Tables, DAX, and Single-Table Design Patterns',
        description: `Advanced DynamoDB usage requires mastering three independent capabilities: multi-region replication via Global Tables, in-memory microsecond caching via DAX, and single-table schema design.

Global Tables provide active-active multi-region replication. Writes are accepted in any configured region. DynamoDB replicates changes to all other regions using a conflict resolution strategy of last-writer-wins based on an internal timestamp. This means simultaneous writes to the same item from two different regions will result in one silently winning. The practical mitigation is home-region routing: hash the item's partition key modulo the number of regions and route each entity consistently to one primary region for writes. Secondary regions serve reads locally and absorb writes during regional failures. Global Tables require on-demand capacity mode or auto-scaling on provisioned tables and DynamoDB Streams must be enabled.

DAX (DynamoDB Accelerator) is an in-memory write-through cache cluster purpose-built for DynamoDB. It is SDK-compatible -- replace the DynamoDB client with the DAX client and caching is transparent. DAX maintains two caches: an item cache for GetItem/BatchGetItem responses and a query cache for Query/Scan results. Writes go through DAX to DynamoDB synchronously (write-through). DAX reduces read latency from single-digit milliseconds to microseconds and significantly reduces consumed RCUs on hot-key workloads. Deploy DAX in Multi-AZ. Implement fallback to the standard DynamoDB client if DAX is unavailable.

Single-table design stores multiple entity types in one table using generic PK and SK attributes with type prefixes (CUSTOMER#123, ORDER#2024-01). Co-locating related entities in the same partition enables fetching a customer and all their orders in a single Query. Sparse GSIs index only items that have the GSI key defined -- efficient for indexing subsets (only active orders, only admin users). The write sharding pattern for hot partitions appends a random suffix (key_1 through key_N) to distribute high-volume writes, then reads all N shards with a parallel BatchGetItem and aggregates in the application.

PartiQL provides SQL-like syntax over DynamoDB but does not change the underlying key-based model -- a SELECT without a key predicate still performs a full table scan.`,
      },
    ],
    quickFire: [
      { q: 'How does DynamoDB Global Tables resolve write conflicts?', a: 'Last-writer-wins based on an internal timestamp. Conflicts are silent -- one write wins and the other is discarded without error. Mitigate by routing each entity to a home region for writes.' },
      { q: 'What is DAX and when should you use it?', a: 'DAX is an in-memory write-through cache for DynamoDB providing microsecond read latency. Use it for hot-key workloads where the same items are read frequently and read latency below DynamoDB single-digit-millisecond is required.' },
      { q: 'Does DAX automatically fall back to DynamoDB if unavailable?', a: 'No -- if DAX is down, the DAX client fails. Implement a circuit breaker in your application that catches DAX errors and falls back to the standard DynamoDB client.' },
      { q: 'What is a sparse GSI?', a: 'A GSI that only indexes items which have the GSI partition key attribute defined. Items without the attribute are excluded from the index, keeping it small and cheap -- useful for indexing subsets like active orders only.' },
      { q: 'What is write sharding and when do you need it?', a: 'Append a random suffix (key_1 to key_N) to a hot partition key to distribute writes across N partitions. Read all N shards with BatchGetItem and aggregate in the application. Needed when a small set of keys receives very high write throughput.' },
      { q: 'What does the PartiQL SELECT without a WHERE key predicate do?', a: 'It performs a full table scan -- extremely expensive at scale. Despite SQL-like syntax, DynamoDB still requires key predicates for efficient access. Always verify queries use primary key or GSI key conditions.' },
      { q: 'What capacity modes are required for DynamoDB Global Tables?', a: 'On-demand mode or provisioned mode with auto scaling enabled. Fixed provisioned capacity without auto scaling is not supported.' },
      { q: 'How do you export DynamoDB data to S3 for analytics without impacting table throughput?', a: 'Use the DynamoDB Export to S3 feature -- it exports a full snapshot or incremental changes without consuming table read capacity. Use Athena or EMR to query the exported Parquet or Ion files.' },
      { q: 'What is the DynamoDB Contributor Insights feature used for?', a: 'Identifies the most frequently accessed and throttled partition keys and sort keys via CloudWatch Contributor Insights -- the first diagnostic step when investigating a hot partition problem in production.' },
    ],
  },
  {
    id: 'aws-elasticache',
    title: 'ElastiCache — Managed In-Memory Caching',
    icon: 'zap',
    color: '#8b5cf6',
    description: 'Managed Redis and Memcached for sub-millisecond latency caching and session storage.',
    introduction: 'Amazon ElastiCache is a fully managed in-memory data store and cache service supporting Redis OSS and Memcached. It removes the complexity of deploying and managing in-memory cache environments. ElastiCache for Redis supports data structures beyond simple key-value — strings, hashes, lists, sets, sorted sets — as well as pub/sub messaging, Lua scripting, transactions, and persistence. ElastiCache Serverless automatically scales capacity in response to traffic.',
    whenToUse: 'Use ElastiCache to reduce database load by caching frequent query results, as a session store for stateless web applications, as a pub/sub message broker for real-time notifications, as a rate limiter using Redis atomic increments, as a leaderboard using sorted sets, and as a job queue using Redis Streams.',
    keyConcepts: [
      { title: 'Redis vs Memcached', description: 'Redis supports rich data structures, persistence, replication, clustering, pub/sub, and Lua scripting. It is the right choice for almost all use cases. Memcached is a simpler multi-threaded key-value store without persistence or replication. Use Memcached only if you have existing Memcached applications or specifically need multi-threaded cache with no state requirements.' },
      { title: 'Redis Cluster Mode', description: 'Cluster mode disabled: one primary with up to 5 read replicas sharing the same shard. All data on one shard, replicated. Cluster mode enabled: data sharded across up to 500 node groups, each with one primary and up to 5 replicas. Enables horizontal scaling beyond a single node memory limit. Applications must use cluster-aware Redis clients.' },
      { title: 'Redis Replication and Failover', description: 'Primary receives writes and asynchronously replicates to replicas. Automatic failover promotes a replica to primary when the primary fails. With Multi-AZ enabled, ElastiCache places replicas in different AZs and performs automatic failover within seconds. DNS for the primary endpoint updates to the new primary.' },
      { title: 'Eviction Policies', description: 'When memory is full, Redis evicts keys based on the configured policy. noeviction returns errors on writes. allkeys-lru evicts least recently used keys from all keys. volatile-lru evicts LRU keys with an expiry set. allkeys-lfu evicts least frequently used. For a pure cache, allkeys-lru is the standard choice.' },
      { title: 'ElastiCache Serverless', description: 'Serverless option for ElastiCache that automatically provisions and scales cache capacity based on application traffic. No need to choose a node type or configure shards. Billed per ECPU consumed and GB stored. Ideal for variable workloads or when operational simplicity is prioritized over cost optimization.' },
    ],
    approach: [
      { step: 'Choose between cluster mode enabled and disabled', details: 'Cluster mode disabled is simpler and sufficient when your working set fits in a single large Redis node (up to the r7g.16xlarge with 416GB RAM). Cluster mode enabled is required when you need more memory than a single node provides, need to scale write throughput across shards, or want to avoid rebalancing during node type changes.' },
      { step: 'Implement cache-aside pattern', details: 'Application checks cache first. On cache hit, return cached value. On cache miss, query the database, populate the cache with a TTL, and return the value. Write directly to the database and optionally update or invalidate the cache. This is the most common pattern and keeps the cache and database consistent within the TTL window.' },
      { step: 'Configure memory and eviction policy', details: 'Set maxmemory to a value below total instance memory to leave headroom for Redis overhead. Set maxmemory-policy to allkeys-lru for a pure cache. Monitor Evictions CloudWatch metric — rising evictions indicate undersized cache or poor TTL strategy. Set CacheHitRate alarm to alert when hit rate drops significantly.' },
      { step: 'Use Redis for distributed rate limiting', details: 'Implement rate limiting using INCR and EXPIRE in a Lua script for atomicity. The Lua script increments a counter keyed by user_id and time window, sets expiry on first increment, and returns the current count. The application enforces the limit based on the returned count. This is atomic, fast, and distributed across application instances.' },
    ],
    pitfalls: [
      { title: 'Single-AZ without failover', description: 'A single-node Redis cluster has no replica. If the node fails, the cache is empty and all requests hit the database until the node recovers. For production, always enable Multi-AZ with at least one replica. The read endpoint load-balances across replicas for read scaling in addition to providing failover.' },
      { title: 'Key expiry is not instantaneous', description: 'Redis uses lazy expiration (deletes on access) and active expiration (periodic scan). An expired key may not be immediately freed. Memory usage may temporarily exceed the logical active dataset size. Monitor CurrItems and calculate expected memory to set appropriate maxmemory and eviction policies.' },
      { title: 'Network placement affects latency', description: 'ElastiCache must be in the same VPC and preferably the same AZ as the application to achieve sub-millisecond latency. Cross-AZ access adds 1-3ms. Cross-region access is not supported — use Redis Cluster OSS self-managed in multiple regions or ElastiCache Global Datastore (read-only replicas).' },
    ],
    keyQuestions: [
      { q: 'How do you implement session storage with ElastiCache Redis?', a: 'Generate a session ID on login and store session data as a Redis hash with the session ID as the key. Set an expiry using EXPIRE equal to the session timeout period. On each authenticated request, fetch the session by ID using HGETALL, validate it, and optionally update the expiry to implement sliding sessions with EXPIRE. On logout, delete the key. Configure the web tier as stateless — all instances look up sessions in Redis. ElastiCache Multi-AZ ensures session data survives a node failure. Store only the minimum session data to keep hash size small.' },
      { q: 'Explain the difference between Redis cluster mode enabled and disabled.', a: 'In cluster mode disabled, there is one shard containing all data. The shard has one primary that accepts writes and up to five replicas for reads and failover. Scaling requires resizing the node type, which requires a brief failover. In cluster mode enabled, data is partitioned across multiple shards using consistent hashing on the key slot. Each shard has its own primary and replicas. You can add or remove shards online, scaling memory and write throughput horizontally. Applications must use cluster-aware Redis clients that route commands to the correct shard. Multi-key operations are only supported when all keys hash to the same slot — use hash tags to force co-location.' },
      { q: 'How would you cache database query results and handle cache invalidation?', a: 'Implement cache-aside: on read, compute a cache key from the query parameters, check Redis using GET, and on hit return the cached JSON. On miss, execute the SQL query, serialize the result to JSON, store in Redis with SET key value EX ttl, and return the result. For invalidation, use TTL-based expiration as the primary mechanism — set TTL based on acceptable staleness. For immediate invalidation on writes, delete or update the cache key when the underlying data changes. Use key naming conventions like table:id:field to enable pattern-based invalidation for all cached results related to a specific record.' },
      { q: 'How do you implement a distributed lock with Redis?', a: 'Use the SET command with NX (set if not exists) and PX (expiry in milliseconds) options atomically: SET lock_key unique_value NX PX 30000. If the command succeeds, the lock is acquired. If it fails, another process holds the lock. Always set an expiry to prevent deadlocks if the lock holder crashes before releasing. To release, use a Lua script that checks the value matches the unique token before deleting — this prevents a process from releasing a lock it no longer owns if the lock expired and was re-acquired. The Redlock algorithm extends this to multiple independent Redis nodes for higher reliability.' },
    ],
    references: [
      'https://docs.aws.amazon.com/AmazonElastiCache/latest/red-ug/WhatIs.html',
      'https://redis.io/docs/manual/patterns/',
    ],
    visualizations: [
      {
        title: 'ElastiCache Redis Architecture: Cluster Modes, Replication, and Eviction',
        description: `Amazon ElastiCache for Redis provides sub-millisecond in-memory data access and supports rich data structures, persistence, pub/sub, and scripting that make it far more than a simple cache.

The fundamental deployment choice is between cluster mode disabled and cluster mode enabled. In cluster mode disabled, all data lives in a single shard with one primary node and up to five read replicas. The primary accepts all writes; replicas handle reads and provide failover targets. This is the simpler setup, sufficient when the entire working set fits within the memory of the largest available instance (r7g.16xlarge provides up to 416GB). Scaling requires a node type change, which causes a brief failover.

Cluster mode enabled shards data across up to 500 node groups using consistent hashing on key slot assignments (16,384 slots divided across shards). Each shard has its own primary and replicas. You can add or remove shards online without downtime, scaling both memory and write throughput horizontally. The tradeoff: applications must use cluster-aware clients that route commands to the correct shard. Multi-key operations (MGET, MSET, pipelines) only work when all keys hash to the same slot -- use hash tags {user_id} to force co-location of related keys.

Replication is asynchronous. Automatic failover (with Multi-AZ enabled) promotes a replica to primary when the primary fails -- typically completing in under 60 seconds. The DNS primary endpoint updates automatically. Applications must implement connection retry for the failover window.

Eviction policy is critical for pure cache use cases. When maxmemory is reached, Redis applies the configured policy. allkeys-lru (evict least recently used from all keys) is the standard choice for a pure cache. volatile-lru only evicts keys with an expiry set -- safe when some keys must never be evicted. noeviction returns errors on write -- appropriate for persistent data stores, not caches.

ElastiCache Serverless (2024+) eliminates node type selection and cluster topology management entirely. It automatically provisions capacity based on demand, billed per ECPU and GB stored. Use it for variable workloads or when operational simplicity outweighs per-unit cost optimization.`,
      },
    ],
    quickFire: [
      { q: 'What is the difference between Redis cluster mode enabled and disabled?', a: 'Cluster mode disabled: single shard, all data in one primary + replicas. Cluster mode enabled: data sharded across up to 500 node groups, horizontal scale of both memory and writes. Requires cluster-aware client.' },
      { q: 'What eviction policy should you use for a pure cache workload?', a: 'allkeys-lru -- evicts the least recently used key from all keys when memory is full. Ensures the hottest data stays in cache.' },
      { q: 'Does Redis replication use synchronous or asynchronous replication?', a: 'Asynchronous -- replicas may lag slightly behind the primary. Not suitable for read-after-write consistency requirements.' },
      { q: 'What is the cache-aside pattern?', a: 'Check cache first; on miss, read from DB, write to cache with TTL, return value. Writes go directly to DB and optionally invalidate cache. Most common caching pattern.' },
      { q: 'How do you implement a distributed lock with Redis?', a: 'SET lock_key unique_value NX PX 30000 -- atomic set-if-not-exists with millisecond expiry. Release using a Lua script that checks the value matches before DEL to prevent releasing another owner\'s lock.' },
      { q: 'What Redis data structure do you use for a real-time leaderboard?', a: 'Sorted set (ZADD with score, ZRANGE/ZREVRANGE for top-N). Score is the metric; member is the user ID. O(log N) insertion and O(log N + K) range retrieval.' },
      { q: 'What is the risk of deploying ElastiCache in a single AZ?', a: 'If the node fails the cache is empty and all requests hit the database cold. Always enable Multi-AZ with at least one replica for production.' },
      { q: 'Can ElastiCache Redis be accessed cross-region?', a: 'No -- ElastiCache is VPC-local. Use ElastiCache Global Datastore for read-only cross-region replicas (Redis only), or manage self-hosted Redis for active-active multi-region.' },
      { q: 'What is ElastiCache Serverless and when should you use it?', a: 'A serverless ElastiCache tier that auto-provisions and scales capacity with no node type or shard configuration. Use for variable or spiky workloads where operational simplicity matters more than per-unit cost.' },
    ],
  },
  {
    id: 'aws-redshift',
    title: 'Redshift — Petabyte-Scale Data Warehouse',
    icon: 'database',
    color: '#8b5cf6',
    description: 'Columnar MPP data warehouse for petabyte-scale analytics and business intelligence.',
    introduction: 'Amazon Redshift is a fully managed, petabyte-scale data warehouse service that uses columnar storage and massively parallel processing to deliver fast query performance on large datasets. It integrates with S3 for external tables and data lake queries via Redshift Spectrum, with Kinesis and DMS for streaming ingestion, and with BI tools via JDBC/ODBC. Redshift Serverless automatically scales compute based on query workload.',
    whenToUse: 'Use Redshift for OLAP workloads: business intelligence dashboards, historical trend analysis, multi-dimensional reporting, and aggregating data from multiple operational systems. It is not suitable for OLTP — use RDS or Aurora. Use Redshift Spectrum when you need to query data in S3 without loading it into Redshift.',
    keyConcepts: [
      { title: 'Columnar Storage and Compression', description: 'Redshift stores data by column rather than by row. Analytical queries that read a subset of columns scan only the relevant columns, dramatically reducing I/O. Each column uses the most efficient compression encoding for its data type. The combination of columnar storage and compression reduces storage footprint by 3-10x and improves query speed.' },
      { title: 'Distribution Styles', description: 'Determines how table rows are distributed across compute nodes. EVEN distributes rows round-robin — simple but causes data movement during joins. KEY distributes by a column value, co-locating matching rows on the same node for join efficiency. ALL replicates the entire table to every node — good for small dimension tables. AUTO lets Redshift choose.' },
      { title: 'Sort Keys', description: 'Define the physical sort order of data on disk within each node slice. Range queries on sort key columns skip large portions of data using zone maps. Compound sort keys benefit queries that use all prefix columns. Interleaved sort keys assign equal weight to all columns. VACUUM SORT restores sort order after data loading.' },
      { title: 'Redshift Spectrum', description: 'Queries data directly in S3 without loading it into Redshift. Uses an external schema and external tables pointing to S3 prefixes. Spectrum scales to thousands of nodes and processes queries in parallel. Joins between Redshift local tables and Spectrum external tables work seamlessly.' },
      { title: 'Workload Management', description: 'WLM queues route queries to dedicated compute resources. Configure separate queues for short queries, BI tool queries, and long ETL jobs to prevent resource contention. Manual WLM allows configuring memory percentage and concurrency per queue. Automatic WLM lets Redshift adjust concurrency dynamically.' },
    ],
    approach: [
      { step: 'Choose distribution keys to minimize data movement', details: 'Identify the largest fact tables and their most frequent join columns. Set the distribution key on the join column. Join partner dimension tables can use ALL distribution if small. Queries that join on the distribution key avoid the expensive redistribute operation that moves data across nodes. Run EXPLAIN on critical queries and look for DS_DIST steps.' },
      { step: 'Define sort keys for range predicates', details: 'Set the sort key on columns used in WHERE clauses with range predicates: date, timestamp, and high-cardinality ID columns used in inequality filters. Compound sort keys work well when queries always filter on the same leading columns. After bulk loads, run VACUUM SORT to restore sort order and ANALYZE to update statistics.' },
      { step: 'Load data with COPY command', details: 'COPY from S3 is the most efficient loading mechanism — it reads files in parallel across all compute nodes. Split large files into chunks matching the number of slices (total slices = node count times slices per node). Use columnar file formats like Parquet for best COPY performance. Avoid single-row INSERT statements.' },
      { step: 'Use Concurrency Scaling and Serverless for variable workloads', details: 'Enable Concurrency Scaling to automatically add transient cluster capacity when queue depth grows. Serverless Redshift eliminates capacity planning entirely for variable workloads. For predictable ETL-driven warehouses, provisioned clusters with reserved instances are more cost-efficient.' },
    ],
    pitfalls: [
      { title: 'Neglecting VACUUM and ANALYZE', description: 'Deleted rows leave ghost rows that consume disk space and degrade query performance. VACUUM reclaims space and restores sort order. ANALYZE updates the query planner statistics. Without regular maintenance, query plans degrade. Redshift auto-vacuum handles this in the background but may fall behind on high-churn tables.' },
      { title: 'Wrong distribution key causing skew', description: 'A distribution key with low cardinality or highly skewed data concentrates rows on a few nodes. One overloaded node becomes the bottleneck. Check skew using svv_table_info.skew_rows. Consider EVEN distribution for tables where no natural join key exists, or select a higher-cardinality column.' },
      { title: 'Too many small files in COPY', description: 'Loading millions of tiny S3 files with COPY creates high overhead per file. Aim for files that are at least 1MB and ideally 100MB to 1GB. Use S3 event triggers and aggregation via Kinesis Firehose or AWS Glue to compact small files before loading into Redshift.' },
    ],
    keyQuestions: [
      { q: 'How does Redshift columnar storage improve query performance?', a: 'Relational databases store rows contiguously on disk, so reading any column requires scanning all columns in each row. Columnar storage stores each column as a separate file. An analytical query that SELECTs three out of 100 columns reads only 3% of the data. Combined with per-column compression optimized for the data type and cardinality, the actual bytes read are a fraction of the row-oriented equivalent. Zone maps in each column block enable skipping entire blocks when the range predicate cannot match any value in the block, further reducing I/O.' },
      { q: 'Explain the tradeoffs between Redshift distribution styles.', a: 'EVEN distribution spreads rows uniformly but causes expensive data redistribution during joins since matching rows are on different nodes. KEY distribution co-locates matching rows of join partner tables on the same node, eliminating redistribution for those joins but creating skew if the key is not uniformly distributed. ALL distribution replicates small dimension tables to every node, enabling broadcasts to any node for joins at the cost of additional storage and slower writes. Choose KEY for large tables frequently joined on a high-cardinality column, ALL for small static dimension tables, and EVEN for large tables without a clear distribution key candidate.' },
      { q: 'How do you design a Redshift schema for a star schema analytical workload?', a: 'Place the central fact table on KEY distribution using the foreign key to the dimension with the largest join query volume. Distribute all dimension tables using ALL if they are small (under a few GB). Define a compound sort key on the fact table using the most common filter columns — typically date followed by the most selective filter column. Create a sort key on dimension tables on the column used in WHERE clauses. Use DISTSTYLE AUTO on dimensions and let Redshift choose. Run EXPLAIN on the top 10 queries and look for DS_DIST_INNER and DS_BCAST_INNER distribution operations that indicate suboptimal choices.' },
      { q: 'How does Redshift Spectrum work and when should you use it?', a: 'Spectrum queries data directly in S3 through an external schema and table definition. When a query references an external table, Redshift pushes the computation to a fleet of Spectrum nodes that read and filter the S3 data in parallel, returning only the relevant rows to the Redshift cluster for the final join and aggregation. Use Spectrum for historical data that is infrequently queried and too large to load into Redshift cost-effectively, for querying data lake files from Glue catalog, and for joining operational S3 data with Redshift warehouse data. Queries are slower than native Redshift tables due to S3 latency but scale independently and charge per TB scanned.' },
    ],
    references: [
      'https://docs.aws.amazon.com/redshift/latest/dg/welcome.html',
      'https://docs.aws.amazon.com/redshift/latest/dg/r_tuning_data_distribution.html',
    ],
    visualizations: [
      {
        title: 'Redshift MPP Architecture: Columnar Storage, Distribution, and Query Execution',
        description: `Amazon Redshift is a massively parallel processing (MPP) columnar data warehouse. Understanding its physical execution model is essential for writing fast queries and designing efficient schemas.

A provisioned Redshift cluster has a leader node and multiple compute nodes. The leader node receives queries, builds query plans, and coordinates execution. Compute nodes are divided into slices -- independent processing units, typically two per node. Data is distributed across all slices according to the table's distribution style. Queries execute in parallel across all slices simultaneously; the slowest slice determines total query time. Distribution skew -- where one slice holds significantly more data than others -- is the most common cause of slow queries.

Columnar storage is the first performance multiplier. Row-oriented storage requires reading entire rows to access any column. Columnar storage groups each column's values contiguously on disk. An analytical query reading 5 of 100 columns reads approximately 5% of the data. Each column additionally uses the most efficient compression encoding for its data type (run-length for low-cardinality, delta for timestamps, LZO for general text), reducing physical I/O further.

Zone maps are block-level metadata storing the minimum and maximum value of each 1MB block per column. Range predicates (WHERE date > '2024-01-01') skip entire blocks where the predicate cannot match, without reading any data from those blocks.

Distribution style choice prevents or causes data movement during joins. KEY distribution places all rows with the same key value on the same slice -- a join between two tables both distributed on the same column requires no data movement (co-located join). EVEN distributes rows round-robin -- simple but requires data redistribution (DS_DIST steps in EXPLAIN) during joins. ALL replicates the entire table to every node -- eliminates join movement for small dimension tables at the cost of storage and slower writes.

Sort keys define the physical order of rows on disk. Range predicates on the leading sort key columns benefit from zone map skipping. Always define sort keys on the most common filter columns (date, region) for fact tables.

Redshift Spectrum extends queries to S3 without data loading, using a separate fleet of Spectrum nodes that process S3 data in parallel. Charges per TB scanned -- always push filter predicates into Spectrum queries to minimize scan cost.`,
      },
    ],
    quickFire: [
      { q: 'What makes Redshift faster than row-oriented databases for analytics?', a: 'Columnar storage reads only queried columns (less I/O), per-column compression reduces data volume, zone maps skip blocks that cannot match range predicates, and MPP executes queries in parallel across all slices.' },
      { q: 'What are the three Redshift distribution styles and when do you use each?', a: 'KEY: distribute on join column to co-locate matching rows (eliminates join data movement). EVEN: round-robin for large tables without a natural join key. ALL: replicate small dimension tables to every node for broadcast-free joins.' },
      { q: 'What does DS_DIST_INNER in an EXPLAIN plan mean?', a: 'The inner table of a join was redistributed across nodes to complete the join -- indicates a distribution key mismatch. Resolve by matching the distribution key on both join tables.' },
      { q: 'What is a sort key and what query type benefits most?', a: 'Sort key defines physical row order on disk. Range predicates on the leading sort key column benefit from zone map skipping, which avoids reading blocks that cannot match the predicate.' },
      { q: 'Why must you run VACUUM and ANALYZE regularly on Redshift?', a: 'VACUUM reclaims space from deleted rows (ghost rows) and restores sort order. ANALYZE updates query planner statistics. Without both, query plans degrade over time.' },
      { q: 'What is Redshift Spectrum?', a: 'Queries data directly in S3 via an external schema without loading into Redshift. Uses a separate Spectrum node fleet, charges per TB scanned. Best for infrequently-queried historical data too large to load cost-effectively.' },
      { q: 'What is the COPY command and why use it instead of INSERT?', a: 'COPY reads files from S3 in parallel across all compute slices -- the fastest loading mechanism. Single-row INSERTs are extremely slow in Redshift because they bypass the parallel loading path.' },
      { q: 'What is Redshift Serverless?', a: 'Serverless Redshift eliminates cluster sizing and management. Capacity scales automatically based on query workload. Billed per RPU-second. Best for variable or unpredictable analytical workloads.' },
      { q: 'What causes distribution skew and how do you detect it?', a: 'A distribution key with low cardinality or highly skewed data concentrates rows on a few slices. Detect via svv_table_info.skew_rows and skew_sortkey1. Fix by choosing a higher-cardinality column or switching to EVEN distribution.' },
    ],
  },
  {
    id: 'aws-documentdb',
    title: 'DocumentDB — Managed MongoDB-Compatible Database',
    icon: 'database',
    color: '#8b5cf6',
    description: 'Managed document database compatible with MongoDB drivers for JSON workloads.',
    introduction: 'Amazon DocumentDB is a fully managed document database service designed to be compatible with existing MongoDB drivers and tools. It uses an Aurora-like architecture with a distributed storage volume that replicates data six ways across three availability zones. DocumentDB decouples compute from storage, allowing independent scaling of each and providing built-in high availability without the operational overhead of managing MongoDB clusters.',
    whenToUse: 'Use DocumentDB for JSON document workloads with existing MongoDB application code to migrate to managed infrastructure, content management systems, catalogs, and user profile stores where flexible schema is valuable. Evaluate DocumentDB against MongoDB Atlas when you need full MongoDB API compatibility — DocumentDB supports a subset of MongoDB operations.',
    keyConcepts: [
      { title: 'Cluster Architecture', description: 'DocumentDB uses Aurora storage: one primary instance accepts reads and writes, up to 15 replica instances handle reads only, and all share the same six-way replicated storage volume. Replicas have near-zero replication lag because they read from the same storage. Automatic failover promotes a replica within 30 seconds.' },
      { title: 'Collections and Documents', description: 'Data is stored as BSON documents within collections. Documents are schema-free — each document can have different fields. The _id field is the primary key, indexed automatically. Nested documents and arrays are first-class types. Document size limit is 16MB.' },
      { title: 'Indexes', description: 'Single-field, compound, multi-key (array fields), and text indexes are supported. Partial indexes index only documents matching a filter expression. Sparse indexes skip null values. Indexes are critical for query performance — without an index, DocumentDB performs a collection scan reading every document.' },
      { title: 'Change Streams', description: 'DocumentDB change streams capture ordered change events for a collection or database. Applications subscribe to the stream to react to inserts, updates, and deletes in real time. Used for event-driven architectures, cache invalidation, and CDC pipelines. Change streams are backed by the DocumentDB oplog.' },
      { title: 'Aggregation Pipeline', description: 'Multi-stage transformation pipeline for complex data processing: match, project, group, sort, limit, lookup (join), unwind (flatten arrays), and add fields. More expressive than SQL GROUP BY and supports reshaping documents across pipeline stages. Critical for analytics over document collections.' },
    ],
    approach: [
      { step: 'Validate MongoDB driver compatibility', details: 'DocumentDB supports a subset of the MongoDB 4.0 API. Before migrating, test all MongoDB queries and aggregation pipeline stages against DocumentDB. Run the compatibility checker tool against your codebase. Unsupported operators include mapReduce, $where, and several aggregation stages. Use DocumentDB native queries for unsupported patterns.' },
      { step: 'Design document schema for query access patterns', details: 'Embed related data in documents when it is always read together (order with line items). Reference related data with IDs when the related data is accessed independently or updated frequently (user profile referenced by many orders). Over-embedding leads to large documents with wasted I/O; over-referencing requires multiple queries.' },
      { step: 'Create indexes for all query predicates', details: 'Analyze the query explain plans. For each field that appears in find predicates or aggregation match stages, create an index. Compound indexes follow the ESR rule: Equality fields first, Sort fields second, Range fields third. Use partial indexes for queries that always include a status filter to reduce index size.' },
      { step: 'Use connection pooling with the DocumentDB driver', details: 'Configure the MongoDB driver connection pool size to match the instance class. DocumentDB instances have a connection limit based on instance size. For Lambda or microservices with many instances, deploy a connection proxying layer or use the DocumentDB-compatible connection pooling options in the driver.' },
    ],
    pitfalls: [
      { title: 'TLS required by default', description: 'DocumentDB requires TLS for all connections. The connection string must include tls=true and the RDS CA certificate bundle. Applications connecting without TLS receive a connection refused error. Download the global bundle from the RDS documentation and pass it as the tlsCAFile option in the driver.' },
      { title: 'Not full MongoDB compatibility', description: 'DocumentDB does not implement the complete MongoDB API. Complex aggregations, $where with JavaScript, mapReduce, and some index types are unsupported. Applications that relied on these features require modification. Always run a compatibility audit before committing to DocumentDB for a MongoDB migration.' },
      { title: 'Storage cost for small clusters', description: 'DocumentDB minimum storage billing is 10GB. The Aurora storage layer charges per GB-month of allocated storage, not used storage. For small datasets, the storage cost may be higher than MongoDB Atlas on smaller tiers. Evaluate total cost including compute, storage, and I/O charges against Atlas equivalents.' },
    ],
    keyQuestions: [
      { q: 'How does DocumentDB handle schema evolution differently from a relational database?', a: 'DocumentDB stores schemaless BSON documents — new fields can be added to individual documents without altering a table schema. Existing documents without the new field simply return null for that field. Indexes on new fields can be created online without locking. This flexibility allows gradual migration of data to new schemas without big-bang schema migrations. However, this also means the application must handle documents with missing fields gracefully and validate schema at the application layer, as the database enforces no constraints beyond index uniqueness.' },
      { q: 'How do you write an aggregation pipeline to compute monthly revenue by product category?', a: 'The pipeline stages are: match to filter completed orders in the date range, unwind the line_items array to get one document per line item, lookup to join with the products collection on product_id, add fields to compute line_item_total as quantity times unit_price, group by product category with sum of line_item_total and the month extracted from the order date, then project to shape the output. This demonstrates match before unwind for performance (reduces documents processed), lookup for joining collections, and group for aggregation. Always put match as early in the pipeline as possible to reduce the working set.' },
      { q: 'What is the difference between embedding and referencing documents?', a: 'Embedding stores related data inside the parent document as a subdocument or array. It is optimal when the data is always read together, has a bounded size, and does not change independently. A blog post with comments embedded works if comment count is bounded. Referencing stores the related document separately and stores only its _id in the parent. It is optimal when the related data is large, accessed independently, or shared among multiple parents. An order referencing a user by user_id avoids duplicating user profile data across millions of orders. Use embedding for has-one and small has-many, referencing for large has-many and shared-by-many.' },
      { q: 'How do you monitor and diagnose slow queries in DocumentDB?', a: 'Enable profiling by setting profiler_threshold_ms in the cluster parameter group. DocumentDB logs queries exceeding the threshold to CloudWatch Logs. Query the profiling logs with filter expressions to find the slowest operations. For each slow query, run explain with executionStats to see whether it uses an index or performs a collection scan. A COLLSCAN in the winning plan indicates a missing index. Create the appropriate index and re-run the explain to confirm it switches to IXSCAN. Monitor the CloudWatch MongoRequests metric by operation type and set alarms on p99 latency.' },
    ],
    references: [
      'https://docs.aws.amazon.com/documentdb/latest/developerguide/what-is.html',
      'https://docs.aws.amazon.com/documentdb/latest/developerguide/best_practices.html',
    ],
    visualizations: [
      {
        title: 'DocumentDB Cluster Architecture and Document Modeling Patterns',
        description: `Amazon DocumentDB uses the same Aurora-derived distributed storage architecture as Aurora: one primary instance for reads and writes, up to 15 replica instances sharing the same six-way replicated storage volume across three AZs. Replicas have near-zero lag because they read from shared storage rather than streaming oplogs.

DocumentDB stores BSON documents in collections. Each document can have a completely different structure -- there is no enforced schema. This flexibility allows gradual schema evolution: add a new field to new documents without touching existing ones, and handle missing fields in application code. The _id field is the mandatory primary key, indexed automatically.

The embedding vs referencing decision is the central document modeling tradeoff. Embed subdocuments and arrays when data is always retrieved together, has bounded size, and belongs to exactly one parent (order with line items embedded). Reference by _id when the related data is large, accessed independently, or shared by multiple parents (product referenced by many orders). Over-embedding creates large documents where partial reads waste I/O. Over-referencing requires multiple round trips for data that is conceptually one entity.

Indexes are mandatory for query performance. Without an index DocumentDB performs a COLLSCAN -- reading every document in the collection. Run explain() with executionStats on every query during development. An IXSCAN in the winning plan confirms index usage. The ESR rule for compound indexes (Equality fields first, Sort fields second, Range fields last) minimizes the index traversal and filter work.

The aggregation pipeline transforms documents through ordered stages. Early match stages reduce the working set before expensive stages like lookup (join), unwind (flatten arrays), and group (aggregate). Push all match stages as early as possible. A match on an indexed field at the start of the pipeline uses the index; the same match after an unwind cannot.

DocumentDB requires TLS for all connections. The global bundle CA certificate must be passed to the MongoDB driver. DocumentDB supports a subset of the MongoDB 4.0 API -- validate compatibility of all queries before committing to a migration from self-managed MongoDB.`,
      },
    ],
    quickFire: [
      { q: 'What MongoDB API version is DocumentDB compatible with?', a: 'MongoDB 4.0 API subset. Not all MongoDB features are supported -- validate all queries and aggregation stages before migrating.' },
      { q: 'Does DocumentDB require TLS?', a: 'Yes -- TLS is required for all connections. Pass the RDS global CA bundle as tlsCAFile in the MongoDB driver connection options.' },
      { q: 'What is the document size limit in DocumentDB?', a: '16MB per document -- same as MongoDB. Store large binary data in S3 and keep only a reference in DocumentDB.' },
      { q: 'How do you diagnose a slow query in DocumentDB?', a: 'Enable profiling via profiler_threshold_ms in the cluster parameter group. Profiled queries log to CloudWatch. Run explain(\"executionStats\") to confirm IXSCAN vs COLLSCAN.' },
      { q: 'When should you embed vs reference in DocumentDB?', a: 'Embed when data is always read together, bounded in size, and belongs to one parent. Reference when data is large, accessed independently, or shared across multiple parents.' },
      { q: 'How many read replicas can a DocumentDB cluster have?', a: 'Up to 15 read replicas sharing the same storage volume with near-zero replication lag.' },
      { q: 'What aggregation pipeline stage is equivalent to a SQL JOIN?', a: '$lookup -- performs a left outer join to another collection in the same database. Push $match stages before $lookup to reduce the working set.' },
      { q: 'What is a change stream in DocumentDB?', a: 'An ordered stream of insert/update/delete events for a collection or database. Used for CDC pipelines, cache invalidation, and event-driven architectures. Backed by the DocumentDB oplog.' },
      { q: 'What is the ESR rule for compound indexes?', a: 'Equality fields first, Sort fields second, Range fields last. This ordering minimizes index traversal and in-memory filter work for compound queries.' },
    ],
  },
  {
    id: 'aws-neptune',
    title: 'Neptune — Managed Graph Database',
    icon: 'activity',
    color: '#8b5cf6',
    description: 'Fully managed graph database supporting Gremlin and SPARQL for highly connected data.',
    introduction: 'Amazon Neptune is a purpose-built high-performance graph database engine optimized for storing billions of relationships and querying the graph with millisecond latency. It supports the property graph model with Apache TinkerPop Gremlin traversal language, the RDF graph model with SPARQL query language, and openCypher for property graphs. Neptune is built on an Aurora-like distributed storage architecture providing high availability and durability.',
    whenToUse: 'Use Neptune for highly connected data where relationships are first-class: social networks, knowledge graphs, fraud detection, recommendation engines, network topology management, identity graphs, and compliance/lineage tracking. Graph databases excel when the number of joins in a relational query would be unbounded or when traversing relationships is the primary query type.',
    keyConcepts: [
      { title: 'Property Graph vs RDF', description: 'Property graphs (Gremlin, openCypher) model data as vertices and edges, each with typed labels and key-value properties. Suited for operational applications. RDF graphs model data as subject-predicate-object triples suitable for ontologies and linked data. Use property graph for most application development; use RDF for semantic web and knowledge graph use cases.' },
      { title: 'Gremlin Traversals', description: 'Graph traversal language using a fluent, step-based API. A query navigates from vertices through edges to adjacent vertices. g.V().has(age, gt(30)).out(knows).values(name) finds names of people over 30 known by anyone in the graph. Gremlin is intuitive for developers but requires learning the traversal vocabulary.' },
      { title: 'Graph Storage Architecture', description: 'Neptune stores graph data on a distributed storage volume with six copies across three AZs, identical to Aurora. Compute instances are separate from storage. Read replicas share the same storage with near-zero replication lag. The storage automatically grows in 10GB increments up to 128TB.' },
      { title: 'Neptune Streams', description: 'Captures a complete, ordered sequence of every change made to the graph. Available via HTTP API. Each record includes the operation type, the element changed, and the before/after state. Used for change data capture, replication to other systems, and event-driven graph processing.' },
      { title: 'Neptune Analytics', description: 'Separate service optimized for analytical graph queries: PageRank, shortest path, community detection, and similarity algorithms. Can query a Neptune database or a graph stored in S3. Complements the transactional Neptune database for bulk graph analytics without impacting operational query latency.' },
    ],
    approach: [
      { step: 'Model the graph around traversal patterns', details: 'Identify the starting vertices and traversal paths for every query. Design edge types and vertex labels to make traversals natural. Use edge direction intentionally — most traversals will go outgoing. Model many-to-many relationships as edges with properties. Avoid overly generic edge labels like has_relationship that obscure semantic meaning.' },
      { step: 'Use openCypher for readable graph queries', details: 'openCypher provides SQL-like readability for property graph queries. MATCH (a:Person)-[:KNOWS]->(b:Person) WHERE a.age > 30 RETURN b.name is more approachable than the equivalent Gremlin traversal. Neptune supports openCypher, Gremlin, and SPARQL and you can choose the language that best fits your team.' },
      { step: 'Load data using Bulk Loader for initial ingestion', details: 'The Neptune Bulk Loader reads vertex and edge CSV files from S3 using a parallel import process. It is orders of magnitude faster than transactional insertion for initial data loading. Format vertex files with id, label, and property columns. Format edge files with from, to, label, and property columns. Queue multiple load jobs for parallel execution.' },
      { step: 'Monitor traversal performance with Neptune query logs', details: 'Enable audit logging to capture all queries. Identify slow traversals that scan many vertices without narrowing by properties. Use index-based lookups to start traversals at specific vertices. Neptune supports vertex property indexes that make has-step traversals fast. Avoid cartesian products from unbound traversals.' },
    ],
    pitfalls: [
      { title: 'Unbounded traversals', description: 'A traversal without depth limits on a highly connected graph can expand exponentially, consuming all memory and timing out. Always set maximum traversal depth using range or limit steps. Friendship-of-a-friend queries should limit depth to practical levels (2-3 hops) to avoid exploring the entire graph.' },
      { title: 'Neptune is regional and single-cluster', description: 'Neptune does not have a Global Cluster equivalent for multi-region active-active like Aurora. Cross-region DR requires either periodic snapshots and restore or custom CDC replication via Neptune Streams. Plan for single-region deployment and design for availability within the region using Multi-AZ with replicas.' },
      { title: 'Property graph and RDF cannot be mixed', description: 'A Neptune cluster is either a property graph cluster (Gremlin and openCypher) or an RDF cluster (SPARQL). The two models cannot coexist. Trying to load RDF data into a property graph cluster or vice versa fails. Choose the model that matches your use case before provisioning.' },
    ],
    keyQuestions: [
      { q: 'When would you choose Neptune over a relational database for a recommendation engine?', a: 'A recommendation engine performs multi-hop traversals: find users similar to me, find items they liked, exclude items I have already seen, rank by affinity score. In a relational database, this requires multiple self-joins and recursive CTEs that become prohibitively expensive as the graph depth increases. Neptune traverses relationships natively without join overhead — a three-hop traversal is as efficient as a one-hop traversal relative to the data touched. The graph model also makes it natural to add new relationship types (liked, purchased, viewed, disliked) without schema changes.' },
      { q: 'How do you detect fraud using graph traversal in Neptune?', a: 'Fraud often creates identifiable graph patterns: multiple accounts sharing an IP address, phone number, or device fingerprint; circular money flows where funds travel A to B to C back to A; velocity clusters where many new accounts appear from the same identity cluster within a time window. Model identity elements (email, phone, device) as vertices and link user accounts to shared identity elements via edges. A query that traverses: find all accounts sharing an identity element with this account within 30 days and flags accounts with more than N shared identities detects account farms. Time-bounded traversals using edge timestamp properties add velocity detection.' },
      { q: 'Explain how you would load a large graph dataset into Neptune efficiently.', a: 'Use the Neptune Bulk Loader, which reads from S3 in parallel across compute nodes. Prepare vertex CSV files with columns: id, label (node type), and property columns. Prepare edge CSV files with from (source vertex id), to (target vertex id), label (edge type), and property columns. Upload all files to S3. Submit a load job via the Neptune REST API pointing to the S3 prefix. Neptune processes vertex files before edge files to ensure vertex existence. Submit multiple load jobs for different entity types in parallel. For ongoing ingestion, use the transactional Gremlin API for real-time writes after initial bulk load.' },
      { q: 'How does Neptune handle high availability and what happens during a failover?', a: 'Neptune uses the same six-way distributed storage as Aurora. Compute instances are separate from storage. When the primary instance fails, Neptune promotes the highest-priority replica to primary, which involves updating the cluster endpoint DNS and initializing the new primary write path. Failover typically completes within 30 seconds. All read replicas share the same storage, so there is no data loss and no re-synchronization needed. Applications should implement retry logic with exponential backoff for the brief connection interruption during failover. Neptune Multi-AZ places replicas in different AZs so an AZ failure triggers the same automatic promotion.' },
    ],
    references: [
      'https://docs.aws.amazon.com/neptune/latest/userguide/intro.html',
      'https://docs.aws.amazon.com/neptune/latest/userguide/get-started.html',
    ],
    visualizations: [
      {
        title: 'Neptune Graph Model, Traversal Patterns, and Fraud Detection Architecture',
        description: `Amazon Neptune is a purpose-built graph database optimized for queries where the primary operation is traversing relationships between entities. It stores billions of vertices (nodes) and edges (relationships) and supports millisecond-latency multi-hop traversals.

Neptune supports two graph models. The property graph model represents data as vertices and edges, each with a label (type) and key-value properties. Queries use Apache TinkerPop Gremlin (g.V().has('age', gt(30)).out('knows').values('name')) or openCypher (MATCH (a:Person)-[:KNOWS]->(b:Person) WHERE a.age > 30 RETURN b.name). The RDF model stores data as subject-predicate-object triples queried via SPARQL -- used for semantic web, ontologies, and knowledge graphs. A Neptune cluster runs one model or the other; they cannot be mixed.

The storage architecture mirrors Aurora: six copies across three AZs, compute separated from storage, replicas share the same volume with near-zero lag. Up to 15 read replicas, automatic failover in under 30 seconds.

The traversal-first design principle separates Neptune from relational databases. A three-hop traversal (friends of friends who purchased item X) in a relational database requires three recursive self-joins that become exponentially more expensive with depth. Neptune traverses edges natively -- a three-hop traversal is algorithmically similar to a one-hop traversal relative to the data actually accessed.

Fraud detection is one of Neptune's primary use cases. Identity elements (email, phone number, device ID, IP address) are modeled as vertices. User accounts link to shared identity elements via edges. A query traversing: find all accounts sharing any identity element with this account within the last 30 days immediately exposes account farms and synthetic identity fraud rings that are invisible in row-based data stores.

Neptune Bulk Loader reads vertex and edge CSV files from S3 using a parallel import process -- orders of magnitude faster than transactional writes for initial dataset loading. Neptune Analytics (separate service) handles graph algorithms like PageRank, community detection, and shortest path without impacting operational query latency.

Always bound traversal depth in production queries. An unbounded traversal on a highly connected graph can expand exponentially, exhausting memory. Use range() or limit() steps consistently.`,
      },
    ],
    quickFire: [
      { q: 'What are the two graph models Neptune supports?', a: 'Property graph (Gremlin / openCypher) for vertices and edges with properties. RDF (SPARQL) for subject-predicate-object triples. A cluster supports one model -- they cannot be mixed.' },
      { q: 'Why is graph traversal faster than SQL joins for multi-hop queries?', a: 'Neptune traverses edges natively without join overhead. A three-hop traversal accesses only the relevant connected vertices. SQL recursive joins scan the full join table at each hop, growing exponentially.' },
      { q: 'What is the Neptune Bulk Loader?', a: 'Reads vertex and edge CSV files from S3 in parallel -- orders of magnitude faster than transactional Gremlin inserts for initial dataset loading.' },
      { q: 'Does Neptune have a Global Database like Aurora?', a: 'No -- Neptune does not have an active-active Global Database equivalent. Cross-region DR requires periodic snapshots or custom CDC via Neptune Streams.' },
      { q: 'What is Neptune Analytics?', a: 'A separate Neptune service optimized for bulk graph algorithms (PageRank, shortest path, community detection) on the graph data without impacting operational transactional query latency.' },
      { q: 'What happens if you run an unbounded traversal on a highly connected graph?', a: 'The traversal expands exponentially through the graph, consuming all memory and timing out. Always set depth limits using range() or limit() steps -- cap friendship-of-a-friend at 2-3 hops.' },
      { q: 'How do you load data into Neptune at scale?', a: 'Neptune Bulk Loader: prepare vertex CSV (id, label, properties) and edge CSV (from, to, label, properties), upload to S3, submit a load job via the Neptune REST API.' },
      { q: 'What query language should you prefer for readability on property graphs?', a: 'openCypher -- SQL-like syntax (MATCH, WHERE, RETURN) is more readable than Gremlin traversal steps for most developers. Neptune supports both.' },
      { q: 'How does Neptune handle high availability?', a: 'Six-way storage replication across three AZs (Aurora architecture). Automatic failover promotes a replica in under 30 seconds. No data loss because replicas share the same storage volume.' },
    ],
  },
  {
    id: 'aws-keyspaces',
    title: 'Keyspaces — Managed Apache Cassandra',
    icon: 'database',
    color: '#8b5cf6',
    description: 'Serverless Apache Cassandra-compatible database for wide-column workloads at scale.',
    introduction: 'Amazon Keyspaces is a scalable, highly available, and fully managed Apache Cassandra-compatible database service. It makes it easy to run Cassandra workloads on AWS without managing Cassandra cluster infrastructure. Keyspaces is serverless — it automatically scales tables based on actual application traffic and charges only for the throughput and storage you use. It is compatible with Cassandra Query Language (CQL) drivers.',
    whenToUse: 'Use Keyspaces for migrating existing Cassandra applications to managed infrastructure, wide-column data models for time-series and IoT data, high-velocity write workloads where Cassandras write-optimized storage model shines, and applications requiring geographic distribution with multi-region tables.',
    keyConcepts: [
      { title: 'Keyspaces and Tables', description: 'A keyspace is a container for tables, equivalent to a schema in relational databases. Tables have a primary key of partition key plus optional clustering columns. Data is distributed by partition key hash across storage nodes. Clustering columns determine the sort order within a partition.' },
      { title: 'CQL Data Model', description: 'Wide-column model where each row can have a dynamic set of columns. Partition key determines data distribution. Clustering columns create a sorted order within the partition, enabling efficient range scans. Queries must include the partition key — table scans are inefficient and should be avoided.' },
      { title: 'Consistency Levels', description: 'Keyspaces supports LOCAL_QUORUM (reads acknowledged by majority of replicas in the local region, strongly consistent) and LOCAL_ONE (reads from single replica, eventually consistent and faster). Unlike self-managed Cassandra, Keyspaces always writes with LOCAL_QUORUM durability.' },
      { title: 'Multi-Region Replication', description: 'Keyspaces multi-Region tables replicate data across multiple AWS regions with eventual consistency. Applications write to the nearest region and changes propagate globally. Ideal for globally distributed applications requiring local write latency with data availability everywhere.' },
      { title: 'Point-in-Time Recovery', description: 'Keyspaces supports PITR for Keyspaces tables, enabling recovery to any second within the last 35 days. A restore creates a new table. Useful for accidental bulk delete recovery scenarios that are otherwise unrecoverable in Cassandra.' },
    ],
    approach: [
      { step: 'Design the data model around queries', details: 'In Cassandra and Keyspaces, the data model must be designed to serve specific queries efficiently. Denormalization and query-specific tables are the norm. One query, one table is the guiding principle. Model partition keys to distribute load evenly and clustering columns to sort data in the order queries need it.' },
      { step: 'Avoid wide partitions', details: 'A partition that grows unboundedly (all events for a device since launch, all messages in a chat room) creates a hot partition and eventually hits the 10GB partition size limit. Use time bucketing: partition key is device_id + year_month, clustering key is timestamp. Each partition covers one month of data.' },
      { step: 'Use TTL for data lifecycle management', details: 'Set default_time_to_live on the table or per-row TTL using the USING TTL option in INSERT statements. Expired rows are automatically purged. Essential for time-series data, event logs, and any bounded retention workload. Monitor StorageUsed and confirm TTL cleanup is working as expected.' },
      { step: 'Migrate from self-managed Cassandra using cqlsh and COPY', details: 'Export existing Cassandra data using COPY TO CSV from each table. Import into Keyspaces using COPY FROM or the Keyspaces data loader. Validate row counts. Update application connection strings to the Keyspaces endpoint with TLS and SigV4 authentication. Test each CQL query for compatibility.' },
    ],
    pitfalls: [
      { title: 'CQL compatibility is not 100%', description: 'Keyspaces supports a subset of CQL. Unsupported features include lightweight transactions (LWT), batch operations across multiple partitions, counter columns, materialized views with complex filtering, and some aggregate functions. Test all application queries against Keyspaces before committing to a migration.' },
      { title: 'Hot partitions from skewed partition keys', description: 'A partition key with low cardinality (status with three values, boolean flag) directs all traffic to a few partitions. Keyspaces applies per-partition throughput limits. Add a high-cardinality component to the partition key or append a bucket suffix to spread load.' },
      { title: 'On-demand vs provisioned cost at scale', description: 'Keyspaces on-demand charges per WCU and RCU consumed. For high-velocity steady workloads, provisioned capacity with auto scaling is significantly cheaper. Monitor actual WCU and RCU consumption over time and switch to provisioned once the traffic pattern is predictable.' },
    ],
    keyQuestions: [
      { q: 'How does the Cassandra wide-column data model differ from a relational database?', a: 'In a relational database, all rows in a table have the same columns and schema changes require ALTER TABLE. In Cassandra and Keyspaces, each row can have a different set of non-primary-key columns — columns not present in a row simply take no storage. More fundamentally, the data model must be designed around specific query patterns rather than normalized entities. A single logical entity (customer order history) may be modeled as multiple tables each optimized for a specific access pattern. Joins do not exist; data is denormalized and duplicated across query-specific tables.' },
      { q: 'What is the N+1 anti-pattern in Cassandra and how do you avoid it?', a: 'The N+1 pattern occurs when an application fetches a list of IDs from one table then executes N individual queries to look up details for each ID. In Cassandra, each query has network and I/O overhead. For N=1000 IDs, this generates 1001 round trips. Avoid it by using BatchGetItems with up to 100 keys, by denormalizing related data into the same table so a single query returns everything needed, or by restructuring the data model so the list query already includes the detail fields, eliminating the second query entirely.' },
      { q: 'How would you design a time-series schema for IoT sensor data in Keyspaces?', a: 'Partition key: device_id + year_month. Clustering key: event_timestamp DESC. This groups all events for a device in a given month into one partition, sorted newest-first for efficient retrieval of recent readings. Queries for a device over the last 30 days touch at most two partitions and use a clustering column range predicate. A query for a specific device in June 2024 specifies the exact partition key. TTL on the table automatically purges data older than the retention period. The year_month bucket prevents any single partition from growing unboundedly.' },
      { q: 'How does Keyspaces differ from self-managed Cassandra in terms of operations?', a: 'Self-managed Cassandra requires managing node provisioning, ring rebalancing during scale events, compaction strategies, repair operations to maintain consistency between replicas, and JVM tuning. Keyspaces is serverless — it handles all cluster management, replication, compaction, and scaling automatically. The tradeoff is reduced control over advanced Cassandra features: some CQL features are unsupported, compaction strategy cannot be configured, and authentication is via AWS IAM rather than Cassandra internal auth. For teams without Cassandra expertise who want the wide-column data model, Keyspaces eliminates significant operational overhead.' },
    ],
    references: [
      'https://docs.aws.amazon.com/keyspaces/latest/devguide/what-is-keyspaces.html',
      'https://docs.aws.amazon.com/keyspaces/latest/devguide/best-practices.html',
    ],
    visualizations: [
      {
        title: 'Keyspaces Wide-Column Data Model and Time-Bucketing Patterns',
        description: `Amazon Keyspaces is a serverless, Apache Cassandra-compatible wide-column database. The fundamental difference from relational databases is that the schema must be designed around specific query access patterns, not around normalized entities.

The partition key hashes to determine which storage node holds the data. The clustering key defines the physical sort order within a partition. A query must specify the full partition key -- range scans on clustering keys within a partition are efficient; anything that requires scanning multiple partitions or omits the partition key is expensive.

Wide-column means each row can have a different set of non-primary-key columns. Columns not present in a row consume no storage. This flexibility enables sparse data models where different entity subtypes share a table without nullable column overhead.

The "one query, one table" principle drives Keyspaces schema design. If you need to look up orders by customer ID, create a table with customer_id as the partition key. If you also need orders by status, create a separate table (or a materialized view equivalent) optimized for that query. Denormalization and data duplication are intentional -- Keyspaces (like Cassandra) trades storage for query performance.

Time-bucketing is the standard pattern for time-series data in Keyspaces. An IoT device that writes continuously would create an ever-growing partition if device_id alone is the partition key -- eventually exceeding the 10GB partition limit and creating a hot partition. The solution is a composite partition key: device_id + year_month. Each month's data lives in its own partition. Queries for a device in a specific month specify the exact partition key. Queries spanning multiple months issue parallel requests per month-bucket and merge results in the application.

TTL (time-to-live) is built into Keyspaces at the table and row level. Set default_time_to_live on the table for automatic expiry of all rows, or use USING TTL in individual INSERT statements for per-row expiry. TTL is essential for any data with a bounded retention requirement (sensor readings, session tokens, event logs).

Keyspaces supports LOCAL_QUORUM (strongly consistent majority-acknowledgment reads) and LOCAL_ONE (single-replica, eventually consistent). Always write at LOCAL_QUORUM durability (Keyspaces default). For reads, LOCAL_ONE is faster but may return stale data during replica lag.`,
      },
    ],
    quickFire: [
      { q: 'What is the partition size limit in Keyspaces?', a: '10GB per partition. Time-bucket composite partition keys (device_id + year_month) prevent any single partition from growing unboundedly.' },
      { q: 'What consistency levels does Keyspaces support?', a: 'LOCAL_QUORUM (strongly consistent, majority of replicas acknowledge) and LOCAL_ONE (eventually consistent, single replica). Writes always use LOCAL_QUORUM durability.' },
      { q: 'What is the guiding principle for Keyspaces schema design?', a: 'One query, one table. Design each table to serve one specific access pattern efficiently. Denormalize and duplicate data across query-specific tables -- joins do not exist.' },
      { q: 'How do you prevent hot partitions for time-series data in Keyspaces?', a: 'Use a composite partition key: entity_id + time_bucket (e.g., device_id + year_month). Each time bucket is a separate partition, distributing write load across partitions.' },
      { q: 'Does Keyspaces support Cassandra lightweight transactions (LWT)?', a: 'No -- LWT (IF NOT EXISTS, IF condition) is not supported. This is a major compatibility gap for applications relying on compare-and-set operations.' },
      { q: 'How do you authenticate to Keyspaces?', a: 'AWS SigV4 authentication -- use the Amazon Keyspaces SigV4 plugin for the Cassandra driver. Standard Cassandra username/password authentication is not used.' },
      { q: 'What does TTL do in Keyspaces?', a: 'Automatically deletes rows after a configured number of seconds. Set per-table via default_time_to_live or per-row via USING TTL in INSERT. Essential for bounded-retention workloads like sensor data and session tokens.' },
      { q: 'When is Keyspaces a better choice than DynamoDB?', a: 'When you have existing Cassandra applications using CQL drivers (minimal code changes for migration), need Cassandra-specific data model semantics, or require multi-region tables with eventual consistency for globally distributed writes.' },
      { q: 'Does Keyspaces support point-in-time recovery?', a: 'Yes -- PITR to any second within the last 35 days. A restore creates a new table.' },
    ],
  },
  {
    id: 'aws-timestream',
    title: 'Timestream — Managed Time-Series Database',
    icon: 'activity',
    color: '#8b5cf6',
    description: 'Serverless time-series database optimized for IoT, operational metrics, and telemetry.',
    introduction: 'Amazon Timestream is a fast, scalable, and serverless time-series database designed to store and analyze trillions of time-series data points per day. It automatically scales to adjust capacity as data ingestion and query workloads change. Timestream separates recent (hot) data stored in memory from older (warm) data stored on SSD from historical (cold) data in S3, providing a tiered storage model that balances performance and cost.',
    whenToUse: 'Use Timestream for DevOps metrics and application telemetry, IoT sensor data with high ingestion rates, financial time-series data, application performance monitoring, and any workload producing measurements with a timestamp dimension. It is not a general-purpose database — use it specifically where the primary query dimension is time.',
    keyConcepts: [
      { title: 'Tables and Dimensions', description: 'Timestream tables store time-series records. Each record has a time attribute, one or more dimensions (metadata like host, region, instance_type), and one or more measures (the actual values: cpu_utilization, memory_bytes). Dimensions are strings. Measures can be double, bigint, boolean, varchar, or multi-measure records.' },
      { title: 'Storage Tiers', description: 'Memory store holds the most recent data (configurable retention from 1 hour to 1 year) in memory for fast queries. Magnetic store holds historical data on magnetic storage at low cost. Data automatically transitions from memory to magnetic store based on the retention policy. Queries transparently span both tiers.' },
      { title: 'Scheduled Queries', description: 'Pre-compute aggregations on a schedule and write the results to a new Timestream table. Reduces query cost for frequently accessed aggregate views. A scheduled query computing hourly averages runs once per hour and stores results, so dashboards read the pre-computed table instead of re-aggregating the raw data.' },
      { title: 'Multi-Measure Records', description: 'Store multiple measures in a single record to reduce write cost and improve query performance. A server metrics record can contain cpu, memory, disk_io, and network_bytes as separate measure columns in one record rather than four separate single-measure records. Reduces records written by 4x and improves columnar scan performance.' },
      { title: 'SQL Query Language', description: 'Timestream uses SQL with time-series specific functions: bin() for time bucketing, interpolate() for filling gaps, time_series() for creating series objects, and window functions for rolling aggregations. Queries can join Timestream data with dimension enrichment tables.' },
    ],
    approach: [
      { step: 'Use multi-measure records for efficiency', details: 'Group all measures collected at the same time from the same entity into a single multi-measure record. Instead of writing four separate records for cpu, memory, disk, and network, write one record with all four measures. This reduces ingestion cost (priced per record), improves query performance through better column pruning, and simplifies the time series model.' },
      { step: 'Set appropriate memory store retention', details: 'Memory store is expensive but fast. Set retention to cover the hot query window — typically 24-72 hours for operational dashboards. Data older than the memory retention automatically moves to magnetic storage. Setting memory retention too long wastes cost. Use scheduled queries to pre-compute aggregations for older data.' },
      { step: 'Design dimensions carefully', details: 'Dimensions are metadata that describe the time series. Keep dimension cardinality bounded — hostname and service are good dimensions. Avoid high-cardinality transient values like request_id as dimensions, as they create too many unique time series. Measures are the actual values to store and analyze.' },
      { step: 'Integrate with Grafana for visualization', details: 'Amazon Managed Grafana has a native Timestream data source plugin. Connect Grafana to Timestream using the data source configuration. Build dashboards with time-series panels using Timestream SQL queries. Use Grafana variables for dimension filters (select host, service) to create interactive dashboards.' },
    ],
    pitfalls: [
      { title: 'Late data and memory store backfill limits', description: 'Timestream does not accept records with timestamps older than the memory store retention period. Late-arriving data beyond that window is rejected. Design ingestion pipelines to process data promptly. If backfilling historical data from another system, use the multi-measure record format and ensure timestamps are within the memory retention window, or contact AWS to enable magnetic store writes for bulk historical loads.' },
      { title: 'No primary key or unique constraints', description: 'Timestream does not enforce uniqueness. Duplicate records with the same time and dimensions are stored as separate records. Idempotent ingestion requires deduplication at the application layer before writing to Timestream. Monitor for duplicate data caused by retry logic writing the same record twice.' },
      { title: 'Query cost on magnetic storage', description: 'Queries on magnetic store are priced per GB scanned. A query without time bounds that scans years of historical data can be expensive. Always include time range predicates in WHERE clauses. Use scheduled queries to pre-aggregate historical data and query the aggregate table for dashboard workloads.' },
    ],
    keyQuestions: [
      { q: 'How does Timstreamss tiered storage model reduce cost compared to storing all data in memory?', a: 'Memory store holds data in RAM for fast millisecond query latency but costs approximately 50 times more per GB than magnetic store. By keeping only recent hot data (typically 24-72 hours) in memory, Timestream ensures that operational dashboards querying the last few hours run fast at low cost. Historical data that is queried infrequently moves to magnetic storage automatically based on the retention policy. Queries transparently span both tiers. A query for the last week fetches recent data from memory and older data from magnetic in the same SQL query, paying the lower magnetic scan cost for the bulk of the data.' },
      { q: 'What is a scheduled query and when should you use one?', a: 'A scheduled query runs a Timestream SQL aggregation on a cron schedule and writes the results to a Timestream table. Use it when a dashboard or alert fires the same expensive aggregation query repeatedly against raw data. For example, compute hourly average CPU utilization per host once per hour via scheduled query and store in an aggregate table. The dashboard reads the pre-computed table, paying only the storage cost of the results rather than re-scanning gigabytes of raw records on every dashboard refresh. Scheduled queries dramatically reduce query costs for high-frequency read workloads against large historical datasets.' },
      { q: 'How would you design an IoT ingestion pipeline feeding Timestream?', a: 'IoT devices publish telemetry to AWS IoT Core using MQTT. An IoT rule routes messages matching a topic pattern to a Lambda function or directly to Kinesis Data Streams. A Lambda function batches records (up to 100 per WriteRecords call) and writes to Timestream using multi-measure records, combining all sensor readings from the same device at the same timestamp into one record. Set memory store retention to cover the operational alerting window. Enable magnetic store writes for historical data. Grafana dashboards query Timestream for trend visualization and CloudWatch alarms monitor recent Timestream data via scheduled queries.' },
      { q: 'How do you query for anomalies in Timestream using time-series functions?', a: 'Use the CREATE_TIME_SERIES function to build a series object from a set of time-value pairs, then apply ANOMALY_DETECTION to identify data points that deviate significantly from the expected pattern. Alternatively, use a rolling statistics approach: compute the mean and standard deviation over a sliding window using window functions, then flag points where the value exceeds mean plus three standard deviations. The bin() function buckets time into regular intervals for aggregation before anomaly detection. Scheduled queries can run anomaly detection periodically and write flagged anomalies to a separate table for alerting.' },
    ],
    references: [
      'https://docs.aws.amazon.com/timestream/latest/developerguide/what-is-timestream.html',
      'https://docs.aws.amazon.com/timestream/latest/developerguide/best-practices.html',
    ],
  },
{
  id: 'aws-iam',
  title: 'IAM — Identity and Access Management',
  icon: 'shield',
  color: '#ef4444',
  description: 'AWS identity system — users, roles, policies, and permissions.',
  introduction: 'AWS IAM is the foundation of security in every AWS account. It controls which principals (users, roles, services) can perform which actions on which resources. Every single AWS API call — whether from the console, CLI, or SDK — passes through IAM for authorization. Understanding IAM deeply is non-negotiable for any AWS architect or security engineer, because a misconfigured IAM policy is the most common path to a cloud breach.',
  whenToUse: 'Always. Every AWS workload requires IAM. Use IAM users for human operators with long-term credentials (though SSO is preferred at scale), IAM roles for EC2 instances, Lambda functions, ECS tasks, and any AWS service that needs to call other AWS services, and IAM groups to organize users with common permission sets. Use resource-based policies when granting cross-account or cross-service access directly to a resource.',
  keyConcepts: [
    { title: 'Users vs Roles vs Groups', description: 'IAM users are long-term identities with static credentials for humans or legacy systems. IAM roles are temporary-credential identities assumed by services, EC2 instances, Lambda, or cross-account principals — they do not have passwords or access keys. IAM groups are collections of users that share permission policies; groups cannot be assumed and cannot belong to other groups.' },
    { title: 'Identity-Based vs Resource-Based Policies', description: 'Identity-based policies attach to a principal (user, role, group) and define what that principal can do. Resource-based policies attach to a resource (S3 bucket, KMS key, SQS queue) and define who can access that resource. For same-account access, either type alone grants access. For cross-account access, both the identity-based policy on the caller and the resource-based policy on the resource must allow the action.' },
    { title: 'STS and Temporary Credentials', description: 'AWS Security Token Service issues short-lived credentials (access key, secret key, session token) when a role is assumed. AssumeRole, AssumeRoleWithWebIdentity, and AssumeRoleWithSAML are the primary STS operations. Temporary credentials expire automatically, eliminating the credential-rotation problem and reducing blast radius on compromise. Session duration is configurable from 15 minutes to 12 hours depending on the role settings.' },
    { title: 'Permissions Boundaries', description: 'A permissions boundary is an advanced IAM feature that sets the maximum permissions an identity-based policy can grant to a user or role. The effective permissions are the intersection of the permissions boundary and the identity-based policy. Boundaries are used to delegate permission granting to developers while preventing privilege escalation — a developer can create roles but only up to the boundary ceiling you set.' },
    { title: 'IAM Access Analyzer', description: 'IAM Access Analyzer continuously monitors your resource policies (S3, KMS, SQS, IAM roles, Lambda, Secrets Manager) and flags any that grant access to external principals. It uses automated reasoning to produce definitive findings, not heuristics. It also validates policies against IAM grammar and best practices before deployment, and generates least-privilege policies from CloudTrail activity logs.' },
    { title: 'Credential Report and Last-Used Data', description: 'The IAM credential report is a CSV listing all IAM users and the status of their credentials — password age, access key age, MFA status, and last-used timestamps. Combined with the IAM console last-used data for roles and policies, it lets you identify dormant credentials, users without MFA, and unused permissions that should be removed.' },
  ],
  approach: [
    { step: 'Audit existing permissions', details: 'Generate the IAM credential report and review all users for MFA status, unused access keys, and password age. Use IAM Access Analyzer to find resource policies exposing data to external accounts. Use the access advisor (last-accessed data) in the IAM console to identify services that roles and users have never called, then remove those permissions.' },
    { step: 'Enforce least privilege on new policies', details: 'Start with deny-all and add only the specific actions and resources required. Use IAM Access Analyzer policy validation to check for errors and warnings before attaching. Avoid wildcards in Action fields — specify exact actions like s3:GetObject rather than s3:*. Scope Resource to specific ARNs rather than using *. Add conditions like aws:RequestedRegion to limit where actions can be called.' },
    { step: 'Replace long-term credentials with roles', details: 'Identify all IAM users with programmatic access keys. For workloads running on EC2, Lambda, ECS, or EKS, migrate to IAM roles so the service automatically receives rotating temporary credentials via the instance metadata service. For human operators, migrate to AWS IAM Identity Center (SSO) with federated identity rather than per-user IAM users. Delete or disable any access keys not rotated within 90 days.' },
    { step: 'Implement MFA and strong password policy', details: 'Enforce MFA on all IAM users, especially those with console access or administrative permissions. Set the account password policy to require minimum length (14+ characters), complexity, and rotation. For programmatic access, consider requiring MFA conditions in sensitive policies using aws:MultiFactorAuthPresent. Use virtual MFA for general users and hardware MFA tokens for root and break-glass accounts.' },
    { step: 'Monitor and alert on IAM changes', details: 'Enable CloudTrail in all regions and create CloudWatch metric filters and alarms for IAM policy changes, role creation, user creation, and root account usage. Use AWS Config rules like iam-user-mfa-enabled, iam-root-access-key-check, and access-keys-rotated to continuously evaluate compliance. Set up Security Hub to aggregate IAM-related findings from GuardDuty, Access Analyzer, and Config.' },
  ],
  pitfalls: [
    { title: 'Wildcard permissions', description: 'Using * in Action or Resource fields grants far more access than intended. Action: ["s3:*"] on Resource: ["*"] effectively gives full S3 access across all buckets in the account. Always enumerate specific actions and scope resources to exact ARNs or ARN patterns with conditions.' },
    { title: 'Storing credentials in code or environment variables', description: 'Hardcoding access keys in source code or storing them in environment variables on EC2 or Lambda is a critical mistake. Credentials leaked via public repositories or instance compromise become immediately usable by attackers. Use IAM roles with instance profiles or execution roles so credentials are never handled by application code at all.' },
    { title: 'Overpermissioned service roles', description: 'Lambda functions, EC2 instances, and ECS tasks are frequently given AdministratorAccess or overbroad policies during development and never tightened before production. Run IAM Access Analyzer to generate least-privilege policies from actual CloudTrail usage data before deploying to production. Each service should have a dedicated role with only the permissions it actually needs.' },
    { title: 'Ignoring the root account', description: 'The AWS root account bypasses all IAM policies and cannot be restricted. It must never be used for daily operations. Delete all root access keys immediately after account creation, enable MFA on root, and store root credentials in a physical safe or secrets vault. Set up a CloudWatch alarm on any root login event so you are immediately notified of unauthorized use.' },
  ],
  keyQuestions: [
    { q: 'How do you implement cross-account access securely?', a: 'Create an IAM role in the target account with a trust policy that allows the specific principal from the source account — use the exact IAM role ARN rather than the entire account. The source account principal assumes the role via STS AssumeRole, receiving temporary credentials scoped to only what the target role allows. Never share long-term access keys across accounts. For third-party access, always add an ExternalId condition to the trust policy to prevent confused deputy attacks, where a malicious service tricks your trusted service into assuming a role on their behalf. Audit cross-account role usage by filtering CloudTrail for AssumeRole events.' },
    { q: 'What is the difference between permissions boundaries and SCPs?', a: 'Permissions boundaries restrict the maximum permissions of an individual IAM user or role within a single account — they do not grant permissions on their own, they only cap them. Service Control Policies are AWS Organizations features that apply to entire accounts or organizational units and restrict what actions can be performed within those accounts at all, regardless of what IAM policies allow. SCPs affect every principal in the account including the account root (except the management account root). A developer could have AdministratorAccess but an SCP blocking ec2:* means they still cannot touch EC2. Boundaries control delegation; SCPs control organizational guardrails.' },
    { q: 'How do you implement least privilege at scale in a large organization?', a: 'At scale, manually crafting least-privilege policies is impractical, so you use tooling and automation. Use IAM Access Analyzer to generate policies from actual CloudTrail activity — this shows exactly what actions were called over a configurable lookback period and produces a minimal policy. Implement permission boundaries so teams can create roles but cannot exceed a defined ceiling. Use AWS IAM Identity Center with permission sets assigned to groups, synced from your identity provider, so access is always tied to HR-managed groups. Regularly run automated reports comparing granted vs used permissions and generate Jira tickets for remediation. Use infrastructure-as-code with policy linting (cfn-python-lint, checkov) to catch overpermissive policies before deployment.' },
    { q: 'How does IAM evaluate a request that has both an identity policy and a resource policy?', a: 'IAM evaluates policies in a specific order: first it checks for explicit denies in any policy (SCPs, resource policies, identity policies, permission boundaries, session policies) — an explicit deny always wins. Then it looks for explicit allows. For same-account access, an allow in either the identity policy or the resource policy is sufficient to grant access. For cross-account access, there must be an explicit allow in both the identity-based policy attached to the calling principal and the resource-based policy on the target resource. If any applicable SCP does not allow the action, the request is denied regardless of identity or resource policies.' },
  ],
  references: [
    'https://docs.aws.amazon.com/IAM/latest/UserGuide/',
    'https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies_boundaries.html',
    'https://docs.aws.amazon.com/IAM/latest/UserGuide/access-analyzer-getting-started.html',
  ],
  visualizations: [
    { title: 'IAM Authorization Model', caption: 'How a principal\'s request flows through SCPs, permissions boundaries, and identity/resource policies to reach an explicit Allow or Deny decision.', image: '/diagrams/cloud/aws-iam-model.png' },
    { title: 'IAM Roles — AssumeRole Flow', caption: 'How EC2 instance profiles, Lambda execution roles, and IAM users call STS to obtain temporary credentials for accessing target services.', image: '/diagrams/cloud/aws-iam-roles.png' },
  ],
  visualizations: [
    { title: 'RDS Multi-AZ High Availability', caption: 'Synchronous standby replication across AZs, automatic DNS failover in 60-120 seconds, plus async read replicas for read scaling and cross-region DR.', image: '/diagrams/cloud/aws-rds-ha.png' },
    { title: 'RDS vs Aurora Architecture', caption: 'How Aurora\'s shared cluster storage with 6 copies across 3 AZs differs from standard RDS per-instance EBS volumes and block-level replication.', image: '/diagrams/cloud/aws-rds-vs-aurora.png' },
  ],
  visualizations: [
    { title: 'DynamoDB Data Model', caption: 'Table partitioning by partition key, sort key range queries, LSI (same partition) and GSI (global async replica) index strategies with example query patterns.', image: '/diagrams/cloud/aws-dynamodb-data-model.png' },
    { title: 'DynamoDB Capacity Modes + DAX', caption: 'On-Demand vs Provisioned capacity tradeoffs, DAX in-memory cache for sub-millisecond reads, burst capacity mechanics, and hot-partition failure modes.', image: '/diagrams/cloud/aws-dynamodb-capacity.png' },
  ],
  visualizations: [
    { title: 'VPC Architecture', caption: 'Three-tier subnet layout (public ALB, private app, private data) with Internet Gateway, NAT Gateway, Security Groups, Network ACLs, and VPC Endpoints for private AWS API access.', image: '/diagrams/cloud/aws-vpc-architecture.png' },
    { title: 'VPC Connectivity Options', caption: 'Comparison of VPC Peering (point-to-point) versus Transit Gateway (hub-and-spoke) for multi-VPC routing, plus Site-to-Site VPN and Direct Connect for on-premises connectivity.', image: '/diagrams/cloud/aws-vpc-connectivity.png' },
  ],
},
{
  id: 'aws-iam-advanced',
  title: 'IAM — Advanced Patterns',
  icon: 'lock',
  color: '#ef4444',
  description: 'SCPs, permission boundaries, ABAC, conditions, federation, and confused deputy prevention.',
  introduction: 'Beyond basic IAM users and roles, AWS provides sophisticated mechanisms for fine-grained, scalable access control. Service Control Policies enforce organizational guardrails across entire AWS accounts. Attribute-Based Access Control tags both principals and resources to drive dynamic, self-maintaining policies. IAM Conditions add contextual constraints to every policy statement. Identity federation bridges your corporate IdP to AWS roles without maintaining IAM users. Understanding these patterns separates a basic AWS user from a security architect.',
  whenToUse: 'Use SCPs in any multi-account AWS Organizations setup to enforce mandatory controls — region restrictions, service restrictions, and prevent disabling of security tooling. Use permission boundaries when delegating IAM administration to application teams who should not be able to escalate their own privileges. Use ABAC when you have many resources and principals that share a tagging taxonomy and want policies that grow automatically. Use federation whenever you have more than a handful of human operators — IdP-managed groups are far more maintainable than IAM users.',
  keyConcepts: [
    { title: 'Service Control Policies (SCPs)', description: 'SCPs are AWS Organizations policies attached to the root, an OU, or an individual account. They define the maximum permissions available to all IAM principals in the affected accounts, including the account root. SCPs do not grant permissions — they only restrict what IAM policies can allow. A common pattern is a deny-list SCP that blocks leaving AWS Organizations, disabling CloudTrail, or making changes to security tooling, combined with a policy requiring MFA for sensitive actions.' },
    { title: 'Attribute-Based Access Control (ABAC)', description: 'ABAC uses tags on both IAM principals (via PrincipalTag condition keys) and AWS resources to drive access decisions dynamically. A single ABAC policy statement can grant access to all resources tagged with the same team or environment as the calling principal, without enumerating specific resource ARNs. As new resources are tagged and new roles are tagged, access is automatically correct without policy changes. This dramatically reduces policy proliferation in large environments.' },
    { title: 'IAM Conditions', description: 'Conditions add context-sensitive constraints to policy statements. Key condition keys include aws:RequestedRegion (restrict actions to specific regions), aws:PrincipalTag (match principal tags in ABAC), aws:ResourceTag (match resource tags), aws:SourceIp and aws:VpcSourceIp (network-based restrictions), aws:MultiFactorAuthPresent (require MFA), aws:CalledVia (restrict to calls made through specific services like CloudFormation), and aws:PrincipalOrgID (restrict to principals within your AWS organization).' },
    { title: 'Confused Deputy Problem', description: 'The confused deputy is a security vulnerability where a trusted service (the deputy) is tricked by a malicious actor into performing actions using the trust it has been granted. In AWS, this occurs when a third-party service assumes a role in your account using only your account ID. The mitigation is to require an ExternalId in the trust policy — a unique secret shared out-of-band between you and the third party. The third party must supply this ExternalId in every AssumeRole call, so a malicious actor who only knows your account ID cannot exploit the role.' },
    { title: 'Identity Federation with SAML and OIDC', description: 'SAML 2.0 federation lets your corporate IdP (Active Directory, Okta, Ping) authenticate users and exchange SAML assertions for temporary AWS credentials via AssumeRoleWithSAML. OIDC federation is used for web and mobile identities (Google, Facebook, Amazon) and Kubernetes service accounts (IRSA) via AssumeRoleWithWebIdentity. In both cases, users never get IAM users — they get temporary credentials scoped to roles you define. AWS IAM Identity Center builds on SAML/OIDC with a managed portal, permission sets, and multi-account assignment.' },
    { title: 'Cross-Account Role Chaining', description: 'Role chaining occurs when a role in Account A assumes a role in Account B which then assumes another role in Account C. Each hop uses STS AssumeRole and issues new temporary credentials. The maximum session duration for chained roles is capped at 1 hour regardless of the individual role maximum session duration settings. Role chaining is tracked in CloudTrail with each AssumeRole event showing the source principal, so the full chain is auditable. Avoid deep chains in critical paths because they add latency and complicate debugging.' },
  ],
  approach: [
    { step: 'Design your SCP strategy before deploying accounts', details: 'Define a deny-list of actions that should never be permitted in any account — disabling CloudTrail, leaving the organization, deleting Config rules, making S3 buckets public in production. Apply these SCPs at the root or a high-level OU. Separately, create region-restriction SCPs attached to production OUs that deny all actions outside your approved regions using a NotAction exemption list for global services like IAM, STS, CloudFront, and Route 53.' },
    { step: 'Implement permission boundaries for developer self-service', details: 'Create a permission boundary policy that caps what developers can grant to roles they create — for example, preventing them from granting IAM administrative permissions or disabling security controls. Attach this boundary to all developer roles via SCP or policy condition. When a developer uses iam:CreateRole or iam:PutRolePolicy, a condition can require that the new role has the boundary attached, preventing privilege escalation through role creation.' },
    { step: 'Build ABAC tagging taxonomy', details: 'Define a consistent set of mandatory resource tags — at minimum, Project, Environment, Team, and CostCenter. Enforce tag presence using AWS Config rules and SCP conditions like aws:RequestTag. Write ABAC policies that use aws:PrincipalTag/Project and aws:ResourceTag/Project condition keys so each team can only modify resources tagged with their project. This eliminates the need to update policies as new resources are created.' },
    { step: 'Federate all human access through IAM Identity Center', details: 'Connect your IdP to AWS IAM Identity Center via SAML 2.0 or the managed SCIM provisioning. Create permission sets corresponding to job roles (ReadOnly, Developer, SecurityAdmin, BillingAdmin). Assign permission sets to IdP groups so access is driven by HR-managed group membership. Users access AWS via the Identity Center portal with SSO — no IAM users, no long-term credentials. All access is automatically revoked when the user is removed from the IdP group.' },
    { step: 'Audit and detect anomalous role usage', details: 'Enable CloudTrail in all regions and create EventBridge rules for high-risk IAM events: CreatePolicy, AttachRolePolicy, PutRolePolicy, CreateUser, CreateAccessKey, and UpdateAssumeRolePolicy. Feed these events to Security Hub and GuardDuty. Use IAM Access Analyzer to continuously monitor for roles with overly permissive trust policies and resource policies granting external access. Review the Access Advisor regularly to identify and remove unused service permissions.' },
  ],
  pitfalls: [
    { title: 'SCPs blocking emergency access', description: 'Overly restrictive SCPs applied at the root can lock out even the management account or break-glass roles if not carefully designed. Always test SCPs in a sandbox OU first. Maintain a dedicated break-glass account not under restrictive OUs, and document the emergency access procedure. Never apply SCPs to the management account.' },
    { title: 'Confused deputy without ExternalId', description: 'Granting cross-account role access without an ExternalId in the trust policy condition makes your role vulnerable to confused deputy attacks. Any third party that learns your account ID and role ARN could potentially have a trusted service assume your role. Always require ExternalId for any third-party integrations and rotate it if the third party is no longer trusted.' },
    { title: 'ABAC without enforcing tag immutability', description: 'ABAC policies are only as secure as your tagging controls. If developers can freely tag resources with any project or team tag, they can tag a production resource with their development project tag and gain access. Use SCP conditions and resource-based policies to restrict who can apply specific tag keys and values, and use AWS Config rules to detect unauthorized tag changes.' },
    { title: 'Role chaining session duration surprise', description: 'A common operational issue is when a workflow assumes a role with a 12-hour session duration, then chains to another role expecting the same. The chained session is silently capped at 1 hour, causing unexplained credential expiration mid-workflow. Always account for the 1-hour cap in any architecture involving role chaining and use mechanisms like re-assuming roles or application-level token refresh to handle expiration.' },
  ],
  keyQuestions: [
    { q: 'How do SCPs differ from permission boundaries, and when do you use each?', a: 'SCPs are AWS Organizations features that apply to entire accounts or OUs, restricting the maximum permissions for every principal in those accounts — they are organizational guardrails you control at the infrastructure level. Permission boundaries restrict an individual IAM user or role and are used primarily for delegation — preventing developers who have iam:CreateRole from creating roles more powerful than themselves. You use SCPs to enforce company-wide or account-wide security policies (no public S3, no leaving the org, region restrictions) and permission boundaries to enable safe self-service IAM administration within those guardrails.' },
    { q: 'How does ABAC compare to RBAC in AWS, and what are the tradeoffs?', a: 'Traditional RBAC in AWS means creating explicit policies listing specific resource ARNs for each role, requiring policy updates every time a new resource is provisioned — this leads to policy sprawl and stale permissions. ABAC uses principal and resource tags so a single policy covers all correctly tagged resources automatically as they are created. The tradeoff is that ABAC requires disciplined, enforced tagging and controls on who can modify tags, since a tag change can inadvertently grant or revoke access. ABAC scales much better in large environments but requires upfront investment in tagging governance. Use RBAC for small, stable resource sets and ABAC for large, dynamic environments.' },
    { q: 'How do you prevent the confused deputy problem in AWS?', a: 'The confused deputy problem occurs when a third party tricks a service into using its trust relationship to perform unauthorized actions. In AWS, the mitigation is the ExternalId condition in IAM role trust policies for third-party access. You generate a unique, unguessable string for each third-party relationship and add it as a StringEquals condition on sts:ExternalId in the trust policy. The third party must provide this value in every AssumeRole call. Additionally, use aws:PrincipalOrgID conditions to restrict role assumption to principals within your organization, and use aws:SourceArn and aws:SourceAccount conditions in trust policies for AWS service roles (like S3 replication or CloudTrail) to prevent service-to-service confused deputy scenarios.' },
    { q: 'How do you implement OIDC-based IAM roles for Kubernetes service accounts (IRSA)?', a: 'IRSA (IAM Roles for Service Accounts) eliminates the need for node-level IAM roles on EKS. You create an OIDC provider in IAM pointing to your EKS cluster OIDC endpoint, then create IAM roles with trust policies that allow AssumeRoleWithWebIdentity from the OIDC provider, scoped to specific Kubernetes service account names using StringEquals conditions on the OIDC subject claim. You annotate the Kubernetes service account with the IAM role ARN, and the EKS pod identity webhook automatically mounts a projected service account token that the AWS SDK exchanges for temporary credentials via STS. Each pod gets credentials for only its specific role, not broad node-level permissions, dramatically reducing blast radius from pod compromise.' },
  ],
  references: [
    'https://docs.aws.amazon.com/organizations/latest/userguide/orgs_manage_policies_scps.html',
    'https://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies_boundaries.html',
    'https://docs.aws.amazon.com/IAM/latest/UserGuide/tutorial_attribute-based-access-control.html',
    'https://docs.aws.amazon.com/eks/latest/userguide/iam-roles-for-service-accounts.html',
  ],
},
{
  id: 'aws-cognito',
  title: 'Cognito — User Pools and Identity Pools',
  icon: 'key',
  color: '#ef4444',
  description: 'Managed user authentication, social federation, and AWS credential vending for web and mobile apps.',
  introduction: 'Amazon Cognito provides two distinct but complementary services for application identity. User Pools are a full-featured user directory and authentication system — sign-up, sign-in, MFA, password policies, social federation, and JWT token issuance. Identity Pools (Federated Identities) exchange identity tokens (from User Pools, social providers, SAML, or OIDC) for temporary AWS credentials via STS, allowing authenticated or even unauthenticated users to directly call AWS services. Together they form a complete customer identity and access management solution for web and mobile applications.',
  whenToUse: 'Use Cognito User Pools whenever you need user registration, login, and profile management without building your own auth system. Use them when you need to protect API Gateway endpoints with JWT-based authorization. Use Identity Pools when your mobile or web app needs to call AWS services (S3, DynamoDB, Rekognition) directly from the client — the app exchanges a User Pool JWT or social token for temporary, scoped AWS credentials. This avoids embedding AWS credentials in the app. Use both together for the most complete solution.',
  keyConcepts: [
    { title: 'User Pools vs Identity Pools', description: 'User Pools are user directories that handle authentication — they store user profiles, handle sign-up and sign-in flows, issue JWTs (id token, access token, refresh token), and support MFA and social federation. Identity Pools do not authenticate users — they authorize them for AWS services. They accept identity tokens from various providers and exchange them for temporary AWS credentials via AssumeRoleWithWebIdentity. A common architecture chains them: User Pool authenticates, Identity Pool exchanges the User Pool JWT for AWS credentials.' },
    { title: 'JWT Tokens from User Pools', description: 'Cognito User Pools issue three tokens on successful authentication. The ID token is a JWT containing user identity claims (email, sub, custom attributes) — use this to identify the user in your backend. The access token is a JWT for calling Cognito APIs (get/update user attributes, global sign-out) — do not use it as identity in your backend APIs. The refresh token is a long-lived opaque token used to obtain new ID and access tokens without re-authenticating. ID and access tokens expire in 1 hour by default; refresh tokens expire in 30 days by default, both configurable.' },
    { title: 'Lambda Triggers for Custom Auth', description: 'Cognito User Pools support Lambda triggers at multiple points in the auth lifecycle. Pre-signup validates or transforms registration data. Pre-authentication blocks sign-in attempts based on custom logic. Post-authentication fires after successful login for logging or analytics. Pre-token generation lets you add or suppress claims in the JWT before it is issued — use this to inject group memberships or custom permissions. Define auth challenge and verify auth challenge create fully custom authentication flows (CAPTCHA, device fingerprinting, step-up auth).' },
    { title: 'Social and Enterprise Federation', description: 'User Pools support social identity providers (Google, Facebook, Amazon, Apple) as external IdPs, federating login through Cognito so your application always receives consistent Cognito JWTs regardless of which provider the user chose. For enterprise SSO, User Pools support SAML 2.0 and OIDC providers (Okta, Azure AD, Ping), enabling employees to sign in with their corporate credentials. In all cases, Cognito maps external claims to User Pool attributes, providing a unified user profile.' },
    { title: 'Cognito Groups and Role Mapping', description: 'User Pool groups allow you to organize users and optionally map them to IAM roles via Identity Pools. When a user is in a group, the group name appears in the cognito:groups claim of their JWT. In Identity Pools, you configure role mapping rules based on cognito:groups or other token claims to issue different IAM roles — admin group members get a role with more AWS permissions than regular group members. This enables fine-grained AWS resource access based on application-level group membership.' },
    { title: 'Hosted UI and Custom Domains', description: 'Cognito provides a hosted UI — a pre-built, customizable sign-in and sign-up web UI served from Cognito infrastructure. You can configure a custom domain using your own domain with ACM certificates, making the auth flow appear seamless to users. The hosted UI handles the full OAuth 2.0 authorization code flow and supports callback URLs, logout URLs, and OAuth scopes. Use it to avoid building auth UI from scratch while maintaining the ability to customize branding, CSS, and Lambda-driven behavior.' },
  ],
  approach: [
    { step: 'Design User Pool configuration for security', details: 'Enable multi-factor authentication at minimum as optional, mandatory for admin users. Configure a strong password policy (minimum 12 characters, require uppercase, lowercase, numbers, and symbols). Enable advanced security features (adaptive authentication) to detect compromised credentials and suspicious sign-in patterns using Cognito threat intelligence. Set appropriate token expiration — shorter for high-security apps, longer for improved UX in low-risk apps.' },
    { step: 'Secure API Gateway with Cognito authorizer', details: 'Create a Cognito User Pools authorizer in API Gateway, pointing to your User Pool ARN and specifying which token claim to validate (typically Authorization header with the ID or access token). API Gateway validates the JWT signature against Cognito JWKS endpoint, checks expiration, and optionally validates token scopes. No Lambda function is required for this validation. For HTTP API, use the JWT authorizer type. Pass the sub claim to your backend to identify the authenticated user.' },
    { step: 'Implement token refresh logic in the client', details: 'Build refresh logic into your SDK or API client layer so it is transparent to application code. When an API call returns 401, the client should attempt to use the refresh token to obtain new ID and access tokens via InitiateAuth with REFRESH_TOKEN_AUTH flow. If the refresh token is also expired (default 30 days), redirect to login. Store tokens securely — for web apps use httpOnly cookies or secure in-memory storage, never localStorage, to protect against XSS.' },
    { step: 'Customize tokens with pre-token generation trigger', details: 'Deploy a Lambda function as the pre-token generation trigger to enrich JWT claims with application-specific data — database-driven roles, feature flags, tenant IDs. The function receives the token claims and can add custom claims to the ID token. This eliminates the need for a separate profile API call on every request since all needed authorization context is in the token. Be careful not to add sensitive data that should not be visible to the client, since JWTs are base64-encoded but not encrypted.' },
    { step: 'Configure Identity Pools for direct AWS access', details: 'Create an Identity Pool linked to your User Pool as an authentication provider. Define authenticated and unauthenticated IAM roles. For authenticated users, scope the IAM role to only the specific S3 prefixes, DynamoDB items, or other resources that user should access — use policy variables like cognito-identity.amazonaws.com:sub to scope to per-user resources. For unauthenticated access (guest users), grant minimal read-only permissions only if your use case requires it, and disable unauthenticated access entirely if not needed.' },
  ],
  pitfalls: [
    { title: 'Confusing ID token with access token', description: 'The Cognito access token is for calling Cognito API operations, not for authorizing access to your own application APIs. Your backend should validate the ID token to authenticate users. Using the access token as the bearer token for your custom API is a common mistake — it lacks the user identity claims your backend needs, and its intended audience is Cognito, not your API.' },
    { title: 'Storing tokens in localStorage', description: 'Storing Cognito JWTs in browser localStorage exposes them to XSS attacks — any injected JavaScript can read and exfiltrate the tokens. Use httpOnly cookies for token storage where possible, or keep tokens in memory and use short expiration combined with background refresh. For SPAs, the Auth Code + PKCE flow with httpOnly cookie storage is the current best practice.' },
    { title: 'Over-broad Identity Pool IAM roles', description: 'A common mistake is giving all authenticated Cognito users the same IAM role with broad permissions. Instead, use role-based mapping tied to User Pool groups, and use IAM policy variables (cognito-identity.amazonaws.com:sub) to scope individual users to their own resources in S3 (user-specific prefixes) or DynamoDB (partition key matching their user ID). This enforces data isolation at the AWS policy level.' },
    { title: 'Not validating token audience and issuer', description: 'When manually validating Cognito JWTs in your backend (outside of API Gateway), you must verify the token signature against the Cognito JWKS endpoint, confirm the iss claim matches your User Pool URL, confirm the aud claim matches your app client ID, and check the exp claim. Missing any of these validations creates security vulnerabilities — accepting tokens from other Cognito pools or expired tokens.' },
  ],
  keyQuestions: [
    { q: 'What is the difference between User Pools and Identity Pools, and when do you use each?', a: 'User Pools are an authentication service — they verify who a user is and issue JWTs with identity and access claims. They store user profiles, handle password flows, MFA, and social federation. Identity Pools are an authorization service for AWS resources — they do not store users or authenticate them, they simply exchange a valid identity token from any supported provider for temporary AWS IAM credentials. Use User Pools alone when your app just needs to protect its own API with JWT auth. Use Identity Pools alone when you have users from an external IdP who need direct AWS resource access. Use both together for the complete pattern: User Pool authenticates and issues JWT, Identity Pool exchanges that JWT for AWS credentials so the app can directly call S3 or DynamoDB.' },
    { q: 'How do you secure API Gateway endpoints with Cognito, and what are the limitations?', a: 'For REST API, create a Cognito User Pools authorizer in API Gateway specifying your User Pool ARN and the identity source (Authorization header). API Gateway automatically validates the JWT signature, expiration, and optionally the token scopes — no Lambda required, which means low latency. For HTTP API, use the JWT authorizer with the Cognito issuer URL and audience. The limitation is that Cognito authorizers in API Gateway only work with User Pool tokens, not Identity Pool credentials. They also do not perform fine-grained authorization beyond token validity and scope — for resource-level authorization you still need application logic or a Lambda authorizer that calls your authorization service.' },
    { q: 'How do Lambda triggers enable custom authentication flows in Cognito?', a: 'Cognito supports a custom authentication challenge flow via three triggers: Define Auth Challenge, Create Auth Challenge, and Verify Auth Challenge Response. Define Auth Challenge runs after each step and decides what the next challenge should be or whether authentication is complete. Create Auth Challenge generates and delivers the challenge (for example, sending an OTP via SNS). Verify Auth Challenge Response validates the user response. Together these let you implement passwordless auth, step-up auth for sensitive operations, CAPTCHA verification, or any other custom challenge without building your own auth server. The Pre-Token Generation trigger runs after successful authentication and lets you modify the JWT claims before they are issued, enabling dynamic role injection based on database state.' },
    { q: 'How do you handle token refresh and session management in a production app?', a: 'Cognito ID and access tokens expire in 1 hour by default (configurable 5 minutes to 24 hours). Your application should silently refresh them using the refresh token before expiration. Implement an Axios interceptor or equivalent that catches 401 responses, calls Cognito InitiateAuth with REFRESH_TOKEN_AUTH, updates stored tokens, and retries the original request transparently. The refresh token itself expires (default 30 days, configurable 1 to 3650 days) — when it expires, the user must re-authenticate. For high-security applications, use shorter refresh token expiration and require re-auth. For logout, call Cognito GlobalSignOut to revoke all tokens for the user and clear local storage, then redirect to the Cognito hosted UI logout endpoint to clear the Cognito session cookie.' },
  ],
  references: [
    'https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-user-identity-pools.html',
    'https://docs.aws.amazon.com/cognito/latest/developerguide/cognito-identity.html',
    'https://docs.aws.amazon.com/cognito/latest/developerguide/user-pool-lambda-triggers.html',
  ],
},
{
  id: 'aws-kms',
  title: 'KMS — Key Management Service',
  icon: 'lock',
  color: '#ef4444',
  description: 'Managed encryption key lifecycle, envelope encryption, and integration with AWS services.',
  introduction: 'AWS Key Management Service is the central encryption key management system for AWS. It generates, stores, and controls cryptographic keys backed by FIPS 140-2 validated hardware security modules. Virtually every AWS service that offers encryption at rest uses KMS under the hood. KMS handles key rotation, audit logging via CloudTrail, and fine-grained access control through key policies and IAM policies. Understanding KMS — especially how key policies interact with IAM, how envelope encryption works, and when to use different key types — is essential for designing secure AWS architectures.',
  whenToUse: 'Use KMS whenever you need encryption at rest for AWS resources — S3 objects, EBS volumes, RDS databases, DynamoDB tables, Secrets Manager secrets, Lambda environment variables, CloudWatch Logs. Use customer-managed KMS keys (CMKs) when you need control over key rotation, key policies, and the ability to disable or delete keys — audit requirements often mandate CMKs. Use AWS-managed keys for simplicity when you only need encryption without access control requirements. Use CloudHSM when you need exclusive single-tenant HSM access or FIPS 140-2 Level 3 validation.',
  keyConcepts: [
    { title: 'Key Types: CMK vs AWS-Managed vs AWS-Owned', description: 'Customer-managed keys (CMKs) are keys you create and control. You define the key policy, can enable/disable the key, control rotation, add grants, and view all usage in CloudTrail. AWS-managed keys are created and managed by AWS on your behalf when a service encrypts data (e.g., aws/s3, aws/rds). You cannot manage their policies or rotation schedule but can see their usage in CloudTrail. AWS-owned keys are completely opaque — AWS owns and manages them across multiple accounts with no customer visibility into usage. For compliance and auditing, always use CMKs.' },
    { title: 'Envelope Encryption', description: 'KMS uses envelope encryption for data encryption. Rather than sending your data to KMS for direct encryption (which has a 4KB limit and latency overhead), you call KMS GenerateDataKey, which returns both a plaintext data key and a KMS-encrypted copy of that data key. Your application uses the plaintext data key locally (in memory) to encrypt the data with AES-256, then discards the plaintext key. You store the encrypted data key alongside the ciphertext. To decrypt, you call KMS Decrypt with the encrypted data key to recover the plaintext data key, then decrypt the ciphertext locally. This approach means KMS only handles small keys, not bulk data.' },
    { title: 'Key Policies vs IAM Policies', description: 'KMS key access is governed by key policies, which are resource-based policies attached to the key. Unlike most AWS resources, a KMS key policy must explicitly allow the account root access — otherwise no IAM policy in the account can grant access to the key. The default key policy grants the account root full access, enabling IAM policies to then delegate access. A key policy statement granting kms:* to the account root is what makes IAM policies effective. If you create a restrictive key policy that does not include the root, you may lock yourself out and need to contact AWS Support.' },
    { title: 'Key Rotation', description: 'For CMKs with AWS-managed key material, you can enable automatic annual key rotation. KMS rotates the backing cryptographic material but keeps the same key ID and ARN — existing ciphertext decrypts transparently because KMS retains all previous versions of the key material. Manual rotation (creating a new CMK and updating all references) is required for asymmetric CMKs, CMKs with imported key material, and CMKs in custom key stores. For compliance, log rotation events via CloudTrail and set up Config rules to detect keys without rotation enabled.' },
    { title: 'Grants and ViaService Conditions', description: 'KMS grants are a programmatic way to delegate key usage permissions to another AWS principal without modifying the key policy. Grants are used by AWS services (EBS, RDS, Redshift) when they need to use a CMK on your behalf — the service creates a grant allowing it to call Decrypt or GenerateDataKey for specific operations. The aws:ViaService condition in key policies restricts key usage to specific AWS services — for example, requiring that kms:Decrypt can only be called when the request originates from s3.amazonaws.com, preventing direct API calls to decrypt data outside of S3.' },
    { title: 'Multi-Region Keys', description: 'KMS Multi-Region Keys are sets of interoperable KMS keys in different regions that share the same key material and key ID suffix but have region-specific ARNs. Data encrypted with the primary key can be decrypted with the replica key in another region, enabling disaster recovery, multi-region active-active applications, and global client-side encryption. You must explicitly replicate keys to additional regions — they are not automatically available globally. Multi-region keys are subject to the same key policy and grant requirements in each region independently.' },
  ],
  approach: [
    { step: 'Define key policy with minimum required access', details: 'Start with the default key policy that grants account root access (enabling IAM policy delegation), then add specific key administrators (who can manage the key but not use it for cryptographic operations) and key users (who can encrypt and decrypt but not manage). Use separate statements for administrators and users. Add a statement granting AWS services (via kms:ViaService condition) the specific operations they need. Never grant kms:* to non-root principals.' },
    { step: 'Implement envelope encryption in application code', details: 'Use the AWS Encryption SDK or AWS SDK GenerateDataKey API to implement envelope encryption. For each piece of sensitive data, call GenerateDataKey with your CMK ARN to get a plaintext and encrypted data key. Encrypt data locally with the plaintext key using AES-256-GCM, discard the plaintext key, and store the encrypted data key as metadata alongside or in the header of the ciphertext. On decryption, read the encrypted data key, call KMS Decrypt to recover the plaintext key, decrypt the ciphertext, then immediately discard the plaintext key again.' },
    { step: 'Enable CloudTrail logging and set up alerts', details: 'All KMS API calls are logged to CloudTrail by default — there is no configuration required, but ensure CloudTrail is enabled in every region where you use KMS. Create CloudWatch metric filters and alarms for KMS key deletion events (ScheduleKeyDeletion), key disabling events, and unexpected high-volume Decrypt or GenerateDataKey calls that might indicate data exfiltration. Set a CloudWatch alarm on any use of deprecated key aliases or keys scheduled for deletion.' },
    { step: 'Separate keys by classification and environment', details: 'Use separate CMKs for different data classifications (PII, financial, health) and different environments (dev, staging, prod). This enables fine-grained access control, independent key rotation, and clear audit trails per data type. Use key aliases (aws/myapp/prod/pii) for readability and to allow application configuration to reference alias ARNs rather than key ID ARNs, making key rotation and replacement transparent to applications.' },
    { step: 'Plan for key deletion carefully', details: 'KMS requires a waiting period of 7 to 30 days before permanently deleting a CMK. During this period, you can cancel the deletion. Before scheduling deletion, use CloudTrail to audit that no services are still using the key for Decrypt operations. Enable KMS key deletion alerts in CloudWatch. If you delete a key that was used to encrypt data and no backup of the plaintext data exists, that data is permanently unrecoverable — there is no way to reverse KMS key deletion.' },
  ],
  pitfalls: [
    { title: 'Locking yourself out of a key', description: 'If you create a key policy that does not include an Allow statement for the account root (arn:aws:iam::ACCOUNT:root), no IAM policy can grant access to the key. Even the account root cannot use the key through normal means — you must contact AWS Support to restore access. Always preserve the root account delegation statement in key policies, and test key policy changes in a non-production key first.' },
    { title: 'Confusing key policy and IAM policy precedence', description: 'For KMS, both the key policy and IAM policy must allow the action for access to be granted (unlike most AWS resources where either alone suffices for same-account access). A principal with kms:Decrypt in their IAM policy cannot use the key if the key policy does not allow them — unless the key policy has the root delegation statement. This behavior surprises many engineers who expect IAM policies to be sufficient for same-account resource access.' },
    { title: 'High-frequency KMS API calls causing throttling', description: 'KMS has service quotas (5,000 cryptographic operations per second per region by default). Applications that call KMS Decrypt on every request, rather than caching decrypted values, will hit throttling under load and receive ThrottlingException errors. Use the AWS Encryption SDK data key caching feature to reuse data keys for multiple encrypt operations within a configurable TTL and message count, dramatically reducing KMS API calls.' },
    { title: 'Importing key material without proper backup', description: 'When you import your own key material into KMS, you are responsible for backing up that key material securely outside of AWS. If you delete the key material from KMS (or the key itself), AWS cannot recover it — all data encrypted with that key becomes permanently unrecoverable. Maintain a secure offline backup of imported key material in your own HSM or secure vault.' },
  ],
  keyQuestions: [
    { q: 'Explain envelope encryption and why KMS uses it.', a: 'Envelope encryption solves two problems: the KMS API has a 4KB limit on the data it can encrypt directly, and sending bulk data to a remote service for encryption adds unacceptable latency and cost. Instead, KMS generates a data key — a random symmetric AES-256 key — and returns both the plaintext and a KMS-encrypted version. Your application encrypts the actual data locally using the plaintext data key (which is fast and has no size limits), then discards the plaintext key. You store the KMS-encrypted data key alongside the ciphertext. To decrypt, you only contact KMS to decrypt the small data key, then do bulk decryption locally. This means KMS only ever handles small keys, not your data, and your data never leaves your infrastructure unencrypted.' },
    { q: 'How does KMS key policy interact with IAM policies for access control?', a: 'KMS uses a two-factor access model for same-account principals: both the key policy and the IAM policy must allow the action. This differs from most AWS services where either a resource policy or an identity policy alone is sufficient. However, the default key policy includes a statement granting the account root principal (arn:aws:iam::ACCOUNT:root) full KMS access — this acts as a delegation enabler, meaning IAM policies alone can grant access when this root statement is present. For cross-account access, the key policy must explicitly list the external account or principal, and the external principal must also have an IAM policy allowing the KMS action. Key administrators (who can manage keys) and key users (who can call crypto operations) are typically separate principals in the key policy.' },
    { q: 'What is the difference between KMS and CloudHSM, and when do you choose CloudHSM?', a: 'KMS is a multi-tenant managed service where AWS manages the HSMs and you manage the keys. KMS provides FIPS 140-2 Level 2 validation (Level 3 for the hardware component). CloudHSM is a single-tenant HSM cluster where you have exclusive control of the hardware — AWS cannot access your keys, and you manage all HSM administration. Choose CloudHSM when regulatory requirements mandate FIPS 140-2 Level 3 validated, exclusive-control HSMs (common in financial services and government), when you need to perform non-AWS cryptographic operations using standards like PKCS11 or Java JCE directly on the HSM, or when you need to store key material that AWS-managed KMS cannot import. KMS can use CloudHSM as a custom key store, combining KMS API convenience with CloudHSM key storage exclusivity.' },
    { q: 'How do you handle KMS key rotation in a production system without downtime?', a: 'Enabling automatic KMS key rotation is non-disruptive — KMS rotates the backing cryptographic material annually but keeps the same key ID and ARN. All existing ciphertext remains decryptable because KMS retains all previous versions of the key material indefinitely. Applications reference the key by ARN or alias and are completely unaware of rotation. For manual key rotation (asymmetric keys, imported material), the process requires creating a new CMK, updating application configuration to use the new key ARN or alias, then re-encrypting sensitive data with the new key over time. Use key aliases for all application references so you can switch from the old key ARN to the new one by updating the alias target rather than changing application configuration. Continue to keep the old key active until all data has been re-encrypted, then schedule deletion with the maximum 30-day waiting period as a safety buffer.' },
  ],
  references: [
    'https://docs.aws.amazon.com/kms/latest/developerguide/overview.html',
    'https://docs.aws.amazon.com/kms/latest/developerguide/key-policies.html',
    'https://docs.aws.amazon.com/kms/latest/developerguide/concepts.html',
    'https://docs.aws.amazon.com/encryption-sdk/latest/developer-guide/introduction.html',
  ],
},
{
  id: 'aws-secrets-manager',
  title: 'Secrets Manager — Centralized Secret Storage',
  icon: 'key',
  color: '#ef4444',
  description: 'Managed secret storage, automatic rotation, and cross-account secret sharing for credentials and API keys.',
  introduction: 'AWS Secrets Manager solves the critical problem of how to store and distribute secrets — database passwords, API keys, OAuth tokens, encryption keys — in a way that is secure, auditable, and operationally maintainable. Rather than hardcoding secrets in code, configuration files, or environment variables, applications retrieve secrets programmatically from Secrets Manager at runtime. The defining feature is automatic rotation: Secrets Manager can automatically rotate credentials on a schedule using Lambda functions, updating both the secret in the vault and the actual credential at the service (e.g., RDS database password), with zero application downtime through a versioning system.',
  whenToUse: 'Use Secrets Manager for any secret that requires automatic rotation, cross-account sharing, or fine-grained resource-based access control — primarily database credentials, API keys, and OAuth client secrets. Use it for RDS database passwords specifically because it has native integration for automatic rotation without a custom Lambda function. Use SSM Parameter Store SecureString (which is cheaper, free for standard parameters) for configuration values that do not require rotation, such as feature flags, non-sensitive configuration, or ARNs that simply need secure storage without the rotation machinery.',
  keyConcepts: [
    { title: 'Secret Versioning: AWSCURRENT, AWSPENDING, AWSPREVIOUS', description: 'Secrets Manager uses a staging label system to support zero-downtime rotation. A secret can have multiple versions simultaneously. AWSCURRENT is the version applications actively retrieve. During rotation, the new credentials are stored under AWSPENDING while the old credentials under AWSCURRENT remain valid. Once the rotation Lambda verifies the new credentials work, it moves AWSPENDING to AWSCURRENT and the old version to AWSPREVIOUS. AWSPREVIOUS remains valid briefly to allow in-flight connections using old credentials to complete. Applications always retrieve AWSCURRENT by default.' },
    { title: 'Lambda Rotation Functions', description: 'Automatic rotation is implemented by a Lambda function following a four-step protocol Secrets Manager calls sequentially: createSecret (generate new credentials, store under AWSPENDING), setSecret (apply the new credentials at the service — change the database password, rotate the API key), testSecret (verify the new credentials under AWSPENDING actually work), and finishSecret (promote AWSPENDING to AWSCURRENT). AWS provides managed rotation functions for RDS, Aurora, Redshift, DocumentDB, and other services. For custom secrets, you implement the four-step Lambda yourself.' },
    { title: 'Comparison with SSM Parameter Store', description: 'Secrets Manager is purpose-built for secrets requiring rotation, auditing, and cross-account sharing — it costs approximately 0.40 USD per secret per month. SSM Parameter Store SecureString uses KMS encryption and is free for standard parameters (up to 4KB, 10,000 parameters) and low-cost for advanced. Use Parameter Store for non-rotating config values, environment variables, and non-secret parameters. Use Secrets Manager when you need automatic rotation, resource-based policies for cross-account access, or fine-grained secret-level access control. Both integrate natively with Lambda, ECS, EKS, and EC2 for automatic secret injection.' },
    { title: 'Resource-Based Policies for Cross-Account Access', description: 'Secrets Manager supports resource-based policies attached to individual secrets. This enables cross-account secret sharing without creating IAM roles in the secret-owning account. The resource policy grants the external account or specific role the secretsmanager:GetSecretValue permission. The calling account still needs an IAM policy allowing the call. Combine aws:PrincipalOrgID condition to restrict sharing to accounts within your AWS Organization, preventing accidental exposure to external accounts.' },
    { title: 'Secret Replication Across Regions', description: 'Secrets Manager supports replicating secrets to multiple regions for disaster recovery and multi-region active-active architectures. The primary secret in the home region is replicated to replica regions, where applications can read the current value. Rotation in the primary region does not automatically rotate replica secrets — you replicate the value only. For active-active with independent rotation per region, manage separate primary secrets per region instead. Replication adds per-region storage costs.' },
    { title: 'Native AWS Service Integration', description: 'Secrets Manager integrates natively with RDS, Aurora, Redshift, and DocumentDB for single-click rotation setup and automatic Lambda rotation function deployment. ECS supports injecting Secrets Manager secret values as environment variables or in bind mount files via task definition valueFrom references. EKS with the Secrets Store CSI Driver mounts secrets as volumes in pods. Lambda natively supports reading secrets via the Extensions API, which caches values locally to avoid per-invocation API calls.' },
  ],
  approach: [
    { step: 'Migrate hardcoded credentials to Secrets Manager', details: 'Audit codebase and configuration for hardcoded credentials using tools like git-secrets, truffleHog, or AWS Macie. For each identified credential, create a Secrets Manager secret with a descriptive name following a naming convention (prod/myapp/db-password). Update application code to call GetSecretValue at startup or on cache miss. Use the AWS SDK integrated cache (for Lambda and ECS) to avoid per-call API overhead. Rotate the actual credential after migration to invalidate any copies that may have been leaked.' },
    { step: 'Configure automatic rotation for database passwords', details: 'For RDS and Aurora, enable rotation directly from the Secrets Manager console — AWS deploys the rotation Lambda and configures the schedule automatically. Choose a rotation window that aligns with low-traffic periods. Set the rotation interval based on your security policy (30, 60, or 90 days). Enable rotation immediately to verify the rotation Lambda works correctly in your VPC configuration. Ensure the rotation Lambda has network access to both Secrets Manager (via endpoint or NAT) and the database.' },
    { step: 'Implement least-privilege access to secrets', details: 'Create granular IAM policies that allow access to specific secret ARNs rather than secretsmanager:* on *. Use naming conventions with IAM policy conditions using secretsmanager:SecretId to allow developers access to dev/* secrets but not prod/*. Use resource-based policies on secrets for cross-account access. Tag secrets with environment and application tags and use ABAC conditions to drive access policies. Enable CloudTrail logging and create alarms for unexpected GetSecretValue calls on production secrets.' },
    { step: 'Handle rotation in application connection pooling', details: 'Connection pools hold database connections that use the old password. When rotation changes the database password, existing pool connections continue working (database keeps old password valid for a brief window). New connections after rotation will use the new password from AWSCURRENT. Configure your application to catch authentication errors from the pool and trigger a secret refresh and pool reconnection. For RDS Proxy, proxy handles credential rotation transparently — the proxy retrieves the new password and reconnects without impacting application connections.' },
    { step: 'Set up monitoring and compliance checks', details: 'Use AWS Config rule secretsmanager-rotation-enabled-check to detect secrets without rotation configured. Create CloudWatch alarms on the SecretsManager secretsRotatedSuccessfully metric to alert on rotation failures. Use AWS Security Hub to aggregate Secrets Manager findings. Periodically audit resource-based policies on all secrets using IAM Access Analyzer. Set up CloudTrail metric filters for DeleteSecret and PutResourcePolicy operations to detect unauthorized secret management activity.' },
  ],
  pitfalls: [
    { title: 'Rotation Lambda without VPC access to database', description: 'The rotation Lambda must be able to reach both Secrets Manager (to read AWSPENDING and promote versions) and the database (to apply the new password). If the database is in a private subnet, the Lambda must be in the same VPC with security group rules allowing it to reach the database port. Without this, the setSecret step silently fails or the Lambda times out, and rotation fails, potentially leaving AWSPENDING credentials in an inconsistent state.' },
    { title: 'Caching secrets without TTL refresh', description: 'Applications that cache the secret value at startup and never refresh it will break after rotation — they still try to connect with the now-invalid AWSPREVIOUS credentials. Use the AWS Secrets Manager caching library (available for Python, Java, and JavaScript) which automatically refreshes the cache after a configurable TTL and on authentication errors. For Lambda, the Secrets Manager Extension handles caching with automatic refresh.' },
    { title: 'Overly broad IAM policies for secret access', description: 'Granting secretsmanager:GetSecretValue on Resource: * allows an application to read any secret in the account, including secrets belonging to other applications. Always scope the Resource to specific secret ARNs or ARN patterns. For multi-environment accounts, use naming conventions and IAM policy conditions to enforce that production applications can only access prod/* secrets and development applications can only access dev/*.' },
    { title: 'Deleting secrets without considering recovery', description: 'Secrets Manager enforces a 7 to 30 day recovery window before permanent deletion by default. However, the secret name becomes unavailable for reuse during this window. If you delete and recreate a secret with the same name (for testing or remediation), you must either wait out the recovery window or use ForceDeleteWithoutRecovery, which is irreversible and bypasses the safety window. Applications that have cached the secret ARN will receive ResourceNotFoundException on the first GetSecretValue call after deletion.' },
  ],
  keyQuestions: [
    { q: 'What is the difference between Secrets Manager and SSM Parameter Store, and how do you choose?', a: 'Secrets Manager is purpose-built for secrets requiring lifecycle management — automatic rotation, versioning with staging labels, and resource-based policies for cross-account sharing. It costs approximately 0.40 USD per secret per month plus API call charges. SSM Parameter Store SecureString stores encrypted values using KMS and is free for standard parameters (4KB limit) with no native rotation feature. Choose Secrets Manager for database passwords, API keys that require scheduled rotation, and secrets that must be shared across AWS accounts with fine-grained resource policies. Choose Parameter Store for non-rotating configuration values, environment variables, feature flags, non-secret ARNs, and any situation where the free tier or lower cost is a constraint. Both support Lambda, ECS, and EKS native injection, and both log access to CloudTrail.' },
    { q: 'How does the four-step rotation Lambda protocol work?', a: 'Secrets Manager calls the rotation Lambda up to four times with different step values in the same rotation event. In createSecret, the Lambda generates new credentials and stores them under AWSPENDING staging label while leaving AWSCURRENT untouched — the service still uses the old credentials. In setSecret, the Lambda updates the actual service (changes the database password, rotates the API key at the provider) to accept the new credentials. In testSecret, the Lambda retrieves AWSPENDING credentials and verifies they successfully authenticate to the service — this is the safety net that prevents a failed rotation from locking you out. In finishSecret, the Lambda calls Secrets Manager to promote AWSPENDING to AWSCURRENT and move the old AWSCURRENT to AWSPREVIOUS, making the new credentials live for all applications. If any step fails, the rotation fails and AWSCURRENT remains unchanged.' },
    { q: 'How do you share a secret across AWS accounts securely?', a: 'Attach a resource-based policy to the secret in the owning account that grants secretsmanager:GetSecretValue and secretsmanager:DescribeSecret to the specific IAM role or account in the consuming account. Add a condition using aws:PrincipalOrgID to restrict access to accounts within your AWS Organization, preventing accidental sharing with external entities. The consuming account IAM role must also have an identity-based policy allowing the Secrets Manager calls. The consuming account also needs a KMS key grant or key policy allowing access if the secret uses a CMK rather than the default AWS-managed key — the default aws/secretsmanager key does not support cross-account access, so you must use a CMK with an explicit cross-account key policy.' },
    { q: 'How do you implement secret replication for disaster recovery?', a: 'Enable secret replication from the primary region to one or more replica regions via the Secrets Manager console or API. The replica contains the current value of the primary secret and is read-only — applications in the replica region read from the local replica to avoid cross-region latency and to remain operational if the primary region is unavailable. Rotation in the primary automatically updates the replica value after the rotation Lambda completes. For multi-region active-active workloads that require independent rotation per region, the better pattern is separate primary secrets per region each with their own rotation schedules, rather than replicas, because replicas cannot be rotated independently. Tag all secrets with their replication relationship for operational clarity and set up CloudWatch alarms to detect replication lag or failures.' },
  ],
  references: [
    'https://docs.aws.amazon.com/secretsmanager/latest/userguide/intro.html',
    'https://docs.aws.amazon.com/secretsmanager/latest/userguide/rotating-secrets.html',
    'https://docs.aws.amazon.com/secretsmanager/latest/userguide/auth-and-access.html',
  ],
},
{
  id: 'aws-waf',
  title: 'WAF — Web Application Firewall',
  icon: 'shield',
  color: '#ef4444',
  description: 'Layer 7 firewall protecting web applications from common exploits, bots, and DDoS at the application layer.',
  introduction: 'AWS WAF is a Layer 7 (application layer) firewall that inspects HTTP and HTTPS requests before they reach your application. It protects against OWASP Top 10 vulnerabilities (SQL injection, cross-site scripting, path traversal), bot traffic, credential stuffing, and application-layer DDoS. WAF is deployed at the edge of your application stack — in front of Application Load Balancers, Amazon CloudFront distributions, API Gateway REST APIs, AWS AppSync GraphQL APIs, and Cognito User Pool endpoints. Rules are evaluated in priority order against each incoming request, and WAF allows, blocks, or counts requests based on matching conditions.',
  whenToUse: 'Use WAF whenever your application is internet-facing. At minimum, attach the AWS Managed Core Rule Group (CRS) to your ALB or CloudFront distribution for baseline OWASP protection with no configuration effort. Add rate-based rules to prevent brute force and credential stuffing on login endpoints. Use AWS Bot Control for applications that suffer from scraping, inventory hoarding, or form spam. Use WAF with CloudFront for global distribution of protection at the edge, reducing attack traffic before it enters your VPC. For API Gateway, WAF provides body inspection (API Gateway alone does not inspect request bodies for malicious patterns).',
  keyConcepts: [
    { title: 'Web ACLs and Rule Groups', description: 'A Web ACL (Access Control List) is the top-level WAF resource you attach to one or more AWS resources (ALB, CloudFront, API Gateway). It contains an ordered list of rules and rule groups. Each rule has a priority (lower number = evaluated first), a matching condition, and an action (Allow, Block, Count, CAPTCHA, Challenge). Rule groups are reusable collections of rules — either AWS managed, marketplace-provided, or custom. The Web ACL has a default action applied to requests that do not match any rule.' },
    { title: 'AWS Managed Rule Groups', description: 'AWS provides curated managed rule groups that are updated as new threats emerge, without requiring you to write rules. Core Rule Set (AWSManagedRulesCommonRuleSet) covers OWASP Top 10. Known Bad Inputs (AWSManagedRulesKnownBadInputsRuleSet) blocks common exploit patterns. SQL database rules protect against SQL injection. Linux and POSIX rules protect server-side code. IP Reputation List blocks known malicious IPs using Amazon threat intelligence feeds. Using managed rules gives you immediate coverage and maintenance-free protection, though you may need to set individual rules to Count mode to address false positives in your specific application.' },
    { title: 'Rate-Based Rules', description: 'Rate-based rules aggregate request counts over a 5-minute window and apply an action when a threshold is exceeded. You can rate-limit by IP, by forwarded IP (for requests behind a proxy), or by a custom aggregation key (user ID from a request header, session token). Rate-based rules are essential for protecting login endpoints from credential stuffing, form submissions from spam bots, and APIs from abuse. Set thresholds based on legitimate traffic baseline — too low creates false positives, too high fails to block attacks. Combine with a block or CAPTCHA action.' },
    { title: 'Bot Control and Fraud Control', description: 'AWS WAF Bot Control is a managed rule group that classifies bots into categories: common bots (search engine crawlers, monitoring tools), targeted bots (credential stuffers, scrapers), and verified bots (Googlebot). You can allow verified bots while blocking targeted bots. Fraud Control — Account Takeover Prevention (ATP) — specifically inspects login API requests for credential stuffing patterns, checking stolen credential databases and behavioral signals. Account Creation Fraud Prevention (ACFP) detects bot-driven fake account creation attempts.' },
    { title: 'Geo-Blocking and IP Sets', description: 'WAF supports geographic match conditions to allow or block requests based on the country of origin, determined by IP geolocation. This is useful for compliance requirements (GDPR data residency, US export controls) or simply reducing attack surface by blocking traffic from regions your application does not serve. IP sets are managed lists of CIDR ranges you can reference across multiple rules — use them to maintain allow lists of known trusted IPs (offices, CI/CD systems, partner networks) or block lists of known malicious ranges.' },
    { title: 'WAF Logging and Analytics', description: 'WAF can log all evaluated requests (sampled or full) to Amazon Kinesis Data Firehose for delivery to S3, CloudWatch Logs, or a third-party SIEM. Full logging captures the rule that matched, the action taken, the request headers, URI, body (first 8KB), and source IP. Use logs to tune rules, investigate incidents, build dashboards in Athena or OpenSearch, and generate compliance reports. AWS WAF also provides near-real-time CloudWatch metrics for allowed, blocked, and counted requests per rule, enabling alarming on traffic spikes or sudden block rate increases.' },
  ],
  approach: [
    { step: 'Baseline with managed rule groups in Count mode', details: 'Start by attaching the AWS Managed Core Rule Set with all rules in Count (not Block) mode. Send production traffic through for 1-2 weeks and analyze WAF logs to identify any rules matching legitimate requests (false positives). Common false positives include CMS admin panels, developer tools sending unusual payloads, and API clients with non-standard headers. Move rules causing false positives to Count or add rule exclusions (label match overrides). Once the false positive rate is acceptable, switch the rule group action to Block.' },
    { step: 'Implement rate-based rules for login protection', details: 'Create rate-based rules targeting your login endpoints using URI path match conditions scoped to /login, /auth, /api/token, or equivalent. Set the rate limit based on your traffic analysis — a threshold of 100 requests per 5 minutes per IP is a reasonable starting point for most login endpoints. Add a second, more aggressive rule at 50 requests per 5 minutes that applies CAPTCHA action to challenge suspected bots before outright blocking. Test with synthetic load to verify the threshold does not trigger during legitimate high-traffic periods like morning login spikes.' },
    { step: 'Deploy WAF on CloudFront for global edge protection', details: 'Associate your Web ACL with your CloudFront distribution rather than (or in addition to) your ALB. WAF evaluation on CloudFront happens at AWS edge locations worldwide, blocking malicious traffic before it enters your VPC or origin infrastructure. This reduces origin load from attack traffic and provides sub-50ms blocking latency globally. For origin-direct attack protection (bypassing CloudFront), also attach WAF to the ALB and use ALB security group rules allowing only CloudFront IP ranges (maintained in the aws managed prefix list).' },
    { step: 'Use WAF labels for multi-rule correlation', details: 'WAF rules can add labels to requests as they are evaluated, and subsequent rules can match on those labels. This enables complex multi-signal detection: a first rule labels requests from Tor exit nodes, a second labels requests with SQL injection patterns, and a third blocks only requests carrying both labels — blocking SQL injection from anonymizing proxies with higher confidence than either signal alone. Managed rule groups add labels automatically (e.g., awswaf:managed:aws:core-rule-set:SQLiBody_SQLi_Body) that you can reference in custom rules without duplicating the detection logic.' },
    { step: 'Monitor, alert, and tune continuously', details: 'Create CloudWatch dashboards tracking blocked request rates per rule group, overall allow/block ratios, and geographic distribution of blocks. Set alarms on sudden spikes in blocked requests (possible attack) or sudden drops in block rate (WAF may have been misconfigured or bypassed). Review sampled blocked requests weekly in the WAF console to verify blocking is appropriate. Quarterly, review all Count-mode rules to determine if they should be promoted to Block based on accumulated false positive analysis. Subscribe to AWS Security Bulletins for new managed rule group updates.' },
  ],
  pitfalls: [
    { title: 'Deploying WAF in Block mode without baseline analysis', description: 'Immediately deploying managed rule groups in Block mode on a production application without analyzing existing traffic patterns will inevitably block legitimate users. Custom CMS admin tools, unusual API clients, and internal automation frequently trigger OWASP rules. Always start in Count mode, analyze logs for at least a week, and handle false positives before switching to Block. The WAF console sampling and Athena queries on full logs make this analysis practical.' },
    { title: 'Assuming WAF alone is sufficient DDoS protection', description: 'AWS WAF operates at Layer 7 and is billed per request — a volumetric DDoS attack at Layer 3/4 (SYN flood, UDP flood) or even a high-volume Layer 7 attack can generate costs before WAF blocks traffic. AWS Shield Standard provides automatic Layer 3/4 DDoS protection for free. AWS Shield Advanced provides cost protection (waives overages from DDoS-driven AWS charges), 24/7 DDoS Response Team access, and advanced attack attribution. WAF and Shield are complementary, not alternatives.' },
    { title: 'Not accounting for the 8KB body inspection limit', description: 'WAF inspects only the first 8KB of a request body. A SQL injection or XSS payload in a large JSON body after the first 8KB is not inspected. For APIs with large request bodies, use CONTINUE (inspect what is available) or MATCH (treat oversized content as a threat) handling options. For file uploads, you may need separate scanning (Amazon Macie, third-party AV) since WAF cannot inspect multipart uploads beyond the first 8KB of the overall body.' },
    { title: 'Missing WAF on direct-to-origin attack vectors', description: 'If your architecture has CloudFront in front of an ALB, attackers can bypass CloudFront by directly targeting the ALB endpoint if it is publicly accessible. Always configure the ALB security group to only accept traffic from CloudFront IP ranges (using the managed prefix list com.amazonaws.global.cloudfront.origin-facing) and attach WAF to both CloudFront and the ALB as defense-in-depth. Verify that no DNS records point directly to the ALB origin.' },
  ],
  keyQuestions: [
    { q: 'How does WAF differ from AWS Shield and AWS Network Firewall?', a: 'WAF, Shield, and Network Firewall operate at different network layers and serve different purposes. WAF is a Layer 7 application firewall — it inspects HTTP and HTTPS request content (headers, URI, body, cookies) and makes allow/block decisions based on content patterns. Shield operates at Layers 3 and 4, providing DDoS protection against volumetric attacks (SYN floods, UDP amplification) — Shield Standard is free and automatic for all AWS resources; Shield Advanced adds DRT support, attack attribution, and cost protection. Network Firewall is a Layer 3-7 stateful VPC firewall providing intrusion detection, suricata-compatible rule processing, and traffic filtering for east-west and egress traffic within and between VPCs. WAF is for public-facing web application protection, Shield is for DDoS resilience, and Network Firewall is for comprehensive VPC traffic control.' },
    { q: 'How do you implement effective rate limiting to stop credential stuffing without blocking legitimate users?', a: 'Implement layered rate limiting rather than a single threshold. The first layer applies a permissive rate limit (100 requests per 5 minutes per IP) with a CAPTCHA action — this challenges suspected bots while still allowing them to proceed if they solve the challenge. The second layer applies a strict block at a higher threshold (500 per 5 minutes) for IPs that continue after the CAPTCHA. Scope both rules to the specific login URI path so they do not affect other endpoints. Add a third rule that rate-limits by a custom aggregation key — the username or email extracted from the request body — to catch distributed credential stuffing from many IPs targeting a single account (low-and-slow attacks that evade per-IP limits). Monitor the CAPTCHA success rate: a low success rate indicates sophisticated bots, warranting stricter thresholds.' },
    { q: 'How do you manage WAF false positives in production?', a: 'The key to managing false positives is starting in Count mode and having good logging. Enable full WAF logging to Kinesis Firehose and S3, then query with Athena to identify which rules are matching your legitimate traffic. For managed rule groups, WAF supports rule-level overrides — you can set individual rules within a group to Count mode while keeping the rest in Block, rather than disabling the entire group. For very specific false positives, add label match rules with higher priority that allow requests matching the specific false positive pattern (for example, requests from your admin IP range to the CMS upload endpoint) before the blocking rule fires. Document all false positive exceptions with the business justification, review them quarterly, and remove any that are no longer valid.' },
    { q: 'How does WAF with CloudFront provide better protection than WAF on ALB alone?', a: 'WAF on CloudFront evaluates rules at AWS edge locations distributed globally, meaning attack traffic is dropped before it traverses the AWS network backbone to your region — this reduces latency on blocking, eliminates the cost and capacity impact of attack traffic reaching your origin, and provides uniform protection regardless of which CloudFront PoP the attacker connects through. ALB-only WAF evaluates traffic after it has already entered your VPC region, meaning your network, NAT gateways, and ALB capacity absorb the attack before WAF blocks it. CloudFront also caches responses, so cached content is served without hitting WAF or origin at all. The combination — WAF on CloudFront for edge protection with WAF on ALB as defense-in-depth for direct-to-origin attempts — is the recommended architecture for maximum protection with minimum origin impact.' },
  ],
  references: [
    'https://docs.aws.amazon.com/waf/latest/developerguide/what-is-aws-waf.html',
    'https://docs.aws.amazon.com/waf/latest/developerguide/aws-managed-rule-groups.html',
    'https://docs.aws.amazon.com/waf/latest/developerguide/waf-rule-statement-type-rate-based.html',
    'https://docs.aws.amazon.com/waf/latest/developerguide/logging.html',
  ],
},
{
  id: 'aws-shield',
  title: 'Shield — DDoS Protection',
  icon: 'shield',
  color: '#ef4444',
  description: 'AWS managed DDoS protection service against network and application layer attacks.',
  introduction: 'AWS Shield is a managed Distributed Denial of Service protection service that safeguards AWS applications. It comes in two tiers: Shield Standard, which is automatically included free for all AWS customers, and Shield Advanced, a paid tier offering enhanced protections, near-real-time attack visibility, financial safeguards, and access to the 24x7 AWS DDoS Response Team. Shield integrates tightly with CloudFront, Route 53, Elastic Load Balancing, EC2, and Global Accelerator to defend the full application stack.',
  whenToUse: 'When running public-facing applications on AWS that require protection against volumetric network attacks, protocol attacks, or application layer floods. Shield Advanced is particularly important for high-profile websites, financial services, gaming platforms, and any workload where downtime has a direct financial impact. Use Shield Advanced when you need attack forensics, DRT escalation, and protection against charges incurred during scaling events caused by attacks.',
  keyConcepts: [
    { title: 'Shield Standard', description: 'Automatically included at no cost for all AWS customers. Provides always-on detection and mitigation against common Layer 3 and Layer 4 DDoS attacks such as SYN/UDP floods and reflection attacks. Protection is applied to all AWS resources globally without any configuration required.' },
    { title: 'Shield Advanced', description: 'A paid subscription at approximately 3000 USD per month plus data transfer fees. Provides protection against more sophisticated and larger volumetric attacks across EC2, ELB, CloudFront, Route 53, and Global Accelerator. Includes near-real-time attack notifications, detailed diagnostics, and 24x7 access to the DDoS Response Team.' },
    { title: 'DDoS Response Team (DRT)', description: 'A specialized AWS team available to Shield Advanced subscribers around the clock. The DRT can help write custom WAF rules during an active application layer attack, analyze attack patterns, and recommend architectural improvements. Contact is initiated via the AWS Support console or proactively by granting the DRT role access to your account.' },
    { title: 'Financial Protection', description: 'Shield Advanced subscribers receive service credits if DDoS-related scaling events cause unexpected charges on EC2, ELB, CloudFront, or Route 53. Credits are requested via support after the event, making Shield Advanced a financial backstop as well as a technical one.' },
    { title: 'Layer 7 Protection with WAF', description: 'Shield Standard only covers Layer 3 and Layer 4. Application layer (Layer 7) HTTP floods require AWS WAF deployed on CloudFront or ALB. Shield Advanced includes WAF at no additional cost when protecting resources and allows the DRT to write and deploy WAF rules on your behalf during active attacks.' },
    { title: 'Health-Based Detection', description: 'Shield Advanced can use Route 53 health checks as a signal for attack detection. When a health check transitions to unhealthy during an event, Shield Advanced can trigger faster mitigation and proactively contact the DRT, improving time-to-mitigation for complex multi-vector attacks.' },
  ],
  approach: [
    { step: 'Enable Shield Advanced subscription', details: 'Navigate to the AWS Shield console and subscribe to Shield Advanced. The subscription applies to all resources in the account. For multi-account environments, Shield Advanced can be centrally managed through AWS Organizations and AWS Firewall Manager, allowing consistent protection policies across all member accounts.' },
    { step: 'Protect specific resources', details: 'After subscribing, explicitly add protections for each resource you want covered: EC2 Elastic IPs, ALB or CLB endpoints, CloudFront distributions, Route 53 hosted zones, and Global Accelerator accelerators. Resources must be individually registered to receive advanced protections and appear in attack telemetry.' },
    { step: 'Associate WAF web ACLs for Layer 7', details: 'For CloudFront distributions and ALBs, attach WAF web ACLs to gain application layer protection. Work with the DRT to pre-configure rate-based rules and IP reputation lists. Test rules in Count mode first before switching to Block mode to avoid false positives on legitimate traffic.' },
    { step: 'Configure Route 53 health checks', details: 'Create Route 53 health checks for critical endpoints and associate them with Shield Advanced protections. This enables health-based detection that triggers faster DRT engagement. Ensure health check endpoints are lightweight and not themselves subject to backend bottlenecks that could cause false alarms.' },
    { step: 'Set up CloudWatch alarms and notifications', details: 'Shield Advanced publishes DDoSDetected and DDoSAttackBitsPerSecond metrics to CloudWatch. Create alarms on these metrics and route notifications through SNS to your incident response team. Integrate with EventBridge to trigger Lambda-based automated responses such as activating additional WAF rules or notifying on-call personnel.' },
    { step: 'Engage DRT and review post-attack', details: 'After any significant event, request a post-event review from the DRT. They will provide an attack summary, mitigation timeline, and recommendations. Use this intelligence to refine WAF rules, consider architectural changes like traffic absorption via CloudFront, and update runbooks for future incidents.' },
  ],
  pitfalls: [
    { title: 'Assuming Shield Standard covers application layer attacks', description: 'Shield Standard only mitigates Layer 3 and Layer 4 attacks such as volumetric UDP floods and SYN floods. HTTP floods, Slowloris attacks, and other application layer vectors require WAF with rate-based rules in addition to Shield Advanced. Architects often deploy Shield Standard alone and are surprised when Layer 7 attacks succeed.' },
    { title: 'Not pre-authorizing the DRT before an attack', description: 'DRT assistance requires that you grant the AWSShieldDRTAccessPolicy IAM role to the DRT and optionally share S3 logs in advance. Attempting to grant these permissions during an active high-severity attack while systems are degraded wastes critical minutes. Pre-configure DRT access as part of initial Shield Advanced setup.' },
    { title: 'Overlooking Route 53 as an attack surface', description: 'DNS is frequently targeted in DDoS campaigns to take down all downstream services simultaneously. Route 53 must be explicitly added as a protected resource under Shield Advanced. Without this, attackers can bypass CloudFront and ALB protections entirely by overwhelming the DNS resolution layer.' },
    { title: 'Ignoring Shield Advanced cost structure for high-traffic workloads', description: 'Shield Advanced charges data transfer fees on top of the 3000 USD monthly base. For very high-traffic workloads using CloudFront or ALB, these fees can be substantial. Model costs carefully and compare against the financial risk of downtime before assuming Shield Advanced is always the right choice for every application tier.' },
  ],
  keyQuestions: [
    { q: 'What is the difference between Shield Standard and Shield Advanced?', a: 'Shield Standard is free and automatically protects all AWS resources against common Layer 3 and Layer 4 DDoS attacks such as SYN floods, UDP reflection, and volumetric attacks without any configuration. Shield Advanced is a paid subscription at roughly 3000 USD per month that adds protection against larger and more sophisticated attacks, near-real-time attack visibility and diagnostics, 24x7 access to the DDoS Response Team, financial protection against scaling charges caused by attacks, and WAF included at no additional cost. Shield Advanced must be configured per-resource and supports EC2 Elastic IPs, ELB endpoints, CloudFront distributions, Route 53 hosted zones, and Global Accelerator accelerators. The key decision point is whether the application requires application layer (Layer 7) protection, SLA-backed response times, and forensic attack data.' },
    { q: 'How would you architect a DDoS-resilient application on AWS?', a: 'A DDoS-resilient architecture uses multiple layers of defense. Traffic should enter via CloudFront, which absorbs volumetric attacks at edge locations globally and naturally distributes load away from the origin. Route 53 with latency-based routing and health checks ensures DNS remains available. WAF with rate-based rules and AWS Managed Rule Groups sits in front of CloudFront and ALB to filter application layer attacks. Shield Advanced is subscribed and resources are explicitly protected. Origin servers sit in private subnets behind ALB with no public IPs, so direct-to-origin attacks are impossible. Auto Scaling absorbs residual legitimate traffic spikes. The DRT is pre-authorized and CloudWatch alarms notify the on-call team when attack metrics trigger. This defense-in-depth approach ensures no single layer is the single point of failure.' },
    { q: 'How does Shield Advanced protect Route 53 and why is it important?', a: 'Shield Advanced provides dedicated DDoS protection for Route 53 hosted zones when the zone is explicitly registered as a protected resource. Without this protection, an attacker can flood authoritative DNS servers with query floods, causing DNS resolution to fail for all downstream services including those already protected by CloudFront and WAF. Route 53 is globally distributed across hundreds of edge locations, so it has inherent resilience, but Shield Advanced adds anomaly-based detection and the ability to engage the DRT specifically for DNS-layer attacks. Organizations often protect their compute and CDN resources but forget the DNS layer, creating a critical gap in their DDoS strategy.' },
    { q: 'How does Shield Advanced handle cost management during a DDoS attack?', a: 'Shield Advanced includes a financial protection feature that allows subscribers to request service credits for unexpected AWS charges caused by a DDoS attack triggering Auto Scaling, increased data transfer, or increased load balancer usage. After an attack, you open a support case with the Shield team, provide evidence that the scaling was attack-induced rather than organic growth, and AWS reviews the case for credit eligibility. This protection covers EC2, Elastic Load Balancing, CloudFront, and Route 53 charges. It is important to understand this is a retroactive credit process, not a cap, so large scaling events still generate a bill initially. Proactively setting Service Quotas and budget alerts prevents unbounded scaling even with financial protection in place.' },
  ],
  references: [
    'https://docs.aws.amazon.com/waf/latest/developerguide/shield-chapter.html',
    'https://aws.amazon.com/shield/faqs/',
    'https://docs.aws.amazon.com/waf/latest/developerguide/ddos-responding.html',
  ],
},
{
  id: 'aws-guardduty',
  title: 'GuardDuty — Intelligent Threat Detection',
  icon: 'eye',
  color: '#ef4444',
  description: 'AWS intelligent threat detection service using ML and threat intelligence across CloudTrail, VPC Flow Logs, and DNS logs.',
  introduction: 'Amazon GuardDuty is a managed threat detection service that continuously monitors AWS accounts and workloads for malicious activity and unauthorized behavior. It ingests and analyzes CloudTrail management events, CloudTrail S3 data events, VPC Flow Logs, DNS logs, EKS audit logs, RDS login activity, and Lambda network activity without requiring you to enable or manage those log sources manually. GuardDuty correlates these signals against threat intelligence feeds from AWS and third parties, and uses machine learning anomaly detection to surface findings with severity ratings from informational to high. It is a foundational service in any AWS security posture and integrates natively with Security Hub, EventBridge, and Organizations for multi-account deployments.',
  whenToUse: 'When you need continuous, always-on threat detection across AWS accounts without managing a SIEM or log aggregation pipeline manually. Enable GuardDuty in every account and every region as a baseline security control. It is especially critical in environments with programmatic access, S3 data stores containing sensitive data, container workloads on EKS, and databases accessible from the internet. Use it to detect compromised credentials, crypto mining, data exfiltration, and reconnaissance activities that other perimeter controls may miss.',
  keyConcepts: [
    { title: 'Data Sources and Protection Plans', description: 'GuardDuty core analyzes CloudTrail management events, VPC Flow Logs, and DNS query logs automatically once enabled. Additional protection plans are opt-in: S3 Protection analyzes CloudTrail S3 data events for suspicious bucket activity; EKS Protection monitors Kubernetes audit logs and runtime behavior; Malware Protection scans EBS volumes attached to EC2 or ECS tasks when suspicious activity is detected; RDS Protection identifies anomalous login patterns on Aurora databases; Lambda Protection analyzes network activity from Lambda functions.' },
    { title: 'Finding Types and Categories', description: 'Findings are categorized by threat type: Backdoor (C2 communication), Behavior (unusual API call patterns), Cryptocurrency (mining activity), DefenseEvasion (logging disabled), Discovery (reconnaissance), Exfiltration (data sent to unusual destinations), Impact (resource manipulation), InitialAccess (credential exposure), Pentest (known pentest tool signatures), Persistence (backdoor account creation), Policy (public S3 bucket), PrivilegeEscalation (IAM permission grants), Recon (port scanning), Stealth (CloudTrail disabling), Trojan (DNS-based malware), and UnauthorizedAccess (unusual geographic logins).' },
    { title: 'Severity Levels', description: 'GuardDuty assigns severity scores on a scale of 0.1 to 8.9. Low severity (0.1 to 3.9) findings are informational and often indicate suspicious but not definitively malicious activity. Medium severity (4.0 to 6.9) findings warrant investigation within a reasonable timeframe. High severity (7.0 to 8.9) findings indicate active compromise or imminent threat and require immediate response. Severity scores factor in confidence of the detection, potential impact, and threat intelligence corroboration.' },
    { title: 'Suppression Rules and Trusted IP Lists', description: 'Suppression rules allow you to automatically archive findings that match defined criteria, reducing noise from known-benign activities such as internal vulnerability scanners or authorized penetration tests. Trusted IP lists are CIDR ranges you designate as known-safe, causing GuardDuty to not generate findings for activity originating from those addresses. Threat intelligence lists allow you to add custom known-malicious IPs beyond the default AWS and partner feeds.' },
    { title: 'Multi-Account Deployment with Organizations', description: 'GuardDuty integrates with AWS Organizations to enable centralized management. A delegated administrator account (typically a security tooling account) can auto-enable GuardDuty across all existing and new member accounts, configure protection plans centrally, and aggregate all findings into a single pane. Member accounts cannot disable GuardDuty when it is managed by the organization administrator, ensuring consistent coverage.' },
    { title: 'GuardDuty Malware Protection', description: 'When GuardDuty detects an EC2 or ECS finding that suggests malware activity, Malware Protection can automatically initiate an agentless scan of the attached EBS volumes. A snapshot is taken, copied to a GuardDuty-managed account for scanning using threat signatures, and the snapshot is deleted afterward. Results are returned as a MalwareScanResult finding, and no agents need to be installed on the instance.' },
  ],
  approach: [
    { step: 'Enable GuardDuty in all accounts and regions', details: 'GuardDuty must be enabled per-region, per-account. Use AWS Organizations with a delegated administrator to auto-enable GuardDuty across all accounts and all regions via the Organizations integration. Enable all available protection plans appropriate to your workloads: S3 Protection, EKS Protection, and Malware Protection as a baseline. Regions where you have no workloads should still have GuardDuty enabled to detect unauthorized activity.' },
    { step: 'Configure multi-account aggregation', details: 'Designate a centralized security account as the GuardDuty delegated administrator. All findings from member accounts flow into this account. Use Security Hub in the same account to further aggregate findings alongside those from Inspector, Macie, and Firewall Manager. Create a findings aggregation region in Security Hub to roll up cross-region findings into a single view.' },
    { step: 'Build automated response workflows', details: 'Configure EventBridge rules in the delegated administrator account to capture GuardDuty findings with severity 7.0 or higher. Route these to Lambda functions, Step Functions workflows, or SNS topics. Common automated responses include isolating an EC2 instance by replacing its security group with a quarantine group that blocks all traffic, revoking IAM credentials for a compromised principal, and blocking a source IP in WAF or network ACLs.' },
    { step: 'Establish suppression rules for known-benign activity', details: 'After initial deployment, review findings for a few weeks to identify recurring false positives from authorized activities such as internal scanners, CI/CD pipelines, or admin access from known IP ranges. Create suppression rules to automatically archive these findings, reducing analyst fatigue. Document each suppression rule with a justification and review them quarterly to ensure they remain appropriate.' },
    { step: 'Integrate with Security Hub and SIEM', details: 'Enable the GuardDuty integration with Security Hub so all findings are forwarded in Amazon Security Finding Format. From Security Hub, use EventBridge or a direct integration to forward findings to a SIEM such as Splunk or Sumo Logic for correlation with non-AWS signals. Ensure the SIEM receives both new findings and finding updates, as GuardDuty updates existing findings as attacks evolve rather than creating duplicate entries.' },
    { step: 'Review and act on findings regularly', details: 'Establish a triage cadence: high severity findings trigger immediate pager alerts, medium severity findings are reviewed daily, low severity findings are reviewed weekly. Use the GuardDuty console finding details to pivot into CloudTrail, VPC Flow Logs, and other sources for investigation. Track mean time to respond (MTTR) as a key security metric and use post-incident reviews to improve detection and response playbooks.' },
  ],
  pitfalls: [
    { title: 'Enabling GuardDuty in only some regions', description: 'Attackers frequently pivot to quiet regions where monitoring is absent. Enabling GuardDuty only in your primary regions leaves blind spots in regions where you may have inadvertently created resources, where credential misuse originates, or where attackers establish persistence before moving to monitored regions. Always enable GuardDuty in every region using the Organizations auto-enable feature.' },
    { title: 'Treating all findings as equally urgent', description: 'Without severity-based triage and suppression rules, teams are overwhelmed by low-confidence informational findings and begin to ignore the dashboard. This alert fatigue causes genuine high-severity compromise events to be missed. Invest time early in tuning suppression rules, setting up severity-based alerting thresholds, and establishing clear triage SLAs for each severity level.' },
    { title: 'Not enabling S3 Protection for data-sensitive environments', description: 'GuardDuty core does not analyze S3 data plane events. Anomalous GetObject calls, large-scale data downloads, or public access configuration changes are only detected when S3 Protection is explicitly enabled. Environments with sensitive data in S3 have a critical blind spot without this protection plan, and exfiltration of S3 data can occur without generating any GuardDuty finding.' },
    { title: 'Ignoring GuardDuty finding updates', description: 'GuardDuty updates existing findings as an attack continues or escalates rather than generating new findings for each event. Automated workflows that only trigger on finding creation miss these escalations. Event patterns in EventBridge should match both GuardDuty Finding Type creation and state change events to capture the full lifecycle of an attack.' },
  ],
  keyQuestions: [
    { q: 'How does GuardDuty detect threats without requiring agents or log configuration?', a: 'GuardDuty uses AWS service APIs to pull log data directly from CloudTrail, VPC Flow Logs, and Route 53 DNS resolver logs without requiring you to enable these services or send data to an S3 bucket. AWS grants GuardDuty read-only access to these data sources at the service level, so there is nothing to configure and no storage costs associated with the logs GuardDuty processes. GuardDuty then applies a combination of threat intelligence feeds from AWS Threat Intel feeds and commercial partners like Proofpoint and CrowdStrike, rules-based detection for known-bad patterns, and machine learning models trained on normal baseline behavior per account. The ML models take approximately two weeks to establish a behavioral baseline for a new account, after which anomaly-based findings become more precise.' },
    { q: 'How would you respond to a high-severity GuardDuty finding indicating compromised credentials?', a: 'The immediate priority is containment. For a finding like UnauthorizedAccess:IAMUser/MaliciousIPCaller, the first step is to identify the IAM principal involved from the finding details and immediately attach a deny-all inline policy to revoke all active sessions and prevent new API calls. If the principal is an IAM user, disable the access key; if it is an assumed role, revoke the role session policy. Next, review CloudTrail for the time window around the finding to identify all API calls made by that principal and assess what resources were accessed or modified. Check for persistence mechanisms such as new IAM users, roles, access keys, or EC2 key pairs created by the compromised credential. After containment, remediate any resources that were modified, rotate secrets that may have been exposed, and conduct a root cause analysis to determine how the credential was compromised in the first place.' },
    { q: 'When would you use suppression rules versus trusted IP lists in GuardDuty?', a: 'Trusted IP lists suppress findings based on the source IP address of the activity, making them appropriate when activity from specific known-safe IP ranges should never generate findings regardless of the type of activity. Suppression rules are more flexible and allow you to filter findings based on any combination of finding attributes including finding type, resource type, resource ID, account ID, and severity. Use trusted IP lists for internal security scanners, authorized penetration test IP ranges, and NAT gateway IPs used by internal automation. Use suppression rules for more nuanced cases such as archiving a specific finding type for a specific EC2 instance running a known noisy service, or archiving low-severity policy findings for development accounts while keeping them active in production. Suppression rules are generally preferred because they are more auditable and granular than IP-based suppression.' },
    { q: 'How do you deploy GuardDuty across a multi-account AWS Organizations environment?', a: 'Designate a security tooling account as the GuardDuty delegated administrator through the AWS Organizations management account. From the delegated administrator, enable GuardDuty with auto-enable set to all new member accounts so that any account added to the organization automatically gets GuardDuty enabled. Then run the batch-enable operation to cover all existing member accounts. Configure auto-enable for all protection plans you want applied organization-wide. The delegated administrator receives all findings from all member accounts in a consolidated view, and member accounts cannot disable GuardDuty without the administrator account removing them from the managed configuration. This ensures complete, consistent coverage and prevents shadow IT accounts from operating without threat detection.' },
  ],
  references: [
    'https://docs.aws.amazon.com/guardduty/latest/ug/what-is-guardduty.html',
    'https://docs.aws.amazon.com/guardduty/latest/ug/guardduty_findings.html',
    'https://docs.aws.amazon.com/guardduty/latest/ug/guardduty_organizations.html',
  ],
},
{
  id: 'aws-security-hub',
  title: 'Security Hub — Unified Security Findings',
  icon: 'layers',
  color: '#ef4444',
  description: 'AWS unified security posture management service aggregating findings from AWS services and third-party tools.',
  introduction: 'AWS Security Hub provides a comprehensive view of your security posture in AWS by aggregating, organizing, and prioritizing security findings from multiple AWS services and third-party integrations. All findings are normalized into the Amazon Security Finding Format, enabling consistent querying, correlation, and automated response regardless of the source service. Security Hub runs automated compliance checks against security standards including CIS AWS Foundations Benchmark, AWS Foundational Security Best Practices, PCI DSS, and NIST SP 800-53. It integrates natively with GuardDuty, Inspector, Macie, Firewall Manager, IAM Access Analyzer, and over 60 third-party security solutions, making it the central nervous system for AWS security operations.',
  whenToUse: 'When you need a single pane of glass for security findings across multiple AWS services and accounts, or when compliance reporting against CIS, PCI DSS, or AWS Foundational Standards is required. Use Security Hub as the aggregation layer for automated remediation workflows via EventBridge, and as the bridge between AWS-native security services and enterprise SIEM or ticketing systems. It is most valuable in organizations running multiple accounts and needing a consolidated view of security posture without logging into each account and service individually.',
  keyConcepts: [
    { title: 'Amazon Security Finding Format (ASFF)', description: 'ASFF is the standardized JSON schema all findings in Security Hub conform to. It includes fields for finding source, severity, resource details, remediation guidance, workflow state, and compliance status. Normalization into ASFF means you can write EventBridge rules, Lambda handlers, and SIEM queries that work uniformly across findings from GuardDuty, Inspector, Macie, and third-party integrations without source-specific parsing logic.' },
    { title: 'Security Standards and Compliance Checks', description: 'Security Hub can run automated checks against multiple security standards simultaneously. CIS AWS Foundations Benchmark checks configurations like MFA on root account, CloudTrail enabled, and security group rules. AWS Foundational Security Best Practices covers over 200 controls across services. PCI DSS checks apply to environments handling cardholder data. Each check returns a PASSED, FAILED, WARNING, or NOT AVAILABLE status with a severity and remediation guidance link. Controls can be disabled individually if they do not apply to your environment.' },
    { title: 'Consolidated Findings and Cross-Region Aggregation', description: 'Security Hub can be configured with a home region that aggregates findings from all linked regions. The aggregation region receives all findings from other regions, enabling a truly global view of security posture from a single console and API endpoint. This is essential for organizations with workloads spanning multiple regions who need unified compliance reporting and incident response.' },
    { title: 'Custom Actions and EventBridge Integration', description: 'Custom actions in Security Hub allow analysts to trigger specific EventBridge rules from the console by selecting findings and choosing a custom action from the drop-down menu. This enables human-in-the-loop workflows where an analyst reviews a finding and then triggers a Lambda-based remediation, creates a Jira ticket, or sends a Slack notification with full finding context, without leaving the Security Hub console.' },
    { title: 'Automated Remediation with EventBridge Rules', description: 'EventBridge rules can trigger automatically when Security Hub generates findings matching specific criteria, without requiring analyst intervention. Common automated remediations include enabling CloudTrail logging when the corresponding control fails, removing public access from S3 buckets when a Macie finding is generated, revoking public security group rules, and rotating exposed IAM access keys. These auto-remediations are implemented as Lambda functions triggered by EventBridge rules scoped to specific finding types and severity levels.' },
    { title: 'Third-Party Integrations', description: 'Security Hub supports over 60 partner integrations in two directions: partners that send findings to Security Hub such as Palo Alto, Crowdstrike, Qualys, and Tenable, and partners that receive findings from Security Hub such as Splunk, ServiceNow, Jira, and PagerDuty. This bidirectional integration model makes Security Hub the hub in a hub-and-spoke security operations architecture, allowing teams to work in their preferred tools while maintaining a canonical finding store in AWS.' },
  ],
  approach: [
    { step: 'Enable Security Hub with Organizations integration', details: 'Designate a security account as the Security Hub delegated administrator. Enable auto-enrollment so all new accounts automatically join Security Hub. Run the batch-enable operation for existing accounts. Enable all relevant security standards at the organization level. Configure the aggregation region to consolidate findings from all active regions into the designated home region.' },
    { step: 'Enable native AWS service integrations', details: 'Enable all AWS-native integrations: GuardDuty, Inspector, Macie, Firewall Manager, IAM Access Analyzer, and Systems Manager Patch Manager. Each integration is a one-click enablement in the Security Hub integrations console. Verify that findings from each service are appearing in Security Hub by generating a test finding or checking the integration status indicator.' },
    { step: 'Review and tune security standards', details: 'After enabling security standards, review the initial compliance results. Disable controls that are not applicable to your environment with documented justifications, such as controls requiring specific services you do not use. Focus remediation efforts on critical and high severity failures first. Use the Security Hub console to assign remediation ownership to specific teams via the findings workflow status fields.' },
    { step: 'Build EventBridge-based automated remediation', details: 'Create EventBridge rules targeting security-hub finding import events filtered by specific finding types and severity. Write Lambda remediation functions for high-confidence, low-risk automated remediations such as re-enabling CloudTrail, blocking public S3 access, and tagging non-compliant resources. For higher-risk remediations, use custom actions to require analyst approval before execution.' },
    { step: 'Integrate with SIEM and ticketing systems', details: 'Configure Security Hub to forward findings to your SIEM via a Kinesis Firehose or direct partner integration. Map ASFF severity levels to your SIEM or ticketing system priority levels. Ensure the integration sends finding updates, not just new findings, so the SIEM reflects the current state of findings as they are remediated or suppressed.' },
    { step: 'Establish finding lifecycle management', details: 'Define workflow states for findings: NEW when first generated, NOTIFIED when an owner has been assigned, SUPPRESSED for known-benign false positives with documented justification, and RESOLVED when remediation is confirmed. Use Security Hub notes to document investigation steps and remediation actions. Track key metrics such as mean time to remediate critical findings and overall compliance score trends over time.' },
  ],
  pitfalls: [
    { title: 'Enabling Security Hub without enabling source services', description: 'Security Hub is an aggregation layer; it does not generate findings on its own (except for compliance checks). If GuardDuty, Inspector, and Macie are not enabled and integrated, Security Hub provides only compliance check results and no threat detection findings. A common mistake is enabling Security Hub and assuming it is doing active threat detection without enabling the underlying detection services.' },
    { title: 'Not configuring cross-region aggregation', description: 'By default, Security Hub findings are regional. Without configuring an aggregation region, you must log into each regional console separately to see a complete picture of findings. Cross-region aggregation must be explicitly enabled and adds a per-finding fee. Failing to configure this leads to blind spots when findings occur in secondary regions or creates operational friction that causes teams to miss regional findings during incident response.' },
    { title: 'Treating compliance check failures as security incidents', description: 'Security Hub compliance controls fire frequently for configuration deviations that may be intentional or low-risk in context, such as CloudTrail logging disabled in an account used only for sandbox experimentation. Without distinguishing between compliance findings and active threat findings from GuardDuty, analysts become overwhelmed and lose focus on genuine threats. Use severity filters, suppression rules, and workflow assignments to route compliance findings to a separate remediation queue distinct from active threat response.' },
    { title: 'Missing finding updates in automated workflows', description: 'EventBridge rules for Security Hub must match both the Security Hub Findings - Imported event type for new findings and the Security Hub Findings - Custom Action event for analyst-triggered workflows. Workflows that only match finding creation miss severity updates, state changes, and findings that are updated as an incident evolves. Always test EventBridge patterns with sample events including both create and update scenarios.' },
  ],
  keyQuestions: [
    { q: 'How does Security Hub differ from using native service consoles for each security tool?', a: 'Individual service consoles such as the GuardDuty console or the Macie console show findings only from that service in its native format. Security Hub normalizes all findings into ASFF, enabling cross-service correlation, unified search and filtering, and a single API endpoint for automated workflows. When an EC2 instance has a GuardDuty finding indicating suspicious network activity and an Inspector finding indicating a critical CVE on the same instance, Security Hub lets you correlate these into a prioritized risk picture, which is impossible when viewing services in isolation. Security Hub also runs compliance checks across over 200 controls that no individual service provides, and its EventBridge integration allows a single automation framework to handle findings from all sources.' },
    { q: 'How would you implement automated remediation for Security Hub findings?', a: 'The standard pattern uses EventBridge rules triggered by Security Hub Finding Import events filtered by finding type, severity, and workflow status. For example, a rule matching findings of type Software and Configuration Checks with a specific control ID and status FAILED triggers a Lambda function that calls the relevant AWS API to remediate the configuration, such as calling PutBucketPublicAccessBlock to fix a public S3 bucket finding. The Lambda function then calls the Security Hub BatchUpdateFindings API to set the finding workflow status to RESOLVED and add a note documenting the automated remediation action. For higher-risk remediations, use Security Hub custom actions to require an analyst to select the finding and trigger the action manually, which fires a Security Hub Findings - Custom Action EventBridge event. This human-in-the-loop approach is appropriate for actions like modifying security groups or rotating credentials where false positives could cause service disruption.' },
    { q: 'How do you aggregate Security Hub findings across multiple accounts and regions?', a: 'Security Hub uses two mechanisms for aggregation. For multi-account aggregation, designate a delegated administrator account through AWS Organizations. The delegated administrator automatically receives all findings from member accounts and can manage standards and integrations centrally. For cross-region aggregation, configure a finding aggregation region in the delegated administrator account that links all other regions as linked regions. Findings from linked regions are replicated to the aggregation region in real time with a small per-finding replication fee. The aggregation region then provides a unified view across all accounts and regions. Third-party SIEMs and ticketing systems only need to integrate with the aggregation region Security Hub endpoint to receive all findings from the entire organization.' },
    { q: 'How would you use Security Hub for compliance reporting for PCI DSS or CIS?', a: 'Enable the relevant security standard in Security Hub, which activates automated compliance checks mapped to the standard controls. Security Hub runs these checks continuously and maintains a compliance score showing the percentage of controls that are passing. For PCI DSS reporting, use the Security Hub compliance standard report to export all control statuses, finding details, and remediation histories as evidence for QSA auditors. Custom insights can be saved to track specific control categories over time and demonstrate improvement trends. For controls that do not apply to your environment, disable them with a documented justification and suppression note so auditors understand the exclusion. Integration with third-party governance, risk, and compliance tools via ASFF forwarding allows the compliance data to feed into broader GRC platforms that aggregate evidence across on-premises and cloud environments.' },
  ],
  references: [
    'https://docs.aws.amazon.com/securityhub/latest/userguide/what-is-securityhub.html',
    'https://docs.aws.amazon.com/securityhub/latest/userguide/securityhub-findings-format.html',
    'https://docs.aws.amazon.com/securityhub/latest/userguide/securityhub-standards.html',
  ],
},
{
  id: 'aws-inspector',
  title: 'Inspector — Automated Vulnerability Management',
  icon: 'search',
  color: '#ef4444',
  description: 'AWS automated vulnerability management service for EC2, ECR container images, and Lambda functions.',
  introduction: 'Amazon Inspector v2 is a vulnerability management service that automatically discovers and scans your AWS workloads for software vulnerabilities and unintended network exposure. It continuously scans EC2 instances via AWS Systems Manager Agent, container images in Amazon ECR when they are pushed and continuously for new CVEs, and Lambda functions for code vulnerabilities and package dependencies. Inspector v2 generates findings with risk scores that combine CVSS base scores with environmental context such as network reachability and exploitability data, making it easier to prioritize remediation. Findings are automatically forwarded to Security Hub and can trigger EventBridge-based workflows for automated response and CI/CD integration.',
  whenToUse: 'When you need continuous, automated vulnerability scanning across EC2 fleets, container registries, and Lambda functions without deploying and managing a separate vulnerability scanner. Enable Inspector v2 in all accounts as a baseline security control, particularly in environments with frequent EC2 launches or continuous container image builds. It is essential for teams that need to demonstrate vulnerability management practices for compliance frameworks like PCI DSS, SOC 2, and FedRAMP, and for security teams that want to shift vulnerability detection left into the CI/CD pipeline via ECR scanning.',
  keyConcepts: [
    { title: 'Inspector v2 Architecture and Coverage', description: 'Inspector v2 uses an agentless architecture for ECR and Lambda scanning and leverages the SSM Agent already installed on EC2 instances for host scanning. For EC2, it collects installed package inventory via SSM and correlates it against the CVE database without installing a dedicated agent. For ECR, it scans images on push and re-scans all images when new CVEs are published that affect packages in those images. For Lambda, it scans function code packages and layer dependencies for known CVEs and code vulnerabilities.' },
    { title: 'Finding Types', description: 'Inspector generates three primary finding types: package vulnerability findings identify CVEs in software packages installed on EC2, in ECR images, or in Lambda dependencies; network reachability findings identify EC2 instances with open network paths from the internet to specific ports and services based on VPC configuration analysis; and code vulnerability findings (for Lambda) identify security issues in application code such as SQL injection, path traversal, and hardcoded credentials detected by static analysis.' },
    { title: 'Inspector Risk Score', description: 'Inspector calculates a risk score between 0 and 10 for each finding by combining the CVSS base score with contextual factors. For EC2 findings, contextual factors include whether the instance is internet-reachable, whether the vulnerability has a known exploit, and whether the CVE has been observed in active exploitation campaigns. A vulnerability with CVSS 7.5 on an internet-facing instance with a public exploit kit scores much higher than the same CVE on an isolated internal instance, allowing teams to triage effectively.' },
    { title: 'ECR Container Scanning Integration', description: 'When Inspector v2 is enabled, it automatically scans all ECR images on push for OS package vulnerabilities and programming language package vulnerabilities. Scan results are available in the ECR console alongside the image and are also forwarded to Security Hub. Enhanced scanning in Inspector v2 provides deeper coverage than the basic ECR scanning feature, including programming language packages and access to AWS threat intelligence to identify packages associated with active exploitation.' },
    { title: 'SBOM Export', description: 'Inspector can generate a Software Bill of Materials for any scanned resource, exporting it in CycloneDX or SPDX format to an S3 bucket. SBOMs enumerate all software packages and their versions in a workload, which is increasingly required by government regulations and enterprise vendor security assessments. Automated SBOM generation from Inspector enables compliance with executive orders and frameworks requiring software supply chain transparency.' },
    { title: 'Coverage Gaps and Suppression Rules', description: 'Inspector provides a coverage view showing which resources are actively scanned versus which have coverage gaps due to missing SSM Agent, unsupported operating systems, or unactivated protection types. Suppression rules allow teams to archive findings for accepted risks, patching exceptions, or false positives, with required justification fields. Suppressed findings remain visible and auditable but do not count toward active finding metrics.' },
  ],
  approach: [
    { step: 'Enable Inspector v2 across all accounts with Organizations', details: 'Designate a security account as the Inspector delegated administrator. Enable EC2, ECR, and Lambda scanning types. Use the auto-enable feature to automatically activate Inspector for all new and existing organization accounts. Review the coverage summary after enablement to identify EC2 instances missing SSM Agent and address those gaps before relying on coverage completeness for compliance reporting.' },
    { step: 'Ensure SSM Agent is installed and running on all EC2 instances', details: 'Inspector EC2 scanning requires SSM Agent version 3.1.1767 or later. Use Systems Manager Fleet Manager to identify instances without SSM Agent or with outdated versions. Create an SSM State Manager association to automatically install and maintain SSM Agent across all instances. Ensure the EC2 instance profile includes the AmazonSSMManagedInstanceCore policy to allow SSM connectivity.' },
    { step: 'Configure ECR enhanced scanning', details: 'Ensure enhanced scanning is enabled for all ECR repositories containing production images. Configure repository scanning filters to apply enhanced scanning to all images or specific repository prefixes. Integrate Inspector findings into CI/CD pipelines by using the ECR image scan results in CodePipeline or Jenkins to fail builds when critical CVEs are detected, shifting vulnerability management left before deployment.' },
    { step: 'Prioritize findings using risk scores and filters', details: 'Focus remediation efforts on findings with Inspector risk scores above 7.0 that are on internet-accessible resources. Use the Inspector console filters to group findings by CVE ID to identify CVEs affecting multiple resources simultaneously, prioritizing patching at the package level rather than instance by instance. Export findings to a CSV or forward to a SIEM for tracking remediation progress against SLA targets.' },
    { step: 'Integrate with Security Hub and ticketing systems', details: 'Verify that Inspector findings are flowing into Security Hub. Create EventBridge rules to automatically create Jira or ServiceNow tickets for critical Inspector findings assigned to the responsible team based on EC2 instance tags or ECR repository names. Tag EC2 instances and ECR repositories with owner and application metadata to enable automatic finding routing.' },
    { step: 'Generate SBOMs for compliance and supply chain visibility', details: 'Configure Inspector SBOM exports to run on a scheduled basis or trigger from ECR push events. Store SBOMs in a versioned S3 bucket with lifecycle policies. Share SBOMs with enterprise security teams and external auditors as evidence of software supply chain transparency. Use SBOMs to proactively check for packages affected by newly disclosed CVEs before Inspector re-scans.' },
  ],
  pitfalls: [
    { title: 'Confusing Inspector v1 and Inspector v2 capabilities', description: 'Inspector v1 required you to explicitly define assessment targets and assessment templates and manually trigger assessment runs on a schedule. Inspector v2 is fundamentally different: it is always-on, automatically discovers resources, and continuously re-scans as new CVEs are published without any scheduled assessment configuration. Teams migrating from v1 to v2 sometimes expect to configure assessment schedules and are confused when the v2 console has no such concept. V2 also covers ECR and Lambda, which v1 did not.' },
    { title: 'Treating all CVEs as equally urgent regardless of context', description: 'A critical CVSS score does not automatically mean the finding is exploitable in your environment. Inspector risk scores incorporate network reachability and exploit availability as contextual factors, but teams that ignore the risk score and triage solely by CVSS create unsustainable remediation queues. The most effective approach is to prioritize by Inspector risk score combined with asset criticality tags, addressing high-risk-score findings on internet-facing production systems first.' },
    { title: 'Not addressing network reachability findings', description: 'Package vulnerability findings receive most attention, but network reachability findings from Inspector indicate ports and services exposed to the internet via overly permissive security groups or NACLs. These findings can represent attack surface expansion independent of any CVE. Regularly review and remediate network reachability findings as part of security group hygiene, not just as part of the vulnerability management program.' },
    { title: 'Coverage gaps due to missing SSM Agent on legacy instances', description: 'EC2 instances without SSM Agent are shown in Inspector coverage reports as not covered, but they do not generate active findings indicating their vulnerability status. This creates a false sense of completeness: the Inspector dashboard looks clean because unscanned instances have no findings, not because they have no vulnerabilities. Always monitor the coverage percentage and investigate instances shown as not covered before claiming complete vulnerability management coverage.' },
  ],
  keyQuestions: [
    { q: 'What are the main differences between Amazon Inspector v1 and Inspector v2?', a: 'Inspector v1 required manual configuration of assessment targets, rules packages, and assessment schedules. It ran on-demand or on a defined schedule and required a dedicated Inspector agent installed on each EC2 instance. Inspector v2 is continuous, agentless for ECR and Lambda, and uses the SSM Agent already present on most EC2 instances. V2 automatically discovers all supported resources in your account and begins scanning them without any target or schedule configuration. V2 also expanded coverage to include ECR container images, which it scans on push and re-scans when new CVEs are published, and Lambda functions, which were not supported in v1 at all. The risk scoring model in v2 is also more sophisticated, incorporating network reachability and exploitability intelligence beyond the raw CVSS score.' },
    { q: 'How does Inspector integrate into a container image CI/CD pipeline?', a: 'The integration works through ECR enhanced scanning powered by Inspector v2. When a container image is pushed to ECR, Inspector automatically scans it and publishes scan results to the ECR image metadata. In a CodePipeline or Jenkins pipeline, a post-build step queries the ECR image scan results API and fails the pipeline if any findings exceed a defined severity threshold such as CRITICAL or HIGH. This gates deployment of vulnerable images before they reach production. Teams can also use the Inspector SBOM export feature to produce a software bill of materials for every image build and archive it alongside the image for supply chain auditing. The scan results are also forwarded to Security Hub, where compliance controls can fail if images in a repository have unresolved critical CVEs beyond a defined age.' },
    { q: 'How would you fix a network reachability finding from Amazon Inspector?', a: 'A network reachability finding from Inspector indicates that a network path exists from the internet or another network boundary to a specific port on an EC2 instance based on analysis of security groups, NACLs, route tables, and internet gateways. The remediation depends on whether the exposed port is intentional. For unintentional exposure, remove or restrict the inbound security group rule allowing traffic from 0.0.0.0/0 to the identified port, or add a NACL deny rule. For intentionally public-facing services, acknowledge the finding using a suppression rule with a documented justification and ensure the service is hardened with authentication and encryption. Inspector provides a visual network path diagram in the finding detail that shows the exact chain of security groups and route tables creating the exposure, which simplifies root cause identification. After remediation, Inspector will automatically update or close the finding within the next scan cycle.' },
    { q: 'How would you integrate Inspector findings into a development team workflow to shift vulnerability management left?', a: 'The most effective left-shift approach uses ECR scan results as a deployment gate in the CI/CD pipeline. Configure CodePipeline with a post-build approval action that calls the ECR DescribeImageScanFindings API and fails the pipeline if critical or high findings are present. This gives developers immediate feedback at build time rather than discovering vulnerabilities weeks later during a security review. Supplement this with Inspector findings in Security Hub forwarded to Jira or the team backlog tool via EventBridge and Lambda, with findings tagged to the owning team based on repository or EC2 tags. Run weekly finding review meetings where security and development leads triage the Inspector queue together. Track patching SLAs by severity and make compliance visible to engineering leadership through a Security Hub custom insight showing open critical findings per team. This creates accountability and integrates vulnerability management into normal sprint work rather than treating it as a separate security team activity.' },
  ],
  references: [
    'https://docs.aws.amazon.com/inspector/latest/user/what-is-inspector.html',
    'https://docs.aws.amazon.com/inspector/latest/user/findings-understanding.html',
    'https://docs.aws.amazon.com/inspector/latest/user/sbom-export.html',
  ],
},
{
  id: 'aws-macie',
  title: 'Macie — S3 Data Security and Privacy',
  icon: 'eye',
  color: '#ef4444',
  description: 'AWS fully managed data security service that uses ML to discover and protect sensitive data in S3.',
  introduction: 'Amazon Macie is a fully managed data security and data privacy service that uses machine learning and pattern matching to discover and protect sensitive data in Amazon S3. Macie automatically evaluates all S3 buckets in an account for security and access controls, generating findings for buckets that are publicly accessible, unencrypted, or shared with external accounts. Classification jobs scan bucket contents for sensitive data types including personally identifiable information, financial account numbers, health information, and AWS credentials. Macie integrates with Security Hub and EventBridge to enable automated response workflows and can be centrally managed across an AWS organization.',
  whenToUse: 'When you store sensitive customer data, financial records, health information, or proprietary data in S3 and need to continuously discover, classify, and monitor it for security and compliance. Enable Macie when you need to demonstrate compliance with regulations such as GDPR, HIPAA, PCI DSS, or CCPA that require knowing where sensitive data is stored and ensuring appropriate access controls. Use Macie in data lake environments, backup storage, log archives, and any environment where sensitive data may land in S3 through automated pipelines without explicit classification tagging.',
  keyConcepts: [
    { title: 'Automated Sensitive Data Discovery', description: 'Macie continuously runs automated sensitive data discovery that samples objects across all S3 buckets to build an inventory of sensitive data locations. This background sampling runs continuously without explicit job configuration and provides a data sensitivity heat map across your S3 estate. It differs from classification jobs in that it uses statistical sampling rather than scanning every object, making it cost-effective for initial discovery across large data estates.' },
    { title: 'Managed and Custom Data Identifiers', description: 'Macie includes over 100 managed data identifiers maintained by AWS that detect common sensitive data types such as Social Security Numbers, credit card numbers, passport numbers, bank account details, AWS secret access keys, and healthcare-related information. Custom data identifiers allow you to define your own patterns using regular expressions with optional keyword and ignore lists, enabling detection of proprietary data formats, internal ID schemes, or industry-specific sensitive data patterns unique to your organization.' },
    { title: 'S3 Bucket Inventory and Policy Findings', description: 'Macie continuously monitors all S3 buckets and generates policy findings for security misconfigurations independent of object content. Policy findings cover buckets that are publicly accessible via bucket policy or ACL, buckets with server-side encryption disabled, buckets accessible to external AWS accounts or AWS organizations outside your organization, and buckets where Block Public Access settings have been disabled. These findings appear even without running classification jobs and provide a continuous security posture assessment of your S3 estate.' },
    { title: 'Classification Jobs', description: 'Classification jobs are on-demand or scheduled scans of specific S3 buckets or object subsets that perform full content analysis for sensitive data. Jobs can be scoped by bucket tags, object prefixes, file types, and date ranges. Results identify specific objects containing sensitive data with the data type, occurrence count, and sample values with masking. Jobs are priced per gigabyte scanned, so scoping them carefully to relevant data locations controls cost while ensuring coverage of high-risk buckets.' },
    { title: 'Suppression Rules and Allow Lists', description: 'Suppression rules archive findings that match defined criteria, reducing noise from known-safe patterns such as test data buckets containing synthetic PII or buckets with publicly accessible logos that trigger false policy findings. Allow lists let you define specific text strings or regex patterns that should be excluded from sensitive data findings, useful for sample data, redacted records, or tokenized values that resemble but are not actual sensitive data.' },
    { title: 'Multi-Account Deployment with Organizations', description: 'A designated Macie administrator account manages member accounts centrally through AWS Organizations integration. The administrator can enable Macie across all organization accounts, configure policy finding publication settings, and view aggregated findings from all member accounts. Member accounts cannot disable Macie when managed by the organization administrator. This centralized model is essential for large organizations ensuring consistent data discovery coverage across all accounts including data lake accounts that may contain the most sensitive data.' },
  ],
  approach: [
    { step: 'Enable Macie with Organizations integration', details: 'Designate a security account as the Macie administrator. Enable auto-enrollment for the organization to automatically activate Macie in all current and future member accounts. Verify that automated sensitive data discovery is enabled to begin building the data sensitivity map across all S3 buckets. Review the initial S3 bucket inventory to establish a baseline of policy findings before beginning remediation.' },
    { step: 'Review S3 bucket security posture findings', details: 'Start with policy findings before running expensive classification jobs. Address buckets flagged as publicly accessible or unencrypted as immediate priorities. Work with data owners to confirm whether public access is intentional and document suppression rules with business justifications for any intentional exceptions. Track the percentage of buckets with encryption and public access block enabled as a key security metric.' },
    { step: 'Scope and run classification jobs on high-risk buckets', details: 'Run targeted classification jobs on buckets most likely to contain sensitive data: application databases exported to S3, ETL pipeline outputs, customer data exports, log archives, and backup destinations. Use job scoping to focus on recently modified objects first to limit initial scan costs. Schedule recurring jobs on these high-risk buckets to catch new sensitive data as it arrives, and run one-time comprehensive jobs less frequently for complete coverage.' },
    { step: 'Define custom data identifiers for organization-specific data', details: 'Analyze common internal data formats that would not be covered by managed identifiers: employee ID formats, proprietary product serial numbers, internal account numbers, or healthcare codes specific to your organization. Create custom data identifiers with precise regular expressions and supporting keywords to minimize false positives. Test custom identifiers against sample data in a development account before deploying to production.' },
    { step: 'Build automated response workflows', details: 'Configure EventBridge rules to trigger Lambda functions when Macie generates high-severity sensitive data findings. Common automated responses include tagging the identified S3 object with a data classification label, notifying the data owner via SNS, restricting bucket access by applying a deny policy until review, and creating a security incident ticket. Ensure automated responses are scoped appropriately to avoid locking down buckets based on false positive findings.' },
    { step: 'Integrate with Security Hub for unified visibility', details: 'Verify Macie is publishing findings to Security Hub. Create Security Hub insights to track sensitive data exposure trends over time: total open Macie findings by severity, findings by bucket or account, and remediation velocity. Use these insights in security review meetings to demonstrate data protection posture improvement and identify teams or accounts with persistent sensitive data exposure issues.' },
  ],
  pitfalls: [
    { title: 'Running classification jobs on all buckets without scoping', description: 'Macie classification jobs are priced per gigabyte scanned at approximately 1 USD per GB. Running comprehensive jobs across all S3 buckets in large data lake environments can generate tens of thousands of dollars in scanning costs. Always scope jobs to high-risk buckets first, use sampling for initial discovery, and rely on automated sensitive data discovery for low-risk buckets. Model costs before running jobs on buckets containing terabytes of data.' },
    { title: 'Ignoring policy findings in favor of data findings', description: 'Policy findings about publicly accessible or unencrypted buckets are just as important as findings about sensitive data content but require no per-gigabyte scan cost. Many teams focus exclusively on data discovery findings while buckets remain publicly accessible. A publicly accessible bucket with no sensitive data today may have sensitive data tomorrow, so addressing policy findings first prevents future exposure before it occurs.' },
    { title: 'Not using allow lists for tokenized or synthetic data', description: 'Macie regularly generates false positive sensitive data findings for tokenized payment card data, synthetic test data containing realistic-looking PII, sample datasets used for application testing, and redacted records where values look like sensitive data. Without allow lists, teams spend significant time triaging and suppressing these false positives manually. Create allow lists for known tokenization formats and synthetic data patterns early in the deployment to maintain signal quality.' },
    { title: 'Assuming Macie coverage equals data compliance', description: 'Macie discovers sensitive data in S3 but does not control access to it, enforce encryption, or prevent exfiltration. Discovering that a bucket contains PII is only the first step; organizations must also implement appropriate access controls, encryption, data retention policies, and deletion workflows. Treat Macie findings as a discovery and alerting mechanism, not as a complete data protection solution, and ensure findings feed into a broader data governance program.' },
  ],
  keyQuestions: [
    { q: 'How does Macie differ from manually scanning S3 with custom scripts?', a: 'Macie provides several advantages over custom scanning scripts. It maintains and updates over 100 managed data identifiers for common sensitive data types including international formats across multiple countries, which would require significant ongoing maintenance effort to replicate. It continuously monitors S3 bucket security configurations and generates policy findings for misconfigurations without requiring any object scanning. The automated sensitive data discovery feature provides continuous sampling-based coverage without manual scheduling. Macie integrates natively with Security Hub, EventBridge, and AWS Organizations for centralized multi-account management. Custom scripts require compute infrastructure to run, security review to avoid credential exposure, and operational maintenance. Macie also provides built-in occurrence counting, sample value masking for safe triage, and sensitivity scoring per bucket, which are non-trivial to implement correctly in custom tooling.' },
    { q: 'How would you use custom data identifiers and when would you need them?', a: 'Custom data identifiers extend Macie beyond its managed data types to detect organization-specific sensitive data patterns. You define a custom identifier with a regular expression that matches your data format, optional keywords that must appear near the match to reduce false positives, and an optional ignore list of strings that match the regex but should not be flagged. Use cases include detecting internal employee ID numbers in a specific format, proprietary financial account numbers unique to your industry, healthcare procedure codes or device serial numbers used internally, partner API keys or tokens with a known format, and internal project codenames or classifications that should not appear in shared storage. Custom identifiers are also useful for regulatory requirements specific to your industry or country that are not covered by AWS managed identifiers. Test regex patterns thoroughly before production deployment because overly broad patterns generate excessive false positives that degrade finding quality.' },
    { q: 'How would you respond to a Macie finding that sensitive data was found in an S3 bucket?', a: 'The response follows a triage, contain, investigate, remediate, and prevent sequence. First, confirm the finding is a true positive by reviewing the sample occurrences Macie provides in the finding detail and confirming the data type is genuine sensitive data. Second, assess the exposure: review the bucket policy and ACLs to determine who has access, check if the bucket is publicly accessible, and determine if external accounts can access it. Third, if the bucket is publicly accessible or shared inappropriately, immediately restrict access by enabling Block Public Access or removing external account grants. Fourth, identify how the sensitive data arrived in the bucket by tracing the data pipeline or application that wrote the objects, and assess whether the same data exists in other locations. Fifth, work with the data owner to determine if the sensitive data should remain in S3 with appropriate controls, be moved to a more appropriate location, or be deleted. Finally, implement preventive controls such as S3 Object Lambda to redact sensitive fields on read, SCPs preventing public bucket access, and Macie classification jobs on the pipeline output buckets to catch future occurrences early.' },
    { q: 'How do you manage Macie costs for a large S3 estate with petabytes of data?', a: 'Cost management for Macie at scale requires a tiered approach. First, rely on automated sensitive data discovery, which uses statistical sampling and is priced at a lower per-GB rate than full classification jobs, for initial coverage of the entire estate. Second, use Macie bucket inventory and sensitivity scores from automated discovery to identify the highest-risk buckets for full classification job scanning. Scan only those buckets with full jobs and accept sampling-based coverage for low-risk buckets such as log archives, application asset stores, and infrastructure backups. Third, scope classification jobs using object prefixes, file type filters, and modification date windows to avoid re-scanning unchanged historical data. Fourth, schedule full classification jobs quarterly rather than weekly for stable data sets, and use incremental scanning for buckets with continuous data ingestion. Fifth, set a Macie sensitivity analysis budget in the console to cap monthly discovery costs. Track Macie costs in AWS Cost Explorer and correlate them with finding value to adjust scanning scope over time. For petabyte-scale environments, the combined cost of automated discovery and targeted classification jobs is typically much lower than full continuous scanning of all data.' },
  ],
  references: [
    'https://docs.aws.amazon.com/macie/latest/user/what-is-macie.html',
    'https://docs.aws.amazon.com/macie/latest/user/findings-types.html',
    'https://docs.aws.amazon.com/macie/latest/user/custom-data-identifiers.html',
  ],
},
{
  id: 'aws-certificate-manager',
  title: 'ACM — Certificate Manager',
  icon: 'award',
  color: '#ef4444',
  description: 'AWS service for provisioning, managing, and deploying public and private SSL/TLS certificates.',
  introduction: 'AWS Certificate Manager simplifies the provisioning and lifecycle management of SSL/TLS certificates for use with AWS services. ACM public certificates are issued free of charge by Amazon Trust Services and support automatic renewal, eliminating the operational burden of certificate expiry monitoring and manual renewal that causes outages in traditional PKI environments. ACM integrates directly with Elastic Load Balancing, CloudFront, API Gateway, Elastic Beanstalk, and App Runner to attach certificates with no private key exposure. For internal PKI requirements, ACM Private Certificate Authority provides a managed CA hierarchy with configurable certificate policies, revocation via OCSP and CRL, and support for issuing certificates to on-premises servers and IoT devices.',
  whenToUse: 'When enabling HTTPS for any public-facing AWS workload or internal service requiring TLS, ACM should be the default choice over managing certificates manually. Use ACM public certificates for all CloudFront, ALB, and API Gateway endpoints. Use ACM Private CA when you need to issue certificates to on-premises servers, internal microservices, mutual TLS (mTLS) authentication, IoT devices, or other non-AWS resources that require your own trusted CA. Use ACM only when terminating TLS at an AWS-managed service; for workloads requiring certificate installation on EC2 or on-premises servers, export from Private CA or use ACM with Nitro Enclaves.',
  keyConcepts: [
    { title: 'Public vs Private Certificates', description: 'ACM public certificates are issued by Amazon Trust Services, a publicly trusted CA, and are accepted by all major browsers and operating systems without any additional trust configuration. They are free of charge and support automatic renewal. ACM private certificates are issued by your ACM Private CA hierarchy, are trusted only by systems configured to trust your CA chain, and cost per-certificate and per-CA-operation. Public certificates cannot be exported; private certificates can be exported for use outside AWS-managed services.' },
    { title: 'DNS Validation vs Email Validation', description: 'DNS validation proves domain ownership by adding a CNAME record to the domain DNS zone. It is strongly preferred because it enables fully automatic certificate renewal without human intervention as long as the CNAME record remains in place. Email validation sends confirmation emails to administrative contacts in the domain WHOIS record and requires manual click-through, making automatic renewal impossible. Email validation is a fallback for domains where you cannot modify DNS records. DNS validation also works for wildcard certificates and multi-domain SANs across all domain names in a single certificate.' },
    { title: 'Automatic Certificate Renewal', description: 'ACM automatically renews managed certificates before they expire, typically initiating renewal 60 days before expiry. For DNS-validated certificates, renewal is fully automatic as long as the validation CNAME record is still in DNS. For email-validated certificates, AWS sends renewal request emails that must be manually approved. If renewal fails, ACM sends notifications via EventBridge and email starting 45 days before expiry. The renewed certificate is automatically deployed to all associated AWS services without any user action required.' },
    { title: 'Wildcard and Multi-Domain SANs', description: 'ACM supports wildcard certificates covering all subdomains of a domain such as *.example.com, and Subject Alternative Names certificates covering multiple distinct domain names in a single certificate such as example.com, www.example.com, and api.example.com. SANs are validated individually, so each domain in a SAN certificate requires its own DNS or email validation record. ACM allows up to 10 domain names per certificate by default with a limit increase available via support.' },
    { title: 'ACM Private Certificate Authority', description: 'ACM Private CA is a managed CA service that creates and manages private root and subordinate CAs without the operational burden of running CA infrastructure. It supports RSA and ECDSA key algorithms, configurable certificate templates for different use cases such as end-entity, code signing, and OCSP responder certificates, and automated certificate revocation via OCSP and CRL published to S3. Private CA integrates with the ACM console and API for issuing certificates, with IAM for access control, and with AWS Organizations for cross-account certificate issuance.' },
    { title: 'Cross-Region Considerations for CloudFront', description: 'CloudFront is a global service that requires certificates to be provisioned in the us-east-1 region specifically, regardless of where your origin resources are located. This is a common operational mistake: certificates provisioned in other regions cannot be attached to CloudFront distributions. All other AWS services use certificates from the same region where the service is deployed. Automation that provisions certificates must account for this regional constraint when creating certificates for CloudFront.' },
  ],
  approach: [
    { step: 'Request a public certificate with DNS validation', details: 'In the ACM console or via the CLI, request a public certificate and add all required domain names as SANs including both the apex domain and the www subdomain. Choose DNS validation and export the validation CNAME records. Add these CNAMEs to your DNS provider, either manually or through Route 53 automatic validation if your domain is hosted in Route 53. Wait for ACM to confirm the validation records and issue the certificate, which typically takes a few minutes for Route 53 and up to 30 minutes for external DNS providers.' },
    { step: 'Associate the certificate with AWS services', details: 'Attach the issued certificate to the target service: configure the HTTPS listener on an ALB to use the certificate, associate it with a CloudFront distribution alternate domain name, or configure a custom domain on API Gateway. ACM handles the private key securely without ever exposing it through the console or API. Verify the certificate is serving correctly by checking the browser certificate details or using the openssl s_client command against the endpoint.' },
    { step: 'Configure certificate for CloudFront in us-east-1', details: 'When creating certificates for CloudFront, always switch the AWS console region to US East N. Virginia (us-east-1) before requesting the certificate. Name and tag the certificate clearly to indicate it is for CloudFront use. Automate certificate creation for CloudFront using CloudFormation or Terraform with explicit region configuration, as misconfigured automation that creates certificates in the wrong region is a common source of CloudFront deployment failures.' },
    { step: 'Set up ACM Private CA for internal PKI', details: 'Create a Private CA hierarchy with a root CA and one or more subordinate CAs. Issue end-entity certificates from subordinate CAs, not directly from the root, following PKI best practices. Configure OCSP and CRL for certificate revocation so relying parties can verify certificate validity in real time. Distribute the CA certificate chain to all systems that need to trust internally issued certificates via AWS Systems Manager Parameter Store, Secrets Manager, or configuration management tools.' },
    { step: 'Monitor certificate expiry and renewal status', details: 'Create EventBridge rules to capture ACM certificate expiry notifications at 45, 30, and 15 days before expiry. Route these to SNS topics that notify the platform engineering team. Verify that DNS validation CNAME records are still present for all certificates by running a periodic DNS lookup check via Lambda or a monitoring service. Alert immediately if validation CNAMEs are found missing, as their absence will cause automatic renewal to fail.' },
    { step: 'Export private certificates for non-AWS use', details: 'Private certificates issued by ACM Private CA can be exported in PEM format using the ACM ExportCertificate API. The private key in the export is encrypted with a customer-provided passphrase. Use this capability to provision certificates on EC2 instances, on-premises servers, or containers that require direct certificate file installation. Automate certificate distribution and renewal using Secrets Manager to store the exported certificate and private key, with rotation Lambda functions that call ExportCertificate and update the secret before expiry.' },
  ],
  pitfalls: [
    { title: 'Requesting CloudFront certificates in the wrong region', description: 'CloudFront distributions only accept certificates from ACM in us-east-1 regardless of where the origin resources are located. Requesting the certificate in eu-west-1 or ap-southeast-1 and then attempting to attach it to CloudFront will result in an error stating that no certificates are available, which is confusing because the certificate exists and is valid. Always document and enforce the us-east-1 requirement for CloudFront certificates in team runbooks and IaC templates.' },
    { title: 'Using email validation instead of DNS validation', description: 'Email validation prevents automatic certificate renewal because each renewal cycle requires a human to click a confirmation link in an email. In fast-moving environments with staff turnover, these confirmation emails are missed, causing certificates to expire and outages to occur. Always use DNS validation to enable fully automatic, unattended renewal. Email validation should only be used when DNS record management is outside your control.' },
    { title: 'Assuming ACM public certificates can be exported for on-premises use', description: 'ACM public certificates cannot be exported under any circumstances. They can only be attached to supported AWS services like ALB, CloudFront, and API Gateway. Organizations that need certificates for on-premises servers, Docker containers, or EC2 instances with direct certificate file requirements must use ACM Private CA with certificate export capability, or obtain certificates from a third-party CA. Discovering this limitation during an on-premises migration or hybrid connectivity project causes significant delays.' },
    { title: 'Not monitoring DNS validation record removal', description: 'DNS validation CNAME records must remain in the DNS zone permanently for automatic renewal to work. If a team member cleans up what they believe are stale DNS records and removes the ACM validation CNAME, the next automatic renewal attempt will fail. ACM will not warn that the validation record is missing until renewal is attempted. By the time the first renewal failure notification arrives, the certificate may be close to expiry. Run scheduled DNS checks to verify validation CNAMEs are still present for all ACM-managed certificates.' },
  ],
  keyQuestions: [
    { q: 'How does the ACM automatic certificate renewal process work?', a: 'ACM initiates automatic renewal 60 days before certificate expiry. For DNS-validated certificates, ACM performs a DNS lookup for the validation CNAME record that was added during initial issuance. If the record is still present and points to the correct ACM validation value, ACM automatically issues a renewed certificate and deploys it to all associated AWS services without any manual action. The old certificate continues to serve traffic until the new one is ready. For email-validated certificates, ACM sends renewal request emails to domain administrative contacts and requires manual click-through approval, making automated renewal impossible. ACM publishes certificate expiry and renewal status as EventBridge events and CloudWatch metrics, and sends email notifications at 45, 30, and 15 days before expiry if renewal has not yet completed. Teams should monitor these events as part of their certificate lifecycle management process.' },
    { q: 'When would you choose DNS validation over email validation, and what are the tradeoffs?', a: 'DNS validation is almost always the correct choice. It requires adding a CNAME record to the domain DNS zone, which takes a few minutes with Route 53 and slightly longer with third-party DNS providers, but once added, enables fully automatic renewal indefinitely without human intervention. DNS validation works for wildcard certificates, multi-domain SANs, and certificates in automated provisioning pipelines where human interaction is not possible. Email validation is a fallback for the narrow case where you do not control the DNS zone for a domain, such as a subdomain delegated to a third party or a domain managed by a different team without self-service DNS access. The critical tradeoff is that email validation requires manual renewal approval on every renewal cycle, typically every 13 months, creating an operational dependency that eventually causes an outage when the approval email is missed during staff absence or turnover. Any organization running more than a handful of certificates should enforce DNS validation as a standard to eliminate manual renewal operations entirely.' },
    { q: 'What are the use cases for ACM Private CA versus public certificates?', a: 'ACM public certificates are appropriate for any public-facing service where the certificate must be trusted by browsers, mobile operating systems, or third-party clients without custom trust store configuration. ACM Private CA is the right choice when you need to issue certificates to on-premises servers, virtual machines, or containers that require certificate file installation rather than AWS service attachment. Private CA is also required for mutual TLS (mTLS) authentication where both client and server present certificates, because you need to issue and control the client certificates as well. IoT device provisioning often uses Private CA to issue device identity certificates at manufacturing time. Internal microservices communicating over TLS within a VPC can use Private CA for service-to-service authentication without exposing services to public trust. Code signing certificates and OCSP responder certificates are additional use cases. The key distinction is whether the trusting party is a public browser or a system you can configure to trust your private CA chain.' },
    { q: 'How would you handle certificates for on-premises servers or EC2 instances that need direct file access?', a: 'Since ACM public certificates cannot be exported, the solution requires ACM Private CA. Issue end-entity certificates from the Private CA using the ACM IssueCertificate API or the PCA console, then export them using the ACM ExportCertificate API, which returns the certificate, the certificate chain, and an encrypted private key. Store the exported certificate and private key in AWS Secrets Manager with a rotation Lambda function that calls ExportCertificate before expiry, re-encrypts the private key with a KMS key, and updates the secret. On the target server, use the Secrets Manager agent or SDK to fetch the latest certificate and private key and install them to the appropriate filesystem paths, restarting the service if the certificate changes. For EC2 instances at scale, use Systems Manager Run Command or State Manager associations to automate certificate distribution triggered by Secrets Manager rotation events. Ensure the CA certificate chain is distributed to all servers that need to trust the issued certificates, typically via configuration management tooling or a trusted CA bundle in an S3 bucket with restricted access.' },
  ],
  references: [
    'https://docs.aws.amazon.com/acm/latest/userguide/acm-overview.html',
    'https://docs.aws.amazon.com/acm/latest/userguide/dns-validation.html',
    'https://docs.aws.amazon.com/privateca/latest/userguide/PcaWelcome.html',
  ],
},
{
  id: 'aws-lambda-patterns',
  title: 'Lambda — Serverless Compute Patterns',
  icon: 'zap',
  color: '#f59e0b',
  description: 'AWS Lambda advanced patterns for production serverless workloads.',
  introduction: 'AWS Lambda is the cornerstone of serverless computing on AWS, executing code in response to events without provisioning or managing servers. Lambda automatically scales from zero to thousands of concurrent executions, charging only for compute time consumed. Understanding Lambda internals — execution environments, concurrency models, cold start mechanics, and integration patterns — is essential for building reliable, cost-effective serverless applications at scale.',
  whenToUse: 'When building event-driven microservices, API backends, data processing pipelines, or any workload with unpredictable or spiky traffic patterns. Lambda excels at short-duration tasks under 15 minutes, fan-out processing, and integrating AWS services via event source mappings. Choose Lambda over containers when operational simplicity and fine-grained pay-per-invocation pricing matter more than long-running process support or consistent baseline load handling.',
  keyConcepts: [
    { title: 'Cold Start', description: 'The initialization latency when a new Lambda execution environment is created. AWS must download the deployment package, start the runtime, and run initialization code before the handler executes. Node.js and Python cold starts are typically 100-300ms; Java and .NET can reach 1-2 seconds. Provisioned Concurrency pre-warms environments to eliminate cold starts entirely for latency-sensitive functions.' },
    { title: 'Execution Environment Lifecycle', description: 'Lambda reuses execution environments for subsequent invocations, meaning global-scope variables, database connections, and SDK clients initialized outside the handler persist between calls. This improves performance but requires careful handling of stateful resources. Environments are recycled after periods of inactivity, triggering a cold start on the next invocation.' },
    { title: 'Concurrency Models', description: 'Lambda supports two concurrency controls: Reserved Concurrency sets a hard maximum on simultaneous executions for a function, protecting downstream services from overload and guaranteeing capacity allocation. Provisioned Concurrency pre-initializes a specified number of execution environments, eliminating cold starts for that capacity pool. Account-level concurrency quota defaults to 1000 concurrent executions across all functions in a region.' },
    { title: 'Event Source Mappings', description: 'Lambda can be triggered by polling-based services including SQS, Kinesis, DynamoDB Streams, MSK, and MQ. The Lambda service manages the polling, batching, and retry logic. Configurable parameters include batch size, batching window, starting position, bisect-on-error behavior, and destination for failed batches. These differ fundamentally from push-based triggers like API Gateway or S3.' },
    { title: 'Lambda Layers', description: 'Layers are ZIP archives containing libraries, custom runtimes, or configuration files that can be shared across multiple Lambda functions. Each function can reference up to five layers, which are extracted to /opt in the execution environment. Layers reduce deployment package sizes, enable sharing of common dependencies across functions, and support the principle of separating function code from dependencies.' },
    { title: 'Lambda SnapStart', description: 'A feature for Java 11 and later runtimes that reduces cold start latency by taking a snapshot of the initialized execution environment after the init phase completes. On invocation, Lambda restores the environment from this snapshot rather than initializing from scratch, reducing Java cold starts to sub-second levels. Requires functions to handle network connection re-establishment after restore.' },
  ],
  approach: [
    { step: 'Right-size memory allocation', details: 'Lambda allocates CPU, network bandwidth, and disk I/O proportionally to the configured memory, ranging from 128MB to 10240MB. Use the open-source AWS Lambda Power Tuning tool, which runs your function at different memory levels and plots the cost and performance tradeoffs. Many functions perform significantly faster with higher memory even for CPU-light workloads due to increased network throughput, resulting in lower overall cost despite higher per-ms pricing.' },
    { step: 'Optimize initialization code placement', details: 'Move SDK client initialization, database connection establishment, and configuration loading outside the handler function to the module-level scope. This code runs once during cold start and the initialized objects persist across warm invocations. Validate that connections established during init are still healthy inside the handler, re-initializing if needed, as environments can be reused after long idle periods when connections have timed out.' },
    { step: 'Configure concurrency controls for production', details: 'Set Reserved Concurrency on functions that call downstream services with limited capacity such as RDS databases, third-party APIs, or legacy systems. This prevents Lambda from overwhelming those services during traffic spikes. For latency-sensitive user-facing functions, enable Provisioned Concurrency sized to your baseline load, and pair it with Application Auto Scaling to adjust provisioned capacity based on CloudWatch metrics.' },
    { step: 'Implement Lambda Destinations for async flows', details: 'For asynchronous invocations, configure Destinations to route successful and failed results to SQS queues, SNS topics, EventBridge event buses, or other Lambda functions. Destinations provide better visibility and handling than relying solely on DLQ configurations. Set the maximum retry attempts to 0, 1, or 2 based on idempotency requirements and configure appropriate event age maximums to prevent stale event processing.' },
    { step: 'Handle VPC Lambda performance carefully', details: 'Lambda functions in a VPC require ENI (Elastic Network Interface) creation to communicate with VPC resources. With Hyperplane ENIs introduced in 2019, cold start penalties from VPC attachment are largely eliminated, but functions still need subnets with available IP addresses and appropriate security group rules. Ensure subnets have sufficient IP capacity for peak concurrency and that NAT Gateway or VPC endpoints are configured for internet or AWS service access.' },
  ],
  pitfalls: [
    { title: 'Unbounded concurrency spikes', description: 'Lambda scales to thousands of concurrent executions within seconds, which can overwhelm downstream services like RDS that have connection limits. Always set Reserved Concurrency on functions calling connection-limited services, use RDS Proxy to pool database connections, and use SQS with a Lambda trigger as a buffer to smooth out processing rate regardless of upstream event volume.' },
    { title: 'Ignoring Lambda timeout in distributed transactions', description: 'Lambda has a maximum 15-minute execution timeout. In distributed workflows, a Lambda timing out mid-transaction can leave downstream systems in inconsistent states. Design for idempotency so retries are safe, use Step Functions for long-running orchestration, implement compensating transactions for failures, and use the taskToken callback pattern for workflows that exceed Lambda timeout limits.' },
    { title: 'Packaging unnecessary dependencies', description: 'Large deployment packages increase cold start times because Lambda must download and extract the package before initializing. Use Lambda Layers for shared dependencies, tree-shake JavaScript bundles with esbuild or webpack, use slim base images for container-deployed functions, and keep function packages under 10MB unzipped for fastest cold starts. The Lambda console editor requires packages under 3MB.' },
    { title: 'Treating Lambda like a persistent service', description: 'Lambda execution environments are ephemeral and can be recycled at any time. Do not rely on local /tmp storage persisting across invocations for critical data, do not assume in-memory caches survive between calls without validation, and always handle reconnection logic for database connections and HTTP clients that may have been closed during environment recycling.' },
  ],
  keyQuestions: [
    { q: 'How do you handle cold starts in production Lambda functions?', a: 'Cold starts occur when Lambda must create a new execution environment, adding initialization latency of 100ms to several seconds depending on runtime and package size. For latency-sensitive user-facing functions, Provisioned Concurrency is the primary production solution, pre-warming a defined number of environments and eliminating cold start latency for that capacity. Java workloads specifically benefit from Lambda SnapStart, which snapshots the initialized environment and restores it on invocation. General optimization strategies include choosing Node.js or Python runtimes over JVM-based runtimes, keeping deployment packages minimal using tree-shaking and Lambda Layers, and moving all initialization code outside the handler to run once per environment. Scheduled EventBridge rules pinging functions every few minutes can maintain warm environments as a low-cost approach for low-traffic functions, though Provisioned Concurrency is the architecturally correct solution.' },
    { q: 'How do you handle Lambda timeouts in distributed systems?', a: 'Lambda timeouts in distributed systems require careful design because a timed-out function may have partially completed work, causing inconsistency. The first defense is designing every Lambda operation to be idempotent so retries produce the same result. For orchestrated workflows, use Step Functions with proper Retry and Catch configurations rather than chaining Lambda calls directly, since Step Functions tracks state externally. When Lambda calls external systems, use the taskToken callback pattern where Lambda sends the token to the external system and suspends, allowing the workflow to resume when the external system responds independently of Lambda timeout limits. Always set Lambda timeouts significantly below the timeout of the calling service to allow proper error propagation rather than having API Gateway or upstream services time out first.' },
    { q: 'What are the performance considerations for Lambda functions running inside a VPC?', a: 'Lambda functions configured to run inside a VPC can access private resources like RDS, ElastiCache, and internal services, but historically suffered significant cold start penalties due to ENI creation time. AWS resolved most of this with Hyperplane ENIs, which are created at function configuration time rather than invocation time, making modern VPC Lambda cold starts comparable to non-VPC functions. The remaining considerations are subnet IP address capacity — each concurrent Lambda execution consumes a private IP, so subnets must have enough available addresses for peak concurrency — and internet access configuration, since VPC Lambda cannot reach the internet or AWS services by default and requires NAT Gateway or VPC endpoints. For database access specifically, use RDS Proxy to pool connections and prevent Lambda from exhausting the database connection limit during traffic spikes.' },
    { q: 'How do you decide between Lambda and Fargate for a workload?', a: 'Lambda is optimal for event-driven, short-duration workloads where invocation frequency is unpredictable, the execution time is under 15 minutes, and operational simplicity is valued over fine-grained control. Lambda excels at API backends, event processors, scheduled jobs, and fan-out patterns. Fargate is better suited for long-running processes exceeding 15 minutes, workloads requiring predictable consistent performance without cold start concerns, applications with specific CPU or memory configurations that Lambda cannot satisfy, or services that need to maintain persistent connections or run background threads continuously. Cost comparison depends heavily on traffic patterns: Lambda is extremely cost-effective for spiky or low-volume workloads but can become more expensive than Fargate for sustained high-throughput processing where Fargate reserved pricing applies.' },
  ],
  references: [
    'https://docs.aws.amazon.com/lambda/latest/dg/',
    'https://docs.aws.amazon.com/lambda/latest/dg/provisioned-concurrency.html',
    'https://docs.aws.amazon.com/lambda/latest/dg/snapstart.html',
  ],
},
{
  id: 'aws-step-functions',
  title: 'Step Functions — Serverless Orchestration',
  icon: 'layers',
  color: '#f59e0b',
  description: 'AWS Step Functions for orchestrating distributed serverless workflows with state management.',
  introduction: 'AWS Step Functions is a serverless orchestration service that lets you build complex multi-step workflows using visual state machines defined in Amazon States Language (ASL). Step Functions manages state, handles retries, and coordinates Lambda functions, AWS SDK calls, and HTTP endpoints without you needing to write coordination logic in application code. It provides durable execution, built-in error handling, and full audit trail via execution history, making it the standard tool for long-running or complex distributed workflows on AWS.',
  whenToUse: 'When coordinating multiple services or steps in a workflow where failure at any step requires specific error handling, compensation, or retry logic. Step Functions is ideal for order processing, data pipelines, human approval workflows, ETL jobs, machine learning pipelines, and any multi-step process where you need visibility into each step\'s state and reliable execution guarantees. Use it instead of Lambda-chaining when workflow logic would otherwise be embedded in application code.',
  keyConcepts: [
    { title: 'Standard vs Express Workflows', description: 'Standard Workflows offer exactly-once execution semantics with execution history retained for 90 days, supporting durations up to one year. They are priced per state transition and are suited for long-running, durable workflows where audit history matters. Express Workflows offer at-least-once execution with shorter durations up to 5 minutes, higher throughput, and lower cost per execution, making them suitable for high-volume event processing where idempotency is guaranteed by the application.' },
    { title: 'Amazon States Language States', description: 'ASL defines workflows as JSON state machines with seven state types: Task states call external services or SDK APIs; Choice states implement conditional branching; Wait states pause execution for a duration or until a timestamp; Parallel states execute multiple branches concurrently and wait for all to complete; Map states iterate over an array applying a state machine to each element; Pass states inject or transform data; Succeed and Fail states terminate the execution successfully or with an error.' },
    { title: 'Retry and Catch Configuration', description: 'Task and Parallel states support declarative retry policies with configurable error filters, maximum attempts, initial interval, backoff rate, and jitter type. Catch blocks can trap specific error types and transition to fallback states, enabling compensating transaction patterns. Combining Retry and Catch allows fine-grained resilience: retry transient errors automatically while routing permanent failures to human review or compensating logic.' },
    { title: 'SDK Integrations and Optimistic Execution', description: 'Step Functions supports three integration patterns: Request-Response sends the request and immediately proceeds without waiting for a response, suitable for fire-and-forget operations. Synchronous integrations wait for the job to complete before transitioning. Optimistic integrations use the .sync:2 suffix and are supported for services like ECS, Glue, SageMaker, and CodeBuild, where Step Functions polls for completion internally without consuming Lambda invocations or idle wait cost.' },
    { title: 'Callback Pattern with taskToken', description: 'The waitForTaskToken integration pattern allows a workflow to pause indefinitely while an external system performs work. The task token is sent to the external system (via SQS, SNS, Lambda, or HTTP), and the workflow only resumes when the external system calls SendTaskSuccess or SendTaskFailure with the token. This pattern is essential for human approval steps, third-party API integrations with webhooks, and any operation that cannot be polled.' },
    { title: 'State Machine Versioning and Aliases', description: 'Step Functions supports versioning of state machine definitions, creating immutable snapshots that can be referenced by version number. Aliases map to specific versions or provide weighted traffic routing between versions, enabling blue-green deployments of workflow definitions. This allows gradual migration of in-flight executions to new versions without disrupting running workflows.' },
  ],
  approach: [
    { step: 'Design state machines for visibility and debuggability', details: 'Break workflows into discrete named states that represent meaningful business steps rather than technical operations. Use Pass states to inject test data or transform inputs between steps. Leverage the Step Functions console\'s visual execution graph to trace failures to specific states. Add meaningful Result and Input/Output processing using ResultPath, InputPath, and OutputPath to keep state data clean and prevent large payloads from being passed between every state.' },
    { step: 'Choose workflow type based on execution characteristics', details: 'Select Standard Workflows for business-critical processes requiring audit trails, durations over 5 minutes, or exactly-once semantics at the orchestration level. Use Express Workflows for high-frequency event processing, streaming data pipelines, and microservice coordination where throughput exceeds Standard limits of 2000 state transitions per second. Synchronous Express Workflows allow you to start a workflow and wait for the result inline, useful for request-response API patterns backed by complex multi-step logic.' },
    { step: 'Implement saga pattern for distributed transactions', details: 'For workflows spanning multiple services where partial completion must be rolled back, implement the saga pattern using Step Functions. Define a Catch block at each step that invokes compensating transactions in reverse order when a failure occurs. Use a dedicated error handling branch that calls cleanup steps for each previously completed service. This pattern avoids distributed ACID transactions while maintaining data consistency across services through application-level compensation.' },
    { step: 'Optimize payload handling with S3 for large data', details: 'Step Functions imposes a 256KB limit on state input and output. For workflows processing large documents, images, or datasets, store the actual data in S3 and pass only the S3 reference (bucket and key) between states. Use Lambda functions to read from and write to S3 at each processing step. This pattern is called the S3-backed payload pattern and is essential for ETL, document processing, and ML inference workflows.' },
  ],
  pitfalls: [
    { title: 'Ignoring Standard Workflow cost at high scale', description: 'Standard Workflows charge per state transition, which can become expensive for high-frequency workflows with many states. A workflow with 10 states executing 1 million times per day generates 10 million state transitions daily. Evaluate Express Workflows for high-volume scenarios, consolidate multiple Lambda calls into fewer states using Parallel or SDK direct integrations, and consider whether every conditional check needs to be a Choice state or can be handled within a Lambda function.' },
    { title: 'Blocking on synchronous integrations unnecessarily', description: 'Using Lambda Request-Response integrations for long-running tasks means Lambda is invoked and immediately returns, but the Step Functions state machine idles consuming time while waiting for external completion. Use optimistic .sync integrations for supported services and the waitForTaskToken pattern for custom integrations to allow the workflow to suspend without polling, reducing cost and improving reliability for tasks that take minutes to hours.' },
    { title: 'Insufficient error specificity in Catch blocks', description: 'Catch blocks that catch all errors (States.ALL) can mask unexpected failures, sending them through a recovery path designed for known errors. Define specific error names in Catch blocks to handle expected failures like validation errors or resource not found, and add a final catch-all that routes truly unexpected errors to an alert or dead-letter process for investigation rather than silently recovering.' },
  ],
  keyQuestions: [
    { q: 'What are the tradeoffs between Standard and Express Workflows?', a: 'Standard Workflows provide exactly-once execution semantics at the orchestration level, retain full execution history for 90 days, support durations up to one year, and are priced per state transition at approximately $0.025 per 1000 transitions. They are best for long-running business processes, human approval flows, and any workflow where an audit trail of every step is required. Express Workflows provide at-least-once execution with durations capped at 5 minutes, no retained execution history beyond CloudWatch Logs, higher throughput up to 100,000 executions per second, and lower cost priced by duration and number of executions rather than transitions. The critical implication of at-least-once semantics is that all task states must be idempotent because they may execute more than once. Synchronous Express Workflows add the ability to return results directly to the caller, making them suitable for backing REST API endpoints with complex multi-step logic.' },
    { q: 'How do you handle partial failures in a Parallel state?', a: 'A Parallel state in Step Functions executes all branches concurrently and succeeds only when all branches complete successfully. If any branch fails and the error is not caught within that branch, the entire Parallel state fails with a States.BranchFailed error, and all other running branches are canceled. To handle partial failures gracefully, add Catch blocks within individual branches to handle branch-specific errors and return structured error information in the output rather than propagating the exception. For compensation logic, catch the States.BranchFailed error on the Parallel state itself and invoke rollback steps for the branches that succeeded before the failure. The key design principle is deciding upfront whether partial success is acceptable or whether any branch failure should trigger full rollback.' },
    { q: 'How do you implement the saga pattern with Step Functions?', a: 'The saga pattern ensures consistency across distributed services by pairing each forward action with a compensating transaction that undoes it. In Step Functions, implement this by defining each service call as a Task state with a Catch block that invokes the compensating transaction when that step fails. Chain compensating transactions in reverse order using a dedicated failure path in the state machine. For example, an order processing saga might reserve inventory, charge payment, then confirm shipment; if payment fails, the Catch block releases the inventory reservation before propagating the failure. Use the ResultPath field to preserve both the original input and error information so compensation steps have the context needed to undo previous actions. This pattern trades distributed ACID transactions for application-level eventual consistency with defined rollback semantics.' },
    { q: 'When would you use the callback pattern with taskToken?', a: 'The taskToken callback pattern is essential when a workflow step depends on work that cannot be polled, has an unpredictable duration, or requires human interaction. Common use cases include human approval steps where a manager must review an action before the workflow continues, third-party payment processors that deliver results via webhook rather than synchronous response, legacy system integrations that process requests asynchronously and call back when complete, and long-running external jobs where polling would be inefficient or not supported. The pattern works by including the task token in the message sent to the external system, pausing the workflow execution, and only resuming when SendTaskSuccess or SendTaskFailure is called with the matching token. The workflow can wait indefinitely with a configurable heartbeat timeout that fails the task if the external system does not check in within the specified interval, preventing workflows from hanging forever.' },
  ],
  references: [
    'https://docs.aws.amazon.com/step-functions/latest/dg/',
    'https://docs.aws.amazon.com/step-functions/latest/dg/concepts-standard-vs-express.html',
    'https://docs.aws.amazon.com/step-functions/latest/dg/connect-to-resource.html',
  ],
},
{
  id: 'aws-eventbridge',
  title: 'EventBridge — Serverless Event Bus',
  icon: 'activity',
  color: '#f59e0b',
  description: 'AWS EventBridge for event-driven architectures with routing, filtering, and transformation.',
  introduction: 'AWS EventBridge is a serverless event bus that enables event-driven architectures by routing events between AWS services, custom applications, and SaaS providers. It decouples event producers from consumers through rules that filter and route events to targets, supporting sophisticated event patterns, schema discovery, and transformation. EventBridge Pipes enable point-to-point integrations with filtering and enrichment, while EventBridge Scheduler handles both one-time and recurring scheduling with precise timing guarantees.',
  whenToUse: 'When building loosely coupled, event-driven systems where services need to react to state changes in other services without direct coupling. EventBridge excels at cross-service event routing, SaaS integration via partner event buses, audit and compliance pipelines, real-time application monitoring, and triggering automation in response to AWS service events like EC2 state changes or CodePipeline completions. It is the standard choice for building event-driven microservice architectures on AWS.',
  keyConcepts: [
    { title: 'Event Buses', description: 'EventBridge provides three types of event buses: the default bus receives events from AWS services automatically; custom buses receive events from your applications and can be shared cross-account; and partner buses receive events from SaaS providers like Salesforce, Zendesk, and Auth0 through pre-built integrations. Each bus has independent rules, and events are only delivered to rules on the bus they are published to.' },
    { title: 'Rules and Event Patterns', description: 'Rules define which events are routed to which targets using event pattern matching against the event JSON structure. Patterns support exact matching, prefix matching, suffix matching, numeric ranges, IP address CIDR matching, and complex boolean logic with $or and $and operators. A single rule can route matching events to up to five targets simultaneously, with optional input transformation applied per target.' },
    { title: 'Schema Registry', description: 'The Schema Registry automatically discovers and catalogs the structure of events flowing through your event buses. It generates code bindings in Java, Python, and TypeScript that provide typed event objects, enabling IDE autocompletion and compile-time validation. Schema versioning tracks changes over time and downstream consumers can subscribe to schema changes, making the registry a living contract between event producers and consumers.' },
    { title: 'EventBridge Pipes', description: 'Pipes create point-to-point integrations between a source and a target with optional filtering, enrichment, and transformation steps in between. Sources include SQS, Kinesis, DynamoDB Streams, and MSK; targets include Lambda, Step Functions, API Gateway, and more. Pipes handle polling and batching for streaming sources, apply filter criteria to reduce unnecessary processing, optionally call a Lambda or API for enrichment, and transform the event structure before delivery.' },
    { title: 'Archive and Replay', description: 'EventBridge can archive all events on a custom bus or a filtered subset, retaining them for a configurable period or indefinitely. Archived events can be replayed to re-process them through current rules, enabling event sourcing patterns, debugging historical issues by replaying events through updated routing rules, and recovering from processing errors without re-triggering upstream producers.' },
    { title: 'EventBridge Scheduler', description: 'Scheduler provides a fully managed scheduling service supporting cron expressions, rate expressions, and one-time schedules with sub-minute precision. Unlike CloudWatch Events-based scheduling, Scheduler guarantees at-least-once delivery with configurable retry policies, dead-letter queues for failed invocations, and a flexible time window for accepting executions within a range rather than at an exact second.' },
  ],
  approach: [
    { step: 'Design event schemas as first-class contracts', details: 'Define event schemas before implementing producers and consumers. Use the Schema Registry to publish versioned schemas that document the detail-type, source, and JSON structure of each event. Treat schema changes as API versioning — add new fields without removing old ones for backward compatibility, and increment the schema version for breaking changes. Generate and distribute code bindings to consuming teams to eliminate manual JSON parsing.' },
    { step: 'Use input transformers to normalize event shapes', details: 'Targets often expect specific payload formats that differ from the native EventBridge event envelope. Use Input Transformers to extract fields from the event and construct a custom JSON payload before delivery. This keeps consumers decoupled from EventBridge\'s event structure and prevents producers from needing to format their events for specific consumer expectations. Define transformation templates using JSONPath expressions to extract nested fields.' },
    { step: 'Implement cross-account event routing for isolation', details: 'For organizations with multiple AWS accounts, use resource-based policies on custom event buses to allow specific source accounts to publish events. Configure rules in the central event bus account to route events to targets in the appropriate accounts. This pattern creates a hub-and-spoke event mesh where workload accounts publish domain events and the central bus routes them to subscribers across the organization without requiring direct account-to-account coupling.' },
    { step: 'Monitor delivery failures with DLQ and metrics', details: 'EventBridge retries failed target invocations with exponential backoff for up to 24 hours. Configure a Dead Letter Queue on rules to capture events that exhaust all retry attempts. Enable EventBridge Pipes\' built-in metrics and set CloudWatch alarms on FailedInvocations, ThrottledRules, and MatchedEvents metrics. For critical event flows, implement a canary pattern with a test rule that routes a sample of events to a logging target to verify event delivery independent of the primary target.' },
  ],
  pitfalls: [
    { title: 'Confusing EventBridge with SNS and SQS', description: 'EventBridge, SNS, and SQS serve different purposes. EventBridge is for event routing based on content matching, schema discovery, and integrating with AWS services and SaaS providers. SNS is for pub-sub fan-out to many subscribers with simple filtering. SQS is for reliable point-to-point queuing with visibility timeout and DLQ. EventBridge does not guarantee ordered delivery and is not appropriate for high-throughput message queuing; use SQS for reliable queue semantics and Kinesis for ordered high-volume streaming.' },
    { title: 'Overlooking the 5-target rule limit', description: 'A single EventBridge rule can route events to a maximum of five targets. For fan-out to many consumers, configure one target as an SNS topic that itself fans out to multiple subscribers, or route to an SQS queue that multiple consumers poll. Alternatively, use separate rules with the same event pattern, each targeting different consumers, accepting that each rule evaluation happens independently.' },
    { title: 'Not planning for replay on schema changes', description: 'When event schemas change, archived events in the old format cannot be replayed successfully against consumers expecting the new format. Before changing event schemas, version the schema in the Registry, update consumers to handle both old and new formats using lenient parsing, and test archive replay in a non-production environment. Establish a schema deprecation policy that maintains backward compatibility for at least one major version cycle.' },
  ],
  keyQuestions: [
    { q: 'How do you choose between EventBridge, SNS, and SQS?', a: 'The choice depends on the communication pattern and requirements. Use SQS when you need reliable, ordered queuing with consumer-controlled polling, visibility timeout for at-least-once processing, and dead-letter handling for poison messages — it is the right choice for decoupling a producer from a single consumer with backpressure support. Use SNS when you need to fan out a notification to multiple subscribers simultaneously with lightweight filtering on message attributes; SNS is optimized for high-throughput broadcast to heterogeneous subscriber types including Lambda, SQS, HTTP endpoints, and mobile push. Use EventBridge when you need content-based routing against the full event payload structure, integration with AWS service events or SaaS partners, schema discovery and registry, cross-account event routing, or the ability to archive and replay events. EventBridge is more expensive per event than SNS but provides significantly richer routing and integration capabilities.' },
    { q: 'How do you set up cross-account event routing in EventBridge?', a: 'Cross-account routing in EventBridge uses resource-based policies on custom event buses. In the receiving account, create a custom event bus and attach a resource-based policy that allows PutEvents from specific source account IDs or organization paths using AWS Organizations. In the sending account, create an EventBridge rule with a target pointing to the custom event bus ARN in the receiving account, granting the EventBridge service permission to call PutEvents cross-account. The receiving account\'s custom bus then processes the events through its own rules and routes them to local targets. For organization-wide patterns, use a central event bus account as a hub and configure spoke accounts to forward specific event types to it, enabling centralized monitoring, compliance logging, and cross-service event correlation without direct account-to-account coupling.' },
    { q: 'How do you use EventBridge archive and replay for event sourcing?', a: 'EventBridge archive captures events published to a bus based on an optional filter pattern, retaining them for a configured retention period. For event sourcing, configure an archive on the custom bus with no filter to capture all domain events, effectively creating an append-only event log. When a downstream consumer has a bug, gets deployed with incorrect routing, or a new consumer needs to catch up on historical state, replay the archive from a specific point in time through the current bus rules. The replay uses the original event timestamps, allowing consumers to reconstruct state as of any historical point. This pattern is particularly powerful for rebuilding read models in CQRS architectures or auditing historical event flows. Set archive retention to match your business requirements for event history rather than defaulting to the minimum.' },
    { q: 'When should you use EventBridge Pipes instead of a Lambda function for event transformation?', a: 'EventBridge Pipes provides a fully managed point-to-point integration pipeline with built-in filtering, optional enrichment, and transformation without requiring custom Lambda code for the glue logic. Use Pipes when connecting a streaming source like SQS, Kinesis, or DynamoDB Streams to a target with filtering and transformation requirements but no complex business logic — Pipes handles batching, polling, retries, and scaling automatically. Pipes is also more cost-effective than Lambda for simple transformations since you avoid Lambda cold starts, function invocation costs, and operational overhead. Reserve Lambda for Pipes enrichment steps when the transformation requires external API calls, database lookups, complex conditional logic, or computation that cannot be expressed in the Pipes transformation language. The decision criteria are complexity and cost: simple filter-and-forward belongs in Pipes, while stateful or computationally complex transformations warrant Lambda.' },
  ],
  references: [
    'https://docs.aws.amazon.com/eventbridge/latest/userguide/',
    'https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-pipes.html',
    'https://docs.aws.amazon.com/scheduler/latest/UserGuide/',
  ],
},
{
  id: 'aws-sqs',
  title: 'SQS — Simple Queue Service',
  icon: 'package',
  color: '#f59e0b',
  description: 'AWS SQS for reliable message queuing and decoupling distributed services.',
  introduction: 'Amazon Simple Queue Service is a fully managed message queuing service that enables decoupling and scaling of microservices, distributed systems, and serverless applications. SQS eliminates the need to manage message broker infrastructure and guarantees at-least-once delivery with configurable retention and visibility timeout semantics. Understanding SQS queue types, consumer patterns, Lambda integration configuration, and failure handling is essential for building resilient event-driven architectures on AWS.',
  whenToUse: 'When decoupling producers from consumers in distributed systems where the producer and consumer should operate at independent rates. SQS is the right choice for workload buffering to absorb traffic spikes, reliable task queuing for background processing, ensuring at-least-once delivery for critical operations, and throttling the rate of processing on downstream services. Use SQS when message durability and delivery guarantees are more important than strict ordering, unless FIFO ordering is required.',
  keyConcepts: [
    { title: 'Standard vs FIFO Queues', description: 'Standard queues offer maximum throughput with at-least-once delivery and best-effort ordering. Messages may be delivered out of order and occasionally more than once, requiring idempotent consumers. FIFO queues guarantee exactly-once processing and strict first-in-first-out ordering within a message group, with throughput limited to 3000 messages per second with batching or 300 without. FIFO queues require a MessageGroupId for ordering and a MessageDeduplicationId for exactly-once semantics within a 5-minute deduplication interval.' },
    { title: 'Visibility Timeout', description: 'When a consumer receives a message, SQS makes it invisible to other consumers for the visibility timeout duration. If the consumer successfully processes and deletes the message within this period, the message is removed. If processing fails or times out, the message becomes visible again for reprocessing. The visibility timeout must be longer than the maximum expected processing time. Consumers should extend visibility timeout dynamically for long-running processing using ChangeMessageVisibility.' },
    { title: 'Dead Letter Queues', description: 'A DLQ is an SQS queue that receives messages which have exceeded the maximum receive count on the source queue. When a message fails processing more times than the configured maxReceiveCount, SQS routes it to the DLQ rather than discarding it. DLQs provide a safety net for poison pill messages, enable debugging of failed messages without blocking the main queue, and support redrive operations to replay DLQ messages back to the source queue after fixing the consumer bug.' },
    { title: 'Long Polling vs Short Polling', description: 'Short polling queries only a subset of SQS servers and returns immediately even when the queue is empty, resulting in empty responses and higher API costs. Long polling waits up to 20 seconds for messages to arrive before returning, querying all SQS servers in a single request. Long polling reduces empty responses, decreases cost, and lowers latency for new message arrival. Always configure WaitTimeSeconds to 20 for long polling in production consumers.' },
    { title: 'Lambda Event Source Mapping', description: 'Lambda can be triggered directly by SQS queues, with Lambda managing the polling infrastructure. Key configuration parameters include batch size (1-10000 messages), batching window (0-300 seconds Lambda waits to fill a batch before invoking), maximum concurrency (limits parallel Lambda invocations from this queue), bisect-on-error (splits a failed batch to isolate the bad message), and report-batch-item-failures (allows partial batch success reporting). Lambda scales the number of concurrent pollers based on queue depth.' },
    { title: 'Message Attributes and Extended Client', description: 'SQS message attributes are metadata key-value pairs attached to messages that can be used for filtering without parsing the body. The SQS Extended Client Library enables sending payloads larger than the 256KB SQS message limit by automatically storing the message body in S3 and sending a reference pointer in the SQS message. This is essential for workflows that process large documents, images, or data payloads through a queue-based pipeline.' },
  ],
  approach: [
    { step: 'Size visibility timeout to processing time plus buffer', details: 'Set the visibility timeout to the 99th percentile processing time plus a safety margin of at least 20%. For Lambda-triggered queues, set visibility timeout to six times the Lambda function timeout to account for retries. If processing time varies widely, implement dynamic visibility timeout extension by calling ChangeMessageVisibility with remaining processing time periodically during long operations. Setting visibility timeout too short causes messages to be processed multiple times; too long delays reprocessing after genuine failures.' },
    { step: 'Implement idempotent consumers for Standard queues', details: 'Standard SQS queues guarantee at-least-once delivery, meaning any message may be delivered more than once. Design consumers to be idempotent by checking whether the message has already been processed before acting. Common patterns include tracking processed message IDs in a DynamoDB table with conditional writes, using database upsert semantics rather than inserts, or designing operations that produce the same result regardless of how many times they execute. Never assume Standard queue messages arrive exactly once.' },
    { step: 'Configure DLQ with monitoring and redrive policy', details: 'Every production SQS queue should have a corresponding DLQ. Set maxReceiveCount based on how many retry attempts are appropriate for your use case — typically 3-5 for transient failures. Configure CloudWatch alarms on the DLQ ApproximateNumberOfMessagesVisible metric to alert when messages land in the DLQ, indicating systematic consumer failures. Use the SQS console\'s redrive capability to replay DLQ messages back to the source queue after deploying a consumer fix, and test the fix with a single message before bulk redriving.' },
    { step: 'Tune Lambda SQS trigger for throughput and cost', details: 'The Lambda event source mapping for SQS provides several levers for tuning. Increase batch size to reduce Lambda invocations and per-invocation overhead, improving throughput for workloads where Lambda startup cost dominates. Set a batching window to allow Lambda to accumulate messages for a full batch before invoking, reducing invocations further at the cost of slight latency. Configure maximum concurrency to prevent Lambda from scaling to a level that overwhelms downstream services. Enable report-batch-item-failures to allow a Lambda function to return only the message IDs that failed, allowing SQS to retry only those specific messages rather than the entire batch.' },
  ],
  pitfalls: [
    { title: 'Using Standard queues for order-sensitive operations', description: 'Standard SQS queues do not guarantee delivery order. Financial transactions, sequential workflow steps, and operations that must be applied in order will produce incorrect results if processed out of sequence on a Standard queue. Use FIFO queues with appropriate MessageGroupId values to group messages that must be ordered relative to each other, while allowing parallelism across different groups. Be aware that FIFO queues have lower throughput limits than Standard queues and plan capacity accordingly.' },
    { title: 'Setting visibility timeout shorter than processing time', description: 'When Lambda or consumer processing takes longer than the visibility timeout, SQS makes the message visible again while the original consumer is still processing it. This causes duplicate processing, data corruption in non-idempotent systems, and inflated cost from repeated Lambda invocations. Always measure actual processing latency at the 99th percentile and set visibility timeout well above this value, using dynamic extension via ChangeMessageVisibility for variable-duration processing tasks.' },
    { title: 'Not monitoring queue depth for scaling signals', description: 'Queue depth (ApproximateNumberOfMessagesVisible) is the primary indicator of consumer throughput issues. Without alarms on queue depth, backlogs can grow silently until consumers are hours or days behind. Configure CloudWatch alarms to trigger Auto Scaling of EC2-based consumers or to notify on-call when depth exceeds acceptable thresholds. For Lambda-triggered queues, monitor concurrent executions approaching reserved concurrency limits, which can stall queue processing.' },
  ],
  keyQuestions: [
    { q: 'What are the key tradeoffs between Standard and FIFO queues?', a: 'Standard queues provide near-unlimited throughput, at-least-once delivery, and best-effort ordering. They are appropriate for most workloads where ordering is not critical and consumers are idempotent. FIFO queues guarantee exactly-once processing within a 5-minute deduplication window and strict ordering within a message group, making them essential for financial transactions, inventory management, and any sequence-dependent operations. The tradeoffs are throughput — FIFO is limited to 3000 messages per second with batching versus virtually unlimited for Standard — and complexity, as FIFO requires explicit MessageGroupId and MessageDeduplicationId on each message. FIFO queues also cost approximately 50% more per million requests. The choice should be driven by whether duplicate or out-of-order processing would cause correctness issues in the consuming application, not by a preference for stronger guarantees.' },
    { q: 'How do you design a DLQ strategy for production SQS workloads?', a: 'An effective DLQ strategy starts with setting maxReceiveCount on the source queue to distinguish genuine failures from transient ones — typically 3 to 5 retries for recoverable errors. Create a dedicated DLQ for each source queue rather than sharing DLQs, so alarms and operational procedures can be tailored per queue. Configure a CloudWatch alarm on the DLQ\'s ApproximateNumberOfMessagesVisible metric to trigger immediately when messages arrive, as DLQ messages represent unprocessed failures requiring investigation. Implement a DLQ processor that logs message details including the approximate receive count and exception context to a structured log system for analysis. When fixing the consumer bug, use the SQS redrive feature to replay a sample of DLQ messages to verify the fix before bulk redriving, and monitor the source queue depth and DLQ size during redrive to ensure the fixed consumer is processing correctly.' },
    { q: 'How do you handle poison pill messages in SQS?', a: 'A poison pill is a message that consistently fails consumer processing, potentially blocking other messages in a FIFO queue or consuming concurrency in a Standard queue through repeated retries. The primary defense is the DLQ configured with maxReceiveCount to automatically route messages that fail repeatedly out of the main queue. For Lambda-triggered queues, enable bisect-on-error which splits a failed batch in half to isolate the specific bad message, and enable report-batch-item-failures to allow partial batch success so only the poison pill is retried. In consumer code, implement specific exception handling that can distinguish a malformed message that will never succeed from a transient infrastructure failure, and for unrecoverable messages, delete them from the queue programmatically while sending an alert with the message contents for investigation. Never let a consumer spin in an infinite loop processing a poison pill message.' },
    { q: 'How do you tune the Lambda SQS event source mapping for optimal performance?', a: 'Tuning the Lambda SQS event source mapping involves balancing throughput, cost, latency, and downstream protection. Batch size controls how many messages Lambda processes per invocation — larger batches reduce per-invocation overhead and cost but increase the blast radius of a single failure and extend the time before messages are acknowledged. Batching window allows Lambda to wait up to 300 seconds to accumulate a full batch, reducing invocations at the cost of processing latency, which is appropriate for near-real-time but not truly real-time workloads. Maximum concurrency provides a throttle on how many concurrent Lambda executions the SQS trigger spawns, protecting downstream services like databases from connection overload. Set maximum concurrency based on the downstream service\'s connection or request limits divided by the expected per-Lambda-invocation resource consumption. Enable report-batch-item-failures and implement the partial batch response pattern in Lambda so individual message failures do not cause the entire batch to be retried, significantly reducing redundant processing in workloads with occasional bad messages.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/',
    'https://docs.aws.amazon.com/lambda/latest/dg/with-sqs.html',
    'https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-dead-letter-queues.html',
  ],
  visualizations: [
    { title: 'SQS Standard vs FIFO', caption: 'Architecture of Standard and FIFO queues showing producer/consumer patterns, visibility timeout, and how messages route to a Dead Letter Queue on failure.', image: '/diagrams/cloud/aws-sqs-architecture.png' },
    { title: 'SQS vs SNS Fan-out Pattern', caption: 'SNS push fan-out to multiple SQS queues enabling independent worker pools to consume the same event at their own pace.', image: '/diagrams/cloud/aws-sqs-vs-sns.png' },
  ],
},
{
  id: 'aws-sns',
  title: 'SNS — Simple Notification Service',
  icon: 'activity',
  color: '#f59e0b',
  description: 'AWS SNS for pub-sub messaging and fan-out notification patterns.',
  introduction: 'Amazon Simple Notification Service is a fully managed pub-sub messaging service that decouples message producers from consumers through topics and subscriptions. SNS enables fan-out patterns where a single published message is delivered to multiple subscribers simultaneously, supporting Lambda, SQS, HTTP/S endpoints, email, SMS, and mobile push as subscription targets. SNS message filtering reduces unnecessary invocations by routing messages to subscribers based on message attributes, and FIFO topics provide ordered delivery with deduplication for order-sensitive fan-out.',
  whenToUse: 'When a single event needs to be delivered to multiple consumers simultaneously without the producer knowing or caring about subscriber count. SNS is ideal for system-wide notifications, fan-out to multiple SQS queues for parallel processing, mobile push notification delivery at scale, cross-service event notification in microservice architectures, and triggering multiple downstream workflows from a single upstream event. Choose SNS over EventBridge when you need simple pub-sub without content-based routing complexity.',
  keyConcepts: [
    { title: 'Topics and Subscription Types', description: 'SNS topics are named channels to which publishers send messages and subscribers receive them. Subscription endpoints support Lambda functions, SQS queues, HTTP/S endpoints, email addresses, SMS phone numbers, and mobile push via APNS (Apple) and FCM/GCM (Google). Each topic can have up to 12.5 million subscriptions. Lambda and SQS subscribers receive messages synchronously in the SNS message envelope; HTTP/S endpoints receive HTTP POST requests with the message as the body.' },
    { title: 'Message Filtering', description: 'Subscription filter policies allow subscribers to receive only messages matching specific attribute criteria. Filter policies are JSON documents specifying attribute names and matching conditions including exact string match, prefix, suffix, numeric ranges, and anything-but rules. Filtering happens in the SNS service layer, meaning filtered-out messages never reach the subscriber, reducing Lambda invocations, SQS message volume, and associated costs significantly in high-volume fan-out scenarios.' },
    { title: 'SNS to SQS Fan-Out Pattern', description: 'The fan-out pattern publishes a message to an SNS topic and delivers it to multiple SQS queues subscribed to that topic. Each SQS queue can be processed by a different consumer at its own rate and with independent DLQ and retry configurations. This pattern decouples producers from all downstream consumers, allows new consumers to be added by simply subscribing a new SQS queue, and ensures each consumer gets a reliable copy of every message with SQS delivery guarantees.' },
    { title: 'FIFO Topics', description: 'SNS FIFO topics provide strict message ordering and exactly-once delivery within a message group, similar to SQS FIFO queues. FIFO topics can only deliver to SQS FIFO queues as subscribers. Publishers specify a MessageGroupId to order messages within a group and an optional MessageDeduplicationId or enable content-based deduplication. FIFO topics are limited to 300 published messages per second, appropriate for lower-volume, order-sensitive fan-out scenarios like financial event streaming.' },
    { title: 'Dead Letter Queues for Subscriptions', description: 'SNS can be configured with a redrive policy at the subscription level, routing undeliverable messages to an SQS DLQ when all delivery attempts are exhausted. This differs from the topic-level DLQ and applies to individual subscriber delivery failures such as Lambda throttling, HTTP endpoint unavailability, or SQS queue access errors. Monitoring subscription DLQs is essential for detecting when a specific subscriber is consistently failing to receive messages.' },
    { title: 'Mobile Push and SMS', description: 'SNS supports mobile push notifications through platform application integrations with APNS for iOS, FCM for Android, and ADM for Amazon devices. Direct addressing targets individual device tokens for user-specific notifications; topic subscriptions broadcast to all registered devices. SMS delivery supports transactional messages (high priority, higher cost) and promotional messages (best-effort delivery). The SMS sandbox in new accounts requires verified phone numbers for testing before requesting production access.' },
  ],
  approach: [
    { step: 'Design message attribute schemas for filtering', details: 'Define message attributes as structured metadata attached to every published message. Design attribute schemas to match the filtering dimensions consumers will need — event type, severity level, tenant ID, region, or service name. Publish attributes consistently on every message even when values are default, as absent attributes do not match explicit filter conditions. Document the attribute schema alongside the topic definition so subscriber teams can write correct filter policies without needing to inspect message bodies.' },
    { step: 'Implement fan-out with SQS for reliable multi-consumer delivery', details: 'For workloads where multiple services must process each event, publish to an SNS topic and subscribe one SQS Standard or FIFO queue per consumer. This ensures each consumer has its own queue with independent depth, visibility timeout, DLQ, and scaling configuration. Adding new consumers requires only subscribing a new SQS queue without changing the producer. Each consumer can process at its own pace without head-of-line blocking from slow consumers affecting others, unlike direct SNS to Lambda fan-out where slow Lambdas can throttle on concurrency.' },
    { step: 'Configure subscription DLQs for every critical subscriber', details: 'SNS retries failed deliveries to Lambda and HTTP endpoints with exponential backoff for up to 23 days for default delivery policies. Configure a redrive policy with a DLQ on subscriptions where message loss is unacceptable, such as subscriptions delivering to financial processing queues or audit systems. Monitor subscription DLQ depth with CloudWatch alarms, and build operational runbooks for redriving DLQ messages back to the subscriber once the root cause of delivery failure is resolved.' },
    { step: 'Use cross-region SNS for disaster recovery notification', details: 'For critical notification flows, subscribe SQS queues in secondary regions to an SNS topic in the primary region to propagate events cross-region for DR consumers. Alternatively, publish the same event to SNS topics in multiple regions from the producer. Test cross-region delivery latency and failure behavior during simulated regional outages as part of DR drills, and document the expected delivery lag for cross-region subscriptions to set appropriate consumer timeout expectations.' },
  ],
  pitfalls: [
    { title: 'Publishing sensitive data to email or SMS subscriptions', description: 'SNS email and SMS subscriptions deliver message content in plaintext to external systems. Never publish messages containing PII, credentials, financial data, or internal system details to topics with email or SMS subscribers. For notification patterns that need to alert humans, publish a reference ID or summary to SNS and have the consumer look up full details from a secure API rather than embedding sensitive data in the notification payload itself.' },
    { title: 'Not accounting for SNS to Lambda throttling', description: 'When an SNS topic delivers directly to Lambda and Lambda is throttled due to concurrency limits, SNS retries the delivery for up to 6 hours using exponential backoff. During this retry window, Lambda may see duplicate invocations. For high-volume topics, use SNS to SQS to Lambda architecture where SQS buffers the message volume, the Lambda event source mapping controls concurrency, and SQS provides visibility into queue depth and backlog rather than relying on invisible SNS retry behavior.' },
    { title: 'Filtering on message body content instead of attributes', description: 'SNS filter policies only work on message attributes, not on the message body content. Teams sometimes publish structured JSON bodies intending to filter on nested fields and are surprised to find all messages delivered to all subscribers regardless of body content. Design your message schema to include key routing dimensions as message attributes alongside the body, or use EventBridge which supports content-based routing against the full event JSON payload.' },
  ],
  keyQuestions: [
    { q: 'How do you implement the fan-out pattern with SNS and SQS?', a: 'The SNS to SQS fan-out pattern publishes a message once to an SNS topic which delivers a copy to each subscribed SQS queue. Each SQS queue is consumed by a different service with its own processing logic, scaling behavior, and failure handling configuration. To implement this, create an SNS Standard or FIFO topic, create one SQS queue per consuming service, and subscribe each SQS queue to the topic ensuring the SQS access policy grants the SNS topic permission to call SendMessage on the queue. This pattern provides loose coupling — producers are unaware of consumers — independent scaling and reliability per consumer, and the ability to add new consumers by subscribing additional queues without any producer change. Use SNS message filtering on subscriptions to route only relevant messages to each queue, reducing processing costs for consumers that only care about a subset of published events.' },
    { q: 'How does SNS message filtering compare to using multiple topics?', a: 'SNS message filtering and multiple topics are both valid approaches to routing messages to specific subscribers, with different operational tradeoffs. Message filtering uses a single topic for all event variants with attributes differentiating them, and subscribers declare filter policies that specify which attribute values they care about. This simplifies the producer — one publish call covers all consumers — and makes it easy to add new subscriber segments without changing producer code. Multiple topics per event type provide explicit contracts, cleaner IAM policies per topic, and simpler debugging since all messages on a topic are by definition relevant to subscribers. The practical guidance is to use a single topic with filtering when events are variants of the same logical event type from one producer, and use multiple topics when event types are functionally distinct, come from different producers, or require different IAM access control boundaries.' },
    { q: 'What are the differences between SNS FIFO and SQS FIFO, and when do you combine them?', a: 'SNS FIFO topics provide ordered, deduplicated pub-sub delivery from one producer to multiple SQS FIFO queue subscribers simultaneously, while SQS FIFO queues provide ordered, deduplicated point-to-point queuing to a single consumer group. SNS FIFO can only deliver to SQS FIFO queues — not to Lambda or HTTP endpoints directly — making the combination the only way to achieve FIFO fan-out. Use SNS FIFO when you need multiple consumers to each receive an ordered, deduplicated copy of every event; for example, an order management system where both inventory service and billing service must process order events in the exact order they occurred. The combined throughput limit is 300 messages per second due to SNS FIFO constraints, making this pattern appropriate for lower-volume, order-critical workflows rather than high-throughput streaming. For high-volume ordered streaming, consider Kinesis Data Streams as an alternative.' },
    { q: 'How would you architect a cross-region notification system using SNS?', a: 'A cross-region SNS architecture depends on whether you need the same message delivered to subscribers in multiple regions or failover in case of a regional outage. For multi-region delivery of the same event, publish to SNS topics in each required region either by having producers publish to all regions simultaneously or by using a Lambda function subscribed to the primary region\'s topic that republishes to secondary region topics. For DR failover, configure your producer to detect the primary region\'s SNS availability and fail over to a secondary region topic when the primary is unavailable. Active-active architectures typically require consumers to be idempotent since the same message may be published to both regions. Monitor delivery success rates per region with CloudWatch metrics and use subscription DLQs in each region to capture delivery failures. For mobile push specifically, consider using a single global SNS topic since APNS and FCM delivery is region-independent, with regional SQS queues for server-side processing.' },
  ],
  references: [
    'https://docs.aws.amazon.com/sns/latest/dg/',
    'https://docs.aws.amazon.com/sns/latest/dg/sns-message-filtering.html',
    'https://docs.aws.amazon.com/sns/latest/dg/fifo-topic-code-examples.html',
  ],
  visualizations: [
    { title: 'SNS Fan-out with Filter Policies', caption: 'A single SNS topic fanning out to SQS queues, Lambda, HTTP webhooks, email, and SMS endpoints with subscription filter policies routing by message attribute.', image: '/diagrams/cloud/aws-sns-fanout.png' },
  ],
},
{
  id: 'aws-kinesis',
  title: 'Kinesis — Real-Time Data Streaming',
  icon: 'activity',
  color: '#f59e0b',
  description: 'AWS Kinesis for real-time data streaming, ingestion, and analytics at scale.',
  introduction: 'Amazon Kinesis is a family of services for real-time data streaming and analytics. Kinesis Data Streams provides durable, ordered, replayable streams for building real-time applications with multiple consumers. Kinesis Data Firehose offers a fully managed pipeline for loading streaming data into S3, Redshift, OpenSearch, and other destinations with optional transformation. Kinesis Data Analytics enables real-time SQL or Apache Flink processing on streaming data. Understanding the shard-based architecture, consumer patterns, and integration options is essential for designing high-throughput, low-latency data pipelines on AWS.',
  whenToUse: 'When processing high-volume real-time data streams that require ordering, multiple independent consumers, or replay capabilities. Kinesis is the right choice for clickstream analytics, log aggregation, IoT telemetry, financial transaction monitoring, real-time fraud detection, and any scenario where multiple consumers need to process the same stream of ordered records at different speeds. Choose Kinesis over SQS when you need ordered delivery, stream replay, multiple independent consumers reading at their own pace, or sub-second latency at high throughput.',
  keyConcepts: [
    { title: 'Shards and Partition Keys', description: 'A Kinesis Data Stream is divided into shards, each providing 1 MB/s write capacity and 2 MB/s read capacity. Records are assigned to shards using a partition key that is hashed to determine the shard. Records with the same partition key always go to the same shard, guaranteeing ordering for that key. Choosing partition keys is critical: high-cardinality keys distribute load evenly, while low-cardinality keys create hot shards that hit per-shard limits.' },
    { title: 'Enhanced Fan-Out', description: 'Standard consumers share the 2 MB/s per-shard read throughput across all consumers using GetRecords polling. Enhanced fan-out provides dedicated 2 MB/s throughput per consumer per shard using SubscribeToShard with HTTP/2 push delivery, eliminating competition between consumers and reducing end-to-end latency from seconds to typically under 70ms. Use enhanced fan-out when you have more than one consumer on a stream or when latency is critical.' },
    { title: 'Kinesis Data Firehose', description: 'Firehose is a fully managed service that loads streaming data into destinations including S3, Redshift, OpenSearch Service, Datadog, Splunk, and HTTP endpoints. Firehose handles buffering, compression, encryption, and delivery retries automatically. It can optionally invoke a Lambda function for record transformation before delivery. Key configuration parameters are buffer size (1-128 MB) and buffer interval (60-900 seconds) that control delivery frequency — whichever threshold is reached first triggers delivery.' },
    { title: 'Kinesis Client Library', description: 'The Kinesis Client Library is a Java library that handles the complexity of consuming from Kinesis streams: shard discovery, checkpointing consumed positions in DynamoDB, load balancing shards across multiple consumer instances, and handling shard splits and merges. KCL maintains a lease table in DynamoDB where each row represents a shard lease held by a consumer instance. While KCL simplifies consumer development, it adds DynamoDB read/write costs proportional to shard count.' },
    { title: 'Resharding Operations', description: 'Resharding adjusts the shard count to accommodate changes in data volume. Splitting a shard divides it into two child shards to increase capacity; merging two adjacent shards combines them to reduce cost. Resharding is non-disruptive but requires consumers to handle parent shard completion before processing child shards. The Kinesis Scaling Utility and UpdateShardCount API support automated resharding based on throughput metrics.' },
    { title: 'Kinesis Data Analytics with Apache Flink', description: 'Kinesis Data Analytics provides fully managed Apache Flink runtime for stateful stream processing with exactly-once semantics, windowed aggregations, joins between streams, and complex event processing. Applications are written in Java, Scala, or Python using the Flink API and deployed without managing Flink clusters. Use it for real-time aggregations, anomaly detection, stream enrichment from reference data, and routing records to different destinations based on stream content.' },
  ],
  approach: [
    { step: 'Design partition key strategy to avoid hot shards', details: 'Hot shards occur when a disproportionate volume of records hash to the same shard, hitting the 1 MB/s write limit and causing ThrottlingException errors. Use high-cardinality partition keys such as user ID, device ID, or request ID that distribute evenly across shards. For sources with inherently skewed keys, add a random suffix or prefix to the key to spread load, then strip it during processing. Monitor the IncomingBytes and IncomingRecords metrics per shard using the GetShardIterator API or CloudWatch Contributor Insights to identify uneven shard utilization.' },
    { step: 'Configure Lambda consumer for Kinesis streams correctly', details: 'Lambda can be configured as a Kinesis consumer via event source mapping. Key parameters include starting position (TRIM_HORIZON for all records or LATEST for new records only), batch size (1-10000 records), parallelization factor (1-10 concurrent Lambda invocations per shard for higher throughput), bisect-on-error (splits failed batches to isolate bad records), and maximum retry attempts. Set the destination for failed records to an SQS queue or S3 to preserve records that cannot be processed after exhausting retries rather than discarding them.' },
    { step: 'Implement checkpointing strategy for KCL consumers', details: 'KCL consumers checkpoint their position in the stream to DynamoDB so they can resume from the last processed record after a restart. Checkpoint after successfully processing each batch rather than per-record to reduce DynamoDB writes and improve throughput. Handle the case where processing succeeds but checkpointing fails — the consumer will reprocess records on restart, so ensure downstream operations are idempotent. Monitor the MillisBehindLatest metric to track how far behind the consumer is from the tip of the stream.' },
    { step: 'Size shards based on peak throughput requirements', details: 'Calculate the required shard count by dividing peak expected write throughput in bytes per second by 1,000,000 (1 MB/s per shard write limit), and dividing peak read throughput by 2,000,000 (2 MB/s per shard read limit) accounting for the number of consumers. Add 25-50% headroom for traffic spikes. Use Amazon Kinesis Scaling Utilities or implement CloudWatch alarms on PutRecord.Success and WriteProvisionedThroughputExceeded metrics to trigger automatic resharding via UpdateShardCount when utilization consistently exceeds 70-80% of capacity.' },
  ],
  pitfalls: [
    { title: 'Confusing Kinesis vs SQS for the wrong use case', description: 'Kinesis and SQS solve different problems. Kinesis is not a queue: it retains records for the retention period regardless of whether they have been consumed, supports multiple independent consumers each reading from their own position, and preserves ordering within a shard. SQS is a queue: messages are consumed once and deleted, there is typically one consumer group, and ordering is only guaranteed in FIFO mode. Using Kinesis for simple point-to-point task queuing adds unnecessary complexity and cost; using SQS when multiple consumers need to read the same records requires workarounds that should instead use Kinesis or SNS fan-out.' },
    { title: 'Not handling shard splits in consumers', description: 'When a shard is split into two child shards, consumers must finish reading all records from the parent shard before reading from the child shards to maintain ordering guarantees. KCL handles this automatically, but custom consumers built directly on the Kinesis API must implement this logic. Failing to handle shard lineage correctly results in out-of-order processing and incorrect state in consumers that depend on record ordering within a partition key.' },
    { title: 'Ignoring Firehose buffering latency', description: 'Kinesis Data Firehose buffers records before delivering them to destinations. The minimum buffer interval is 60 seconds, meaning records can arrive in destinations up to 60 seconds after production — not in real time. If your use case requires sub-second or near-real-time delivery to S3 or downstream systems, Firehose is not appropriate; use Kinesis Data Streams with a Lambda consumer or Flink application instead. Firehose is designed for near-real-time analytics where some delivery latency in exchange for operational simplicity is acceptable.' },
  ],
  keyQuestions: [
    { q: 'When do you choose Kinesis over SQS for real-time processing?', a: 'Kinesis is the right choice over SQS when you need multiple independent consumers to each process every record in the stream at their own pace, when record ordering matters within a logical grouping like user sessions or transactions from the same account, or when you need the ability to replay records by moving consumer positions backward in time within the retention window of 1 to 365 days. Kinesis also excels at very high throughput scenarios where records arrive faster than any single consumer can process and where the processing latency requirement is under one second. SQS is preferable when you need at-most-once or at-least-once delivery to a single consumer group, when messages should be deleted after consumption, when you need visibility timeout and DLQ semantics for task processing, or when throughput is lower and the simpler managed service model of SQS reduces operational overhead without needing Kinesis\'s streaming guarantees.' },
    { q: 'How do you calculate the right number of shards for a Kinesis stream?', a: 'Shard sizing requires understanding both ingestion rate and consumption rate. On the write side, each shard supports 1000 records per second and 1 MB/s of data. Calculate the required write shards by dividing your peak records per second by 1000 and your peak MB/s by 1, taking the larger value. On the read side, each standard consumer shares 2 MB/s per shard across all consumers; with enhanced fan-out, each consumer gets 2 MB/s dedicated per shard. For N standard consumers, you need to multiply the read throughput per consumer by N and divide by 2 MB/s per shard. Always add 25-50% headroom above calculated requirements to absorb traffic spikes without triggering ProvisionedThroughputExceededException errors. Monitor IncomingBytes and IncomingRecords CloudWatch metrics and set alarms at 80% of capacity to trigger resharding before hitting limits.' },
    { q: 'How do you handle hot partitions in Kinesis?', a: 'Hot partitions occur when a small number of partition keys generate a disproportionate share of the stream\'s data volume, exceeding the 1 MB/s write limit per shard. Detection involves monitoring the per-shard write metrics using CloudWatch Contributor Insights, which shows the top partition keys by record count and byte volume. Mitigation strategies include randomizing the partition key by appending a random number suffix to spread records across more shards for keys that do not require strict ordering, then reading from all suffixed partitions in consumers to reassemble the logical stream. For time-series data where the natural key is a timestamp, use a combination of device ID and a bucketed timestamp to balance distribution. For cases where load spikes are predictable, pre-provision additional shards before the expected spike using UpdateShardCount, and scale down after the spike to reduce cost.' },
    { q: 'How do you use Kinesis Firehose with Lambda transformation to deliver to S3?', a: 'Kinesis Data Firehose can invoke a Lambda function to transform records before delivering them to S3, enabling format conversion, filtering, enrichment, and routing logic without requiring a separate consumer application. The Lambda function receives a batch of records in base64-encoded format and must return each record with a result of Ok, Dropped, or ProcessingFailed. Ok records are included in the delivery; Dropped records are excluded; ProcessingFailed records are sent to an S3 error prefix for inspection. A common pattern is using the transformation Lambda to convert raw JSON events to Parquet or ORC format using the Kinesis Firehose record format conversion feature, which uses AWS Glue schemas to produce analytics-optimized columnar files. Configure the buffer size and interval on the Firehose delivery stream based on your target file size for S3 — larger files are more cost-efficient for Athena and EMR queries — typically 128 MB or 5-15 minute intervals. Ensure the Lambda transformation function has a timeout longer than the Firehose invocation timeout and handles base64 decoding and encoding of records correctly.' },
  ],
  references: [
    'https://docs.aws.amazon.com/streams/latest/dev/',
    'https://docs.aws.amazon.com/firehose/latest/dev/',
    'https://docs.aws.amazon.com/kinesisanalytics/latest/java/',
  ],
  visualizations: [
    { title: 'Kinesis Data Streams Architecture', caption: 'Multiple producers writing to shards via partition key, with KCL consumers, Lambda event source mappings, Firehose, and Kinesis Data Analytics all reading the stream.', image: '/diagrams/cloud/aws-kinesis-architecture.png' },
    { title: 'Kinesis vs SQS — Decision Guide', caption: 'Side-by-side comparison of when to choose Kinesis (replay, ordered, high-throughput, time-series) versus SQS (task queue, delete-after-process, variable load).', image: '/diagrams/cloud/aws-kinesis-vs-sqs.png' },
  ],
},
{
  id: 'aws-sam',
  title: 'SAM — Serverless Application Model',
  icon: 'package',
  color: '#f59e0b',
  description: 'AWS SAM for defining, building, and deploying serverless applications as infrastructure as code.',
  introduction: 'The AWS Serverless Application Model is an open-source framework that simplifies defining and deploying serverless applications through a higher-level CloudFormation extension. SAM templates use shorthand resource types like AWS::Serverless::Function and AWS::Serverless::Api that expand to the underlying CloudFormation resources during deployment. The SAM CLI provides commands for building, testing locally, deploying, and observing serverless applications, with SAM Accelerate enabling rapid iterative development through hot-swap deployments that update Lambda function code without full CloudFormation stack updates.',
  whenToUse: 'When building serverless applications primarily using Lambda, API Gateway, DynamoDB, and Step Functions and you want a streamlined IaC experience tailored to serverless patterns. SAM is particularly effective for teams new to serverless who benefit from its opinionated defaults, built-in local testing capabilities, and simple deployment workflow. Choose SAM over raw CloudFormation for serverless workloads to reduce template verbosity, and consider CDK for applications where programmatic constructs, custom provisioning logic, or non-serverless resources are equally prominent.',
  keyConcepts: [
    { title: 'Serverless Resource Types', description: 'SAM extends CloudFormation with five higher-level resource types: AWS::Serverless::Function creates a Lambda function with associated IAM role, CloudWatch log group, and optional event source mappings from a concise definition. AWS::Serverless::Api creates an API Gateway REST API with optional usage plans and Cognito authorizers. AWS::Serverless::SimpleTable creates a DynamoDB table with a single hash key. AWS::Serverless::StateMachine creates a Step Functions state machine. AWS::Serverless::HttpApi creates an HTTP API (API Gateway v2) with lower latency and cost than REST APIs.' },
    { title: 'Globals Section', description: 'The Globals section in a SAM template defines default property values that apply to all resources of a specified type, avoiding repetition across many functions. For example, setting a global Timeout, MemorySize, Runtime, Environment, and Tracing applies those values to every AWS::Serverless::Function in the template unless overridden at the function level. This reduces boilerplate and ensures consistent configuration across all functions in the application.' },
    { title: 'SAM CLI Build and Deploy', description: 'The sam build command compiles Lambda function code, installs dependencies, and creates a build artifact in .aws-sam/build. The sam deploy command packages the built artifacts to S3 and deploys the CloudFormation stack, accepting parameters for environment-specific configuration. The --guided flag on first deploy creates a samconfig.toml file that saves deployment parameters for subsequent runs. SAM also supports sam package for creating deployable artifacts without deploying, useful for CI/CD pipelines.' },
    { title: 'Local Testing with sam local', description: 'SAM CLI provides sam local invoke to test Lambda functions locally using Docker containers that replicate the Lambda execution environment. sam local start-api runs a local API Gateway emulator that routes HTTP requests to local Lambda invocations, enabling end-to-end API testing without deploying to AWS. sam local start-lambda exposes a local Lambda endpoint compatible with the AWS SDK, allowing services to invoke local functions during integration testing. These capabilities significantly accelerate the development feedback loop.' },
    { title: 'SAM Accelerate and sam sync', description: 'SAM Accelerate (sam sync) provides rapid deployment for iterative development by detecting changes and applying only the minimal update. For Lambda function code changes, sam sync updates the function code directly without a CloudFormation deployment, reducing update time from minutes to seconds. Infrastructure changes that require CloudFormation are detected and deployed via a stack update. The --watch flag enables continuous sync that monitors the filesystem and automatically deploys changes as files are saved.' },
    { title: 'Policy Templates', description: 'SAM provides a library of predefined policy templates that grant Lambda functions common permissions with minimal IAM configuration. For example, S3ReadPolicy grants read access to a specific S3 bucket, DynamoDBCrudPolicy grants full CRUD access to a DynamoDB table, SQSPollerPolicy grants permissions to poll and delete messages from an SQS queue, and SNSPublishMessagePolicy grants permission to publish to an SNS topic. Policy templates reduce the need to write verbose IAM policy documents for standard access patterns.' },
  ],
  approach: [
    { step: 'Structure SAM applications with nested stacks for modularity', details: 'For large serverless applications, use SAM\'s support for nested applications via AWS::Serverless::Application resource type or CloudFormation nested stacks to split templates into logical domains. Each nested stack can be developed and deployed independently while being referenced from a parent stack. This prevents hitting CloudFormation resource limits in large applications, enables team ownership of individual stack segments, and allows reuse of common infrastructure patterns like authentication or logging stacks across multiple applications.' },
    { step: 'Use SAM Pipelines for CI/CD integration', details: 'SAM Pipelines generates CI/CD pipeline configuration for GitHub Actions, CodePipeline, Bitbucket Pipelines, GitLab CI, or Jenkins. Run sam pipeline bootstrap to provision the necessary IAM roles, S3 buckets, and ECR repositories per environment, then sam pipeline init to generate the pipeline definition file. The generated pipelines implement the recommended two-phase deploy pattern: build once and deploy the same artifact to staging, run automated tests, then promote to production on approval without rebuilding.' },
    { step: 'Parameterize environments with sam deploy parameters', details: 'Define CloudFormation Parameters in the SAM template for values that differ between environments such as database connection strings, API keys stored in Parameter Store references, or environment-specific feature flags. Pass environment-specific values via --parameter-overrides during sam deploy or store them in samconfig.toml with environment-specific profiles. Reference AWS Systems Manager Parameter Store and AWS Secrets Manager values in templates using dynamic references to avoid storing secrets in template files or deployment pipelines.' },
    { step: 'Configure event source types for Lambda triggers declaratively', details: 'SAM supports a rich set of event source types in the Events property of a function: Api and HttpApi for API Gateway triggers, SQS for queue triggers with batch size and batching window, DynamoDB for stream triggers, Kinesis for stream triggers, Schedule for EventBridge scheduled rules, EventBridgeRule for custom event patterns, S3 for object events, and SNS for topic subscriptions. Using SAM event source declarations creates the trigger, the necessary IAM permissions, and the event source mapping resource automatically, eliminating the need to manually wire these resources.' },
  ],
  pitfalls: [
    { title: 'Using SAM for primarily non-serverless infrastructure', description: 'SAM is optimized for Lambda, API Gateway, DynamoDB, and Step Functions. Using SAM templates for applications dominated by EC2, ECS, RDS, or complex VPC networking adds friction because SAM lacks higher-level abstractions for those resources, requiring verbose raw CloudFormation definitions. In those cases, AWS CDK with its L2 and L3 constructs provides much richer abstractions for non-serverless resources, or Terraform may provide better multi-provider support. Choose the tool that matches the dominant resource type in your application.' },
    { title: 'Running sam deploy without --guided in new environments', description: 'Deploying to a new AWS account or region without the --guided flag skips the interactive configuration that creates samconfig.toml with the correct S3 bucket, region, stack name, and parameter values. Deploying with incorrect or missing configuration can create stacks in unintended regions, use deployment S3 buckets that do not exist, or deploy with missing parameter values causing CloudFormation rollback. Always run sam deploy --guided for the first deployment to any new environment and commit the resulting samconfig.toml to source control.' },
    { title: 'Not testing Lambda locally before deploying', description: 'Skipping local testing with sam local invoke and sam local start-api forces a full cloud deployment cycle for every code change, dramatically slowing development iteration. More critically, not testing locally means type errors, missing environment variables, and incorrect event format assumptions are only discovered after deployment, which may impact production if a staging environment is not used. Invest in local test event JSON files representing real Lambda payloads and include sam local invoke in the development workflow before every push.' },
  ],
  keyQuestions: [
    { q: 'How do SAM and CDK compare for serverless applications?', a: 'SAM and CDK serve different developer preferences and use cases. SAM uses YAML-based templates with serverless-specific resource types and a CLI designed around serverless development workflows including local testing, making it accessible to developers without deep programming language expertise in IaC. CDK uses TypeScript, Python, Java, or other languages to define infrastructure programmatically, enabling loops, conditionals, abstractions, and reusable constructs that are difficult to express in YAML templates. CDK provides L2 constructs for a wide range of AWS services beyond serverless, making it superior for applications mixing serverless and non-serverless resources. SAM is better when the team is primarily backend developers who prefer configuration over code, when the application is purely serverless, and when the local testing workflow of sam local is valuable. CDK is better for complex infrastructure requiring programmatic abstractions, large teams benefiting from typed APIs and IDE autocompletion, or applications requiring sophisticated multi-service architectures.' },
    { q: 'How do you test Lambda functions locally with SAM before deploying to AWS?', a: 'SAM CLI provides several local testing mechanisms using Docker to replicate the Lambda execution environment. sam local invoke runs a single function invocation with a test event passed via a JSON file or stdin, returning the function output and logs to the terminal — ideal for unit testing function behavior against specific event payloads. sam local start-api starts a local HTTP server that emulates API Gateway and routes requests to the appropriate Lambda function handlers based on the SAM template definitions, enabling full end-to-end API testing without cloud deployment. For environment variables referenced in the template, pass a JSON file of overrides using the --env-vars flag. For functions requiring AWS service access, use aws-local or mock services like DynamoDB Local and S3-compatible endpoints to avoid cloud calls during development. Create a directory of test event JSON files representing each integration scenario and include sam local invoke commands in your development README and CI pipeline for pre-deploy validation.' },
    { q: 'How do SAM policy templates simplify IAM configuration?', a: 'SAM policy templates are predefined CloudFormation IAM policy snippets that grant common permission sets to Lambda functions with a single line declaration rather than a full IAM policy document. For example, specifying DynamoDBCrudPolicy with a TableName parameter expands to the five DynamoDB actions needed for full CRUD access to that specific table, applying least-privilege scoping automatically. S3ReadPolicy expands to GetObject and ListBucket permissions scoped to the specified bucket. This reduces IAM configuration errors, enforces least privilege by scoping permissions to specific resource ARNs, and makes the function\'s required permissions self-documenting in the template. When no matching policy template exists for a required permission set, you can still define custom inline IAM policies directly on the function, and both approaches can be combined in the same function definition.' },
    { q: 'How do you deploy a SAM application to multiple environments?', a: 'Deploying to multiple environments with SAM uses a combination of CloudFormation parameters, samconfig.toml profiles, and CI/CD pipeline stages. Define environment-specific values as CloudFormation Parameters in the template with AllowedValues constraints for safety, and reference shared secrets via AWS::SSM::Parameter::Value dynamic references rather than hardcoding them. In samconfig.toml, create separate deployment profiles for each environment using TOML table headers like default.deploy.parameters and prod.deploy.parameters with environment-specific stack names, S3 buckets, regions, and parameter overrides. In CI/CD, use sam deploy --config-env staging for the staging stage and --config-env prod for the production stage after automated tests pass. Use IAM permission boundaries on the deployment roles for each environment to prevent developers from escalating privileges through infrastructure changes, and require pull request approval for production deployments by restricting the production deployment role to the CI/CD system only.' },
  ],
  references: [
    'https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/',
    'https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-local-invoke.html',
    'https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/serverless-policy-templates.html',
  ],
},
{
  id: 'aws-app-runner',
  title: 'App Runner — Managed Container Service',
  icon: 'box',
  color: '#f59e0b',
  description: 'AWS App Runner for deploying containerized web applications without managing infrastructure.',
  introduction: 'AWS App Runner is a fully managed service that builds and deploys containerized web applications and APIs from source code repositories or container images, handling all infrastructure provisioning, load balancing, scaling, and certificate management automatically. App Runner abstracts away the operational complexity of ECS, ALB, Auto Scaling groups, and VPC networking while still providing production-grade reliability and scalability. It is positioned between the simplicity of Lambda and the full control of ECS Fargate, making it ideal for teams that want to deploy containers without becoming AWS infrastructure experts.',
  whenToUse: 'When you need to deploy a containerized web application or API quickly without configuring load balancers, ECS task definitions, VPC subnets, or security groups. App Runner is well-suited for startups, small teams, and internal applications where developer velocity is more important than fine-grained infrastructure control. It is also effective for migrating existing containerized applications from other cloud providers or Heroku-like platforms, and for deploying web APIs backed by serverless functions where the Lambda 15-minute timeout or cold start behavior is a concern.',
  keyConcepts: [
    { title: 'Source Types and Deployment Triggers', description: 'App Runner supports two source types: ECR for pre-built container images from Elastic Container Registry, and GitHub via App Runner connections for source code that App Runner builds automatically using Buildpacks or a provided Dockerfile. ECR sources support both public and private repositories with configurable automatic deployment on new image pushes. GitHub sources trigger automatic deployments on new commits to the configured branch. Manual deployment trigger is also available for cases where automatic deployment should be gated by CI/CD approval.' },
    { title: 'Auto-Scaling Configuration', description: 'App Runner auto-scales the number of provisioned container instances based on concurrent requests. The auto-scaling configuration specifies minimum instances (always running, preventing cold starts), maximum instances (cap on scaling), and max concurrency (requests per instance before adding another instance). Setting minimum to zero reduces cost for low-traffic applications by scaling to zero during idle periods, though the first request after scale-to-zero incurs a cold start of several seconds as the instance starts.' },
    { title: 'VPC Connector', description: 'By default, App Runner instances cannot access private VPC resources. A VPC Connector attaches App Runner instances to specified VPC subnets and security groups, enabling private connectivity to RDS databases, ElastiCache clusters, internal ALBs, and other VPC-private resources. The connector creates VPC endpoints in your subnets and routes outbound traffic from App Runner through your VPC. Multiple services can share a VPC Connector, and the connector must be in the same region as the App Runner service.' },
    { title: 'Health Checks', description: 'App Runner performs health checks on running instances to detect unhealthy containers and replace them automatically. HTTP health checks send a GET request to a configurable path and expect a 2xx response; TCP health checks verify the port is accepting connections. Configurable parameters include path, interval (5-20 seconds), timeout, healthy threshold, and unhealthy threshold. The health check path should verify the application is ready to serve traffic, including any required database connectivity checks.' },
    { title: 'Observability with X-Ray and CloudWatch', description: 'App Runner integrates with AWS X-Ray for distributed request tracing when the X-Ray daemon is running in the container or when using the X-Ray SDK. CloudWatch automatically receives service-level metrics including RequestCount, Latency (p50, p75, p90, p95, p99), HTTPStatusCode2xx/4xx/5xx, ActiveInstances, and CPUUtilization. Application logs written to stdout and stderr are automatically captured to CloudWatch Logs. Configure CloudWatch alarms on Latency and error rate metrics for production monitoring.' },
    { title: 'Custom Domains and TLS', description: 'App Runner provides a default HTTPS endpoint at a subdomain of awsapprunner.com with TLS managed automatically. Custom domains are configured by associating a domain name with the service, and App Runner provides CNAME or alias records to configure in your DNS provider. App Runner handles TLS certificate provisioning and renewal via AWS Certificate Manager automatically. SSL/TLS termination happens at the App Runner service level before traffic reaches the container instances.' },
  ],
  approach: [
    { step: 'Configure auto-scaling based on traffic patterns', details: 'Size the auto-scaling configuration based on expected traffic patterns and cost tolerance. Set minimum instances to 1 for services requiring zero cold-start latency, accepting the baseline cost of always-on instances. Set to 0 for development or low-traffic services where occasional cold starts are acceptable. Determine max concurrency by load testing the container under realistic workloads to find the requests-per-instance ceiling before response times degrade; set max concurrency below this ceiling. Set maximum instances based on cost constraints and downstream service capacity limits such as database connection pools.' },
    { step: 'Use VPC Connector for database and cache connectivity', details: 'Create a VPC Connector in the same VPC as your RDS or ElastiCache resources, specifying at least two subnets in different availability zones for high availability. Assign a security group to the connector that is referenced as the source in the database or cache security group inbound rules, rather than using IP CIDR ranges which change as App Runner scales. After attaching the connector, configure the application\'s database connection string using the RDS private DNS name or ElastiCache endpoint, and verify connectivity with a health check endpoint that tests the database connection before the service is considered healthy.' },
    { step: 'Implement rolling deployments with health checks', details: 'App Runner performs rolling deployments by starting new instances of the updated container, waiting for them to pass health checks, and then replacing old instances. Configure the health check path to return a non-200 response until the application is fully initialized — this prevents traffic from routing to instances that are starting up but not yet ready. For blue-green deployment patterns, consider using two App Runner services with weighted DNS routing via Route 53, shifting traffic gradually from the old to new version and reverting instantly by updating DNS weights if the new version shows elevated error rates.' },
    { step: 'Build and push images via CI/CD to ECR for deployment', details: 'For production deployments, build container images in a CI/CD pipeline, push to ECR, and rely on App Runner\'s automatic deployment trigger on new image pushes. Tag images with the commit SHA in addition to latest so deployments are traceable to specific code versions. Configure the App Runner service to watch for new image pushes on the production tag. For environments where deployments should be gated by approval, use the manual deployment trigger and invoke the StartDeployment API call explicitly from the CI/CD pipeline after approval steps complete.' },
  ],
  pitfalls: [
    { title: 'Not sizing minimum instances for production latency requirements', description: 'App Runner with minimum instances set to zero scales the service down to zero during idle periods, eliminating baseline costs but causing cold starts of 5-15 seconds on the first request after scale-down. For customer-facing APIs where request latency is a user experience metric, this behavior is unacceptable. Always set minimum instances to at least 1 for production services, treating the baseline instance cost as the price of zero-latency readiness. Reserve scale-to-zero for development and staging environments.' },
    { title: 'Deploying stateful applications without external state stores', description: 'App Runner instances are ephemeral and can be replaced during health check failures, deployments, or scaling events. Applications that store user sessions, uploaded files, or processing state in container local memory or filesystem will lose that state on instance replacement. Design App Runner services as stateless, storing all persistent state in external services like RDS, ElastiCache, DynamoDB, or S3. Pass configuration via environment variables or Secrets Manager references rather than configuration files baked into the image.' },
    { title: 'Ignoring the App Runner vs ECS Fargate total cost of ownership', description: 'App Runner pricing includes a per-active-instance compute charge that is higher per vCPU-hour than equivalent ECS Fargate task pricing, in exchange for eliminating ALB, NAT Gateway, and operational overhead costs. For applications requiring more than a handful of container instances continuously, the operational savings may not justify the higher compute unit price. Perform a TCO analysis comparing App Runner active compute cost against Fargate plus ALB plus NAT Gateway plus engineering time to manage ECS task definitions, services, and auto-scaling policies before choosing between the two.' },
  ],
  keyQuestions: [
    { q: 'How do you choose between App Runner, ECS Fargate, and Lambda for a new API service?', a: 'The decision framework balances operational complexity, control, cost, and workload characteristics. Lambda is best for event-driven, short-duration workloads under 15 minutes with highly variable traffic, offering per-invocation pricing and zero operational overhead, but cold starts and execution time limits are genuine constraints. App Runner is best for containerized web APIs where the team lacks AWS infrastructure expertise, deployment speed and simplicity are priorities over fine-grained control, and traffic patterns are relatively steady or predictable. ECS Fargate is best when you need precise control over VPC networking, IAM task roles, sidecar containers, custom health check configuration, blue-green deployments via CodeDeploy, or integration with service meshes like App Mesh; the operational complexity is justified by the flexibility and typically lower per-unit compute cost at scale. For an API with moderate steady traffic, a small team, and no advanced networking requirements, App Runner is often the right starting point with a migration path to Fargate as requirements grow.' },
    { q: 'How do you configure App Runner to access an RDS database in a private VPC?', a: 'Connecting App Runner to an RDS database in a private VPC requires creating an App Runner VPC Connector. First, create a security group specifically for the App Runner VPC Connector in the same VPC as the RDS instance. Then update the RDS instance\'s security group inbound rules to allow the PostgreSQL or MySQL port from the App Runner Connector security group as the source rather than an IP range. Create the VPC Connector in the App Runner console or via API, specifying at least two subnets in different AZs for high availability and the App Runner security group. Attach the VPC Connector to the App Runner service in the networking configuration. The RDS connection string in the application should use the RDS private DNS name, available from the RDS console. Inject the database credentials via App Runner environment variables referencing Secrets Manager secrets rather than embedding them in the container image or environment variable values.' },
    { q: 'How does App Runner auto-scaling work, and how do you configure it for production?', a: 'App Runner auto-scaling operates on concurrent requests per instance rather than CPU or memory metrics, which is better suited to web API workloads where concurrency is the primary capacity dimension. When the number of concurrent requests exceeds the configured max-concurrency threshold multiplied by the current number of instances, App Runner provisions additional instances up to the maximum. Conversely, when concurrency drops, idle instances are removed after a cooldown period. For production configuration, establish max-concurrency by load testing your container to find the point where response latency begins to degrade under increasing concurrent load; set max-concurrency to 80% of that value as a safety margin. Set minimum instances to 1 to eliminate cold starts for production traffic. Set maximum instances based on the downstream system capacity — if your RDS database accepts 100 connections and each App Runner instance holds 5 connections, maximum instances should not exceed 20. Monitor the ActiveInstances metric and set alarms when approaching the maximum to alert before the service hits capacity limits.' },
    { q: 'How do you implement CI/CD with App Runner for a containerized API?', a: 'The recommended CI/CD pattern for App Runner uses ECR as the artifact store with App Runner\'s automatic deployment trigger on image push. In the CI pipeline triggered by a git push, build the Docker image, run unit and integration tests, scan the image for vulnerabilities using ECR image scanning or a tool like Trivy, and if all checks pass, push the image to ECR with both the commit SHA tag and the environment-specific tag like production or staging. App Runner detects the new image on the watched tag and initiates an automatic rolling deployment. For deployments requiring manual approval before production, tag the image with the commit SHA only during CI, and invoke the App Runner StartDeployment API call explicitly from the CD pipeline after a manual approval step or canary analysis completes, specifying the ECR image URI with the commit SHA tag. Monitor the deployment status via the App Runner DescribeDeployments API until the deployment completes or fails, and trigger automatic rollback by redeploying the previous image tag if the deployment health check failure rate exceeds a threshold.' },
  ],
  references: [
    'https://docs.aws.amazon.com/apprunner/latest/dg/',
    'https://docs.aws.amazon.com/apprunner/latest/dg/network-vpc.html',
    'https://docs.aws.amazon.com/apprunner/latest/dg/manage-autoscaling.html',
  ],
},
{
  id: 'aws-ecs-deep-dive',
  title: 'ECS — Elastic Container Service Deep Dive',
  icon: 'box',
  color: '#ec4899',
  description: 'AWS-native container orchestration with EC2 and Fargate launch types.',
  introduction: '**Amazon Elastic Container Service (ECS)** is AWS\'s proprietary container orchestration platform that manages the placement, scheduling, and lifecycle of Docker containers. ECS operates at two layers: the **control plane** that tracks desired state and the **data plane** where containers actually run. Unlike Kubernetes, ECS has a simpler mental model with fewer abstractions, making it easier to operate for teams that do not need the full Kubernetes ecosystem. ECS integrates deeply with AWS services including **ALB**, **IAM**, **CloudWatch**, **Service Discovery**, and **CodeDeploy**, making it a natural choice for teams already invested in the AWS ecosystem.',
  whenToUse: 'When running containerized workloads on AWS and you want deep AWS integration without the operational overhead of managing Kubernetes. Ideal for teams migrating from EC2 to containers, organizations that want native AWS service integration, workloads where Fargate\'s serverless model simplifies operations, and when blue/green deployments via CodeDeploy are required for production safety.',
  keyConcepts: [
    { title: 'Task Definition', description: '**Blueprint** for your application specifying `container images`, `CPU/memory`, networking mode, volumes, environment variables, secrets, log configuration, and IAM roles. Task definitions are **versioned and immutable** once registered. Each new revision creates a new version number. Services reference a specific task definition revision or track the latest.' },
    { title: 'ECS Service', description: '**Long-running workload manager** that ensures a desired number of task replicas are running, integrates with load balancers, handles **rolling deployments** or **CodeDeploy blue/green**, and connects with **Service Auto Scaling**. Services monitor task health and replace failed tasks automatically to maintain desired count.' },
    { title: 'Launch Types and Capacity Providers', description: '`EC2` launch type runs tasks on EC2 instances you manage in the cluster. `Fargate` launch type is **serverless** with AWS managing the underlying infrastructure. **Capacity providers** are the modern abstraction replacing raw launch types, allowing weight-based traffic splitting between `FARGATE`, `FARGATE_SPOT`, and EC2 Auto Scaling Groups. A default capacity provider strategy is set per cluster.' },
    { title: 'Task Role vs Execution Role', description: 'Two distinct **IAM roles** serve different purposes. The **task execution role** grants the ECS agent permission to pull images from `ECR`, retrieve secrets from `Secrets Manager` or `Parameter Store`, and write logs to `CloudWatch`. The **task role** grants permissions to the application code running inside the container to call AWS services like `S3` or `DynamoDB`. Both must be configured separately following least privilege principles.' },
    { title: 'Service Discovery and Load Balancing', description: 'ECS integrates with **AWS Cloud Map** for service discovery, automatically registering task IP addresses as healthy instances when tasks start. For external traffic, services connect to an **ALB** with a dedicated target group per service using `IP` target type in `awsvpc` mode. Each task gets a unique **ENI** and IP address that the ALB routes to directly, enabling per-task security groups.' },
    { title: 'Deployment Strategies', description: '**Rolling update** is the default where ECS replaces old tasks with new ones respecting `minimum healthy percent` and `maximum percent` constraints. **Blue/green deployment** via `CodeDeploy` creates a new target group (green), shifts traffic incrementally or all-at-once from blue to green, and retains the ability to roll back by shifting traffic back. This enables zero-downtime deployments with automated rollback on **CloudWatch** alarm triggers.' },
  ],
  approach: [
    { step: 'Design the task definition', details: 'Start by defining container images, resource allocations (CPU in units where 1024 = 1 vCPU, memory in MB), network mode (awsvpc for Fargate and IP-based routing, bridge for EC2 multi-container on same host), and log configuration using the awslogs driver pointing to a CloudWatch log group. Define the task execution role and task role with scoped permissions.' },
    { step: 'Configure the cluster and capacity providers', details: 'Create an ECS cluster and attach capacity providers. For Fargate workloads, attach FARGATE and FARGATE_SPOT providers. For EC2 workloads, create an Auto Scaling Group and attach it as a capacity provider with managed scaling enabled so ECS adjusts the ASG size based on pending tasks. Set a default capacity provider strategy with weights that reflect your cost and availability preferences.' },
    { step: 'Set up the ECS service with load balancer integration', details: 'Create the service specifying desired count, capacity provider strategy, deployment configuration (rolling or CodeDeploy), and ALB target group. Use IP target type to register tasks directly by their ENI IP. Configure health check grace period to give containers time to start before health checks begin. Set minimum healthy percent to 100 and maximum percent to 200 for zero-downtime rolling updates.' },
    { step: 'Configure Service Auto Scaling', details: 'Attach Application Auto Scaling to the ECS service. Target tracking scaling policies are simplest, automatically adjusting task count to maintain a target metric like 70 percent CPU utilization or request count per target. Step scaling gives more control for predictable load patterns. Scheduled scaling handles known traffic spikes like business hours by setting min/max/desired counts on a cron schedule.' },
    { step: 'Implement observability', details: 'Enable Container Insights at the cluster level for task-level CPU, memory, and network metrics in CloudWatch. Configure structured logging in containers to write JSON to stdout so the awslogs driver captures it in CloudWatch Logs. Use AWS X-Ray sidecar container in the task definition for distributed tracing. Set CloudWatch alarms on deployment errors to trigger automatic CodeDeploy rollback.' },
  ],
  pitfalls: [
    { title: 'Task role vs execution role confusion', description: 'The task execution role grants ECS agent permission to pull images and write logs. The task role grants permissions to the application code itself. A common mistake is granting all permissions to the execution role and then wondering why the application cannot call S3. These are separate IAM roles with different purposes and must be configured independently.' },
    { title: 'awsvpc mode and ENI limits', description: 'Each ECS task in awsvpc mode gets its own ENI, and EC2 instances have hard limits on ENI count based on instance type. A c5.xlarge supports only 15 ENIs including the primary interface, meaning at most 14 tasks per instance. ENI trunking (trunk network interface) can increase this to hundreds per instance but requires specific instance types and ECS agent configuration. Fargate sidesteps this by managing ENI allocation separately.' },
    { title: 'Deployment minimum healthy percent misconfiguration', description: 'Setting minimum healthy percent to 0 means ECS can terminate all running tasks before starting new ones, causing downtime. Setting maximum percent to 100 means no extra tasks can start during deployment, so ECS must terminate old tasks first. The correct zero-downtime configuration is minimum healthy percent 100 and maximum percent 200, allowing new tasks to start alongside old ones before old ones are terminated.' },
    { title: 'Service discovery vs ALB routing conflation', description: 'Cloud Map service discovery is for east-west (service-to-service) communication within the cluster using DNS names. ALB integration is for north-south (client-to-service) traffic from the internet or internal VPC. These solve different problems and are often both configured simultaneously. Using ALB for internal service mesh traffic adds unnecessary latency and cost compared to direct Cloud Map DNS resolution.' },
  ],
  keyQuestions: [
    { q: 'What is the difference between ECS task role and task execution role?', a: 'The **task execution role** is used by the ECS agent to perform AWS API calls on behalf of ECS itself — pulling container images from **ECR**, retrieving secrets from **Secrets Manager** or **Parameter Store**, and writing logs to **CloudWatch**. The **task role** is the IAM role assumed by the application code running inside the container when it needs to call other AWS services like `S3`, `DynamoDB`, or `SQS`. Both roles follow least privilege principles and should be scoped to only the permissions needed. A common mistake is putting application permissions in the execution role, which works initially but violates security boundaries and makes auditing difficult. Confusing the two is the most common source of ECS permission errors where the container starts but the application cannot access AWS services.' },
    { q: 'How does blue/green deployment work in ECS with CodeDeploy?', a: '**ECS blue/green deployment** with **CodeDeploy** creates two ALB target groups: the **blue** target group serving current production traffic and a **green** target group for the new version. When a deployment starts, ECS launches new tasks and registers them with the green target group while the blue target group continues serving traffic. CodeDeploy then shifts traffic using a configurable strategy:\n- `AllAtOnce` — immediately redirects all traffic\n- `Canary` — shifts a small percentage first, then all traffic after a wait period\n- `Linear` — shifts traffic in equal increments at regular intervals\n\nAutomatic rollback can be configured on **CloudWatch** alarms so that if error rates spike on the green target group, CodeDeploy immediately shifts all traffic back to blue without manual intervention. The original blue tasks are retained for a configurable termination wait time to allow rollback even after traffic is fully shifted.' },
    { q: 'How do capacity provider strategies work and when would you use FARGATE_SPOT?', a: '**Capacity provider strategies** define how ECS distributes tasks across multiple capacity providers using `base` and `weight` values. The base value specifies a minimum number of tasks that must run on a provider before weights take effect. Weights determine proportional distribution — a strategy with `FARGATE` weight 1 and `FARGATE_SPOT` weight 4 means roughly 80% of tasks run on Spot. **FARGATE_SPOT** uses spare AWS Fargate capacity at up to 70% discount, but tasks can be interrupted with a two-minute warning when AWS needs the capacity back. FARGATE_SPOT is ideal for batch processing, fault-tolerant workers, and non-production environments where occasional interruptions are acceptable. For production web services, a mixed strategy with `FARGATE` as the base and `FARGATE_SPOT` for additional capacity provides cost savings while ensuring availability. ECS handles Spot interruptions by draining the task gracefully before termination, and services automatically replace interrupted tasks.' },
    { q: 'How does ECS service discovery with Cloud Map work compared to ALB?', a: '**AWS Cloud Map** service discovery integrates with ECS to automatically register task IP addresses and ports as service instances when tasks start and deregister them when tasks stop or fail health checks. Other services can discover endpoints using DNS queries to the private hosted zone — for example calling `payment-service.internal` — or using the Cloud Map API directly for richer metadata. This enables **direct service-to-service communication** within the VPC without going through a load balancer, reducing latency and cost for internal traffic. **ALB** integration serves a different purpose: routing external traffic from outside the cluster to ECS services, with features like path-based routing, SSL termination, and request-level metrics. In a microservices architecture, you typically use both simultaneously — **ALB** for north-south ingress traffic and **Cloud Map** for east-west internal service communication.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/',
    'https://docs.aws.amazon.com/AmazonECS/latest/bestpracticesguide/',
    'https://docs.aws.amazon.com/codedeploy/latest/userguide/deployments-create-ecs.html',
  ],
},
{
  id: 'aws-eks-deep-dive',
  title: 'EKS — Elastic Kubernetes Service Deep Dive',
  icon: 'layers',
  color: '#ec4899',
  description: 'Managed Kubernetes on AWS with deep VPC, IAM, and ecosystem integration.',
  introduction: '**Amazon Elastic Kubernetes Service (EKS)** provides a managed Kubernetes control plane where AWS operates the API server, etcd, and controller manager across multiple availability zones. EKS lets teams run standard Kubernetes workloads while AWS handles control plane availability, version upgrades, and security patching. The data plane is customer-managed through **EC2 node groups** or **Fargate profiles**. EKS integrates with the broader AWS ecosystem through the **VPC CNI plugin** for pod networking, **IAM Roles for Service Accounts (IRSA)** for fine-grained pod-level permissions, and the **AWS Load Balancer Controller** for ALB and NLB provisioning from Kubernetes Ingress and Service resources.',
  whenToUse: 'When running Kubernetes workloads that require portability across cloud providers, when your team has Kubernetes expertise and wants to leverage the broader CNCF ecosystem, for large multi-team organizations that need namespace-level isolation, and when you need advanced features like custom operators, service meshes, or GitOps workflows that are better supported in Kubernetes than ECS.',
  keyConcepts: [
    { title: 'EKS Control Plane and Node Types', description: '**EKS** manages the Kubernetes control plane across three availability zones. Three node options exist:\n- **Managed node groups** — AWS handles node provisioning and updates with rolling update support\n- **Self-managed node groups** — for custom AMIs or configurations\n- **Fargate profiles** — run pods serverlessly matching namespace and label selectors without any EC2 management' },
    { title: 'VPC CNI and Pod Networking', description: 'The **AWS VPC CNI plugin** assigns actual VPC IP addresses to pods, meaning pods have routable IPs within the VPC without NAT. Each pod gets an IP from the node\'s subnet, and secondary IP addresses are pre-allocated on the node\'s ENIs for fast pod startup. **Security Groups for Pods** allows assigning VPC security groups directly to pods for fine-grained network policy at the infrastructure level, separate from Kubernetes `NetworkPolicy`.' },
    { title: 'IAM Roles for Service Accounts (IRSA)', description: '**IRSA** enables Kubernetes service accounts to assume IAM roles without node-level instance profile credentials. EKS creates an **OIDC identity provider** for the cluster, and IAM roles trust the OIDC provider with a condition matching the specific namespace and service account name. The pod receives a projected token volume that the AWS SDK exchanges for temporary credentials via `STS AssumeRoleWithWebIdentity`. This provides **pod-level least privilege** without sharing credentials across all pods on a node.' },
    { title: 'EKS Add-ons and Cluster Components', description: '**EKS add-ons** are managed versions of critical cluster components that AWS keeps updated and security-patched. Core add-ons include:\n- `vpc-cni` — pod networking\n- `coredns` — cluster DNS\n- `kube-proxy` — service networking\n- `aws-ebs-csi-driver` — persistent volume support\n\nAdd-ons can be installed and version-managed through the EKS API or console, with automatic conflict detection if customizations exist.' },
    { title: 'Cluster Autoscaler vs Karpenter', description: '**Cluster Autoscaler** scales EC2 node groups based on unschedulable pods, working with Auto Scaling Groups. It is slow to provision new nodes (2-3 minutes) and is constrained to pre-defined instance types in the ASG. **Karpenter** is a next-generation autoscaler that provisions EC2 instances directly without ASGs, selects optimal instance types based on pod requirements, and can consolidate nodes when underutilized. Karpenter provisions nodes in **under 60 seconds** and supports mixed instance families for cost optimization.' },
    { title: 'AWS Load Balancer Controller', description: 'The **AWS Load Balancer Controller** translates Kubernetes `Ingress` resources into ALB configurations and `Service` resources of type `LoadBalancer` into NLBs. It supports advanced ALB features like path-based routing, host-based routing, authentication via **Cognito** or OIDC, WAF integration, and SSL termination. **Pod readiness gates** ensure pods are only marked ready after ALB target registration completes, preventing traffic from routing to pods that are not yet registered.' },
  ],
  approach: [
    { step: 'Plan cluster architecture and networking', details: 'Design the VPC with sufficient IP space for pods since each pod consumes a VPC IP. A common issue is running out of IPs in small subnets. Plan for at least three to four times the expected pod count in available IPs. Choose between public, private, or hybrid API endpoint access. Private endpoints keep control plane traffic inside the VPC. Configure the cluster OIDC provider during creation to enable IRSA from the start.' },
    { step: 'Set up node groups with appropriate instance types', details: 'Create managed node groups with multiple instance types for better spot availability. Use on-demand instances for system-critical pods and spot for batch or fault-tolerant workloads. Configure node taints and labels to direct specific workloads to appropriate node groups. Enable node group update configuration to control maximum unavailable nodes during rolling updates. For GPU workloads, create separate node groups with GPU instance types and the NVIDIA device plugin.' },
    { step: 'Implement IRSA for all service accounts', details: 'Audit all workloads that call AWS services and create dedicated IAM roles for each. Configure the trust policy to allow only the specific namespace and service account. Annotate the Kubernetes service account with the IAM role ARN using the eks.amazonaws.com/role-arn annotation. Ensure pods use the annotated service account and the AWS SDK version supports web identity tokens. Avoid using node instance profiles for application credentials as they grant the same permissions to all pods on the node.' },
    { step: 'Install and configure core add-ons and controllers', details: 'Install EKS managed add-ons for VPC CNI, CoreDNS, and kube-proxy through the EKS API to benefit from AWS-managed updates. Install the AWS Load Balancer Controller via Helm with the appropriate IRSA role granting permissions to manage ELB resources. Install the EBS CSI driver as an EKS add-on for persistent storage. Configure ExternalDNS with Route 53 permissions to automatically manage DNS records for Ingress and Service resources.' },
    { step: 'Configure multi-tenancy and namespace isolation', details: 'Create namespaces for each team or application. Apply resource quotas to prevent any single namespace from consuming all cluster resources. Use LimitRange to set default CPU and memory requests and limits. Configure RBAC roles and role bindings to grant teams access only to their namespaces. Apply network policies to restrict pod-to-pod communication across namespaces. For stronger isolation, consider separate node groups per team with node selectors and taints.' },
  ],
  pitfalls: [
    { title: 'IP exhaustion from VPC CNI', description: 'The AWS VPC CNI pre-warms ENI secondary IPs on each node, consuming IPs even when pods do not use them. A single node can consume 15 to 50 IPs depending on the instance type and WARM_IP_TARGET configuration. In a cluster with 50 nodes and a /24 subnet (256 IPs), the pre-warming alone can exhaust the subnet. Plan subnets with at least three to four times the expected IP count or enable prefix delegation mode which assigns /28 prefixes to ENIs, dramatically increasing IP capacity.' },
    { title: 'IRSA trust policy misconfiguration', description: 'A common IRSA mistake is using a wildcard in the trust policy condition instead of the specific namespace and service account. This allows any pod in the cluster to assume the role, defeating the purpose of least privilege. The condition must specify the exact subject in format system:serviceaccount:namespace:serviceaccountname. Another mistake is forgetting to annotate the Kubernetes service account after creating the IAM role, causing pods to fall back to the node instance profile.' },
    { title: 'Cluster Autoscaler with multiple node groups', description: 'Cluster Autoscaler can thrash between node groups if expander configuration is not set correctly. With the random expander, it may keep creating nodes in expensive on-demand groups instead of cheaper spot groups. Use the least-waste expander to minimize resource waste or the priority expander to explicitly define preference order. Also, node groups must have the required auto-discovery tags for Cluster Autoscaler to discover and manage them automatically.' },
    { title: 'EKS version upgrade complexity', description: 'EKS requires upgrading the control plane first, then managed node groups, then add-ons, in that order. Each EKS minor version is supported for 14 months and then enters extended support with additional cost. Skipping add-on upgrades after control plane upgrades can cause compatibility issues since add-ons like the VPC CNI have version compatibility requirements with the Kubernetes API version. Plan upgrades quarterly to avoid being forced into emergency upgrades with no testing time.' },
  ],
  keyQuestions: [
    { q: 'How does IRSA work and why is it better than using node instance profiles?', a: '**IRSA** uses the EKS cluster\'s **OIDC identity provider** to allow Kubernetes service accounts to assume IAM roles. When a pod runs with an annotated service account, EKS mounts a signed JWT token as a projected volume into the pod. The AWS SDK in the pod exchanges this token with STS using `AssumeRoleWithWebIdentity`, receiving temporary credentials scoped to the specific IAM role. The IAM role trust policy uses a condition matching the exact namespace and service account name, so only that specific service account can assume the role. **Node instance profiles**, by contrast, grant the same IAM permissions to all pods running on the node, making it impossible to apply least privilege at the pod level. If any pod on the node is compromised, the attacker gains all permissions of the node role. IRSA eliminates this blast radius by giving each microservice its own scoped IAM identity, and temporary credentials rotate automatically without any credential management.' },
    { q: 'What are the key differences between Karpenter and Cluster Autoscaler?', a: '**Cluster Autoscaler** works by monitoring unschedulable pods and scaling up the node group ASG that can accommodate them, selecting from instance types pre-configured in the ASG launch template. This creates scaling latency of 2-3 minutes and requires manually defining which instance types are available. **Karpenter** bypasses ASGs entirely, using EC2 Fleet APIs to provision exactly the right instance type for the workload within 60 seconds. Karpenter reads pod resource requests, affinities, and tolerations to select the most cost-efficient instance, automatically choosing from hundreds of instance types. Karpenter also performs **node consolidation** by moving pods from underutilized nodes and terminating those nodes. The tradeoff is that Karpenter is newer and more complex to configure, requiring `NodePool` and `EC2NodeClass` resources to define provisioning constraints, whereas Cluster Autoscaler is simpler and more battle-tested in large production environments.' },
    { q: 'How do you design multi-tenant EKS clusters to isolate teams?', a: 'Multi-tenant EKS isolation operates at several layers:\n\n- **Namespace layer** — each team gets dedicated namespaces with `ResourceQuota` enforcing CPU, memory, and object count limits\n- **RBAC** — role bindings restrict each team to only their namespaces, preventing cross-namespace resource access\n- **Network policies** — block pod-to-pod traffic across namespaces by default, explicitly allowing only required paths\n- **Compute isolation** — dedicated node groups with taints and tolerations ensure team workloads run on separate EC2 instances\n- **IRSA** — each team\'s service accounts can only assume their own IAM roles\n\nFor the strongest isolation requirements, consider **separate clusters per team**, accepting the higher operational overhead in exchange for complete blast radius containment.' },
    { q: 'How does the AWS VPC CNI handle pod IP assignment and what is prefix delegation?', a: 'The **VPC CNI plugin** assigns real VPC IP addresses to pods by managing secondary IP addresses on EC2 network interfaces. Each EC2 instance has one or more ENIs, and each ENI can have multiple secondary IP addresses. The CNI pre-allocates a pool of secondary IPs on the node so that pod startup is fast. The `WARM_IP_TARGET` and `WARM_ENI_TARGET` environment variables control how aggressively IPs are pre-warmed.\n\n**Prefix delegation** mode assigns `/28` CIDR prefixes to ENIs, where each prefix provides 16 IP addresses. This dramatically increases pod density per node and reduces EC2 API calls for IP management. A `c5.xlarge` that normally supports ~58 pods can support **over 900 pods** with prefix delegation enabled. Prefix delegation requires Amazon Linux 2 nodes and Nitro-based instance types.' },
  ],
  references: [
    'https://docs.aws.amazon.com/eks/latest/userguide/',
    'https://aws.github.io/aws-eks-best-practices/',
    'https://karpenter.sh/docs/',
    'https://github.com/aws/aws-load-balancer-controller',
  ],
},
{
  id: 'aws-ecr',
  title: 'ECR — Elastic Container Registry',
  icon: 'package',
  color: '#ec4899',
  description: 'Managed container image registry with scanning, replication, and lifecycle policies.',
  introduction: '**Amazon Elastic Container Registry (ECR)** is a fully managed Docker-compatible container registry that eliminates the need to operate your own registry infrastructure. ECR integrates natively with **ECS**, **EKS**, and **CodeBuild**, making it the standard image storage solution for AWS container workloads. ECR supports both **private repositories** for internal images and a **public registry** for sharing images publicly. Key enterprise features include:\n- Automated image vulnerability scanning powered by **Amazon Inspector**\n- Lifecycle policies for cost management\n- Cross-region and cross-account replication for global deployments\n- VPC endpoints for keeping image pulls off the public internet',
  whenToUse: 'When storing container images for workloads running on ECS, EKS, or Lambda. Ideal when you want tight IAM integration for access control, need automated vulnerability scanning, want to avoid managing registry infrastructure, or require cross-region replication for multi-region deployments. Also the right choice when security requirements mandate that image pulls must not traverse the public internet.',
  keyConcepts: [
    { title: 'Private and Public Registries', description: 'Each AWS account has one **private ECR registry** per region, hosting multiple repositories. Private registries require AWS authentication and support IAM-based access control. **ECR Public** is a separate global registry at `public.ecr.aws` for sharing images publicly without authentication for pulls. Public repositories are globally distributed for low-latency access and do not count against private registry pull quotas.' },
    { title: 'Lifecycle Policies', description: '**Lifecycle policies** automatically expire images based on rules evaluated daily. Rules can target untagged images, images with specific tag prefixes, or images older than a specified number of days. Rules with lower priority numbers are evaluated first — a common pattern is keeping the last 10 tagged releases and expiring all untagged images older than one day. Without lifecycle policies, registries accumulate thousands of images and incur significant storage costs.' },
    { title: 'Image Scanning', description: '**Basic scanning** uses the open-source Clair scanner to detect CVEs in OS packages and is triggered on push or manually. **Enhanced scanning** integrates with **Amazon Inspector** to continuously re-scan images as new CVEs are discovered, covering both OS packages and programming language packages like `npm` and `pip` dependencies. Enhanced scanning provides scan findings in **Security Hub** and **EventBridge** for automated remediation workflows.' },
    { title: 'Image Tag Mutability', description: '**Tag mutability** controls whether existing image tags can be overwritten. With **mutable tags** (the default), pushing a new image with the same tag silently replaces the previous image — convenient for `latest` tags but dangerous for production tags like `v1.2.3` that should be immutable. Setting a repository to **immutable tags** causes a push to fail if the tag already exists, enforcing that production image references are stable and auditable.' },
    { title: 'Pull-Through Cache', description: '**Pull-through cache** repositories act as proxies for upstream registries including Docker Hub, ECR Public, Quay, GitHub Container Registry, and Kubernetes registry. When an image is pulled from the pull-through cache URL, ECR fetches it from the upstream registry, caches it in ECR, and serves future requests from the cache. This avoids Docker Hub rate limits and keeps image pulls within the AWS network.' },
    { title: 'Cross-Account and Cross-Region Access', description: '**Cross-account image access** is controlled through **ECR repository policies** — resource-based IAM policies attached to the repository. Adding the target account\'s root ARN or specific role ARN grants that account pull access. **Cross-region replication** automatically copies images to destination regions on push, supporting both same-account and cross-account replication. This ensures images are available in every deployment region without manual sync processes.' },
  ],
  approach: [
    { step: 'Authenticate and structure repositories', details: 'Authenticate the Docker client using aws ecr get-login-password piped to docker login. The password is a temporary token valid for 12 hours. For CI/CD, use the ECR credential helper that handles token refresh automatically. Structure repositories by service name, environment, or team. A common convention is servicename/imagename for logical grouping. Set immutable tags on all repositories used by production workloads.' },
    { step: 'Configure lifecycle policies for cost control', details: 'Attach lifecycle policies to every repository immediately after creation. A baseline policy should expire untagged images after one day and keep only the last 20 tagged images. For repositories with many tag prefixes, create rules per prefix: keep last 5 production-tagged images, last 10 release-candidate images, and expire all feature-branch images after 7 days. Review ECR costs in Cost Explorer monthly and tighten policies if storage costs are significant.' },
    { step: 'Enable image scanning and integrate with CI/CD', details: 'Enable enhanced scanning at the registry level to automatically scan all repositories. Configure an EventBridge rule to catch Inspector scan findings with CRITICAL or HIGH severity and route them to SNS or a Slack webhook. In CI/CD pipelines, add a step after image push that waits for the initial scan to complete and fails the build if any critical vulnerabilities are found. This prevents vulnerable images from reaching deployment.' },
    { step: 'Set up VPC endpoints for private access', details: 'Create interface VPC endpoints for ECR API (com.amazonaws.region.ecr.api) and ECR DKR (com.amazonaws.region.ecr.dkr) to keep image pulls within the VPC network. Also create an S3 gateway endpoint since ECR stores image layers in S3 and pulls will still traverse the internet if the S3 endpoint is missing. Attach endpoint policies to restrict which repositories can be accessed. This is mandatory for EKS clusters in private subnets with no internet gateway.' },
    { step: 'Configure cross-region replication for multi-region deployments', details: 'Configure replication rules at the registry level specifying destination regions and optionally destination accounts. Replication is asynchronous and triggered on image push. For global deployments, replicate to all deployment regions so that image pulls happen locally rather than crossing regions. Apply replication rules with repository filters to replicate only production repositories, not every developer test image.' },
  ],
  pitfalls: [
    { title: 'Missing S3 gateway endpoint breaks ECR pulls', description: 'ECR stores image layer data in S3. Even with ECR interface endpoints configured, image pulls will fail or fall back to the public internet if the VPC lacks an S3 gateway endpoint. This is a non-obvious dependency because the ECR API calls succeed through the interface endpoint but layer downloads fail. The error messages are cryptic network errors rather than clear permission errors. Always pair ECR VPC endpoints with an S3 gateway endpoint.' },
    { title: 'Mutable tags in production allow silent image replacement', description: 'Default mutable tag settings allow any principal with push permission to overwrite a tag like v2.0.0 with a completely different image. If production deployments reference v2.0.0, a bad push can change what gets deployed without any version change in the deployment manifest. Enable immutable tags on all production repositories. If you need to update a tag, delete it and re-push, which creates an intentional action with audit trail in CloudTrail.' },
    { title: 'Lifecycle policy rule ordering causes unexpected deletions', description: 'ECR evaluates lifecycle policy rules by priority number in ascending order, with lower numbers evaluated first. A rule with priority 1 that expires all untagged images will match and mark images for deletion before a rule with priority 2 that tries to protect the last 5 untagged images can apply. Test lifecycle policies in dry run mode (using the API) before applying them to production repositories to verify which images would be deleted.' },
    { title: 'Cross-account pulls failing due to missing repository policy', description: 'When an EKS cluster in Account B tries to pull an image from Account A\'s ECR, the default repository policy does not grant any cross-account access. The node role in Account B needs both the ECR repository policy in Account A granting pull permissions and the node role\'s own IAM policy allowing ecr:GetAuthorizationToken. Missing either layer causes pull failures. The authorization token API call is a regional API call against the account hosting the registry, not against your own account.' },
  ],
  keyQuestions: [
    { q: 'How would you design a lifecycle policy strategy to control ECR storage costs?', a: 'An effective ECR lifecycle strategy segments images into tiers based on their purpose and retention requirements. For production tagged images using semantic versioning, retain the last 10 to 20 versions to support rollback scenarios, since rollbacks rarely require more than a few prior versions. For CI build images tagged with git commit SHAs or branch names, retain only the last 5 images per branch prefix and expire all untagged images after 24 hours since untagged images are intermediate build artifacts with no deployment value. Feature branch images can be set to expire after 7 days since branches are typically short-lived. Configure the policies immediately when repositories are created using infrastructure as code, not retroactively when storage costs have already grown. Review ECR line items in AWS Cost Explorer monthly and audit repositories that lack lifecycle policies using a Lambda function or AWS Config rule. The biggest cost driver is typically untagged images from failed builds accumulating over months.' },
    { q: 'How does cross-account image sharing work in ECR?', a: 'Cross-account ECR access requires configuration on both the repository side and the IAM side of the consuming account. On the ECR repository in the source account, attach a repository policy granting the target account\'s IAM principal permission to call ecr:BatchCheckLayerAvailability, ecr:GetDownloadUrlForLayer, ecr:BatchGetImage, and ecr:DescribeRepositories. The principal can be the target account root to allow all roles in that account, or a specific role ARN for least privilege. In the consuming account, the IAM role used by the workload needs permission to call ecr:GetAuthorizationToken against the source account\'s registry, since authentication tokens are account-specific. For EKS, this means the node role or the IRSA role needs the cross-account GetAuthorizationToken permission. A common pattern for multi-account organizations is having a centralized tooling account hosting shared base images with repository policies granting read access to all workload accounts.' },
    { q: 'What is the difference between ECR basic scanning and enhanced scanning?', a: 'Basic scanning uses the open-source Clair vulnerability scanner to check container images against the CVE database for OS package vulnerabilities. It can be configured to run automatically on push or triggered manually, and findings are available in the ECR console. Enhanced scanning is powered by Amazon Inspector and provides continuous re-scanning, meaning images are automatically rescanned when new CVEs are discovered even without a new image push. Enhanced scanning covers both OS package vulnerabilities and programming language package vulnerabilities including Node.js, Python, Ruby, and Java dependencies, making it significantly more comprehensive. Enhanced scanning findings integrate with AWS Security Hub for centralized security posture management and emit events to EventBridge enabling automated responses like blocking deployments or creating tickets. The tradeoff is cost: enhanced scanning incurs an Inspector pricing charge per image per month, while basic scanning has no additional cost beyond the standard ECR storage and data transfer fees.' },
    { q: 'How does ECR pull-through cache work and when would you use it?', a: 'Pull-through cache creates a proxy repository in ECR that mirrors an upstream registry such as Docker Hub, ECR Public, Quay, GitHub Container Registry, or the Kubernetes container registry. When a workload pulls from the pull-through cache repository URL, ECR checks if the image is cached locally. On a cache miss, ECR fetches the image from the upstream registry, stores it in the pull-through cache repository, and serves it to the caller. Subsequent pulls are served from the ECR cache. This solves several problems: Docker Hub rate limits of 100 anonymous pulls per six hours or 200 authenticated pulls per hour can throttle large Kubernetes clusters pulling base images, and pull-through cache bypasses this by counting as a single authenticated pull. It also reduces latency for clusters in regions far from Docker Hub, eliminates external registry dependency for production workloads, and ensures image availability even if the upstream registry has an outage. A secondary benefit is that cached images can be scanned by ECR Enhanced Scanning, giving visibility into vulnerabilities in third-party base images.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonECR/latest/userguide/',
    'https://docs.aws.amazon.com/AmazonECR/latest/userguide/LifecyclePolicies.html',
    'https://docs.aws.amazon.com/AmazonECR/latest/userguide/image-scanning-enhanced.html',
  ],
},
{
  id: 'aws-fargate-patterns',
  title: 'Fargate — Serverless Container Patterns',
  icon: 'cpu',
  color: '#ec4899',
  description: 'Serverless container execution eliminating EC2 management for ECS and EKS workloads.',
  introduction: '**AWS Fargate** is a serverless compute engine for containers that removes the need to provision, configure, or scale EC2 instances. With Fargate, you define the CPU and memory requirements for your containers, and AWS handles everything below the task level: selecting physical hardware, configuring networking, applying security patches, and managing the underlying operating system. Fargate runs tasks in **isolated single-tenant microVMs** with dedicated kernel, CPU, and memory resources per task, providing strong workload isolation without sharing a VM with other customers. Fargate works with both **ECS** and **EKS**, though its integration model differs between the two orchestrators.',
  whenToUse: 'When you want to eliminate EC2 operational overhead for container workloads. Ideal for organizations that want containers without a platform team, for variable workloads where right-sizing EC2 instances is difficult, for batch and ETL jobs that need isolated ephemeral compute, and for security-sensitive workloads that benefit from per-task VM isolation. Not ideal for GPU workloads, Windows containers requiring specific versions, workloads needing custom Linux kernel parameters, or extreme cost optimization scenarios requiring EC2 reserved instances.',
  keyConcepts: [
    { title: 'Task Sizing and vCPU/Memory Combinations', description: 'Fargate tasks have **defined CPU and memory combinations** rather than arbitrary values. CPU is specified in units from `256` (0.25 vCPU) to `16384` (16 vCPU). Memory must be within the allowed range for the selected CPU — for `1 vCPU`, memory ranges from 2GB to 8GB. Choosing the right size requires profiling actual container resource usage to avoid over-provisioning and paying for unused capacity.' },
    { title: 'Fargate Spot', description: '**Fargate Spot** uses spare AWS Fargate capacity at up to **70% discount** compared to standard Fargate. Spot tasks can be interrupted with a two-minute `SIGTERM` warning when AWS needs the capacity back, triggering graceful shutdown. Fargate Spot is ideal for batch processing, fault-tolerant workers, and non-production workloads. For production, a mixed capacity provider strategy uses standard `FARGATE` for the base with `FARGATE_SPOT` for burst capacity.' },
    { title: 'Networking in Fargate (awsvpc Only)', description: 'Fargate tasks exclusively use `awsvpc` network mode, giving each task a **dedicated ENI** with a private IP address from the VPC subnet. This means tasks have their own security groups for fine-grained network control, tasks are individually addressable within the VPC, and there is no port mapping complexity from host networking modes. Each task\'s ENI is managed by AWS and automatically cleaned up when the task stops.' },
    { title: 'EFS Integration for Stateful Workloads', description: 'Fargate supports **Amazon EFS** for shared persistent storage across tasks. EFS volumes are mounted at defined mount points in the task definition. Multiple tasks can mount the same EFS filesystem simultaneously. **EFS access point** authorization allows different tasks to access different directories of the same filesystem with separate permissions. Fargate ephemeral storage provides `20GB` by default (expandable to `200GB`) for task-local scratch space that does not persist after task termination.' },
    { title: 'Security Model', description: 'Fargate provides strong isolation guarantees: each task runs in a **dedicated microVM** with no shared kernel with other tasks or customers. Security best practices include: running containers as non-root users, enabling read-only root filesystem to prevent runtime modification, dropping unnecessary Linux capabilities, and using **Fargate task roles** with least privilege instead of environment variable credentials.' },
    { title: 'Fargate for EKS Pods', description: '**EKS Fargate profiles** define which Kubernetes pods run on Fargate based on namespace and label selectors. Pods matching a profile are scheduled on dedicated Fargate nodes that appear as regular nodes in `kubectl` but are ephemeral and single-use. Key limitations: no support for `DaemonSets` (each Fargate node is single-use), stateful sets with `PersistentVolumeClaims` require EFS not EBS, and node size is automatically selected based on pod resource requests.' },
  ],
  approach: [
    { step: 'Profile workloads before sizing Fargate tasks', details: 'Run the application on EC2 or in a local Docker environment with monitoring to capture actual CPU and memory usage under representative load. Avoid guessing sizes. A common mistake is selecting 1 vCPU when the container only needs 0.25 vCPU, tripling the compute cost. Use CloudWatch Container Insights to see actual CPU and memory utilization per task after initial deployment and downsize if utilization is consistently below 30 percent.' },
    { step: 'Design for statelessness or use EFS for state', details: 'Fargate tasks have ephemeral local storage that disappears when the task stops. Design applications to be stateless with all state in external systems like RDS, DynamoDB, ElastiCache, or S3. When stateful local storage is needed between task restarts, mount an EFS access point into the container. Use EFS in-transit encryption and authorization via IAM access points to control which tasks can access which directories. Avoid writing session state or file uploads to the container filesystem.' },
    { step: 'Implement Fargate Spot for cost optimization', details: 'Configure a capacity provider strategy with FARGATE_SPOT carrying a higher weight than FARGATE for workloads that can tolerate interruption. Implement graceful shutdown handling in the application by catching SIGTERM and completing in-progress work within two minutes. For ECS worker services processing SQS messages, configure visibility timeout to be longer than two minutes and implement message visibility renewal so in-progress messages return to the queue if the task is interrupted before completion.' },
    { step: 'Harden Fargate task security', details: 'Set the container user to a non-root UID in the Dockerfile. Enable read-only root filesystem in the task definition and mount a tmpfs volume for directories the application must write to like /tmp. Drop all Linux capabilities and add back only required capabilities. Enable Fargate task network observability using VPC Flow Logs to capture traffic to and from each task ENI. Rotate task roles using IRSA on EKS or task roles on ECS rather than embedding AWS credentials as environment variables.' },
    { step: 'Optimize startup time for latency-sensitive workloads', details: 'Fargate cold start time is typically 30 to 60 seconds from task creation to container accepting traffic. Minimize this by keeping container images small with multi-stage builds and using ECR to avoid Docker Hub rate limits. Use Amazon Linux 2023 base images which are optimized for fast pull in the AWS network. For services that need fast scale-out, pre-warm capacity using ECS service minimum healthy percent or Kubernetes pod disruption budgets to keep a minimum number of tasks always running.' },
  ],
  pitfalls: [
    { title: 'Ephemeral storage is not shared between containers in a task', description: 'While multiple containers in a Fargate task definition share the same task-level ephemeral storage, this storage is not shared between separate task instances. If you have a service with three task replicas and a file is written to ephemeral storage in one task, the other two tasks cannot read it. Any data that needs to be accessed across task instances must be stored in EFS, S3, or another external store. This is a common confusion when migrating monoliths that use local filesystem communication between processes.' },
    { title: 'Fargate Spot interruption handling omitted', description: 'Deploying workers on Fargate Spot without implementing SIGTERM handling causes data loss and failed jobs when tasks are interrupted. The ECS task receives SIGTERM two minutes before forceful termination. Applications must catch this signal, stop accepting new work, complete or checkpoint current work, and exit cleanly. SQS consumers must also extend message visibility or return messages to the queue. Skipping this implementation means interrupted tasks abandon partially processed messages that then become invisible in the queue for the visibility timeout period.' },
    { title: 'awsvpc ENI limits causing task launch failures', description: 'Fargate tasks use awsvpc mode and each task gets its own ENI. VPC subnets have limited IP addresses. If all IPs in the task subnet are allocated, new tasks cannot launch. This commonly happens with small subnets like /27 (30 hosts) when scaling to 30+ Fargate tasks. Use dedicated subnets with /24 or larger CIDR blocks for Fargate tasks. Monitor available IPs in CloudWatch under the VPC metrics to alert before exhaustion.' },
    { title: 'EFS mount causes task startup failure without correct permissions', description: 'Fargate tasks mounting EFS fail to start if the task execution role lacks the correct permissions or if the EFS access point authorization is misconfigured. The task execution role needs elasticfilesystem:ClientMount and elasticfilesystem:ClientRootAccess permissions. The EFS security group must allow NFS port 2049 inbound from the Fargate task security group. A common oversight is configuring the EFS security group to allow the VPC CIDR block but forgetting that Fargate tasks use specific security groups, not just any VPC source.' },
  ],
  keyQuestions: [
    { q: 'How do you evaluate whether Fargate or EC2 is more cost-effective for a workload?', a: 'Fargate pricing is based on vCPU and memory consumption per second, so you pay only while tasks are running with no EC2 idle cost. For variable workloads that scale to zero overnight, Fargate can be significantly cheaper than EC2 since you do not pay for idle capacity. EC2 with Reserved Instances or Savings Plans is cheaper for consistently high utilization workloads because reserved pricing can reduce costs by 40 to 60 percent compared to on-demand, and you can achieve higher resource utilization by bin-packing multiple containers on each instance. The crossover point is typically around 60 to 70 percent utilization: below that, Fargate is competitive or cheaper; above that, EC2 with reservations wins on cost. Fargate Spot changes this calculus significantly by offering up to 70 percent discount for fault-tolerant workloads, making Fargate Spot competitive with EC2 Spot while eliminating instance management complexity. Always model the specific workload against both options using the AWS pricing calculator with realistic utilization patterns before committing.' },
    { q: 'How do you run stateful workloads on Fargate with EFS?', a: 'Running stateful workloads on Fargate requires mounting Amazon EFS into the container at task definition time. Define an EFS volume in the task definition specifying the EFS filesystem ID, the access point ID for directory-level isolation, and whether to use IAM authorization for the mount. The container definition maps the volume to a mount point path inside the container. The task execution role must have permissions to call elasticfilesystem:ClientMount on the specific EFS filesystem and access point. The EFS filesystem security group must allow inbound NFS traffic (port 2049) from the Fargate task security group. For multi-task services where each task should have its own isolated persistent storage, create separate EFS access points per task type, each rooted at a different directory. For shared storage where all task instances read and write the same data, mount the same access point across all tasks. Enable EFS encryption at rest and in transit to meet compliance requirements.' },
    { q: 'How does Fargate Spot handle interruptions and how should applications respond?', a: 'When AWS needs to reclaim Fargate Spot capacity, ECS sends a SIGTERM signal to the container and simultaneously updates the task\'s lastStatus to DEPROVISIONING. The container has up to two minutes to finish in-progress work and exit cleanly before AWS sends SIGKILL. Applications should install a SIGTERM handler that stops accepting new requests, completes processing of current in-flight requests, flushes write buffers, closes database connections, and calls os.exit or equivalent to trigger a clean shutdown. For SQS consumers, the handler should stop polling for new messages, let the current message processing complete if it will finish within the window, and if not completable in time, the handler should extend the message visibility timeout to prevent the message from being processed by another consumer. Load balancer deregistration happens automatically when ECS drains the task, but the SIGTERM handler should also stop accepting new HTTP connections and wait for in-flight requests to complete up to the configured deregistration delay timeout. Testing interruption handling by manually stopping Fargate Spot tasks and verifying no data loss is an important part of production readiness.' },
    { q: 'What are the security hardening best practices for Fargate tasks?', a: 'Fargate\'s microVM isolation provides a strong baseline, but application-level hardening is still essential. Define a non-root user in the Dockerfile using the USER directive with a specific UID like 1000 that does not have sudo privileges. Enable readonlyRootFilesystem in the container definition to prevent any writes to the container filesystem, then mount tmpfs volumes at specific paths the application must write to like /tmp or /var/run. Use the linuxParameters section to drop all Linux capabilities with dropCapabilities set to ALL and add back only specific capabilities the application requires, commonly none for web applications. Store secrets in Secrets Manager or Parameter Store and inject them as environment variables via the secrets section in the task definition rather than passing them as plain environment variables through the deployment pipeline. Enable Fargate task metadata endpoint version 4 to allow applications to retrieve task-level IAM credentials and region information. Configure VPC Flow Logs on task subnets to capture all network traffic for security incident investigation. Never enable privileged mode in Fargate container definitions as it provides near-host access and largely defeats the isolation benefits.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonECS/latest/userguide/what-is-fargate.html',
    'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/fargate-security-considerations.html',
    'https://aws.amazon.com/blogs/containers/theoretical-cost-optimization-by-amazon-ecs-launch-type-fargate-vs-ec2/',
  ],
},
{
  id: 'aws-copilot',
  title: 'Copilot — AWS Copilot CLI',
  icon: 'package',
  color: '#ec4899',
  description: 'Developer-focused CLI for deploying containerized applications on ECS and Fargate.',
  introduction: '**AWS Copilot** is an open-source CLI that provides a simplified developer experience for building, releasing, and operating production-ready containerized applications on **ECS** and **Fargate**. Copilot abstracts the complexity of **CloudFormation**, ECS task definitions, ALBs, IAM roles, and VPC configuration behind opinionated higher-level constructs. Developers describe their services in a `manifest.yml` file and Copilot generates and manages the underlying infrastructure. Copilot organizes workloads into **applications**, **environments**, and **services**, matching how teams naturally think about deployments rather than requiring knowledge of every underlying AWS resource.',
  whenToUse: 'When teams want to deploy containers to ECS without deep AWS expertise in CloudFormation and ECS primitives. Ideal for startups and small teams moving from local Docker Compose to AWS, for developers who want a Heroku-like experience on AWS infrastructure, and for organizations that want opinionated defaults with escape hatches via addons. Not ideal when you need full control over generated CloudFormation, when you are already using Terraform or CDK as your infrastructure tool, or when deploying to EKS.',
  keyConcepts: [
    { title: 'Applications, Environments, and Services', description: 'Copilot organizes deployments into three levels. An **application** is the top-level grouping stored as metadata in `SSM Parameter Store`. **Environments** are deployment targets (test, staging, production), each with its own VPC, ECS cluster, and isolated infrastructure. **Services** are individual workloads deployed to one or more environments. This three-level hierarchy enables environment-specific configuration without duplicating service definitions.' },
    { title: 'Service Types', description: 'Copilot provides five service types: **Load Balanced Web Service** (deploys behind an ALB for internet-facing HTTP services), **Backend Service** (private service reachable only within the VPC via Service Connect), **Worker Service** (consumes messages from an SQS queue for async processing), **Scheduled Job** (runs on a cron schedule using ECS scheduled tasks), and **Request-Driven Web Service** (uses App Runner for even simpler auto-scaling including scale-to-zero).' },
    { title: 'Manifest and Configuration', description: 'Each service has a `manifest.yml` file describing its configuration: container image, CPU/memory, port, health check, scaling, environment variables, secrets, and storage. The manifest supports **environment-specific overrides** using YAML anchors, allowing production to have higher minimum count and CPU than test environments while sharing the same base configuration. Manifest files are committed to the repository alongside application code.' },
    { title: 'Addons for Additional AWS Resources', description: '**Addons** are CloudFormation templates placed in `copilot/servicename/addons/` that provision additional AWS resources like RDS, DynamoDB, S3, or ElastiCache. Copilot automatically creates IAM permissions for the service to access addon resources and injects connection strings or ARNs as environment variables into the task. Addons keep custom AWS resources linked to the service lifecycle.' },
    { title: 'Pipelines and Multi-Environment Promotion', description: '**Copilot pipelines** integrate with **CodePipeline** to automate multi-environment deployments. A pipeline is defined in `copilot/pipeline.yml` specifying source (GitHub, BitBucket, CodeCommit), build stage, and deployment stages in order. Each stage can include **manual approval gates** before promoting to production. The pipeline builds the Docker image in **CodeBuild**, pushes it to **ECR**, and deploys to each environment in sequence.' },
    { title: 'Service Connect and Service Discovery', description: 'Copilot configures **ECS Service Connect** for backend-to-backend communication using service names as DNS hostnames within the application. Services call each other by name without knowing IP addresses or load balancer DNS. **Service Connect** provides built-in client-side load balancing and automatic retry, simplifying east-west communication compared to manually configuring Cloud Map or internal ALBs.' },
  ],
  approach: [
    { step: 'Initialize application and environment', details: 'Run copilot app init to create the application and register it in SSM. Then run copilot env init to create test and production environments, each provisioning a VPC with public and private subnets, an ECS cluster, and networking infrastructure via CloudFormation. Environments take three to five minutes to create. Use the default VPC configuration for most workloads or pass custom VPC IDs to import existing network infrastructure.' },
    { step: 'Initialize and configure services', details: 'Run copilot svc init in the directory containing the Dockerfile to create the service manifest. Select the appropriate service type based on the workload pattern. Edit the generated manifest.yml to set CPU, memory, port, health check path, and desired count. Configure scaling policies using the scaling section with target CPU or request count metrics. Add environment variables and secrets referencing SSM Parameter Store or Secrets Manager paths.' },
    { step: 'Add addons for dependent AWS resources', details: 'Create the copilot/servicename/addons/ directory and add CloudFormation templates for additional resources. Use Copilot\'s built-in addon templates for common resources (copilot storage init for S3 and DynamoDB) or write custom CloudFormation for RDS or ElastiCache. Copilot automatically creates IAM policies granting the service access to addon resources and injects resource identifiers as environment variables. Reference the injected environment variables in application code.' },
    { step: 'Set up the deployment pipeline', details: 'Run copilot pipeline init to generate the pipeline.yml and buildspec.yml. Configure the source repository connection in CodePipeline using a GitHub OAuth connection or CodeStar connection. Define the stage order from test to staging to production with manual approval between staging and production. The pipeline automatically triggers on code push to the main branch, runs tests in CodeBuild, builds and pushes the Docker image, and deploys to each environment sequentially.' },
    { step: 'Operate and debug running services', details: 'Use copilot svc show to display service status, ALB URL, task count, and recent deployment history. Use copilot svc logs to stream CloudWatch log output for debugging. Use copilot svc exec to run an interactive shell inside a running task for debugging, requiring the ECS Exec prerequisites of SSM agent and the correct task role permissions. Use copilot svc deploy with the image flag to deploy a specific image tag rather than rebuilding.' },
  ],
  pitfalls: [
    { title: 'Addon CloudFormation dependencies not correctly declared', description: 'When an addon CloudFormation template has resources that depend on other resources within the same template, the dependency order must be explicit using DependsOn attributes. Copilot deploys addons as a single CloudFormation stack, so circular dependencies or missing DependsOn declarations cause stack deployment failures with cryptic CloudFormation error messages. Also, addon templates must export specific output keys that Copilot expects to inject as environment variables, and deviating from the expected output key naming convention causes the environment variable injection to silently fail.' },
    { title: 'Environment configuration drift between manifest and deployed state', description: 'If infrastructure changes are made directly in the AWS console or CloudFormation outside of Copilot, the manifest.yml no longer reflects the actual deployed state. Future copilot svc deploy commands may overwrite manual changes or fail with drift detection errors. All configuration changes must go through Copilot manifest edits and redeployment. Use copilot svc show and compare with manifest to detect drift. Establish a team rule that no one modifies Copilot-managed resources directly in the console.' },
    { title: 'Secret injection requires prior creation in Secrets Manager', description: 'Copilot manifest secrets sections reference Secrets Manager or SSM Parameter Store paths, but Copilot does not create the secret values themselves. If the secret path does not exist when the service is deployed, the ECS task fails to start with a ResourceInitializationError about the missing secret. Secrets must be created in Secrets Manager or Parameter Store before deploying the service. In automated pipelines, add a pre-deployment step to verify all referenced secrets exist before running copilot svc deploy.' },
    { title: 'Pipeline stages require all services to deploy successfully to proceed', description: 'Copilot pipelines deploy all services in an environment in a single stage. If any one service deployment fails, the entire stage fails and promotion to subsequent environments is blocked. This means a bad change to a non-critical service can block production deployments of critical services. The workaround is to structure pipelines with service-level stages or use separate pipelines per service for independent deployment cadences, accepting the operational complexity of managing multiple pipelines.' },
  ],
  keyQuestions: [
    { q: 'What are the Copilot service types and when would you use each?', a: 'Copilot provides five service types targeting different workload patterns. Load Balanced Web Service is for internet-facing HTTP or HTTPS services that need an ALB for SSL termination, path-based routing, and public accessibility. This is the most common type for web APIs and frontend applications. Backend Service is for internal microservices that should only be reachable from within the VPC using Service Connect DNS, with no public endpoint and no ALB. Worker Service is for asynchronous processing where the service consumes messages from an automatically provisioned SQS queue, ideal for decoupled data processing pipelines and event-driven workflows. Scheduled Job uses ECS scheduled tasks triggered by EventBridge cron expressions for periodic tasks like report generation, data cleanup, or batch processing that runs on a time-based schedule rather than continuously. Request-Driven Web Service uses AWS App Runner instead of ECS, providing an even simpler deployment model where App Runner handles all scaling including scale-to-zero, suitable for services with intermittent traffic patterns where the simplicity of App Runner outweighs the per-request pricing model.' },
    { q: 'How do you set up a Copilot pipeline for multi-environment promotion?', a: 'Setting up a Copilot pipeline requires three steps. First, run copilot pipeline init in the repository to generate the pipeline.yml file describing the source, stages, and a buildspec.yml defining the CodeBuild build process. The pipeline.yml specifies the ordered stages such as test then staging then production, and optionally adds requires_approval flags before production stages to create a manual approval gate. Second, connect the source repository to CodePipeline by creating a CodeStar connection to GitHub or Bitbucket in the AWS console and referencing the connection ARN in the pipeline configuration. Third, run copilot pipeline deploy to create the CodePipeline resources in CloudFormation. From that point, every push to the configured branch triggers the pipeline, which builds the Docker image in CodeBuild, pushes it to ECR, and deploys to test automatically. The pipeline then pauses at staging for testing and pauses again at production for manual approval if configured. Failed deployments in any stage block promotion so you always know the production state is the last successfully validated version.' },
    { q: 'How do you provision a database for a Copilot service using addons?', a: 'Copilot addons extend service infrastructure with custom CloudFormation resources that are lifecycle-linked to the service. For an RDS database, run copilot storage init and select Aurora Serverless or RDS to generate a baseline addon template, or write a custom CloudFormation template in copilot/servicename/addons/rds.yml. The template defines the RDS instance, subnet group using the private subnets exported by the Copilot environment stack, and a security group allowing inbound database traffic only from the service\'s security group. Copilot automatically creates an IAM policy allowing the service to read the database credentials from Secrets Manager and attaches it to the task role. Copilot injects the Secrets Manager ARN and optionally the connection string as environment variables using the Outputs section of the addon template with specific naming conventions. The application reads the injected environment variable to get the connection string at runtime. Addons are deployed as part of the service CloudFormation stack, so database creation and service deployment are atomic and the database is automatically deleted when the service is deleted, which requires care in production to prevent accidental data deletion.' },
    { q: 'How does Copilot compare to CDK and Terraform for ECS deployments?', a: 'Copilot, CDK, and Terraform occupy different points on the spectrum between developer simplicity and operational control. Copilot provides the highest level of abstraction with opinionated defaults: a single CLI command deploys a complete ECS service with ALB, IAM roles, Service Connect, and CloudWatch logging configured to AWS best practices. The tradeoff is limited customization; if you need to configure an ECS feature that Copilot does not expose in its manifest schema, you must use addons or wait for Copilot to add support. CDK sits in the middle, offering full access to every AWS resource property through typed constructs while still providing helpful higher-level constructs like the ecs_patterns module that generates ECS services with ALBs in fewer lines. CDK requires TypeScript or Python knowledge and understanding of CloudFormation concepts. Terraform provides maximum portability across cloud providers and is the default choice for organizations with multi-cloud infrastructure or existing Terraform investment. Terraform requires the most AWS ECS knowledge to write correct configuration and does not provide any opinionated defaults. In practice, many teams start with Copilot for rapid initial deployment, then migrate to CDK or Terraform when they outgrow Copilot\'s abstractions or need features that require direct CloudFormation access.' },
  ],
  references: [
    'https://aws.github.io/copilot-cli/',
    'https://aws.github.io/copilot-cli/docs/developing/addons/workload/',
    'https://aws.github.io/copilot-cli/docs/concepts/pipelines/',
  ],
},
{
  id: 'aws-cloudwatch',
  title: 'CloudWatch — Monitoring and Observability',
  icon: 'activity',
  color: '#22c55e',
  description: 'AWS native monitoring, logging, and alerting service.',
  introduction: '**Amazon CloudWatch** collects metrics, logs, and events from AWS services and on-premises resources. It provides a unified view of operational health through **dashboards**, **alarms**, and **Logs Insights** queries, enabling teams to detect anomalies, set automated actions, and troubleshoot issues. Pair with **AWS X-Ray** for distributed tracing and **EventBridge** for event-driven automation.',
  whenToUse: 'Use CloudWatch for all AWS resource monitoring. Combine metrics, logs, and alarms for complete observability. Pair with X-Ray for distributed tracing and with EventBridge for event-driven automation.',
  keyConcepts: [
    { title: 'Metrics', description: '**Time-series data points** from AWS services. EC2 sends CPU and network metrics automatically; custom metrics are published via the `PutMetricData` API. Standard resolution is `1-minute`; high-resolution drops to `1-second` at higher cost.' },
    { title: 'Log Groups', description: '**Log Groups** are containers for log streams from a common source. **Retention policies** (1 day to never-expire) prevent unbounded storage costs. **Metric Filters** extract numeric signals from raw log text and publish them as custom CloudWatch metrics.' },
    { title: 'Alarms', description: '**Alarms** trigger actions when metrics cross thresholds. States are `OK`, `ALARM`, and `INSUFFICIENT_DATA`. **Composite alarms** combine multiple alarms with AND/OR logic to reduce alert fatigue by requiring multiple signals before paging.' },
    { title: 'Logs Insights', description: '**Logs Insights** is an interactive query engine for log groups using a SQL-like syntax. Supports field `parse`, `filter`, `stats`, and visualization without pre-indexing. Useful for ad-hoc troubleshooting and building metric dashboards from unstructured logs.' },
    { title: 'Container Insights', description: '**Container Insights** provides automated dashboards and metrics for ECS, EKS, and Kubernetes on EC2. Collects CPU, memory, disk, and network at the task and pod level via the **CloudWatch agent** or **Fluent Bit** sidecar.' },
  ],
  approach: [
    { step: 'Define metrics and dashboards', details: 'Identify key SLIs for each service before launch. Create dashboards covering error rate, latency percentiles, and saturation metrics. Use automatic dashboards for quick wins then customize for service-specific signals.' },
    { step: 'Set up log aggregation', details: 'Install the CloudWatch agent on EC2 and on-premises servers to push system-level logs and custom application logs. Use the agent config to define log file paths, log group names, and structured field parsing.' },
    { step: 'Configure alarms and actions', details: 'Create alarms on p99 latency and error rate rather than just CPU. Link alarms to SNS topics for alerting and to Auto Scaling policies for automated remediation. Composite alarms reduce noise by requiring multiple signals to fire together.' },
    { step: 'Enable Contributor Insights', details: 'Identify top contributors to metric spikes. For DynamoDB, it surfaces the most consumed partition keys. For VPC Flow Logs, it highlights the highest-traffic IP pairs without requiring custom queries.' },
  ],
  pitfalls: [
    { title: 'Missing custom metrics', description: 'Default EC2 metrics exclude memory and disk utilization. You must install the CloudWatch agent or push custom metrics explicitly via PutMetricData, otherwise memory pressure is invisible until an out-of-memory event occurs.' },
    { title: 'High-resolution cost', description: 'Sub-minute metrics cost 10x standard-resolution metrics. Evaluate whether 1-second granularity is operationally necessary before enabling across all services; most SLO alarms work fine at 1-minute resolution.' },
    { title: 'Log retention unconfigured', description: 'New log groups default to never-expire. Without explicit retention policies, log storage costs grow unboundedly. Apply an organization-level Config rule to enforce retention on all log groups.' },
  ],
  keyQuestions: [
    { q: 'How do you set up effective alerting in CloudWatch?', a: 'Define SLO-based alarms on p99 latency and error rate metrics rather than just CPU utilization, since CPU rarely correlates directly with user-facing impact. Use composite alarms to require multiple signals, such as both elevated error rate and elevated latency, before paging an engineer, which reduces false positives. Route alerts through SNS to an incident management tool like PagerDuty or OpsGenie, and configure OK and INSUFFICIENT_DATA actions to auto-resolve tickets when the condition clears. Set evaluation periods of two to three datapoints to smooth transient spikes before alarming.' },
    { q: 'How do you use CloudWatch Logs Insights for troubleshooting?', a: 'Logs Insights provides a SQL-like query language to search and aggregate across log groups without pre-indexing. I write queries that parse JSON log fields using the parse command, filter by error codes or request IDs, and compute statistics like count, avg, and percentile to find outliers. For distributed systems I correlate trace IDs across multiple log groups in a single query using the LogGroup function. I also use Logs Insights to build metric dashboards from unstructured text logs when structured metrics are not available, extracting latency values from log lines and graphing them over time.' },
    { q: 'What is the difference between CloudWatch Metrics and CloudWatch Logs?', a: 'Metrics are numeric time-series data points stored with dimensions and a namespace; they are pre-aggregated by AWS and queried efficiently for dashboards, alarms, and anomaly detection. Logs are raw text or structured event records retained for forensic investigation and pattern matching, with queries running on-demand over stored data. Metric Filters bridge the two by extracting numeric values from log patterns, such as error counts or latency values, and publishing them as custom metrics to enable alarms on log-derived signals without needing to store every event in a metric time series.' },
    { q: 'How do you monitor containerized workloads with CloudWatch?', a: 'Container Insights automatically collects CPU, memory, network, and disk metrics at the cluster, service, task, and pod level for ECS and EKS by deploying the CloudWatch agent as a DaemonSet or sidecar. I enable Container Insights at the cluster level to get automatic dashboards and then add custom log routing from Fluent Bit to push application logs from containers to log groups organized by service name. For EKS I also deploy the CloudWatch Observability add-on, which bundles the agent, Fluent Bit, and the operator in a single managed deployment so it stays updated with Kubernetes version upgrades.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/',
    'https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/',
  ],
  visualizations: [
    { title: 'CloudWatch Observability Pillars', caption: 'Full data-flow from EC2, Lambda, and application sources into Metrics, Logs, and X-Ray Traces, feeding alarms, dashboards, and EventBridge rules.', image: '/diagrams/cloud/aws-cloudwatch-pillars.png' },
    { title: 'Alarm States and Auto-Scaling', caption: 'CloudWatch alarm state machine (OK / ALARM / INSUFFICIENT_DATA) triggering SNS alerts and Auto Scaling scale-out and scale-in actions.', image: '/diagrams/cloud/aws-cloudwatch-alarm.png' },
  ],
},
{
  id: 'aws-cloudtrail',
  title: 'CloudTrail — API Audit Logging',
  icon: 'shield',
  color: '#22c55e',
  description: 'AWS API call history for governance, compliance, and security auditing.',
  introduction: '**AWS CloudTrail** records every API call made in your AWS account, capturing who made the call, from where, and what resource was affected. It is the primary tool for **compliance auditing**, **security investigation**, and **change management** across all AWS services, storing events in **S3** with optional real-time streaming to **CloudWatch Logs** and **EventBridge**.',
  whenToUse: 'Enable CloudTrail in every account and region from day one. Use it for security investigations, compliance reports, change tracking, and detecting unauthorized access. Combine with AWS Security Hub and GuardDuty for automated threat detection.',
  keyConcepts: [
    { title: 'Management Events', description: '**Control-plane API calls** such as creating EC2 instances, modifying IAM policies, and deleting S3 buckets. Enabled by default and critical for change auditing. Includes both read (`Describe*`, `List*`) and write (`Create*`, `Delete*`) operations.' },
    { title: 'Data Events', description: '**High-volume object-level operations** like `S3:GetObject`, `S3:PutObject`, and `Lambda:Invoke`. Disabled by default due to volume; enable selectively for sensitive buckets. Can generate millions of events per day at $0.10 per 100,000 events.' },
    { title: 'Insights Events', description: '**CloudTrail Insights** detects anomalies by comparing API call rates against a 7-day baseline. Useful for detecting **credential abuse** or misconfigured automation that is making unexpected bursts of API calls. Delivered to the same S3 bucket and to EventBridge.' },
    { title: 'Organization Trail', description: '**Organization Trail** is a single trail that collects events from all accounts in an **AWS Organization** and stores them in a central S3 bucket in the management account. Prevents account-level tampering and eliminates per-account trail configuration.' },
    { title: 'Event History', description: '**Free 90-day lookup** of management events available in the console and CLI with no trail required. Trails extend retention indefinitely by writing to **S3**, with optional **Athena** querying for cost-efficient forensic investigations over months of history.' },
  ],
  approach: [
    { step: 'Create an organization trail', details: 'Enable a single multi-region trail at the Organization level so all member accounts are covered without per-account configuration. Write to a dedicated S3 bucket in the security account with S3 Object Lock to prevent deletion.' },
    { step: 'Enable log file validation', details: 'CloudTrail computes a SHA-256 hash of each log file and delivers a digest file. Use aws cloudtrail validate-logs to confirm no files were modified or deleted, satisfying compliance requirements for tamper-evident audit logs.' },
    { step: 'Stream to CloudWatch Logs and EventBridge', details: 'Push trail events to a CloudWatch Log Group for Logs Insights queries and to EventBridge for real-time alerting on sensitive actions like IAM policy changes or root login.' },
    { step: 'Query with Athena', details: 'Partition CloudTrail logs by account, region, and date in S3 to enable cost-efficient Athena queries for forensic investigations covering months of history without loading data into a database.' },
  ],
  pitfalls: [
    { title: 'Single-region trail gap', description: 'Creating a trail in only one region misses global service events and multi-region activity. Always enable multi-region trails; AWS recommends a single organization-wide trail over per-region trails.' },
    { title: 'Unprotected S3 bucket', description: 'If the CloudTrail destination bucket lacks a restrictive bucket policy, an attacker who gains account access can delete logs to cover their tracks. Use S3 Object Lock in compliance mode and block all public access.' },
    { title: 'Data event cost surprise', description: 'Enabling data events on a high-traffic S3 bucket or Lambda function can generate millions of events per day at $0.10 per 100,000 events. Scope data events to sensitive buckets only, not account-wide.' },
  ],
  keyQuestions: [
    { q: 'How do you investigate a suspected privilege escalation using CloudTrail?', a: 'I start by querying CloudTrail for the suspected IAM principal and looking for AttachUserPolicy, CreateAccessKey, AssumeRole, or PutUserPolicy events in the relevant time window. Using Athena against partitioned S3 logs I filter by userIdentity.arn and sort by eventTime to reconstruct the sequence of actions. I then trace the role assumption chain by following AssumeRole events to see what roles were chained together. Finally I compare the permissions that existed before and after the change to determine what additional access was gained and whether it was used to access sensitive resources.' },
    { q: 'How do you ensure CloudTrail logs cannot be tampered with?', a: 'I enable log file validation when creating the trail, which causes CloudTrail to compute a SHA-256 digest of each log file and deliver a signed digest file every hour. The aws cloudtrail validate-logs command verifies the hash chain and detects any modification or deletion. I store logs in an S3 bucket in the dedicated security account that production accounts cannot write to or delete from, enforced by a bucket policy that only allows CloudTrail PutObject. I also enable S3 Object Lock in compliance mode with a retention period matching the compliance requirement, which prevents deletion even by the root user.' },
    { q: 'What is the difference between CloudTrail and AWS Config?', a: 'CloudTrail records who made API calls and when, providing an event-based audit log of actions. AWS Config records the state of AWS resources at regular intervals and whenever a change occurs, providing a configuration history and the ability to evaluate resources against compliance rules. CloudTrail answers the question of what happened and who did it; Config answers what does my infrastructure look like now and how has it changed. For a complete governance posture I use both together: Config to detect when a resource drifts out of compliance and CloudTrail to determine which API call caused the drift and which identity made it.' },
    { q: 'How do you use CloudTrail Insights?', a: 'CloudTrail Insights analyzes management event write API call rates against a 7-day baseline and alerts when the current rate deviates significantly. I enable Insights on trails in production accounts to detect sudden spikes in resource creation, which could indicate a misconfigured automation that is provisioning resources in a loop or a credential theft scenario where an attacker is trying to exfiltrate data rapidly. Insights events are delivered to the same S3 bucket and to EventBridge, so I trigger a Lambda function on each Insights event to notify the security team and optionally invoke an automated response playbook.' },
  ],
  references: [
    'https://docs.aws.amazon.com/awscloudtrail/latest/userguide/',
    'https://docs.aws.amazon.com/awscloudtrail/latest/userguide/cloudtrail-log-file-validation-intro.html',
  ],
},
{
  id: 'aws-xray',
  title: 'X-Ray — Distributed Tracing',
  icon: 'activity',
  color: '#22c55e',
  description: 'End-to-end distributed tracing for AWS applications.',
  introduction: '**AWS X-Ray** traces requests as they travel through distributed applications, collecting timing data from each service and producing a **service map** that shows call relationships, latency, and error rates. It integrates natively with **Lambda**, **API Gateway**, **ECS**, and **Elastic Beanstalk** and supports custom instrumentation through SDKs for Node.js, Python, Java, and Go.',
  whenToUse: 'Use X-Ray when debugging latency in microservices architectures where a single user request fans out across multiple services. It is especially valuable for identifying which downstream dependency is causing tail latency or which Lambda cold start is inflating response times.',
  keyConcepts: [
    { title: 'Trace', description: 'A **Trace** is a collection of segments representing a single end-to-end request. Identified by a unique **Trace ID** propagated across service boundaries in HTTP headers (`X-Amzn-Trace-Id`) or SQS message attributes.' },
    { title: 'Segment', description: 'A **Segment** is a single service\'s contribution to a trace. Contains start and end timestamps, metadata, **annotations** (searchable key-value pairs), and subsegments for downstream calls.' },
    { title: 'Subsegment', description: 'A **Subsegment** is a granular unit within a segment representing an outgoing call, SQL query, or custom operation. Enables pinpointing which specific `DynamoDB` call or HTTP request caused latency spikes.' },
    { title: 'Service Map', description: 'The **Service Map** is a visual graph of all services and their connections, annotated with request rate, error rate, and latency percentiles. Automatically generated from trace data without manual configuration.' },
    { title: 'Sampling', description: '**Sampling** controls the percentage of requests traced to manage cost. The default rule samples `5%` of requests with a minimum of 1 request per second. **Custom sampling rules** target specific paths, HTTP methods, or service names with priority ordering.' },
  ],
  approach: [
    { step: 'Enable active tracing', details: 'Turn on active tracing in Lambda function configuration and API Gateway stages with a single toggle. X-Ray automatically instruments incoming requests and propagates the Trace ID to downstream Lambda invocations.' },
    { step: 'Instrument the SDK', details: 'Wrap the AWS SDK with the X-Ray SDK using AWSXRay.captureAWS(require("aws-sdk")) to automatically trace all AWS service calls as subsegments. For HTTP calls use AWSXRay.captureHTTPsGlobal to trace external requests.' },
    { step: 'Add annotations and metadata', details: 'Use segment.addAnnotation for searchable key-value pairs like user ID or order ID. Use segment.addMetadata for larger non-searchable data. Annotations enable filtering traces by business context in the X-Ray console.' },
    { step: 'Configure sampling rules', details: 'Create custom sampling rules in the X-Ray console to increase sampling for critical paths and reduce it for health checks. Rules match on host, URL path, HTTP method, and service name with priority ordering.' },
  ],
  pitfalls: [
    { title: 'Missing context propagation', description: 'When passing work through SQS or SNS, the Trace ID must be explicitly included in the message attributes or the receiving Lambda starts a new disconnected trace. Use the X-Ray SDK send helpers to carry context automatically.' },
    { title: 'Cold start inflation', description: 'X-Ray subsegments for cold start initialization appear in the trace but are counted against the function duration. Be careful when drawing latency SLO conclusions from Lambda traces that mix warm and cold invocations.' },
    { title: 'Sampling gaps in analytics', description: 'Default 5% sampling means most traces are never recorded. For low-traffic critical paths, increase the reservoir rate so enough traces are captured to diagnose rare failures statistically.' },
  ],
  keyQuestions: [
    { q: 'How do you find the root cause of a latency spike in a microservices system using X-Ray?', a: 'I start with the X-Ray service map to identify which service or dependency shows elevated p99 latency during the spike window. I then drill into the traces for that service, sorting by duration to find the slowest requests. Within a slow trace I expand the subsegments to see which specific downstream call, whether a DynamoDB query, an HTTP API, or a database connection wait, consumed the most time. I use trace annotations like request type or customer tier to determine if the spike affects a specific segment of traffic, which helps separate a broad infrastructure issue from a data-specific hotspot.' },
    { q: 'How does X-Ray integrate with Lambda and API Gateway?', a: 'API Gateway supports active tracing with a toggle on the stage settings, which causes it to generate a root segment for each request and inject the X-Ray Trace ID header before forwarding to the integration. Lambda also supports active tracing, which makes it generate a segment for the invocation and automatically create subsegments for AWS SDK calls when the X-Ray SDK is initialized. The Trace ID flows automatically from API Gateway through Lambda to any downstream Lambda functions it invokes synchronously. For asynchronous SQS-triggered Lambda functions, the SDK provides helpers to extract and restore the trace context from SQS message attributes.' },
    { q: 'What is the difference between X-Ray and CloudWatch ServiceLens?', a: 'X-Ray is the underlying distributed tracing service that collects and stores segment data and generates the trace graph. CloudWatch ServiceLens is a CloudWatch console feature that combines X-Ray service maps with CloudWatch metrics and alarms in a single view, adding health status indicators from CloudWatch alarms onto each service node in the map. ServiceLens does not change what data is collected; it adds a unified monitoring layer on top of existing X-Ray and CloudWatch data so operators can see both the trace topology and the metric health without switching between services.' },
  ],
  references: [
    'https://docs.aws.amazon.com/xray/latest/devguide/',
    'https://docs.aws.amazon.com/lambda/latest/dg/services-xray.html',
  ],
},
{
  id: 'aws-config',
  title: 'AWS Config — Resource Configuration History',
  icon: 'settings',
  color: '#22c55e',
  description: 'Continuous recording and compliance evaluation of AWS resource configurations.',
  introduction: '**AWS Config** continuously records the configuration state of AWS resources and evaluates them against compliance rules. It maintains a **configuration history and timeline** for every resource, enabling teams to answer what a resource looked like at a specific point in time and whether resources comply with organizational security and operational policies.',
  whenToUse: 'Enable AWS Config in every account and region for compliance frameworks like SOC 2, PCI DSS, and HIPAA. Use it to detect configuration drift, track changes that caused incidents, and enforce guardrails through Config Rules with auto-remediation.',
  keyConcepts: [
    { title: 'Configuration Item', description: 'A **Configuration Item** is a point-in-time snapshot of a resource\'s configuration, including relationships to other resources. Recorded whenever a resource changes and delivered to an **S3 bucket** for retention.' },
    { title: 'Config Rules', description: '**Config Rules** are compliance checks against resource configurations. **AWS Managed Rules** cover common controls (S3 public access, MFA enabled, required tags); **custom rules** use Lambda functions for organization-specific logic.' },
    { title: 'Conformance Packs', description: '**Conformance Packs** are collections of Config Rules and remediation actions packaged as a single unit. AWS provides sample packs for **CIS**, **PCI DSS**, and **HIPAA**; teams can author custom packs for organization-specific controls.' },
    { title: 'Aggregator', description: 'An **Aggregator** collects Config data from multiple accounts and regions into a single view. Essential for multi-account environments to report compliance posture and detect non-compliant resources across the organization from one place.' },
    { title: 'Remediation', description: '**Config Rules** support manual or **automatic remediation** using **SSM Automation** documents. Auto-remediation corrects non-compliant resources without human intervention — for example, automatically removing public access from an S3 bucket when the rule detects it.' },
  ],
  approach: [
    { step: 'Enable Config organization-wide', details: 'Use the AWS Organizations integration to enable Config in all member accounts with delegated administrator. All configuration items stream to a central S3 bucket in the security account.' },
    { step: 'Deploy conformance packs', details: 'Start with AWS-provided sample packs for your compliance framework, such as the Operational Best Practices for CIS AWS Foundations pack. This deploys dozens of rules instantly without writing custom code.' },
    { step: 'Add custom rules for business logic', details: 'Write Lambda-based custom rules for organization-specific policies such as requiring cost-allocation tags on all resources or restricting EC2 instance types to an approved list.' },
    { step: 'Configure auto-remediation', details: 'For well-understood non-compliant states like public S3 buckets or unrestricted security groups, attach SSM Automation runbooks to auto-remediate immediately after detection, reducing the window of exposure.' },
  ],
  pitfalls: [
    { title: 'Config vs CloudTrail confusion', description: 'Config records what a resource looks like; CloudTrail records who changed it. Teams sometimes rely on Config alone for root cause analysis and miss the who information. Use both together for complete incident reconstruction.' },
    { title: 'Rule evaluation lag', description: 'Change-triggered rules evaluate within minutes but periodic rules only run every 1, 3, 6, or 24 hours. Do not use periodic rules for security controls that require near-real-time detection of risky configurations.' },
    { title: 'Cost from high-change resources', description: 'Config charges per configuration item recorded. Frequent resource changes, such as Auto Scaling group instance launches, generate large numbers of items. Scope recording to resource types relevant to compliance rather than enabling all types globally.' },
  ],
  keyQuestions: [
    { q: 'How do you use AWS Config to track a configuration change that caused an incident?', a: 'In the Config console I navigate to the resource involved and open its configuration timeline, which shows every recorded configuration item in chronological order. I identify the item recorded just before the incident started and compare it with the previous item using the diff view to see exactly which fields changed. I then correlate the timestamp of the configuration change with CloudTrail to identify which API call caused it and which identity made the call. This combination of Config for the what and CloudTrail for the who gives a complete picture of the change chain.' },
    { q: 'How do you enforce compliance across a multi-account AWS organization using Config?', a: 'I deploy Config with the Organizations integration, designating a security account as the delegated administrator. An aggregator in that account collects compliance data from all member accounts and all regions into a single dashboard. I deploy conformance packs as organization-wide packs from the management account so all member accounts receive the same rule set without per-account configuration. For auto-remediation I use cross-account IAM roles that the remediation SSM runbook assumes in each target account, ensuring consistent remediation behavior regardless of which account triggers the non-compliant finding.' },
    { q: 'What is the difference between Config Rules and AWS Security Hub controls?', a: 'Config Rules evaluate specific resource configurations against defined policies and record compliance state per resource. Security Hub aggregates findings from multiple security services including Config Rules, GuardDuty, Inspector, and Macie into a normalized finding format and calculates a security score by control. Security Hub controls map Config Rules to specific compliance framework requirements so a single Config Rule can satisfy multiple framework controls simultaneously. In practice I use Config as the detection engine and Security Hub as the consolidated reporting and workflow layer, with findings routed to a ticketing system for tracking remediation.' },
  ],
  references: [
    'https://docs.aws.amazon.com/config/latest/developerguide/',
    'https://docs.aws.amazon.com/config/latest/developerguide/conformance-packs.html',
  ],
},
{
  id: 'aws-trusted-advisor',
  title: 'Trusted Advisor — Best Practice Recommendations',
  icon: 'check-circle',
  color: '#22c55e',
  description: 'Automated best practice checks across cost, performance, security, fault tolerance, and service limits.',
  introduction: '**AWS Trusted Advisor** analyzes your AWS account and provides real-time recommendations across five pillars: **Cost Optimization** (idle resources, reserved instance coverage), **Performance** (EBS throughput, CloudFront configuration), **Security** (open ports, MFA, IAM), **Fault Tolerance** (Multi-AZ, backups), and **Service Limits** (approaching quota thresholds). It compares your configuration against AWS best practices and highlights gaps with prioritized action items.',
  whenToUse: 'Review Trusted Advisor regularly as part of operational reviews and before architecture audits. Use it to identify idle resources wasting money, security groups with unrestricted access, and accounts approaching service limit thresholds before they cause incidents.',
  keyConcepts: [
    { title: 'Check Categories', description: 'Five pillars covered by Trusted Advisor checks: **Cost Optimization** (idle EC2 instances, unattached EBS volumes, RI coverage), **Performance** (EBS throughput, CloudFront usage), **Security** (open ports, MFA on root, IAM access keys), **Fault Tolerance** (Multi-AZ RDS, EBS snapshots), and **Service Limits** (resources approaching quota thresholds).' },
    { title: 'Check Status', description: 'Each check returns `red` (action recommended), `yellow` (investigation recommended), or `green` (no problems detected). Status refreshes weekly by default; **Business and Enterprise support** tiers allow on-demand refresh via the Trusted Advisor API.' },
    { title: 'Organizational View', description: '**Business and Enterprise support** customers can view Trusted Advisor findings across all accounts in an **AWS Organization** from a single dashboard, aggregating recommendations by category and surfacing the highest-impact issues across the fleet.' },
    { title: 'EventBridge Integration', description: '**Trusted Advisor** publishes check status change events to **EventBridge**, enabling automated workflows that notify teams or trigger remediation when a check transitions from `green` to `red`. Use this to create tickets automatically for every new finding.' },
    { title: 'Service Quotas Integration', description: '**Service Limits** checks in Trusted Advisor link directly to **Service Quotas**, where you can request increases for limits approaching the threshold without navigating to each service console separately. Alerts at `80%` of the limit give enough lead time to request an increase.' },
  ],
  approach: [
    { step: 'Enable organizational view', details: 'Delegate Trusted Advisor administration to a management account and enable the organizational view to see all member account recommendations aggregated in one place, prioritizing the most impactful issues across the fleet.' },
    { step: 'Automate alerting on status changes', details: 'Create an EventBridge rule matching Trusted Advisor check status change events and route them to SNS for notification. Specifically alert on security checks transitioning to red, such as newly opened unrestricted security groups.' },
    { step: 'Review cost optimization weekly', details: 'Schedule a weekly review of the cost optimization category to identify idle EC2 instances, unattached EBS volumes, and low-utilization RDS instances. Feed findings into a cost review meeting with resource owners.' },
    { step: 'Monitor service limits proactively', details: 'Set EventBridge alerts when service limit checks reach yellow (80% of limit) to request quota increases before traffic spikes cause throttling. This is particularly important for EC2 instance vCPU limits in new regions.' },
  ],
  pitfalls: [
    { title: 'Basic support limitation', description: 'Accounts on Basic or Developer support only have access to six core security checks. The full set of 115+ checks requires Business or Enterprise support. Many teams do not realize the scope difference until they need a specific check.' },
    { title: 'Stale data without refresh', description: 'Checks refresh weekly by default on lower support tiers. On Business and Enterprise support, use the API to trigger a refresh before audits rather than relying on week-old data that may not reflect recent changes.' },
    { title: 'Recommendation without action', description: 'Teams often review Trusted Advisor findings but do not track them to resolution. Integrate findings with a ticketing system via EventBridge so each red finding creates a ticket with an owner and SLA for remediation.' },
  ],
  keyQuestions: [
    { q: 'How do you use Trusted Advisor to reduce AWS costs?', a: 'I start with the cost optimization category, which surfaces idle EC2 instances with CPU below 10% over 14 days, unattached EBS volumes, and Elastic IP addresses not associated with running instances. For Reserved Instance recommendations I look at the RI utilization and coverage checks, which show the gap between current on-demand spend and the savings available by purchasing reservations for predictable workloads. I export the findings to a spreadsheet, assign resource owners, and track remediation in a monthly cost review meeting, using the estimated monthly savings figures in each check to prioritize which items to address first.' },
    { q: 'How does Trusted Advisor differ from AWS Security Hub?', a: 'Trusted Advisor is a best-practice advisor that covers cost, performance, fault tolerance, and service limits in addition to security, and it generates its own findings from analyzing account configuration. Security Hub is a dedicated security posture management service that aggregates findings from multiple sources including GuardDuty, Inspector, Macie, and Config Rules, normalizes them to a standard finding format, and maps them to compliance framework controls. Security Hub also includes its own set of security controls that overlap with some Trusted Advisor security checks. In practice Trusted Advisor provides broad operational guidance while Security Hub provides depth on security posture with compliance mapping and workflow integration.' },
    { q: 'How do you prevent service limit throttling using Trusted Advisor?', a: 'I configure EventBridge rules to alert when Trusted Advisor service limit checks transition to yellow, which indicates usage has reached 80% of the current limit. For limits critical to scaling, such as EC2 vCPU counts per region or Lambda concurrent executions, I request increases preemptively when the check hits yellow rather than waiting until the limit is reached. I maintain a runbook for each limit-sensitive service that documents the current limit, the request process, and the lead time for approval, so the team can act within the alert window before traffic growth causes throttling. For accounts in new regions I run a manual Trusted Advisor refresh on day one to baseline the limit checks before workloads are deployed.' },
  ],
  references: [
    'https://docs.aws.amazon.com/awssupport/latest/user/trusted-advisor.html',
    'https://docs.aws.amazon.com/awssupport/latest/user/organizational-view.html',
  ],
},
{
  id: 'aws-health',
  title: 'AWS Health — Service Event Notifications',
  icon: 'heart',
  color: '#22c55e',
  description: 'Personalized notifications of AWS service events affecting your resources.',
  introduction: '**AWS Health** provides a personalized view of the health of AWS services and accounts, surfacing events that may impact your specific resources — unlike the public **AWS Service Health Dashboard**, which shows only general availability. It delivers notifications about scheduled maintenance, service disruptions, and security bulletins through the **AWS Health console**, **EventBridge**, and the **Health API**.',
  whenToUse: 'Use AWS Health to proactively receive maintenance notifications before they cause unexpected downtime, to integrate health events into your incident management workflow, and to build automated runbooks that respond to AWS-initiated events like EC2 retirement notices.',
  keyConcepts: [
    { title: 'Account Events', description: '**Account Events** are specific to your account and resources — for example, an EC2 instance scheduled for retirement or an RDS database requiring a maintenance patch. More actionable than public service events because they include the specific resource ARNs affected.' },
    { title: 'Service Events', description: '**Service Events** are general AWS service availability issues visible to any account. Less targeted than account events but useful for correlating an outage or degradation with your application\'s behavior during the same time window.' },
    { title: 'Organizational Health View', description: '**Business and Enterprise support** customers can view health events across all accounts in an **AWS Organization** from a single dashboard, enabling centralized incident coordination without logging into each account separately.' },
    { title: 'EventBridge Integration', description: 'All Health events are published to **EventBridge** with source `aws.health`. Rules can trigger **Lambda**, **SNS**, or **Step Functions** workflows to automate responses — for example, automatically stopping and restarting an EC2 instance before its retirement deadline.' },
    { title: 'Event Categories', description: 'Events are categorized as `issue` (active service disruption requiring immediate attention), `scheduledChange` (planned maintenance with advance notice), and `accountNotification` (security or account-level notice). Different categories warrant different response urgencies and can be filtered separately in EventBridge rules.' },
  ],
  approach: [
    { step: 'Subscribe to Health events via EventBridge', details: 'Create an EventBridge rule matching source aws.health to forward all health events to an SNS topic that notifies the operations team. Filter by event category to separate incidents from scheduled maintenance.' },
    { step: 'Automate EC2 retirement responses', details: 'AWS sends account events when EC2 instances are scheduled for retirement due to underlying hardware issues. Build a Lambda function triggered by the aws.health EC2 retirement event that automatically stops and starts the instance to migrate it to new hardware before the retirement date.' },
    { step: 'Enable organizational view', details: 'Delegate AWS Health administration to a central operations account so all member account events appear in one dashboard. This is critical during major AWS incidents to determine which accounts and services are affected without logging into each account separately.' },
    { step: 'Integrate with incident management', details: 'Route Health events to PagerDuty or OpsGenie via EventBridge so they create incidents with the affected resource ARN and AWS event description pre-populated, reducing the time to triage when an AWS-initiated event correlates with an application alert.' },
  ],
  pitfalls: [
    { title: 'Confusing public and account dashboards', description: 'The AWS Service Health Dashboard at status.aws.amazon.com shows aggregate service health but not your specific resources. The AWS Health console shows account-specific events. Teams sometimes check the public dashboard during incidents and miss account-specific notices that explain the impact more precisely.' },
    { title: 'Missing retirement window', description: 'EC2 retirement events have a deadline after which the instance is forcibly stopped. Without automated alerting, teams may miss the notification and experience unplanned downtime when AWS terminates the instance on the retirement date.' },
    { title: 'Organizational view requires Business support', description: 'The organizational health view is only available with Business or Enterprise support on the management account. Teams on Developer support cannot aggregate events across accounts, requiring manual per-account checks during incidents.' },
  ],
  keyQuestions: [
    { q: 'How do you automate responses to EC2 instance retirement notices from AWS Health?', a: 'When AWS identifies hardware degradation, it sends an account event with eventTypeCode aws.ec2.instance-retirement-scheduled listing the affected instance IDs and the retirement deadline. I create an EventBridge rule matching this event type that triggers a Lambda function. The function checks whether the instance is part of an Auto Scaling group; if so, it terminates the instance and lets Auto Scaling launch a replacement on healthy hardware. If the instance is standalone, the function stops and starts it, which migrates it to new hardware and clears the retirement notice. The function posts a notification to Slack with the instance ID and action taken for visibility.' },
    { q: 'How does AWS Health differ from the AWS Service Health Dashboard?', a: 'The AWS Service Health Dashboard is a public webpage showing the aggregate health of each AWS service per region, visible to anyone regardless of account status. AWS Health is a private, personalized service that surfaces only the events affecting your specific resources, providing resource ARNs, affected account IDs, and recommended actions. For example, the public dashboard may show an EC2 API degradation affecting a region broadly, while AWS Health would show that three specific instances in your account are impacted and link to guidance for that scenario. AWS Health events are also programmatically accessible via EventBridge and the Health API, unlike the public dashboard.' },
    { q: 'How do you use AWS Health in a multi-account organization during an incident?', a: 'I enable the organizational view by granting the management account delegated access to all member accounts\' Health data. During an incident I open the organizational health view, which shows all active events across accounts filtered by event category and affected service. This immediately shows whether the issue is isolated to one account or affects multiple accounts, and which specific resource ARNs are impacted across the organization. I export the event list and cross-reference with the application dependency map to identify which customer-facing services are at risk. The event timeline in Health also shows when AWS first detected the issue and what mitigations are underway, which I use to set customer communication expectations.' },
  ],
  references: [
    'https://docs.aws.amazon.com/health/latest/ug/',
    'https://docs.aws.amazon.com/health/latest/ug/cloudwatch-events-health.html',
  ],
},
{
  id: 'aws-sagemaker',
  title: 'SageMaker — Managed ML Platform',
  icon: 'brain',
  color: '#a855f7',
  description: 'End-to-end managed platform for building, training, and deploying ML models.',
  introduction: '**Amazon SageMaker** is a fully managed machine learning platform that covers the entire ML lifecycle — from data preparation and model training to deployment and monitoring. It provides **Studio notebooks** (Jupyter-based), distributed training infrastructure, managed model hosting with auto-scaling, and MLOps capabilities through **SageMaker Pipelines**, **Feature Store**, and **Model Monitor**.',
  whenToUse: 'Use SageMaker when you need to train custom models at scale, manage the full ML lifecycle in one platform, or operationalize ML workflows with CI/CD. It is best suited for teams building bespoke models rather than consuming pre-built APIs like Rekognition or Comprehend.',
  keyConcepts: [
    { title: 'Training Jobs', description: '**Training Jobs** are managed compute jobs that run training scripts in Docker containers. Supports **distributed training** across multiple instances using data parallelism and model parallelism strategies. Billed per second of instance usage.' },
    { title: 'Endpoints', description: '**Real-time inference endpoints** that auto-scale the number of instances based on traffic. Supports **multiple model variants** with traffic weighting for A/B testing. **Multi-model endpoints** host many models on a single instance to reduce cost.' },
    { title: 'SageMaker Pipelines', description: '**SageMaker Pipelines** is an ML workflow orchestration service that chains `Processing`, `Training`, `Evaluation`, `Condition`, and `RegisterModel` steps. Integrates with **CodePipeline** for CI/CD-triggered model retraining and deployment.' },
    { title: 'Feature Store', description: '**SageMaker Feature Store** is a centralized repository for ML features with both an **online store** (low-latency real-time lookup) and an **offline store** (S3-backed for training). Eliminates feature recomputation and ensures consistency between training and inference.' },
    { title: 'Model Monitor', description: '**Model Monitor** continuously monitors deployed models for data quality drift, model quality drift, bias drift, and feature attribution drift. Alerts when live inference data diverges from the training distribution baseline, enabling proactive model retraining.' },
  ],
  approach: [
    { step: 'Structure data in S3', details: 'Organize training, validation, and test data in S3 with consistent prefixes. Use SageMaker Processing Jobs for feature engineering before training to keep preprocessing logic versioned and reproducible.' },
    { step: 'Use built-in algorithms or custom containers', details: 'Start with SageMaker built-in algorithms like XGBoost or BlazingText for common tasks; bring custom Docker containers for specialized frameworks. This avoids managing ML infrastructure while retaining flexibility.' },
    { step: 'Automate with Pipelines', details: 'Define the full training workflow as a SageMaker Pipeline with conditional steps that register the model to the Model Registry only if evaluation metrics exceed a threshold, preventing regressions from reaching production.' },
    { step: 'Monitor deployed models', details: 'Capture inference inputs and outputs to S3 using Data Capture. Configure Model Monitor schedules to compare live data distributions against the training baseline and alert on drift before model performance degrades visibly.' },
  ],
  pitfalls: [
    { title: 'Idle notebook costs', description: 'SageMaker Studio notebooks and classic notebook instances run on persistent EC2 instances that accrue cost when idle. Set lifecycle configurations to auto-stop notebooks after a period of inactivity.' },
    { title: 'Cold start latency on endpoints', description: 'Single-instance endpoints go idle and cold-start when not called for extended periods, causing initial requests to experience multi-second delays. Use provisioned concurrency or keep-alive invocations for latency-sensitive endpoints.' },
    { title: 'Skipping Model Registry', description: 'Teams that deploy models directly from notebooks bypass the approval workflow and audit trail. All model versions should be registered in the SageMaker Model Registry with metadata before deployment so the lineage from training data to endpoint is traceable.' },
  ],
  keyQuestions: [
    { q: 'How do you set up a repeatable ML training and deployment pipeline using SageMaker?', a: 'I define the full workflow as a SageMaker Pipeline with distinct steps: a Processing step for feature engineering, a Training step for model training, an Evaluation step that computes metrics on the test set, a Condition step that only proceeds if accuracy exceeds a threshold, and a RegisterModel step that adds the model version to the Model Registry with the evaluation metrics attached. I trigger the pipeline from CodePipeline when new training data lands in S3, and I configure the Model Registry to require manual approval before the model is deployed to the production endpoint, giving the team a gate to review metrics before any change reaches live traffic.' },
    { q: 'How do you reduce inference latency on SageMaker endpoints?', a: 'I evaluate several options depending on latency targets. For synchronous single-digit millisecond requirements I use multi-model endpoints with model caching to share instance capacity across models and reduce per-model cold starts. For transformer-based models I apply SageMaker Neo to compile the model for the target hardware, which typically reduces latency by 25 to 50 percent. I also configure auto-scaling with a minimum instance count above zero to avoid scale-to-zero cold starts. For batch workloads that do not need real-time latency I use SageMaker Batch Transform instead of endpoints to avoid idle instance costs.' },
    { q: 'How do you detect and respond to model drift in production?', a: 'I configure Data Capture on the endpoint to log a sample of inference requests and responses to S3. I then create a Model Monitor baseline job that profiles the training data distribution using statistical measures for each feature. A scheduled monitoring job runs hourly, compares the live inference data against the baseline, and generates a violation report when a feature\'s distribution diverges beyond a configured threshold. The monitoring job publishes metrics to CloudWatch, and I set a CloudWatch alarm that triggers an SNS notification to the ML team when violations exceed a weekly count. The response playbook includes retraining the model on recent data and running the SageMaker Pipeline to deploy the updated model through the approval workflow.' },
  ],
  references: [
    'https://docs.aws.amazon.com/sagemaker/latest/dg/',
    'https://docs.aws.amazon.com/sagemaker/latest/dg/model-monitor.html',
  ],
},
{
  id: 'aws-bedrock',
  title: 'Bedrock — Foundation Model API',
  icon: 'zap',
  color: '#a855f7',
  description: 'Managed API access to foundation models from Anthropic, Amazon, Meta, and others.',
  introduction: '**Amazon Bedrock** provides a unified API to access foundation models from multiple providers — including **Anthropic Claude**, **Amazon Titan**, **Meta Llama**, **Mistral**, and **Cohere** — without managing infrastructure. It supports text generation, embeddings, image generation, and **Bedrock Agents** that can invoke tools and retrieve context from knowledge bases, all within the AWS security perimeter.',
  whenToUse: 'Use Bedrock when you need to call large language models or embedding models from within AWS infrastructure with VPC integration, IAM access control, and no data sent to external providers. Ideal for enterprise applications requiring data residency and audit logging of every model invocation.',
  keyConcepts: [
    { title: 'Model Access', description: 'Models must be **explicitly enabled** per account per region before invocation. Each model has distinct pricing and capability profiles — **Claude** models excel at reasoning and long-context tasks while **Titan Embeddings** is optimized for vector search.' },
    { title: 'InvokeModel API', description: 'The `InvokeModel` API is synchronous and suited for single-turn requests. Each provider uses a different request/response schema. The **Converse API** provides a unified schema across all supported models, making provider switching require only a model ID change.' },
    { title: 'InvokeModelWithResponseStream', description: '`InvokeModelWithResponseStream` streams tokens as they are generated, reducing **time-to-first-token** for latency-sensitive applications. Required for interactive chat applications where waiting for the full response degrades the user experience.' },
    { title: 'Bedrock Agents', description: '**Bedrock Agents** is an orchestration layer that connects a foundation model with tools (Lambda functions), knowledge bases (RAG), and memory. The agent plans multi-step actions and calls tools iteratively in a **ReAct-style loop** until the goal is satisfied.' },
    { title: 'Knowledge Bases', description: '**Bedrock Knowledge Bases** is a managed **RAG** pipeline that ingests documents from S3 into a vector store (**OpenSearch Serverless** or Pinecone), chunks them, and retrieves relevant context automatically when the agent or `RetrieveAndGenerate` API is invoked.' },
  ],
  approach: [
    { step: 'Use the Converse API', details: 'Prefer the Converse API over the provider-specific InvokeModel schema. It provides a unified message format across all Bedrock models so switching providers requires only a model ID change, not a request format rewrite.' },
    { step: 'Enable model invocation logging', details: 'Configure model invocation logging to deliver request metadata, input prompts, and output text to CloudWatch Logs and S3. This is required for compliance auditing and provides the data needed to debug unexpected model outputs.' },
    { step: 'Use Bedrock Guardrails', details: 'Apply Guardrails to block harmful content categories, PII in responses, and topic denial lists. Guardrails apply across all models through a single configuration, avoiding per-model content filtering logic in application code.' },
    { step: 'Estimate and control costs', details: 'Bedrock charges per 1,000 input and output tokens. Use the tokenizer to estimate prompt sizes before production. Implement caching for repeated identical prompts using a Redis layer or Bedrock Prompt Caching where available.' },
  ],
  pitfalls: [
    { title: 'Region availability gaps', description: 'Not all models are available in all regions. Claude and other third-party models are available in a subset of regions. Cross-region inference is available but adds latency; verify model availability in the target region before architecting.' },
    { title: 'Converse API schema differences', description: 'While Converse unifies the message format, tool use schemas and system prompt handling still have per-model differences. Test tool use across model swaps explicitly rather than assuming full compatibility.' },
    { title: 'No fine-tuning for all models', description: 'Not all Bedrock models support fine-tuning. Amazon Titan and Cohere models support fine-tuning; Claude does not. Teams expecting to fine-tune Claude on proprietary data need to evaluate alternative approaches like prompt engineering or RAG.' },
  ],
  keyQuestions: [
    { q: 'How do you build a RAG application using Bedrock Knowledge Bases?', a: 'I create a Knowledge Base that points to an S3 bucket containing the source documents. Bedrock automatically chunks the documents, embeds them using the configured embedding model (Titan Embeddings or Cohere), and stores the vectors in an OpenSearch Serverless collection. At query time I call the RetrieveAndGenerate API with the user question; Bedrock embeds the question, retrieves the top-k relevant chunks, constructs a prompt with the retrieved context, and invokes the generation model to produce a grounded answer with source citations. I tune the chunking strategy (fixed-size versus semantic) and the number of retrieved chunks based on the average document length and the complexity of questions the system needs to answer.' },
    { q: 'How do you enforce data governance when using Bedrock in an enterprise?', a: 'I enable model invocation logging to deliver all request and response payloads to a CloudWatch Log Group and S3 bucket in the security account, providing a tamper-evident audit trail of every LLM interaction. I use IAM condition keys to restrict which models specific roles can invoke and to enforce that all Bedrock calls originate from within the VPC using a VPC endpoint, preventing data from traversing the public internet. I apply Bedrock Guardrails to block PII in model outputs and to enforce topic restrictions so the model cannot be prompted into producing content outside its authorized use cases. CloudTrail records all Bedrock management API calls including model access changes for additional audit coverage.' },
    { q: 'How do you design Bedrock Agents for a multi-step workflow?', a: 'I define the agent with an instruction prompt that describes its role and available tools, then register Lambda functions as action groups with OpenAPI schemas that describe each function\'s inputs, outputs, and purpose. The agent uses the model to reason about which tool to call next given the conversation history and tool outputs, following a ReAct-style plan-act-observe loop. For workflows that require querying internal data I attach a Knowledge Base to the agent so it can retrieve context alongside calling tools. I design each Lambda action to be idempotent and to return structured JSON responses, because the agent passes these outputs directly into the next reasoning step and malformed or inconsistent outputs cause the planning loop to fail or hallucinate.' },
  ],
  references: [
    'https://docs.aws.amazon.com/bedrock/latest/userguide/',
    'https://docs.aws.amazon.com/bedrock/latest/userguide/agents.html',
  ],
},
{
  id: 'aws-rekognition',
  title: 'Rekognition — Computer Vision API',
  icon: 'eye',
  color: '#a855f7',
  description: 'Managed computer vision for image and video analysis.',
  introduction: '**Amazon Rekognition** provides pre-trained computer vision capabilities through a simple API without requiring ML expertise. It detects objects, scenes, faces, text, and unsafe content in images and videos, and enables identity verification through face comparison and face search against indexed collections. Video analysis supports both real-time streaming via **Kinesis Video Streams** and asynchronous processing of stored videos.',
  whenToUse: 'Use Rekognition for content moderation, identity verification, face-based search, object and scene detection, and text extraction from images. It is appropriate when you need computer vision without building custom models. For highly specialized detection tasks with domain-specific objects, a custom SageMaker model may outperform it.',
  keyConcepts: [
    { title: 'DetectLabels', description: '`DetectLabels` identifies objects, scenes, activities, and concepts in images with confidence scores. Returns a **label hierarchy** — for example, `Animal > Mammal > Dog` — enabling granular filtering on specific object types.' },
    { title: 'Face Collections', description: '**Face Collections** are indexed stores of face embeddings. `IndexFaces` stores a face with an `ExternalImageId`; `SearchFacesByImage` finds matching faces by similarity score. Used for identity verification and employee access systems.' },
    { title: 'Content Moderation', description: '`DetectModerationLabels` identifies explicit, suggestive, or violent content. Returns a **two-level label hierarchy** and confidence score for each category, enabling threshold-based auto-reject and human review routing.' },
    { title: 'DetectText', description: '`DetectText` extracts text from images including signs, license plates, and overlaid captions. Returns **bounding boxes** and a detected text string for each word and line, suitable for OCR-light use cases.' },
    { title: 'Video Analysis', description: '`StartLabelDetection`, `StartFaceSearch`, and related **async APIs** process stored S3 video files. Results are delivered via **SNS** notification when processing completes, then retrieved paginated via the `Get*` APIs.' },
  ],
  approach: [
    { step: 'Choose sync vs async', details: 'Use synchronous APIs (DetectLabels, DetectFaces) for individual image analysis in real-time flows. Use async APIs (StartLabelDetection, GetLabelDetection) for video or batch image workflows where latency is not user-facing.' },
    { step: 'Build face collections', details: 'Create a collection per use case, for example one for employee verification and a separate one for customer identity. Index faces with an ExternalImageId matching your user ID for easy lookup when a match is returned.' },
    { step: 'Set confidence thresholds', details: 'Tune the MinConfidence parameter based on the cost of false positives versus false negatives. For content moderation, a lower threshold catches more borderline content at the expense of more false positives that require human review.' },
    { step: 'Integrate human review with Augmented AI', details: 'For high-stakes decisions like identity verification or moderation of borderline content, route low-confidence Rekognition results to Amazon Augmented AI for human review rather than making automated decisions on uncertain predictions.' },
  ],
  pitfalls: [
    { title: 'Bias in face recognition', description: 'Face analysis APIs have documented accuracy disparities across demographic groups. AWS recommends not using Rekognition for making sole determinations about identity in high-stakes scenarios. Always combine with human review and audit results for demographic bias.' },
    { title: 'Collection size and search latency', description: 'Face collection search scales with collection size. Very large collections (millions of faces) require partitioning by attribute (region, user segment) to keep search latency within SLA.' },
    { title: 'Image quality requirements', description: 'Rekognition performs poorly on small, blurry, or heavily occluded faces. Implement client-side quality checks (minimum image size, basic blur detection) before sending to Rekognition to reduce API calls on images that will return low-confidence results anyway.' },
  ],
  keyQuestions: [
    { q: 'How do you build a content moderation pipeline using Rekognition?', a: 'I design the pipeline with user-uploaded content going to S3, which triggers a Lambda function that calls DetectModerationLabels on the image. I apply confidence thresholds tuned to the platform\'s risk tolerance: content above 90% confidence is auto-rejected, content between 50% and 90% is routed to Amazon Augmented AI for a human reviewer, and content below 50% is auto-approved. The Lambda writes the Rekognition result and the final disposition to a DynamoDB table for audit purposes. For video content I use an async Rekognition video job triggered by S3 put events, with results delivered to an SQS queue that drives the same review workflow.' },
    { q: 'How do you implement identity verification using Rekognition face comparison?', a: 'For document-based identity verification I call CompareFaces with two images: the photo from the user\'s government ID and a selfie taken during the onboarding flow. CompareFaces returns a similarity score between 0 and 100; I set an acceptance threshold of 95 or above to minimize false acceptances while routing lower scores to human review. I never store the government ID image beyond the verification session. If the platform requires repeated verification (such as for proctoring), I index the verified selfie into a Rekognition collection using IndexFaces and then use SearchFacesByImage for subsequent verification checks, which avoids re-uploading reference documents.' },
    { q: 'What are the limitations of Rekognition compared to a custom-trained vision model?', a: 'Rekognition excels at general-purpose detection tasks it was trained on, including common objects, faces, text, and standard moderation categories. It performs poorly on domain-specific objects it was never trained to recognize, such as specialized industrial components, rare species, or proprietary product variants. A custom SageMaker model trained on domain-specific data will outperform Rekognition for specialized tasks but requires labeled training data, ML expertise, and ongoing retraining. Rekognition also cannot be fine-tuned; Custom Labels is a separate feature that trains an image classification or object detection model on your own labeled images, sitting between general Rekognition and fully custom SageMaker models in flexibility and effort.' },
  ],
  references: [
    'https://docs.aws.amazon.com/rekognition/latest/dg/',
    'https://docs.aws.amazon.com/rekognition/latest/dg/video.html',
  ],
},
{
  id: 'aws-comprehend',
  title: 'Comprehend — Natural Language Processing',
  icon: 'file-text',
  color: '#a855f7',
  description: 'Managed NLP for entity extraction, sentiment analysis, and topic modeling.',
  introduction: '**Amazon Comprehend** applies natural language processing to extract insights from text. It detects entities (people, places, organizations), analyzes sentiment at document and entity level, identifies the dominant language, and extracts key phrases. **Comprehend Medical** specializes in clinical text, extracting medical conditions, medications, and dosages linked to standardized ontologies like **SNOMED CT** and **RxNorm**.',
  whenToUse: 'Use Comprehend for customer feedback analysis, support ticket routing, document classification, compliance screening for PII, and processing clinical notes. It requires no ML training for built-in features and supports custom classifiers and entity recognizers for domain-specific text.',
  keyConcepts: [
    { title: 'Entity Recognition', description: '**Entity Recognition** identifies named entities (`PERSON`, `LOCATION`, `ORGANIZATION`, `DATE`, `QUANTITY`, etc.) with confidence scores. A **Custom Entity Recognizer** extends this to domain-specific terms not in the default model.' },
    { title: 'Sentiment Analysis', description: '**Sentiment Analysis** classifies text as `POSITIVE`, `NEGATIVE`, `NEUTRAL`, or `MIXED` with a confidence score per class. **Targeted sentiment analysis** returns per-entity sentiment within the same document, enabling product-level feedback analysis.' },
    { title: 'Custom Classification', description: '**Custom Classification** trains a document classifier on labeled examples to assign custom categories. Suitable for routing support tickets, categorizing news articles, or classifying compliance documents. Requires a minimum of 10 examples per class.' },
    { title: 'PII Detection', description: '**PII Detection** identifies personally identifiable information including names, addresses, SSNs, credit card numbers, and phone numbers. Returns **character offsets** for precise redaction workflows. Use `ContainsPiiEntities` first to screen, then `DetectPiiEntities` for exact positions.' },
    { title: 'Async Batch Jobs', description: 'For large document volumes, **batch analysis jobs** read from S3, process in parallel, and write structured JSON results back to S3. Significantly more cost-effective than per-document synchronous API calls at scale.' },
  ],
  approach: [
    { step: 'Use real-time analysis for interactive applications', details: 'Call DetectSentiment, DetectEntities, and DetectPiiEntities synchronously for user-facing applications that need immediate results, such as routing a support chat to the right queue based on detected product entities.' },
    { step: 'Use batch jobs for document processing', details: 'For processing thousands of documents such as legal contracts or medical records, submit a batch analysis job that reads from S3 and writes structured JSON results. This is far cheaper than synchronous per-document calls at scale.' },
    { step: 'Train custom classifiers for domain text', details: 'Collect labeled examples (minimum 10 per class, 50+ recommended) and submit a custom classifier training job. Use the endpoint to call real-time classification; use the batch job for bulk classification of historical documents.' },
    { step: 'Build PII redaction pipelines', details: 'Use ContainsPiiEntities to screen documents and DetectPiiEntities to get exact offsets, then replace PII spans in the original text with placeholder tokens before storing or sharing the document.' },
  ],
  pitfalls: [
    { title: 'Language support gap', description: 'Not all Comprehend features support all languages. Sentiment and entity detection support a handful of languages; key phrase extraction and custom models are more limited. Check language availability for each feature against your text corpus language before designing the pipeline.' },
    { title: 'Custom model training data quality', description: 'Custom classifiers trained on imbalanced or noisy labels produce poor real-world accuracy. Spend time on annotation guidelines and inter-annotator agreement before training, and evaluate on a held-out test set before deploying the endpoint.' },
    { title: 'PII false negatives', description: 'Comprehend PII detection has a recall rate below 100% on all PII types. Do not rely solely on Comprehend for regulatory-mandated PII removal; supplement with deterministic regex patterns for high-risk fields like SSNs and credit cards, and treat Comprehend as a probabilistic additional layer.' },
  ],
  keyQuestions: [
    { q: 'How do you build a customer feedback analysis pipeline using Comprehend?', a: 'I stream incoming reviews or support tickets to Kinesis Data Streams, which triggers a Lambda function that calls Comprehend\'s DetectSentiment and DetectEntities synchronously. The Lambda writes the sentiment scores and detected product and feature entities to DynamoDB, keyed by submission timestamp and product ID. A separate analytics job runs nightly using Comprehend\'s batch sentiment analysis on the day\'s data to produce aggregated sentiment trends per product feature. I visualize these trends in a QuickSight dashboard that surfaces which features are receiving negative sentiment so the product team can prioritize fixes.' },
    { q: 'When would you use Comprehend Medical versus standard Comprehend?', a: 'Comprehend Medical is trained on clinical and medical literature and can detect medical entities that standard Comprehend misses or miscategorizes, including medical conditions, medications with dosage and route, anatomical locations, procedures, and medical abbreviations like BP for blood pressure. It also links entities to standardized medical ontologies like SNOMED CT and RxNorm for interoperability with EHR systems. I use Comprehend Medical for processing clinical notes, discharge summaries, and prescription records, and standard Comprehend for general business text. Mixing them incorrectly, such as using standard Comprehend on clinical text, produces low-confidence entity detections and incorrect categorizations.' },
    { q: 'How do you handle the accuracy limitations of Comprehend for production use cases?', a: 'I evaluate Comprehend on a representative sample of production data before deployment, measuring precision and recall per entity type or sentiment class. For use cases where errors have downstream consequences, such as compliance screening, I implement a confidence threshold and route low-confidence predictions to human review using Amazon Augmented AI. I also supplement Comprehend\'s probabilistic outputs with deterministic rule-based checks for high-priority patterns, such as regex for known PII formats. I instrument the production pipeline to log prediction confidence distributions and set CloudWatch alarms that alert when average confidence drops, which can indicate a domain shift in the input text that may degrade model performance.' },
  ],
  references: [
    'https://docs.aws.amazon.com/comprehend/latest/dg/',
    'https://docs.aws.amazon.com/comprehend/latest/dg/comprehend-medical.html',
  ],
},
{
  id: 'aws-textract',
  title: 'Textract — Document Text Extraction',
  icon: 'file-text',
  color: '#a855f7',
  description: 'Managed OCR and structured document extraction for PDFs and images.',
  introduction: '**Amazon Textract** goes beyond simple OCR to extract text, tables, forms, and structured data from scanned documents, PDFs, and images. It preserves the **spatial layout** of documents, extracting key-value pairs from forms and cell data from tables without requiring templates or custom training. This makes it suitable for processing invoices, tax forms, medical records, and contracts at scale.',
  whenToUse: 'Use Textract when you need to extract structured data from documents that humans fill out, such as W-2s, insurance claims, or mortgage applications. Combine with Comprehend for downstream NLP on extracted text, and use the Queries feature to extract specific fields by asking natural language questions about the document.',
  keyConcepts: [
    { title: 'DetectDocumentText', description: '`DetectDocumentText` is synchronous OCR that extracts raw text blocks and lines. Simplest operation — use when you only need text without structural understanding of the document layout.' },
    { title: 'AnalyzeDocument', description: '`AnalyzeDocument` extracts **forms** (key-value pairs), **tables** (rows and columns), and signature fields. Returns structural `Block` objects that can be traversed to reconstruct the document structure programmatically.' },
    { title: 'Queries', description: '**Queries** allow asking natural language questions about a specific document — for example, `What is the patient name?` — and Textract returns the answer extracted from the document. Reduces post-processing logic for well-understood document types.' },
    { title: 'AnalyzeExpense', description: '`AnalyzeExpense` is specialized extraction for receipts and invoices. Identifies **vendor name**, line items, amounts, dates, and tax totals using a pre-trained expense document model — no template configuration required.' },
    { title: 'Async APIs', description: '`StartDocumentTextDetection` and `StartDocumentAnalysis` process **multi-page PDFs** asynchronously. Results are delivered via **SNS** notification when processing completes, then retrieved paginated using `GetDocumentTextDetection` or `GetDocumentAnalysis` with `NextToken`.' },
  ],
  approach: [
    { step: 'Choose the right API for the document type', details: 'Use AnalyzeExpense for invoices and receipts, AnalyzeDocument with FORMS for structured form data, and the Queries feature when you know the specific fields you need. Avoid calling AnalyzeDocument with all feature types when only one is needed, as it increases latency and cost.' },
    { step: 'Use async for multi-page documents', details: 'For PDFs with more than one page, always use the async APIs. Synchronous calls only process single-page documents. Design the processing pipeline with an SQS queue receiving the SNS completion notification and a Lambda function calling GetDocumentAnalysis to retrieve paginated results.' },
    { step: 'Post-process with confidence thresholds', details: 'Textract returns a confidence score per block. Filter out low-confidence extractions (below 80%) and flag them for human review rather than propagating uncertain values into downstream systems.' },
    { step: 'Combine with A2I for human review', details: 'Integrate Augmented AI to create a human review loop for documents where Textract confidence is low or where a critical field was not detected. This creates a feedback loop that can also be used to refine extraction logic over time.' },
  ],
  pitfalls: [
    { title: 'Table structure assumptions', description: 'Textract table extraction assumes rectangular grid tables. Merged cells, nested tables, and irregularly formatted tables often produce incorrect cell assignments. Always validate extracted table data against the expected schema for the document type.' },
    { title: 'Handwriting accuracy', description: 'Textract supports handwriting but accuracy drops significantly compared to printed text, especially for poor-quality scans or unusual handwriting styles. Build confidence-based human review into any pipeline that processes handwritten documents.' },
    { title: 'Cost at scale', description: 'Textract pricing is per page and varies by feature. AnalyzeDocument with all features costs more than DetectDocumentText. For high-volume pipelines, profile exactly which features each document type requires to avoid paying for unused analysis.' },
  ],
  keyQuestions: [
    { q: 'How do you build a document processing pipeline for mortgage applications using Textract?', a: 'I design the pipeline to accept PDF uploads to S3, which triggers a Lambda function that calls StartDocumentAnalysis with the FORMS and TABLES features plus Queries for known fields like applicant name and loan amount. The Lambda registers the job ID in DynamoDB. When the SNS completion notification arrives, a second Lambda calls GetDocumentAnalysis paginated to retrieve all blocks and reconstructs the key-value form fields. I apply a confidence threshold of 85%, writing high-confidence fields directly to the application database and routing low-confidence fields plus missing required fields to an Augmented AI human review task. The complete extraction result, including confidence scores and the original document, is archived in S3 for the audit trail.' },
    { q: 'How does Textract handle multi-page PDFs?', a: 'Textract processes multi-page PDFs only through the asynchronous APIs: StartDocumentTextDetection for raw text or StartDocumentAnalysis for structured extraction. The job runs in the background; when complete, Textract sends a notification to the configured SNS topic. The client then calls GetDocumentTextDetection or GetDocumentAnalysis with the job ID, and since results are paginated, iterates through NextToken until all pages are retrieved. The response includes a Page attribute on each block indicating which page it came from, enabling per-page reconstruction of tables and forms. For high throughput I design the completion handler as a Step Functions workflow that polls GetDocumentAnalysis in a loop with exponential backoff until the job status is SUCCEEDED.' },
    { q: 'When would you choose Textract over a custom OCR solution?', a: 'Textract is the right choice when the document types are common business forms (invoices, tax documents, medical records, contracts) where its pre-trained models produce high accuracy without customization. It handles variable document layouts without template configuration, which would require significant engineering effort to replicate in a custom OCR pipeline. I choose a custom solution only when documents are highly specialized with unique layouts not covered by Textract\'s pre-trained models, when per-page costs at projected volume make Textract more expensive than managing a self-hosted OCR cluster, or when specific regulatory requirements prohibit sending document content to a managed cloud service even within the AWS boundary.' },
  ],
  references: [
    'https://docs.aws.amazon.com/textract/latest/dg/',
    'https://docs.aws.amazon.com/textract/latest/dg/async.html',
  ],
},

{
  id: 'aws-transcribe',
  title: 'Transcribe — Speech-to-Text',
  icon: 'mic',
  color: '#a855f7',
  description: 'Managed automatic speech recognition for audio and video files.',
  introduction: '**Amazon Transcribe** converts speech to text using deep learning, supporting batch transcription of stored audio files and real-time streaming transcription for live audio. It supports over 100 languages, produces speaker-labeled output for multi-speaker recordings, and can be customized with domain-specific vocabulary and language models for improved accuracy on technical or specialized content.',
  whenToUse: 'Use Transcribe for call center analytics, meeting transcription, subtitle generation, voice-driven applications, and compliance recording archival. Use Transcribe Medical for clinical documentation requiring HIPAA-compliant transcription with medical terminology accuracy.',
  keyConcepts: [
    { title: 'Batch Transcription', description: '`StartTranscriptionJob` processes audio files in S3 asynchronously. Supports MP3, MP4, WAV, FLAC, and other formats. Delivers a JSON transcript to S3 on completion.' },
    { title: 'Streaming Transcription', description: '**Real-time transcription** over WebSocket or HTTP/2. Returns partial results as words are spoken and finalizes segments after a pause. Suitable for live captioning and real-time voice UIs.' },
    { title: 'Speaker Diarization', description: '**Speaker diarization** identifies who spoke when in multi-speaker recordings. Returns speaker labels on each word segment. Supports up to 10 speakers.' },
    { title: 'Custom Vocabulary', description: '**Custom vocabulary** improves accuracy on technical terms, product names, and acronyms by providing a pronunciation table. Custom language models go further, training an N-gram model on domain text to boost contextual accuracy.' },
    { title: 'Automatic Content Redaction', description: '**Automatic content redaction** detects and redacts PII in transcripts (SSNs, credit cards, phone numbers) by replacing detected spans with `[PII]` tokens or by removing them entirely. Supports both batch and streaming.' },
  ],
  approach: [
    { step: 'Optimize audio input quality', details: 'Transcription accuracy depends heavily on audio quality. Recommend clients to record at 8kHz minimum (16kHz+ preferred), mono channel, with noise cancellation. Poor quality audio cannot be fully compensated by the model.' },
    { step: 'Use custom vocabulary for domain terms', details: 'Create a custom vocabulary file listing technical terms, brand names, and acronyms with phonetic representations. Apply it to all jobs in the domain. For sustained accuracy improvements, train a custom language model on representative transcripts.' },
    { step: 'Enable speaker diarization for call recordings', details: 'Set ShowSpeakerLabels to true for two-party call recordings to separate agent and customer speech. Combine with Call Analytics, a purpose-built Transcribe variant for contact center use cases that also provides sentiment, talk time, and interruption detection.' },
    { step: 'Post-process transcripts with Comprehend', details: 'Feed Transcribe output into Comprehend DetectSentiment and DetectEntities to extract customer sentiment and mentioned products from call transcripts, building a call analytics pipeline without a call center platform.' },
  ],
  pitfalls: [
    { title: 'Partial result stale references', description: 'Streaming transcription returns partial results that are replaced by final results. Applications that render partial transcripts must handle the replacement by matching the ResultId, not by appending all events sequentially.' },
    { title: 'Language detection limitations', description: 'Automatic language detection is available but reduces accuracy compared to specifying the language explicitly. For multilingual content mixed in a single recording, language detection can produce mixed-language transcripts with poor accuracy at language switch points.' },
    { title: 'Custom model training data requirements', description: 'Training a custom language model requires at least 100,000 words of domain text. Smaller datasets produce models that barely outperform the base model. Use custom vocabulary for small domain glossaries and reserve custom language models for genuinely high-volume specialized domains.' },
  ],
  keyQuestions: [
    { q: 'How do you build a real-time transcription system using Amazon Transcribe?', a: 'I use the **Transcribe** streaming API over WebSocket, which accepts an audio stream in PCM format and returns partial and final transcript events in near real-time. On the client side I capture microphone audio using the Web Audio API or native platform APIs and chunk it into 100ms frames before sending over the WebSocket connection. On the server side I implement a WebSocket proxy that relays audio from the client to the Transcribe streaming endpoint and forwards transcript events back to the client, adding speaker context and formatting. I handle partial results by replacing previous partials with the same `ResultId` and appending finalized segments, producing a clean rolling transcript that can be displayed in real-time to the user.' },
    { q: 'How do you improve transcription accuracy for a technical domain?', a: 'I start with a **custom vocabulary** file that lists technical terms the base model misrecognizes, providing display forms and optional IPA phonetic representations for unusual words. If the vocabulary improvement is insufficient I train a **custom language model** by feeding it existing text from the domain: documentation, past transcripts, forum posts, and technical manuals, using at least 100,000 words. I evaluate accuracy improvement on a held-out test set of manually transcribed recordings using word error rate as the metric. For high-stakes accuracy requirements I also implement a post-processing step that applies a regex-based correction dictionary for known systematic errors the model consistently makes on specific technical phrases.' },
    { q: 'How do you build a call center analytics pipeline using Transcribe?', a: 'I use **Transcribe Call Analytics** rather than standard Transcribe, because it is purpose-built for two-party call recordings and adds per-turn sentiment, talk time statistics, interruption counts, loudness, and issue detection without requiring separate NLP calls. Call Analytics jobs are submitted with channel definitions identifying agent and customer audio channels and produce structured JSON output enriched with these analytics fields. I store the structured output in **S3**, partition by date and queue, and query it with **Athena** to produce weekly dashboards showing average sentiment per agent, common customer issues by category, and compliance metrics like required call opening scripts being spoken. Long-term trends are visualized in **QuickSight** with direct Athena integration.' },
  ],
  references: [
    'https://docs.aws.amazon.com/transcribe/latest/dg/',
    'https://docs.aws.amazon.com/transcribe/latest/dg/call-analytics.html',
  ],
},
{
  id: 'aws-polly',
  title: 'Polly — Text-to-Speech',
  icon: 'volume-2',
  color: '#a855f7',
  description: 'Managed text-to-speech with neural voices and SSML support.',
  introduction: '**Amazon Polly** converts text to lifelike speech using deep learning. It offers standard voices based on concatenative synthesis and Neural Text-to-Speech (NTTS) voices with significantly higher naturalness and expressiveness. Polly supports SSML tags for fine-grained control over pronunciation, pauses, emphasis, and speaking rate, making it suitable for production voice applications.',
  whenToUse: 'Use Polly for generating audio content for e-learning, IVR systems, screen readers, podcast generation, accessibility features, and notification audio. NTTS voices are appropriate for customer-facing applications; standard voices are sufficient for internal tools where cost is a concern.',
  keyConcepts: [
    { title: 'Neural TTS', description: '**NTTS voices** use a neural network to synthesize speech directly from text, producing more natural prosody and fewer robotic artifacts than standard voices. Priced at 4x standard but appropriate for customer-facing applications.' },
    { title: 'SSML', description: '**Speech Synthesis Markup Language** tags embedded in the input text control pause length, phoneme pronunciation, speaking rate, volume, pitch, and emphasis. Essential for achieving natural-sounding output on specialized content.' },
    { title: 'Speech Marks', description: '**Speech marks** provide word, sentence, viseme, and SSML mark timestamps alongside audio to synchronize text highlighting, lip animation, or captions with the audio stream.' },
    { title: 'Lexicons', description: '**Lexicons** are custom pronunciation dictionaries that override default pronunciation of specific words. Useful for brand names, acronyms, and domain-specific terms Polly consistently mispronounces.' },
    { title: 'Long-form synthesis', description: '**Long-form synthesis** handles text longer than the 3,000 character synchronous limit by using the asynchronous `StartSpeechSynthesisTask` API, which writes an MP3 or OGG file to S3.' },
  ],
  approach: [
    { step: 'Select voice for the use case', details: 'Choose NTTS voices for customer-facing audio. Evaluate multiple voices in the target language for tone and brand fit using the Polly console preview before committing to a voice in code. Consider using different voices for different content types.' },
    { step: 'Use SSML for pronunciation control', details: 'Wrap input text in &lt;speak&gt; tags and add &lt;break&gt; tags for natural pauses at punctuation boundaries, &lt;phoneme&gt; tags for acronyms and brand names, and &lt;prosody rate="slow"&gt; for sections requiring emphasis. Test SSML inputs in the console before deploying.' },
    { step: 'Cache synthesized audio', details: 'For static or infrequently changing content, synthesize once and store the resulting MP3 in S3 or CloudFront. Polly charges per character on each synthesis call; caching eliminates redundant charges on repeated content.' },
    { step: 'Use speech marks for synchronized experiences', details: 'Request word-level speech marks alongside audio synthesis to get timestamps for each word. Use these timestamps to drive real-time text highlighting in e-learning players or to synchronize avatar lip movements in animated interfaces.' },
  ],
  pitfalls: [
    { title: 'Character limit on synchronous API', description: 'SynthesizeSpeech accepts a maximum of 3,000 characters including SSML markup. Content longer than this must be split at sentence boundaries before calling the API or submitted as an asynchronous task, which some teams discover only when processing longer content in production.' },
    { title: 'SSML injection risk', description: 'If user-provided text is passed directly to Polly with SSML enabled, malicious SSML tags in the input can alter the synthesized output. Always escape or strip SSML tags from user input before embedding it in a controlled SSML template.' },
    { title: 'Voice consistency across regions', description: 'Not all voices are available in all regions, and some voices are available exclusively in certain regions. Deploying a multi-region application that uses Polly requires verifying voice availability in each target region to avoid runtime availability errors.' },
  ],
  keyQuestions: [
    { q: 'How do you build an audio content generation pipeline using Polly?', a: 'For a content platform generating narrated articles, I design a pipeline where the editorial CMS triggers an **EventBridge** event on article publish. A **Lambda** function retrieves the article text, strips HTML, and pre-processes it to add SSML pauses at paragraph breaks and phoneme corrections for known brand names. I call `StartSpeechSynthesisTask` with the NTTS voice chosen for the brand, specifying an S3 output bucket and requesting word-level speech marks in a separate file. When the S3 `PutObject` event fires for the audio file, another Lambda updates the article record in the database with the audio URL and speech marks URL so the article player can highlight words as they are spoken.' },
    { q: 'How do you handle pronunciation errors in Polly for technical content?', a: 'I use a combination of **lexicons** and SSML phoneme tags to correct systematic mispronunciations. For domain-wide corrections I create a Polly lexicon using PLS format that maps the display form of each term to its IPA or X-SAMPA phonetic representation, then reference the lexicon ARN in every synthesis call. For one-off corrections in specific content, I use inline SSML phoneme tags to override the pronunciation of a single occurrence. When testing new content I use the Polly console to preview synthesis and identify mispronounced words before they reach production audio. I maintain a shared mispronunciation inventory that the content team uses to report new issues and that feeds updates to the lexicon.' },
    { q: 'What are the trade-offs between Polly Neural and Standard voices?', a: '**NTTS voices** use a neural synthesis architecture that models prosody directly from the text, producing speech with natural intonation, appropriate stress patterns, and minimal robotic artifacts. **Standard voices** use a unit selection or HMM approach that concatenates or parameterizes pre-recorded speech segments, which is less natural but has lower synthesis latency and costs four times less per character. For customer-facing audio like IVR greetings, e-learning narration, or voice assistants I always choose NTTS because the quality difference is immediately perceptible to users and directly affects trust and engagement. For internal tooling, bulk notification generation, or developer testing where per-character cost adds up quickly, standard voices are acceptable and significantly cheaper.' },
  ],
  references: [
    'https://docs.aws.amazon.com/polly/latest/dg/',
    'https://docs.aws.amazon.com/polly/latest/dg/ssml.html',
  ],
},
{
  id: 'aws-lex',
  title: 'Lex — Conversational AI',
  icon: 'message-square',
  color: '#a855f7',
  description: 'Managed service for building conversational chatbots and voice interfaces.',
  introduction: '**Amazon Lex** provides natural language understanding and automatic speech recognition capabilities for building conversational interfaces. The same deep learning technology that powers Alexa is available as an API to create chatbots for web applications, mobile apps, and contact centers. **Lex V2** supports multi-turn dialogues, intent confidence scores, and direct integration with **Connect** for cloud contact center automation.',
  whenToUse: 'Use Lex when building customer-facing chatbots, IVR self-service flows, internal help desk bots, or voice-driven applications. It handles the conversation management complexity so application code focuses on business logic in Lambda fulfillment functions rather than NLU training.',
  keyConcepts: [
    { title: 'Intents', description: '**Intents** represent actions the user wants to perform. Each intent has sample utterances that train the NLU model. Fulfillment logic runs in a Lambda function when the intent is confirmed.' },
    { title: 'Slots', description: '**Slots** are parameters required to fulfill an intent. Lex automatically prompts for missing slots and validates slot values. Custom slot types define the valid values or regex patterns for domain-specific entities.' },
    { title: 'Conversation Flow', description: '**Lex** manages multi-turn dialogue, tracking which slots are filled and which still need to be elicited. Dialog code hooks in Lambda can intercept each turn to add custom validation or conditional branching.' },
    { title: 'Fulfillment', description: 'The **fulfillment Lambda** is invoked when all required slots are filled and the user confirms the action. It receives the intent name and slot values, returns a response message, and closes the conversation or elicits the next intent.' },
    { title: 'Amazon Connect Integration', description: '**Lex bots** can be embedded directly into Amazon Connect contact flows, enabling self-service IVR that handles common inquiries without agent involvement and transfers to live agents when needed.' },
  ],
  approach: [
    { step: 'Design intents around user goals', details: 'Identify the 5-10 most common user requests and model each as an intent with 15-20 diverse sample utterances. Utterances should vary in phrasing to improve NLU generalization, not just list synonyms.' },
    { step: 'Define slots with custom types', details: 'Create custom slot types for domain-specific values like product names or account categories. Use the built-in date, time, and number slot types to handle parsing of those common value types automatically.' },
    { step: 'Use dialog code hooks for validation', details: 'Implement a Lambda dialog code hook to validate slot values as they are collected, such as verifying that an account number exists in the database before confirming the intent. This prevents fulfillment failures on invalid inputs.' },
    { step: 'Implement graceful fallback', details: 'Configure the AMAZON.FallbackIntent to handle utterances that do not match any intent. The fallback handler should either prompt the user to rephrase, escalate to a human agent, or provide a menu of supported actions.' },
  ],
  pitfalls: [
    { title: 'Insufficient training utterances', description: 'Intents trained with fewer than 10 utterances often fail to recognize paraphrases common in real user inputs. Aim for 20+ utterances per intent, including common misspellings and colloquial phrasing drawn from actual user messages.' },
    { title: 'Ambiguous intents', description: 'Intents with overlapping utterances cause the NLU model to misclassify user input. Use the built-in intent conflict detection in the Lex console and revise utterances to ensure each intent has a distinct linguistic signature.' },
    { title: 'Stateless Lambda assumptions', description: 'Each Lex turn invokes the Lambda function independently. Lambda must not rely on in-memory session state between turns. Pass session attributes in the Lex response to carry context across turns, or use DynamoDB to store conversation state keyed by the session ID.' },
  ],
  keyQuestions: [
    { q: 'How do you design a Lex bot for a customer service application?', a: 'I start by analyzing support ticket data to identify the top 10 intents by volume, such as `CheckOrderStatus`, `CancelOrder`, `ResetPassword`, and `ScheduleCallback`. For each intent I write 20 diverse sample utterances covering different phrasings, including casual language and common misspellings. I define required slots with validation **Lambda** hooks that verify data against backend APIs before confirming, so the bot never reaches fulfillment with invalid input. I configure the fallback intent to offer a curated menu when the user intent is not recognized, and I set a maximum retries limit so the bot escalates to a live agent after two failed recognition attempts rather than looping indefinitely.' },
    { q: 'How does Lex handle multi-turn conversations?', a: '**Lex** tracks dialog state across turns automatically, remembering which slots have been filled and which still need to be collected. After each user turn, Lex determines whether to elicit a missing slot, confirm the intent, or invoke fulfillment based on the current slot map. **Lambda dialog code hooks** can intercept each turn to add custom logic such as conditionally skipping certain slots based on previously collected values, or dynamically changing the elicitation prompt based on context. **Session attributes** are a key-value store on the session that persists across all turns and is passed to both dialog hooks and the fulfillment Lambda, enabling stateful business logic without external storage for simple sessions.' },
    { q: 'How do you measure and improve the performance of a Lex bot in production?', a: 'I enable conversation logs to capture all user utterances, intent classifications, confidence scores, and fulfillment outcomes in **CloudWatch Logs**. I query the logs weekly using **Logs Insights** to compute the intent recognition rate (utterances where Lex matched an intent versus fell back), the slot validation failure rate, and the escalation rate to live agents. For misclassified utterances I review the actual text and either add it as a training utterance to the correct intent or create a new intent if it represents an unhandled use case. I track week-over-week trends on recognition rate and escalation rate as the primary KPIs for bot quality, aiming to reduce escalation rate while maintaining high fulfillment success.' },
  ],
  references: [
    'https://docs.aws.amazon.com/lexv2/latest/dg/',
    'https://docs.aws.amazon.com/lexv2/latest/dg/conversation-logs.html',
  ],
},
{
  id: 'aws-translate',
  title: 'Translate — Neural Machine Translation',
  icon: 'globe',
  color: '#a855f7',
  description: 'Managed neural machine translation for real-time and batch text translation.',
  introduction: '**Amazon Translate** provides fast, high-quality neural machine translation between 75+ languages. It supports real-time translation for individual text requests and asynchronous batch translation jobs for large document volumes stored in S3. Custom terminology and parallel data enable fine-tuning translations for brand-specific vocabulary and stylistic preferences.',
  whenToUse: 'Use Translate to localize application content, translate customer support messages, process multilingual document repositories, and build cross-language search. Combine with Comprehend to detect the source language automatically and with Polly to produce multilingual audio narration.',
  keyConcepts: [
    { title: 'TranslateText', description: '`TranslateText` is a synchronous API that translates up to 10,000 bytes of text in a single request. Accepts explicit source and target language codes or auto-detection from Comprehend.' },
    { title: 'Custom Terminology', description: '**Custom terminology** is a glossary of source-language terms mapped to exact target-language translations. Overrides the model output for specified terms, ensuring brand names, product names, and legal terms are never changed.' },
    { title: 'Parallel Data', description: '**Parallel data** is sentence-aligned bilingual data used to customize the translation model for a specific domain or style. Requires a managed parallel data resource and substantially more data than custom terminology.' },
    { title: 'Batch Translation Jobs', description: '`StartTextTranslationJob` processes an S3 prefix of documents asynchronously. Supports TXT, HTML, DOCX, XLSX, PPTX, and XLIFF formats with layout preservation where applicable.' },
    { title: 'Formality Setting', description: 'The **formality setting** controls whether the translation uses formal pronouns and register for supported language pairs. Important for customer-facing content in languages with grammatical formality distinctions.' },
  ],
  approach: [
    { step: 'Auto-detect source language', details: "Use Comprehend DetectDominantLanguage before calling Translate when the source language is not known. Pass the detected language code as the SourceLanguageCode; this avoids the additional latency and reduced accuracy of Translate's own auto-detection mode." },
    { step: 'Create custom terminology files', details: 'Compile a CSV of brand names, product names, trademarks, and legal terms that must not be translated or must use a specific target translation. Apply the terminology ARN to all TranslateText and batch job calls to ensure consistency across all translated content.' },
    { step: 'Use batch jobs for document localization', details: 'For localizing large content libraries such as help documentation or marketing materials, submit a batch translation job targeting each locale. Translate preserves DOCX and HTML formatting so the translated output requires minimal post-processing before publication.' },
    { step: 'Set formality for customer-facing content', details: 'For languages like German, French, Spanish, Japanese, and others with formal and informal registers, configure the Formality setting on TranslateText to match the brand voice. Formal is typically appropriate for enterprise and B2B content; informal for consumer and social applications.' },
  ],
  pitfalls: [
    { title: 'Text length limit per call', description: 'TranslateText is limited to 10,000 bytes per request. Applications that pass larger documents must split at sentence or paragraph boundaries before calling the API and rejoin translated segments. Splitting mid-sentence causes incoherent translations.' },
    { title: 'Custom terminology side effects', description: 'Custom terminology terms are matched exactly including case. A term listed as uppercase will not match lowercase instances in the source text. Maintain both cased variants in the terminology file and test with representative sample texts.' },
    { title: 'Profanity filter bypasses terminology', description: 'When the profanity masking option is enabled, Translate may override custom terminology for terms it classifies as profane. Review custom terminology against the profanity filter behavior when operating in markets with strict content standards.' },
  ],
  keyQuestions: [
    { q: 'How do you build a multilingual customer support system using Amazon Translate?', a: 'I design the system to detect the language of each incoming support message using **Comprehend** `DetectDominantLanguage`, then translate it to English for the support team using **Translate** `TranslateText` with custom terminology ensuring product names are preserved. The support agent reads and responds in English; when the response is sent, Translate converts it back to the customer\'s detected language before delivery. I store both the original and translated versions of each message in the ticket system for audit purposes. For languages with formal and informal registers I configure formality based on the customer tier, using formal for enterprise customers and informal for consumer support channels, reflecting the brand voice guidelines.' },
    { q: 'How do you localize a documentation site using Translate batch jobs?', a: 'I organize the source documentation as HTML files in an S3 prefix structured by language code, with the English source under a source prefix. When new content is published, a **Step Functions** workflow starts batch translation jobs targeting each supported locale simultaneously. **Translate** processes HTML files with layout preservation, maintaining heading structure, list formatting, and internal link text. I apply custom terminology ARNs for brand names and technical terms to all jobs. On completion, the translated files land in locale-specific S3 prefixes where a **CloudFront** distribution serves them under locale-specific URL paths. A post-processing Lambda checks for untranslated segments flagged by the batch job and queues them for human translator review.' },
    { q: 'What are the accuracy limitations of Amazon Translate and how do you mitigate them?', a: '**Neural machine translation** excels at high-resource language pairs with large training corpora, such as English-Spanish, English-French, and English-German, but accuracy drops for low-resource pairs and degrades further for technical, legal, or medical content with specialized terminology. I mitigate terminology errors with **custom terminology** files that force specific translations for domain terms. For legal documents and medical records I treat Translate output as a draft requiring human post-editing rather than a final translation, and I communicate this distinction to stakeholders to prevent regulatory risk from relying on unreviewed MT output. I also measure translation quality using BLEU scores against human reference translations on a representative sample to establish a baseline and track quality changes after model updates.' },
  ],
  references: [
    'https://docs.aws.amazon.com/translate/latest/dg/',
    'https://docs.aws.amazon.com/translate/latest/dg/customizing-translations-terminology.html',
  ],
},
{
  id: 'aws-codepipeline',
  title: 'CodePipeline — Continuous Delivery Orchestration',
  icon: 'git-merge',
  color: '#14b8a6',
  description: 'Fully managed CI/CD pipeline service for automated software releases.',
  introduction: '**AWS CodePipeline** is a fully managed continuous delivery service that models, visualizes, and automates software release pipelines. It integrates with **CodeCommit**, **GitHub**, **CodeBuild**, **CodeDeploy**, and third-party tools, orchestrating each stage from source through build to deploy with approvals and rollbacks built in. Pipelines are defined as code via **CloudFormation** or **CDK** and version-controlled alongside application code.',
  whenToUse: 'Use CodePipeline when you need a managed pipeline that integrates deeply with AWS services. Prefer it over Jenkins for teams already on AWS to reduce operational overhead. Combine with CodeBuild for build stages and CodeDeploy for deployment targets. It is especially strong for multi-account, multi-region delivery and for teams that need audit trails and manual approval gates built into the pipeline.',
  keyConcepts: [
    { title: 'Stages and Actions', description: '**Pipelines** have stages such as Source, Build, Test, and Deploy each containing one or more actions that run sequentially or in parallel. Stage failures block downstream stages and can trigger notifications.' },
    { title: 'Artifacts', description: '**Artifacts** are outputs from each action stored in S3. Input artifacts pass code or build output between stages, forming a traceable chain from commit to deployment.' },
    { title: 'Approval Actions', description: '**Manual approval gates** pause execution until a designated reviewer approves via the console, CLI, or SNS notification. This enables change-control processes in regulated environments.' },
    { title: 'Pipeline Triggers', description: '**Pipeline triggers** can fire on CloudWatch Events for CodeCommit push, S3 object changes, or webhooks from GitHub. Event-driven triggers are faster and cheaper than polling.' },
  ],
  approach: [
    { step: 'Model your release process as stages', details: 'Map your existing deployment steps to CodePipeline stages. Keep stages small so failures are localized and retries are cheap. A typical shape is Source, Build, Staging Deploy, Approval, Production Deploy.' },
    { step: 'Define source triggers', details: 'Connect the source stage to CodeCommit or GitHub. Enable CloudWatch Events for push-triggered pipelines rather than polling, which reduces latency from commit to pipeline start from minutes to seconds.' },
    { step: 'Configure deployment targets', details: 'Use a CodeDeploy action for EC2, ECS, or Lambda targets. Use a CloudFormation action for infrastructure updates. Stack both in parallel deploy stages when you have independent services to speed up overall pipeline time.' },
    { step: 'Manage cross-account access', details: 'Create a pipeline execution role in the source account and cross-account deployment roles in target accounts. Share the artifact KMS key with each target account so CodePipeline can decrypt artifacts it copies across account boundaries.' },
  ],
  pitfalls: [
    { title: 'Artifact size limits', description: 'S3 artifact buckets have a 3GB limit per artifact. Large build outputs must be split or stored separately in S3 and referenced by path rather than passed as CodePipeline artifacts.' },
    { title: 'Cross-account pipelines', description: 'Pipelines that deploy to multiple AWS accounts require explicit KMS key sharing and cross-account IAM roles, which are easy to misconfigure and produce opaque access-denied errors at runtime rather than at pipeline creation time.' },
    { title: 'Stale source detection', description: 'Polling-based source detection can miss rapid successive commits because the pipeline only picks up the latest revision when it polls. Switch to event-driven triggers to process every push.' },
  ],
  keyQuestions: [
    { q: 'How do you handle rollbacks in CodePipeline?', a: '**CodePipeline** does not natively roll back the pipeline itself; rollback is implemented at the deployment stage. For **CodeDeploy** targets I configure automatic rollback on alarm or deployment failure in the deployment group settings. For **CloudFormation** stages I use stack rollback policies and set `OnFailure` to `ROLLBACK` so the stack reverts to the last known-good state. For ECS blue/green deployments I rely on CodeDeploy traffic shifting with a **CloudWatch** alarm that triggers rollback if error rates spike during the configured bake time window.' },
    { q: 'How do you implement multi-region deployments with CodePipeline?', a: 'I configure a pipeline with parallel deployment stages each targeting a different region. Each region needs its own artifact store S3 bucket, and **CodePipeline** cross-region actions automatically copy artifacts to the target region bucket before executing the deployment action. IAM roles in each target region must trust the pipeline execution role from the primary region. I also replicate the KMS key or use the AWS managed key in each region so artifacts can be decrypted by **CodeDeploy** or **CloudFormation** in the target.' },
    { q: 'What is the difference between CodePipeline and CodeBuild?', a: '**CodePipeline** is an orchestration service that defines the sequence, gating logic, and approval flow of a release process; it does not execute build commands itself. **CodeBuild** is a build execution environment that compiles code, runs tests, and produces artifacts in an ephemeral container. A typical setup uses CodePipeline to trigger and sequence the stages and CodeBuild inside the Build stage to actually run `npm run build` or a Makefile target. They complement each other rather than overlap: you always need both for a complete CI/CD pipeline on AWS.' },
    { q: 'How do you enforce least-privilege IAM for a CodePipeline pipeline?', a: 'I create a dedicated pipeline execution role with only the permissions required by each action type. For **CodeBuild** actions the role needs `codebuild:StartBuild` and `codebuild:BatchGetBuilds`. For S3 artifact actions it needs scoped `s3:GetObject` and `s3:PutObject` on the artifact bucket ARN. For **CodeDeploy** it needs `codedeploy:CreateDeployment` and `codedeploy:GetDeployment`. I attach the pipeline role to the pipeline resource and separately scope each action provider role so that a compromised build stage cannot escalate to deploy-stage permissions.' },
  ],
  references: [
    'https://docs.aws.amazon.com/codepipeline/latest/userguide/',
  ],
},
{
  id: 'aws-codebuild',
  title: 'CodeBuild — Managed Build and Test Service',
  icon: 'hammer',
  color: '#14b8a6',
  description: 'Fully managed build service that compiles source code, runs tests, and produces deployable artifacts.',
  introduction: '**AWS CodeBuild** is a fully managed continuous integration service that compiles source code, runs unit tests, and produces deployment-ready artifacts without requiring you to provision or manage build servers. Each build runs in a fresh Docker container from a curated or custom image, ensuring reproducible builds. Build specifications are defined in a `buildspec.yml` file committed alongside the source code.',
  whenToUse: 'Use CodeBuild when you want managed, scalable build infrastructure without the operational burden of maintaining Jenkins agents or self-hosted CI runners. It integrates natively with CodePipeline, CodeCommit, GitHub, and ECR. It is particularly strong for containerized workloads where custom build images with specific toolchains are needed.',
  keyConcepts: [
    { title: 'Buildspec File', description: 'A YAML file named `buildspec.yml` at the repository root that defines `install`, `pre-build`, `build`, and `post-build` phases plus artifact export paths. It can also declare cache paths and environment variable overrides.' },
    { title: 'Build Environments', description: '**CodeBuild** provides AWS-curated managed images for common runtimes or accepts a custom ECR image. The compute type controls vCPU and memory, ranging from small to large to GPU instances.' },
    { title: 'Artifacts and Cache', description: '**Build outputs** are uploaded to S3. Local cache or S3 cache speeds up subsequent builds by persisting dependencies such as `node_modules` or Maven `.m2` between runs.' },
    { title: 'VPC Integration', description: '**VPC-mode builds** run inside a VPC to access private resources such as RDS, ElastiCache, or internal package registries without exposing them to the internet.' },
  ],
  approach: [
    { step: 'Write a minimal buildspec', details: 'Start with only the phases you need. Define install to set runtime versions, build to run your compile and test commands, and artifacts to declare output paths. Add post-build for image pushes or notifications.' },
    { step: 'Choose the right compute type', details: 'Start with BUILD_GENERAL1_SMALL for most workloads and scale up only if build times exceed SLOs. Large compute costs three times more per minute; benchmark before committing.' },
    { step: 'Enable dependency caching', details: 'Configure S3 cache for package manager directories. This can cut build times by 50 to 70 percent for projects with large dependency trees by avoiding re-downloading packages on every run.' },
    { step: 'Secure secrets', details: 'Reference Secrets Manager or Parameter Store ARNs in the environment section of buildspec rather than hardcoding credentials. CodeBuild resolves them at runtime and masks them in logs.' },
  ],
  pitfalls: [
    { title: 'Build timeout defaults', description: 'The default build timeout is 60 minutes. Long-running integration test suites silently time out and report failure without a clear timeout message unless you explicitly set a higher timeout in the project configuration.' },
    { title: 'Cache invalidation surprises', description: 'S3 cache is keyed on the cache configuration prefix. Changing the buildspec cache paths or switching branches does not automatically invalidate stale cache entries, which can cause builds to pick up old artifacts.' },
    { title: 'IAM role scope creep', description: 'The CodeBuild service role accumulates permissions over time as new integrations are added. Audit it regularly; it commonly ends up with overly broad S3 or ECR permissions that violate least-privilege.' },
  ],
  keyQuestions: [
    { q: 'How do you build and push a Docker image in CodeBuild without a privileged mode security risk?', a: 'For most cases I enable privileged mode on the build project, which is required to run the Docker daemon. To reduce risk I use a minimal custom build image, scope the **CodeBuild** service role to only allow `ecr:GetAuthorizationToken` and `ecr:BatchCheckLayerAvailability` on specific repository ARNs, and never mount the Docker socket from the host. For stricter environments I use **kaniko** or **buildkit** in rootless mode inside the CodeBuild container, which does not require privileged mode and produces compatible OCI images.' },
    { q: 'How do you pass environment-specific configuration to a CodeBuild project invoked from CodePipeline?', a: '**CodePipeline** passes environment variable overrides in the action configuration `environmentVariablesOverride` field. I define a set of variables in the **CodeBuild** project with placeholder values and let the pipeline override them per stage, for example setting `DEPLOY_ENV` to `staging` in the test deploy action and `production` in the final deploy action. Sensitive values such as API keys are stored in **Secrets Manager** and referenced by ARN in the buildspec `env` block under `secrets-manager`, so the actual value is never stored in the pipeline definition.' },
    { q: 'What strategies do you use to speed up CodeBuild builds?', a: 'I apply three layers of optimization. First, I enable S3 or local cache for package manager directories, which is the highest-leverage change for dependency-heavy projects. Second, I parallelize independent test suites by splitting them into multiple **CodeBuild** projects that run as parallel actions in the pipeline, reducing wall-clock time. Third, I tune the compute type to match actual resource usage by profiling CPU and memory during the build phase; over-provisioned builds waste money while under-provisioned ones thrash on disk I/O.' },
    { q: 'How does CodeBuild handle VPC access and what are the networking gotchas?', a: 'When VPC mode is enabled **CodeBuild** places the build container ENI inside the specified subnets and security groups. The primary gotcha is that CodeBuild containers in a VPC lose internet access by default, so you must route outbound traffic through a **NAT gateway** or **VPC endpoint**. Builds that pull public dependencies fail silently with timeout errors rather than clear DNS-resolution failures if the NAT is misconfigured. I always validate VPC builds with a simple `curl` test in the install phase before adding complex build logic.' },
  ],
  references: [
    'https://docs.aws.amazon.com/codebuild/latest/userguide/',
  ],
},
{
  id: 'aws-codedeploy',
  title: 'CodeDeploy — Automated Application Deployment',
  icon: 'rocket',
  color: '#14b8a6',
  description: 'Managed deployment service that automates code deployments to EC2, Lambda, and ECS.',
  introduction: '**AWS CodeDeploy** is a fully managed deployment service that automates software deployments to a variety of compute services including Amazon EC2 instances, on-premises servers, AWS Lambda functions, and Amazon ECS services. It handles the complexity of updating applications while minimizing downtime through configurable deployment strategies including in-place, blue/green, canary, and linear traffic shifting.',
  whenToUse: 'Use CodeDeploy when you need a standardized, auditable deployment mechanism across fleets of EC2 instances or for Lambda and ECS traffic shifting. It integrates tightly with CodePipeline, CloudWatch alarms, and Auto Scaling groups. Choose it over manual scripts when deployments need rollback automation, lifecycle hooks, and deployment history tracking.',
  keyConcepts: [
    { title: 'Deployment Groups', description: 'A **deployment group** targets a set of instances identified by EC2 tags or Auto Scaling group name. It defines the deployment configuration, load balancer, and rollback triggers.' },
    { title: 'AppSpec File', description: 'The **AppSpec file** is a YAML or JSON file that maps deployment lifecycle hooks to scripts and specifies file copy locations for EC2 deployments or Lambda traffic shifting configuration for serverless targets.' },
    { title: 'Lifecycle Hooks', description: '**Lifecycle hooks** such as `ApplicationStop`, `BeforeInstall`, `AfterInstall`, `ApplicationStart`, and `ValidateService` allow you to run custom scripts at each deployment phase to drain connections, validate health, or notify monitoring systems.' },
    { title: 'Traffic Shifting Configurations', description: '**Traffic shifting** for Lambda and ECS supports canary (shift a percentage then all at once), linear (shift in equal increments over time), and all-at-once configurations paired with CloudWatch alarm rollback triggers.' },
  ],
  approach: [
    { step: 'Write the AppSpec file', details: 'For EC2 deployments define files to copy, permissions to set, and lifecycle hook scripts. For Lambda deployments specify the current and target function versions and any pre- and post-traffic hooks that run validation tests.' },
    { step: 'Configure rollback triggers', details: 'Attach CloudWatch alarms measuring error rate or latency to the deployment group. CodeDeploy will automatically roll back the deployment if any alarm enters an alarm state during the bake window.' },
    { step: 'Use blue/green for zero-downtime', details: 'For EC2 targets configure blue/green with an Elastic Load Balancer. CodeDeploy provisions a new Auto Scaling group, deploys to it, waits for health checks, then shifts traffic and terminates the original group after a configurable wait.' },
    { step: 'Validate with lifecycle hooks', details: 'Implement a ValidateService hook that runs smoke tests against the newly deployed instance or Lambda version before CodeDeploy considers the deployment successful. Hook failure triggers automatic rollback.' },
  ],
  pitfalls: [
    { title: 'Agent version drift on EC2', description: 'The CodeDeploy agent on EC2 instances can fall behind releases if not updated regularly. Stale agents fail deployments with cryptic errors. Use Systems Manager State Manager to enforce the latest agent version across the fleet.' },
    { title: 'ECS task definition replacement confusion', description: 'For ECS blue/green the AppSpec references a task definition placeholder that CodePipeline substitutes at deploy time. If the imageDetail.json file format from the build stage does not match exactly, the substitution silently fails and the old image is deployed.' },
    { title: 'Bake time misconfiguration', description: 'Setting the bake window too short means alarms have insufficient time to fire before CodeDeploy declares success and terminates the blue environment, making rollback impossible for slowly manifesting errors.' },
  ],
  keyQuestions: [
    { q: 'How does CodeDeploy blue/green deployment work for ECS?', a: 'For ECS, **CodeDeploy** registers a new task set using the replacement task definition alongside the existing task set on the same service. It then shifts load balancer listener rules from the original target group to the replacement target group according to the configured traffic shifting strategy. During the bake window both task sets are live and **CloudWatch** alarms are monitored. If no alarms fire within the window CodeDeploy terminates the original task set; if an alarm fires it reroutes traffic back to the original and deletes the replacement.' },
    { q: 'How do you implement a canary deployment for a Lambda function with CodeDeploy?', a: 'I configure the deployment configuration as `CodeDeployDefault.LambdaCanary10Percent5Minutes`, which shifts 10 percent of traffic to the new Lambda version immediately and waits 5 minutes before shifting the remaining 90 percent. I define a `PreTraffic` hook function that runs integration tests against the new version using the qualifier and returns success or failure. If the hook fails or a **CloudWatch** alarm on error rate fires during the bake window, **CodeDeploy** rolls back by pointing the alias entirely back to the original version.' },
    { q: 'How do you troubleshoot a deployment stuck in the BeforeInstall phase?', a: 'A deployment stuck in `BeforeInstall` almost always indicates a problem with the CodeDeploy agent or the lifecycle hook script. I check the agent log at `/var/log/aws/codedeploy-agent` on the instance and the deployment lifecycle log in the **CodeDeploy** console to find the specific script that timed out. Common causes are the hook script missing execute permissions, the script calling a command not installed on the instance, or the script not exiting cleanly with `exit 0` on success. I also verify the IAM instance profile has `s3:GetObject` permission on the artifact bucket so the agent can download the revision.' },
    { q: 'What is the difference between in-place and blue/green deployments in CodeDeploy?', a: 'In **in-place deployment** **CodeDeploy** stops the application on each instance, installs the new revision, and restarts it. The instances serving traffic are the same ones being updated, so there is an interruption window unless behind a load balancer with connection draining. **Blue/green deployment** provisions a fresh set of instances or task sets, deploys to them while the original continues serving traffic, then shifts traffic after health checks pass. Blue/green costs more temporarily because you run double capacity during the cutover but provides zero-downtime rollback simply by shifting traffic back.' },
  ],
  references: [
    'https://docs.aws.amazon.com/codedeploy/latest/userguide/',
  ],
},
{
  id: 'aws-codecommit',
  title: 'CodeCommit — Managed Git Repository Service',
  icon: 'git-branch',
  color: '#14b8a6',
  description: 'Fully managed, private Git repositories hosted on AWS with IAM-based access control.',
  introduction: '**AWS CodeCommit** is a fully managed source control service that hosts secure Git-based repositories. It eliminates the need to operate your own version control system and integrates natively with other AWS developer tools. Repositories are encrypted at rest and in transit, and access is controlled entirely through IAM policies and Git credentials, making it a natural fit for teams that want to keep source code within the AWS security boundary.',
  whenToUse: 'Use CodeCommit when your organization requires source code to remain within AWS for compliance reasons, when you need tight IAM integration with existing roles without managing a separate identity system, or when building CI/CD pipelines that react to commits with CloudWatch Events. For teams with existing GitHub or GitLab investments, the integration overhead often outweighs the benefits.',
  keyConcepts: [
    { title: 'IAM-based Authentication', description: '**IAM-based authentication** controls access via IAM users with Git credentials, SSH keys, or temporary credentials via the AWS CLI credential helper. There are no repository-specific passwords; everything flows through IAM policies.' },
    { title: 'Repository Triggers', description: '**Repository triggers** invoke Lambda functions or send SNS notifications on push events to specified branches, enabling event-driven workflows like automatic CI pipeline starts or Slack notifications.' },
    { title: 'Pull Requests and Approvals', description: '**CodeCommit** supports pull requests with inline comments, approval rules that require a minimum number of reviewers, and approval rule templates that auto-apply to new pull requests in a repository.' },
    { title: 'Cross-Account Access', description: '**Cross-account access** via IAM roles in other accounts can be granted repository access through resource-based policies, enabling centralized source repositories shared across a multi-account organization.' },
  ],
  approach: [
    { step: 'Configure Git credentials or credential helper', details: 'Generate HTTPS Git credentials in the IAM console for each developer or configure the AWS credential helper in .gitconfig to use temporary STS credentials. The credential helper approach works with IAM roles and is better for CI environments.' },
    { step: 'Set branch protection with approval rules', details: 'Create an approval rule template requiring at least one approver for pull requests targeting the main branch. Attach the template to repositories in the account so protection applies to all new pull requests automatically.' },
    { step: 'Wire CloudWatch Events for automation', details: 'Create an EventBridge rule that matches CodeCommit referenceCreated and referenceUpdated events on target branches and targets a CodePipeline pipeline or Lambda function to start automated workflows on push.' },
    { step: 'Enable notifications', details: 'Configure notification rules on repositories to send pull request and comment events to an SNS topic connected to email or a chat webhook so the team is informed without polling the console.' },
  ],
  pitfalls: [
    { title: 'SSH key rotation is manual', description: 'CodeCommit SSH keys for IAM users do not expire automatically. Establish a rotation policy and audit active SSH keys quarterly using IAM credential reports to avoid credential sprawl.' },
    { title: 'No org-level visibility', description: 'CodeCommit has no multi-account repository browser. Finding repositories across a large organization requires querying each account individually, which makes governance difficult at scale compared to GitHub Enterprise.' },
    { title: 'Large file storage limitations', description: 'CodeCommit does not support Git LFS natively. Large binary files committed to repositories bloat clone times and repository size. Store large assets in S3 and reference them by URL rather than committing them.' },
  ],
  keyQuestions: [
    { q: 'How do you control branch-level access in CodeCommit?', a: '**CodeCommit** does not have native branch-level permissions in the traditional sense, but you can approximate them with IAM condition keys. The `codecommit:GitPush` action supports the `codecommit:References` condition key, which matches against branch reference patterns. I create IAM policies that allow `GitPush` on the repository ARN but deny it when the reference matches `refs/heads/main`, and attach that deny policy to developer roles. An explicit allow for the same reference on a release-manager role then grants push access only to that group.' },
    { q: 'How do you migrate an existing GitHub repository to CodeCommit?', a: 'I use a mirror clone approach: `git clone --mirror` on the GitHub repository to capture all branches, tags, and refs, then add the **CodeCommit** repository as a remote with `git remote add` and run `git push --mirror` to the CodeCommit remote. This preserves full history, all branches, and tags. After migration I update CI/CD pipeline source configurations and developer remote URLs. For ongoing mirroring during a transition period I configure a GitHub Actions workflow or a **CodeBuild** project triggered by GitHub webhooks to push to **CodeCommit** on every merge.' },
    { q: 'What happens to CodeCommit triggers when a repository is deleted?', a: 'Deleting a **CodeCommit** repository permanently removes the repository and all its triggers. The Lambda functions or SNS topics referenced by those triggers are not deleted, but they become orphaned. I always document triggers separately in infrastructure-as-code using **CloudFormation** or **CDK** so that repository recreation recreates the triggers as well. Before deleting I also archive the repository by doing a mirror clone to an S3 bucket so the history is preserved for audit purposes.' },
    { q: 'How do you audit CodeCommit access and activity for compliance?', a: 'All **CodeCommit** API calls including `GetRepository`, `GitPull`, `GitPush`, and `CreatePullRequest` are logged to **CloudTrail** automatically as data events if data event logging is enabled on the trail. I enable S3 data event logging on the artifact bucket and CodeCommit management API logging in CloudTrail, then ship logs to **CloudWatch Logs Insights** for querying. For ongoing compliance I create CloudWatch alarms on unusual push patterns, such as pushes to main without a pull request, by filtering the CloudTrail log group for `GitPush` events on the main branch reference.' },
  ],
  references: [
    'https://docs.aws.amazon.com/codecommit/latest/userguide/',
  ],
},
{
  id: 'aws-cloudformation',
  title: 'CloudFormation — Infrastructure as Code',
  icon: 'layers',
  color: '#14b8a6',
  description: 'AWS native infrastructure-as-code service for provisioning and managing resources declaratively.',
  introduction: '**AWS CloudFormation** lets you model your entire AWS infrastructure as code using JSON or YAML templates. You declare the desired state of resources, and CloudFormation determines the creation order, handles dependencies, and manages the complete lifecycle from provisioning through updates to deletion. Stacks group related resources so you can manage, update, and delete them as a unit with full rollback support.',
  whenToUse: 'Use CloudFormation when you want AWS-native IaC with no external state management, deep AWS service coverage on day-zero of new service launches, and built-in drift detection. It is the right choice for teams standardizing on AWS tooling and for resources that need to integrate with Service Catalog or AWS Organizations for governance. Prefer CDK over raw CloudFormation when templates exceed a few hundred lines to reduce YAML complexity.',
  keyConcepts: [
    { title: 'Stacks and StackSets', description: 'A **stack** is a collection of AWS resources managed as a single unit. **StackSets** extend stacks across multiple accounts and regions from a single CloudFormation template, enabling organization-wide baseline infrastructure.' },
    { title: 'Change Sets', description: 'A **change set** lets you preview the exact set of resource additions, modifications, and deletions before applying an update. This surfaces unintended replacements before they cause downtime.' },
    { title: 'Drift Detection', description: '**Drift detection** identifies when a resource has been modified outside of CloudFormation, helping maintain consistency between the template and deployed infrastructure.' },
    { title: 'Custom Resources', description: '**Lambda-backed custom resources** extend CloudFormation to manage anything not natively supported, from third-party APIs to in-template database initialization, by responding to Create, Update, and Delete lifecycle signals.' },
  ],
  approach: [
    { step: 'Organize resources into logical stacks', details: 'Split infrastructure into stacks by lifecycle and ownership. Networking resources that change rarely belong in a foundation stack. Application resources that deploy frequently belong in their own stack. Use stack exports and imports to share values like VPC IDs between stacks.' },
    { step: 'Use parameters and conditions', details: 'Parameterize environment-specific values like instance types and replica counts. Use Conditions to create or configure resources differently across environments without duplicating templates.' },
    { step: 'Review change sets before every update', details: 'Always generate and inspect a change set before executing an update in production. Look specifically for Replacement rows, which indicate the resource will be deleted and recreated, potentially causing data loss or downtime.' },
    { step: 'Enable termination protection', details: 'Enable stack termination protection on production stacks to prevent accidental deletion. Pair with stack policies that deny Replace and Delete actions on stateful resources like RDS instances and S3 buckets.' },
  ],
  pitfalls: [
    { title: 'UPDATE_ROLLBACK_FAILED state', description: 'A stack can enter UPDATE_ROLLBACK_FAILED when the rollback itself fails due to manual resource changes. Escaping requires using the ContinueUpdateRollback API with resources to skip, which can leave the stack in an inconsistent state.' },
    { title: 'Resource limit in a single stack', description: 'CloudFormation has a hard limit of 500 resources per stack. Large applications that approach this limit must be split into nested stacks or separate stacks, which increases cross-stack dependency complexity.' },
    { title: 'Circular dependency errors', description: 'CloudFormation cannot resolve circular dependencies between resources. A common trap is two security groups each referencing each other. The solution is to use a separate security group ingress resource to break the cycle.' },
  ],
  keyQuestions: [
    { q: 'How do you handle stateful resource updates that require replacement in CloudFormation?', a: 'When a property change requires resource replacement, such as changing an RDS engine version or renaming an S3 bucket, I first snapshot or back up the data, then use a blue/green approach at the stack level: add the new resource with the updated configuration alongside the old one, migrate data, switch application references, and then remove the old resource in a subsequent update. For RDS I use the snapshot-and-restore pattern with a **CloudFormation** custom resource or a manual restore step before removing the original instance from the template.' },
    { q: 'What is the difference between nested stacks and stack imports in CloudFormation?', a: '**Nested stacks** are child stacks managed as resources within a parent stack template using `AWS::CloudFormation::Stack`. Updates to the parent can update child stacks, keeping them in sync, but they are tightly coupled to the parent lifecycle. **Stack imports** use the `Fn::ImportValue` function to reference Outputs exported by a completely separate stack. Imported stacks are decoupled in lifecycle; you can update the exporting stack independently, but you cannot delete an exported output while it is being imported elsewhere. I use nested stacks for components that should always deploy together and stack imports for shared infrastructure like VPCs that many application stacks consume.' },
    { q: 'How do you deploy CloudFormation across multiple accounts with StackSets?', a: 'I use **CloudFormation StackSets** with AWS Organizations integration, which allows service-managed StackSets to automatically deploy to all accounts in specified organizational units without manually specifying account IDs. The management account or a delegated administrator account creates the StackSet. I set deployment options including fault tolerance count and region concurrency to control how aggressively changes propagate. For sensitive updates I set maximum concurrent accounts to one and test in a single account first before expanding.' },
    { q: 'How do you manage secrets in CloudFormation templates?', a: 'I never put secret values directly in CloudFormation parameters because they appear in the stack event history and drift detection output in plaintext. Instead I store secrets in **Secrets Manager** or **Parameter Store** before stack creation and reference them in the template using dynamic references in the format `resolve:ssm-secure:parameter-name` or `resolve:secretsmanager:secret-name:SecretString:key`. **CloudFormation** resolves these at deployment time without exposing values in the template, console, or CLI output. For cross-account deployments I ensure the CloudFormation execution role has the necessary Secrets Manager permissions in the target account.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/',
  ],
},
{
  id: 'aws-cdk',
  title: 'CDK — Cloud Development Kit',
  icon: 'code-2',
  color: '#14b8a6',
  description: 'Open-source framework for defining cloud infrastructure using familiar programming languages.',
  introduction: 'The **AWS Cloud Development Kit** is an open-source software development framework that lets you define cloud infrastructure using TypeScript, Python, Java, Go, or .NET. CDK synthesizes CloudFormation templates from your code, giving you the full power of a programming language including loops, conditionals, abstractions, and package distribution while retaining **CloudFormation** as the deployment engine. Constructs are the core abstraction, composable building blocks that range from low-level L1 constructs mirroring CloudFormation resources to high-level L3 constructs that encode opinionated patterns.',
  whenToUse: 'Use CDK when your infrastructure is complex enough that YAML repetition becomes unmanageable, when you want to distribute infrastructure patterns as reusable npm or PyPI packages, or when your team is more comfortable in a general-purpose language than in YAML. CDK is especially powerful for teams building internal platforms because constructs can encode security and compliance defaults that application teams inherit without knowing the details.',
  keyConcepts: [
    { title: 'Constructs', description: '**Constructs** are reusable cloud components that encapsulate AWS resources. L1 constructs map one-to-one to CloudFormation resources. L2 constructs add convenience methods and sensible defaults. L3 constructs implement complete patterns like a load-balanced Fargate service.' },
    { title: 'Stacks and Apps', description: 'A **CDK App** contains one or more **Stacks**. Each Stack synthesizes to a single CloudFormation stack. Environment-specific stacks are instantiated with different parameters in the app entry point.' },
    { title: 'CDK Synth and Deploy', description: '`cdk synth` produces CloudFormation templates in the `cdk.out` directory for inspection. `cdk deploy` synthesizes and then calls CloudFormation to deploy. `cdk diff` shows the CloudFormation change set before deployment.' },
    { title: 'Bootstrapping', description: '**CDK bootstrapping** provisions an S3 bucket for assets and an ECR repository for container images in each account and region before CDK can deploy. The bootstrap stack version must be compatible with the CDK version used.' },
  ],
  approach: [
    { step: 'Start with L2 constructs', details: 'Use L2 constructs for common services like S3 buckets, Lambda functions, and VPCs. They handle encryption, logging, and IAM grants with sensible defaults so you do not need to wire up every property manually.' },
    { step: 'Build constructs for reusable patterns', details: 'When the same infrastructure pattern appears more than twice, extract it into a custom Construct class. This enforces consistency and lets you update defaults in one place.' },
    { step: 'Use aspects for policy enforcement', details: 'CDK Aspects traverse the construct tree after synthesis and can modify or validate resources. Use them to enforce org-wide policies like tagging requirements, encryption at rest, or disabling public S3 bucket access.' },
    { step: 'Inspect synth output before deploying', details: 'Run cdk synth and inspect the generated CloudFormation in cdk.out before deploying to production. Look for unintended resource replacements the same way you would inspect a change set.' },
  ],
  pitfalls: [
    { title: 'Logical ID instability', description: 'CDK generates CloudFormation logical IDs from the construct tree path. Refactoring construct hierarchy or renaming constructs changes logical IDs, causing CloudFormation to delete and recreate resources. Use id overrides sparingly to stabilize IDs for stateful resources.' },
    { title: 'Asset hash churn', description: 'Lambda function code assets are hashed by content. Trivial changes like reformatting a file or changing a comment hash differently and trigger a Lambda update even when the behavior is unchanged. Use bundling options consistently to avoid spurious deployments.' },
    { title: 'Bootstrap version mismatch', description: 'Upgrading CDK without re-bootstrapping target accounts can fail deployments if the new CDK version requires a higher bootstrap stack version. Establish a process to update bootstrap stacks before upgrading CDK library versions in applications.' },
  ],
  keyQuestions: [
    { q: 'How do you share infrastructure values between CDK stacks?', a: 'I use two approaches depending on the coupling requirements. For stacks in the same CDK app I pass construct references directly between stacks as constructor properties; CDK handles the CloudFormation Outputs and cross-stack references automatically. For stacks deployed independently or in different accounts I use **SSM Parameter Store**: the producing stack writes values to SSM in a post-deploy step or as a custom resource, and the consuming stack reads them at synth time using `ssm.StringParameter.valueFromLookup`. The lookup approach bakes the value into the synthesized template, so changes require re-synthesis.' },
    { q: 'How do you handle database migrations in a CDK deployment pipeline?', a: 'I run migrations as a separate step in the deployment pipeline, not inside the CDK stack itself. After the `cdk deploy` action updates the ECS task definition or Lambda function with the new application code, a subsequent pipeline action triggers a **CodeBuild** project or a one-off ECS task that runs the migration command against the database. I gate the production deploy action on the successful completion of the migration step. This separates infrastructure changes from data changes and keeps CloudFormation from timing out waiting for a long-running migration.' },
    { q: 'What is the CDK escape hatch and when should you use it?', a: 'The **escape hatch** allows you to access the underlying L1 CloudFormation resource from an L2 construct and set raw CloudFormation properties that the L2 API does not expose. You access it through the `node.defaultChild` property cast to the `CfnXxx` type and then call `addPropertyOverride` or `addOverride`. I use it sparingly for properties that are new in CloudFormation but not yet surfaced in the CDK L2 construct, or for workarounds to L2 bugs. Heavy use of escape hatches is a signal that a custom construct or an L1 resource would be cleaner.' },
    { q: 'How do you test CDK infrastructure code?', a: 'I use the **CDK assertions module** for unit tests that synthesize the stack in memory and assert on the generated CloudFormation template using `Template.hasResourceProperties` checks. These run in milliseconds and catch regressions in construct logic without deploying anything. For integration testing I use **CDK Integ Tests**, which actually deploy the stack to an AWS account in a sandbox environment, run assertions against live AWS resources, and then clean up. I run unit tests in CI on every pull request and integration tests on a nightly schedule or before production deployments.' },
  ],
  references: [
    'https://docs.aws.amazon.com/cdk/v2/guide/',
  ],
},
{
  id: 'aws-cloud9',
  title: 'Cloud9 — Cloud-Based IDE',
  icon: 'monitor',
  color: '#14b8a6',
  description: 'Cloud-based integrated development environment that runs in the browser on an EC2 instance.',
  introduction: '**AWS Cloud9** is a cloud-based integrated development environment that you access through a web browser. Each environment runs on an EC2 instance or an SSH-connected server, giving you a terminal, code editor, and debugger without local software installation. Cloud9 environments come pre-configured with the AWS CLI, common runtimes, and direct integration with AWS services, making them particularly useful for serverless development, workshops, and onboarding new team members quickly.',
  whenToUse: 'Use Cloud9 for Lambda function development where the direct AWS service integration reduces round-trip testing time, for running workshops and training where participants need a uniform environment without local setup friction, and for pair programming sessions where two developers share a single environment. It is less suitable as a primary daily IDE for large projects due to latency and limited extension ecosystem compared to VS Code.',
  keyConcepts: [
    { title: 'EC2-Backed Environments', description: '**Cloud9** provisions an EC2 instance to host the IDE server. You choose instance type and select automatic hibernate to stop the instance after 30 minutes of inactivity, reducing cost for occasional use.' },
    { title: 'Collaborative Editing', description: '**Collaborative editing** allows multiple users to be invited to the same Cloud9 environment and edit files simultaneously with real-time cursor visibility, similar to Google Docs. Access is controlled through IAM user permissions.' },
    { title: 'Lambda Direct Invocation', description: '**Cloud9** integrates with the Lambda console to run and debug Lambda functions locally or against AWS without deploying each iteration, accelerating the Lambda development loop.' },
    { title: 'Managed Temporary Credentials', description: '**AWS managed temporary credentials** are injected into the terminal automatically, so you can run AWS CLI commands with the permissions of the Cloud9 user without storing long-term credentials on the instance.' },
  ],
  approach: [
    { step: 'Right-size the instance', details: 'For simple scripting and Lambda development t3.small is sufficient. For compiling large codebases or running Docker builds choose t3.medium or larger. The instance can be resized later by stopping the environment and changing the instance type.' },
    { step: 'Use auto-hibernate to control cost', details: 'Enable the 30-minute auto-hibernate setting on environments used intermittently. The instance stops after inactivity and resumes in about 30 seconds when you reconnect, costing nothing while stopped.' },
    { step: 'Restrict environment access via IAM', details: 'Cloud9 environment sharing grants full terminal access to the EC2 instance. Share only with trusted team members and scope the Cloud9 environment IAM role to least-privilege so that even a shared environment cannot access unrelated AWS resources.' },
    { step: 'Store code in CodeCommit or GitHub', details: 'The EC2 instance backing a Cloud9 environment is ephemeral infrastructure. Commit and push code to a remote repository frequently so that environment termination does not lose work.' },
  ],
  pitfalls: [
    { title: 'Disk space exhaustion on default volumes', description: 'The default 10GB EBS volume fills up quickly when installing dependencies or pulling Docker images. Resize the volume proactively by modifying the EBS volume and running resize2fs, or the environment will become unusable with no warning.' },
    { title: 'Managed credential conflicts with instance roles', description: 'If the EC2 instance backing Cloud9 has an IAM instance role attached, it conflicts with Cloud9 managed temporary credentials. The result is inconsistent credential resolution that confuses AWS CLI behavior. Use one approach exclusively.' },
    { title: 'Latency in distant regions', description: 'Cloud9 runs the IDE server in the region where the environment is created. If the developer is geographically distant from that region, keystroke latency makes the editor uncomfortable to use. Create environments in the AWS region closest to the developer.' },
  ],
  keyQuestions: [
    { q: 'How do you secure a shared Cloud9 environment for a team?', a: 'I create one **Cloud9** environment per developer rather than sharing a single environment across the team, because environment sharing grants full shell access to the backing EC2 instance and to all files on it. When sharing is necessary for pair programming I grant read/write access only for the duration of the session and revoke it afterward through the environment member settings. I also scope the EC2 IAM role to only the AWS services the developer needs, so even full shell access cannot reach unrelated production resources.' },
    { q: 'How does Cloud9 manage AWS credentials for the terminal?', a: '**Cloud9** uses **AWS managed temporary credentials** that rotate short-lived STS credentials automatically and inject them into the terminal session. These credentials are tied to the IAM entity that owns the Cloud9 environment and are rotated every five minutes. The credentials appear in `~/.aws/credentials` but should not be copied or persisted because they expire. If you need the terminal to assume a different role you use `aws sts assume-role` and set the resulting credentials in shell environment variables for the duration of that session.' },
    { q: 'What are the alternatives to Cloud9 for cloud-based development on AWS?', a: 'The primary alternative is **AWS CodeCatalyst Dev Environments**, which provides VS Code-compatible browser-based development environments with better extension support and tighter integration with CodeCatalyst project workflows. For teams committed to VS Code, the **Remote SSH extension** connecting to an EC2 instance offers the full local VS Code experience with cloud compute. **GitHub Codespaces** is another strong option for teams whose source is on GitHub. **Cloud9** remains useful specifically for its Lambda direct invocation integration and for workshops where participants need a zero-setup environment in a specific AWS account.' },
    { q: 'How do you extend the disk size of a Cloud9 environment?', a: 'I run the AWS-provided resize script which first modifies the EBS volume size using the EC2 `ModifyVolume` API and then calls `growpart` and `resize2fs` to expand the filesystem to use the new space without stopping the instance. The script is available in the Cloud9 documentation and can be run directly from the Cloud9 terminal. After resizing I verify with `df -h` that the root filesystem reflects the new size. I typically set environments to 20GB at creation for developer use to avoid mid-session interruptions.' },
  ],
  references: [
    'https://docs.aws.amazon.com/cloud9/latest/user-guide/',
  ],
},
{
  id: 'aws-migration-hub',
  title: 'Migration Hub — Centralized Migration Tracking',
  icon: 'map',
  color: '#6366f1',
  description: 'Central hub for tracking application migrations across multiple AWS and partner migration tools.',
  introduction: '**AWS Migration Hub** provides a single location to track the progress of application migrations across multiple AWS and partner migration tools. It aggregates status from services like **Application Migration Service**, **Database Migration Service**, and partner tools through the Migration Hub API, giving you a unified view of which servers have been discovered, assessed, and migrated. Migration Hub does not perform migrations itself but serves as the coordination and visibility layer across a migration program.',
  whenToUse: 'Use Migration Hub when you are running a large-scale migration involving dozens or hundreds of servers and need a unified dashboard to report progress to stakeholders. It is most valuable when using multiple migration tools simultaneously, for example Application Migration Service for servers and DMS for databases, because it consolidates status across all tools into a single view. For small migrations involving only a few servers its overhead may not be justified.',
  keyConcepts: [
    { title: 'Application Grouping', description: '**Application grouping** lets you group servers into logical applications so you can track migration progress at the application level rather than individual server level. This maps to how business stakeholders think about migration waves.' },
    { title: 'Migration Home Region', description: 'Migration Hub data is stored in a **single home region** that you select once and cannot change. All migration tool status updates must be routed to this region. Selecting the wrong region early is a common mistake.' },
    { title: 'Discovery Connectors and Agents', description: '**Application Discovery Service** feeds server inventory into Migration Hub via agentless VMware connectors or lightweight agents installed on servers. This discovery data underpins the application grouping and migration tracking.' },
    { title: 'Migration Hub Refactor Spaces', description: '**Refactor Spaces** is an extension of Migration Hub that provides infrastructure for incrementally refactoring monolithic applications to microservices by routing traffic between the legacy monolith and new services during the refactoring process.' },
  ],
  approach: [
    { step: 'Select and lock the home region', details: 'Choose the home region before onboarding any migration tools. All tools must send updates to this region. Once set it cannot be changed without starting the migration tracking over.' },
    { step: 'Run Application Discovery Service', details: 'Deploy VMware agentless connectors or server agents to discover your on-premises inventory. Discovery data populates Migration Hub with server profiles, dependency maps, and resource utilization that inform migration grouping and wave planning.' },
    { step: 'Group servers into applications', details: 'Create application entries in Migration Hub and associate discovered servers with each application. This is the unit of progress tracking and helps identify when all servers in an application tier have been migrated.' },
    { step: 'Connect migration tools', details: 'Configure Application Migration Service and DMS to send status updates to Migration Hub. Review the unified dashboard regularly to identify stalled migrations and servers that have not yet been addressed.' },
  ],
  pitfalls: [
    { title: 'Home region lock-in', description: 'The Migration Hub home region is permanent. Choosing a region far from your migration team or target infrastructure means all API calls for status updates and queries are cross-region, adding latency and potential cost.' },
    { title: 'Manual association required', description: 'Migration Hub does not automatically associate discovered servers with applications. The association step requires manual or scripted effort, and unapplied servers show as untracked, making progress reports misleading until the mapping is complete.' },
    { title: 'Limited native analytics', description: 'Migration Hub provides status tracking but limited analytics or reporting. For detailed migration reporting and stakeholder dashboards you typically need to export data to QuickSight or a third-party tool.' },
  ],
  keyQuestions: [
    { q: 'How do you use Migration Hub to plan migration waves?', a: 'I start by running **Application Discovery Service** to populate **Migration Hub** with server inventory and network dependency data. I then group servers into applications based on the dependency map, ensuring servers that communicate frequently are in the same wave to minimize cross-environment latency during cut-over. I assign migration strategies to each application using the Migration Hub Strategies feature, labeling each server as rehost, replatform, refactor, retire, or retain. Wave assignments are then derived from application dependencies and business priority, and I use the Migration Hub dashboard to track completion percentage per wave as the program progresses.' },
    { q: 'How does Migration Hub integrate with Application Migration Service?', a: '**Application Migration Service** automatically registers itself with **Migration Hub** when you set your MGN account to send updates to the Migration Hub home region. As servers are added to MGN, pass through replication, and are launched as test or cutover instances, MGN sends status updates to Migration Hub that appear on the application tracking dashboard. This means you can see a server progress from Discovered to Replicating to Ready for Testing to Cutover Complete without manually querying MGN separately. The integration is automatic once the home region is configured in the MGN settings.' },
    { q: 'What is the Migration Hub import feature and when would you use it?', a: 'The **Migration Hub import feature** allows you to upload a CSV file describing your on-premises servers into Migration Hub without deploying discovery agents or connectors. It is useful when you already have a CMDB or an asset inventory export and want to populate Migration Hub quickly for tracking purposes without running a discovery exercise. The CSV maps to server attributes like hostname, IP, CPU, and RAM. The limitation is that imported servers lack network dependency data that agent-based discovery provides, so wave planning based on communication patterns requires supplementing with dependency mapping from another source.' },
    { q: 'How do you track migration progress for compliance reporting?', a: 'I export **Migration Hub** server and application status via the AWS CLI using `list-application-states` and `list-server-summary-states` commands and feed the output into a **QuickSight** dataset refreshed daily. This produces a dashboard showing total servers, servers migrated, servers in progress, and not-started counts by migration wave and business unit. For compliance sign-off I capture point-in-time snapshots of the export at the end of each wave and store them in S3 with version tagging so that auditors can see exactly which servers were migrated in each wave and on what date.' },
  ],
  references: [
    'https://docs.aws.amazon.com/migrationhub/latest/ug/',
  ],
},
{
  id: 'aws-dms',
  title: 'DMS — Database Migration Service',
  icon: 'database',
  color: '#6366f1',
  description: 'Managed service for migrating databases to AWS with minimal downtime using continuous replication.',
  introduction: '**AWS Database Migration Service** helps you migrate databases to AWS quickly and securely. The source database remains fully operational during the migration, minimizing downtime for applications that depend on it. **DMS** supports homogeneous migrations such as Oracle to Oracle on RDS as well as heterogeneous migrations between different database engines such as SQL Server to Aurora PostgreSQL. It also supports ongoing replication after the initial migration for near-zero-downtime cutovers.',
  whenToUse: 'Use DMS when you need to migrate a production database with minimal downtime by running continuous replication until cutover. It is the right choice for heterogeneous database migrations where schema and data type conversion is needed alongside the data move. Combine with the Schema Conversion Tool for heterogeneous migrations to handle stored procedure and view translation. For lift-and-shift of the same database engine a native backup-and-restore approach is simpler and faster for large databases.',
  keyConcepts: [
    { title: 'Replication Instance', description: 'A **replication instance** is a managed EC2 instance that runs the DMS replication engine. It reads from the source endpoint, transforms data, and writes to the target endpoint. Instance size determines throughput capacity.' },
    { title: 'Endpoints', description: '**Source and target endpoints** specify connection details, credentials, and engine-specific settings like SSL mode and extra connection attributes for performance tuning.' },
    { title: 'Replication Tasks', description: 'A **replication task** defines which tables to migrate, the migration type (`full-load`, `cdc`, or `full-load-and-cdc`), and table mapping rules including column filtering and renaming transformations.' },
    { title: 'Change Data Capture (CDC)', description: '**CDC** uses the source database transaction log (`binlog` for MySQL, redo log for Oracle, WAL for PostgreSQL) to capture ongoing changes and apply them to the target, enabling continuous replication after the initial full load.' },
  ],
  approach: [
    { step: 'Assess the source database', details: 'Run the Schema Conversion Tool assessment report for heterogeneous migrations to identify objects that cannot be automatically converted. Plan manual remediation for complex stored procedures before starting the migration.' },
    { step: 'Configure source database for CDC', details: 'Enable binary logging on MySQL, supplemental logging on Oracle, or logical decoding on PostgreSQL. These are prerequisites for CDC and require brief maintenance windows if not already enabled.' },
    { step: 'Run full load and validate', details: 'Start the full load task and monitor the table statistics in the DMS console. After full load completes run the AWS DMS data validation feature to compare row counts and spot-check data between source and target.' },
    { step: 'Enable CDC and monitor lag', details: 'Switch to full load plus CDC mode. Monitor CDCLatencySource and CDCLatencyTarget CloudWatch metrics. High latency indicates the replication instance is undersized or the target has write bottlenecks that need tuning.' },
  ],
  pitfalls: [
    { title: 'LOB column performance', description: 'Large Object columns like BLOB, CLOB, and TEXT require full LOB mode which is significantly slower than limited LOB mode. Analyze actual maximum LOB sizes and use limited LOB mode with the correct size cap to avoid unnecessary performance penalties.' },
    { title: 'Missing primary keys', description: 'DMS cannot efficiently apply CDC updates to tables without primary keys because it cannot identify which row to update. Tables without primary keys fall back to full table scans for each UPDATE, causing severe replication lag on large tables.' },
    { title: 'Schema drift during migration', description: 'DDL changes on the source during active replication can break the CDC stream. DMS supports only a limited set of DDL replication; complex schema changes like column type alterations require pausing the task, applying the DDL on the target manually, and resuming.' },
  ],
  keyQuestions: [
    { q: 'How do you minimize downtime during a database cutover with DMS?', a: 'I use a three-phase approach. First I run a full load task to copy the initial dataset to the target. Second I add **CDC** replication to keep the target in sync with ongoing changes on the source. I monitor `CDCLatencyTarget` until it falls below five seconds, indicating the target is nearly current. For cutover I stop writes to the source application, wait for the CDC lag to reach zero confirming all changes have been applied, then update application connection strings and restart, all in a maintenance window of minutes rather than hours.' },
    { q: 'How do you handle heterogeneous database migrations with DMS?', a: 'For heterogeneous migrations like Oracle to Aurora PostgreSQL I first run the **Schema Conversion Tool** to convert schema objects including tables, indexes, views, and stored procedures. SCT produces a report categorizing objects by conversion complexity and flags items requiring manual work. I address manual conversion items before starting **DMS**. I then create DMS endpoints with the converted target schema in place and run the full load plus CDC task using DMS data type mappings configured to handle Oracle-specific types like `NUMBER` and `DATE` that map differently to PostgreSQL. Post-migration I run application-level regression tests before cutting over traffic.' },
    { q: 'What are the CloudWatch metrics you monitor for a DMS replication task?', a: 'The key metrics are `CDCLatencySource`, which measures the gap between when a change occurs on the source and when DMS reads it from the transaction log, and `CDCLatencyTarget`, which measures the gap between reading the change and applying it to the target. High `CDCLatencySource` usually indicates the replication instance cannot read fast enough and needs a larger instance class. High `CDCLatencyTarget` with low `CDCLatencySource` indicates the target is the bottleneck, often due to index overhead on the target during load. I also watch `FullLoadThroughputRowsSource` and `CPUUtilization` on the replication instance to detect saturation before it causes lag.' },
    { q: 'How do you handle DMS task failures mid-migration?', a: '**DMS** tasks can be resumed from the point of failure for CDC tasks because the task stores the current log sequence number or SCN. For full load tasks that fail mid-table I configure the task to truncate the target table and restart from the beginning rather than resuming, because partial loads leave the target in an inconsistent state. I enable **CloudWatch** logging at the `DEBUG` level during the initial migration to capture detailed error context for failures. For persistent failures related to specific tables I use table mapping rules to exclude the failing table, migrate the rest, and handle that table separately with a native export and import.' },
  ],
  references: [
    'https://docs.aws.amazon.com/dms/latest/userguide/',
  ],
},
{
  id: 'aws-snow-family',
  title: 'Snow Family — Physical Data Transfer Devices',
  icon: 'box',
  color: '#6366f1',
  description: 'Physical edge computing and data transfer devices for moving large data volumes to AWS.',
  introduction: 'The **AWS Snow Family** is a collection of physical devices that AWS ships to your location for large-scale data transfer to AWS and for edge computing in environments without reliable internet connectivity. **Snowcone** is a small ruggedized device for up to 8TB of data. **Snowball Edge** comes in storage-optimized and compute-optimized variants for 80-210TB transfers and on-device compute. **Snowmobile** is a shipping container on a semi-truck for exabyte-scale data center migrations.',
  whenToUse: 'Use the Snow Family when network transfer is impractical due to bandwidth, cost, or time constraints. The general rule is that if transferring your data over your current internet connection would take more than a week, a Snow device is likely faster and cheaper. Use Snowball Edge Compute Optimized for edge computing workloads that need Lambda or EC2 processing before data is ingested, such as industrial IoT data collection or content creation pipelines in remote locations.',
  keyConcepts: [
    { title: 'Snowcone', description: '**Snowcone** is a small portable device weighing about 2kg that holds up to 8TB of usable storage. It can be connected to a server via `DataSync` for online data transfer or shipped back to AWS. It runs EC2-compatible compute for simple edge workloads.' },
    { title: 'Snowball Edge Storage Optimized', description: '**Snowball Edge Storage Optimized** holds 80TB of usable storage with limited compute for simple pre-processing. Designed for bulk data transfer. Multiple devices can be clustered for larger capacity.' },
    { title: 'Snowball Edge Compute Optimized', description: '**Snowball Edge Compute Optimized** holds 42TB of usable storage with 52 vCPUs and optional GPU, running a local EC2 and S3-compatible environment. Used for machine learning inference, video processing, and IoT aggregation at the edge.' },
    { title: 'Snowmobile', description: '**Snowmobile** is a 45-foot shipping container that holds up to 100PB per unit. AWS drives it to your data center and connects it to your network via a high-speed fiber connection. Used only for full data center evacuation.' },
  ],
  approach: [
    { step: 'Size your transfer requirements', details: 'Calculate total data size and your available network bandwidth. If the transfer time at full bandwidth exceeds one week, a Snow device is likely faster and cheaper. Factor in multiple device trips for datasets above a single device capacity.' },
    { step: 'Order and receive the device', details: 'Create a job in the Snow console specifying device type, S3 destination bucket, IAM role, and encryption KMS key. AWS ships the device within a few business days. The device arrives pre-configured and unlocked with a 32-character unlock code.' },
    { step: 'Transfer data using Snowball client', details: 'Install the Snowball Edge client on a local server connected to the device. Use the snowball cp command or the S3-compatible interface to copy data. DataSync agents on the device provide managed transfer with integrity checking.' },
    { step: 'Ship the device back', details: 'Power off the device and affix the prepaid return shipping label. AWS receives the device, imports the data to S3, and sends an import completion notification. The device is then wiped per NIST standards.' },
  ],
  pitfalls: [
    { title: 'Transfer speed depends on local network', description: 'Snowball devices connect at 10GbE or 25GbE. If the local server connecting to the device has a slower network card or is connected through a congested switch, transfer rates will be limited to that bottleneck, not the device maximum.' },
    { title: 'Encryption key management', description: 'Data on Snow devices is encrypted with a KMS key that you specify at job creation. If that key is deleted before AWS ingests the data, the data is permanently unrecoverable. Never delete the KMS key until the import job shows complete in the console.' },
    { title: 'Physical handling and shipping damage', description: 'While Snow devices are ruggedized, they can be damaged in transit. Always photograph the device on arrival to document condition and report damage to AWS Support before attempting to connect it, as a damaged device may not unlock.' },
  ],
  keyQuestions: [
    { q: 'How do you decide between Snowball Edge Storage Optimized and Compute Optimized?', a: 'The decision comes down to whether you need to process data at the source before shipping. **Snowball Edge Storage Optimized** is the right choice when you simply need to move a large dataset to S3 and have no on-device processing requirements; it maximizes usable storage per device. **Snowball Edge Compute Optimized** is appropriate when the source environment needs to run EC2 workloads locally, such as pre-processing sensor data, running inference on video before upload, or operating in a disconnected environment like a ship or remote manufacturing site where the device needs to function as a local data center for weeks before being shipped back.' },
    { q: 'How does data encryption work on Snow devices?', a: 'All data on Snow devices is encrypted at rest using **256-bit encryption** with keys managed by **AWS KMS**. You specify the KMS key at job creation; AWS generates a data encryption key, wraps it with your KMS key, and stores the wrapped key on the device. The device can only be unlocked using the job manifest file and unlock code provided by AWS after you authenticate with your AWS credentials. When AWS receives the device it uses the same KMS key to unwrap the data encryption key and decrypt the data during import to S3. If you use your own customer-managed KMS key the key must be in the same region as the Snow job.' },
    { q: 'What is the workflow for a petabyte-scale migration using multiple Snowball devices?', a: 'For petabyte-scale migrations I plan the job as a series of Snowball rounds. I calculate how many devices are needed to cover the total dataset, accounting for approximately 80TB usable per Storage Optimized device. I run multiple devices concurrently: while one set is in transit to AWS for import, another set is at my site being loaded. I use **AWS OpsHub** or the Snowball client on each device simultaneously with separate copy jobs targeting different portions of the dataset, coordinated through a staging manifest. After each import I validate that S3 object counts and sizes match the source before retiring that portion of the source data.' },
    { q: 'How do you use Snowball Edge for edge computing in a disconnected environment?', a: 'I configure the **Snowball Edge Compute Optimized** device in the Snow console with an Amazon Machine Image for the EC2 instances I want to run on-device and an S3 bucket for local object storage. I pre-install application code on the AMI before the device ships. On-site the device runs the EC2 instances and exposes an S3-compatible endpoint that local equipment can write data to. Lambda functions running on the device can trigger on object creation for real-time processing. When the device eventually connects to the network or is shipped back, **DataSync** or a manual bulk copy job uploads the accumulated data to AWS S3.' },
  ],
  references: [
    'https://docs.aws.amazon.com/snowball/latest/developer-guide/',
  ],
},
{
  id: 'aws-transfer-family',
  title: 'Transfer Family — Managed File Transfer Service',
  icon: 'arrow-right-left',
  color: '#6366f1',
  description: 'Fully managed service for file transfers to and from S3 and EFS using SFTP, FTPS, FTP, and AS2.',
  introduction: '**AWS Transfer Family** provides fully managed support for file transfers over SFTP, FTPS, FTP, and AS2 directly to and from **Amazon S3** and **Amazon EFS**. It enables you to migrate file transfer workflows to AWS without changing client-side applications or scripts. Transfer Family handles server provisioning, protocol termination, user authentication, and data routing, eliminating the need to manage dedicated file transfer servers.',
  whenToUse: 'Use Transfer Family when you are migrating workloads that rely on SFTP or AS2 for business-to-business data exchange and want to eliminate the operational burden of managing dedicated file transfer servers. It is particularly valuable for financial services, healthcare, and retail use cases that exchange files with trading partners using standardized protocols. For internal engineering file exchange, S3 presigned URLs or AWS DataSync are often simpler alternatives.',
  keyConcepts: [
    { title: 'Servers and Endpoints', description: 'A **Transfer Family server** is an endpoint for a specific protocol. It can be publicly accessible, VPC-internal, or VPC-internal with internet access. Each server gets a DNS name for client connections.' },
    { title: 'User Authentication', description: '**Transfer Family** supports three authentication modes: service-managed users with SSH public keys, custom identity providers via Lambda or API Gateway, and **AWS Directory Service** for Active Directory authentication.' },
    { title: 'Logical Directories', description: '**Logical directory mappings** isolate users from seeing the underlying bucket structure and prevent path traversal to unauthorized prefixes by mapping user home directories to S3 bucket prefixes or EFS paths.' },
    { title: 'AS2 for B2B Exchange', description: '**AS2** (Applicability Statement 2) support enables encrypted and signed message exchange with trading partners using EDI standards. Transfer Family acts as the AS2 endpoint, handling partner certificate management and message receipts.' },
  ],
  approach: [
    { step: 'Choose authentication mode', details: 'For user accounts managed entirely within AWS use service-managed users with SSH key authentication. For organizations with an existing Active Directory or LDAP use a custom identity provider Lambda that proxies authentication to the directory. This avoids duplicating user management.' },
    { step: 'Configure logical home directories', details: 'Map each user to a specific S3 prefix using logical directory configuration. Set the scope-down policy or restrict the user IAM role to only allow access to their home prefix. This prevents users from traversing to other prefixes.' },
    { step: 'Set up S3 event notifications', details: 'Configure S3 event notifications or EventBridge rules on the destination bucket to trigger downstream processing when files are uploaded via Transfer Family. This replaces polling mechanisms common in legacy file transfer architectures.' },
    { step: 'Enable server logging', details: 'Enable structured JSON logging to CloudWatch Logs on the Transfer Family server. Logs capture each file transfer including user, filename, bytes transferred, and status, supporting audit requirements and troubleshooting.' },
  ],
  pitfalls: [
    { title: 'S3 eventual consistency edge cases', description: 'While S3 now provides strong read-after-write consistency, Transfer Family uploads that immediately trigger downstream Lambda processing can occasionally race against S3 metadata propagation. Add a brief buffer or implement idempotency in downstream processors.' },
    { title: 'Idle connection timeouts', description: 'Transfer Family terminates SFTP sessions after 30 minutes of inactivity. Long-running batch scripts that open a session, pause, and then resume transfers will be disconnected. Clients must implement reconnect logic.' },
    { title: 'AS2 certificate management', description: 'AS2 requires managing trading partner public certificates and rotating your own certificates before expiry. Expired certificates cause all inbound AS2 messages to fail. Create CloudWatch alarms on certificate expiry dates using a Lambda that audits Transfer Family AS2 partner configurations.' },
  ],
  keyQuestions: [
    { q: 'How do you migrate an existing SFTP server to Transfer Family with zero downtime?', a: 'I use a **DNS cutover strategy**. First I create the **Transfer Family** server and configure all existing users with their SSH public keys, verifying access works by connecting to the Transfer Family server hostname directly. Then I reduce the TTL of the SFTP DNS record to 60 seconds 48 hours before the migration window. During the cutover window I update the DNS record to point to the Transfer Family server endpoint and wait for TTL to expire. Existing sessions on the old server drain naturally; new sessions connect to Transfer Family. I keep the old server running for 24 hours post-cutover to handle any long-lived sessions or clients that cached the old IP.' },
    { q: 'How does Transfer Family authentication work with a custom identity provider?', a: 'A **custom identity provider** is a Lambda function that **Transfer Family** calls with a JSON payload containing the username, password or SSH public key, and server ID. The Lambda validates credentials against an external directory such as LDAP, Active Directory, or a custom database and returns a JSON response specifying the user IAM role, home directory mappings, and optionally a scope-down policy. The Lambda must respond within 10 seconds or the authentication times out. I implement caching of successful authentication results in **ElastiCache** with a short TTL to reduce LDAP load for clients that reconnect frequently.' },
    { q: "How do you enforce user isolation so SFTP users cannot see each other's files?", a: 'I use **logical directory mappings** to map each user to their own S3 prefix and attach a scope-down IAM policy to the user that restricts `s3:GetObject` and `s3:PutObject` to only the ARN matching their prefix. The logical directory configuration uses the `Transfer:HomeBucket` and `Transfer:HomeDirectory` variables so the policy is evaluated with the user\'s specific prefix substituted at runtime. I also deny `s3:ListBucket` on the root bucket level and grant it only on the specific prefix, preventing users from enumerating bucket contents outside their home directory.' },
    { q: 'What is AS2 and when would you choose Transfer Family AS2 over SFTP?', a: '**AS2** is a protocol for encrypted and digitally signed file exchange designed for business-to-business EDI transactions. It uses HTTP or HTTPS as the transport layer with S/MIME encryption and digital signatures wrapping the payload, providing non-repudiation and payload integrity guarantees beyond what SFTP offers. I choose AS2 when trading partners in retail, automotive, or healthcare mandate it for EDI document exchange, typically for purchase orders, invoices, and advance ship notices. **Transfer Family AS2** eliminates the need to run a dedicated AS2 gateway like IBM Sterling or OpenAS2, reducing infrastructure and operational complexity while maintaining AS2 protocol compatibility with all major trading partners.' },
  ],
  references: [
    'https://docs.aws.amazon.com/transfer/latest/userguide/',
  ],
},
{
  id: 'aws-datasync',
  title: 'DataSync — Automated Data Transfer Service',
  icon: 'refresh-cw',
  color: '#6366f1',
  description: 'Online data transfer service that automates moving data between on-premises storage and AWS.',
  introduction: '**AWS DataSync** is an online data movement and discovery service that simplifies and accelerates moving data between on-premises storage and AWS storage services. It handles many tasks automatically including network optimization with parallel multi-part transfers, encryption in transit, data verification, and scheduling. DataSync can transfer data to S3, EFS, FSx for Windows File Server, FSx for Lustre, and FSx for OpenZFS from NFS, SMB, HDFS, object storage, and other sources.',
  whenToUse: 'Use DataSync for ongoing or one-time transfers from on-premises NAS or file servers to AWS storage, for migrating file-based workloads to EFS or FSx, and for replicating data between AWS storage services. It is more appropriate than Snow devices when network bandwidth is sufficient and you need incremental synchronization rather than a one-time bulk transfer. For database migrations use DMS; DataSync is specifically for file and object storage.',
  keyConcepts: [
    { title: 'DataSync Agent', description: 'A **DataSync agent** is a VM deployed in your on-premises environment that connects to DataSync. The agent manages the connection to source NFS or SMB storage and the encrypted transfer channel to AWS. Agents are deployed as OVA for VMware or as an AMI for EC2.' },
    { title: 'Locations', description: '**Locations** are source and destination endpoints defining the storage systems to transfer between. Supported locations include NFS, SMB, HDFS, S3-compatible object storage, S3, EFS, and FSx file systems.' },
    { title: 'Tasks', description: 'A **DataSync task** defines a transfer job between a source and destination location including filtering rules, scheduling, verification behavior, and bandwidth limits. Tasks can run once or on a schedule.' },
    { title: 'Data Verification', description: '**DataSync** verifies data integrity by comparing checksums between source and destination after transfer. Verification can run during transfer or as a separate post-transfer pass and ensures no data corruption occurred in transit.' },
  ],
  approach: [
    { step: 'Deploy and activate the DataSync agent', details: 'Download the OVA or EC2 AMI, deploy it in the environment with network access to the source storage and outbound internet or VPC endpoint access to AWS. Activate the agent in the DataSync console using the activation key presented by the agent web interface.' },
    { step: 'Create source and destination locations', details: 'Define the source NFS mount point or SMB share and the destination S3 bucket, EFS filesystem, or FSx filesystem. Provide IAM roles and credentials with the appropriate permissions for each endpoint.' },
    { step: 'Configure task settings for performance', details: 'Set the number of concurrent files to transfer based on available network bandwidth. Enable bandwidth limiting during business hours if the transfer competes with production traffic. Configure filtering to exclude temporary files, logs, and system metadata that do not need migration.' },
    { step: 'Schedule incremental syncs', details: 'After the initial bulk transfer, schedule hourly or daily tasks to sync only changed files. DataSync uses file metadata comparison to identify changed files efficiently without checksumming the entire dataset on every run.' },
  ],
  pitfalls: [
    { title: 'Agent throughput bottleneck', description: 'A single DataSync agent is limited by the VM\'s CPU and network capacity. For large datasets exceeding a few hundred terabytes deploy multiple agents across multiple source NFS shares in parallel rather than routing all data through one agent.' },
    { title: 'S3 storage class costs during migration', description: 'DataSync defaults to uploading to S3 Standard storage class. For infrequently accessed migration staging data use task configuration to set the target storage class to S3 Standard-IA or Intelligent-Tiering to avoid unnecessary storage costs during a long migration period.' },
    { title: 'Permission mapping for EFS migrations', description: 'When migrating from on-premises NFS to EFS, POSIX user and group IDs must be consistent between source and destination for permissions to be correct. If the source environment uses an LDAP directory for UID/GID mapping, verify that the same mapping is available in the AWS environment before relying on the migrated file permissions.' },
  ],
  keyQuestions: [
    { q: 'How does DataSync differ from S3 Transfer Acceleration for data upload?', a: '**DataSync** is a managed service optimized for bulk file and dataset transfers from on-premises storage, with a deployed agent, multi-part parallel transfers, and built-in integrity verification. **S3 Transfer Acceleration** uses CloudFront edge locations to accelerate HTTP PUT uploads from individual clients by routing data over the AWS backbone network instead of the public internet. DataSync is appropriate for bulk NFS or SMB dataset migration where you need scheduling, filtering, and verification. S3 Transfer Acceleration is appropriate for application-level uploads where individual clients are uploading files directly to S3 and latency to the S3 region is the bottleneck.' },
    { q: 'How do you transfer data between two AWS regions using DataSync?', a: 'For inter-region transfers **DataSync** does not require an on-premises agent. I create S3 or EFS locations in both the source and destination regions directly in the DataSync console and create a task that transfers between them. DataSync handles cross-region routing over the AWS backbone network. For EFS to EFS cross-region replication I create an **EFS Replication** configuration instead, which is specifically optimized for that use case. For S3 to S3 within AWS, **S3 Replication** is often simpler than DataSync, but DataSync adds filtering and scheduling capabilities that replication rules cannot match.' },
    { q: 'How do you validate that a DataSync migration completed without data loss?', a: 'I configure the DataSync task to run verification in `ONLY_FILES_TRANSFERRED` mode first, which checks checksums only on transferred files. After transfer completes I review the task execution report in **CloudWatch Logs** for any files that failed checksum verification and retransfer them. For a final validation before decommissioning the source I run a separate DataSync task in `POINT_IN_TIME_CONSISTENT` mode, which verifies all files in the destination match the source at a consistent point in time. I also compare file counts and total byte sizes between source and destination using CLI tools as a sanity check.' },
    { q: 'How do you handle large datasets that exceed the migration window?', a: 'I use a **multi-phase approach**. In the first phase I run **DataSync** to transfer the bulk of the data during off-peak hours without a deadline. Once the initial load is complete I switch to incremental daily synchronization tasks that keep the destination current while the source remains live. In the final phase, during the planned cutover window, I stop writes to the source, run a final incremental DataSync pass to catch any remaining delta, verify completion with the task execution report, and switch application mounts or DNS to the destination. This pattern limits the cutover window to the time required for the final incremental sync, typically minutes to an hour rather than days.' },
  ],
  references: [
    'https://docs.aws.amazon.com/datasync/latest/userguide/',
  ],
},
{
  id: 'aws-application-migration',
  title: 'Application Migration Service — Lift-and-Shift Rehost',
  icon: 'move',
  color: '#6366f1',
  description: 'Automated lift-and-shift rehosting service that migrates physical, virtual, and cloud servers to AWS.',
  introduction: '**AWS Application Migration Service (MGN)** is the primary recommended service for lift-and-shift migrations of physical servers, virtual machines, and cloud instances to AWS. It uses **continuous block-level replication** to keep a copy of source servers in sync with their AWS counterparts, enabling cutover with minimal downtime. MGN replaced **Server Migration Service** as the preferred AWS rehosting tool and supports Windows and Linux from a wide range of source environments.',
  whenToUse: 'Use Application Migration Service for rehosting workloads that need to move to AWS with minimal application changes. It is the right choice when you want to move first and optimize later, when the application does not have cloud-native alternatives, or when migration velocity is more important than architecture optimization. Use it alongside DMS for the database tier and Migration Hub for portfolio-level tracking.',
  keyConcepts: [
    { title: 'AWS Replication Agent', description: 'A **lightweight replication agent** installed on the source server continuously replicates block-level changes to a staging area in AWS using an encrypted channel. The agent requires outbound HTTPS to AWS endpoints and does not impact source server performance significantly.' },
    { title: 'Staging Area', description: 'MGN maintains a **staging area** of lightweight EC2 instances and EBS volumes in a replication subnet that mirror the source server disk contents in real time. These are internal to MGN and not directly accessible.' },
    { title: 'Launch Templates and Settings', description: '**Launch settings** define the target EC2 instance type, subnet, security groups, and post-launch actions for each source server. These settings are used when launching test instances and cutover instances.' },
    { title: 'Test and Cutover Lifecycle', description: 'The migration lifecycle progresses through Not Ready, Ready for Testing, Test in Progress, Ready for Cutover, Cutover in Progress, and Cutover Complete states. **Test launches** create a temporary EC2 instance from the replicated data without affecting replication.' },
  ],
  approach: [
    { step: 'Install the replication agent', details: 'Download the MGN agent installer and run it on each source server with AWS credentials that have the AWSApplicationMigrationAgentPolicy. The agent registers with MGN and begins initial sync, which may take hours to days depending on disk size and bandwidth.' },
    { step: 'Configure launch settings', details: 'For each server define the target instance type, VPC, subnet, security groups, and any post-launch scripts that need to run after the instance starts, such as updating configuration files with AWS-specific endpoints or running a domain-join script.' },
    { step: 'Perform test launches and validate', details: 'Once replication reaches Ready for Testing, launch a test instance. The test instance is a copy of the source at the current replication point. Validate application functionality, connectivity, and performance. Terminate the test instance when done without interrupting replication.' },
    { step: 'Execute cutover', details: 'During the maintenance window initiate the cutover. MGN launches a final cutover instance from the most current replication snapshot. After validation shut down the source server. MGN will mark the migration as complete and you can finalize by archiving the server in MGN.' },
  ],
  pitfalls: [
    { title: 'Replication lag at cutover', description: 'If the source server has high write throughput, replication lag can accumulate faster than MGN can drain it. Monitor the replication lag metric and plan cutovers during low-write windows so the final cutover instance reflects a recent snapshot.' },
    { title: 'Security group and network misconfiguration', description: 'The replication agent requires outbound HTTPS on port 443 to MGN endpoints and TCP port 1500 to the replication server. Firewalls or security groups that block these connections cause replication to stall. Validate connectivity before installing the agent by testing with curl or telnet.' },
    { title: 'Post-launch script failures', description: 'Post-launch scripts that fail do not fail the cutover visibly; the instance launches and appears healthy while the script error is only logged. Always test post-launch scripts thoroughly during the test launch phase and add explicit exit code checking.' },
  ],
  keyQuestions: [
    { q: 'How does the Application Migration Service replication process work?', a: 'The **MGN agent** on the source server intercepts block-level I/O at the kernel level and streams changed blocks over an encrypted TLS connection to a replication server EC2 instance in a staging subnet in the target AWS account. The replication server applies those blocks to EBS volumes that mirror the source disks. After the initial full replication sync, the agent sends only changed blocks continuously, keeping the staging volumes within seconds of the source. When a test or cutover is triggered **MGN** creates an EC2 instance using the staging volumes, converts the boot volume to AWS-compatible format including installing EC2-compatible drivers, and launches the instance according to the configured launch settings.' },
    { q: 'How do you migrate a Windows Server with Active Directory dependencies using MGN?', a: 'The server itself migrates cleanly with **MGN** replication since block-level replication captures all files and registry state. The challenge is network and identity dependencies. I document all service accounts, DNS names, and domain trust relationships before migration. In the target VPC I establish Active Directory connectivity either by extending the on-premises domain via VPN with a domain controller in AWS or by using **AWS Managed Microsoft AD**. Post-launch scripts handle tasks like re-joining the domain with the correct DNS suffix, updating any hardcoded IP references in configuration files to use DNS names, and reconfiguring services that registered under the old IP address.' },
    { q: 'What is the difference between a test launch and a cutover in MGN?', a: 'A **test launch** creates a temporary EC2 instance from the current replication state without pausing or finalizing replication. The source server continues running and replication continues uninterrupted. The test instance lets you validate that the server boots correctly, applications are functional, and network connectivity works as expected in the target VPC. A **cutover** is the final migration action: **MGN** takes a final snapshot of the source, launches the cutover instance, and marks the migration as finalized. After cutover the source server should be shut down and the MGN job archived. Replication stops after cutover; any changes to the source after that point are not reflected in the AWS instance.' },
    { q: 'How do you handle large-scale migrations with hundreds of servers using MGN?', a: 'I use the **MGN API** and AWS CloudShell or a management Lambda to automate bulk operations. For agent installation I use **Systems Manager Run Command** or Ansible to deploy the MGN agent across all servers in a wave simultaneously rather than logging in to each server manually. I use MGN application and wave groupings to organize servers by dependency tier and migration wave. For launch settings I use the MGN default template with CloudFormation macros to generate per-server settings programmatically. Progress is tracked through **Migration Hub** with application-level groupings. I run test launches for each wave and validate before scheduling the cutover maintenance window for that wave, typically targeting no more than 20 to 30 servers per cutover night.' },
  ],
  references: [
    'https://docs.aws.amazon.com/mgn/latest/ug/',
  ],
},

{
  id: 'iaas-paas-saas',
  title: 'IaaS vs PaaS vs SaaS',
  icon: 'cpu',
  color: '#f97316',
  description: 'Three cloud service models. IaaS = rent servers (you manage OS/runtime/app). PaaS = rent platform (you manage app/data). SaaS = use software (provider manages everything).',
  introduction: '**IaaS**, **PaaS**, and **SaaS** are the three fundamental cloud service delivery models, differing in how much the provider manages vs how much the customer manages.\n\n- **IaaS (Infrastructure as a Service)**: Rent raw compute, storage, and networking. You manage everything from the OS up. Think: renting an empty apartment — you bring your own furniture.\n- **PaaS (Platform as a Service)**: Rent a development platform. You manage only the application and data. Think: renting a fully furnished workspace.\n- **SaaS (Software as a Service)**: Use complete software delivered over the internet. You manage nothing — just use the application. Think: staying in a hotel.',
  whenToUse: 'Use IaaS when you need full OS-level control, custom runtimes, or lift-and-shift migrations. Use PaaS when you want to focus on application development without managing infrastructure — best for teams that want to deploy code without DevOps overhead. Use SaaS when you want to consume software as a service without any deployment concern — best for business tools like email, CRM, or collaboration.',
  keyConcepts: [
    { title: 'IaaS — Infrastructure as a Service', description: '**You manage**: OS, runtime, middleware, application, data. **Provider manages**: hardware, virtualization, networking, storage. Examples: **AWS EC2**, **GCP Compute Engine**, **Azure VMs**, **DigitalOcean Droplets**. Full control but full responsibility — you patch the OS, configure the runtime, and manage scaling.' },
    { title: 'PaaS — Platform as a Service', description: '**You manage**: application code and data. **Provider manages**: OS, runtime, middleware, scaling, patching. Examples: **Heroku**, **AWS Elastic Beanstalk**, **Google App Engine**, **Render**, **Railway**. You deploy code; the platform handles the rest. Ideal for web apps and APIs where infrastructure management is not a differentiator.' },
    { title: 'SaaS — Software as a Service', description: '**You manage**: nothing (just use it). **Provider manages**: everything including the application itself. Examples: **Gmail**, **Slack**, **Salesforce**, **Zoom**, **GitHub**, **ChatGPT**. Consumed over the internet, typically via a browser or API. No deployment, no patching, no scaling decisions.' },
    { title: 'Responsibility Comparison', description: 'Layer ownership by model — Hardware/Virtualization/Networking/Storage: always provider. OS/Runtime/Middleware: IaaS=Customer, PaaS=Provider, SaaS=Provider. Application/Data: IaaS=Customer, PaaS=Customer, SaaS=Provider. The higher the model, the less the customer owns.' },
  ],
  approach: [
    { step: 'Choose the right model for your workload', details: 'Start with the question: how much control do you need vs how much operational overhead can you tolerate? If the team has deep DevOps skills and needs a custom runtime — IaaS. If the team wants to ship code fast without managing servers — PaaS. If you just need business software — SaaS.' },
    { step: 'Understand the shared responsibility model', details: 'Every cloud provider has a shared responsibility model. In IaaS, you are responsible for OS patching, runtime security, and application security. In PaaS, the provider handles OS and runtime security but you still own application code security and data protection. In SaaS, you are responsible only for data governance and access management (who has accounts).' },
    { step: 'Evaluate total cost of ownership', details: 'IaaS appears cheapest per compute hour but has hidden costs: DevOps engineer time, OS licensing, patching windows, capacity planning. PaaS costs more per unit but eliminates most operational overhead. SaaS has the highest per-seat cost but zero infrastructure cost. Calculate the true TCO including engineering time.' },
  ],
  pitfalls: [
    { title: 'Vendor lock-in increases up the stack', description: 'IaaS workloads are most portable (VMs run anywhere). PaaS adds some lock-in (Heroku buildpacks, Elastic Beanstalk config). SaaS creates the deepest lock-in — migrating from Salesforce or Workday is a multi-year project. Factor portability into long-term architecture decisions.' },
    { title: 'Misclassifying serverless', description: 'AWS Lambda and Cloud Functions are sometimes called "serverless" and sometimes PaaS. They sit between PaaS and SaaS — you manage code but not runtime or scaling. In interviews, calling Lambda/Functions "serverless PaaS" or "FaaS (Function as a Service)" is more precise.' },
    { title: 'Assuming PaaS removes all operational burden', description: 'PaaS still requires you to manage application configuration, secrets, environment variables, database connections, and application-level scaling settings. It removes OS-level ops, not all ops.' },
  ],
  keyQuestions: [
    { q: 'What are IaaS, PaaS, and SaaS? Give examples of each.', a: '**IaaS (Infrastructure as a Service)**: Provider manages hardware, virtualization, networking, and storage. You manage OS, runtime, app, and data. Examples: AWS EC2, GCP Compute Engine, Azure VMs.\n\n**PaaS (Platform as a Service)**: Provider manages OS, runtime, and middleware. You manage only the application and data. Examples: Heroku, AWS Elastic Beanstalk, Google App Engine.\n\n**SaaS (Software as a Service)**: Provider manages everything including the application. You just use it. Examples: Gmail, Slack, Salesforce, Zoom.\n\nMemory trick: IaaS = "I rent servers", PaaS = "I build apps", SaaS = "I use software".' },
    { q: 'Which cloud model gives you the most control? Which gives the least?', a: '**IaaS gives the most control** — you manage the OS, runtime, middleware, application, and data. You can install any software, use any runtime, and configure the system at the kernel level. This comes with the most operational responsibility.\n\n**SaaS gives the least control** — you consume the application as-is. You cannot change the runtime, OS, or infrastructure. You only control your own data and user access settings.\n\nPaaS sits in between — you control the application and data but accept the provider\'s choice of OS and runtime.' },
    { q: 'How do you decide between IaaS and PaaS for a new web application?', a: 'Choose **PaaS** (Heroku, App Engine, Elastic Beanstalk) when:\n- The team is small and wants to focus on product code, not infrastructure\n- The application uses standard runtimes (Node.js, Python, Ruby, Java)\n- You want automatic scaling, managed patching, and built-in CI/CD\n- Time-to-market matters more than cost optimization\n\nChoose **IaaS** (EC2, GCP VMs, Azure VMs) when:\n- You need a specific OS version, kernel module, or custom runtime\n- The workload is stateful and requires persistent local storage\n- You need fine-grained control over networking, security groups, or placement\n- Cost at scale justifies the DevOps investment\n- You are doing a lift-and-shift migration of an existing application' },
  ],
  references: [
    'https://docs.aws.amazon.com/whitepapers/latest/aws-overview/types-of-cloud-computing.html',
    'https://cloud.google.com/learn/paas-vs-iaas-vs-saas',
  ],
  visualizations: [
    { title: 'IaaS vs PaaS vs SaaS Responsibility Stack', caption: 'Vertical stack showing which layers (Hardware, Virtualization, Networking, Storage, OS, Runtime, Middleware, App, Data) are managed by Provider vs Customer in each service model.', image: '/diagrams/linkdiags/iaas-paas-saas.png' },
  ],
},

{
  id: 'aws-ecs-vs-eks',
  title: 'AWS ECS vs EKS — When to Use Each',
  icon: 'package',
  color: '#ec4899',
  description: 'ECS is AWS-native and simpler (Tasks/Services/Clusters). EKS is Kubernetes-standard and portable (Pods/Deployments/Services). ECS for AWS-specific teams, EKS for Kubernetes teams or multi-cloud needs.',
  introduction: '**ECS (Elastic Container Service)** and **EKS (Elastic Kubernetes Service)** are both AWS container orchestration platforms, but they take fundamentally different approaches. ECS is an AWS-native service with its own API and abstractions — simpler to operate for AWS-focused teams. EKS runs upstream Kubernetes, giving you the full K8s ecosystem and portability across clouds and on-premises.',
  whenToUse: 'Use ECS when your team is AWS-native, the workload is straightforward, and you want minimal operational complexity. Use EKS when your team has Kubernetes expertise, you need the full Kubernetes feature set (custom CRDs, Helm charts, service mesh), or you need portability across clouds. Some organizations run both — ECS for simple internal services, EKS for complex microservices platforms.',
  keyConcepts: [
    { title: 'ECS Core Concepts', description: '**Task Definition**: blueprint for a container (image, CPU, memory, env vars). **Task**: a running instance of a task definition (like a pod). **Service**: ensures N tasks are always running, integrates with ALB. **Cluster**: logical group of EC2 instances or Fargate capacity. ECS manages task placement and scheduling using its own proprietary scheduler.' },
    { title: 'EKS Core Concepts', description: '**Pod**: smallest deployable unit, one or more containers. **Deployment**: manages pod replicas, rolling updates. **Service**: stable network endpoint for a set of pods. **ConfigMap/Secret**: configuration and secret management. **Ingress**: HTTP routing rules. **Namespace**: logical cluster partitioning. EKS runs upstream Kubernetes — all standard K8s tooling works.' },
    { title: 'Operational Complexity', description: 'ECS has fewer concepts to learn — tasks, services, clusters, and task definitions cover most use cases. EKS requires understanding the full Kubernetes object model (Pods, Deployments, StatefulSets, DaemonSets, Services, Ingress, ConfigMaps, Secrets, RBAC, CRDs) and cluster components (kube-apiserver, etcd, CoreDNS, kube-proxy, CNI).' },
    { title: 'Ecosystem and Tooling', description: 'ECS integrates deeply with AWS services — IAM task roles, CloudWatch, ALB, Service Discovery, Secrets Manager, ECR. EKS has a much larger ecosystem: Helm charts, Operators, ArgoCD, Istio, Prometheus/Grafana stack, Cert-Manager, External-DNS, Karpenter — most of which also work with EKS-specific AWS integrations via the AWS Load Balancer Controller, EBS CSI driver, and EFS CSI driver.' },
  ],
  approach: [
    { step: 'Assess team expertise', details: 'If the team knows Kubernetes, EKS reduces friction. If the team is AWS-native with no K8s experience, ECS has a much shorter learning curve. Training a team on Kubernetes from scratch is a 2-3 month investment before they are productive.' },
    { step: 'Evaluate workload complexity', details: 'Simple stateless web services, batch jobs, and lift-and-shift containers fit well in ECS. Complex microservices platforms with custom operators, service mesh, multi-tenancy, GitOps workflows, and cross-cloud portability requirements are better served by EKS.' },
    { step: 'Consider portability requirements', details: 'If the organization runs workloads on-prem, GCP, or Azure as well as AWS, EKS workloads can be ported to GKE, AKS, or self-managed K8s. ECS workloads are AWS-only — migration requires a full rewrite of deployment configuration.' },
    { step: 'Factor in cost and scaling needs', details: 'ECS control plane is free; you pay only for compute (EC2 or Fargate). EKS control plane costs $0.10/hour per cluster (~$73/month). For small workloads this is significant. For large clusters with many services, the operational savings of EKS (Helm, ArgoCD, Karpenter) can outweigh the control plane cost.' },
  ],
  pitfalls: [
    { title: 'Choosing EKS for simplicity', description: 'EKS is not simpler than ECS — it has more moving parts, more concepts, more failure modes, and more operational overhead. If simplicity is the goal, ECS or even AWS App Runner is the right choice. Only choose EKS when the Kubernetes ecosystem is genuinely needed.' },
    { title: 'Running small workloads on EKS', description: 'A single microservice or a small team does not need Kubernetes. The EKS control plane cost plus the operational complexity of managing node groups, CNI, CoreDNS, and cluster upgrades is not justified for low-scale workloads. ECS or Fargate with App Runner is a better fit.' },
    { title: 'Not using Fargate with ECS', description: 'ECS on EC2 requires managing EC2 instances — patching, scaling, AMI updates. ECS on Fargate removes all node management. For most teams starting with ECS, Fargate is the right default until workload patterns justify EC2 for cost optimization.' },
  ],
  keyQuestions: [
    { q: 'When would you choose ECS over EKS?', a: 'Choose **ECS** when:\n- The team is AWS-native with no Kubernetes experience\n- The workload is simple (stateless web services, batch jobs, lift-and-shift)\n- You want minimal operational complexity and fewer concepts to learn\n- You are using AWS Fargate for serverless containers — ECS+Fargate is the simplest path\n- You do not need the Kubernetes ecosystem (Helm, Operators, custom CRDs, service mesh)\n\nChoose **EKS** when:\n- The team already uses Kubernetes or has K8s expertise\n- You need portability across AWS, on-premises, GCP, or Azure\n- You need the full K8s ecosystem: Helm, ArgoCD, Istio, Prometheus, custom operators\n- You are building a platform for multiple teams and need multi-tenancy via namespaces and RBAC\n- Workload complexity justifies the operational investment' },
    { q: 'What are the core differences in how ECS and EKS schedule containers?', a: '**ECS** uses its own proprietary scheduler. You define task definitions and services; ECS places tasks on EC2 instances (or Fargate) based on resource availability and placement strategies (spread, binpack, random). ECS integrates directly with ALB via target groups registered by the ECS service scheduler.\n\n**EKS** uses the Kubernetes scheduler (kube-scheduler), which places Pods on nodes based on resource requests, node affinity/anti-affinity rules, taints/tolerations, and topology spread constraints. EKS integrates with ALB via the AWS Load Balancer Controller, which watches Ingress and Service objects and creates ALB/NLB resources accordingly.\n\nKey operational difference: ECS services are managed via AWS Console/CLI/CloudFormation. EKS workloads are managed via kubectl/Helm/ArgoCD with Kubernetes manifests.' },
    { q: 'How do you handle secrets in ECS vs EKS?', a: '**ECS**: Inject secrets using the `secrets` field in task definitions, referencing **AWS Secrets Manager** ARNs or **SSM Parameter Store** paths. ECS pulls the value at task launch and injects it as an environment variable or into a file. The ECS task role must have IAM permission to read the secret. This is the recommended approach — no secrets in task definition JSON.\n\n**EKS**: The native approach is Kubernetes Secrets (base64-encoded, stored in etcd — not truly secure by default). For production, use **AWS Secrets Manager** via the **Secrets Store CSI Driver** with the AWS provider, which mounts secrets as files or syncs them to K8s Secrets. Alternatively, use **External Secrets Operator** to sync Secrets Manager values into K8s Secrets automatically. Always enable **etcd encryption** for the EKS cluster to encrypt K8s Secrets at rest.' },
  ],
  references: [
    'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/',
    'https://docs.aws.amazon.com/eks/latest/userguide/',
    'https://aws.amazon.com/blogs/containers/amazon-ecs-vs-amazon-eks-making-sense-of-aws-container-services/',
  ],
  visualizations: [
    { title: 'ECS vs EKS Architecture', caption: 'Side-by-side: ECS cluster with tasks behind ALB, and EKS cluster with pods behind Ingress/ALB via AWS Load Balancer Controller. Shows component mapping between the two models.', image: '/diagrams/linkdiags/ecs-vs-eks.png' },
  ],
},

{
  id: 'aws-architecture-3tier',
  title: 'AWS 3-Tier Web Application Architecture',
  icon: 'server',
  color: '#f97316',
  description: 'Standard 3-tier web architecture on AWS: Route 53 → ALB → EC2 Auto Scaling Group → RDS + S3 + IAM. High availability, auto scaling, managed database.',
  introduction: 'The **3-tier web application architecture** on AWS is the most common pattern for hosting web applications in the cloud. It separates concerns into three layers — presentation, application, and data — with each tier using AWS managed services for reliability and scalability.\n\nFlow: **User → Route 53 (DNS) → Application Load Balancer → EC2 Auto Scaling Group → Amazon RDS + S3**\n\nThis pattern provides high availability, automatic scaling, managed database operations, and clear security boundaries between tiers.',
  whenToUse: 'Use the 3-tier architecture for standard web applications, REST APIs, and e-commerce platforms that need high availability and automatic scaling. It is the starting point for most AWS web workloads before evolving to microservices or serverless. It is also the canonical pattern for AWS Solutions Architect Associate exam questions.',
  keyConcepts: [
    { title: 'Route 53 — DNS Layer', description: '**Amazon Route 53** resolves the application domain name to the ALB\'s DNS name. It supports latency-based routing, health-check-based failover, and weighted routing for blue/green deployments. For multi-region architectures, Route 53 can route traffic to the nearest healthy region.' },
    { title: 'Application Load Balancer — Presentation Tier', description: '**ALB** distributes incoming HTTP/HTTPS traffic across EC2 instances in the Auto Scaling Group. It performs health checks on targets, removing unhealthy instances from rotation. ALB terminates TLS, can route based on path or host, and integrates with WAF for DDoS and application-layer protection.' },
    { title: 'EC2 Auto Scaling Group — Application Tier', description: '**Auto Scaling Group** maintains a fleet of EC2 instances running the application. It automatically adds instances when CPU/load increases and removes them when demand drops. Instances span multiple Availability Zones for fault tolerance. The ASG registers new instances with the ALB target group automatically.' },
    { title: 'Amazon RDS — Data Tier', description: '**Amazon RDS** provides a managed relational database (MySQL, PostgreSQL, MariaDB, Oracle, SQL Server). It handles automated backups, patching, Multi-AZ failover, and read replicas. EC2 instances connect to RDS via a private subnet — RDS is never exposed to the internet.' },
    { title: 'S3 and IAM', description: '**S3** stores static assets (images, CSS, JavaScript files, user uploads) served directly or via CloudFront CDN. **IAM roles** attached to EC2 instances grant least-privilege access to S3, Secrets Manager, and other AWS services without embedding credentials in the application.' },
  ],
  approach: [
    { step: 'Design the VPC and subnet layout', details: 'Create a VPC with public subnets (ALB) and private subnets (EC2, RDS) across at least two AZs. ALB sits in public subnets with internet-facing configuration. EC2 instances in private subnets use NAT Gateway for outbound internet access (software updates, API calls). RDS in isolated private subnets with no route to the internet.' },
    { step: 'Configure the ALB and target group', details: 'Create an ALB in the public subnets. Create a target group for the EC2 instances with HTTP health check on the application\'s health endpoint. Configure listener rules for HTTP (redirect to HTTPS) and HTTPS (forward to target group). Attach an ACM certificate for TLS termination.' },
    { step: 'Create the Auto Scaling Group', details: 'Define a launch template with the EC2 AMI, instance type, IAM role, security group, and user data script that installs the application on boot. Create the ASG referencing the launch template, private subnets across AZs, and the ALB target group. Set scaling policies based on ALB RequestCountPerTarget or CPU utilization.' },
    { step: 'Set up RDS with Multi-AZ', details: 'Create an RDS instance in a DB subnet group spanning private subnets in multiple AZs. Enable Multi-AZ for automatic failover. Store the DB endpoint and credentials in Secrets Manager. Grant the EC2 IAM role permission to read the secret. Never hardcode database credentials in application code or environment files checked into source control.' },
  ],
  pitfalls: [
    { title: 'Single AZ deployment', description: 'Deploying all EC2 instances or the RDS instance in a single AZ eliminates the high availability benefit. Always spread EC2 across at least two AZs and enable RDS Multi-AZ. An AZ outage (which does occur) will take down a single-AZ architecture completely.' },
    { title: 'Placing RDS in a public subnet', description: 'RDS should always be in a private subnet with no internet gateway route. Placing it in a public subnet exposes the database port to the internet — even with security groups, this violates the principle of defense in depth and is a common security audit finding.' },
    { title: 'Not using an IAM instance role', description: 'Hardcoding AWS credentials in the EC2 instance or application configuration is a critical security risk. Always use an IAM instance profile — the EC2 metadata service provides short-lived credentials automatically. No long-term credentials should ever appear in application code or config files.' },
  ],
  keyQuestions: [
    { q: 'Walk me through a 3-tier AWS web architecture.', a: 'A standard 3-tier AWS architecture works as follows:\n\n1. **DNS**: User\'s browser queries **Route 53** which resolves the domain to the ALB\'s DNS name.\n2. **Load Balancer**: **Application Load Balancer** in public subnets receives the request, terminates TLS, and forwards it to a healthy EC2 instance based on the configured routing rules.\n3. **Application Tier**: **EC2 instances** in an Auto Scaling Group in private subnets process the request. They run the application code and connect to RDS for data and S3 for static assets.\n4. **Data Tier**: **Amazon RDS** (Multi-AZ) stores application data in private subnets. A standby replica in another AZ provides automatic failover.\n5. **Static Assets**: **S3** stores images, CSS, and JS, optionally served via **CloudFront** CDN.\n6. **Security**: EC2 instances use **IAM roles** to access S3 and Secrets Manager. **Security groups** restrict traffic: ALB accepts 443 from the internet; EC2 accepts traffic only from the ALB security group; RDS accepts traffic only from the EC2 security group.' },
    { q: 'How does Auto Scaling know when to add or remove EC2 instances?', a: '**Auto Scaling** uses **scaling policies** triggered by **CloudWatch metrics**:\n\n- **Target Tracking**: maintains a target value for a metric — e.g., keep average CPU at 50%. ASG adds instances when CPU rises above 50% and removes them when it drops below. Simplest and recommended for most use cases.\n- **Step Scaling**: adds or removes a specific number of instances based on metric thresholds — e.g., add 2 instances when CPU > 70%, add 4 when CPU > 90%.\n- **Scheduled Scaling**: pre-scale at known traffic peaks — e.g., add 10 instances every weekday at 8am.\n\nFor web traffic, **ALB RequestCountPerTarget** is often a better metric than CPU — it scales based on actual request load rather than CPU utilization, which varies by instance type.' },
    { q: 'How would you achieve zero-downtime deployments in this 3-tier architecture?', a: 'Two main patterns:\n\n**Rolling Deployment (simpler)**: Update the Auto Scaling Group\'s launch template with the new AMI or user data. Configure the ASG update policy to replace instances in batches — e.g., replace 25% at a time. ASG launches new instances, waits for them to pass ALB health checks, then terminates old ones. The ALB continues routing traffic to healthy instances throughout.\n\n**Blue/Green Deployment (safer)**: Create a second "green" Auto Scaling Group with the new version, registered to a separate target group. Shift traffic gradually via ALB weighted target group rules — e.g., 10% to green, monitor, then 50%, then 100%. On success, decommission the blue ASG. On failure, shift all traffic back to blue instantly.\n\nBlue/green is preferred for major changes because it allows instant rollback by shifting traffic back to the old ASG.' },
    { q: 'How do you secure the 3-tier architecture?', a: '**Network security**:\n- ALB in public subnets; EC2 and RDS in private subnets\n- Security group chain: internet → ALB (443), ALB → EC2 (app port), EC2 → RDS (5432/3306)\n- NACLs as a secondary stateless L4 defense at subnet boundaries\n- WAF attached to ALB for SQL injection, XSS, and rate limiting\n\n**Identity and access**:\n- EC2 IAM role with least-privilege access to S3, Secrets Manager, SQS — no long-term credentials\n- RDS credentials stored in Secrets Manager, rotated automatically\n- S3 bucket policy blocks public access; CloudFront OAC for serving static assets\n\n**Data security**:\n- RDS encryption at rest (KMS), SSL in transit\n- S3 server-side encryption (SSE-S3 or SSE-KMS)\n- CloudTrail for API audit logging, VPC Flow Logs for network audit' },
  ],
  references: [
    'https://docs.aws.amazon.com/whitepapers/latest/web-application-hosting-best-practices/',
    'https://aws.amazon.com/architecture/web-hosting/',
    'https://docs.aws.amazon.com/autoscaling/ec2/userguide/as-scale-based-on-demand.html',
  ],
  visualizations: [
    { title: 'AWS 3-Tier Web Architecture', caption: 'Full architecture diagram: User → Route 53 → ALB (public subnets, 2 AZs) → EC2 Auto Scaling Group (private subnets) → RDS Multi-AZ + S3 + IAM. Shows security group boundaries and traffic flow.', image: '/diagrams/linkdiags/aws-concepts.png' },
  ],
},
];
