/**
 * Centralized company brand colors.
 *
 * These hexes are intentionally literal — they mirror each company's official
 * brand color, so they are NOT design tokens. Import COMPANY_COLORS instead of
 * hardcoding brand hexes in components. Keys are lowercase company names.
 */
export const COMPANY_COLORS: Record<string, string> = {
  google: '#4285F4',
  amazon: '#FF9900',
  meta: '#0668E1',
  apple: '#555555',
  microsoft: '#00A4EF',
  netflix: '#E50914',
};
