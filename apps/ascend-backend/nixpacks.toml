[phases.setup]
nixPkgs = ["nodejs_20", "python3", "python3Packages.pip", "graphviz", "gcc", "go", "rustc", "openjdk17", "typescript", "bash"]

[phases.install]
cmds = ["npm install", "pip install diagrams anthropic"]

[phases.build]
cmds = ["echo 'build complete'"]

[start]
cmd = "node src/index.js"
