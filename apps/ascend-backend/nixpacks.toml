[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "go", "rustc", "openjdk17", "typescript", "bash"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'build complete'"]

[start]
cmd = "node src/index.js"
