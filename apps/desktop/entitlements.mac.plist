<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Apple Events / Screen capture: needed by Electron's
         desktopCapturer + setDisplayMediaRequestHandler so the
         interviewer-audio loopback path works on macOS 14+. Without
         these, getDisplayMedia silently returns a dead stream and the
         user sees no error. -->
    <key>com.apple.security.scripting-targets</key>
    <dict>
        <key>com.apple.systemevents</key>
        <array>
            <string>com.apple.systemevents.screensharing</string>
        </array>
    </dict>
</dict>
</plist>
